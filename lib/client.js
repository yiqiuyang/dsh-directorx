window.__ModuleLoader__.load({ id: "dsh-directorx", factory: (require) => { var module = { exports: {} }; var exports = module.exports;
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name2 in all)
    __defProp(target, name2, { get: all[name2], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
var require_use_sync_external_store_shim_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
    "use strict";
    (function() {
      function is(x3, y2) {
        return x3 === y2 && (0 !== x3 || 1 / x3 === 1 / y2) || x3 !== x3 && y2 !== y2;
      }
      function useSyncExternalStore$2(subscribe, getSnapshot) {
        didWarnOld18Alpha || void 0 === React.startTransition || (didWarnOld18Alpha = true, console.error(
          "You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."
        ));
        var value = getSnapshot();
        if (!didWarnUncachedGetSnapshot) {
          var cachedValue = getSnapshot();
          objectIs(value, cachedValue) || (console.error(
            "The result of getSnapshot should be cached to avoid an infinite loop"
          ), didWarnUncachedGetSnapshot = true);
        }
        cachedValue = useState15({
          inst: { value, getSnapshot }
        });
        var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
        useLayoutEffect2(
          function() {
            inst.value = value;
            inst.getSnapshot = getSnapshot;
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
          },
          [subscribe, value, getSnapshot]
        );
        useEffect14(
          function() {
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            return subscribe(function() {
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            });
          },
          [subscribe]
        );
        useDebugValue2(value);
        return value;
      }
      function checkIfSnapshotChanged(inst) {
        var latestGetSnapshot = inst.getSnapshot;
        inst = inst.value;
        try {
          var nextValue = latestGetSnapshot();
          return !objectIs(inst, nextValue);
        } catch (error) {
          return true;
        }
      }
      function useSyncExternalStore$1(subscribe, getSnapshot) {
        return getSnapshot();
      }
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
      var React = require("react"), objectIs = "function" === typeof Object.is ? Object.is : is, useState15 = React.useState, useEffect14 = React.useEffect, useLayoutEffect2 = React.useLayoutEffect, useDebugValue2 = React.useDebugValue, didWarnOld18Alpha = false, didWarnUncachedGetSnapshot = false, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
      exports.useSyncExternalStore = void 0 !== React.useSyncExternalStore ? React.useSyncExternalStore : shim;
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    })();
  }
});

// node_modules/use-sync-external-store/shim/index.js
var require_shim = __commonJS({
  "node_modules/use-sync-external-store/shim/index.js"(exports, module2) {
    "use strict";
    if (false) {
      module2.exports = null;
    } else {
      module2.exports = require_use_sync_external_store_shim_development();
    }
  }
});

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js
var require_with_selector_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js"(exports) {
    "use strict";
    (function() {
      function is(x3, y2) {
        return x3 === y2 && (0 !== x3 || 1 / x3 === 1 / y2) || x3 !== x3 && y2 !== y2;
      }
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
      var React = require("react"), shim = require_shim(), objectIs = "function" === typeof Object.is ? Object.is : is, useSyncExternalStore3 = shim.useSyncExternalStore, useRef12 = React.useRef, useEffect14 = React.useEffect, useMemo5 = React.useMemo, useDebugValue2 = React.useDebugValue;
      exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
        var instRef = useRef12(null);
        if (null === instRef.current) {
          var inst = { hasValue: false, value: null };
          instRef.current = inst;
        } else inst = instRef.current;
        instRef = useMemo5(
          function() {
            function memoizedSelector(nextSnapshot) {
              if (!hasMemo) {
                hasMemo = true;
                memoizedSnapshot = nextSnapshot;
                nextSnapshot = selector(nextSnapshot);
                if (void 0 !== isEqual && inst.hasValue) {
                  var currentSelection = inst.value;
                  if (isEqual(currentSelection, nextSnapshot))
                    return memoizedSelection = currentSelection;
                }
                return memoizedSelection = nextSnapshot;
              }
              currentSelection = memoizedSelection;
              if (objectIs(memoizedSnapshot, nextSnapshot))
                return currentSelection;
              var nextSelection = selector(nextSnapshot);
              if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
                return memoizedSnapshot = nextSnapshot, currentSelection;
              memoizedSnapshot = nextSnapshot;
              return memoizedSelection = nextSelection;
            }
            var hasMemo = false, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
            return [
              function() {
                return memoizedSelector(getSnapshot());
              },
              null === maybeGetServerSnapshot ? void 0 : function() {
                return memoizedSelector(maybeGetServerSnapshot());
              }
            ];
          },
          [getSnapshot, getServerSnapshot, selector, isEqual]
        );
        var value = useSyncExternalStore3(subscribe, instRef[0], instRef[1]);
        useEffect14(
          function() {
            inst.hasValue = true;
            inst.value = value;
          },
          [value]
        );
        useDebugValue2(value);
        return value;
      };
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    })();
  }
});

// node_modules/use-sync-external-store/shim/with-selector.js
var require_with_selector = __commonJS({
  "node_modules/use-sync-external-store/shim/with-selector.js"(exports, module2) {
    "use strict";
    if (false) {
      module2.exports = null;
    } else {
      module2.exports = require_with_selector_development();
    }
  }
});

// node_modules/@webav/mp4box.js/dist/mp4box.all.js
var require_mp4box_all = __commonJS({
  "node_modules/@webav/mp4box.js/dist/mp4box.all.js"(exports) {
    var Log = /* @__PURE__ */ (function() {
      var start2 = /* @__PURE__ */ new Date();
      var LOG_LEVEL_ERROR = 4;
      var LOG_LEVEL_WARNING = 3;
      var LOG_LEVEL_INFO = 2;
      var LOG_LEVEL_DEBUG = 1;
      var log_level = LOG_LEVEL_ERROR;
      var logObject = {
        setLogLevel: function(level) {
          if (level == this.debug) log_level = LOG_LEVEL_DEBUG;
          else if (level == this.info) log_level = LOG_LEVEL_INFO;
          else if (level == this.warn) log_level = LOG_LEVEL_WARNING;
          else if (level == this.error) log_level = LOG_LEVEL_ERROR;
          else log_level = LOG_LEVEL_ERROR;
        },
        debug: function(module3, msg) {
          if (console.debug === void 0) {
            console.debug = console.log;
          }
          if (LOG_LEVEL_DEBUG >= log_level) {
            console.debug("[" + Log.getDurationString(/* @__PURE__ */ new Date() - start2, 1e3) + "]", "[" + module3 + "]", msg);
          }
        },
        log: function(module3, msg) {
          this.debug(module3.msg);
        },
        info: function(module3, msg) {
          if (LOG_LEVEL_INFO >= log_level) {
            console.info("[" + Log.getDurationString(/* @__PURE__ */ new Date() - start2, 1e3) + "]", "[" + module3 + "]", msg);
          }
        },
        warn: function(module3, msg) {
          if (LOG_LEVEL_WARNING >= log_level) {
            console.warn("[" + Log.getDurationString(/* @__PURE__ */ new Date() - start2, 1e3) + "]", "[" + module3 + "]", msg);
          }
        },
        error: function(module3, msg) {
          if (LOG_LEVEL_ERROR >= log_level) {
            console.error("[" + Log.getDurationString(/* @__PURE__ */ new Date() - start2, 1e3) + "]", "[" + module3 + "]", msg);
          }
        }
      };
      return logObject;
    })();
    Log.getDurationString = function(duration, _timescale) {
      var neg;
      function pad(number, length) {
        var str = "" + number;
        var a = str.split(".");
        while (a[0].length < length) {
          a[0] = "0" + a[0];
        }
        return a.join(".");
      }
      if (duration < 0) {
        neg = true;
        duration = -duration;
      } else {
        neg = false;
      }
      var timescale = _timescale || 1;
      var duration_sec = duration / timescale;
      var hours = Math.floor(duration_sec / 3600);
      duration_sec -= hours * 3600;
      var minutes = Math.floor(duration_sec / 60);
      duration_sec -= minutes * 60;
      var msec = duration_sec * 1e3;
      duration_sec = Math.floor(duration_sec);
      msec -= duration_sec * 1e3;
      msec = Math.floor(msec);
      return (neg ? "-" : "") + hours + ":" + pad(minutes, 2) + ":" + pad(duration_sec, 2) + "." + pad(msec, 3);
    };
    Log.printRanges = function(ranges) {
      var length = ranges.length;
      if (length > 0) {
        var str = "";
        for (var i2 = 0; i2 < length; i2++) {
          if (i2 > 0) str += ",";
          str += "[" + Log.getDurationString(ranges.start(i2)) + "," + Log.getDurationString(ranges.end(i2)) + "]";
        }
        return str;
      } else {
        return "(empty)";
      }
    };
    if (typeof exports !== "undefined") {
      exports.Log = Log;
    }
    var MP4BoxStream = function(arrayBuffer) {
      if (arrayBuffer instanceof ArrayBuffer) {
        this.buffer = arrayBuffer;
        this.dataview = new DataView(arrayBuffer);
      } else {
        throw "Needs an array buffer";
      }
      this.position = 0;
    };
    MP4BoxStream.prototype.getPosition = function() {
      return this.position;
    };
    MP4BoxStream.prototype.getEndPosition = function() {
      return this.buffer.byteLength;
    };
    MP4BoxStream.prototype.getLength = function() {
      return this.buffer.byteLength;
    };
    MP4BoxStream.prototype.seek = function(pos) {
      var npos = Math.max(0, Math.min(this.buffer.byteLength, pos));
      this.position = isNaN(npos) || !isFinite(npos) ? 0 : npos;
      return true;
    };
    MP4BoxStream.prototype.isEos = function() {
      return this.getPosition() >= this.getEndPosition();
    };
    MP4BoxStream.prototype.readAnyInt = function(size, signed) {
      var res = 0;
      if (this.position + size <= this.buffer.byteLength) {
        switch (size) {
          case 1:
            if (signed) {
              res = this.dataview.getInt8(this.position);
            } else {
              res = this.dataview.getUint8(this.position);
            }
            break;
          case 2:
            if (signed) {
              res = this.dataview.getInt16(this.position);
            } else {
              res = this.dataview.getUint16(this.position);
            }
            break;
          case 3:
            if (signed) {
              throw "No method for reading signed 24 bits values";
            } else {
              res = this.dataview.getUint8(this.position) << 16;
              res |= this.dataview.getUint8(this.position + 1) << 8;
              res |= this.dataview.getUint8(this.position + 2);
            }
            break;
          case 4:
            if (signed) {
              res = this.dataview.getInt32(this.position);
            } else {
              res = this.dataview.getUint32(this.position);
            }
            break;
          case 8:
            if (signed) {
              throw "No method for reading signed 64 bits values";
            } else {
              res = this.dataview.getUint32(this.position) << 32;
              res |= this.dataview.getUint32(this.position + 4);
            }
            break;
          default:
            throw "readInt method not implemented for size: " + size;
        }
        this.position += size;
        return res;
      } else {
        throw "Not enough bytes in buffer";
      }
    };
    MP4BoxStream.prototype.readUint8 = function() {
      return this.readAnyInt(1, false);
    };
    MP4BoxStream.prototype.readUint16 = function() {
      return this.readAnyInt(2, false);
    };
    MP4BoxStream.prototype.readUint24 = function() {
      return this.readAnyInt(3, false);
    };
    MP4BoxStream.prototype.readUint32 = function() {
      return this.readAnyInt(4, false);
    };
    MP4BoxStream.prototype.readUint64 = function() {
      return this.readAnyInt(8, false);
    };
    MP4BoxStream.prototype.readString = function(length) {
      if (this.position + length <= this.buffer.byteLength) {
        var s = "";
        for (var i2 = 0; i2 < length; i2++) {
          s += String.fromCharCode(this.readUint8());
        }
        return s;
      } else {
        throw "Not enough bytes in buffer";
      }
    };
    MP4BoxStream.prototype.readCString = function() {
      var arr = [];
      while (true) {
        var b = this.readUint8();
        if (b !== 0) {
          arr.push(b);
        } else {
          break;
        }
      }
      return String.fromCharCode.apply(null, arr);
    };
    MP4BoxStream.prototype.readInt8 = function() {
      return this.readAnyInt(1, true);
    };
    MP4BoxStream.prototype.readInt16 = function() {
      return this.readAnyInt(2, true);
    };
    MP4BoxStream.prototype.readInt32 = function() {
      return this.readAnyInt(4, true);
    };
    MP4BoxStream.prototype.readInt64 = function() {
      return this.readAnyInt(8, false);
    };
    MP4BoxStream.prototype.readUint8Array = function(length) {
      var arr = new Uint8Array(length);
      for (var i2 = 0; i2 < length; i2++) {
        arr[i2] = this.readUint8();
      }
      return arr;
    };
    MP4BoxStream.prototype.readInt16Array = function(length) {
      var arr = new Int16Array(length);
      for (var i2 = 0; i2 < length; i2++) {
        arr[i2] = this.readInt16();
      }
      return arr;
    };
    MP4BoxStream.prototype.readUint16Array = function(length) {
      var arr = new Int16Array(length);
      for (var i2 = 0; i2 < length; i2++) {
        arr[i2] = this.readUint16();
      }
      return arr;
    };
    MP4BoxStream.prototype.readUint32Array = function(length) {
      var arr = new Uint32Array(length);
      for (var i2 = 0; i2 < length; i2++) {
        arr[i2] = this.readUint32();
      }
      return arr;
    };
    MP4BoxStream.prototype.readInt32Array = function(length) {
      var arr = new Int32Array(length);
      for (var i2 = 0; i2 < length; i2++) {
        arr[i2] = this.readInt32();
      }
      return arr;
    };
    if (typeof exports !== "undefined") {
      exports.MP4BoxStream = MP4BoxStream;
    }
    var DataStream = function(arrayBuffer, byteOffset, endianness) {
      this._byteOffset = byteOffset || 0;
      if (arrayBuffer instanceof ArrayBuffer) {
        this.buffer = arrayBuffer;
      } else if (typeof arrayBuffer == "object") {
        this.dataView = arrayBuffer;
        if (byteOffset) {
          this._byteOffset += byteOffset;
        }
      } else {
        this.buffer = new ArrayBuffer(arrayBuffer || 0);
      }
      this.position = 0;
      this.endianness = endianness == null ? DataStream.LITTLE_ENDIAN : endianness;
    };
    DataStream.prototype = {};
    DataStream.prototype.getPosition = function() {
      return this.position;
    };
    DataStream.prototype._realloc = function(extra) {
      if (!this._dynamicSize) {
        return;
      }
      var req = this._byteOffset + this.position + extra;
      var blen = this._buffer.byteLength;
      if (req <= blen) {
        if (req > this._byteLength) {
          this._byteLength = req;
        }
        return;
      }
      if (blen < 1) {
        blen = 1;
      }
      while (req > blen) {
        blen *= 2;
      }
      var buf = new ArrayBuffer(blen);
      var src = new Uint8Array(this._buffer);
      var dst = new Uint8Array(buf, 0, src.length);
      dst.set(src);
      this.buffer = buf;
      this._byteLength = req;
    };
    DataStream.prototype._trimAlloc = function() {
      if (this._byteLength == this._buffer.byteLength) {
        return;
      }
      var buf = new ArrayBuffer(this._byteLength);
      var dst = new Uint8Array(buf);
      var src = new Uint8Array(this._buffer, 0, dst.length);
      dst.set(src);
      this.buffer = buf;
    };
    DataStream.BIG_ENDIAN = false;
    DataStream.LITTLE_ENDIAN = true;
    DataStream.prototype._byteLength = 0;
    Object.defineProperty(
      DataStream.prototype,
      "byteLength",
      { get: function() {
        return this._byteLength - this._byteOffset;
      } }
    );
    Object.defineProperty(
      DataStream.prototype,
      "buffer",
      {
        get: function() {
          this._trimAlloc();
          return this._buffer;
        },
        set: function(v2) {
          this._buffer = v2;
          this._dataView = new DataView(this._buffer, this._byteOffset);
          this._byteLength = this._buffer.byteLength;
        }
      }
    );
    Object.defineProperty(
      DataStream.prototype,
      "byteOffset",
      {
        get: function() {
          return this._byteOffset;
        },
        set: function(v2) {
          this._byteOffset = v2;
          this._dataView = new DataView(this._buffer, this._byteOffset);
          this._byteLength = this._buffer.byteLength;
        }
      }
    );
    Object.defineProperty(
      DataStream.prototype,
      "dataView",
      {
        get: function() {
          return this._dataView;
        },
        set: function(v2) {
          this._byteOffset = v2.byteOffset;
          this._buffer = v2.buffer;
          this._dataView = new DataView(this._buffer, this._byteOffset);
          this._byteLength = this._byteOffset + v2.byteLength;
        }
      }
    );
    DataStream.prototype.seek = function(pos) {
      var npos = Math.max(0, Math.min(this.byteLength, pos));
      this.position = isNaN(npos) || !isFinite(npos) ? 0 : npos;
    };
    DataStream.prototype.isEof = function() {
      return this.position >= this._byteLength;
    };
    DataStream.prototype.mapUint8Array = function(length) {
      this._realloc(length * 1);
      var arr = new Uint8Array(this._buffer, this.byteOffset + this.position, length);
      this.position += length * 1;
      return arr;
    };
    DataStream.prototype.readInt32Array = function(length, e) {
      length = length == null ? this.byteLength - this.position / 4 : length;
      var arr = new Int32Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readInt16Array = function(length, e) {
      length = length == null ? this.byteLength - this.position / 2 : length;
      var arr = new Int16Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readInt8Array = function(length) {
      length = length == null ? this.byteLength - this.position : length;
      var arr = new Int8Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readUint32Array = function(length, e) {
      length = length == null ? this.byteLength - this.position / 4 : length;
      var arr = new Uint32Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readUint16Array = function(length, e) {
      length = length == null ? this.byteLength - this.position / 2 : length;
      var arr = new Uint16Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readUint8Array = function(length) {
      length = length == null ? this.byteLength - this.position : length;
      var arr = new Uint8Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readFloat64Array = function(length, e) {
      length = length == null ? this.byteLength - this.position / 8 : length;
      var arr = new Float64Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readFloat32Array = function(length, e) {
      length = length == null ? this.byteLength - this.position / 4 : length;
      var arr = new Float32Array(length);
      DataStream.memcpy(
        arr.buffer,
        0,
        this.buffer,
        this.byteOffset + this.position,
        length * arr.BYTES_PER_ELEMENT
      );
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += arr.byteLength;
      return arr;
    };
    DataStream.prototype.readInt32 = function(e) {
      var v2 = this._dataView.getInt32(this.position, e == null ? this.endianness : e);
      this.position += 4;
      return v2;
    };
    DataStream.prototype.readInt16 = function(e) {
      var v2 = this._dataView.getInt16(this.position, e == null ? this.endianness : e);
      this.position += 2;
      return v2;
    };
    DataStream.prototype.readInt8 = function() {
      var v2 = this._dataView.getInt8(this.position);
      this.position += 1;
      return v2;
    };
    DataStream.prototype.readUint32 = function(e) {
      var v2 = this._dataView.getUint32(this.position, e == null ? this.endianness : e);
      this.position += 4;
      return v2;
    };
    DataStream.prototype.readUint16 = function(e) {
      var v2 = this._dataView.getUint16(this.position, e == null ? this.endianness : e);
      this.position += 2;
      return v2;
    };
    DataStream.prototype.readUint8 = function() {
      var v2 = this._dataView.getUint8(this.position);
      this.position += 1;
      return v2;
    };
    DataStream.prototype.readFloat32 = function(e) {
      var v2 = this._dataView.getFloat32(this.position, e == null ? this.endianness : e);
      this.position += 4;
      return v2;
    };
    DataStream.prototype.readFloat64 = function(e) {
      var v2 = this._dataView.getFloat64(this.position, e == null ? this.endianness : e);
      this.position += 8;
      return v2;
    };
    DataStream.endianness = new Int8Array(new Int16Array([1]).buffer)[0] > 0;
    DataStream.memcpy = function(dst, dstOffset, src, srcOffset, byteLength) {
      var dstU8 = new Uint8Array(dst, dstOffset, byteLength);
      var srcU8 = new Uint8Array(src, srcOffset, byteLength);
      dstU8.set(srcU8);
    };
    DataStream.arrayToNative = function(array2, arrayIsLittleEndian) {
      if (arrayIsLittleEndian == this.endianness) {
        return array2;
      } else {
        return this.flipArrayEndianness(array2);
      }
    };
    DataStream.nativeToEndian = function(array2, littleEndian) {
      if (this.endianness == littleEndian) {
        return array2;
      } else {
        return this.flipArrayEndianness(array2);
      }
    };
    DataStream.flipArrayEndianness = function(array2) {
      var u8 = new Uint8Array(array2.buffer, array2.byteOffset, array2.byteLength);
      for (var i2 = 0; i2 < array2.byteLength; i2 += array2.BYTES_PER_ELEMENT) {
        for (var j3 = i2 + array2.BYTES_PER_ELEMENT - 1, k2 = i2; j3 > k2; j3--, k2++) {
          var tmp = u8[k2];
          u8[k2] = u8[j3];
          u8[j3] = tmp;
        }
      }
      return array2;
    };
    DataStream.prototype.failurePosition = 0;
    String.fromCharCodeUint8 = function(uint8arr) {
      var arr = [];
      for (var i2 = 0; i2 < uint8arr.length; i2++) {
        arr[i2] = uint8arr[i2];
      }
      return String.fromCharCode.apply(null, arr);
    };
    DataStream.prototype.readString = function(length, encoding) {
      if (encoding == null || encoding == "ASCII") {
        return String.fromCharCodeUint8.apply(null, [this.mapUint8Array(length == null ? this.byteLength - this.position : length)]);
      } else {
        return new TextDecoder(encoding).decode(this.mapUint8Array(length));
      }
    };
    DataStream.prototype.readCString = function(length) {
      var blen = this.byteLength - this.position;
      var u8 = new Uint8Array(this._buffer, this._byteOffset + this.position);
      var len = blen;
      if (length != null) {
        len = Math.min(length, blen);
      }
      for (var i2 = 0; i2 < len && u8[i2] !== 0; i2++) ;
      var s = String.fromCharCodeUint8.apply(null, [this.mapUint8Array(i2)]);
      if (length != null) {
        this.position += len - i2;
      } else if (i2 != blen) {
        this.position += 1;
      }
      return s;
    };
    var MAX_SIZE = Math.pow(2, 32);
    DataStream.prototype.readInt64 = function() {
      return this.readInt32() * MAX_SIZE + this.readUint32();
    };
    DataStream.prototype.readUint64 = function() {
      return this.readUint32() * MAX_SIZE + this.readUint32();
    };
    DataStream.prototype.readInt64 = function() {
      return this.readUint32() * MAX_SIZE + this.readUint32();
    };
    DataStream.prototype.readUint24 = function() {
      return (this.readUint8() << 16) + (this.readUint8() << 8) + this.readUint8();
    };
    if (typeof exports !== "undefined") {
      exports.DataStream = DataStream;
    }
    DataStream.prototype.save = function(filename) {
      var blob = new Blob([this.buffer]);
      if (window.URL && URL.createObjectURL) {
        var url = window.URL.createObjectURL(blob);
        var a = document.createElement("a");
        document.body.appendChild(a);
        a.setAttribute("href", url);
        a.setAttribute("download", filename);
        a.setAttribute("target", "_self");
        a.click();
        window.URL.revokeObjectURL(url);
      } else {
        throw "DataStream.save: Can't create object URL.";
      }
    };
    DataStream.prototype._dynamicSize = true;
    Object.defineProperty(
      DataStream.prototype,
      "dynamicSize",
      {
        get: function() {
          return this._dynamicSize;
        },
        set: function(v2) {
          if (!v2) {
            this._trimAlloc();
          }
          this._dynamicSize = v2;
        }
      }
    );
    DataStream.prototype.shift = function(offset) {
      var buf = new ArrayBuffer(this._byteLength - offset);
      var dst = new Uint8Array(buf);
      var src = new Uint8Array(this._buffer, offset, dst.length);
      dst.set(src);
      this.buffer = buf;
      this.position -= offset;
    };
    DataStream.prototype.writeInt32Array = function(arr, e) {
      this._realloc(arr.length * 4);
      if (arr instanceof Int32Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapInt32Array(arr.length, e);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeInt32(arr[i2], e);
        }
      }
    };
    DataStream.prototype.writeInt16Array = function(arr, e) {
      this._realloc(arr.length * 2);
      if (arr instanceof Int16Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapInt16Array(arr.length, e);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeInt16(arr[i2], e);
        }
      }
    };
    DataStream.prototype.writeInt8Array = function(arr) {
      this._realloc(arr.length * 1);
      if (arr instanceof Int8Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapInt8Array(arr.length);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeInt8(arr[i2]);
        }
      }
    };
    DataStream.prototype.writeUint32Array = function(arr, e) {
      this._realloc(arr.length * 4);
      if (arr instanceof Uint32Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapUint32Array(arr.length, e);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeUint32(arr[i2], e);
        }
      }
    };
    DataStream.prototype.writeUint16Array = function(arr, e) {
      this._realloc(arr.length * 2);
      if (arr instanceof Uint16Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapUint16Array(arr.length, e);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeUint16(arr[i2], e);
        }
      }
    };
    DataStream.prototype.writeUint8Array = function(arr) {
      this._realloc(arr.length * 1);
      if (arr instanceof Uint8Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapUint8Array(arr.length);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeUint8(arr[i2]);
        }
      }
    };
    DataStream.prototype.writeFloat64Array = function(arr, e) {
      this._realloc(arr.length * 8);
      if (arr instanceof Float64Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapFloat64Array(arr.length, e);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeFloat64(arr[i2], e);
        }
      }
    };
    DataStream.prototype.writeFloat32Array = function(arr, e) {
      this._realloc(arr.length * 4);
      if (arr instanceof Float32Array && this.byteOffset + this.position % arr.BYTES_PER_ELEMENT === 0) {
        DataStream.memcpy(
          this._buffer,
          this.byteOffset + this.position,
          arr.buffer,
          0,
          arr.byteLength
        );
        this.mapFloat32Array(arr.length, e);
      } else {
        for (var i2 = 0; i2 < arr.length; i2++) {
          this.writeFloat32(arr[i2], e);
        }
      }
    };
    DataStream.prototype.writeInt32 = function(v2, e) {
      this._realloc(4);
      this._dataView.setInt32(this.position, v2, e == null ? this.endianness : e);
      this.position += 4;
    };
    DataStream.prototype.writeInt16 = function(v2, e) {
      this._realloc(2);
      this._dataView.setInt16(this.position, v2, e == null ? this.endianness : e);
      this.position += 2;
    };
    DataStream.prototype.writeInt8 = function(v2) {
      this._realloc(1);
      this._dataView.setInt8(this.position, v2);
      this.position += 1;
    };
    DataStream.prototype.writeUint32 = function(v2, e) {
      this._realloc(4);
      this._dataView.setUint32(this.position, v2, e == null ? this.endianness : e);
      this.position += 4;
    };
    DataStream.prototype.writeUint16 = function(v2, e) {
      this._realloc(2);
      this._dataView.setUint16(this.position, v2, e == null ? this.endianness : e);
      this.position += 2;
    };
    DataStream.prototype.writeUint8 = function(v2) {
      this._realloc(1);
      this._dataView.setUint8(this.position, v2);
      this.position += 1;
    };
    DataStream.prototype.writeFloat32 = function(v2, e) {
      this._realloc(4);
      this._dataView.setFloat32(this.position, v2, e == null ? this.endianness : e);
      this.position += 4;
    };
    DataStream.prototype.writeFloat64 = function(v2, e) {
      this._realloc(8);
      this._dataView.setFloat64(this.position, v2, e == null ? this.endianness : e);
      this.position += 8;
    };
    DataStream.prototype.writeUCS2String = function(str, endianness, lengthOverride) {
      if (lengthOverride == null) {
        lengthOverride = str.length;
      }
      for (var i2 = 0; i2 < str.length && i2 < lengthOverride; i2++) {
        this.writeUint16(str.charCodeAt(i2), endianness);
      }
      for (; i2 < lengthOverride; i2++) {
        this.writeUint16(0);
      }
    };
    DataStream.prototype.writeString = function(s, encoding, length) {
      var i2 = 0;
      if (encoding == null || encoding == "ASCII") {
        if (length != null) {
          var len = Math.min(s.length, length);
          for (i2 = 0; i2 < len; i2++) {
            this.writeUint8(s.charCodeAt(i2));
          }
          for (; i2 < length; i2++) {
            this.writeUint8(0);
          }
        } else {
          for (i2 = 0; i2 < s.length; i2++) {
            this.writeUint8(s.charCodeAt(i2));
          }
        }
      } else {
        this.writeUint8Array(new TextEncoder(encoding).encode(s.substring(0, length)));
      }
    };
    DataStream.prototype.writeCString = function(s, length) {
      var i2 = 0;
      if (length != null) {
        var len = Math.min(s.length, length);
        for (i2 = 0; i2 < len; i2++) {
          this.writeUint8(s.charCodeAt(i2));
        }
        for (; i2 < length; i2++) {
          this.writeUint8(0);
        }
      } else {
        for (i2 = 0; i2 < s.length; i2++) {
          this.writeUint8(s.charCodeAt(i2));
        }
        this.writeUint8(0);
      }
    };
    DataStream.prototype.writeStruct = function(structDefinition, struct) {
      for (var i2 = 0; i2 < structDefinition.length; i2 += 2) {
        var t = structDefinition[i2 + 1];
        this.writeType(t, struct[structDefinition[i2]], struct);
      }
    };
    DataStream.prototype.writeType = function(t, v2, struct) {
      var tp;
      if (typeof t == "function") {
        return t(this, v2);
      } else if (typeof t == "object" && !(t instanceof Array)) {
        return t.set(this, v2, struct);
      }
      var lengthOverride = null;
      var charset = "ASCII";
      var pos = this.position;
      if (typeof t == "string" && /:/.test(t)) {
        tp = t.split(":");
        t = tp[0];
        lengthOverride = parseInt(tp[1]);
      }
      if (typeof t == "string" && /,/.test(t)) {
        tp = t.split(",");
        t = tp[0];
        charset = parseInt(tp[1]);
      }
      switch (t) {
        case "uint8":
          this.writeUint8(v2);
          break;
        case "int8":
          this.writeInt8(v2);
          break;
        case "uint16":
          this.writeUint16(v2, this.endianness);
          break;
        case "int16":
          this.writeInt16(v2, this.endianness);
          break;
        case "uint32":
          this.writeUint32(v2, this.endianness);
          break;
        case "int32":
          this.writeInt32(v2, this.endianness);
          break;
        case "float32":
          this.writeFloat32(v2, this.endianness);
          break;
        case "float64":
          this.writeFloat64(v2, this.endianness);
          break;
        case "uint16be":
          this.writeUint16(v2, DataStream.BIG_ENDIAN);
          break;
        case "int16be":
          this.writeInt16(v2, DataStream.BIG_ENDIAN);
          break;
        case "uint32be":
          this.writeUint32(v2, DataStream.BIG_ENDIAN);
          break;
        case "int32be":
          this.writeInt32(v2, DataStream.BIG_ENDIAN);
          break;
        case "float32be":
          this.writeFloat32(v2, DataStream.BIG_ENDIAN);
          break;
        case "float64be":
          this.writeFloat64(v2, DataStream.BIG_ENDIAN);
          break;
        case "uint16le":
          this.writeUint16(v2, DataStream.LITTLE_ENDIAN);
          break;
        case "int16le":
          this.writeInt16(v2, DataStream.LITTLE_ENDIAN);
          break;
        case "uint32le":
          this.writeUint32(v2, DataStream.LITTLE_ENDIAN);
          break;
        case "int32le":
          this.writeInt32(v2, DataStream.LITTLE_ENDIAN);
          break;
        case "float32le":
          this.writeFloat32(v2, DataStream.LITTLE_ENDIAN);
          break;
        case "float64le":
          this.writeFloat64(v2, DataStream.LITTLE_ENDIAN);
          break;
        case "cstring":
          this.writeCString(v2, lengthOverride);
          break;
        case "string":
          this.writeString(v2, charset, lengthOverride);
          break;
        case "u16string":
          this.writeUCS2String(v2, this.endianness, lengthOverride);
          break;
        case "u16stringle":
          this.writeUCS2String(v2, DataStream.LITTLE_ENDIAN, lengthOverride);
          break;
        case "u16stringbe":
          this.writeUCS2String(v2, DataStream.BIG_ENDIAN, lengthOverride);
          break;
        default:
          if (t.length == 3) {
            var ta = t[1];
            for (var i2 = 0; i2 < v2.length; i2++) {
              this.writeType(ta, v2[i2]);
            }
            break;
          } else {
            this.writeStruct(t, v2);
            break;
          }
      }
      if (lengthOverride != null) {
        this.position = pos;
        this._realloc(lengthOverride);
        this.position = pos + lengthOverride;
      }
    };
    DataStream.prototype.writeUint64 = function(v2) {
      var h = Math.floor(v2 / MAX_SIZE);
      this.writeUint32(h);
      this.writeUint32(v2 & 4294967295);
    };
    DataStream.prototype.writeUint24 = function(v2) {
      this.writeUint8((v2 & 16711680) >> 16);
      this.writeUint8((v2 & 65280) >> 8);
      this.writeUint8(v2 & 255);
    };
    DataStream.prototype.adjustUint32 = function(position, value) {
      var pos = this.position;
      this.seek(position);
      this.writeUint32(value);
      this.seek(pos);
    };
    DataStream.prototype.mapInt32Array = function(length, e) {
      this._realloc(length * 4);
      var arr = new Int32Array(this._buffer, this.byteOffset + this.position, length);
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += length * 4;
      return arr;
    };
    DataStream.prototype.mapInt16Array = function(length, e) {
      this._realloc(length * 2);
      var arr = new Int16Array(this._buffer, this.byteOffset + this.position, length);
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += length * 2;
      return arr;
    };
    DataStream.prototype.mapInt8Array = function(length) {
      this._realloc(length * 1);
      var arr = new Int8Array(this._buffer, this.byteOffset + this.position, length);
      this.position += length * 1;
      return arr;
    };
    DataStream.prototype.mapUint32Array = function(length, e) {
      this._realloc(length * 4);
      var arr = new Uint32Array(this._buffer, this.byteOffset + this.position, length);
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += length * 4;
      return arr;
    };
    DataStream.prototype.mapUint16Array = function(length, e) {
      this._realloc(length * 2);
      var arr = new Uint16Array(this._buffer, this.byteOffset + this.position, length);
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += length * 2;
      return arr;
    };
    DataStream.prototype.mapFloat64Array = function(length, e) {
      this._realloc(length * 8);
      var arr = new Float64Array(this._buffer, this.byteOffset + this.position, length);
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += length * 8;
      return arr;
    };
    DataStream.prototype.mapFloat32Array = function(length, e) {
      this._realloc(length * 4);
      var arr = new Float32Array(this._buffer, this.byteOffset + this.position, length);
      DataStream.arrayToNative(arr, e == null ? this.endianness : e);
      this.position += length * 4;
      return arr;
    };
    var MultiBufferStream = function(buffer) {
      this.buffers = [];
      this.bufferIndex = -1;
      if (buffer) {
        this.insertBuffer(buffer);
        this.bufferIndex = 0;
      }
    };
    MultiBufferStream.prototype = new DataStream(new ArrayBuffer(), 0, DataStream.BIG_ENDIAN);
    MultiBufferStream.prototype.initialized = function() {
      var firstBuffer;
      if (this.bufferIndex > -1) {
        return true;
      } else if (this.buffers.length > 0) {
        firstBuffer = this.buffers[0];
        if (firstBuffer.fileStart === 0) {
          this.buffer = firstBuffer;
          this.bufferIndex = 0;
          Log.debug("MultiBufferStream", "Stream ready for parsing");
          return true;
        } else {
          Log.warn("MultiBufferStream", "The first buffer should have a fileStart of 0");
          this.logBufferLevel();
          return false;
        }
      } else {
        Log.warn("MultiBufferStream", "No buffer to start parsing from");
        this.logBufferLevel();
        return false;
      }
    };
    ArrayBuffer.concat = function(buffer1, buffer2) {
      Log.debug("ArrayBuffer", "Trying to create a new buffer of size: " + (buffer1.byteLength + buffer2.byteLength));
      var tmp = new Uint8Array(buffer1.byteLength + buffer2.byteLength);
      tmp.set(new Uint8Array(buffer1), 0);
      tmp.set(new Uint8Array(buffer2), buffer1.byteLength);
      return tmp.buffer;
    };
    MultiBufferStream.prototype.reduceBuffer = function(buffer, offset, newLength) {
      var smallB;
      smallB = new Uint8Array(newLength);
      smallB.set(new Uint8Array(buffer, offset, newLength));
      smallB.buffer.fileStart = buffer.fileStart + offset;
      smallB.buffer.usedBytes = 0;
      return smallB.buffer;
    };
    MultiBufferStream.prototype.insertBuffer = function(ab) {
      var to_add = true;
      for (var i2 = 0; i2 < this.buffers.length; i2++) {
        var b = this.buffers[i2];
        if (ab.fileStart <= b.fileStart) {
          if (ab.fileStart === b.fileStart) {
            if (ab.byteLength > b.byteLength) {
              this.buffers.splice(i2, 1);
              i2--;
              continue;
            } else {
              Log.warn("MultiBufferStream", "Buffer (fileStart: " + ab.fileStart + " - Length: " + ab.byteLength + ") already appended, ignoring");
            }
          } else {
            if (ab.fileStart + ab.byteLength <= b.fileStart) {
            } else {
              ab = this.reduceBuffer(ab, 0, b.fileStart - ab.fileStart);
            }
            Log.debug("MultiBufferStream", "Appending new buffer (fileStart: " + ab.fileStart + " - Length: " + ab.byteLength + ")");
            this.buffers.splice(i2, 0, ab);
            if (i2 === 0) {
              this.buffer = ab;
            }
          }
          to_add = false;
          break;
        } else if (ab.fileStart < b.fileStart + b.byteLength) {
          var offset = b.fileStart + b.byteLength - ab.fileStart;
          var newLength = ab.byteLength - offset;
          if (newLength > 0) {
            ab = this.reduceBuffer(ab, offset, newLength);
          } else {
            to_add = false;
            break;
          }
        }
      }
      if (to_add) {
        Log.debug("MultiBufferStream", "Appending new buffer (fileStart: " + ab.fileStart + " - Length: " + ab.byteLength + ")");
        this.buffers.push(ab);
        if (i2 === 0) {
          this.buffer = ab;
        }
      }
    };
    MultiBufferStream.prototype.logBufferLevel = function(info) {
      var i2;
      var buffer;
      var used, total;
      var ranges = [];
      var range;
      var bufferedString = "";
      used = 0;
      total = 0;
      for (i2 = 0; i2 < this.buffers.length; i2++) {
        buffer = this.buffers[i2];
        if (i2 === 0) {
          range = {};
          ranges.push(range);
          range.start = buffer.fileStart;
          range.end = buffer.fileStart + buffer.byteLength;
          bufferedString += "[" + range.start + "-";
        } else if (range.end === buffer.fileStart) {
          range.end = buffer.fileStart + buffer.byteLength;
        } else {
          range = {};
          range.start = buffer.fileStart;
          bufferedString += ranges[ranges.length - 1].end - 1 + "], [" + range.start + "-";
          range.end = buffer.fileStart + buffer.byteLength;
          ranges.push(range);
        }
        used += buffer.usedBytes;
        total += buffer.byteLength;
      }
      if (ranges.length > 0) {
        bufferedString += range.end - 1 + "]";
      }
      var log = info ? Log.info : Log.debug;
      if (this.buffers.length === 0) {
        log("MultiBufferStream", "No more buffer in memory");
      } else {
        log("MultiBufferStream", "" + this.buffers.length + " stored buffer(s) (" + used + "/" + total + " bytes), continuous ranges: " + bufferedString);
      }
    };
    MultiBufferStream.prototype.cleanBuffers = function() {
      var i2;
      var buffer;
      for (i2 = 0; i2 < this.buffers.length; i2++) {
        buffer = this.buffers[i2];
        if (buffer.usedBytes === buffer.byteLength) {
          Log.debug("MultiBufferStream", "Removing buffer #" + i2);
          this.buffers.splice(i2, 1);
          i2--;
        }
      }
    };
    MultiBufferStream.prototype.mergeNextBuffer = function() {
      var next_buffer;
      if (this.bufferIndex + 1 < this.buffers.length) {
        next_buffer = this.buffers[this.bufferIndex + 1];
        if (next_buffer.fileStart === this.buffer.fileStart + this.buffer.byteLength) {
          var oldLength = this.buffer.byteLength;
          var oldUsedBytes = this.buffer.usedBytes;
          var oldFileStart = this.buffer.fileStart;
          this.buffers[this.bufferIndex] = ArrayBuffer.concat(this.buffer, next_buffer);
          this.buffer = this.buffers[this.bufferIndex];
          this.buffers.splice(this.bufferIndex + 1, 1);
          this.buffer.usedBytes = oldUsedBytes;
          this.buffer.fileStart = oldFileStart;
          Log.debug("ISOFile", "Concatenating buffer for box parsing (length: " + oldLength + "->" + this.buffer.byteLength + ")");
          return true;
        } else {
          return false;
        }
      } else {
        return false;
      }
    };
    MultiBufferStream.prototype.findPosition = function(fromStart, filePosition, markAsUsed) {
      var i2;
      var abuffer = null;
      var index2 = -1;
      if (fromStart === true) {
        i2 = 0;
      } else {
        i2 = this.bufferIndex;
      }
      while (i2 < this.buffers.length) {
        abuffer = this.buffers[i2];
        if (abuffer.fileStart <= filePosition) {
          index2 = i2;
          if (markAsUsed) {
            if (abuffer.fileStart + abuffer.byteLength <= filePosition) {
              abuffer.usedBytes = abuffer.byteLength;
            } else {
              abuffer.usedBytes = filePosition - abuffer.fileStart;
            }
            this.logBufferLevel();
          }
        } else {
          break;
        }
        i2++;
      }
      if (index2 !== -1) {
        abuffer = this.buffers[index2];
        if (abuffer.fileStart + abuffer.byteLength >= filePosition) {
          Log.debug("MultiBufferStream", "Found position in existing buffer #" + index2);
          return index2;
        } else {
          return -1;
        }
      } else {
        return -1;
      }
    };
    MultiBufferStream.prototype.findEndContiguousBuf = function(inputindex) {
      var i2;
      var currentBuf;
      var nextBuf;
      var index2 = inputindex !== void 0 ? inputindex : this.bufferIndex;
      currentBuf = this.buffers[index2];
      if (this.buffers.length > index2 + 1) {
        for (i2 = index2 + 1; i2 < this.buffers.length; i2++) {
          nextBuf = this.buffers[i2];
          if (nextBuf.fileStart === currentBuf.fileStart + currentBuf.byteLength) {
            currentBuf = nextBuf;
          } else {
            break;
          }
        }
      }
      return currentBuf.fileStart + currentBuf.byteLength;
    };
    MultiBufferStream.prototype.getEndFilePositionAfter = function(pos) {
      var index2 = this.findPosition(true, pos, false);
      if (index2 !== -1) {
        return this.findEndContiguousBuf(index2);
      } else {
        return pos;
      }
    };
    MultiBufferStream.prototype.addUsedBytes = function(nbBytes) {
      this.buffer.usedBytes += nbBytes;
      this.logBufferLevel();
    };
    MultiBufferStream.prototype.setAllUsedBytes = function() {
      this.buffer.usedBytes = this.buffer.byteLength;
      this.logBufferLevel();
    };
    MultiBufferStream.prototype.seek = function(filePosition, fromStart, markAsUsed) {
      var index2;
      index2 = this.findPosition(fromStart, filePosition, markAsUsed);
      if (index2 !== -1) {
        this.buffer = this.buffers[index2];
        this.bufferIndex = index2;
        this.position = filePosition - this.buffer.fileStart;
        Log.debug("MultiBufferStream", "Repositioning parser at buffer position: " + this.position);
        return true;
      } else {
        Log.debug("MultiBufferStream", "Position " + filePosition + " not found in buffered data");
        return false;
      }
    };
    MultiBufferStream.prototype.getPosition = function() {
      if (this.bufferIndex === -1 || this.buffers[this.bufferIndex] === null) {
        throw "Error accessing position in the MultiBufferStream";
      }
      return this.buffers[this.bufferIndex].fileStart + this.position;
    };
    MultiBufferStream.prototype.getLength = function() {
      return this.byteLength;
    };
    MultiBufferStream.prototype.getEndPosition = function() {
      if (this.bufferIndex === -1 || this.buffers[this.bufferIndex] === null) {
        throw "Error accessing position in the MultiBufferStream";
      }
      return this.buffers[this.bufferIndex].fileStart + this.byteLength;
    };
    if (typeof exports !== "undefined") {
      exports.MultiBufferStream = MultiBufferStream;
    }
    var MPEG4DescriptorParser = function() {
      var ES_DescrTag = 3;
      var DecoderConfigDescrTag = 4;
      var DecSpecificInfoTag = 5;
      var SLConfigDescrTag = 6;
      var descTagToName = [];
      descTagToName[ES_DescrTag] = "ES_Descriptor";
      descTagToName[DecoderConfigDescrTag] = "DecoderConfigDescriptor";
      descTagToName[DecSpecificInfoTag] = "DecoderSpecificInfo";
      descTagToName[SLConfigDescrTag] = "SLConfigDescriptor";
      this.getDescriptorName = function(tag) {
        return descTagToName[tag];
      };
      var that = this;
      var classes = {};
      this.parseOneDescriptor = function(stream) {
        var hdrSize = 0;
        var size = 0;
        var tag;
        var desc;
        var byteRead;
        tag = stream.readUint8();
        hdrSize++;
        byteRead = stream.readUint8();
        hdrSize++;
        while (byteRead & 128) {
          size = (byteRead & 127) << 7;
          byteRead = stream.readUint8();
          hdrSize++;
        }
        size += byteRead & 127;
        Log.debug("MPEG4DescriptorParser", "Found " + (descTagToName[tag] || "Descriptor " + tag) + ", size " + size + " at position " + stream.getPosition());
        if (descTagToName[tag]) {
          desc = new classes[descTagToName[tag]](size);
        } else {
          desc = new classes.Descriptor(size);
        }
        desc.parse(stream);
        return desc;
      };
      classes.Descriptor = function(_tag, _size) {
        this.tag = _tag;
        this.size = _size;
        this.descs = [];
      };
      classes.Descriptor.prototype.parse = function(stream) {
        this.data = stream.readUint8Array(this.size);
      };
      classes.Descriptor.prototype.findDescriptor = function(tag) {
        for (var i2 = 0; i2 < this.descs.length; i2++) {
          if (this.descs[i2].tag == tag) {
            return this.descs[i2];
          }
        }
        return null;
      };
      classes.Descriptor.prototype.parseRemainingDescriptors = function(stream) {
        var start2 = stream.position;
        while (stream.position < start2 + this.size) {
          var desc = that.parseOneDescriptor(stream);
          this.descs.push(desc);
        }
      };
      classes.ES_Descriptor = function(size) {
        classes.Descriptor.call(this, ES_DescrTag, size);
      };
      classes.ES_Descriptor.prototype = new classes.Descriptor();
      classes.ES_Descriptor.prototype.parse = function(stream) {
        this.ES_ID = stream.readUint16();
        this.flags = stream.readUint8();
        this.size -= 3;
        if (this.flags & 128) {
          this.dependsOn_ES_ID = stream.readUint16();
          this.size -= 2;
        } else {
          this.dependsOn_ES_ID = 0;
        }
        if (this.flags & 64) {
          var l2 = stream.readUint8();
          this.URL = stream.readString(l2);
          this.size -= l2 + 1;
        } else {
          this.URL = "";
        }
        if (this.flags & 32) {
          this.OCR_ES_ID = stream.readUint16();
          this.size -= 2;
        } else {
          this.OCR_ES_ID = 0;
        }
        this.parseRemainingDescriptors(stream);
      };
      classes.ES_Descriptor.prototype.getOTI = function(stream) {
        var dcd = this.findDescriptor(DecoderConfigDescrTag);
        if (dcd) {
          return dcd.oti;
        } else {
          return 0;
        }
      };
      classes.ES_Descriptor.prototype.getAudioConfig = function(stream) {
        var dcd = this.findDescriptor(DecoderConfigDescrTag);
        if (!dcd) return null;
        var dsi = dcd.findDescriptor(DecSpecificInfoTag);
        if (dsi && dsi.data) {
          var audioObjectType = (dsi.data[0] & 248) >> 3;
          if (audioObjectType === 31 && dsi.data.length >= 2) {
            audioObjectType = 32 + ((dsi.data[0] & 7) << 3) + ((dsi.data[1] & 224) >> 5);
          }
          return audioObjectType;
        } else {
          return null;
        }
      };
      classes.DecoderConfigDescriptor = function(size) {
        classes.Descriptor.call(this, DecoderConfigDescrTag, size);
      };
      classes.DecoderConfigDescriptor.prototype = new classes.Descriptor();
      classes.DecoderConfigDescriptor.prototype.parse = function(stream) {
        this.oti = stream.readUint8();
        this.streamType = stream.readUint8();
        this.upStream = (this.streamType >> 1 & 1) !== 0;
        this.streamType = this.streamType >>> 2;
        this.bufferSize = stream.readUint24();
        this.maxBitrate = stream.readUint32();
        this.avgBitrate = stream.readUint32();
        this.size -= 13;
        this.parseRemainingDescriptors(stream);
      };
      classes.DecoderSpecificInfo = function(size) {
        classes.Descriptor.call(this, DecSpecificInfoTag, size);
      };
      classes.DecoderSpecificInfo.prototype = new classes.Descriptor();
      classes.SLConfigDescriptor = function(size) {
        classes.Descriptor.call(this, SLConfigDescrTag, size);
      };
      classes.SLConfigDescriptor.prototype = new classes.Descriptor();
      return this;
    };
    if (typeof exports !== "undefined") {
      exports.MPEG4DescriptorParser = MPEG4DescriptorParser;
    }
    var BoxParser = {
      ERR_INVALID_DATA: -1,
      ERR_NOT_ENOUGH_DATA: 0,
      OK: 1,
      // Boxes to be created with default parsing
      BASIC_BOXES: ["mdat", "idat", "free", "skip", "meco", "strk"],
      FULL_BOXES: ["hmhd", "nmhd", "iods", "xml ", "bxml", "ipro", "mere"],
      CONTAINER_BOXES: [
        ["moov", ["trak", "pssh"]],
        ["trak"],
        ["edts"],
        ["mdia"],
        ["minf"],
        ["dinf"],
        ["stbl", ["sgpd", "sbgp"]],
        ["mvex", ["trex"]],
        ["moof", ["traf"]],
        ["traf", ["trun", "sgpd", "sbgp"]],
        ["vttc"],
        ["tref"],
        ["iref"],
        ["mfra", ["tfra"]],
        ["meco"],
        ["hnti"],
        ["hinf"],
        ["strk"],
        ["strd"],
        ["sinf"],
        ["rinf"],
        ["schi"],
        ["trgr"],
        ["udta", ["kind"]],
        ["iprp", ["ipma"]],
        ["ipco"],
        ["grpl"],
        ["j2kH"],
        ["etyp", ["tyco"]]
      ],
      // Boxes effectively created
      boxCodes: [],
      fullBoxCodes: [],
      containerBoxCodes: [],
      sampleEntryCodes: {},
      sampleGroupEntryCodes: [],
      trackGroupTypes: [],
      UUIDBoxes: {},
      UUIDs: [],
      initialize: function() {
        BoxParser.FullBox.prototype = new BoxParser.Box();
        BoxParser.ContainerBox.prototype = new BoxParser.Box();
        BoxParser.SampleEntry.prototype = new BoxParser.Box();
        BoxParser.TrackGroupTypeBox.prototype = new BoxParser.FullBox();
        BoxParser.BASIC_BOXES.forEach(function(type) {
          BoxParser.createBoxCtor(type);
        });
        BoxParser.FULL_BOXES.forEach(function(type) {
          BoxParser.createFullBoxCtor(type);
        });
        BoxParser.CONTAINER_BOXES.forEach(function(types) {
          BoxParser.createContainerBoxCtor(types[0], null, types[1]);
        });
      },
      Box: function(_type, _size, _uuid) {
        this.type = _type;
        this.size = _size;
        this.uuid = _uuid;
      },
      FullBox: function(type, size, uuid) {
        BoxParser.Box.call(this, type, size, uuid);
        this.flags = 0;
        this.version = 0;
      },
      ContainerBox: function(type, size, uuid) {
        BoxParser.Box.call(this, type, size, uuid);
        this.boxes = [];
      },
      SampleEntry: function(type, size, hdr_size, start2) {
        BoxParser.ContainerBox.call(this, type, size);
        this.hdr_size = hdr_size;
        this.start = start2;
      },
      SampleGroupEntry: function(type) {
        this.grouping_type = type;
      },
      TrackGroupTypeBox: function(type, size) {
        BoxParser.FullBox.call(this, type, size);
      },
      createBoxCtor: function(type, parseMethod) {
        BoxParser.boxCodes.push(type);
        BoxParser[type + "Box"] = function(size) {
          BoxParser.Box.call(this, type, size);
        };
        BoxParser[type + "Box"].prototype = new BoxParser.Box();
        if (parseMethod) BoxParser[type + "Box"].prototype.parse = parseMethod;
      },
      createFullBoxCtor: function(type, parseMethod) {
        BoxParser[type + "Box"] = function(size) {
          BoxParser.FullBox.call(this, type, size);
        };
        BoxParser[type + "Box"].prototype = new BoxParser.FullBox();
        BoxParser[type + "Box"].prototype.parse = function(stream) {
          this.parseFullHeader(stream);
          if (parseMethod) {
            parseMethod.call(this, stream);
          }
        };
      },
      addSubBoxArrays: function(subBoxNames) {
        if (subBoxNames) {
          this.subBoxNames = subBoxNames;
          var nbSubBoxes = subBoxNames.length;
          for (var k2 = 0; k2 < nbSubBoxes; k2++) {
            this[subBoxNames[k2] + "s"] = [];
          }
        }
      },
      createContainerBoxCtor: function(type, parseMethod, subBoxNames) {
        BoxParser[type + "Box"] = function(size) {
          BoxParser.ContainerBox.call(this, type, size);
          BoxParser.addSubBoxArrays.call(this, subBoxNames);
        };
        BoxParser[type + "Box"].prototype = new BoxParser.ContainerBox();
        if (parseMethod) BoxParser[type + "Box"].prototype.parse = parseMethod;
      },
      createMediaSampleEntryCtor: function(mediaType, parseMethod, subBoxNames) {
        BoxParser.sampleEntryCodes[mediaType] = [];
        BoxParser[mediaType + "SampleEntry"] = function(type, size) {
          BoxParser.SampleEntry.call(this, type, size);
          BoxParser.addSubBoxArrays.call(this, subBoxNames);
        };
        BoxParser[mediaType + "SampleEntry"].prototype = new BoxParser.SampleEntry();
        if (parseMethod) BoxParser[mediaType + "SampleEntry"].prototype.parse = parseMethod;
      },
      createSampleEntryCtor: function(mediaType, type, parseMethod, subBoxNames) {
        BoxParser.sampleEntryCodes[mediaType].push(type);
        BoxParser[type + "SampleEntry"] = function(size) {
          BoxParser[mediaType + "SampleEntry"].call(this, type, size);
          BoxParser.addSubBoxArrays.call(this, subBoxNames);
        };
        BoxParser[type + "SampleEntry"].prototype = new BoxParser[mediaType + "SampleEntry"]();
        if (parseMethod) BoxParser[type + "SampleEntry"].prototype.parse = parseMethod;
      },
      createEncryptedSampleEntryCtor: function(mediaType, type, parseMethod) {
        BoxParser.createSampleEntryCtor.call(this, mediaType, type, parseMethod, ["sinf"]);
      },
      createSampleGroupCtor: function(type, parseMethod) {
        BoxParser[type + "SampleGroupEntry"] = function(size) {
          BoxParser.SampleGroupEntry.call(this, type, size);
        };
        BoxParser[type + "SampleGroupEntry"].prototype = new BoxParser.SampleGroupEntry();
        if (parseMethod) BoxParser[type + "SampleGroupEntry"].prototype.parse = parseMethod;
      },
      createTrackGroupCtor: function(type, parseMethod) {
        BoxParser[type + "TrackGroupTypeBox"] = function(size) {
          BoxParser.TrackGroupTypeBox.call(this, type, size);
        };
        BoxParser[type + "TrackGroupTypeBox"].prototype = new BoxParser.TrackGroupTypeBox();
        if (parseMethod) BoxParser[type + "TrackGroupTypeBox"].prototype.parse = parseMethod;
      },
      createUUIDBox: function(uuid, isFullBox, isContainerBox, parseMethod) {
        BoxParser.UUIDs.push(uuid);
        BoxParser.UUIDBoxes[uuid] = function(size) {
          if (isFullBox) {
            BoxParser.FullBox.call(this, "uuid", size, uuid);
          } else {
            if (isContainerBox) {
              BoxParser.ContainerBox.call(this, "uuid", size, uuid);
            } else {
              BoxParser.Box.call(this, "uuid", size, uuid);
            }
          }
        };
        BoxParser.UUIDBoxes[uuid].prototype = isFullBox ? new BoxParser.FullBox() : isContainerBox ? new BoxParser.ContainerBox() : new BoxParser.Box();
        if (parseMethod) {
          if (isFullBox) {
            BoxParser.UUIDBoxes[uuid].prototype.parse = function(stream) {
              this.parseFullHeader(stream);
              if (parseMethod) {
                parseMethod.call(this, stream);
              }
            };
          } else {
            BoxParser.UUIDBoxes[uuid].prototype.parse = parseMethod;
          }
        }
      }
    };
    BoxParser.initialize();
    BoxParser.TKHD_FLAG_ENABLED = 1;
    BoxParser.TKHD_FLAG_IN_MOVIE = 2;
    BoxParser.TKHD_FLAG_IN_PREVIEW = 4;
    BoxParser.TFHD_FLAG_BASE_DATA_OFFSET = 1;
    BoxParser.TFHD_FLAG_SAMPLE_DESC = 2;
    BoxParser.TFHD_FLAG_SAMPLE_DUR = 8;
    BoxParser.TFHD_FLAG_SAMPLE_SIZE = 16;
    BoxParser.TFHD_FLAG_SAMPLE_FLAGS = 32;
    BoxParser.TFHD_FLAG_DUR_EMPTY = 65536;
    BoxParser.TFHD_FLAG_DEFAULT_BASE_IS_MOOF = 131072;
    BoxParser.TRUN_FLAGS_DATA_OFFSET = 1;
    BoxParser.TRUN_FLAGS_FIRST_FLAG = 4;
    BoxParser.TRUN_FLAGS_DURATION = 256;
    BoxParser.TRUN_FLAGS_SIZE = 512;
    BoxParser.TRUN_FLAGS_FLAGS = 1024;
    BoxParser.TRUN_FLAGS_CTS_OFFSET = 2048;
    BoxParser.Box.prototype.add = function(name2) {
      return this.addBox(new BoxParser[name2 + "Box"]());
    };
    BoxParser.Box.prototype.addBox = function(box2) {
      this.boxes.push(box2);
      if (this[box2.type + "s"]) {
        this[box2.type + "s"].push(box2);
      } else {
        this[box2.type] = box2;
      }
      return box2;
    };
    BoxParser.Box.prototype.set = function(prop, value) {
      this[prop] = value;
      return this;
    };
    BoxParser.Box.prototype.addEntry = function(value, _prop) {
      var prop = _prop || "entries";
      if (!this[prop]) {
        this[prop] = [];
      }
      this[prop].push(value);
      return this;
    };
    if (typeof exports !== "undefined") {
      exports.BoxParser = BoxParser;
    }
    BoxParser.parseUUID = function(stream) {
      return BoxParser.parseHex16(stream);
    };
    BoxParser.parseHex16 = function(stream) {
      var hex16 = "";
      for (var i2 = 0; i2 < 16; i2++) {
        var hex2 = stream.readUint8().toString(16);
        hex16 += hex2.length === 1 ? "0" + hex2 : hex2;
      }
      return hex16;
    };
    BoxParser.parseOneBox = function(stream, headerOnly, parentSize) {
      var box2;
      var start2 = stream.getPosition();
      var hdr_size = 0;
      var diff;
      var uuid;
      if (stream.getEndPosition() - start2 < 8) {
        Log.debug("BoxParser", "Not enough data in stream to parse the type and size of the box");
        return { code: BoxParser.ERR_NOT_ENOUGH_DATA };
      }
      if (parentSize && parentSize < 8) {
        Log.debug("BoxParser", "Not enough bytes left in the parent box to parse a new box");
        return { code: BoxParser.ERR_NOT_ENOUGH_DATA };
      }
      var size = stream.readUint32();
      var type = stream.readString(4);
      var box_type = type;
      Log.debug("BoxParser", "Found box of type '" + type + "' and size " + size + " at position " + start2);
      hdr_size = 8;
      if (type == "uuid") {
        if (stream.getEndPosition() - stream.getPosition() < 16 || parentSize - hdr_size < 16) {
          stream.seek(start2);
          Log.debug("BoxParser", "Not enough bytes left in the parent box to parse a UUID box");
          return { code: BoxParser.ERR_NOT_ENOUGH_DATA };
        }
        uuid = BoxParser.parseUUID(stream);
        hdr_size += 16;
        box_type = uuid;
      }
      if (size == 1) {
        if (stream.getEndPosition() - stream.getPosition() < 8 || parentSize && parentSize - hdr_size < 8) {
          stream.seek(start2);
          Log.warn("BoxParser", 'Not enough data in stream to parse the extended size of the "' + type + '" box');
          return { code: BoxParser.ERR_NOT_ENOUGH_DATA };
        }
        size = stream.readUint64();
        hdr_size += 8;
      } else if (size === 0) {
        if (parentSize) {
          size = parentSize;
        } else {
          if (type !== "mdat") {
            Log.error("BoxParser", "Unlimited box size not supported for type: '" + type + "'");
            box2 = new BoxParser.Box(type, size);
            return { code: BoxParser.OK, box: box2, size: box2.size };
          }
        }
      }
      if (size !== 0 && size < hdr_size) {
        Log.error("BoxParser", "Box of type " + type + " has an invalid size " + size + " (too small to be a box)");
        return { code: BoxParser.ERR_NOT_ENOUGH_DATA, type, size, hdr_size, start: start2 };
      }
      if (size !== 0 && parentSize && size > parentSize) {
        Log.error("BoxParser", "Box of type '" + type + "' has a size " + size + " greater than its container size " + parentSize);
        return { code: BoxParser.ERR_NOT_ENOUGH_DATA, type, size, hdr_size, start: start2 };
      }
      if (size !== 0 && start2 + size > stream.getEndPosition()) {
        stream.seek(start2);
        Log.info("BoxParser", "Not enough data in stream to parse the entire '" + type + "' box");
        return { code: BoxParser.ERR_NOT_ENOUGH_DATA, type, size, hdr_size, start: start2 };
      }
      if (headerOnly) {
        return { code: BoxParser.OK, type, size, hdr_size, start: start2 };
      } else {
        if (BoxParser[type + "Box"]) {
          box2 = new BoxParser[type + "Box"](size);
        } else {
          if (type !== "uuid") {
            Log.warn("BoxParser", "Unknown box type: '" + type + "'");
            box2 = new BoxParser.Box(type, size);
            box2.has_unparsed_data = true;
          } else {
            if (BoxParser.UUIDBoxes[uuid]) {
              box2 = new BoxParser.UUIDBoxes[uuid](size);
            } else {
              Log.warn("BoxParser", "Unknown uuid type: '" + uuid + "'");
              box2 = new BoxParser.Box(type, size);
              box2.uuid = uuid;
              box2.has_unparsed_data = true;
            }
          }
        }
      }
      box2.hdr_size = hdr_size;
      box2.start = start2;
      if (box2.write === BoxParser.Box.prototype.write && box2.type !== "mdat") {
        Log.info("BoxParser", "'" + box_type + "' box writing not yet implemented, keeping unparsed data in memory for later write");
        box2.parseDataAndRewind(stream);
      }
      box2.parse(stream);
      diff = stream.getPosition() - (box2.start + box2.size);
      if (diff < 0) {
        Log.warn("BoxParser", "Parsing of box '" + box_type + "' did not read the entire indicated box data size (missing " + -diff + " bytes), seeking forward");
        stream.seek(box2.start + box2.size);
      } else if (diff > 0) {
        Log.error("BoxParser", "Parsing of box '" + box_type + "' read " + diff + " more bytes than the indicated box data size, seeking backwards");
        if (box2.size !== 0) stream.seek(box2.start + box2.size);
      }
      return { code: BoxParser.OK, box: box2, size: box2.size };
    };
    BoxParser.Box.prototype.parse = function(stream) {
      if (this.type != "mdat") {
        this.data = stream.readUint8Array(this.size - this.hdr_size);
      } else {
        if (this.size === 0) {
          stream.seek(stream.getEndPosition());
        } else {
          stream.seek(this.start + this.size);
        }
      }
    };
    BoxParser.Box.prototype.parseDataAndRewind = function(stream) {
      this.data = stream.readUint8Array(this.size - this.hdr_size);
      stream.position -= this.size - this.hdr_size;
    };
    BoxParser.FullBox.prototype.parseDataAndRewind = function(stream) {
      this.parseFullHeader(stream);
      this.data = stream.readUint8Array(this.size - this.hdr_size);
      this.hdr_size -= 4;
      stream.position -= this.size - this.hdr_size;
    };
    BoxParser.FullBox.prototype.parseFullHeader = function(stream) {
      this.version = stream.readUint8();
      this.flags = stream.readUint24();
      this.hdr_size += 4;
    };
    BoxParser.FullBox.prototype.parse = function(stream) {
      this.parseFullHeader(stream);
      this.data = stream.readUint8Array(this.size - this.hdr_size);
    };
    BoxParser.ContainerBox.prototype.parse = function(stream) {
      var ret2;
      var box2;
      while (stream.getPosition() < this.start + this.size) {
        ret2 = BoxParser.parseOneBox(stream, false, this.size - (stream.getPosition() - this.start));
        if (ret2.code === BoxParser.OK) {
          box2 = ret2.box;
          this.boxes.push(box2);
          if (this.subBoxNames && this.subBoxNames.indexOf(box2.type) != -1) {
            this[this.subBoxNames[this.subBoxNames.indexOf(box2.type)] + "s"].push(box2);
          } else {
            var box_type = box2.type !== "uuid" ? box2.type : box2.uuid;
            if (this[box_type]) {
              Log.warn("Box of type " + box_type + " already stored in field of this type");
            } else {
              this[box_type] = box2;
            }
          }
        } else {
          return;
        }
      }
    };
    BoxParser.Box.prototype.parseLanguage = function(stream) {
      this.language = stream.readUint16();
      var chars = [];
      chars[0] = this.language >> 10 & 31;
      chars[1] = this.language >> 5 & 31;
      chars[2] = this.language & 31;
      this.languageString = String.fromCharCode(chars[0] + 96, chars[1] + 96, chars[2] + 96);
    };
    BoxParser.SAMPLE_ENTRY_TYPE_VISUAL = "Visual";
    BoxParser.SAMPLE_ENTRY_TYPE_AUDIO = "Audio";
    BoxParser.SAMPLE_ENTRY_TYPE_HINT = "Hint";
    BoxParser.SAMPLE_ENTRY_TYPE_METADATA = "Metadata";
    BoxParser.SAMPLE_ENTRY_TYPE_SUBTITLE = "Subtitle";
    BoxParser.SAMPLE_ENTRY_TYPE_SYSTEM = "System";
    BoxParser.SAMPLE_ENTRY_TYPE_TEXT = "Text";
    BoxParser.SampleEntry.prototype.parseHeader = function(stream) {
      stream.readUint8Array(6);
      this.data_reference_index = stream.readUint16();
      this.hdr_size += 8;
    };
    BoxParser.SampleEntry.prototype.parse = function(stream) {
      this.parseHeader(stream);
      this.data = stream.readUint8Array(this.size - this.hdr_size);
    };
    BoxParser.SampleEntry.prototype.parseDataAndRewind = function(stream) {
      this.parseHeader(stream);
      this.data = stream.readUint8Array(this.size - this.hdr_size);
      this.hdr_size -= 8;
      stream.position -= this.size - this.hdr_size;
    };
    BoxParser.SampleEntry.prototype.parseFooter = function(stream) {
      BoxParser.ContainerBox.prototype.parse.call(this, stream);
    };
    BoxParser.createMediaSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_HINT);
    BoxParser.createMediaSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_METADATA);
    BoxParser.createMediaSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SUBTITLE);
    BoxParser.createMediaSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SYSTEM);
    BoxParser.createMediaSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_TEXT);
    BoxParser.createMediaSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, function(stream) {
      var compressorname_length;
      this.parseHeader(stream);
      stream.readUint16();
      stream.readUint16();
      stream.readUint32Array(3);
      this.width = stream.readUint16();
      this.height = stream.readUint16();
      this.horizresolution = stream.readUint32();
      this.vertresolution = stream.readUint32();
      stream.readUint32();
      this.frame_count = stream.readUint16();
      compressorname_length = Math.min(31, stream.readUint8());
      this.compressorname = stream.readString(compressorname_length);
      if (compressorname_length < 31) {
        stream.readString(31 - compressorname_length);
      }
      this.depth = stream.readUint16();
      stream.readUint16();
      this.parseFooter(stream);
    });
    BoxParser.createMediaSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, function(stream) {
      this.parseHeader(stream);
      stream.readUint32Array(2);
      this.channel_count = stream.readUint16();
      this.samplesize = stream.readUint16();
      stream.readUint16();
      stream.readUint16();
      this.samplerate = stream.readUint32() / (1 << 16);
      this.parseFooter(stream);
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "avc1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "avc2");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "avc3");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "avc4");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "av01");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "dav1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "hvc1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "hev1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "hvt1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "lhe1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "dvh1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "dvhe");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "vvc1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "vvi1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "vvs1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "vvcN");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "vp08");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "vp09");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "avs3");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "j2ki");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "mjp2");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "mjpg");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "uncv");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "mp4a");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "ac-3");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "ac-4");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "ec-3");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "Opus");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "mha1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "mha2");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "mhm1");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "mhm2");
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "fLaC");
    BoxParser.createEncryptedSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_VISUAL, "encv");
    BoxParser.createEncryptedSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_AUDIO, "enca");
    BoxParser.createEncryptedSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SUBTITLE, "encu");
    BoxParser.createEncryptedSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SYSTEM, "encs");
    BoxParser.createEncryptedSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_TEXT, "enct");
    BoxParser.createEncryptedSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_METADATA, "encm");
    BoxParser.createBoxCtor("a1lx", function(stream) {
      var large_size = stream.readUint8() & 1;
      var FieldLength = ((large_size & 1) + 1) * 16;
      this.layer_size = [];
      for (var i2 = 0; i2 < 3; i2++) {
        if (FieldLength == 16) {
          this.layer_size[i2] = stream.readUint16();
        } else {
          this.layer_size[i2] = stream.readUint32();
        }
      }
    });
    BoxParser.createBoxCtor("a1op", function(stream) {
      this.op_index = stream.readUint8();
    });
    BoxParser.createFullBoxCtor("auxC", function(stream) {
      this.aux_type = stream.readCString();
      var aux_subtype_length = this.size - this.hdr_size - (this.aux_type.length + 1);
      this.aux_subtype = stream.readUint8Array(aux_subtype_length);
    });
    BoxParser.createBoxCtor("av1C", function(stream) {
      var i2;
      var toparse;
      var tmp = stream.readUint8();
      if (tmp >> 7 & false) {
        Log.error("av1C marker problem");
        return;
      }
      this.version = tmp & 127;
      if (this.version !== 1) {
        Log.error("av1C version " + this.version + " not supported");
        return;
      }
      tmp = stream.readUint8();
      this.seq_profile = tmp >> 5 & 7;
      this.seq_level_idx_0 = tmp & 31;
      tmp = stream.readUint8();
      this.seq_tier_0 = tmp >> 7 & 1;
      this.high_bitdepth = tmp >> 6 & 1;
      this.twelve_bit = tmp >> 5 & 1;
      this.monochrome = tmp >> 4 & 1;
      this.chroma_subsampling_x = tmp >> 3 & 1;
      this.chroma_subsampling_y = tmp >> 2 & 1;
      this.chroma_sample_position = tmp & 3;
      tmp = stream.readUint8();
      this.reserved_1 = tmp >> 5 & 7;
      if (this.reserved_1 !== 0) {
        Log.error("av1C reserved_1 parsing problem");
        return;
      }
      this.initial_presentation_delay_present = tmp >> 4 & 1;
      if (this.initial_presentation_delay_present === 1) {
        this.initial_presentation_delay_minus_one = tmp & 15;
      } else {
        this.reserved_2 = tmp & 15;
        if (this.reserved_2 !== 0) {
          Log.error("av1C reserved_2 parsing problem");
          return;
        }
      }
      var configOBUs_length = this.size - this.hdr_size - 4;
      this.configOBUs = stream.readUint8Array(configOBUs_length);
    });
    BoxParser.createBoxCtor("avcC", function(stream) {
      var i2;
      var toparse;
      this.configurationVersion = stream.readUint8();
      this.AVCProfileIndication = stream.readUint8();
      this.profile_compatibility = stream.readUint8();
      this.AVCLevelIndication = stream.readUint8();
      this.lengthSizeMinusOne = stream.readUint8() & 3;
      this.nb_SPS_nalus = stream.readUint8() & 31;
      toparse = this.size - this.hdr_size - 6;
      this.SPS = [];
      for (i2 = 0; i2 < this.nb_SPS_nalus; i2++) {
        this.SPS[i2] = {};
        this.SPS[i2].length = stream.readUint16();
        this.SPS[i2].nalu = stream.readUint8Array(this.SPS[i2].length);
        toparse -= 2 + this.SPS[i2].length;
      }
      this.nb_PPS_nalus = stream.readUint8();
      toparse--;
      this.PPS = [];
      for (i2 = 0; i2 < this.nb_PPS_nalus; i2++) {
        this.PPS[i2] = {};
        this.PPS[i2].length = stream.readUint16();
        this.PPS[i2].nalu = stream.readUint8Array(this.PPS[i2].length);
        toparse -= 2 + this.PPS[i2].length;
      }
      if (toparse > 0) {
        this.ext = stream.readUint8Array(toparse);
      }
    });
    BoxParser.createBoxCtor("btrt", function(stream) {
      this.bufferSizeDB = stream.readUint32();
      this.maxBitrate = stream.readUint32();
      this.avgBitrate = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("ccst", function(stream) {
      var flags = stream.readUint8();
      this.all_ref_pics_intra = (flags & 128) == 128;
      this.intra_pred_used = (flags & 64) == 64;
      this.max_ref_per_pic = (flags & 63) >> 2;
      stream.readUint24();
    });
    BoxParser.createBoxCtor("cdef", function(stream) {
      var i2;
      this.channel_count = stream.readUint16();
      this.channel_indexes = [];
      this.channel_types = [];
      this.channel_associations = [];
      for (i2 = 0; i2 < this.channel_count; i2++) {
        this.channel_indexes.push(stream.readUint16());
        this.channel_types.push(stream.readUint16());
        this.channel_associations.push(stream.readUint16());
      }
    });
    BoxParser.createBoxCtor("clap", function(stream) {
      this.cleanApertureWidthN = stream.readUint32();
      this.cleanApertureWidthD = stream.readUint32();
      this.cleanApertureHeightN = stream.readUint32();
      this.cleanApertureHeightD = stream.readUint32();
      this.horizOffN = stream.readUint32();
      this.horizOffD = stream.readUint32();
      this.vertOffN = stream.readUint32();
      this.vertOffD = stream.readUint32();
    });
    BoxParser.createBoxCtor("clli", function(stream) {
      this.max_content_light_level = stream.readUint16();
      this.max_pic_average_light_level = stream.readUint16();
    });
    BoxParser.createFullBoxCtor("cmex", function(stream) {
      if (this.flags & 1) {
        this.pos_x = stream.readInt32();
      }
      if (this.flags & 2) {
        this.pos_y = stream.readInt32();
      }
      if (this.flags & 4) {
        this.pos_z = stream.readInt32();
      }
      if (this.flags & 8) {
        if (this.version == 0) {
          if (this.flags & 16) {
            this.quat_x = stream.readInt32();
            this.quat_y = stream.readInt32();
            this.quat_z = stream.readInt32();
          } else {
            this.quat_x = stream.readInt16();
            this.quat_y = stream.readInt16();
            this.quat_z = stream.readInt16();
          }
        } else if (this.version == 1) {
        }
      }
      if (this.flags & 32) {
        this.id = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("cmin", function(stream) {
      this.focal_length_x = stream.readInt32();
      this.principal_point_x = stream.readInt32();
      this.principal_point_y = stream.readInt32();
      if (this.flags & 1) {
        this.focal_length_y = stream.readInt32();
        this.skew_factor = stream.readInt32();
      }
    });
    BoxParser.createBoxCtor("cmpd", function(stream) {
      this.component_count = stream.readUint32();
      this.component_types = [];
      this.component_type_urls = [];
      for (i = 0; i < this.component_count; i++) {
        var component_type = stream.readUint16();
        this.component_types.push(component_type);
        if (component_type >= 32768) {
          this.component_type_urls.push(stream.readCString());
        }
      }
    });
    BoxParser.createFullBoxCtor("co64", function(stream) {
      var entry_count2;
      var i2;
      entry_count2 = stream.readUint32();
      this.chunk_offsets = [];
      if (this.version === 0) {
        for (i2 = 0; i2 < entry_count2; i2++) {
          this.chunk_offsets.push(stream.readUint64());
        }
      }
    });
    BoxParser.createFullBoxCtor("CoLL", function(stream) {
      this.maxCLL = stream.readUint16();
      this.maxFALL = stream.readUint16();
    });
    BoxParser.createBoxCtor("colr", function(stream) {
      this.colour_type = stream.readString(4);
      if (this.colour_type === "nclx") {
        this.colour_primaries = stream.readUint16();
        this.transfer_characteristics = stream.readUint16();
        this.matrix_coefficients = stream.readUint16();
        var tmp = stream.readUint8();
        this.full_range_flag = tmp >> 7;
      } else if (this.colour_type === "rICC") {
        this.ICC_profile = stream.readUint8Array(this.size - 4);
      } else if (this.colour_type === "prof") {
        this.ICC_profile = stream.readUint8Array(this.size - 4);
      }
    });
    BoxParser.createFullBoxCtor("cprt", function(stream) {
      this.parseLanguage(stream);
      this.notice = stream.readCString();
    });
    BoxParser.createFullBoxCtor("cslg", function(stream) {
      var entry_count2;
      if (this.version === 0) {
        this.compositionToDTSShift = stream.readInt32();
        this.leastDecodeToDisplayDelta = stream.readInt32();
        this.greatestDecodeToDisplayDelta = stream.readInt32();
        this.compositionStartTime = stream.readInt32();
        this.compositionEndTime = stream.readInt32();
      }
    });
    BoxParser.createFullBoxCtor("ctts", function(stream) {
      var entry_count2;
      var i2;
      entry_count2 = stream.readUint32();
      this.sample_counts = [];
      this.sample_offsets = [];
      if (this.version === 0) {
        for (i2 = 0; i2 < entry_count2; i2++) {
          this.sample_counts.push(stream.readUint32());
          var value = stream.readInt32();
          if (value < 0) {
            Log.warn("BoxParser", "ctts box uses negative values without using version 1");
          }
          this.sample_offsets.push(value);
        }
      } else if (this.version == 1) {
        for (i2 = 0; i2 < entry_count2; i2++) {
          this.sample_counts.push(stream.readUint32());
          this.sample_offsets.push(stream.readInt32());
        }
      }
    });
    BoxParser.createBoxCtor("dac3", function(stream) {
      var tmp_byte1 = stream.readUint8();
      var tmp_byte2 = stream.readUint8();
      var tmp_byte3 = stream.readUint8();
      this.fscod = tmp_byte1 >> 6;
      this.bsid = tmp_byte1 >> 1 & 31;
      this.bsmod = (tmp_byte1 & 1) << 2 | tmp_byte2 >> 6 & 3;
      this.acmod = tmp_byte2 >> 3 & 7;
      this.lfeon = tmp_byte2 >> 2 & 1;
      this.bit_rate_code = tmp_byte2 & 3 | tmp_byte3 >> 5 & 7;
    });
    BoxParser.createBoxCtor("dec3", function(stream) {
      var tmp_16 = stream.readUint16();
      this.data_rate = tmp_16 >> 3;
      this.num_ind_sub = tmp_16 & 7;
      this.ind_subs = [];
      for (var i2 = 0; i2 < this.num_ind_sub + 1; i2++) {
        var ind_sub = {};
        this.ind_subs.push(ind_sub);
        var tmp_byte1 = stream.readUint8();
        var tmp_byte2 = stream.readUint8();
        var tmp_byte3 = stream.readUint8();
        ind_sub.fscod = tmp_byte1 >> 6;
        ind_sub.bsid = tmp_byte1 >> 1 & 31;
        ind_sub.bsmod = (tmp_byte1 & 1) << 4 | tmp_byte2 >> 4 & 15;
        ind_sub.acmod = tmp_byte2 >> 1 & 7;
        ind_sub.lfeon = tmp_byte2 & 1;
        ind_sub.num_dep_sub = tmp_byte3 >> 1 & 15;
        if (ind_sub.num_dep_sub > 0) {
          ind_sub.chan_loc = (tmp_byte3 & 1) << 8 | stream.readUint8();
        }
      }
    });
    BoxParser.createFullBoxCtor("dfLa", function(stream) {
      var BLOCKTYPE_MASK = 127;
      var LASTMETADATABLOCKFLAG_MASK = 128;
      var boxesFound = [];
      var knownBlockTypes = [
        "STREAMINFO",
        "PADDING",
        "APPLICATION",
        "SEEKTABLE",
        "VORBIS_COMMENT",
        "CUESHEET",
        "PICTURE",
        "RESERVED"
      ];
      do {
        var flagAndType = stream.readUint8();
        var type = Math.min(
          flagAndType & BLOCKTYPE_MASK,
          knownBlockTypes.length - 1
        );
        if (!type) {
          stream.readUint8Array(13);
          this.samplerate = stream.readUint32() >> 12;
          stream.readUint8Array(20);
        } else {
          stream.readUint8Array(stream.readUint24());
        }
        boxesFound.push(knownBlockTypes[type]);
        if (!!(flagAndType & LASTMETADATABLOCKFLAG_MASK)) {
          break;
        }
      } while (true);
      this.numMetadataBlocks = boxesFound.length + " (" + boxesFound.join(", ") + ")";
    });
    BoxParser.createBoxCtor("dimm", function(stream) {
      this.bytessent = stream.readUint64();
    });
    BoxParser.createBoxCtor("dmax", function(stream) {
      this.time = stream.readUint32();
    });
    BoxParser.createBoxCtor("dmed", function(stream) {
      this.bytessent = stream.readUint64();
    });
    BoxParser.createBoxCtor("dOps", function(stream) {
      this.Version = stream.readUint8();
      this.OutputChannelCount = stream.readUint8();
      this.PreSkip = stream.readUint16();
      this.InputSampleRate = stream.readUint32();
      this.OutputGain = stream.readInt16();
      this.ChannelMappingFamily = stream.readUint8();
      if (this.ChannelMappingFamily !== 0) {
        this.StreamCount = stream.readUint8();
        this.CoupledCount = stream.readUint8();
        this.ChannelMapping = [];
        for (var i2 = 0; i2 < this.OutputChannelCount; i2++) {
          this.ChannelMapping[i2] = stream.readUint8();
        }
      }
    });
    BoxParser.createFullBoxCtor("dref", function(stream) {
      var ret2;
      var box2;
      this.entries = [];
      var entry_count2 = stream.readUint32();
      for (var i2 = 0; i2 < entry_count2; i2++) {
        ret2 = BoxParser.parseOneBox(stream, false, this.size - (stream.getPosition() - this.start));
        if (ret2.code === BoxParser.OK) {
          box2 = ret2.box;
          this.entries.push(box2);
        } else {
          return;
        }
      }
    });
    BoxParser.createBoxCtor("drep", function(stream) {
      this.bytessent = stream.readUint64();
    });
    BoxParser.createFullBoxCtor("elng", function(stream) {
      this.extended_language = stream.readString(this.size - this.hdr_size);
    });
    BoxParser.createFullBoxCtor("elst", function(stream) {
      this.entries = [];
      var entry_count2 = stream.readUint32();
      for (var i2 = 0; i2 < entry_count2; i2++) {
        var entry = {};
        this.entries.push(entry);
        if (this.version === 1) {
          entry.segment_duration = stream.readUint64();
          entry.media_time = stream.readInt64();
        } else {
          entry.segment_duration = stream.readUint32();
          entry.media_time = stream.readInt32();
        }
        entry.media_rate_integer = stream.readInt16();
        entry.media_rate_fraction = stream.readInt16();
      }
    });
    BoxParser.createFullBoxCtor("emsg", function(stream) {
      if (this.version == 1) {
        this.timescale = stream.readUint32();
        this.presentation_time = stream.readUint64();
        this.event_duration = stream.readUint32();
        this.id = stream.readUint32();
        this.scheme_id_uri = stream.readCString();
        this.value = stream.readCString();
      } else {
        this.scheme_id_uri = stream.readCString();
        this.value = stream.readCString();
        this.timescale = stream.readUint32();
        this.presentation_time_delta = stream.readUint32();
        this.event_duration = stream.readUint32();
        this.id = stream.readUint32();
      }
      var message_size = this.size - this.hdr_size - (4 * 4 + (this.scheme_id_uri.length + 1) + (this.value.length + 1));
      if (this.version == 1) {
        message_size -= 4;
      }
      this.message_data = stream.readUint8Array(message_size);
    });
    BoxParser.createEntityToGroupCtor = function(type, parseMethod) {
      BoxParser[type + "Box"] = function(size) {
        BoxParser.FullBox.call(this, type, size);
      };
      BoxParser[type + "Box"].prototype = new BoxParser.FullBox();
      BoxParser[type + "Box"].prototype.parse = function(stream) {
        this.parseFullHeader(stream);
        if (parseMethod) {
          parseMethod.call(this, stream);
        } else {
          this.group_id = stream.readUint32();
          this.num_entities_in_group = stream.readUint32();
          this.entity_ids = [];
          for (i = 0; i < this.num_entities_in_group; i++) {
            var entity_id = stream.readUint32();
            this.entity_ids.push(entity_id);
          }
        }
      };
    };
    BoxParser.createEntityToGroupCtor("aebr");
    BoxParser.createEntityToGroupCtor("afbr");
    BoxParser.createEntityToGroupCtor("albc");
    BoxParser.createEntityToGroupCtor("altr");
    BoxParser.createEntityToGroupCtor("brst");
    BoxParser.createEntityToGroupCtor("dobr");
    BoxParser.createEntityToGroupCtor("eqiv");
    BoxParser.createEntityToGroupCtor("favc");
    BoxParser.createEntityToGroupCtor("fobr");
    BoxParser.createEntityToGroupCtor("iaug");
    BoxParser.createEntityToGroupCtor("pano");
    BoxParser.createEntityToGroupCtor("slid");
    BoxParser.createEntityToGroupCtor("ster");
    BoxParser.createEntityToGroupCtor("tsyn");
    BoxParser.createEntityToGroupCtor("wbbr");
    BoxParser.createEntityToGroupCtor("prgr");
    BoxParser.createEntityToGroupCtor("pymd", function(stream) {
      this.group_id = stream.readUint32();
      this.num_entities_in_group = stream.readUint32();
      this.entity_ids = [];
      for (var i2 = 0; i2 < this.num_entities_in_group; i2++) {
        var entity_id = stream.readUint32();
        this.entity_ids.push(entity_id);
      }
      this.tile_size_x = stream.readUint16();
      this.tile_size_y = stream.readUint16();
      this.layer_binning = [];
      this.tiles_in_layer_column_minus1 = [];
      this.tiles_in_layer_row_minus1 = [];
      for (i2 = 0; i2 < this.num_entities_in_group; i2++) {
        this.layer_binning[i2] = stream.readUint16();
        this.tiles_in_layer_row_minus1[i2] = stream.readUint16();
        this.tiles_in_layer_column_minus1[i2] = stream.readUint16();
      }
    });
    BoxParser.createFullBoxCtor("esds", function(stream) {
      var esd_data = stream.readUint8Array(this.size - this.hdr_size);
      this.data = esd_data;
      if (typeof MPEG4DescriptorParser !== "undefined") {
        var esd_parser = new MPEG4DescriptorParser();
        this.esd = esd_parser.parseOneDescriptor(new DataStream(esd_data.buffer, 0, DataStream.BIG_ENDIAN));
      }
    });
    BoxParser.createBoxCtor("fiel", function(stream) {
      this.fieldCount = stream.readUint8();
      this.fieldOrdering = stream.readUint8();
    });
    BoxParser.createBoxCtor("frma", function(stream) {
      this.data_format = stream.readString(4);
    });
    BoxParser.createBoxCtor("ftyp", function(stream) {
      var toparse = this.size - this.hdr_size;
      this.major_brand = stream.readString(4);
      this.minor_version = stream.readUint32();
      toparse -= 8;
      this.compatible_brands = [];
      var i2 = 0;
      while (toparse >= 4) {
        this.compatible_brands[i2] = stream.readString(4);
        toparse -= 4;
        i2++;
      }
    });
    BoxParser.createFullBoxCtor("hdlr", function(stream) {
      if (this.version === 0) {
        stream.readUint32();
        this.handler = stream.readString(4);
        stream.readUint32Array(3);
        this.name = stream.readString(this.size - this.hdr_size - 20);
        if (this.name[this.name.length - 1] === "\0") {
          this.name = this.name.slice(0, -1);
        }
      }
    });
    BoxParser.createBoxCtor("hvcC", function(stream) {
      var i2, j3;
      var nb_nalus;
      var length;
      var tmp_byte;
      this.configurationVersion = stream.readUint8();
      tmp_byte = stream.readUint8();
      this.general_profile_space = tmp_byte >> 6;
      this.general_tier_flag = (tmp_byte & 32) >> 5;
      this.general_profile_idc = tmp_byte & 31;
      this.general_profile_compatibility = stream.readUint32();
      this.general_constraint_indicator = stream.readUint8Array(6);
      this.general_level_idc = stream.readUint8();
      this.min_spatial_segmentation_idc = stream.readUint16() & 4095;
      this.parallelismType = stream.readUint8() & 3;
      this.chroma_format_idc = stream.readUint8() & 3;
      this.bit_depth_luma_minus8 = stream.readUint8() & 7;
      this.bit_depth_chroma_minus8 = stream.readUint8() & 7;
      this.avgFrameRate = stream.readUint16();
      tmp_byte = stream.readUint8();
      this.constantFrameRate = tmp_byte >> 6;
      this.numTemporalLayers = (tmp_byte & 13) >> 3;
      this.temporalIdNested = (tmp_byte & 4) >> 2;
      this.lengthSizeMinusOne = tmp_byte & 3;
      this.nalu_arrays = [];
      var numOfArrays = stream.readUint8();
      for (i2 = 0; i2 < numOfArrays; i2++) {
        var nalu_array = [];
        this.nalu_arrays.push(nalu_array);
        tmp_byte = stream.readUint8();
        nalu_array.completeness = (tmp_byte & 128) >> 7;
        nalu_array.nalu_type = tmp_byte & 63;
        var numNalus = stream.readUint16();
        for (j3 = 0; j3 < numNalus; j3++) {
          var nalu = {};
          nalu_array.push(nalu);
          length = stream.readUint16();
          nalu.data = stream.readUint8Array(length);
        }
      }
    });
    BoxParser.createFullBoxCtor("iinf", function(stream) {
      var ret2;
      if (this.version === 0) {
        this.entry_count = stream.readUint16();
      } else {
        this.entry_count = stream.readUint32();
      }
      this.item_infos = [];
      for (var i2 = 0; i2 < this.entry_count; i2++) {
        ret2 = BoxParser.parseOneBox(stream, false, this.size - (stream.getPosition() - this.start));
        if (ret2.code === BoxParser.OK) {
          if (ret2.box.type !== "infe") {
            Log.error("BoxParser", "Expected 'infe' box, got " + ret2.box.type);
          }
          this.item_infos[i2] = ret2.box;
        } else {
          return;
        }
      }
    });
    BoxParser.createFullBoxCtor("iloc", function(stream) {
      var byte;
      byte = stream.readUint8();
      this.offset_size = byte >> 4 & 15;
      this.length_size = byte & 15;
      byte = stream.readUint8();
      this.base_offset_size = byte >> 4 & 15;
      if (this.version === 1 || this.version === 2) {
        this.index_size = byte & 15;
      } else {
        this.index_size = 0;
      }
      this.items = [];
      var item_count = 0;
      if (this.version < 2) {
        item_count = stream.readUint16();
      } else if (this.version === 2) {
        item_count = stream.readUint32();
      } else {
        throw "version of iloc box not supported";
      }
      for (var i2 = 0; i2 < item_count; i2++) {
        var item3 = {};
        this.items.push(item3);
        if (this.version < 2) {
          item3.item_ID = stream.readUint16();
        } else if (this.version === 2) {
          item3.item_ID = stream.readUint32();
        } else {
          throw "version of iloc box not supported";
        }
        if (this.version === 1 || this.version === 2) {
          item3.construction_method = stream.readUint16() & 15;
        } else {
          item3.construction_method = 0;
        }
        item3.data_reference_index = stream.readUint16();
        switch (this.base_offset_size) {
          case 0:
            item3.base_offset = 0;
            break;
          case 4:
            item3.base_offset = stream.readUint32();
            break;
          case 8:
            item3.base_offset = stream.readUint64();
            break;
          default:
            throw "Error reading base offset size";
        }
        var extent_count = stream.readUint16();
        item3.extents = [];
        for (var j3 = 0; j3 < extent_count; j3++) {
          var extent = {};
          item3.extents.push(extent);
          if (this.version === 1 || this.version === 2) {
            switch (this.index_size) {
              case 0:
                extent.extent_index = 0;
                break;
              case 4:
                extent.extent_index = stream.readUint32();
                break;
              case 8:
                extent.extent_index = stream.readUint64();
                break;
              default:
                throw "Error reading extent index";
            }
          }
          switch (this.offset_size) {
            case 0:
              extent.extent_offset = 0;
              break;
            case 4:
              extent.extent_offset = stream.readUint32();
              break;
            case 8:
              extent.extent_offset = stream.readUint64();
              break;
            default:
              throw "Error reading extent index";
          }
          switch (this.length_size) {
            case 0:
              extent.extent_length = 0;
              break;
            case 4:
              extent.extent_length = stream.readUint32();
              break;
            case 8:
              extent.extent_length = stream.readUint64();
              break;
            default:
              throw "Error reading extent index";
          }
        }
      }
    });
    BoxParser.createBoxCtor("imir", function(stream) {
      var tmp = stream.readUint8();
      this.reserved = tmp >> 7;
      this.axis = tmp & 1;
    });
    BoxParser.createFullBoxCtor("infe", function(stream) {
      if (this.version === 0 || this.version === 1) {
        this.item_ID = stream.readUint16();
        this.item_protection_index = stream.readUint16();
        this.item_name = stream.readCString();
        this.content_type = stream.readCString();
        this.content_encoding = stream.readCString();
      }
      if (this.version === 1) {
        this.extension_type = stream.readString(4);
        Log.warn("BoxParser", "Cannot parse extension type");
        stream.seek(this.start + this.size);
        return;
      }
      if (this.version >= 2) {
        if (this.version === 2) {
          this.item_ID = stream.readUint16();
        } else if (this.version === 3) {
          this.item_ID = stream.readUint32();
        }
        this.item_protection_index = stream.readUint16();
        this.item_type = stream.readString(4);
        this.item_name = stream.readCString();
        if (this.item_type === "mime") {
          this.content_type = stream.readCString();
          this.content_encoding = stream.readCString();
        } else if (this.item_type === "uri ") {
          this.item_uri_type = stream.readCString();
        }
      }
    });
    BoxParser.createFullBoxCtor("ipma", function(stream) {
      var i2, j3;
      entry_count = stream.readUint32();
      this.associations = [];
      for (i2 = 0; i2 < entry_count; i2++) {
        var item_assoc = {};
        this.associations.push(item_assoc);
        if (this.version < 1) {
          item_assoc.id = stream.readUint16();
        } else {
          item_assoc.id = stream.readUint32();
        }
        var association_count = stream.readUint8();
        item_assoc.props = [];
        for (j3 = 0; j3 < association_count; j3++) {
          var tmp = stream.readUint8();
          var p2 = {};
          item_assoc.props.push(p2);
          p2.essential = (tmp & 128) >> 7 === 1;
          if (this.flags & 1) {
            p2.property_index = (tmp & 127) << 8 | stream.readUint8();
          } else {
            p2.property_index = tmp & 127;
          }
        }
      }
    });
    BoxParser.createFullBoxCtor("iref", function(stream) {
      var ret2;
      var entryCount;
      var box2;
      this.references = [];
      while (stream.getPosition() < this.start + this.size) {
        ret2 = BoxParser.parseOneBox(stream, true, this.size - (stream.getPosition() - this.start));
        if (ret2.code === BoxParser.OK) {
          if (this.version === 0) {
            box2 = new BoxParser.SingleItemTypeReferenceBox(ret2.type, ret2.size, ret2.hdr_size, ret2.start);
          } else {
            box2 = new BoxParser.SingleItemTypeReferenceBoxLarge(ret2.type, ret2.size, ret2.hdr_size, ret2.start);
          }
          if (box2.write === BoxParser.Box.prototype.write && box2.type !== "mdat") {
            Log.warn("BoxParser", box2.type + " box writing not yet implemented, keeping unparsed data in memory for later write");
            box2.parseDataAndRewind(stream);
          }
          box2.parse(stream);
          this.references.push(box2);
        } else {
          return;
        }
      }
    });
    BoxParser.createBoxCtor("irot", function(stream) {
      this.angle = stream.readUint8() & 3;
    });
    BoxParser.createFullBoxCtor("ispe", function(stream) {
      this.image_width = stream.readUint32();
      this.image_height = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("kind", function(stream) {
      this.schemeURI = stream.readCString();
      this.value = stream.readCString();
    });
    BoxParser.createFullBoxCtor("leva", function(stream) {
      var count = stream.readUint8();
      this.levels = [];
      for (var i2 = 0; i2 < count; i2++) {
        var level = {};
        this.levels[i2] = level;
        level.track_ID = stream.readUint32();
        var tmp_byte = stream.readUint8();
        level.padding_flag = tmp_byte >> 7;
        level.assignment_type = tmp_byte & 127;
        switch (level.assignment_type) {
          case 0:
            level.grouping_type = stream.readString(4);
            break;
          case 1:
            level.grouping_type = stream.readString(4);
            level.grouping_type_parameter = stream.readUint32();
            break;
          case 2:
            break;
          case 3:
            break;
          case 4:
            level.sub_track_id = stream.readUint32();
            break;
          default:
            Log.warn("BoxParser", "Unknown leva assignement type");
        }
      }
    });
    BoxParser.createBoxCtor("lhvC", function(stream) {
      var i2, j3;
      var tmp_byte;
      this.configurationVersion = stream.readUint8();
      this.min_spatial_segmentation_idc = stream.readUint16() & 4095;
      this.parallelismType = stream.readUint8() & 3;
      tmp_byte = stream.readUint8();
      this.numTemporalLayers = (tmp_byte & 13) >> 3;
      this.temporalIdNested = (tmp_byte & 4) >> 2;
      this.lengthSizeMinusOne = tmp_byte & 3;
      this.nalu_arrays = [];
      var numOfArrays = stream.readUint8();
      for (i2 = 0; i2 < numOfArrays; i2++) {
        var nalu_array = [];
        this.nalu_arrays.push(nalu_array);
        tmp_byte = stream.readUint8();
        nalu_array.completeness = (tmp_byte & 128) >> 7;
        nalu_array.nalu_type = tmp_byte & 63;
        var numNalus = stream.readUint16();
        for (j3 = 0; j3 < numNalus; j3++) {
          var nalu = {};
          nalu_array.push(nalu);
          var length = stream.readUint16();
          nalu.data = stream.readUint8Array(length);
        }
      }
    });
    BoxParser.createBoxCtor("lsel", function(stream) {
      this.layer_id = stream.readUint16();
    });
    BoxParser.createBoxCtor("maxr", function(stream) {
      this.period = stream.readUint32();
      this.bytes = stream.readUint32();
    });
    function ColorPoint(x3, y2) {
      this.x = x3;
      this.y = y2;
    }
    ColorPoint.prototype.toString = function() {
      return "(" + this.x + "," + this.y + ")";
    };
    BoxParser.createBoxCtor("mdcv", function(stream) {
      this.display_primaries = [];
      this.display_primaries[0] = new ColorPoint(stream.readUint16(), stream.readUint16());
      this.display_primaries[1] = new ColorPoint(stream.readUint16(), stream.readUint16());
      this.display_primaries[2] = new ColorPoint(stream.readUint16(), stream.readUint16());
      this.white_point = new ColorPoint(stream.readUint16(), stream.readUint16());
      this.max_display_mastering_luminance = stream.readUint32();
      this.min_display_mastering_luminance = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("mdhd", function(stream) {
      if (this.version == 1) {
        this.creation_time = stream.readUint64();
        this.modification_time = stream.readUint64();
        this.timescale = stream.readUint32();
        this.duration = stream.readUint64();
      } else {
        this.creation_time = stream.readUint32();
        this.modification_time = stream.readUint32();
        this.timescale = stream.readUint32();
        this.duration = stream.readUint32();
      }
      this.parseLanguage(stream);
      stream.readUint16();
    });
    BoxParser.createFullBoxCtor("mehd", function(stream) {
      if (this.flags & 1) {
        Log.warn("BoxParser", "mehd box incorrectly uses flags set to 1, converting version to 1");
        this.version = 1;
      }
      if (this.version == 1) {
        this.fragment_duration = stream.readUint64();
      } else {
        this.fragment_duration = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("meta", function(stream) {
      this.boxes = [];
      BoxParser.ContainerBox.prototype.parse.call(this, stream);
    });
    BoxParser.createFullBoxCtor("mfhd", function(stream) {
      this.sequence_number = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("mfro", function(stream) {
      this._size = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("mskC", function(stream) {
      this.bits_per_pixel = stream.readUint8();
    });
    BoxParser.createFullBoxCtor("mvhd", function(stream) {
      if (this.version == 1) {
        this.creation_time = stream.readUint64();
        this.modification_time = stream.readUint64();
        this.timescale = stream.readUint32();
        this.duration = stream.readUint64();
      } else {
        this.creation_time = stream.readUint32();
        this.modification_time = stream.readUint32();
        this.timescale = stream.readUint32();
        this.duration = stream.readUint32();
      }
      this.rate = stream.readUint32();
      this.volume = stream.readUint16() >> 8;
      stream.readUint16();
      stream.readUint32Array(2);
      this.matrix = stream.readUint32Array(9);
      stream.readUint32Array(6);
      this.next_track_id = stream.readUint32();
    });
    BoxParser.createBoxCtor("npck", function(stream) {
      this.packetssent = stream.readUint32();
    });
    BoxParser.createBoxCtor("nump", function(stream) {
      this.packetssent = stream.readUint64();
    });
    BoxParser.createFullBoxCtor("padb", function(stream) {
      var sample_count = stream.readUint32();
      this.padbits = [];
      for (var i2 = 0; i2 < Math.floor((sample_count + 1) / 2); i2++) {
        this.padbits = stream.readUint8();
      }
    });
    BoxParser.createBoxCtor("pasp", function(stream) {
      this.hSpacing = stream.readUint32();
      this.vSpacing = stream.readUint32();
    });
    BoxParser.createBoxCtor("payl", function(stream) {
      this.text = stream.readString(this.size - this.hdr_size);
    });
    BoxParser.createBoxCtor("payt", function(stream) {
      this.payloadID = stream.readUint32();
      var count = stream.readUint8();
      this.rtpmap_string = stream.readString(count);
    });
    BoxParser.createFullBoxCtor("pdin", function(stream) {
      var count = (this.size - this.hdr_size) / 8;
      this.rate = [];
      this.initial_delay = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.rate[i2] = stream.readUint32();
        this.initial_delay[i2] = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("pitm", function(stream) {
      if (this.version === 0) {
        this.item_id = stream.readUint16();
      } else {
        this.item_id = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("pixi", function(stream) {
      var i2;
      this.num_channels = stream.readUint8();
      this.bits_per_channels = [];
      for (i2 = 0; i2 < this.num_channels; i2++) {
        this.bits_per_channels[i2] = stream.readUint8();
      }
    });
    BoxParser.createBoxCtor("pmax", function(stream) {
      this.bytes = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("prdi", function(stream) {
      this.step_count = stream.readUint16();
      this.item_count = [];
      if (this.flags & 2) {
        for (var i2 = 0; i2 < this.step_count; i2++) {
          this.item_count[i2] = stream.readUint16();
        }
      }
    });
    BoxParser.createFullBoxCtor("prft", function(stream) {
      this.ref_track_id = stream.readUint32();
      this.ntp_timestamp = stream.readUint64();
      if (this.version === 0) {
        this.media_time = stream.readUint32();
      } else {
        this.media_time = stream.readUint64();
      }
    });
    BoxParser.createFullBoxCtor("pssh", function(stream) {
      this.system_id = BoxParser.parseHex16(stream);
      if (this.version > 0) {
        var count = stream.readUint32();
        this.kid = [];
        for (var i2 = 0; i2 < count; i2++) {
          this.kid[i2] = BoxParser.parseHex16(stream);
        }
      }
      var datasize = stream.readUint32();
      if (datasize > 0) {
        this.data = stream.readUint8Array(datasize);
      }
    });
    BoxParser.createFullBoxCtor("clef", function(stream) {
      this.width = stream.readUint32();
      this.height = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("enof", function(stream) {
      this.width = stream.readUint32();
      this.height = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("prof", function(stream) {
      this.width = stream.readUint32();
      this.height = stream.readUint32();
    });
    BoxParser.createContainerBoxCtor("tapt", null, ["clef", "prof", "enof"]);
    BoxParser.createBoxCtor("rtp ", function(stream) {
      this.descriptionformat = stream.readString(4);
      this.sdptext = stream.readString(this.size - this.hdr_size - 4);
    });
    BoxParser.createFullBoxCtor("saio", function(stream) {
      if (this.flags & 1) {
        this.aux_info_type = stream.readUint32();
        this.aux_info_type_parameter = stream.readUint32();
      }
      var count = stream.readUint32();
      this.offset = [];
      for (var i2 = 0; i2 < count; i2++) {
        if (this.version === 0) {
          this.offset[i2] = stream.readUint32();
        } else {
          this.offset[i2] = stream.readUint64();
        }
      }
    });
    BoxParser.createFullBoxCtor("saiz", function(stream) {
      if (this.flags & 1) {
        this.aux_info_type = stream.readUint32();
        this.aux_info_type_parameter = stream.readUint32();
      }
      this.default_sample_info_size = stream.readUint8();
      var count = stream.readUint32();
      this.sample_info_size = [];
      if (this.default_sample_info_size === 0) {
        for (var i2 = 0; i2 < count; i2++) {
          this.sample_info_size[i2] = stream.readUint8();
        }
      }
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_METADATA, "mett", function(stream) {
      this.parseHeader(stream);
      this.content_encoding = stream.readCString();
      this.mime_format = stream.readCString();
      this.parseFooter(stream);
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_METADATA, "metx", function(stream) {
      this.parseHeader(stream);
      this.content_encoding = stream.readCString();
      this.namespace = stream.readCString();
      this.schema_location = stream.readCString();
      this.parseFooter(stream);
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SUBTITLE, "sbtt", function(stream) {
      this.parseHeader(stream);
      this.content_encoding = stream.readCString();
      this.mime_format = stream.readCString();
      this.parseFooter(stream);
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SUBTITLE, "stpp", function(stream) {
      this.parseHeader(stream);
      this.namespace = stream.readCString();
      this.schema_location = stream.readCString();
      this.auxiliary_mime_types = stream.readCString();
      this.parseFooter(stream);
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SUBTITLE, "stxt", function(stream) {
      this.parseHeader(stream);
      this.content_encoding = stream.readCString();
      this.mime_format = stream.readCString();
      this.parseFooter(stream);
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_SUBTITLE, "tx3g", function(stream) {
      this.parseHeader(stream);
      this.displayFlags = stream.readUint32();
      this.horizontal_justification = stream.readInt8();
      this.vertical_justification = stream.readInt8();
      this.bg_color_rgba = stream.readUint8Array(4);
      this.box_record = stream.readInt16Array(4);
      this.style_record = stream.readUint8Array(12);
      this.parseFooter(stream);
    });
    BoxParser.createSampleEntryCtor(BoxParser.SAMPLE_ENTRY_TYPE_METADATA, "wvtt", function(stream) {
      this.parseHeader(stream);
      this.parseFooter(stream);
    });
    BoxParser.createSampleGroupCtor("alst", function(stream) {
      var i2;
      var roll_count = stream.readUint16();
      this.first_output_sample = stream.readUint16();
      this.sample_offset = [];
      for (i2 = 0; i2 < roll_count; i2++) {
        this.sample_offset[i2] = stream.readUint32();
      }
      var remaining = this.description_length - 4 - 4 * roll_count;
      this.num_output_samples = [];
      this.num_total_samples = [];
      for (i2 = 0; i2 < remaining / 4; i2++) {
        this.num_output_samples[i2] = stream.readUint16();
        this.num_total_samples[i2] = stream.readUint16();
      }
    });
    BoxParser.createSampleGroupCtor("avll", function(stream) {
      this.layerNumber = stream.readUint8();
      this.accurateStatisticsFlag = stream.readUint8();
      this.avgBitRate = stream.readUint16();
      this.avgFrameRate = stream.readUint16();
    });
    BoxParser.createSampleGroupCtor("avss", function(stream) {
      this.subSequenceIdentifier = stream.readUint16();
      this.layerNumber = stream.readUint8();
      var tmp_byte = stream.readUint8();
      this.durationFlag = tmp_byte >> 7;
      this.avgRateFlag = tmp_byte >> 6 & 1;
      if (this.durationFlag) {
        this.duration = stream.readUint32();
      }
      if (this.avgRateFlag) {
        this.accurateStatisticsFlag = stream.readUint8();
        this.avgBitRate = stream.readUint16();
        this.avgFrameRate = stream.readUint16();
      }
      this.dependency = [];
      var numReferences = stream.readUint8();
      for (var i2 = 0; i2 < numReferences; i2++) {
        var dependencyInfo = {};
        this.dependency.push(dependencyInfo);
        dependencyInfo.subSeqDirectionFlag = stream.readUint8();
        dependencyInfo.layerNumber = stream.readUint8();
        dependencyInfo.subSequenceIdentifier = stream.readUint16();
      }
    });
    BoxParser.createSampleGroupCtor("dtrt", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createSampleGroupCtor("mvif", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createSampleGroupCtor("prol", function(stream) {
      this.roll_distance = stream.readInt16();
    });
    BoxParser.createSampleGroupCtor("rap ", function(stream) {
      var tmp_byte = stream.readUint8();
      this.num_leading_samples_known = tmp_byte >> 7;
      this.num_leading_samples = tmp_byte & 127;
    });
    BoxParser.createSampleGroupCtor("rash", function(stream) {
      this.operation_point_count = stream.readUint16();
      if (this.description_length !== 2 + (this.operation_point_count === 1 ? 2 : this.operation_point_count * 6) + 9) {
        Log.warn("BoxParser", "Mismatch in " + this.grouping_type + " sample group length");
        this.data = stream.readUint8Array(this.description_length - 2);
      } else {
        if (this.operation_point_count === 1) {
          this.target_rate_share = stream.readUint16();
        } else {
          this.target_rate_share = [];
          this.available_bitrate = [];
          for (var i2 = 0; i2 < this.operation_point_count; i2++) {
            this.available_bitrate[i2] = stream.readUint32();
            this.target_rate_share[i2] = stream.readUint16();
          }
        }
        this.maximum_bitrate = stream.readUint32();
        this.minimum_bitrate = stream.readUint32();
        this.discard_priority = stream.readUint8();
      }
    });
    BoxParser.createSampleGroupCtor("roll", function(stream) {
      this.roll_distance = stream.readInt16();
    });
    BoxParser.SampleGroupEntry.prototype.parse = function(stream) {
      Log.warn("BoxParser", "Unknown Sample Group type: " + this.grouping_type);
      this.data = stream.readUint8Array(this.description_length);
    };
    BoxParser.createSampleGroupCtor("scif", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createSampleGroupCtor("scnm", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createSampleGroupCtor("seig", function(stream) {
      this.reserved = stream.readUint8();
      var tmp = stream.readUint8();
      this.crypt_byte_block = tmp >> 4;
      this.skip_byte_block = tmp & 15;
      this.isProtected = stream.readUint8();
      this.Per_Sample_IV_Size = stream.readUint8();
      this.KID = BoxParser.parseHex16(stream);
      this.constant_IV_size = 0;
      this.constant_IV = 0;
      if (this.isProtected === 1 && this.Per_Sample_IV_Size === 0) {
        this.constant_IV_size = stream.readUint8();
        this.constant_IV = stream.readUint8Array(this.constant_IV_size);
      }
    });
    BoxParser.createSampleGroupCtor("stsa", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createSampleGroupCtor("sync", function(stream) {
      var tmp_byte = stream.readUint8();
      this.NAL_unit_type = tmp_byte & 63;
    });
    BoxParser.createSampleGroupCtor("tele", function(stream) {
      var tmp_byte = stream.readUint8();
      this.level_independently_decodable = tmp_byte >> 7;
    });
    BoxParser.createSampleGroupCtor("tsas", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createSampleGroupCtor("tscl", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createSampleGroupCtor("vipr", function(stream) {
      Log.warn("BoxParser", "Sample Group type: " + this.grouping_type + " not fully parsed");
    });
    BoxParser.createFullBoxCtor("sbgp", function(stream) {
      this.grouping_type = stream.readString(4);
      if (this.version === 1) {
        this.grouping_type_parameter = stream.readUint32();
      } else {
        this.grouping_type_parameter = 0;
      }
      this.entries = [];
      var entry_count2 = stream.readUint32();
      for (var i2 = 0; i2 < entry_count2; i2++) {
        var entry = {};
        this.entries.push(entry);
        entry.sample_count = stream.readInt32();
        entry.group_description_index = stream.readInt32();
      }
    });
    function Pixel(row3, col) {
      this.bad_pixel_row = row3;
      this.bad_pixel_column = col;
    }
    Pixel.prototype.toString = function pixelToString() {
      return "[row: " + this.bad_pixel_row + ", column: " + this.bad_pixel_column + "]";
    };
    BoxParser.createFullBoxCtor("sbpm", function(stream) {
      var i2;
      this.component_count = stream.readUint16();
      this.component_index = [];
      for (i2 = 0; i2 < this.component_count; i2++) {
        this.component_index.push(stream.readUint16());
      }
      var flags = stream.readUint8();
      this.correction_applied = 128 == (flags & 128);
      this.num_bad_rows = stream.readUint32();
      this.num_bad_cols = stream.readUint32();
      this.num_bad_pixels = stream.readUint32();
      this.bad_rows = [];
      this.bad_columns = [];
      this.bad_pixels = [];
      for (i2 = 0; i2 < this.num_bad_rows; i2++) {
        this.bad_rows.push(stream.readUint32());
      }
      for (i2 = 0; i2 < this.num_bad_cols; i2++) {
        this.bad_columns.push(stream.readUint32());
      }
      for (i2 = 0; i2 < this.num_bad_pixels; i2++) {
        var row3 = stream.readUint32();
        var col = stream.readUint32();
        this.bad_pixels.push(new Pixel(row3, col));
      }
    });
    BoxParser.createFullBoxCtor("schm", function(stream) {
      this.scheme_type = stream.readString(4);
      this.scheme_version = stream.readUint32();
      if (this.flags & 1) {
        this.scheme_uri = stream.readString(this.size - this.hdr_size - 8);
      }
    });
    BoxParser.createBoxCtor("sdp ", function(stream) {
      this.sdptext = stream.readString(this.size - this.hdr_size);
    });
    BoxParser.createFullBoxCtor("sdtp", function(stream) {
      var tmp_byte;
      var count = this.size - this.hdr_size;
      this.is_leading = [];
      this.sample_depends_on = [];
      this.sample_is_depended_on = [];
      this.sample_has_redundancy = [];
      for (var i2 = 0; i2 < count; i2++) {
        tmp_byte = stream.readUint8();
        this.is_leading[i2] = tmp_byte >> 6;
        this.sample_depends_on[i2] = tmp_byte >> 4 & 3;
        this.sample_is_depended_on[i2] = tmp_byte >> 2 & 3;
        this.sample_has_redundancy[i2] = tmp_byte & 3;
      }
    });
    BoxParser.createFullBoxCtor(
      "senc"
      /*, function(stream) {
      	this.parseFullHeader(stream);
      	var sample_count = stream.readUint32();
      	this.samples = [];
      	for (var i = 0; i < sample_count; i++) {
      		var sample = {};
      		// tenc.default_Per_Sample_IV_Size or seig.Per_Sample_IV_Size
      		sample.InitializationVector = this.readUint8Array(Per_Sample_IV_Size*8);
      		if (this.flags & 0x2) {
      			sample.subsamples = [];
      			subsample_count = stream.readUint16();
      			for (var j = 0; j < subsample_count; j++) {
      				var subsample = {};
      				subsample.BytesOfClearData = stream.readUint16();
      				subsample.BytesOfProtectedData = stream.readUint32();
      				sample.subsamples.push(subsample);
      			}
      		}
      		// TODO
      		this.samples.push(sample);
      	}
      }*/
    );
    BoxParser.createFullBoxCtor("sgpd", function(stream) {
      this.grouping_type = stream.readString(4);
      Log.debug("BoxParser", "Found Sample Groups of type " + this.grouping_type);
      if (this.version === 1) {
        this.default_length = stream.readUint32();
      } else {
        this.default_length = 0;
      }
      if (this.version >= 2) {
        this.default_group_description_index = stream.readUint32();
      }
      this.entries = [];
      var entry_count2 = stream.readUint32();
      for (var i2 = 0; i2 < entry_count2; i2++) {
        var entry;
        if (BoxParser[this.grouping_type + "SampleGroupEntry"]) {
          entry = new BoxParser[this.grouping_type + "SampleGroupEntry"](this.grouping_type);
        } else {
          entry = new BoxParser.SampleGroupEntry(this.grouping_type);
        }
        this.entries.push(entry);
        if (this.version === 1) {
          if (this.default_length === 0) {
            entry.description_length = stream.readUint32();
          } else {
            entry.description_length = this.default_length;
          }
        } else {
          entry.description_length = this.default_length;
        }
        if (entry.write === BoxParser.SampleGroupEntry.prototype.write) {
          Log.info("BoxParser", "SampleGroup for type " + this.grouping_type + " writing not yet implemented, keeping unparsed data in memory for later write");
          entry.data = stream.readUint8Array(entry.description_length);
          stream.position -= entry.description_length;
        }
        entry.parse(stream);
      }
    });
    BoxParser.createFullBoxCtor("sidx", function(stream) {
      this.reference_ID = stream.readUint32();
      this.timescale = stream.readUint32();
      if (this.version === 0) {
        this.earliest_presentation_time = stream.readUint32();
        this.first_offset = stream.readUint32();
      } else {
        this.earliest_presentation_time = stream.readUint64();
        this.first_offset = stream.readUint64();
      }
      stream.readUint16();
      this.references = [];
      var count = stream.readUint16();
      for (var i2 = 0; i2 < count; i2++) {
        var ref = {};
        this.references.push(ref);
        var tmp_32 = stream.readUint32();
        ref.reference_type = tmp_32 >> 31 & 1;
        ref.referenced_size = tmp_32 & 2147483647;
        ref.subsegment_duration = stream.readUint32();
        tmp_32 = stream.readUint32();
        ref.starts_with_SAP = tmp_32 >> 31 & 1;
        ref.SAP_type = tmp_32 >> 28 & 7;
        ref.SAP_delta_time = tmp_32 & 268435455;
      }
    });
    BoxParser.SingleItemTypeReferenceBox = function(type, size, hdr_size, start2) {
      BoxParser.Box.call(this, type, size);
      this.hdr_size = hdr_size;
      this.start = start2;
    };
    BoxParser.SingleItemTypeReferenceBox.prototype = new BoxParser.Box();
    BoxParser.SingleItemTypeReferenceBox.prototype.parse = function(stream) {
      this.from_item_ID = stream.readUint16();
      var count = stream.readUint16();
      this.references = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.references[i2] = {};
        this.references[i2].to_item_ID = stream.readUint16();
      }
    };
    BoxParser.SingleItemTypeReferenceBoxLarge = function(type, size, hdr_size, start2) {
      BoxParser.Box.call(this, type, size);
      this.hdr_size = hdr_size;
      this.start = start2;
    };
    BoxParser.SingleItemTypeReferenceBoxLarge.prototype = new BoxParser.Box();
    BoxParser.SingleItemTypeReferenceBoxLarge.prototype.parse = function(stream) {
      this.from_item_ID = stream.readUint32();
      var count = stream.readUint16();
      this.references = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.references[i2] = {};
        this.references[i2].to_item_ID = stream.readUint32();
      }
    };
    BoxParser.createFullBoxCtor("SmDm", function(stream) {
      this.primaryRChromaticity_x = stream.readUint16();
      this.primaryRChromaticity_y = stream.readUint16();
      this.primaryGChromaticity_x = stream.readUint16();
      this.primaryGChromaticity_y = stream.readUint16();
      this.primaryBChromaticity_x = stream.readUint16();
      this.primaryBChromaticity_y = stream.readUint16();
      this.whitePointChromaticity_x = stream.readUint16();
      this.whitePointChromaticity_y = stream.readUint16();
      this.luminanceMax = stream.readUint32();
      this.luminanceMin = stream.readUint32();
    });
    BoxParser.createFullBoxCtor("smhd", function(stream) {
      this.balance = stream.readUint16();
      stream.readUint16();
    });
    BoxParser.createFullBoxCtor("ssix", function(stream) {
      this.subsegments = [];
      var subsegment_count = stream.readUint32();
      for (var i2 = 0; i2 < subsegment_count; i2++) {
        var subsegment = {};
        this.subsegments.push(subsegment);
        subsegment.ranges = [];
        var range_count = stream.readUint32();
        for (var j3 = 0; j3 < range_count; j3++) {
          var range = {};
          subsegment.ranges.push(range);
          range.level = stream.readUint8();
          range.range_size = stream.readUint24();
        }
      }
    });
    BoxParser.createFullBoxCtor("stco", function(stream) {
      var entry_count2;
      entry_count2 = stream.readUint32();
      this.chunk_offsets = [];
      if (this.version === 0) {
        for (var i2 = 0; i2 < entry_count2; i2++) {
          this.chunk_offsets.push(stream.readUint32());
        }
      }
    });
    BoxParser.createFullBoxCtor("stdp", function(stream) {
      var count = (this.size - this.hdr_size) / 2;
      this.priority = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.priority[i2] = stream.readUint16();
      }
    });
    BoxParser.createFullBoxCtor("sthd");
    BoxParser.createFullBoxCtor("stri", function(stream) {
      this.switch_group = stream.readUint16();
      this.alternate_group = stream.readUint16();
      this.sub_track_id = stream.readUint32();
      var count = (this.size - this.hdr_size - 8) / 4;
      this.attribute_list = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.attribute_list[i2] = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("stsc", function(stream) {
      var entry_count2;
      var i2;
      entry_count2 = stream.readUint32();
      this.first_chunk = [];
      this.samples_per_chunk = [];
      this.sample_description_index = [];
      if (this.version === 0) {
        for (i2 = 0; i2 < entry_count2; i2++) {
          this.first_chunk.push(stream.readUint32());
          this.samples_per_chunk.push(stream.readUint32());
          this.sample_description_index.push(stream.readUint32());
        }
      }
    });
    BoxParser.createFullBoxCtor("stsd", function(stream) {
      var i2;
      var ret2;
      var entryCount;
      var box2;
      this.entries = [];
      entryCount = stream.readUint32();
      for (i2 = 1; i2 <= entryCount; i2++) {
        ret2 = BoxParser.parseOneBox(stream, true, this.size - (stream.getPosition() - this.start));
        if (ret2.code === BoxParser.OK) {
          if (BoxParser[ret2.type + "SampleEntry"]) {
            box2 = new BoxParser[ret2.type + "SampleEntry"](ret2.size);
            box2.hdr_size = ret2.hdr_size;
            box2.start = ret2.start;
          } else {
            Log.warn("BoxParser", "Unknown sample entry type: " + ret2.type);
            box2 = new BoxParser.SampleEntry(ret2.type, ret2.size, ret2.hdr_size, ret2.start);
          }
          if (box2.write === BoxParser.SampleEntry.prototype.write) {
            Log.info("BoxParser", "SampleEntry " + box2.type + " box writing not yet implemented, keeping unparsed data in memory for later write");
            box2.parseDataAndRewind(stream);
          }
          box2.parse(stream);
          this.entries.push(box2);
        } else {
          return;
        }
      }
    });
    BoxParser.createFullBoxCtor("stsg", function(stream) {
      this.grouping_type = stream.readUint32();
      var count = stream.readUint16();
      this.group_description_index = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.group_description_index[i2] = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("stsh", function(stream) {
      var entry_count2;
      var i2;
      entry_count2 = stream.readUint32();
      this.shadowed_sample_numbers = [];
      this.sync_sample_numbers = [];
      if (this.version === 0) {
        for (i2 = 0; i2 < entry_count2; i2++) {
          this.shadowed_sample_numbers.push(stream.readUint32());
          this.sync_sample_numbers.push(stream.readUint32());
        }
      }
    });
    BoxParser.createFullBoxCtor("stss", function(stream) {
      var i2;
      var entry_count2;
      entry_count2 = stream.readUint32();
      if (this.version === 0) {
        this.sample_numbers = [];
        for (i2 = 0; i2 < entry_count2; i2++) {
          this.sample_numbers.push(stream.readUint32());
        }
      }
    });
    BoxParser.createFullBoxCtor("stsz", function(stream) {
      var i2;
      this.sample_sizes = [];
      if (this.version === 0) {
        this.sample_size = stream.readUint32();
        this.sample_count = stream.readUint32();
        for (i2 = 0; i2 < this.sample_count; i2++) {
          if (this.sample_size === 0) {
            this.sample_sizes.push(stream.readUint32());
          } else {
            this.sample_sizes[i2] = this.sample_size;
          }
        }
      }
    });
    BoxParser.createFullBoxCtor("stts", function(stream) {
      var entry_count2;
      var i2;
      var delta;
      entry_count2 = stream.readUint32();
      this.sample_counts = [];
      this.sample_deltas = [];
      if (this.version === 0) {
        for (i2 = 0; i2 < entry_count2; i2++) {
          this.sample_counts.push(stream.readUint32());
          delta = stream.readInt32();
          if (delta < 0) {
            Log.warn("BoxParser", "File uses negative stts sample delta, using value 1 instead, sync may be lost!");
            delta = 1;
          }
          this.sample_deltas.push(delta);
        }
      }
    });
    BoxParser.createFullBoxCtor("stvi", function(stream) {
      var tmp32 = stream.readUint32();
      this.single_view_allowed = tmp32 & 3;
      this.stereo_scheme = stream.readUint32();
      var length = stream.readUint32();
      this.stereo_indication_type = stream.readString(length);
      var ret2;
      var box2;
      this.boxes = [];
      while (stream.getPosition() < this.start + this.size) {
        ret2 = BoxParser.parseOneBox(stream, false, this.size - (stream.getPosition() - this.start));
        if (ret2.code === BoxParser.OK) {
          box2 = ret2.box;
          this.boxes.push(box2);
          this[box2.type] = box2;
        } else {
          return;
        }
      }
    });
    BoxParser.createBoxCtor("styp", function(stream) {
      BoxParser.ftypBox.prototype.parse.call(this, stream);
    });
    BoxParser.createFullBoxCtor("stz2", function(stream) {
      var i2;
      var sample_size;
      var sample_count;
      this.sample_sizes = [];
      if (this.version === 0) {
        this.reserved = stream.readUint24();
        this.field_size = stream.readUint8();
        sample_count = stream.readUint32();
        if (this.field_size === 4) {
          for (i2 = 0; i2 < sample_count; i2 += 2) {
            var tmp = stream.readUint8();
            this.sample_sizes[i2] = tmp >> 4 & 15;
            this.sample_sizes[i2 + 1] = tmp & 15;
          }
        } else if (this.field_size === 8) {
          for (i2 = 0; i2 < sample_count; i2++) {
            this.sample_sizes[i2] = stream.readUint8();
          }
        } else if (this.field_size === 16) {
          for (i2 = 0; i2 < sample_count; i2++) {
            this.sample_sizes[i2] = stream.readUint16();
          }
        } else {
          Log.error("BoxParser", "Error in length field in stz2 box");
        }
      }
    });
    BoxParser.createFullBoxCtor("subs", function(stream) {
      var i2, j3;
      var entry_count2;
      var subsample_count;
      entry_count2 = stream.readUint32();
      this.entries = [];
      for (i2 = 0; i2 < entry_count2; i2++) {
        var sampleInfo = {};
        this.entries[i2] = sampleInfo;
        sampleInfo.sample_delta = stream.readUint32();
        sampleInfo.subsamples = [];
        subsample_count = stream.readUint16();
        if (subsample_count > 0) {
          for (j3 = 0; j3 < subsample_count; j3++) {
            var subsample = {};
            sampleInfo.subsamples.push(subsample);
            if (this.version == 1) {
              subsample.size = stream.readUint32();
            } else {
              subsample.size = stream.readUint16();
            }
            subsample.priority = stream.readUint8();
            subsample.discardable = stream.readUint8();
            subsample.codec_specific_parameters = stream.readUint32();
          }
        }
      }
    });
    BoxParser.createFullBoxCtor("tenc", function(stream) {
      stream.readUint8();
      if (this.version === 0) {
        stream.readUint8();
      } else {
        var tmp = stream.readUint8();
        this.default_crypt_byte_block = tmp >> 4 & 15;
        this.default_skip_byte_block = tmp & 15;
      }
      this.default_isProtected = stream.readUint8();
      this.default_Per_Sample_IV_Size = stream.readUint8();
      this.default_KID = BoxParser.parseHex16(stream);
      if (this.default_isProtected === 1 && this.default_Per_Sample_IV_Size === 0) {
        this.default_constant_IV_size = stream.readUint8();
        this.default_constant_IV = stream.readUint8Array(this.default_constant_IV_size);
      }
    });
    BoxParser.createFullBoxCtor("tfdt", function(stream) {
      if (this.version == 1) {
        this.baseMediaDecodeTime = stream.readUint64();
      } else {
        this.baseMediaDecodeTime = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("tfhd", function(stream) {
      var readBytes = 0;
      this.track_id = stream.readUint32();
      if (this.size - this.hdr_size > readBytes && this.flags & BoxParser.TFHD_FLAG_BASE_DATA_OFFSET) {
        this.base_data_offset = stream.readUint64();
        readBytes += 8;
      } else {
        this.base_data_offset = 0;
      }
      if (this.size - this.hdr_size > readBytes && this.flags & BoxParser.TFHD_FLAG_SAMPLE_DESC) {
        this.default_sample_description_index = stream.readUint32();
        readBytes += 4;
      } else {
        this.default_sample_description_index = 0;
      }
      if (this.size - this.hdr_size > readBytes && this.flags & BoxParser.TFHD_FLAG_SAMPLE_DUR) {
        this.default_sample_duration = stream.readUint32();
        readBytes += 4;
      } else {
        this.default_sample_duration = 0;
      }
      if (this.size - this.hdr_size > readBytes && this.flags & BoxParser.TFHD_FLAG_SAMPLE_SIZE) {
        this.default_sample_size = stream.readUint32();
        readBytes += 4;
      } else {
        this.default_sample_size = 0;
      }
      if (this.size - this.hdr_size > readBytes && this.flags & BoxParser.TFHD_FLAG_SAMPLE_FLAGS) {
        this.default_sample_flags = stream.readUint32();
        readBytes += 4;
      } else {
        this.default_sample_flags = 0;
      }
    });
    BoxParser.createFullBoxCtor("tfra", function(stream) {
      this.track_ID = stream.readUint32();
      stream.readUint24();
      var tmp_byte = stream.readUint8();
      this.length_size_of_traf_num = tmp_byte >> 4 & 3;
      this.length_size_of_trun_num = tmp_byte >> 2 & 3;
      this.length_size_of_sample_num = tmp_byte & 3;
      this.entries = [];
      var number_of_entries = stream.readUint32();
      for (var i2 = 0; i2 < number_of_entries; i2++) {
        if (this.version === 1) {
          this.time = stream.readUint64();
          this.moof_offset = stream.readUint64();
        } else {
          this.time = stream.readUint32();
          this.moof_offset = stream.readUint32();
        }
        this.traf_number = stream["readUint" + 8 * (this.length_size_of_traf_num + 1)]();
        this.trun_number = stream["readUint" + 8 * (this.length_size_of_trun_num + 1)]();
        this.sample_number = stream["readUint" + 8 * (this.length_size_of_sample_num + 1)]();
      }
    });
    BoxParser.createFullBoxCtor("tkhd", function(stream) {
      if (this.version == 1) {
        this.creation_time = stream.readUint64();
        this.modification_time = stream.readUint64();
        this.track_id = stream.readUint32();
        stream.readUint32();
        this.duration = stream.readUint64();
      } else {
        this.creation_time = stream.readUint32();
        this.modification_time = stream.readUint32();
        this.track_id = stream.readUint32();
        stream.readUint32();
        this.duration = stream.readUint32();
      }
      stream.readUint32Array(2);
      this.layer = stream.readInt16();
      this.alternate_group = stream.readInt16();
      this.volume = stream.readInt16() >> 8;
      stream.readUint16();
      this.matrix = stream.readInt32Array(9);
      this.width = stream.readUint32();
      this.height = stream.readUint32();
    });
    BoxParser.createBoxCtor("tmax", function(stream) {
      this.time = stream.readUint32();
    });
    BoxParser.createBoxCtor("tmin", function(stream) {
      this.time = stream.readUint32();
    });
    BoxParser.createBoxCtor("totl", function(stream) {
      this.bytessent = stream.readUint32();
    });
    BoxParser.createBoxCtor("tpay", function(stream) {
      this.bytessent = stream.readUint32();
    });
    BoxParser.createBoxCtor("tpyl", function(stream) {
      this.bytessent = stream.readUint64();
    });
    BoxParser.TrackGroupTypeBox.prototype.parse = function(stream) {
      this.parseFullHeader(stream);
      this.track_group_id = stream.readUint32();
    };
    BoxParser.createTrackGroupCtor("msrc");
    BoxParser.TrackReferenceTypeBox = function(type, size, hdr_size, start2) {
      BoxParser.Box.call(this, type, size);
      this.hdr_size = hdr_size;
      this.start = start2;
    };
    BoxParser.TrackReferenceTypeBox.prototype = new BoxParser.Box();
    BoxParser.TrackReferenceTypeBox.prototype.parse = function(stream) {
      this.track_ids = stream.readUint32Array((this.size - this.hdr_size) / 4);
    };
    BoxParser.trefBox.prototype.parse = function(stream) {
      var ret2;
      var box2;
      while (stream.getPosition() < this.start + this.size) {
        ret2 = BoxParser.parseOneBox(stream, true, this.size - (stream.getPosition() - this.start));
        if (ret2.code === BoxParser.OK) {
          box2 = new BoxParser.TrackReferenceTypeBox(ret2.type, ret2.size, ret2.hdr_size, ret2.start);
          if (box2.write === BoxParser.Box.prototype.write && box2.type !== "mdat") {
            Log.info("BoxParser", "TrackReference " + box2.type + " box writing not yet implemented, keeping unparsed data in memory for later write");
            box2.parseDataAndRewind(stream);
          }
          box2.parse(stream);
          this.boxes.push(box2);
        } else {
          return;
        }
      }
    };
    BoxParser.createFullBoxCtor("trep", function(stream) {
      this.track_ID = stream.readUint32();
      this.boxes = [];
      while (stream.getPosition() < this.start + this.size) {
        ret = BoxParser.parseOneBox(stream, false, this.size - (stream.getPosition() - this.start));
        if (ret.code === BoxParser.OK) {
          box = ret.box;
          this.boxes.push(box);
        } else {
          return;
        }
      }
    });
    BoxParser.createFullBoxCtor("trex", function(stream) {
      this.track_id = stream.readUint32();
      this.default_sample_description_index = stream.readUint32();
      this.default_sample_duration = stream.readUint32();
      this.default_sample_size = stream.readUint32();
      this.default_sample_flags = stream.readUint32();
    });
    BoxParser.createBoxCtor("trpy", function(stream) {
      this.bytessent = stream.readUint64();
    });
    BoxParser.createFullBoxCtor("trun", function(stream) {
      var readBytes = 0;
      this.sample_count = stream.readUint32();
      readBytes += 4;
      if (this.size - this.hdr_size > readBytes && this.flags & BoxParser.TRUN_FLAGS_DATA_OFFSET) {
        this.data_offset = stream.readInt32();
        readBytes += 4;
      } else {
        this.data_offset = 0;
      }
      if (this.size - this.hdr_size > readBytes && this.flags & BoxParser.TRUN_FLAGS_FIRST_FLAG) {
        this.first_sample_flags = stream.readUint32();
        readBytes += 4;
      } else {
        this.first_sample_flags = 0;
      }
      this.sample_duration = [];
      this.sample_size = [];
      this.sample_flags = [];
      this.sample_composition_time_offset = [];
      if (this.size - this.hdr_size > readBytes) {
        for (var i2 = 0; i2 < this.sample_count; i2++) {
          if (this.flags & BoxParser.TRUN_FLAGS_DURATION) {
            this.sample_duration[i2] = stream.readUint32();
          }
          if (this.flags & BoxParser.TRUN_FLAGS_SIZE) {
            this.sample_size[i2] = stream.readUint32();
          }
          if (this.flags & BoxParser.TRUN_FLAGS_FLAGS) {
            this.sample_flags[i2] = stream.readUint32();
          }
          if (this.flags & BoxParser.TRUN_FLAGS_CTS_OFFSET) {
            if (this.version === 0) {
              this.sample_composition_time_offset[i2] = stream.readUint32();
            } else {
              this.sample_composition_time_offset[i2] = stream.readInt32();
            }
          }
        }
      }
    });
    BoxParser.createFullBoxCtor("tsel", function(stream) {
      this.switch_group = stream.readUint32();
      var count = (this.size - this.hdr_size - 4) / 4;
      this.attribute_list = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.attribute_list[i2] = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("txtC", function(stream) {
      this.config = stream.readCString();
    });
    BoxParser.createBoxCtor("tyco", function(stream) {
      var count = (this.size - this.hdr_size) / 4;
      this.compatible_brands = [];
      for (var i2 = 0; i2 < count; i2++) {
        this.compatible_brands[i2] = stream.readString(4);
      }
    });
    BoxParser.createFullBoxCtor("udes", function(stream) {
      this.lang = stream.readCString();
      this.name = stream.readCString();
      this.description = stream.readCString();
      this.tags = stream.readCString();
    });
    BoxParser.createFullBoxCtor("uncC", function(stream) {
      var i2;
      this.profile = stream.readUint32();
      if (this.version == 1) {
      } else if (this.version == 0) {
        this.component_count = stream.readUint32();
        this.component_index = [];
        this.component_bit_depth_minus_one = [];
        this.component_format = [];
        this.component_align_size = [];
        for (i2 = 0; i2 < this.component_count; i2++) {
          this.component_index.push(stream.readUint16());
          this.component_bit_depth_minus_one.push(stream.readUint8());
          this.component_format.push(stream.readUint8());
          this.component_align_size.push(stream.readUint8());
        }
        this.sampling_type = stream.readUint8();
        this.interleave_type = stream.readUint8();
        this.block_size = stream.readUint8();
        var flags = stream.readUint8();
        this.component_little_endian = flags >> 7 & 1;
        this.block_pad_lsb = flags >> 6 & 1;
        this.block_little_endian = flags >> 5 & 1;
        this.block_reversed = flags >> 4 & 1;
        this.pad_unknown = flags >> 3 & 1;
        this.pixel_size = stream.readUint32();
        this.row_align_size = stream.readUint32();
        this.tile_align_size = stream.readUint32();
        this.num_tile_cols_minus_one = stream.readUint32();
        this.num_tile_rows_minus_one = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("url ", function(stream) {
      if (this.flags !== 1) {
        this.location = stream.readCString();
      }
    });
    BoxParser.createFullBoxCtor("urn ", function(stream) {
      this.name = stream.readCString();
      if (this.size - this.hdr_size - this.name.length - 1 > 0) {
        this.location = stream.readCString();
      }
    });
    BoxParser.createUUIDBox("a5d40b30e81411ddba2f0800200c9a66", true, false, function(stream) {
      this.LiveServerManifest = stream.readString(this.size - this.hdr_size).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");
    });
    BoxParser.createUUIDBox("d08a4f1810f34a82b6c832d8aba183d3", true, false, function(stream) {
      this.system_id = BoxParser.parseHex16(stream);
      var datasize = stream.readUint32();
      if (datasize > 0) {
        this.data = stream.readUint8Array(datasize);
      }
    });
    BoxParser.createUUIDBox(
      "a2394f525a9b4f14a2446c427c648df4",
      true,
      false
      /*, function(stream) {
      	if (this.flags & 0x1) {
      		this.AlgorithmID = stream.readUint24();
      		this.IV_size = stream.readUint8();
      		this.KID = BoxParser.parseHex16(stream);
      	}
      	var sample_count = stream.readUint32();
      	this.samples = [];
      	for (var i = 0; i < sample_count; i++) {
      		var sample = {};
      		sample.InitializationVector = this.readUint8Array(this.IV_size*8);
      		if (this.flags & 0x2) {
      			sample.subsamples = [];
      			sample.NumberOfEntries = stream.readUint16();
      			for (var j = 0; j < sample.NumberOfEntries; j++) {
      				var subsample = {};
      				subsample.BytesOfClearData = stream.readUint16();
      				subsample.BytesOfProtectedData = stream.readUint32();
      				sample.subsamples.push(subsample);
      			}
      		}
      		this.samples.push(sample);
      	}
      }*/
    );
    BoxParser.createUUIDBox("8974dbce7be74c5184f97148f9882554", true, false, function(stream) {
      this.default_AlgorithmID = stream.readUint24();
      this.default_IV_size = stream.readUint8();
      this.default_KID = BoxParser.parseHex16(stream);
    });
    BoxParser.createUUIDBox("d4807ef2ca3946958e5426cb9e46a79f", true, false, function(stream) {
      this.fragment_count = stream.readUint8();
      this.entries = [];
      for (var i2 = 0; i2 < this.fragment_count; i2++) {
        var entry = {};
        var absolute_time = 0;
        var absolute_duration = 0;
        if (this.version === 1) {
          absolute_time = stream.readUint64();
          absolute_duration = stream.readUint64();
        } else {
          absolute_time = stream.readUint32();
          absolute_duration = stream.readUint32();
        }
        entry.absolute_time = absolute_time;
        entry.absolute_duration = absolute_duration;
        this.entries.push(entry);
      }
    });
    BoxParser.createUUIDBox("6d1d9b0542d544e680e2141daff757b2", true, false, function(stream) {
      if (this.version === 1) {
        this.absolute_time = stream.readUint64();
        this.duration = stream.readUint64();
      } else {
        this.absolute_time = stream.readUint32();
        this.duration = stream.readUint32();
      }
    });
    BoxParser.createFullBoxCtor("vmhd", function(stream) {
      this.graphicsmode = stream.readUint16();
      this.opcolor = stream.readUint16Array(3);
    });
    BoxParser.createFullBoxCtor("vpcC", function(stream) {
      var tmp;
      if (this.version === 1) {
        this.profile = stream.readUint8();
        this.level = stream.readUint8();
        tmp = stream.readUint8();
        this.bitDepth = tmp >> 4;
        this.chromaSubsampling = tmp >> 1 & 7;
        this.videoFullRangeFlag = tmp & 1;
        this.colourPrimaries = stream.readUint8();
        this.transferCharacteristics = stream.readUint8();
        this.matrixCoefficients = stream.readUint8();
        this.codecIntializationDataSize = stream.readUint16();
        this.codecIntializationData = stream.readUint8Array(this.codecIntializationDataSize);
      } else {
        this.profile = stream.readUint8();
        this.level = stream.readUint8();
        tmp = stream.readUint8();
        this.bitDepth = tmp >> 4 & 15;
        this.colorSpace = tmp & 15;
        tmp = stream.readUint8();
        this.chromaSubsampling = tmp >> 4 & 15;
        this.transferFunction = tmp >> 1 & 7;
        this.videoFullRangeFlag = tmp & 1;
        this.codecIntializationDataSize = stream.readUint16();
        this.codecIntializationData = stream.readUint8Array(this.codecIntializationDataSize);
      }
    });
    BoxParser.createBoxCtor("vttC", function(stream) {
      this.text = stream.readString(this.size - this.hdr_size);
    });
    BoxParser.createFullBoxCtor("vvcC", function(stream) {
      var i2, j3;
      var bitReader = {
        held_bits: void 0,
        num_held_bits: 0,
        stream_read_1_bytes: function(strm2) {
          this.held_bits = strm2.readUint8();
          this.num_held_bits = 1 * 8;
        },
        stream_read_2_bytes: function(strm2) {
          this.held_bits = strm2.readUint16();
          this.num_held_bits = 2 * 8;
        },
        extract_bits: function(num_bits) {
          var ret2 = this.held_bits >> this.num_held_bits - num_bits & (1 << num_bits) - 1;
          this.num_held_bits -= num_bits;
          return ret2;
        }
      };
      bitReader.stream_read_1_bytes(stream);
      bitReader.extract_bits(5);
      this.lengthSizeMinusOne = bitReader.extract_bits(2);
      this.ptl_present_flag = bitReader.extract_bits(1);
      if (this.ptl_present_flag) {
        bitReader.stream_read_2_bytes(stream);
        this.ols_idx = bitReader.extract_bits(9);
        this.num_sublayers = bitReader.extract_bits(3);
        this.constant_frame_rate = bitReader.extract_bits(2);
        this.chroma_format_idc = bitReader.extract_bits(2);
        bitReader.stream_read_1_bytes(stream);
        this.bit_depth_minus8 = bitReader.extract_bits(3);
        bitReader.extract_bits(5);
        {
          bitReader.stream_read_2_bytes(stream);
          bitReader.extract_bits(2);
          this.num_bytes_constraint_info = bitReader.extract_bits(6);
          this.general_profile_idc = bitReader.extract_bits(7);
          this.general_tier_flag = bitReader.extract_bits(1);
          this.general_level_idc = stream.readUint8();
          bitReader.stream_read_1_bytes(stream);
          this.ptl_frame_only_constraint_flag = bitReader.extract_bits(1);
          this.ptl_multilayer_enabled_flag = bitReader.extract_bits(1);
          this.general_constraint_info = new Uint8Array(this.num_bytes_constraint_info);
          if (this.num_bytes_constraint_info) {
            for (i2 = 0; i2 < this.num_bytes_constraint_info - 1; i2++) {
              var cnstr1 = bitReader.extract_bits(6);
              bitReader.stream_read_1_bytes(stream);
              var cnstr2 = bitReader.extract_bits(2);
              this.general_constraint_info[i2] = cnstr1 << 2 | cnstr2;
            }
            this.general_constraint_info[this.num_bytes_constraint_info - 1] = bitReader.extract_bits(6);
          } else {
            bitReader.extract_bits(6);
          }
          if (this.num_sublayers > 1) {
            bitReader.stream_read_1_bytes(stream);
            this.ptl_sublayer_present_mask = 0;
            for (j3 = this.num_sublayers - 2; j3 >= 0; --j3) {
              var val = bitReader.extract_bits(1);
              this.ptl_sublayer_present_mask |= val << j3;
            }
            for (j3 = this.num_sublayers; j3 <= 8 && this.num_sublayers > 1; ++j3) {
              bitReader.extract_bits(1);
            }
            this.sublayer_level_idc = [];
            for (j3 = this.num_sublayers - 2; j3 >= 0; --j3) {
              if (this.ptl_sublayer_present_mask & 1 << j3) {
                this.sublayer_level_idc[j3] = stream.readUint8();
              }
            }
          }
          this.ptl_num_sub_profiles = stream.readUint8();
          this.general_sub_profile_idc = [];
          if (this.ptl_num_sub_profiles) {
            for (i2 = 0; i2 < this.ptl_num_sub_profiles; i2++) {
              this.general_sub_profile_idc.push(stream.readUint32());
            }
          }
        }
        this.max_picture_width = stream.readUint16();
        this.max_picture_height = stream.readUint16();
        this.avg_frame_rate = stream.readUint16();
      }
      var VVC_NALU_OPI = 12;
      var VVC_NALU_DEC_PARAM = 13;
      this.nalu_arrays = [];
      var num_of_arrays = stream.readUint8();
      for (i2 = 0; i2 < num_of_arrays; i2++) {
        var nalu_array = [];
        this.nalu_arrays.push(nalu_array);
        bitReader.stream_read_1_bytes(stream);
        nalu_array.completeness = bitReader.extract_bits(1);
        bitReader.extract_bits(2);
        nalu_array.nalu_type = bitReader.extract_bits(5);
        var numNalus = 1;
        if (nalu_array.nalu_type != VVC_NALU_DEC_PARAM && nalu_array.nalu_type != VVC_NALU_OPI) {
          numNalus = stream.readUint16();
        }
        for (j3 = 0; j3 < numNalus; j3++) {
          var len = stream.readUint16();
          nalu_array.push({
            data: stream.readUint8Array(len),
            length: len
          });
        }
      }
    });
    BoxParser.createFullBoxCtor("vvnC", function(stream) {
      var tmp = strm.readUint8();
      this.lengthSizeMinusOne = tmp & 3;
    });
    BoxParser.SampleEntry.prototype.isVideo = function() {
      return false;
    };
    BoxParser.SampleEntry.prototype.isAudio = function() {
      return false;
    };
    BoxParser.SampleEntry.prototype.isSubtitle = function() {
      return false;
    };
    BoxParser.SampleEntry.prototype.isMetadata = function() {
      return false;
    };
    BoxParser.SampleEntry.prototype.isHint = function() {
      return false;
    };
    BoxParser.SampleEntry.prototype.getCodec = function() {
      return this.type.replace(".", "");
    };
    BoxParser.SampleEntry.prototype.getWidth = function() {
      return "";
    };
    BoxParser.SampleEntry.prototype.getHeight = function() {
      return "";
    };
    BoxParser.SampleEntry.prototype.getChannelCount = function() {
      return "";
    };
    BoxParser.SampleEntry.prototype.getSampleRate = function() {
      return "";
    };
    BoxParser.SampleEntry.prototype.getSampleSize = function() {
      return "";
    };
    BoxParser.VisualSampleEntry.prototype.isVideo = function() {
      return true;
    };
    BoxParser.VisualSampleEntry.prototype.getWidth = function() {
      return this.width;
    };
    BoxParser.VisualSampleEntry.prototype.getHeight = function() {
      return this.height;
    };
    BoxParser.AudioSampleEntry.prototype.isAudio = function() {
      return true;
    };
    BoxParser.AudioSampleEntry.prototype.getChannelCount = function() {
      return this.channel_count;
    };
    BoxParser.AudioSampleEntry.prototype.getSampleRate = function() {
      return this.samplerate;
    };
    BoxParser.AudioSampleEntry.prototype.getSampleSize = function() {
      return this.samplesize;
    };
    BoxParser.SubtitleSampleEntry.prototype.isSubtitle = function() {
      return true;
    };
    BoxParser.MetadataSampleEntry.prototype.isMetadata = function() {
      return true;
    };
    BoxParser.decimalToHex = function(d2, padding) {
      var hex2 = Number(d2).toString(16);
      padding = typeof padding === "undefined" || padding === null ? padding = 2 : padding;
      while (hex2.length < padding) {
        hex2 = "0" + hex2;
      }
      return hex2;
    };
    BoxParser.avc1SampleEntry.prototype.getCodec = BoxParser.avc2SampleEntry.prototype.getCodec = BoxParser.avc3SampleEntry.prototype.getCodec = BoxParser.avc4SampleEntry.prototype.getCodec = function() {
      var baseCodec = BoxParser.SampleEntry.prototype.getCodec.call(this);
      if (this.avcC) {
        return baseCodec + "." + BoxParser.decimalToHex(this.avcC.AVCProfileIndication) + BoxParser.decimalToHex(this.avcC.profile_compatibility) + BoxParser.decimalToHex(this.avcC.AVCLevelIndication);
      } else {
        return baseCodec;
      }
    };
    BoxParser.hev1SampleEntry.prototype.getCodec = BoxParser.hvc1SampleEntry.prototype.getCodec = function() {
      var i2;
      var baseCodec = BoxParser.SampleEntry.prototype.getCodec.call(this);
      if (this.hvcC) {
        baseCodec += ".";
        switch (this.hvcC.general_profile_space) {
          case 0:
            baseCodec += "";
            break;
          case 1:
            baseCodec += "A";
            break;
          case 2:
            baseCodec += "B";
            break;
          case 3:
            baseCodec += "C";
            break;
        }
        baseCodec += this.hvcC.general_profile_idc;
        baseCodec += ".";
        var val = this.hvcC.general_profile_compatibility;
        var reversed = 0;
        for (i2 = 0; i2 < 32; i2++) {
          reversed |= val & 1;
          if (i2 == 31) break;
          reversed <<= 1;
          val >>= 1;
        }
        baseCodec += BoxParser.decimalToHex(reversed, 0);
        baseCodec += ".";
        if (this.hvcC.general_tier_flag === 0) {
          baseCodec += "L";
        } else {
          baseCodec += "H";
        }
        baseCodec += this.hvcC.general_level_idc;
        var hasByte = false;
        var constraint_string = "";
        for (i2 = 5; i2 >= 0; i2--) {
          if (this.hvcC.general_constraint_indicator[i2] || hasByte) {
            constraint_string = "." + BoxParser.decimalToHex(this.hvcC.general_constraint_indicator[i2], 0) + constraint_string;
            hasByte = true;
          }
        }
        baseCodec += constraint_string;
      }
      return baseCodec;
    };
    BoxParser.vvc1SampleEntry.prototype.getCodec = BoxParser.vvi1SampleEntry.prototype.getCodec = function() {
      var i2;
      var baseCodec = BoxParser.SampleEntry.prototype.getCodec.call(this);
      if (this.vvcC) {
        baseCodec += "." + this.vvcC.general_profile_idc;
        if (this.vvcC.general_tier_flag) {
          baseCodec += ".H";
        } else {
          baseCodec += ".L";
        }
        baseCodec += this.vvcC.general_level_idc;
        var constraint_string = "";
        if (this.vvcC.general_constraint_info) {
          var bytes = [];
          var byte = 0;
          byte |= this.vvcC.ptl_frame_only_constraint << 7;
          byte |= this.vvcC.ptl_multilayer_enabled << 6;
          var last_nonzero;
          for (i2 = 0; i2 < this.vvcC.general_constraint_info.length; ++i2) {
            byte |= this.vvcC.general_constraint_info[i2] >> 2 & 63;
            bytes.push(byte);
            if (byte) {
              last_nonzero = i2;
            }
            byte = this.vvcC.general_constraint_info[i2] >> 2 & 3;
          }
          if (last_nonzero === void 0) {
            constraint_string = ".CA";
          } else {
            constraint_string = ".C";
            var base32_chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ234567";
            var held_bits = 0;
            var num_held_bits = 0;
            for (i2 = 0; i2 <= last_nonzero; ++i2) {
              held_bits = held_bits << 8 | bytes[i2];
              num_held_bits += 8;
              while (num_held_bits >= 5) {
                var val = held_bits >> num_held_bits - 5 & 31;
                constraint_string += base32_chars[val];
                num_held_bits -= 5;
                held_bits &= (1 << num_held_bits) - 1;
              }
            }
            if (num_held_bits) {
              held_bits <<= 5 - num_held_bits;
              constraint_string += base32_chars[held_bits & 31];
            }
          }
        }
        baseCodec += constraint_string;
      }
      return baseCodec;
    };
    BoxParser.mp4aSampleEntry.prototype.getCodec = function() {
      var baseCodec = BoxParser.SampleEntry.prototype.getCodec.call(this);
      if (this.esds && this.esds.esd) {
        var oti = this.esds.esd.getOTI();
        var dsi = this.esds.esd.getAudioConfig();
        return baseCodec + "." + BoxParser.decimalToHex(oti) + (dsi ? "." + dsi : "");
      } else {
        return baseCodec;
      }
    };
    BoxParser.stxtSampleEntry.prototype.getCodec = function() {
      var baseCodec = BoxParser.SampleEntry.prototype.getCodec.call(this);
      if (this.mime_format) {
        return baseCodec + "." + this.mime_format;
      } else {
        return baseCodec;
      }
    };
    BoxParser.vp08SampleEntry.prototype.getCodec = BoxParser.vp09SampleEntry.prototype.getCodec = function() {
      var baseCodec = BoxParser.SampleEntry.prototype.getCodec.call(this);
      var level = this.vpcC.level;
      if (level == 0) {
        level = "00";
      }
      var bitDepth = this.vpcC.bitDepth;
      if (bitDepth == 8) {
        bitDepth = "08";
      }
      return baseCodec + ".0" + this.vpcC.profile + "." + level + "." + bitDepth;
    };
    BoxParser.av01SampleEntry.prototype.getCodec = function() {
      var baseCodec = BoxParser.SampleEntry.prototype.getCodec.call(this);
      var level = this.av1C.seq_level_idx_0;
      if (level < 10) {
        level = "0" + level;
      }
      var bitdepth;
      if (this.av1C.seq_profile === 2 && this.av1C.high_bitdepth === 1) {
        bitdepth = this.av1C.twelve_bit === 1 ? "12" : "10";
      } else if (this.av1C.seq_profile <= 2) {
        bitdepth = this.av1C.high_bitdepth === 1 ? "10" : "08";
      }
      return baseCodec + "." + this.av1C.seq_profile + "." + level + (this.av1C.seq_tier_0 ? "H" : "M") + "." + bitdepth;
    };
    BoxParser.Box.prototype.writeHeader = function(stream, msg) {
      this.size += 8;
      if (this.size > MAX_SIZE) {
        this.size += 8;
      }
      if (this.type === "uuid") {
        this.size += 16;
      }
      Log.debug("BoxWriter", "Writing box " + this.type + " of size: " + this.size + " at position " + stream.getPosition() + (msg || ""));
      if (this.size > MAX_SIZE) {
        stream.writeUint32(1);
      } else {
        this.sizePosition = stream.getPosition();
        stream.writeUint32(this.size);
      }
      stream.writeString(this.type, null, 4);
      if (this.type === "uuid") {
        stream.writeUint8Array(this.uuid);
      }
      if (this.size > MAX_SIZE) {
        stream.writeUint64(this.size);
      }
    };
    BoxParser.FullBox.prototype.writeHeader = function(stream) {
      this.size += 4;
      BoxParser.Box.prototype.writeHeader.call(this, stream, " v=" + this.version + " f=" + this.flags);
      stream.writeUint8(this.version);
      stream.writeUint24(this.flags);
    };
    BoxParser.Box.prototype.write = function(stream) {
      if (this.type === "mdat") {
        if (this.data) {
          this.size = this.data.length;
          this.writeHeader(stream);
          stream.writeUint8Array(this.data);
        }
      } else {
        this.size = this.data ? this.data.length : 0;
        this.writeHeader(stream);
        if (this.data) {
          stream.writeUint8Array(this.data);
        }
      }
    };
    BoxParser.ContainerBox.prototype.write = function(stream) {
      this.size = 0;
      this.writeHeader(stream);
      for (var i2 = 0; i2 < this.boxes.length; i2++) {
        if (this.boxes[i2]) {
          this.boxes[i2].write(stream);
          this.size += this.boxes[i2].size;
        }
      }
      Log.debug("BoxWriter", "Adjusting box " + this.type + " with new size " + this.size);
      stream.adjustUint32(this.sizePosition, this.size);
    };
    BoxParser.TrackReferenceTypeBox.prototype.write = function(stream) {
      this.size = this.track_ids.length * 4;
      this.writeHeader(stream);
      stream.writeUint32Array(this.track_ids);
    };
    BoxParser.avcCBox.prototype.write = function(stream) {
      var i2;
      this.size = 7;
      for (i2 = 0; i2 < this.SPS.length; i2++) {
        this.size += 2 + this.SPS[i2].length;
      }
      for (i2 = 0; i2 < this.PPS.length; i2++) {
        this.size += 2 + this.PPS[i2].length;
      }
      if (this.ext) {
        this.size += this.ext.length;
      }
      this.writeHeader(stream);
      stream.writeUint8(this.configurationVersion);
      stream.writeUint8(this.AVCProfileIndication);
      stream.writeUint8(this.profile_compatibility);
      stream.writeUint8(this.AVCLevelIndication);
      stream.writeUint8(this.lengthSizeMinusOne + (63 << 2));
      stream.writeUint8(this.SPS.length + (7 << 5));
      for (i2 = 0; i2 < this.SPS.length; i2++) {
        stream.writeUint16(this.SPS[i2].length);
        stream.writeUint8Array(this.SPS[i2].nalu);
      }
      stream.writeUint8(this.PPS.length);
      for (i2 = 0; i2 < this.PPS.length; i2++) {
        stream.writeUint16(this.PPS[i2].length);
        stream.writeUint8Array(this.PPS[i2].nalu);
      }
      if (this.ext) {
        stream.writeUint8Array(this.ext);
      }
    };
    BoxParser.co64Box.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 8 * this.chunk_offsets.length;
      this.writeHeader(stream);
      stream.writeUint32(this.chunk_offsets.length);
      for (i2 = 0; i2 < this.chunk_offsets.length; i2++) {
        stream.writeUint64(this.chunk_offsets[i2]);
      }
    };
    BoxParser.cslgBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 0;
      this.size = 4 * 5;
      this.writeHeader(stream);
      stream.writeInt32(this.compositionToDTSShift);
      stream.writeInt32(this.leastDecodeToDisplayDelta);
      stream.writeInt32(this.greatestDecodeToDisplayDelta);
      stream.writeInt32(this.compositionStartTime);
      stream.writeInt32(this.compositionEndTime);
    };
    BoxParser.cttsBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 8 * this.sample_counts.length;
      this.writeHeader(stream);
      stream.writeUint32(this.sample_counts.length);
      for (i2 = 0; i2 < this.sample_counts.length; i2++) {
        stream.writeUint32(this.sample_counts[i2]);
        if (this.version === 1) {
          stream.writeInt32(this.sample_offsets[i2]);
        } else {
          stream.writeUint32(this.sample_offsets[i2]);
        }
      }
    };
    BoxParser.drefBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4;
      this.writeHeader(stream);
      stream.writeUint32(this.entries.length);
      for (var i2 = 0; i2 < this.entries.length; i2++) {
        this.entries[i2].write(stream);
        this.size += this.entries[i2].size;
      }
      Log.debug("BoxWriter", "Adjusting box " + this.type + " with new size " + this.size);
      stream.adjustUint32(this.sizePosition, this.size);
    };
    BoxParser.elngBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = this.extended_language.length;
      this.writeHeader(stream);
      stream.writeString(this.extended_language);
    };
    BoxParser.elstBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 12 * this.entries.length;
      this.writeHeader(stream);
      stream.writeUint32(this.entries.length);
      for (var i2 = 0; i2 < this.entries.length; i2++) {
        var entry = this.entries[i2];
        stream.writeUint32(entry.segment_duration);
        stream.writeInt32(entry.media_time);
        stream.writeInt16(entry.media_rate_integer);
        stream.writeInt16(entry.media_rate_fraction);
      }
    };
    BoxParser.emsgBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4 * 4 + this.message_data.length + (this.scheme_id_uri.length + 1) + (this.value.length + 1);
      this.writeHeader(stream);
      stream.writeCString(this.scheme_id_uri);
      stream.writeCString(this.value);
      stream.writeUint32(this.timescale);
      stream.writeUint32(this.presentation_time_delta);
      stream.writeUint32(this.event_duration);
      stream.writeUint32(this.id);
      stream.writeUint8Array(this.message_data);
    };
    BoxParser.ftypBox.prototype.write = function(stream) {
      this.size = 8 + 4 * this.compatible_brands.length;
      this.writeHeader(stream);
      stream.writeString(this.major_brand, null, 4);
      stream.writeUint32(this.minor_version);
      for (var i2 = 0; i2 < this.compatible_brands.length; i2++) {
        stream.writeString(this.compatible_brands[i2], null, 4);
      }
    };
    BoxParser.hdlrBox.prototype.write = function(stream) {
      this.size = 5 * 4 + this.name.length + 1;
      this.version = 0;
      this.flags = 0;
      this.writeHeader(stream);
      stream.writeUint32(0);
      stream.writeString(this.handler, null, 4);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeCString(this.name);
    };
    BoxParser.hvcCBox.prototype.write = function(stream) {
      var i2, j3;
      this.size = 23;
      for (i2 = 0; i2 < this.nalu_arrays.length; i2++) {
        this.size += 3;
        for (j3 = 0; j3 < this.nalu_arrays[i2].length; j3++) {
          this.size += 2 + this.nalu_arrays[i2][j3].data.length;
        }
      }
      this.writeHeader(stream);
      stream.writeUint8(this.configurationVersion);
      stream.writeUint8((this.general_profile_space << 6) + (this.general_tier_flag << 5) + this.general_profile_idc);
      stream.writeUint32(this.general_profile_compatibility);
      stream.writeUint8Array(this.general_constraint_indicator);
      stream.writeUint8(this.general_level_idc);
      stream.writeUint16(this.min_spatial_segmentation_idc + (15 << 24));
      stream.writeUint8(this.parallelismType + (63 << 2));
      stream.writeUint8(this.chroma_format_idc + (63 << 2));
      stream.writeUint8(this.bit_depth_luma_minus8 + (31 << 3));
      stream.writeUint8(this.bit_depth_chroma_minus8 + (31 << 3));
      stream.writeUint16(this.avgFrameRate);
      stream.writeUint8((this.constantFrameRate << 6) + (this.numTemporalLayers << 3) + (this.temporalIdNested << 2) + this.lengthSizeMinusOne);
      stream.writeUint8(this.nalu_arrays.length);
      for (i2 = 0; i2 < this.nalu_arrays.length; i2++) {
        stream.writeUint8((this.nalu_arrays[i2].completeness << 7) + this.nalu_arrays[i2].nalu_type);
        stream.writeUint16(this.nalu_arrays[i2].length);
        for (j3 = 0; j3 < this.nalu_arrays[i2].length; j3++) {
          stream.writeUint16(this.nalu_arrays[i2][j3].data.length);
          stream.writeUint8Array(this.nalu_arrays[i2][j3].data);
        }
      }
    };
    BoxParser.kindBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = this.schemeURI.length + 1 + (this.value.length + 1);
      this.writeHeader(stream);
      stream.writeCString(this.schemeURI);
      stream.writeCString(this.value);
    };
    BoxParser.mdhdBox.prototype.write = function(stream) {
      this.size = 4 * 4 + 2 * 2;
      this.flags = 0;
      this.version = 0;
      this.writeHeader(stream);
      stream.writeUint32(this.creation_time);
      stream.writeUint32(this.modification_time);
      stream.writeUint32(this.timescale);
      stream.writeUint32(this.duration);
      stream.writeUint16(this.language);
      stream.writeUint16(0);
    };
    BoxParser.mehdBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4;
      this.writeHeader(stream);
      stream.writeUint32(this.fragment_duration);
    };
    BoxParser.mfhdBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4;
      this.writeHeader(stream);
      stream.writeUint32(this.sequence_number);
    };
    BoxParser.mvhdBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 23 * 4 + 2 * 2;
      this.writeHeader(stream);
      stream.writeUint32(this.creation_time);
      stream.writeUint32(this.modification_time);
      stream.writeUint32(this.timescale);
      stream.writeUint32(this.duration);
      stream.writeUint32(this.rate);
      stream.writeUint16(this.volume << 8);
      stream.writeUint16(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32Array(this.matrix);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32(this.next_track_id);
    };
    BoxParser.SampleEntry.prototype.writeHeader = function(stream) {
      this.size = 8;
      BoxParser.Box.prototype.writeHeader.call(this, stream);
      stream.writeUint8(0);
      stream.writeUint8(0);
      stream.writeUint8(0);
      stream.writeUint8(0);
      stream.writeUint8(0);
      stream.writeUint8(0);
      stream.writeUint16(this.data_reference_index);
    };
    BoxParser.SampleEntry.prototype.writeFooter = function(stream) {
      for (var i2 = 0; i2 < this.boxes.length; i2++) {
        this.boxes[i2].write(stream);
        this.size += this.boxes[i2].size;
      }
      Log.debug("BoxWriter", "Adjusting box " + this.type + " with new size " + this.size);
      stream.adjustUint32(this.sizePosition, this.size);
    };
    BoxParser.SampleEntry.prototype.write = function(stream) {
      this.writeHeader(stream);
      stream.writeUint8Array(this.data);
      this.size += this.data.length;
      Log.debug("BoxWriter", "Adjusting box " + this.type + " with new size " + this.size);
      stream.adjustUint32(this.sizePosition, this.size);
    };
    BoxParser.VisualSampleEntry.prototype.write = function(stream) {
      this.writeHeader(stream);
      this.size += 2 * 7 + 6 * 4 + 32;
      stream.writeUint16(0);
      stream.writeUint16(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint16(this.width);
      stream.writeUint16(this.height);
      stream.writeUint32(this.horizresolution);
      stream.writeUint32(this.vertresolution);
      stream.writeUint32(0);
      stream.writeUint16(this.frame_count);
      stream.writeUint8(Math.min(31, this.compressorname.length));
      stream.writeString(this.compressorname, null, 31);
      stream.writeUint16(this.depth);
      stream.writeInt16(-1);
      this.writeFooter(stream);
    };
    BoxParser.AudioSampleEntry.prototype.write = function(stream) {
      this.writeHeader(stream);
      this.size += 2 * 4 + 3 * 4;
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeUint16(this.channel_count);
      stream.writeUint16(this.samplesize);
      stream.writeUint16(0);
      stream.writeUint16(0);
      stream.writeUint32(this.samplerate << 16);
      this.writeFooter(stream);
    };
    BoxParser.stppSampleEntry.prototype.write = function(stream) {
      this.writeHeader(stream);
      this.size += this.namespace.length + 1 + this.schema_location.length + 1 + this.auxiliary_mime_types.length + 1;
      stream.writeCString(this.namespace);
      stream.writeCString(this.schema_location);
      stream.writeCString(this.auxiliary_mime_types);
      this.writeFooter(stream);
    };
    BoxParser.SampleGroupEntry.prototype.write = function(stream) {
      stream.writeUint8Array(this.data);
    };
    BoxParser.sbgpBox.prototype.write = function(stream) {
      this.version = 1;
      this.flags = 0;
      this.size = 12 + 8 * this.entries.length;
      this.writeHeader(stream);
      stream.writeString(this.grouping_type, null, 4);
      stream.writeUint32(this.grouping_type_parameter);
      stream.writeUint32(this.entries.length);
      for (var i2 = 0; i2 < this.entries.length; i2++) {
        var entry = this.entries[i2];
        stream.writeInt32(entry.sample_count);
        stream.writeInt32(entry.group_description_index);
      }
    };
    BoxParser.sgpdBox.prototype.write = function(stream) {
      var i2;
      var entry;
      this.flags = 0;
      this.size = 12;
      for (i2 = 0; i2 < this.entries.length; i2++) {
        entry = this.entries[i2];
        if (this.version === 1) {
          if (this.default_length === 0) {
            this.size += 4;
          }
          this.size += entry.data.length;
        }
      }
      this.writeHeader(stream);
      stream.writeString(this.grouping_type, null, 4);
      if (this.version === 1) {
        stream.writeUint32(this.default_length);
      }
      if (this.version >= 2) {
        stream.writeUint32(this.default_sample_description_index);
      }
      stream.writeUint32(this.entries.length);
      for (i2 = 0; i2 < this.entries.length; i2++) {
        entry = this.entries[i2];
        if (this.version === 1) {
          if (this.default_length === 0) {
            stream.writeUint32(entry.description_length);
          }
        }
        entry.write(stream);
      }
    };
    BoxParser.sidxBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4 * 4 + 2 + 2 + 12 * this.references.length;
      this.writeHeader(stream);
      stream.writeUint32(this.reference_ID);
      stream.writeUint32(this.timescale);
      stream.writeUint32(this.earliest_presentation_time);
      stream.writeUint32(this.first_offset);
      stream.writeUint16(0);
      stream.writeUint16(this.references.length);
      for (var i2 = 0; i2 < this.references.length; i2++) {
        var ref = this.references[i2];
        stream.writeUint32(ref.reference_type << 31 | ref.referenced_size);
        stream.writeUint32(ref.subsegment_duration);
        stream.writeUint32(ref.starts_with_SAP << 31 | ref.SAP_type << 28 | ref.SAP_delta_time);
      }
    };
    BoxParser.smhdBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 1;
      this.size = 4;
      this.writeHeader(stream);
      stream.writeUint16(this.balance);
      stream.writeUint16(0);
    };
    BoxParser.stcoBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 4 * this.chunk_offsets.length;
      this.writeHeader(stream);
      stream.writeUint32(this.chunk_offsets.length);
      stream.writeUint32Array(this.chunk_offsets);
    };
    BoxParser.stscBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 12 * this.first_chunk.length;
      this.writeHeader(stream);
      stream.writeUint32(this.first_chunk.length);
      for (i2 = 0; i2 < this.first_chunk.length; i2++) {
        stream.writeUint32(this.first_chunk[i2]);
        stream.writeUint32(this.samples_per_chunk[i2]);
        stream.writeUint32(this.sample_description_index[i2]);
      }
    };
    BoxParser.stsdBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 0;
      this.size = 0;
      this.writeHeader(stream);
      stream.writeUint32(this.entries.length);
      this.size += 4;
      for (i2 = 0; i2 < this.entries.length; i2++) {
        this.entries[i2].write(stream);
        this.size += this.entries[i2].size;
      }
      Log.debug("BoxWriter", "Adjusting box " + this.type + " with new size " + this.size);
      stream.adjustUint32(this.sizePosition, this.size);
    };
    BoxParser.stshBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 8 * this.shadowed_sample_numbers.length;
      this.writeHeader(stream);
      stream.writeUint32(this.shadowed_sample_numbers.length);
      for (i2 = 0; i2 < this.shadowed_sample_numbers.length; i2++) {
        stream.writeUint32(this.shadowed_sample_numbers[i2]);
        stream.writeUint32(this.sync_sample_numbers[i2]);
      }
    };
    BoxParser.stssBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 4 * this.sample_numbers.length;
      this.writeHeader(stream);
      stream.writeUint32(this.sample_numbers.length);
      stream.writeUint32Array(this.sample_numbers);
    };
    BoxParser.stszBox.prototype.write = function(stream) {
      var i2;
      var constant = true;
      this.version = 0;
      this.flags = 0;
      if (this.sample_sizes.length > 0) {
        i2 = 0;
        while (i2 + 1 < this.sample_sizes.length) {
          if (this.sample_sizes[i2 + 1] !== this.sample_sizes[0]) {
            constant = false;
            break;
          } else {
            i2++;
          }
        }
      } else {
        constant = false;
      }
      this.size = 8;
      if (!constant) {
        this.size += 4 * this.sample_sizes.length;
      }
      this.writeHeader(stream);
      if (!constant) {
        stream.writeUint32(0);
      } else {
        stream.writeUint32(this.sample_sizes[0]);
      }
      stream.writeUint32(this.sample_sizes.length);
      if (!constant) {
        stream.writeUint32Array(this.sample_sizes);
      }
    };
    BoxParser.sttsBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 0;
      this.size = 4 + 8 * this.sample_counts.length;
      this.writeHeader(stream);
      stream.writeUint32(this.sample_counts.length);
      for (i2 = 0; i2 < this.sample_counts.length; i2++) {
        stream.writeUint32(this.sample_counts[i2]);
        stream.writeUint32(this.sample_deltas[i2]);
      }
    };
    BoxParser.tfdtBox.prototype.write = function(stream) {
      var UINT32_MAX = Math.pow(2, 32) - 1;
      this.version = this.baseMediaDecodeTime > UINT32_MAX ? 1 : 0;
      this.flags = 0;
      this.size = 4;
      if (this.version === 1) {
        this.size += 4;
      }
      this.writeHeader(stream);
      if (this.version === 1) {
        stream.writeUint64(this.baseMediaDecodeTime);
      } else {
        stream.writeUint32(this.baseMediaDecodeTime);
      }
    };
    BoxParser.tfhdBox.prototype.write = function(stream) {
      this.version = 0;
      this.size = 4;
      if (this.flags & BoxParser.TFHD_FLAG_BASE_DATA_OFFSET) {
        this.size += 8;
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_DESC) {
        this.size += 4;
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_DUR) {
        this.size += 4;
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_SIZE) {
        this.size += 4;
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_FLAGS) {
        this.size += 4;
      }
      this.writeHeader(stream);
      stream.writeUint32(this.track_id);
      if (this.flags & BoxParser.TFHD_FLAG_BASE_DATA_OFFSET) {
        stream.writeUint64(this.base_data_offset);
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_DESC) {
        stream.writeUint32(this.default_sample_description_index);
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_DUR) {
        stream.writeUint32(this.default_sample_duration);
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_SIZE) {
        stream.writeUint32(this.default_sample_size);
      }
      if (this.flags & BoxParser.TFHD_FLAG_SAMPLE_FLAGS) {
        stream.writeUint32(this.default_sample_flags);
      }
    };
    BoxParser.tkhdBox.prototype.write = function(stream) {
      this.version = 0;
      this.size = 4 * 18 + 2 * 4;
      this.writeHeader(stream);
      stream.writeUint32(this.creation_time);
      stream.writeUint32(this.modification_time);
      stream.writeUint32(this.track_id);
      stream.writeUint32(0);
      stream.writeUint32(this.duration);
      stream.writeUint32(0);
      stream.writeUint32(0);
      stream.writeInt16(this.layer);
      stream.writeInt16(this.alternate_group);
      stream.writeInt16(this.volume << 8);
      stream.writeUint16(0);
      stream.writeInt32Array(this.matrix);
      stream.writeUint32(this.width);
      stream.writeUint32(this.height);
    };
    BoxParser.trexBox.prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = 4 * 5;
      this.writeHeader(stream);
      stream.writeUint32(this.track_id);
      stream.writeUint32(this.default_sample_description_index);
      stream.writeUint32(this.default_sample_duration);
      stream.writeUint32(this.default_sample_size);
      stream.writeUint32(this.default_sample_flags);
    };
    BoxParser.trunBox.prototype.write = function(stream) {
      this.version = 0;
      this.size = 4;
      if (this.flags & BoxParser.TRUN_FLAGS_DATA_OFFSET) {
        this.size += 4;
      }
      if (this.flags & BoxParser.TRUN_FLAGS_FIRST_FLAG) {
        this.size += 4;
      }
      if (this.flags & BoxParser.TRUN_FLAGS_DURATION) {
        this.size += 4 * this.sample_duration.length;
      }
      if (this.flags & BoxParser.TRUN_FLAGS_SIZE) {
        this.size += 4 * this.sample_size.length;
      }
      if (this.flags & BoxParser.TRUN_FLAGS_FLAGS) {
        this.size += 4 * this.sample_flags.length;
      }
      if (this.flags & BoxParser.TRUN_FLAGS_CTS_OFFSET) {
        this.size += 4 * this.sample_composition_time_offset.length;
      }
      this.writeHeader(stream);
      stream.writeUint32(this.sample_count);
      if (this.flags & BoxParser.TRUN_FLAGS_DATA_OFFSET) {
        this.data_offset_position = stream.getPosition();
        stream.writeInt32(this.data_offset);
      }
      if (this.flags & BoxParser.TRUN_FLAGS_FIRST_FLAG) {
        stream.writeUint32(this.first_sample_flags);
      }
      for (var i2 = 0; i2 < this.sample_count; i2++) {
        if (this.flags & BoxParser.TRUN_FLAGS_DURATION) {
          stream.writeUint32(this.sample_duration[i2]);
        }
        if (this.flags & BoxParser.TRUN_FLAGS_SIZE) {
          stream.writeUint32(this.sample_size[i2]);
        }
        if (this.flags & BoxParser.TRUN_FLAGS_FLAGS) {
          stream.writeUint32(this.sample_flags[i2]);
        }
        if (this.flags & BoxParser.TRUN_FLAGS_CTS_OFFSET) {
          if (this.version === 0) {
            stream.writeUint32(this.sample_composition_time_offset[i2]);
          } else {
            stream.writeInt32(this.sample_composition_time_offset[i2]);
          }
        }
      }
    };
    BoxParser["url Box"].prototype.write = function(stream) {
      this.version = 0;
      if (this.location) {
        this.flags = 0;
        this.size = this.location.length + 1;
      } else {
        this.flags = 1;
        this.size = 0;
      }
      this.writeHeader(stream);
      if (this.location) {
        stream.writeCString(this.location);
      }
    };
    BoxParser["urn Box"].prototype.write = function(stream) {
      this.version = 0;
      this.flags = 0;
      this.size = this.name.length + 1 + (this.location ? this.location.length + 1 : 0);
      this.writeHeader(stream);
      stream.writeCString(this.name);
      if (this.location) {
        stream.writeCString(this.location);
      }
    };
    BoxParser.vmhdBox.prototype.write = function(stream) {
      var i2;
      this.version = 0;
      this.flags = 1;
      this.size = 8;
      this.writeHeader(stream);
      stream.writeUint16(this.graphicsmode);
      stream.writeUint16Array(this.opcolor);
    };
    BoxParser.vpcCBox.prototype.write = function(stream) {
      this.version = 1;
      const bodySize = 8 + this.codecIntializationDataSize;
      this.size = bodySize;
      this.writeHeader(stream);
      stream.writeUint8(this.profile);
      stream.writeUint8(this.level);
      let byte4 = this.bitDepth << 4 | (this.chromaSubsampling & 7) << 1 | this.videoFullRangeFlag & 1;
      stream.writeUint8(byte4);
      stream.writeUint8(this.colourPrimaries);
      stream.writeUint8(this.transferCharacteristics);
      stream.writeUint8(this.matrixCoefficients);
      stream.writeUint16(this.codecIntializationDataSize);
      if (this.codecIntializationDataSize > 0) {
        stream.writeUint8Array(this.codecIntializationData);
      }
    };
    BoxParser.cttsBox.prototype.unpack = function(samples) {
      var i2, j3, k2;
      k2 = 0;
      for (i2 = 0; i2 < this.sample_counts.length; i2++) {
        for (j3 = 0; j3 < this.sample_counts[i2]; j3++) {
          samples[k2].pts = samples[k2].dts + this.sample_offsets[i2];
          k2++;
        }
      }
    };
    BoxParser.sttsBox.prototype.unpack = function(samples) {
      var i2, j3, k2;
      k2 = 0;
      for (i2 = 0; i2 < this.sample_counts.length; i2++) {
        for (j3 = 0; j3 < this.sample_counts[i2]; j3++) {
          if (k2 === 0) {
            samples[k2].dts = 0;
          } else {
            samples[k2].dts = samples[k2 - 1].dts + this.sample_deltas[i2];
          }
          k2++;
        }
      }
    };
    BoxParser.stcoBox.prototype.unpack = function(samples) {
      var i2;
      for (i2 = 0; i2 < this.chunk_offsets.length; i2++) {
        samples[i2].offset = this.chunk_offsets[i2];
      }
    };
    BoxParser.stscBox.prototype.unpack = function(samples) {
      var i2, j3, k2, l2, m2;
      l2 = 0;
      m2 = 0;
      for (i2 = 0; i2 < this.first_chunk.length; i2++) {
        for (j3 = 0; j3 < (i2 + 1 < this.first_chunk.length ? this.first_chunk[i2 + 1] : Infinity); j3++) {
          m2++;
          for (k2 = 0; k2 < this.samples_per_chunk[i2]; k2++) {
            if (samples[l2]) {
              samples[l2].description_index = this.sample_description_index[i2];
              samples[l2].chunk_index = m2;
            } else {
              return;
            }
            l2++;
          }
        }
      }
    };
    BoxParser.stszBox.prototype.unpack = function(samples) {
      var i2;
      for (i2 = 0; i2 < this.sample_sizes.length; i2++) {
        samples[i2].size = this.sample_sizes[i2];
      }
    };
    BoxParser.DIFF_BOXES_PROP_NAMES = [
      "boxes",
      "entries",
      "references",
      "subsamples",
      "items",
      "item_infos",
      "extents",
      "associations",
      "subsegments",
      "ranges",
      "seekLists",
      "seekPoints",
      "esd",
      "levels"
    ];
    BoxParser.DIFF_PRIMITIVE_ARRAY_PROP_NAMES = [
      "compatible_brands",
      "matrix",
      "opcolor",
      "sample_counts",
      "sample_counts",
      "sample_deltas",
      "first_chunk",
      "samples_per_chunk",
      "sample_sizes",
      "chunk_offsets",
      "sample_offsets",
      "sample_description_index",
      "sample_duration"
    ];
    BoxParser.boxEqualFields = function(box_a, box_b) {
      if (box_a && !box_b) return false;
      var prop;
      for (prop in box_a) {
        if (BoxParser.DIFF_BOXES_PROP_NAMES.indexOf(prop) > -1) {
          continue;
        } else if (box_a[prop] instanceof BoxParser.Box || box_b[prop] instanceof BoxParser.Box) {
          continue;
        } else if (typeof box_a[prop] === "undefined" || typeof box_b[prop] === "undefined") {
          continue;
        } else if (typeof box_a[prop] === "function" || typeof box_b[prop] === "function") {
          continue;
        } else if (box_a.subBoxNames && box_a.subBoxNames.indexOf(prop.slice(0, 4)) > -1 || box_b.subBoxNames && box_b.subBoxNames.indexOf(prop.slice(0, 4)) > -1) {
          continue;
        } else {
          if (prop === "data" || prop === "start" || prop === "size" || prop === "creation_time" || prop === "modification_time") {
            continue;
          } else if (BoxParser.DIFF_PRIMITIVE_ARRAY_PROP_NAMES.indexOf(prop) > -1) {
            continue;
          } else {
            if (box_a[prop] !== box_b[prop]) {
              return false;
            }
          }
        }
      }
      return true;
    };
    BoxParser.boxEqual = function(box_a, box_b) {
      if (!BoxParser.boxEqualFields(box_a, box_b)) {
        return false;
      }
      for (var j3 = 0; j3 < BoxParser.DIFF_BOXES_PROP_NAMES.length; j3++) {
        var name2 = BoxParser.DIFF_BOXES_PROP_NAMES[j3];
        if (box_a[name2] && box_b[name2]) {
          if (!BoxParser.boxEqual(box_a[name2], box_b[name2])) {
            return false;
          }
        }
      }
      return true;
    };
    var VTTin4Parser = function() {
    };
    VTTin4Parser.prototype.parseSample = function(data) {
      var cues, cue;
      var stream = new MP4BoxStream(data.buffer);
      cues = [];
      while (!stream.isEos()) {
        cue = BoxParser.parseOneBox(stream, false);
        if (cue.code === BoxParser.OK && cue.box.type === "vttc") {
          cues.push(cue.box);
        }
      }
      return cues;
    };
    VTTin4Parser.prototype.getText = function(startTime, endTime, data) {
      function pad(n2, width, z3) {
        z3 = z3 || "0";
        n2 = n2 + "";
        return n2.length >= width ? n2 : new Array(width - n2.length + 1).join(z3) + n2;
      }
      function secToTimestamp(insec) {
        var h = Math.floor(insec / 3600);
        var m2 = Math.floor((insec - h * 3600) / 60);
        var s = Math.floor(insec - h * 3600 - m2 * 60);
        var ms = Math.floor((insec - h * 3600 - m2 * 60 - s) * 1e3);
        return "" + pad(h, 2) + ":" + pad(m2, 2) + ":" + pad(s, 2) + "." + pad(ms, 3);
      }
      var cues = this.parseSample(data);
      var string = "";
      for (var i2 = 0; i2 < cues.length; i2++) {
        var cueIn4 = cues[i2];
        string += secToTimestamp(startTime) + " --> " + secToTimestamp(endTime) + "\r\n";
        string += cueIn4.payl.text;
      }
      return string;
    };
    var XMLSubtitlein4Parser = function() {
    };
    XMLSubtitlein4Parser.prototype.parseSample = function(sample) {
      var res = {};
      var i2;
      res.resources = [];
      var stream = new MP4BoxStream(sample.data.buffer);
      if (!sample.subsamples || sample.subsamples.length === 0) {
        res.documentString = stream.readString(sample.data.length);
      } else {
        res.documentString = stream.readString(sample.subsamples[0].size);
        if (sample.subsamples.length > 1) {
          for (i2 = 1; i2 < sample.subsamples.length; i2++) {
            res.resources[i2] = stream.readUint8Array(sample.subsamples[i2].size);
          }
        }
      }
      if (typeof DOMParser !== "undefined") {
        res.document = new DOMParser().parseFromString(res.documentString, "application/xml");
      }
      return res;
    };
    var Textin4Parser = function() {
    };
    Textin4Parser.prototype.parseSample = function(sample) {
      var textString;
      var stream = new MP4BoxStream(sample.data.buffer);
      textString = stream.readString(sample.data.length);
      return textString;
    };
    Textin4Parser.prototype.parseConfig = function(data) {
      var textString;
      var stream = new MP4BoxStream(data.buffer);
      stream.readUint32();
      textString = stream.readCString();
      return textString;
    };
    if (typeof exports !== "undefined") {
      exports.XMLSubtitlein4Parser = XMLSubtitlein4Parser;
      exports.Textin4Parser = Textin4Parser;
    }
    var ISOFile = function(stream) {
      this.stream = stream || new MultiBufferStream();
      this.boxes = [];
      this.mdats = [];
      this.moofs = [];
      this.isProgressive = false;
      this.moovStartFound = false;
      this.onMoovStart = null;
      this.moovStartSent = false;
      this.onReady = null;
      this.readySent = false;
      this.onSegment = null;
      this.onSamples = null;
      this.onError = null;
      this.sampleListBuilt = false;
      this.fragmentedTracks = [];
      this.extractedTracks = [];
      this.isFragmentationInitialized = false;
      this.sampleProcessingStarted = false;
      this.nextMoofNumber = 0;
      this.itemListBuilt = false;
      this.onSidx = null;
      this.sidxSent = false;
    };
    ISOFile.prototype.setSegmentOptions = function(id2, user, options) {
      var trak = this.getTrackById(id2);
      if (trak) {
        var fragTrack = {};
        this.fragmentedTracks.push(fragTrack);
        fragTrack.id = id2;
        fragTrack.user = user;
        fragTrack.trak = trak;
        trak.nextSample = 0;
        fragTrack.segmentStream = null;
        fragTrack.nb_samples = 1e3;
        fragTrack.rapAlignement = true;
        if (options) {
          if (options.nbSamples) fragTrack.nb_samples = options.nbSamples;
          if (options.rapAlignement) fragTrack.rapAlignement = options.rapAlignement;
        }
      }
    };
    ISOFile.prototype.unsetSegmentOptions = function(id2) {
      var index2 = -1;
      for (var i2 = 0; i2 < this.fragmentedTracks.length; i2++) {
        var fragTrack = this.fragmentedTracks[i2];
        if (fragTrack.id == id2) {
          index2 = i2;
        }
      }
      if (index2 > -1) {
        this.fragmentedTracks.splice(index2, 1);
      }
    };
    ISOFile.prototype.setExtractionOptions = function(id2, user, options) {
      var trak = this.getTrackById(id2);
      if (trak) {
        var extractTrack = {};
        this.extractedTracks.push(extractTrack);
        extractTrack.id = id2;
        extractTrack.user = user;
        extractTrack.trak = trak;
        trak.nextSample = 0;
        extractTrack.nb_samples = 1e3;
        extractTrack.samples = [];
        if (options) {
          if (options.nbSamples) extractTrack.nb_samples = options.nbSamples;
        }
      }
    };
    ISOFile.prototype.unsetExtractionOptions = function(id2) {
      var index2 = -1;
      for (var i2 = 0; i2 < this.extractedTracks.length; i2++) {
        var extractTrack = this.extractedTracks[i2];
        if (extractTrack.id == id2) {
          index2 = i2;
        }
      }
      if (index2 > -1) {
        this.extractedTracks.splice(index2, 1);
      }
    };
    ISOFile.prototype.parse = function() {
      var found;
      var ret2;
      var box2;
      var parseBoxHeadersOnly = false;
      if (this.restoreParsePosition) {
        if (!this.restoreParsePosition()) {
          return;
        }
      }
      while (true) {
        if (this.hasIncompleteMdat && this.hasIncompleteMdat()) {
          if (this.processIncompleteMdat()) {
            continue;
          } else {
            return;
          }
        } else {
          if (this.saveParsePosition) {
            this.saveParsePosition();
          }
          ret2 = BoxParser.parseOneBox(this.stream, parseBoxHeadersOnly);
          if (ret2.code === BoxParser.ERR_NOT_ENOUGH_DATA) {
            if (this.processIncompleteBox) {
              if (this.processIncompleteBox(ret2)) {
                continue;
              } else {
                return;
              }
            } else {
              return;
            }
          } else {
            var box_type;
            box2 = ret2.box;
            box_type = box2.type !== "uuid" ? box2.type : box2.uuid;
            this.boxes.push(box2);
            switch (box_type) {
              case "mdat":
                this.mdats.push(box2);
                break;
              case "moof":
                this.moofs.push(box2);
                break;
              case "moov":
                this.moovStartFound = true;
                if (this.mdats.length === 0) {
                  this.isProgressive = true;
                }
              /* no break */
              /* falls through */
              default:
                if (this[box_type] !== void 0) {
                  Log.warn("ISOFile", "Duplicate Box of type: " + box_type + ", overriding previous occurrence");
                }
                this[box_type] = box2;
                break;
            }
            if (this.updateUsedBytes) {
              this.updateUsedBytes(box2, ret2);
            }
          }
        }
      }
    };
    ISOFile.prototype.checkBuffer = function(ab) {
      if (ab === null || ab === void 0) {
        throw "Buffer must be defined and non empty";
      }
      if (ab.fileStart === void 0) {
        throw "Buffer must have a fileStart property";
      }
      if (ab.byteLength === 0) {
        Log.warn("ISOFile", "Ignoring empty buffer (fileStart: " + ab.fileStart + ")");
        this.stream.logBufferLevel();
        return false;
      }
      Log.info("ISOFile", "Processing buffer (fileStart: " + ab.fileStart + ")");
      ab.usedBytes = 0;
      this.stream.insertBuffer(ab);
      this.stream.logBufferLevel();
      if (!this.stream.initialized()) {
        Log.warn("ISOFile", "Not ready to start parsing");
        return false;
      }
      return true;
    };
    ISOFile.prototype.appendBuffer = function(ab, last) {
      var nextFileStart;
      if (!this.checkBuffer(ab)) {
        return;
      }
      this.parse();
      if (this.moovStartFound && !this.moovStartSent) {
        this.moovStartSent = true;
        if (this.onMoovStart) this.onMoovStart();
      }
      if (this.moov) {
        if (!this.sampleListBuilt) {
          this.buildSampleLists();
          this.sampleListBuilt = true;
        }
        this.updateSampleLists();
        if (this.onReady && !this.readySent) {
          this.readySent = true;
          this.onReady(this.getInfo());
        }
        this.processSamples(last);
        if (this.nextSeekPosition) {
          nextFileStart = this.nextSeekPosition;
          this.nextSeekPosition = void 0;
        } else {
          nextFileStart = this.nextParsePosition;
        }
        if (this.stream.getEndFilePositionAfter) {
          nextFileStart = this.stream.getEndFilePositionAfter(nextFileStart);
        }
      } else {
        if (this.nextParsePosition) {
          nextFileStart = this.nextParsePosition;
        } else {
          nextFileStart = 0;
        }
      }
      if (this.sidx) {
        if (this.onSidx && !this.sidxSent) {
          this.onSidx(this.sidx);
          this.sidxSent = true;
        }
      }
      if (this.meta) {
        if (this.flattenItemInfo && !this.itemListBuilt) {
          this.flattenItemInfo();
          this.itemListBuilt = true;
        }
        if (this.processItems) {
          this.processItems(this.onItem);
        }
      }
      if (this.stream.cleanBuffers) {
        Log.info("ISOFile", "Done processing buffer (fileStart: " + ab.fileStart + ") - next buffer to fetch should have a fileStart position of " + nextFileStart);
        this.stream.logBufferLevel();
        this.stream.cleanBuffers();
        this.stream.logBufferLevel(true);
        Log.info("ISOFile", "Sample data size in memory: " + this.getAllocatedSampleDataSize());
      }
      return nextFileStart;
    };
    ISOFile.prototype.getInfo = function() {
      var i2, j3;
      var movie = {};
      var trak;
      var track;
      var ref;
      var sample_desc;
      var _1904 = (/* @__PURE__ */ new Date("1904-01-01T00:00:00Z")).getTime();
      if (this.moov) {
        movie.hasMoov = true;
        movie.duration = this.moov.mvhd.duration;
        movie.timescale = this.moov.mvhd.timescale;
        movie.isFragmented = this.moov.mvex != null;
        if (movie.isFragmented && this.moov.mvex.mehd) {
          movie.fragment_duration = this.moov.mvex.mehd.fragment_duration;
        }
        movie.isProgressive = this.isProgressive;
        movie.hasIOD = this.moov.iods != null;
        movie.brands = [];
        movie.brands.push(this.ftyp.major_brand);
        movie.brands = movie.brands.concat(this.ftyp.compatible_brands);
        movie.created = new Date(_1904 + this.moov.mvhd.creation_time * 1e3);
        movie.modified = new Date(_1904 + this.moov.mvhd.modification_time * 1e3);
        movie.tracks = [];
        movie.audioTracks = [];
        movie.videoTracks = [];
        movie.subtitleTracks = [];
        movie.metadataTracks = [];
        movie.hintTracks = [];
        movie.otherTracks = [];
        for (i2 = 0; i2 < this.moov.traks.length; i2++) {
          trak = this.moov.traks[i2];
          sample_desc = trak.mdia.minf.stbl.stsd.entries[0];
          track = {};
          movie.tracks.push(track);
          track.id = trak.tkhd.track_id;
          track.name = trak.mdia.hdlr.name;
          track.references = [];
          if (trak.tref) {
            for (j3 = 0; j3 < trak.tref.boxes.length; j3++) {
              ref = {};
              track.references.push(ref);
              ref.type = trak.tref.boxes[j3].type;
              ref.track_ids = trak.tref.boxes[j3].track_ids;
            }
          }
          if (trak.edts) {
            track.edits = trak.edts.elst.entries;
          }
          track.created = new Date(_1904 + trak.tkhd.creation_time * 1e3);
          track.modified = new Date(_1904 + trak.tkhd.modification_time * 1e3);
          track.movie_duration = trak.tkhd.duration;
          track.movie_timescale = movie.timescale;
          track.layer = trak.tkhd.layer;
          track.alternate_group = trak.tkhd.alternate_group;
          track.volume = trak.tkhd.volume;
          track.matrix = trak.tkhd.matrix;
          track.track_width = trak.tkhd.width / (1 << 16);
          track.track_height = trak.tkhd.height / (1 << 16);
          track.timescale = trak.mdia.mdhd.timescale;
          track.cts_shift = trak.mdia.minf.stbl.cslg;
          track.duration = trak.mdia.mdhd.duration;
          track.samples_duration = trak.samples_duration;
          track.codec = sample_desc.getCodec();
          track.kind = trak.udta && trak.udta.kinds.length ? trak.udta.kinds[0] : { schemeURI: "", value: "" };
          track.language = trak.mdia.elng ? trak.mdia.elng.extended_language : trak.mdia.mdhd.languageString;
          track.nb_samples = trak.samples.length;
          track.size = trak.samples_size;
          track.bitrate = track.size * 8 * track.timescale / track.samples_duration;
          if (sample_desc.isAudio()) {
            track.type = "audio";
            movie.audioTracks.push(track);
            track.audio = {};
            track.audio.sample_rate = sample_desc.getSampleRate();
            track.audio.channel_count = sample_desc.getChannelCount();
            track.audio.sample_size = sample_desc.getSampleSize();
          } else if (sample_desc.isVideo()) {
            track.type = "video";
            movie.videoTracks.push(track);
            track.video = {};
            track.video.width = sample_desc.getWidth();
            track.video.height = sample_desc.getHeight();
          } else if (sample_desc.isSubtitle()) {
            track.type = "subtitles";
            movie.subtitleTracks.push(track);
          } else if (sample_desc.isHint()) {
            track.type = "metadata";
            movie.hintTracks.push(track);
          } else if (sample_desc.isMetadata()) {
            track.type = "metadata";
            movie.metadataTracks.push(track);
          } else {
            track.type = "metadata";
            movie.otherTracks.push(track);
          }
        }
      } else {
        movie.hasMoov = false;
      }
      movie.mime = "";
      if (movie.hasMoov && movie.tracks) {
        if (movie.videoTracks && movie.videoTracks.length > 0) {
          movie.mime += 'video/mp4; codecs="';
        } else if (movie.audioTracks && movie.audioTracks.length > 0) {
          movie.mime += 'audio/mp4; codecs="';
        } else {
          movie.mime += 'application/mp4; codecs="';
        }
        for (i2 = 0; i2 < movie.tracks.length; i2++) {
          if (i2 !== 0) movie.mime += ",";
          movie.mime += movie.tracks[i2].codec;
        }
        movie.mime += '"; profiles="';
        movie.mime += this.ftyp.compatible_brands.join();
        movie.mime += '"';
      }
      return movie;
    };
    ISOFile.prototype.setNextSeekPositionFromSample = function(sample) {
      if (!sample) {
        return;
      }
      if (this.nextSeekPosition) {
        this.nextSeekPosition = Math.min(sample.offset + sample.alreadyRead, this.nextSeekPosition);
      } else {
        this.nextSeekPosition = sample.offset + sample.alreadyRead;
      }
    };
    ISOFile.prototype.processSamples = function(last) {
      var i2;
      var trak;
      if (!this.sampleProcessingStarted) return;
      if (this.isFragmentationInitialized && this.onSegment !== null) {
        for (i2 = 0; i2 < this.fragmentedTracks.length; i2++) {
          var fragTrak = this.fragmentedTracks[i2];
          trak = fragTrak.trak;
          while (trak.nextSample < trak.samples.length && this.sampleProcessingStarted) {
            Log.debug("ISOFile", "Creating media fragment on track #" + fragTrak.id + " for sample " + trak.nextSample);
            var result = this.createFragment(fragTrak.id, trak.nextSample, fragTrak.segmentStream);
            if (result) {
              fragTrak.segmentStream = result;
              trak.nextSample++;
            } else {
              break;
            }
            if (trak.nextSample % fragTrak.nb_samples === 0 || (last || trak.nextSample >= trak.samples.length)) {
              Log.info("ISOFile", "Sending fragmented data on track #" + fragTrak.id + " for samples [" + Math.max(0, trak.nextSample - fragTrak.nb_samples) + "," + (trak.nextSample - 1) + "]");
              Log.info("ISOFile", "Sample data size in memory: " + this.getAllocatedSampleDataSize());
              if (this.onSegment) {
                this.onSegment(fragTrak.id, fragTrak.user, fragTrak.segmentStream.buffer, trak.nextSample, last || trak.nextSample >= trak.samples.length);
              }
              fragTrak.segmentStream = null;
              if (fragTrak !== this.fragmentedTracks[i2]) {
                break;
              }
            }
          }
        }
      }
      if (this.onSamples !== null) {
        for (i2 = 0; i2 < this.extractedTracks.length; i2++) {
          var extractTrak = this.extractedTracks[i2];
          trak = extractTrak.trak;
          while (trak.nextSample < trak.samples.length && this.sampleProcessingStarted) {
            Log.debug("ISOFile", "Exporting on track #" + extractTrak.id + " sample #" + trak.nextSample);
            var sample = this.getSample(trak, trak.nextSample);
            if (sample) {
              trak.nextSample++;
              extractTrak.samples.push(sample);
            } else {
              this.setNextSeekPositionFromSample(trak.samples[trak.nextSample]);
              break;
            }
            if (trak.nextSample % extractTrak.nb_samples === 0 || trak.nextSample >= trak.samples.length) {
              Log.debug("ISOFile", "Sending samples on track #" + extractTrak.id + " for sample " + trak.nextSample);
              if (this.onSamples) {
                this.onSamples(extractTrak.id, extractTrak.user, extractTrak.samples);
              }
              extractTrak.samples = [];
              if (extractTrak !== this.extractedTracks[i2]) {
                break;
              }
            }
          }
        }
      }
    };
    ISOFile.prototype.getBox = function(type) {
      var result = this.getBoxes(type, true);
      return result.length ? result[0] : null;
    };
    ISOFile.prototype.getBoxes = function(type, returnEarly) {
      var result = [];
      ISOFile._sweep.call(this, type, result, returnEarly);
      return result;
    };
    ISOFile._sweep = function(type, result, returnEarly) {
      if (this.type && this.type == type) result.push(this);
      for (var box2 in this.boxes) {
        if (result.length && returnEarly) return;
        ISOFile._sweep.call(this.boxes[box2], type, result, returnEarly);
      }
    };
    ISOFile.prototype.getTrackSamplesInfo = function(track_id) {
      var track = this.getTrackById(track_id);
      if (track) {
        return track.samples;
      } else {
        return;
      }
    };
    ISOFile.prototype.getTrackSample = function(track_id, number) {
      var track = this.getTrackById(track_id);
      var sample = this.getSample(track, number);
      return sample;
    };
    ISOFile.prototype.releaseUsedSamples = function(id2, sampleNum) {
      var size = 0;
      var trak = this.getTrackById(id2);
      if (!trak.lastValidSample) trak.lastValidSample = 0;
      for (var i2 = trak.lastValidSample; i2 < sampleNum; i2++) {
        size += this.releaseSample(trak, i2);
      }
      Log.info("ISOFile", "Track #" + id2 + " released samples up to " + sampleNum + " (released size: " + size + ", remaining: " + this.samplesDataSize + ")");
      trak.lastValidSample = sampleNum;
    };
    ISOFile.prototype.start = function() {
      this.sampleProcessingStarted = true;
      this.processSamples(false);
    };
    ISOFile.prototype.stop = function() {
      this.sampleProcessingStarted = false;
    };
    ISOFile.prototype.flush = function() {
      Log.info("ISOFile", "Flushing remaining samples");
      this.updateSampleLists();
      this.processSamples(true);
      this.stream.cleanBuffers();
      this.stream.logBufferLevel(true);
    };
    ISOFile.prototype.seekTrack = function(time, useRap, trak) {
      var j3;
      var sample;
      var seek_offset = Infinity;
      var rap_seek_sample_num = 0;
      var seek_sample_num = 0;
      var timescale;
      if (trak.samples.length === 0) {
        Log.info("ISOFile", "No sample in track, cannot seek! Using time " + Log.getDurationString(0, 1) + " and offset: 0");
        return { offset: 0, time: 0 };
      }
      for (j3 = 0; j3 < trak.samples.length; j3++) {
        sample = trak.samples[j3];
        if (j3 === 0) {
          seek_sample_num = 0;
          timescale = sample.timescale;
        } else if (sample.cts > time * sample.timescale) {
          seek_sample_num = j3 - 1;
          break;
        }
        if (useRap && sample.is_sync) {
          rap_seek_sample_num = j3;
        }
      }
      if (useRap) {
        seek_sample_num = rap_seek_sample_num;
      }
      time = trak.samples[seek_sample_num].cts;
      trak.nextSample = seek_sample_num;
      while (trak.samples[seek_sample_num].alreadyRead === trak.samples[seek_sample_num].size) {
        if (!trak.samples[seek_sample_num + 1]) {
          break;
        }
        seek_sample_num++;
      }
      seek_offset = trak.samples[seek_sample_num].offset + trak.samples[seek_sample_num].alreadyRead;
      Log.info("ISOFile", "Seeking to " + (useRap ? "RAP" : "") + " sample #" + trak.nextSample + " on track " + trak.tkhd.track_id + ", time " + Log.getDurationString(time, timescale) + " and offset: " + seek_offset);
      return { offset: seek_offset, time: time / timescale };
    };
    ISOFile.prototype.getTrackDuration = function(trak) {
      var sample;
      if (!trak.samples) {
        return Infinity;
      }
      sample = trak.samples[trak.samples.length - 1];
      return (sample.cts + sample.duration) / sample.timescale;
    };
    ISOFile.prototype.seek = function(time, useRap) {
      var moov = this.moov;
      var trak;
      var trak_seek_info;
      var i2;
      var seek_info = { offset: Infinity, time: Infinity };
      if (!this.moov) {
        throw "Cannot seek: moov not received!";
      } else {
        for (i2 = 0; i2 < moov.traks.length; i2++) {
          trak = moov.traks[i2];
          if (time > this.getTrackDuration(trak)) {
            continue;
          }
          trak_seek_info = this.seekTrack(time, useRap, trak);
          if (trak_seek_info.offset < seek_info.offset) {
            seek_info.offset = trak_seek_info.offset;
          }
          if (trak_seek_info.time < seek_info.time) {
            seek_info.time = trak_seek_info.time;
          }
        }
        Log.info("ISOFile", "Seeking at time " + Log.getDurationString(seek_info.time, 1) + " needs a buffer with a fileStart position of " + seek_info.offset);
        if (seek_info.offset === Infinity) {
          seek_info = { offset: this.nextParsePosition, time: 0 };
        } else {
          seek_info.offset = this.stream.getEndFilePositionAfter(seek_info.offset);
        }
        Log.info("ISOFile", "Adjusted seek position (after checking data already in buffer): " + seek_info.offset);
        return seek_info;
      }
    };
    ISOFile.prototype.equal = function(b) {
      var box_index = 0;
      while (box_index < this.boxes.length && box_index < b.boxes.length) {
        var a_box = this.boxes[box_index];
        var b_box = b.boxes[box_index];
        if (!BoxParser.boxEqual(a_box, b_box)) {
          return false;
        }
        box_index++;
      }
      return true;
    };
    if (typeof exports !== "undefined") {
      exports.ISOFile = ISOFile;
    }
    ISOFile.prototype.lastBoxStartPosition = 0;
    ISOFile.prototype.parsingMdat = null;
    ISOFile.prototype.nextParsePosition = 0;
    ISOFile.prototype.discardMdatData = false;
    ISOFile.prototype.processIncompleteBox = function(ret2) {
      var box2;
      var merged;
      var found;
      if (ret2.type === "mdat") {
        box2 = new BoxParser[ret2.type + "Box"](ret2.size);
        this.parsingMdat = box2;
        this.boxes.push(box2);
        this.mdats.push(box2);
        box2.start = ret2.start;
        box2.hdr_size = ret2.hdr_size;
        this.stream.addUsedBytes(box2.hdr_size);
        this.lastBoxStartPosition = box2.start + box2.size;
        found = this.stream.seek(box2.start + box2.size, false, this.discardMdatData);
        if (found) {
          this.parsingMdat = null;
          return true;
        } else {
          if (!this.moovStartFound) {
            this.nextParsePosition = box2.start + box2.size;
          } else {
            this.nextParsePosition = this.stream.findEndContiguousBuf();
          }
          return false;
        }
      } else {
        if (ret2.type === "moov") {
          this.moovStartFound = true;
          if (this.mdats.length === 0) {
            this.isProgressive = true;
          }
        }
        merged = this.stream.mergeNextBuffer ? this.stream.mergeNextBuffer() : false;
        if (merged) {
          this.nextParsePosition = this.stream.getEndPosition();
          return true;
        } else {
          if (!ret2.type) {
            this.nextParsePosition = this.stream.getEndPosition();
          } else {
            if (this.moovStartFound) {
              this.nextParsePosition = this.stream.getEndPosition();
            } else {
              this.nextParsePosition = this.stream.getPosition() + ret2.size;
            }
          }
          return false;
        }
      }
    };
    ISOFile.prototype.hasIncompleteMdat = function() {
      return this.parsingMdat !== null;
    };
    ISOFile.prototype.processIncompleteMdat = function() {
      var box2;
      var found;
      box2 = this.parsingMdat;
      found = this.stream.seek(box2.start + box2.size, false, this.discardMdatData);
      if (found) {
        Log.debug("ISOFile", "Found 'mdat' end in buffered data");
        this.parsingMdat = null;
        return true;
      } else {
        this.nextParsePosition = this.stream.findEndContiguousBuf();
        return false;
      }
    };
    ISOFile.prototype.restoreParsePosition = function() {
      return this.stream.seek(this.lastBoxStartPosition, true, this.discardMdatData);
    };
    ISOFile.prototype.saveParsePosition = function() {
      this.lastBoxStartPosition = this.stream.getPosition();
    };
    ISOFile.prototype.updateUsedBytes = function(box2, ret2) {
      if (this.stream.addUsedBytes) {
        if (box2.type === "mdat") {
          this.stream.addUsedBytes(box2.hdr_size);
          if (this.discardMdatData) {
            this.stream.addUsedBytes(box2.size - box2.hdr_size);
          }
        } else {
          this.stream.addUsedBytes(box2.size);
        }
      }
    };
    ISOFile.prototype.add = BoxParser.Box.prototype.add;
    ISOFile.prototype.addBox = BoxParser.Box.prototype.addBox;
    ISOFile.prototype.init = function(_options) {
      var options = _options || {};
      var ftyp = this.add("ftyp").set("major_brand", options.brands && options.brands[0] || "iso4").set("minor_version", 0).set("compatible_brands", options.brands || ["iso4"]);
      var moov = this.add("moov");
      moov.add("mvhd").set("timescale", options.timescale || 600).set("rate", options.rate || 1 << 16).set("creation_time", 0).set("modification_time", 0).set("duration", options.duration || 0).set("volume", options.width ? 0 : 256).set("matrix", [1 << 16, 0, 0, 0, 1 << 16, 0, 0, 0, 1073741824]).set("next_track_id", 1);
      moov.add("mvex");
      return this;
    };
    ISOFile.prototype.addTrack = function(_options) {
      if (!this.moov) {
        this.init(_options);
      }
      var options = _options || {};
      options.width = options.width || 320;
      options.height = options.height || 320;
      options.id = options.id || this.moov.mvhd.next_track_id;
      options.type = options.type || "avc1";
      var trak = this.moov.add("trak");
      this.moov.mvhd.next_track_id = options.id + 1;
      trak.add("tkhd").set("flags", BoxParser.TKHD_FLAG_ENABLED | BoxParser.TKHD_FLAG_IN_MOVIE | BoxParser.TKHD_FLAG_IN_PREVIEW).set("creation_time", 0).set("modification_time", 0).set("track_id", options.id).set("duration", options.duration || 0).set("layer", options.layer || 0).set("alternate_group", 0).set("volume", 1).set("matrix", [1 << 16, 0, 0, 0, 1 << 16, 0, 0, 0, 1073741824]).set("width", options.width << 16).set("height", options.height << 16);
      var mdia = trak.add("mdia");
      mdia.add("mdhd").set("creation_time", 0).set("modification_time", 0).set("timescale", options.timescale || 1).set("duration", options.media_duration || 0).set("language", options.language || "und");
      mdia.add("hdlr").set("handler", options.hdlr || "vide").set("name", options.name || "Track created with MP4Box.js");
      mdia.add("elng").set("extended_language", options.language || "fr-FR");
      var minf = mdia.add("minf");
      if (BoxParser[options.type + "SampleEntry"] === void 0) return;
      var sample_description_entry = new BoxParser[options.type + "SampleEntry"]();
      sample_description_entry.data_reference_index = 1;
      var media_type = "";
      for (var mediaType in BoxParser.sampleEntryCodes) {
        var codes = BoxParser.sampleEntryCodes[mediaType];
        for (var i2 = 0; i2 < codes.length; i2++) {
          if (codes.indexOf(options.type) > -1) {
            media_type = mediaType;
            break;
          }
        }
      }
      switch (media_type) {
        case "Visual":
          minf.add("vmhd").set("graphicsmode", 0).set("opcolor", [0, 0, 0]);
          sample_description_entry.set("width", options.width).set("height", options.height).set("horizresolution", 72 << 16).set("vertresolution", 72 << 16).set("frame_count", 1).set("compressorname", options.type + " Compressor").set("depth", 24);
          if (options.avcDecoderConfigRecord) {
            var avcC = new BoxParser.avcCBox();
            avcC.parse(new MP4BoxStream(options.avcDecoderConfigRecord));
            sample_description_entry.addBox(avcC);
          } else if (options.hevcDecoderConfigRecord) {
            var hvcC = new BoxParser.hvcCBox();
            hvcC.parse(new MP4BoxStream(options.hevcDecoderConfigRecord));
            sample_description_entry.addBox(hvcC);
          } else if (options.vpcDecoderConfigRecord) {
            var vpcC = new BoxParser.vpcCBox();
            vpcC.parse(new MP4BoxStream(options.vpcDecoderConfigRecord));
            sample_description_entry.addBox(vpcC);
          }
          break;
        case "Audio":
          minf.add("smhd").set("balance", options.balance || 0);
          sample_description_entry.set("channel_count", options.channel_count || 2).set("samplesize", options.samplesize || 16).set("samplerate", options.samplerate || 1 << 16);
          break;
        case "Hint":
          minf.add("hmhd");
          break;
        case "Subtitle":
          minf.add("sthd");
          switch (options.type) {
            case "stpp":
              sample_description_entry.set("namespace", options.namespace || "nonamespace").set("schema_location", options.schema_location || "").set("auxiliary_mime_types", options.auxiliary_mime_types || "");
              break;
          }
          break;
        case "Metadata":
          minf.add("nmhd");
          break;
        case "System":
          minf.add("nmhd");
          break;
        default:
          minf.add("nmhd");
          break;
      }
      if (options.description) {
        sample_description_entry.addBox(options.description);
      }
      if (options.description_boxes) {
        options.description_boxes.forEach(function(b) {
          sample_description_entry.addBox(b);
        });
      }
      minf.add("dinf").add("dref").addEntry(new BoxParser["url Box"]().set("flags", 1));
      var stbl = minf.add("stbl");
      stbl.add("stsd").addEntry(sample_description_entry);
      stbl.add("stts").set("sample_counts", []).set("sample_deltas", []);
      stbl.add("stsc").set("first_chunk", []).set("samples_per_chunk", []).set("sample_description_index", []);
      stbl.add("stco").set("chunk_offsets", []);
      stbl.add("stsz").set("sample_sizes", []);
      this.moov.mvex.add("trex").set("track_id", options.id).set("default_sample_description_index", options.default_sample_description_index || 1).set("default_sample_duration", options.default_sample_duration || 0).set("default_sample_size", options.default_sample_size || 0).set("default_sample_flags", options.default_sample_flags || 0);
      this.buildTrakSampleLists(trak);
      return options.id;
    };
    BoxParser.Box.prototype.computeSize = function(stream_) {
      var stream = stream_ || new DataStream();
      stream.endianness = DataStream.BIG_ENDIAN;
      this.write(stream);
    };
    ISOFile.prototype.addSample = function(track_id, data, _options) {
      var options = _options || {};
      var sample = {};
      var trak = this.getTrackById(track_id);
      if (trak === null) return;
      sample.number = trak.samples.length;
      sample.track_id = trak.tkhd.track_id;
      sample.timescale = trak.mdia.mdhd.timescale;
      sample.description_index = options.sample_description_index ? options.sample_description_index - 1 : 0;
      sample.description = trak.mdia.minf.stbl.stsd.entries[sample.description_index];
      sample.data = data;
      sample.size = data.byteLength;
      sample.alreadyRead = sample.size;
      sample.duration = options.duration || 1;
      sample.cts = options.cts || 0;
      sample.dts = options.dts || 0;
      sample.is_sync = options.is_sync || false;
      sample.is_leading = options.is_leading || 0;
      sample.depends_on = options.depends_on || 0;
      sample.is_depended_on = options.is_depended_on || 0;
      sample.has_redundancy = options.has_redundancy || 0;
      sample.degradation_priority = options.degradation_priority || 0;
      sample.offset = 0;
      sample.subsamples = options.subsamples;
      trak.samples.push(sample);
      trak.samples_size += sample.size;
      trak.samples_duration += sample.duration;
      if (trak.first_dts === void 0) {
        trak.first_dts = options.dts;
      }
      this.processSamples();
      var moof = this.createSingleSampleMoof(sample);
      this.addBox(moof);
      moof.computeSize();
      moof.trafs[0].truns[0].data_offset = moof.size + 8;
      this.add("mdat").data = new Uint8Array(data);
      return sample;
    };
    ISOFile.prototype.createSingleSampleMoof = function(sample) {
      var sample_flags = 0;
      if (sample.is_sync)
        sample_flags = 1 << 25;
      else
        sample_flags = 1 << 16;
      var moof = new BoxParser.moofBox();
      moof.add("mfhd").set("sequence_number", this.nextMoofNumber);
      this.nextMoofNumber++;
      var traf = moof.add("traf");
      var trak = this.getTrackById(sample.track_id);
      traf.add("tfhd").set("track_id", sample.track_id).set("flags", BoxParser.TFHD_FLAG_DEFAULT_BASE_IS_MOOF);
      traf.add("tfdt").set("baseMediaDecodeTime", sample.dts - (trak.first_dts || 0));
      traf.add("trun").set("flags", BoxParser.TRUN_FLAGS_DATA_OFFSET | BoxParser.TRUN_FLAGS_DURATION | BoxParser.TRUN_FLAGS_SIZE | BoxParser.TRUN_FLAGS_FLAGS | BoxParser.TRUN_FLAGS_CTS_OFFSET).set("data_offset", 0).set("first_sample_flags", 0).set("sample_count", 1).set("sample_duration", [sample.duration]).set("sample_size", [sample.size]).set("sample_flags", [sample_flags]).set("sample_composition_time_offset", [sample.cts - sample.dts]);
      return moof;
    };
    ISOFile.prototype.lastMoofIndex = 0;
    ISOFile.prototype.samplesDataSize = 0;
    ISOFile.prototype.resetTables = function() {
      var i2;
      var trak, stco, stsc, stsz, stts, ctts, stss;
      this.initial_duration = this.moov.mvhd.duration;
      this.moov.mvhd.duration = 0;
      for (i2 = 0; i2 < this.moov.traks.length; i2++) {
        trak = this.moov.traks[i2];
        trak.tkhd.duration = 0;
        trak.mdia.mdhd.duration = 0;
        stco = trak.mdia.minf.stbl.stco || trak.mdia.minf.stbl.co64;
        stco.chunk_offsets = [];
        stsc = trak.mdia.minf.stbl.stsc;
        stsc.first_chunk = [];
        stsc.samples_per_chunk = [];
        stsc.sample_description_index = [];
        stsz = trak.mdia.minf.stbl.stsz || trak.mdia.minf.stbl.stz2;
        stsz.sample_sizes = [];
        stts = trak.mdia.minf.stbl.stts;
        stts.sample_counts = [];
        stts.sample_deltas = [];
        ctts = trak.mdia.minf.stbl.ctts;
        if (ctts) {
          ctts.sample_counts = [];
          ctts.sample_offsets = [];
        }
        stss = trak.mdia.minf.stbl.stss;
        var k2 = trak.mdia.minf.stbl.boxes.indexOf(stss);
        if (k2 != -1) trak.mdia.minf.stbl.boxes[k2] = null;
      }
    };
    ISOFile.initSampleGroups = function(trak, traf, sbgps, trak_sgpds, traf_sgpds) {
      var l2;
      var k2;
      var sample_groups_info;
      var sample_group_info;
      var sample_group_key;
      function SampleGroupInfo(_type, _parameter, _sbgp) {
        this.grouping_type = _type;
        this.grouping_type_parameter = _parameter;
        this.sbgp = _sbgp;
        this.last_sample_in_run = -1;
        this.entry_index = -1;
      }
      if (traf) {
        traf.sample_groups_info = [];
      }
      if (!trak.sample_groups_info) {
        trak.sample_groups_info = [];
      }
      for (k2 = 0; k2 < sbgps.length; k2++) {
        sample_group_key = sbgps[k2].grouping_type + "/" + sbgps[k2].grouping_type_parameter;
        sample_group_info = new SampleGroupInfo(sbgps[k2].grouping_type, sbgps[k2].grouping_type_parameter, sbgps[k2]);
        if (traf) {
          traf.sample_groups_info[sample_group_key] = sample_group_info;
        }
        if (!trak.sample_groups_info[sample_group_key]) {
          trak.sample_groups_info[sample_group_key] = sample_group_info;
        }
        for (l2 = 0; l2 < trak_sgpds.length; l2++) {
          if (trak_sgpds[l2].grouping_type === sbgps[k2].grouping_type) {
            sample_group_info.description = trak_sgpds[l2];
            sample_group_info.description.used = true;
          }
        }
        if (traf_sgpds) {
          for (l2 = 0; l2 < traf_sgpds.length; l2++) {
            if (traf_sgpds[l2].grouping_type === sbgps[k2].grouping_type) {
              sample_group_info.fragment_description = traf_sgpds[l2];
              sample_group_info.fragment_description.used = true;
              sample_group_info.is_fragment = true;
            }
          }
        }
      }
      if (!traf) {
        for (k2 = 0; k2 < trak_sgpds.length; k2++) {
          if (!trak_sgpds[k2].used && trak_sgpds[k2].version >= 2) {
            sample_group_key = trak_sgpds[k2].grouping_type + "/0";
            sample_group_info = new SampleGroupInfo(trak_sgpds[k2].grouping_type, 0);
            if (!trak.sample_groups_info[sample_group_key]) {
              trak.sample_groups_info[sample_group_key] = sample_group_info;
            }
          }
        }
      } else {
        if (traf_sgpds) {
          for (k2 = 0; k2 < traf_sgpds.length; k2++) {
            if (!traf_sgpds[k2].used && traf_sgpds[k2].version >= 2) {
              sample_group_key = traf_sgpds[k2].grouping_type + "/0";
              sample_group_info = new SampleGroupInfo(traf_sgpds[k2].grouping_type, 0);
              sample_group_info.is_fragment = true;
              if (!traf.sample_groups_info[sample_group_key]) {
                traf.sample_groups_info[sample_group_key] = sample_group_info;
              }
            }
          }
        }
      }
    };
    ISOFile.setSampleGroupProperties = function(trak, sample, sample_number, sample_groups_info) {
      var k2;
      var index2;
      sample.sample_groups = [];
      for (k2 in sample_groups_info) {
        sample.sample_groups[k2] = {};
        sample.sample_groups[k2].grouping_type = sample_groups_info[k2].grouping_type;
        sample.sample_groups[k2].grouping_type_parameter = sample_groups_info[k2].grouping_type_parameter;
        if (sample_number >= sample_groups_info[k2].last_sample_in_run) {
          if (sample_groups_info[k2].last_sample_in_run < 0) {
            sample_groups_info[k2].last_sample_in_run = 0;
          }
          sample_groups_info[k2].entry_index++;
          if (sample_groups_info[k2].entry_index <= sample_groups_info[k2].sbgp.entries.length - 1) {
            sample_groups_info[k2].last_sample_in_run += sample_groups_info[k2].sbgp.entries[sample_groups_info[k2].entry_index].sample_count;
          }
        }
        if (sample_groups_info[k2].entry_index <= sample_groups_info[k2].sbgp.entries.length - 1) {
          sample.sample_groups[k2].group_description_index = sample_groups_info[k2].sbgp.entries[sample_groups_info[k2].entry_index].group_description_index;
        } else {
          sample.sample_groups[k2].group_description_index = -1;
        }
        if (sample.sample_groups[k2].group_description_index !== 0) {
          var description;
          if (sample_groups_info[k2].fragment_description) {
            description = sample_groups_info[k2].fragment_description;
          } else {
            description = sample_groups_info[k2].description;
          }
          if (sample.sample_groups[k2].group_description_index > 0) {
            if (sample.sample_groups[k2].group_description_index > 65535) {
              index2 = (sample.sample_groups[k2].group_description_index >> 16) - 1;
            } else {
              index2 = sample.sample_groups[k2].group_description_index - 1;
            }
            if (description && index2 >= 0) {
              sample.sample_groups[k2].description = description.entries[index2];
            }
          } else {
            if (description && description.version >= 2) {
              if (description.default_group_description_index > 0) {
                sample.sample_groups[k2].description = description.entries[description.default_group_description_index - 1];
              }
            }
          }
        }
      }
    };
    ISOFile.process_sdtp = function(sdtp, sample, number) {
      if (!sample) {
        return;
      }
      if (sdtp) {
        sample.is_leading = sdtp.is_leading[number];
        sample.depends_on = sdtp.sample_depends_on[number];
        sample.is_depended_on = sdtp.sample_is_depended_on[number];
        sample.has_redundancy = sdtp.sample_has_redundancy[number];
      } else {
        sample.is_leading = 0;
        sample.depends_on = 0;
        sample.is_depended_on = 0;
        sample.has_redundancy = 0;
      }
    };
    ISOFile.prototype.buildSampleLists = function() {
      var i2;
      var trak;
      for (i2 = 0; i2 < this.moov.traks.length; i2++) {
        trak = this.moov.traks[i2];
        this.buildTrakSampleLists(trak);
      }
    };
    ISOFile.prototype.buildTrakSampleLists = function(trak) {
      var j3, k2;
      var stco, stsc, stsz, stts, ctts, stss, stsd, subs, sbgps, sgpds, stdp;
      var chunk_run_index, chunk_index, last_chunk_in_run, offset_in_chunk, last_sample_in_chunk;
      var last_sample_in_stts_run, stts_run_index, last_sample_in_ctts_run, ctts_run_index, last_stss_index, last_subs_index, subs_entry_index, last_subs_sample_index;
      trak.samples = [];
      trak.samples_duration = 0;
      trak.samples_size = 0;
      stco = trak.mdia.minf.stbl.stco || trak.mdia.minf.stbl.co64;
      stsc = trak.mdia.minf.stbl.stsc;
      stsz = trak.mdia.minf.stbl.stsz || trak.mdia.minf.stbl.stz2;
      stts = trak.mdia.minf.stbl.stts;
      ctts = trak.mdia.minf.stbl.ctts;
      stss = trak.mdia.minf.stbl.stss;
      stsd = trak.mdia.minf.stbl.stsd;
      subs = trak.mdia.minf.stbl.subs;
      stdp = trak.mdia.minf.stbl.stdp;
      sbgps = trak.mdia.minf.stbl.sbgps;
      sgpds = trak.mdia.minf.stbl.sgpds;
      last_sample_in_stts_run = -1;
      stts_run_index = -1;
      last_sample_in_ctts_run = -1;
      ctts_run_index = -1;
      last_stss_index = 0;
      subs_entry_index = 0;
      last_subs_sample_index = 0;
      ISOFile.initSampleGroups(trak, null, sbgps, sgpds);
      if (typeof stsz === "undefined") {
        return;
      }
      for (j3 = 0; j3 < stsz.sample_sizes.length; j3++) {
        var sample = {};
        sample.number = j3;
        sample.track_id = trak.tkhd.track_id;
        sample.timescale = trak.mdia.mdhd.timescale;
        sample.alreadyRead = 0;
        trak.samples[j3] = sample;
        sample.size = stsz.sample_sizes[j3];
        trak.samples_size += sample.size;
        if (j3 === 0) {
          chunk_index = 1;
          chunk_run_index = 0;
          sample.chunk_index = chunk_index;
          sample.chunk_run_index = chunk_run_index;
          last_sample_in_chunk = stsc.samples_per_chunk[chunk_run_index];
          offset_in_chunk = 0;
          if (chunk_run_index + 1 < stsc.first_chunk.length) {
            last_chunk_in_run = stsc.first_chunk[chunk_run_index + 1] - 1;
          } else {
            last_chunk_in_run = Infinity;
          }
        } else {
          if (j3 < last_sample_in_chunk) {
            sample.chunk_index = chunk_index;
            sample.chunk_run_index = chunk_run_index;
          } else {
            chunk_index++;
            sample.chunk_index = chunk_index;
            offset_in_chunk = 0;
            if (chunk_index <= last_chunk_in_run) {
            } else {
              chunk_run_index++;
              if (chunk_run_index + 1 < stsc.first_chunk.length) {
                last_chunk_in_run = stsc.first_chunk[chunk_run_index + 1] - 1;
              } else {
                last_chunk_in_run = Infinity;
              }
            }
            sample.chunk_run_index = chunk_run_index;
            last_sample_in_chunk += stsc.samples_per_chunk[chunk_run_index];
          }
        }
        sample.description_index = stsc.sample_description_index[sample.chunk_run_index] - 1;
        sample.description = stsd.entries[sample.description_index];
        sample.offset = stco.chunk_offsets[sample.chunk_index - 1] + offset_in_chunk;
        offset_in_chunk += sample.size;
        if (j3 > last_sample_in_stts_run) {
          stts_run_index++;
          if (last_sample_in_stts_run < 0) {
            last_sample_in_stts_run = 0;
          }
          last_sample_in_stts_run += stts.sample_counts[stts_run_index];
        }
        if (j3 > 0) {
          trak.samples[j3 - 1].duration = stts.sample_deltas[stts_run_index];
          trak.samples_duration += trak.samples[j3 - 1].duration;
          sample.dts = trak.samples[j3 - 1].dts + trak.samples[j3 - 1].duration;
        } else {
          sample.dts = 0;
        }
        if (ctts) {
          if (j3 >= last_sample_in_ctts_run) {
            ctts_run_index++;
            if (last_sample_in_ctts_run < 0) {
              last_sample_in_ctts_run = 0;
            }
            last_sample_in_ctts_run += ctts.sample_counts[ctts_run_index];
          }
          sample.cts = trak.samples[j3].dts + ctts.sample_offsets[ctts_run_index];
        } else {
          sample.cts = sample.dts;
        }
        if (stss) {
          if (j3 == stss.sample_numbers[last_stss_index] - 1) {
            sample.is_sync = true;
            last_stss_index++;
          } else {
            sample.is_sync = false;
            sample.degradation_priority = 0;
          }
          if (subs) {
            if (subs.entries[subs_entry_index].sample_delta + last_subs_sample_index == j3 + 1) {
              sample.subsamples = subs.entries[subs_entry_index].subsamples;
              last_subs_sample_index += subs.entries[subs_entry_index].sample_delta;
              subs_entry_index++;
            }
          }
        } else {
          sample.is_sync = true;
        }
        ISOFile.process_sdtp(trak.mdia.minf.stbl.sdtp, sample, sample.number);
        if (stdp) {
          sample.degradation_priority = stdp.priority[j3];
        } else {
          sample.degradation_priority = 0;
        }
        if (subs) {
          if (subs.entries[subs_entry_index].sample_delta + last_subs_sample_index == j3) {
            sample.subsamples = subs.entries[subs_entry_index].subsamples;
            last_subs_sample_index += subs.entries[subs_entry_index].sample_delta;
          }
        }
        if (sbgps.length > 0 || sgpds.length > 0) {
          ISOFile.setSampleGroupProperties(trak, sample, j3, trak.sample_groups_info);
        }
      }
      if (j3 > 0) {
        trak.samples[j3 - 1].duration = Math.max(trak.mdia.mdhd.duration - trak.samples[j3 - 1].dts, 0);
        trak.samples_duration += trak.samples[j3 - 1].duration;
      }
    };
    ISOFile.prototype.updateSampleLists = function() {
      var i2, j3, k2;
      var default_sample_description_index, default_sample_duration, default_sample_size, default_sample_flags;
      var last_run_position;
      var box2, moof, traf, trak, trex;
      var sample;
      var sample_flags;
      if (this.moov === void 0) {
        return;
      }
      while (this.lastMoofIndex < this.moofs.length) {
        box2 = this.moofs[this.lastMoofIndex];
        this.lastMoofIndex++;
        if (box2.type == "moof") {
          moof = box2;
          for (i2 = 0; i2 < moof.trafs.length; i2++) {
            traf = moof.trafs[i2];
            trak = this.getTrackById(traf.tfhd.track_id);
            if (trak.samples == null) trak.samples = [];
            trex = this.getTrexById(traf.tfhd.track_id);
            if (traf.tfhd.flags & BoxParser.TFHD_FLAG_SAMPLE_DESC) {
              default_sample_description_index = traf.tfhd.default_sample_description_index;
            } else {
              default_sample_description_index = trex ? trex.default_sample_description_index : 1;
            }
            if (traf.tfhd.flags & BoxParser.TFHD_FLAG_SAMPLE_DUR) {
              default_sample_duration = traf.tfhd.default_sample_duration;
            } else {
              default_sample_duration = trex ? trex.default_sample_duration : 0;
            }
            if (traf.tfhd.flags & BoxParser.TFHD_FLAG_SAMPLE_SIZE) {
              default_sample_size = traf.tfhd.default_sample_size;
            } else {
              default_sample_size = trex ? trex.default_sample_size : 0;
            }
            if (traf.tfhd.flags & BoxParser.TFHD_FLAG_SAMPLE_FLAGS) {
              default_sample_flags = traf.tfhd.default_sample_flags;
            } else {
              default_sample_flags = trex ? trex.default_sample_flags : 0;
            }
            traf.sample_number = 0;
            if (traf.sbgps.length > 0) {
              ISOFile.initSampleGroups(trak, traf, traf.sbgps, trak.mdia.minf.stbl.sgpds, traf.sgpds);
            }
            for (j3 = 0; j3 < traf.truns.length; j3++) {
              var trun = traf.truns[j3];
              for (k2 = 0; k2 < trun.sample_count; k2++) {
                sample = {};
                sample.moof_number = this.lastMoofIndex;
                sample.number_in_traf = traf.sample_number;
                traf.sample_number++;
                sample.number = trak.samples.length;
                traf.first_sample_index = trak.samples.length;
                trak.samples.push(sample);
                sample.track_id = trak.tkhd.track_id;
                sample.timescale = trak.mdia.mdhd.timescale;
                sample.description_index = default_sample_description_index - 1;
                sample.description = trak.mdia.minf.stbl.stsd.entries[sample.description_index];
                sample.size = default_sample_size;
                if (trun.flags & BoxParser.TRUN_FLAGS_SIZE) {
                  sample.size = trun.sample_size[k2];
                }
                trak.samples_size += sample.size;
                sample.duration = default_sample_duration;
                if (trun.flags & BoxParser.TRUN_FLAGS_DURATION) {
                  sample.duration = trun.sample_duration[k2];
                }
                trak.samples_duration += sample.duration;
                if (trak.first_traf_merged || k2 > 0) {
                  sample.dts = trak.samples[trak.samples.length - 2].dts + trak.samples[trak.samples.length - 2].duration;
                } else {
                  if (traf.tfdt) {
                    sample.dts = traf.tfdt.baseMediaDecodeTime;
                  } else {
                    sample.dts = 0;
                  }
                  trak.first_traf_merged = true;
                }
                sample.cts = sample.dts;
                if (trun.flags & BoxParser.TRUN_FLAGS_CTS_OFFSET) {
                  sample.cts = sample.dts + trun.sample_composition_time_offset[k2];
                }
                sample_flags = default_sample_flags;
                if (trun.flags & BoxParser.TRUN_FLAGS_FLAGS) {
                  sample_flags = trun.sample_flags[k2];
                } else if (k2 === 0 && trun.flags & BoxParser.TRUN_FLAGS_FIRST_FLAG) {
                  sample_flags = trun.first_sample_flags;
                }
                sample.is_sync = sample_flags >> 16 & 1 ? false : true;
                sample.is_leading = sample_flags >> 26 & 3;
                sample.depends_on = sample_flags >> 24 & 3;
                sample.is_depended_on = sample_flags >> 22 & 3;
                sample.has_redundancy = sample_flags >> 20 & 3;
                sample.degradation_priority = sample_flags & 65535;
                var bdop = traf.tfhd.flags & BoxParser.TFHD_FLAG_BASE_DATA_OFFSET ? true : false;
                var dbim = traf.tfhd.flags & BoxParser.TFHD_FLAG_DEFAULT_BASE_IS_MOOF ? true : false;
                var dop = trun.flags & BoxParser.TRUN_FLAGS_DATA_OFFSET ? true : false;
                var bdo = 0;
                if (!bdop) {
                  if (!dbim) {
                    if (j3 === 0) {
                      bdo = moof.start;
                    } else {
                      bdo = last_run_position;
                    }
                  } else {
                    bdo = moof.start;
                  }
                } else {
                  bdo = traf.tfhd.base_data_offset;
                }
                if (j3 === 0 && k2 === 0) {
                  if (dop) {
                    sample.offset = bdo + trun.data_offset;
                  } else {
                    sample.offset = bdo;
                  }
                } else {
                  sample.offset = last_run_position;
                }
                last_run_position = sample.offset + sample.size;
                if (traf.sbgps.length > 0 || traf.sgpds.length > 0 || trak.mdia.minf.stbl.sbgps.length > 0 || trak.mdia.minf.stbl.sgpds.length > 0) {
                  ISOFile.setSampleGroupProperties(trak, sample, sample.number_in_traf, traf.sample_groups_info);
                }
              }
            }
            if (traf.subs) {
              trak.has_fragment_subsamples = true;
              var sample_index = traf.first_sample_index;
              for (j3 = 0; j3 < traf.subs.entries.length; j3++) {
                sample_index += traf.subs.entries[j3].sample_delta;
                sample = trak.samples[sample_index - 1];
                sample.subsamples = traf.subs.entries[j3].subsamples;
              }
            }
          }
        }
      }
    };
    ISOFile.prototype.getSample = function(trak, sampleNum) {
      var buffer;
      var sample = trak.samples[sampleNum];
      if (!this.moov) {
        return null;
      }
      if (!sample.data) {
        sample.data = new Uint8Array(sample.size);
        sample.alreadyRead = 0;
        this.samplesDataSize += sample.size;
        Log.debug("ISOFile", "Allocating sample #" + sampleNum + " on track #" + trak.tkhd.track_id + " of size " + sample.size + " (total: " + this.samplesDataSize + ")");
      } else if (sample.alreadyRead == sample.size) {
        return sample;
      }
      while (true) {
        var index2 = this.stream.findPosition(true, sample.offset + sample.alreadyRead, false);
        if (index2 > -1) {
          buffer = this.stream.buffers[index2];
          var lengthAfterStart = buffer.byteLength - (sample.offset + sample.alreadyRead - buffer.fileStart);
          if (sample.size - sample.alreadyRead <= lengthAfterStart) {
            Log.debug("ISOFile", "Getting sample #" + sampleNum + " data (alreadyRead: " + sample.alreadyRead + " offset: " + (sample.offset + sample.alreadyRead - buffer.fileStart) + " read size: " + (sample.size - sample.alreadyRead) + " full size: " + sample.size + ")");
            DataStream.memcpy(
              sample.data.buffer,
              sample.alreadyRead,
              buffer,
              sample.offset + sample.alreadyRead - buffer.fileStart,
              sample.size - sample.alreadyRead
            );
            buffer.usedBytes += sample.size - sample.alreadyRead;
            this.stream.logBufferLevel();
            sample.alreadyRead = sample.size;
            return sample;
          } else {
            if (lengthAfterStart === 0) return null;
            Log.debug("ISOFile", "Getting sample #" + sampleNum + " partial data (alreadyRead: " + sample.alreadyRead + " offset: " + (sample.offset + sample.alreadyRead - buffer.fileStart) + " read size: " + lengthAfterStart + " full size: " + sample.size + ")");
            DataStream.memcpy(
              sample.data.buffer,
              sample.alreadyRead,
              buffer,
              sample.offset + sample.alreadyRead - buffer.fileStart,
              lengthAfterStart
            );
            sample.alreadyRead += lengthAfterStart;
            buffer.usedBytes += lengthAfterStart;
            this.stream.logBufferLevel();
          }
        } else {
          return null;
        }
      }
    };
    ISOFile.prototype.releaseSample = function(trak, sampleNum) {
      var sample = trak.samples[sampleNum];
      if (sample.data) {
        this.samplesDataSize -= sample.size;
        sample.data = null;
        sample.alreadyRead = 0;
        return sample.size;
      } else {
        return 0;
      }
    };
    ISOFile.prototype.getAllocatedSampleDataSize = function() {
      return this.samplesDataSize;
    };
    ISOFile.prototype.getCodecs = function() {
      var i2;
      var codecs = "";
      for (i2 = 0; i2 < this.moov.traks.length; i2++) {
        var trak = this.moov.traks[i2];
        if (i2 > 0) {
          codecs += ",";
        }
        codecs += trak.mdia.minf.stbl.stsd.entries[0].getCodec();
      }
      return codecs;
    };
    ISOFile.prototype.getTrexById = function(id2) {
      var i2;
      if (!this.moov || !this.moov.mvex) return null;
      for (i2 = 0; i2 < this.moov.mvex.trexs.length; i2++) {
        var trex = this.moov.mvex.trexs[i2];
        if (trex.track_id == id2) return trex;
      }
      return null;
    };
    ISOFile.prototype.getTrackById = function(id2) {
      if (this.moov === void 0) {
        return null;
      }
      for (var j3 = 0; j3 < this.moov.traks.length; j3++) {
        var trak = this.moov.traks[j3];
        if (trak.tkhd.track_id == id2) return trak;
      }
      return null;
    };
    ISOFile.prototype.items = [];
    ISOFile.prototype.entity_groups = [];
    ISOFile.prototype.itemsDataSize = 0;
    ISOFile.prototype.flattenItemInfo = function() {
      var items = this.items;
      var entity_groups = this.entity_groups;
      var i2, j3;
      var item3;
      var meta = this.meta;
      if (meta === null || meta === void 0) return;
      if (meta.hdlr === void 0) return;
      if (meta.iinf === void 0) return;
      for (i2 = 0; i2 < meta.iinf.item_infos.length; i2++) {
        item3 = {};
        item3.id = meta.iinf.item_infos[i2].item_ID;
        items[item3.id] = item3;
        item3.ref_to = [];
        item3.name = meta.iinf.item_infos[i2].item_name;
        if (meta.iinf.item_infos[i2].protection_index > 0) {
          item3.protection = meta.ipro.protections[meta.iinf.item_infos[i2].protection_index - 1];
        }
        if (meta.iinf.item_infos[i2].item_type) {
          item3.type = meta.iinf.item_infos[i2].item_type;
        } else {
          item3.type = "mime";
        }
        item3.content_type = meta.iinf.item_infos[i2].content_type;
        item3.content_encoding = meta.iinf.item_infos[i2].content_encoding;
      }
      if (meta.grpl) {
        for (i2 = 0; i2 < meta.grpl.boxes.length; i2++) {
          entity_group = {};
          entity_group.id = meta.grpl.boxes[i2].group_id;
          entity_group.entity_ids = meta.grpl.boxes[i2].entity_ids;
          entity_group.type = meta.grpl.boxes[i2].type;
          entity_groups[entity_group.id] = entity_group;
        }
      }
      if (meta.iloc) {
        for (i2 = 0; i2 < meta.iloc.items.length; i2++) {
          var offset;
          var itemloc = meta.iloc.items[i2];
          item3 = items[itemloc.item_ID];
          if (itemloc.data_reference_index !== 0) {
            Log.warn("Item storage with reference to other files: not supported");
            item3.source = meta.dinf.boxes[itemloc.data_reference_index - 1];
          }
          switch (itemloc.construction_method) {
            case 0:
              break;
            case 1:
              Log.warn("Item storage with construction_method : not supported");
              break;
            case 2:
              Log.warn("Item storage with construction_method : not supported");
              break;
          }
          item3.extents = [];
          item3.size = 0;
          for (j3 = 0; j3 < itemloc.extents.length; j3++) {
            item3.extents[j3] = {};
            item3.extents[j3].offset = itemloc.extents[j3].extent_offset + itemloc.base_offset;
            item3.extents[j3].length = itemloc.extents[j3].extent_length;
            item3.extents[j3].alreadyRead = 0;
            item3.size += item3.extents[j3].length;
          }
        }
      }
      if (meta.pitm) {
        items[meta.pitm.item_id].primary = true;
      }
      if (meta.iref) {
        for (i2 = 0; i2 < meta.iref.references.length; i2++) {
          var ref = meta.iref.references[i2];
          for (j3 = 0; j3 < ref.references.length; j3++) {
            items[ref.from_item_ID].ref_to.push({ type: ref.type, id: ref.references[j3] });
          }
        }
      }
      if (meta.iprp) {
        for (var k2 = 0; k2 < meta.iprp.ipmas.length; k2++) {
          var ipma = meta.iprp.ipmas[k2];
          for (i2 = 0; i2 < ipma.associations.length; i2++) {
            var association = ipma.associations[i2];
            item3 = items[association.id];
            if (!item3) {
              item3 = entity_groups[association.id];
            }
            if (item3) {
              if (item3.properties === void 0) {
                item3.properties = {};
                item3.properties.boxes = [];
              }
              for (j3 = 0; j3 < association.props.length; j3++) {
                var propEntry = association.props[j3];
                if (propEntry.property_index > 0 && propEntry.property_index - 1 < meta.iprp.ipco.boxes.length) {
                  var propbox = meta.iprp.ipco.boxes[propEntry.property_index - 1];
                  item3.properties[propbox.type] = propbox;
                  item3.properties.boxes.push(propbox);
                }
              }
            }
          }
        }
      }
    };
    ISOFile.prototype.getItem = function(item_id) {
      var buffer;
      var item3;
      if (!this.meta) {
        return null;
      }
      item3 = this.items[item_id];
      if (!item3.data && item3.size) {
        item3.data = new Uint8Array(item3.size);
        item3.alreadyRead = 0;
        this.itemsDataSize += item3.size;
        Log.debug("ISOFile", "Allocating item #" + item_id + " of size " + item3.size + " (total: " + this.itemsDataSize + ")");
      } else if (item3.alreadyRead === item3.size) {
        return item3;
      }
      for (var i2 = 0; i2 < item3.extents.length; i2++) {
        var extent = item3.extents[i2];
        if (extent.alreadyRead === extent.length) {
          continue;
        } else {
          var index2 = this.stream.findPosition(true, extent.offset + extent.alreadyRead, false);
          if (index2 > -1) {
            buffer = this.stream.buffers[index2];
            var lengthAfterStart = buffer.byteLength - (extent.offset + extent.alreadyRead - buffer.fileStart);
            if (extent.length - extent.alreadyRead <= lengthAfterStart) {
              Log.debug("ISOFile", "Getting item #" + item_id + " extent #" + i2 + " data (alreadyRead: " + extent.alreadyRead + " offset: " + (extent.offset + extent.alreadyRead - buffer.fileStart) + " read size: " + (extent.length - extent.alreadyRead) + " full extent size: " + extent.length + " full item size: " + item3.size + ")");
              DataStream.memcpy(
                item3.data.buffer,
                item3.alreadyRead,
                buffer,
                extent.offset + extent.alreadyRead - buffer.fileStart,
                extent.length - extent.alreadyRead
              );
              buffer.usedBytes += extent.length - extent.alreadyRead;
              this.stream.logBufferLevel();
              item3.alreadyRead += extent.length - extent.alreadyRead;
              extent.alreadyRead = extent.length;
            } else {
              Log.debug("ISOFile", "Getting item #" + item_id + " extent #" + i2 + " partial data (alreadyRead: " + extent.alreadyRead + " offset: " + (extent.offset + extent.alreadyRead - buffer.fileStart) + " read size: " + lengthAfterStart + " full extent size: " + extent.length + " full item size: " + item3.size + ")");
              DataStream.memcpy(
                item3.data.buffer,
                item3.alreadyRead,
                buffer,
                extent.offset + extent.alreadyRead - buffer.fileStart,
                lengthAfterStart
              );
              extent.alreadyRead += lengthAfterStart;
              item3.alreadyRead += lengthAfterStart;
              buffer.usedBytes += lengthAfterStart;
              this.stream.logBufferLevel();
              return null;
            }
          } else {
            return null;
          }
        }
      }
      if (item3.alreadyRead === item3.size) {
        return item3;
      } else {
        return null;
      }
    };
    ISOFile.prototype.releaseItem = function(item_id) {
      var item3 = this.items[item_id];
      if (item3.data) {
        this.itemsDataSize -= item3.size;
        item3.data = null;
        item3.alreadyRead = 0;
        for (var i2 = 0; i2 < item3.extents.length; i2++) {
          var extent = item3.extents[i2];
          extent.alreadyRead = 0;
        }
        return item3.size;
      } else {
        return 0;
      }
    };
    ISOFile.prototype.processItems = function(callback) {
      for (var i2 in this.items) {
        var item3 = this.items[i2];
        this.getItem(item3.id);
        if (callback && !item3.sent) {
          callback(item3);
          item3.sent = true;
          item3.data = null;
        }
      }
    };
    ISOFile.prototype.hasItem = function(name2) {
      for (var i2 in this.items) {
        var item3 = this.items[i2];
        if (item3.name === name2) {
          return item3.id;
        }
      }
      return -1;
    };
    ISOFile.prototype.getMetaHandler = function() {
      if (!this.meta) {
        return null;
      } else {
        return this.meta.hdlr.handler;
      }
    };
    ISOFile.prototype.getPrimaryItem = function() {
      if (!this.meta || !this.meta.pitm) {
        return null;
      } else {
        return this.getItem(this.meta.pitm.item_id);
      }
    };
    ISOFile.prototype.itemToFragmentedTrackFile = function(_options) {
      var options = _options || {};
      var item3 = null;
      if (options.itemId) {
        item3 = this.getItem(options.itemId);
      } else {
        item3 = this.getPrimaryItem();
      }
      if (item3 == null) return null;
      var file = new ISOFile();
      file.discardMdatData = false;
      var trackOptions = { type: item3.type, description_boxes: item3.properties.boxes };
      if (item3.properties.ispe) {
        trackOptions.width = item3.properties.ispe.image_width;
        trackOptions.height = item3.properties.ispe.image_height;
      }
      var trackId = file.addTrack(trackOptions);
      if (trackId) {
        file.addSample(trackId, item3.data);
        return file;
      } else {
        return null;
      }
    };
    ISOFile.prototype.write = function(outstream) {
      for (var i2 = 0; i2 < this.boxes.length; i2++) {
        this.boxes[i2].write(outstream);
      }
    };
    ISOFile.prototype.createFragment = function(track_id, sampleNumber, stream_) {
      var trak = this.getTrackById(track_id);
      var sample = this.getSample(trak, sampleNumber);
      if (sample == null) {
        this.setNextSeekPositionFromSample(trak.samples[sampleNumber]);
        return null;
      }
      var stream = stream_ || new DataStream();
      stream.endianness = DataStream.BIG_ENDIAN;
      var moof = this.createSingleSampleMoof(sample);
      moof.write(stream);
      moof.trafs[0].truns[0].data_offset = moof.size + 8;
      Log.debug("MP4Box", "Adjusting data_offset with new value " + moof.trafs[0].truns[0].data_offset);
      stream.adjustUint32(moof.trafs[0].truns[0].data_offset_position, moof.trafs[0].truns[0].data_offset);
      var mdat = new BoxParser.mdatBox();
      mdat.data = sample.data;
      mdat.write(stream);
      return stream;
    };
    ISOFile.writeInitializationSegment = function(ftyp, moov, total_duration, sample_duration) {
      var i2;
      var index2;
      var mehd;
      var trex;
      var box2;
      Log.debug("ISOFile", "Generating initialization segment");
      var stream = new DataStream();
      stream.endianness = DataStream.BIG_ENDIAN;
      ftyp.write(stream);
      var mvex = moov.add("mvex");
      if (total_duration) {
        mvex.add("mehd").set("fragment_duration", total_duration);
      }
      for (i2 = 0; i2 < moov.traks.length; i2++) {
        mvex.add("trex").set("track_id", moov.traks[i2].tkhd.track_id).set("default_sample_description_index", 1).set("default_sample_duration", sample_duration).set("default_sample_size", 0).set("default_sample_flags", 1 << 16);
      }
      moov.write(stream);
      return stream.buffer;
    };
    ISOFile.prototype.save = function(name2) {
      var stream = new DataStream();
      stream.endianness = DataStream.BIG_ENDIAN;
      this.write(stream);
      stream.save(name2);
    };
    ISOFile.prototype.getBuffer = function() {
      var stream = new DataStream();
      stream.endianness = DataStream.BIG_ENDIAN;
      this.write(stream);
      return stream.buffer;
    };
    ISOFile.prototype.initializeSegmentation = function() {
      var i2;
      var j3;
      var box2;
      var initSegs;
      var trak;
      var seg;
      if (this.onSegment === null) {
        Log.warn("MP4Box", "No segmentation callback set!");
      }
      if (!this.isFragmentationInitialized) {
        this.isFragmentationInitialized = true;
        this.nextMoofNumber = 0;
        this.resetTables();
      }
      initSegs = [];
      for (i2 = 0; i2 < this.fragmentedTracks.length; i2++) {
        var moov = new BoxParser.moovBox();
        moov.mvhd = this.moov.mvhd;
        moov.boxes.push(moov.mvhd);
        trak = this.getTrackById(this.fragmentedTracks[i2].id);
        moov.boxes.push(trak);
        moov.traks.push(trak);
        seg = {};
        seg.id = trak.tkhd.track_id;
        seg.user = this.fragmentedTracks[i2].user;
        seg.buffer = ISOFile.writeInitializationSegment(this.ftyp, moov, this.moov.mvex && this.moov.mvex.mehd ? this.moov.mvex.mehd.fragment_duration : void 0, this.moov.traks[i2].samples.length > 0 ? this.moov.traks[i2].samples[0].duration : 0);
        initSegs.push(seg);
      }
      return initSegs;
    };
    BoxParser.Box.prototype.printHeader = function(output) {
      this.size += 8;
      if (this.size > MAX_SIZE) {
        this.size += 8;
      }
      if (this.type === "uuid") {
        this.size += 16;
      }
      output.log(output.indent + "size:" + this.size);
      output.log(output.indent + "type:" + this.type);
    };
    BoxParser.FullBox.prototype.printHeader = function(output) {
      this.size += 4;
      BoxParser.Box.prototype.printHeader.call(this, output);
      output.log(output.indent + "version:" + this.version);
      output.log(output.indent + "flags:" + this.flags);
    };
    BoxParser.Box.prototype.print = function(output) {
      this.printHeader(output);
    };
    BoxParser.ContainerBox.prototype.print = function(output) {
      this.printHeader(output);
      for (var i2 = 0; i2 < this.boxes.length; i2++) {
        if (this.boxes[i2]) {
          var prev_indent = output.indent;
          output.indent += " ";
          this.boxes[i2].print(output);
          output.indent = prev_indent;
        }
      }
    };
    ISOFile.prototype.print = function(output) {
      output.indent = "";
      for (var i2 = 0; i2 < this.boxes.length; i2++) {
        if (this.boxes[i2]) {
          this.boxes[i2].print(output);
        }
      }
    };
    BoxParser.mvhdBox.prototype.print = function(output) {
      BoxParser.FullBox.prototype.printHeader.call(this, output);
      output.log(output.indent + "creation_time: " + this.creation_time);
      output.log(output.indent + "modification_time: " + this.modification_time);
      output.log(output.indent + "timescale: " + this.timescale);
      output.log(output.indent + "duration: " + this.duration);
      output.log(output.indent + "rate: " + this.rate);
      output.log(output.indent + "volume: " + (this.volume >> 8));
      output.log(output.indent + "matrix: " + this.matrix.join(", "));
      output.log(output.indent + "next_track_id: " + this.next_track_id);
    };
    BoxParser.tkhdBox.prototype.print = function(output) {
      BoxParser.FullBox.prototype.printHeader.call(this, output);
      output.log(output.indent + "creation_time: " + this.creation_time);
      output.log(output.indent + "modification_time: " + this.modification_time);
      output.log(output.indent + "track_id: " + this.track_id);
      output.log(output.indent + "duration: " + this.duration);
      output.log(output.indent + "volume: " + (this.volume >> 8));
      output.log(output.indent + "matrix: " + this.matrix.join(", "));
      output.log(output.indent + "layer: " + this.layer);
      output.log(output.indent + "alternate_group: " + this.alternate_group);
      output.log(output.indent + "width: " + this.width);
      output.log(output.indent + "height: " + this.height);
    };
    var MP4Box = {};
    MP4Box.createFile = function(_keepMdatData, _stream) {
      var keepMdatData = _keepMdatData !== void 0 ? _keepMdatData : true;
      var file = new ISOFile(_stream);
      file.discardMdatData = keepMdatData ? false : true;
      return file;
    };
    if (typeof exports !== "undefined") {
      exports.createFile = MP4Box.createFile;
    }
  }
});

// src/client/index.ts
var index_exports = {};
__export(index_exports, {
  apply: () => apply,
  inject: () => inject,
  name: () => name
});
module.exports = __toCommonJS(index_exports);

// src/client/DirectorxSettingsSection.tsx
var import_react = require("react");

// src/modelverse-catalog.ts
var MODELVERSE_VISION = [
  "gpt-5.6-luna",
  "glm-5v-turbo",
  "qwen3-vl-flash",
  "Qwen/Qwen3-vl-Plus",
  "Qwen/Qwen3-VL-235B-A22B-Instruct",
  "zai-org/glm-4.6v",
  "baidu/ernie-4.5-turbo-vl-32k"
];
var MODELVERSE_IMAGE = [
  "gpt-image-2",
  "doubao-seedream-5-0-pro-260628",
  "doubao-seedream-5-0-260128",
  "doubao-seedream-4.5",
  "Qwen/Qwen-Image",
  "Qwen/Qwen-Image-Edit",
  "wan2.7-image-pro",
  "wan2.7-image"
];
var MODELVERSE_VIDEO = [
  "MiniMax-H3",
  "kling-v3",
  "kling-v3-omni",
  "kling-v2-6",
  "kling-video-o1",
  "wan2.7-t2v",
  "wan2.7-i2v",
  "wan2.7-r2v",
  "viduq3-pro",
  "viduq3-turbo",
  "MiniMax-Hailuo-2.3",
  "MiniMax-Hailuo-2.3-Fast"
];
var MODELVERSE_AUDIO = [
  "gpt-4o-mini-tts",
  "qwen3-tts-flash",
  "cicada-tts",
  "speech-2.8-hd",
  "speech-2.8-turbo",
  "IndexTeam/IndexTTS-2"
];
var DEEPSEEK_FIRST_PARTY_VISION = ["deepseek-v4-flash-vision-exp"];
var MODELVERSE_BY_CAPABILITY = {
  vision: MODELVERSE_VISION,
  image: MODELVERSE_IMAGE,
  video: MODELVERSE_VIDEO,
  audio: MODELVERSE_AUDIO
};

// src/client/stage/session-fold.ts
function mintSessionRequestId() {
  return `directorx-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
}
function sessionTextNeedsFold(line) {
  return line.kind === "assistant" && line.text.length > 420;
}
function toolCaption(name2, args) {
  const raw = name2.trim() || "tool";
  if (raw === "skill") {
    if (args !== void 0 && args.trim() !== "") {
      try {
        const parsed = JSON.parse(args);
        if (typeof parsed.name === "string" && parsed.name.trim() !== "") return `skill\uFF1A${parsed.name.trim()}`;
      } catch {
      }
    }
    return "skill\uFF1A";
  }
  if (raw === "ask_user_question") return "ask user question\uFF1A";
  const short = raw.startsWith("directorx_") ? raw.slice("directorx_".length) : raw;
  return `${short.replaceAll("_", " ")}\uFF1A`;
}
function textFromBlocks(value) {
  if (typeof value === "string") return value.trim();
  if (!Array.isArray(value)) {
    if (value !== null && typeof value === "object") {
      const rec = value;
      if (typeof rec.text === "string") return rec.text.trim();
    }
    return "";
  }
  const parts = [];
  for (const block of value) {
    if (typeof block === "string") {
      if (block.trim() !== "") parts.push(block);
      continue;
    }
    if (block === null || typeof block !== "object") continue;
    const rec = block;
    if (rec.type === "reasoning") continue;
    if (typeof rec.text === "string" && rec.text.trim() !== "") parts.push(rec.text);
  }
  return parts.join("\n").trim();
}
function rpcOk(response) {
  if (response === null || response === void 0) return { ok: false, message: "\u7A7A\u54CD\u5E94" };
  if (typeof response !== "object") return { ok: false, message: "\u54CD\u5E94\u683C\u5F0F\u65E0\u6548" };
  const rec = response;
  const result = rec.result !== null && typeof rec.result === "object" ? rec.result : rec;
  if (result.ok === false) return { ok: false, message: errorMessage(result.error) };
  if ("value" in result) return { ok: true, value: result.value };
  return { ok: true, value: result };
}
function errorMessage(error) {
  if (typeof error === "string" && error.trim() !== "") return error;
  if (error !== null && typeof error === "object") {
    const rec = error;
    if (typeof rec.message === "string" && rec.message.trim() !== "") return rec.message;
    if (typeof rec.code === "string" && rec.code.trim() !== "") return rec.code;
  }
  return "DSH \u8BF7\u6C42\u5931\u8D25";
}
function asRecord(value) {
  if (value === null || typeof value !== "object") return void 0;
  return value;
}
function parseReasoning(input2) {
  const rec = asRecord(input2);
  if (rec === void 0 || !Array.isArray(rec.efforts)) return void 0;
  const efforts = rec.efforts.flatMap((item3) => {
    const row3 = asRecord(item3);
    if (row3 === void 0 || typeof row3.id !== "string" || row3.id === "" || typeof row3.name !== "string") return [];
    return [{ id: row3.id, name: row3.name }];
  });
  if (efforts.length === 0) return void 0;
  return {
    efforts,
    ...typeof rec.defaultEffort === "string" && rec.defaultEffort !== "" ? { defaultEffort: rec.defaultEffort } : {}
  };
}
function parseSessionModels(input2) {
  const parsed = rpcOk(input2);
  if (!parsed.ok) return void 0;
  const rec = asRecord(parsed.value);
  if (rec === void 0) return void 0;
  const currentRec = asRecord(rec.current) ?? asRecord(rec.default);
  const provider = typeof currentRec?.provider === "string" ? currentRec.provider : "";
  const model = typeof currentRec?.model === "string" ? currentRec.model : "";
  if (provider === "" || model === "") return void 0;
  const groups = [];
  if (Array.isArray(rec.groups)) {
    for (const item3 of rec.groups) {
      const group = asRecord(item3);
      if (group === void 0 || typeof group.id !== "string" || group.id === "") continue;
      const models = [];
      if (Array.isArray(group.models)) {
        for (const raw of group.models) {
          const row3 = asRecord(raw);
          if (row3 === void 0 || typeof row3.id !== "string" || row3.id === "") continue;
          const reasoning = parseReasoning(row3.reasoning);
          models.push({
            id: row3.id,
            name: typeof row3.name === "string" && row3.name !== "" ? row3.name : row3.id,
            ...typeof row3.description === "string" && row3.description !== "" ? { description: row3.description } : {},
            ...reasoning !== void 0 ? { reasoning } : {}
          });
        }
      }
      groups.push({
        id: group.id,
        name: typeof group.name === "string" && group.name !== "" ? group.name : group.id,
        models
      });
    }
  }
  const routableProviders = Array.isArray(rec.routableProviders) ? rec.routableProviders.filter((value) => typeof value === "string") : void 0;
  return {
    current: {
      provider,
      model,
      ...typeof currentRec?.reasoningEffort === "string" && currentRec.reasoningEffort !== "" ? { reasoningEffort: currentRec.reasoningEffort } : {}
    },
    routable: rec.routable === false ? false : routableProviders === void 0 ? rec.routable !== false : routableProviders.includes(provider),
    groups
  };
}
function unwrapEvent(item3) {
  const rec = asRecord(item3);
  if (rec === void 0) return void 0;
  const nested = asRecord(rec.event);
  if (nested !== void 0 && typeof nested.type === "string") return nested;
  if (typeof rec.type === "string") return rec;
  return void 0;
}
function extractEvents(input2) {
  const unwrapped = rpcOk(input2);
  const body = unwrapped.ok ? unwrapped.value : input2;
  if (Array.isArray(body)) return body;
  const rec = asRecord(body);
  if (rec === void 0) return [];
  if (Array.isArray(rec.records)) return rec.records;
  if (Array.isArray(rec.events)) return rec.events;
  if (Array.isArray(rec.items)) return rec.items;
  return [];
}
function userSourceKind(data) {
  const source = asRecord(data.source);
  return typeof source?.kind === "string" ? source.kind : "user";
}
function assistantContent(data) {
  const message = asRecord(data.message);
  if (message !== void 0 && message.content !== void 0) return message.content;
  return data.content;
}
function clip(text, max = 4e3) {
  return text.length <= max ? text : `${text.slice(0, max - 1)}\u2026`;
}
function prettyArg(value) {
  if (value === void 0 || value === null) return void 0;
  if (typeof value === "string") {
    const trimmed = value.trim();
    if (trimmed === "") return void 0;
    try {
      return clip(JSON.stringify(JSON.parse(trimmed), null, 2), 6e3);
    } catch {
      return clip(trimmed, 6e3);
    }
  }
  try {
    return clip(JSON.stringify(value, null, 2), 6e3);
  } catch {
    return clip(String(value), 6e3);
  }
}
function stripHarnessNoise(text) {
  const cut = text.indexOf("<system-reminder>");
  return cut === -1 ? text : text.slice(0, cut).trim();
}
function foldSessionHistory(input2) {
  const lines = [];
  let running = false;
  let blocked = false;
  for (const item3 of extractEvents(input2)) {
    const event = unwrapEvent(item3);
    if (event === void 0) continue;
    const type = event.type;
    const seq = typeof event.seq === "number" ? String(event.seq) : String(lines.length);
    const data = asRecord(event.data) ?? {};
    if (type === "turn/start") {
      running = true;
      blocked = false;
      continue;
    }
    if (type === "turn/end") {
      running = false;
      const reason = asRecord(data.reason);
      const kind = typeof reason?.kind === "string" ? reason.kind : typeof data.kind === "string" ? data.kind : "";
      if (kind === "blocked") {
        blocked = true;
        lines.push({ id: `block-${seq}`, kind: "notice", text: "DSH \u5728\u7B49\u6279\u51C6\u6216\u56DE\u7B54" });
      } else if (kind === "error") {
        const err = asRecord(reason?.error) ?? asRecord(data.error);
        const message = typeof err?.message === "string" && err.message !== "" ? err.message : "\u672C\u8F6E\u5931\u8D25";
        lines.push({ id: `err-${seq}`, kind: "notice", text: message });
      }
      continue;
    }
    if (type === "user/message") {
      const source = userSourceKind(data);
      if (source === "plugin") {
        const summary2 = asRecord(data.source);
        const notice2 = typeof summary2?.summary === "string" ? summary2.summary.trim() : "";
        if (notice2 !== "") lines.push({ id: `ctx-${seq}`, kind: "notice", text: clip(notice2, 180) });
        continue;
      }
      if (source === "tool") continue;
      const text = stripHarnessNoise(textFromBlocks(data.content));
      if (text !== "") lines.push({ id: `user-${seq}`, kind: "user", text: clip(text) });
      continue;
    }
    if (type === "assistant/message") {
      const text = textFromBlocks(assistantContent(data));
      if (text !== "") lines.push({ id: `asst-${seq}`, kind: "assistant", text: clip(text) });
      continue;
    }
    if (type === "assistant/chunk") {
      const chunk = asRecord(data.chunk) ?? data;
      if (chunk.type === "text-delta" && typeof chunk.text === "string" && chunk.text !== "") {
        const last = lines.at(-1);
        if (last?.kind === "assistant" && last.id.startsWith("stream-")) last.text = clip(last.text + chunk.text);
        else lines.push({ id: `stream-${seq}`, kind: "assistant", text: clip(chunk.text) });
      }
      continue;
    }
    if (type === "tool/call") {
      const name2 = typeof data.name === "string" ? data.name : "tool";
      const args = prettyArg(data.arguments ?? data.args ?? data.input);
      lines.push({
        id: `tool-${seq}`,
        kind: "tool",
        name: name2,
        text: toolCaption(name2, typeof data.arguments === "string" ? data.arguments : args),
        ...args !== void 0 ? { args } : {},
        status: "running"
      });
      continue;
    }
    if (type === "tool/result") {
      const err = asRecord(data.error);
      const rawResult = data.content ?? data.result ?? data.output;
      const result = textFromBlocks(rawResult) || prettyArg(rawResult) || "";
      const last = [...lines].reverse().find((line) => line.kind === "tool" && line.status === "running");
      if (last !== void 0) {
        last.status = err !== void 0 ? "error" : "ok";
        if (err !== void 0) {
          last.result = typeof err.message === "string" && err.message !== "" ? err.message : "\u5DE5\u5177\u5931\u8D25";
        } else if (result !== "") last.result = result;
      } else if (err !== void 0) {
        const message = typeof err.message === "string" && err.message !== "" ? err.message : "\u5DE5\u5177\u5931\u8D25";
        lines.push({ id: `toolerr-${seq}`, kind: "notice", text: message });
      }
    }
  }
  return { lines, running, blocked };
}
function rpcItems(input2) {
  const unwrapped = rpcOk(input2);
  const body = unwrapped.ok ? unwrapped.value : input2;
  if (Array.isArray(body)) return body;
  const rec = asRecord(body);
  if (rec === void 0) return [];
  if (Array.isArray(rec.items)) return rec.items;
  if (Array.isArray(rec.sessions)) return rec.sessions;
  return [];
}
function normalizeDir(path) {
  return path.replaceAll("\\", "/").replace(/\/+$/, "").trim();
}
function sameDir(left, right) {
  if (left === void 0 || right === void 0) return false;
  const a = normalizeDir(left);
  const b = normalizeDir(right);
  return a !== "" && a === b;
}
function parseSessionList(input2) {
  const rows = [];
  for (const row3 of rpcItems(input2)) {
    const item3 = asRecord(row3);
    if (item3 === void 0) continue;
    const id2 = typeof item3.sessionId === "string" ? item3.sessionId : typeof item3.id === "string" ? item3.id : void 0;
    if (id2 === void 0 || id2 === "") continue;
    const cwd = typeof item3.cwd === "string" ? item3.cwd : typeof asRecord(item3.header)?.cwd === "string" ? String(asRecord(item3.header)?.cwd) : void 0;
    rows.push({
      id: id2,
      ...cwd !== void 0 ? { cwd } : {},
      ...typeof item3.updatedAt === "number" ? { updatedAt: item3.updatedAt } : {},
      ...typeof item3.blank === "boolean" ? { blank: item3.blank } : {}
    });
  }
  return rows;
}
function parseWorkspaceList(input2) {
  const rows = [];
  for (const row3 of rpcItems(input2)) {
    const item3 = asRecord(row3);
    if (item3 === void 0) continue;
    const path = typeof item3.path === "string" ? item3.path : void 0;
    const id2 = typeof item3.workspaceId === "string" ? item3.workspaceId : typeof item3.id === "string" ? item3.id : void 0;
    const sessionIds = Array.isArray(item3.sessionIds) ? item3.sessionIds.filter((value) => typeof value === "string" && value !== "") : [];
    rows.push({
      ...id2 !== void 0 ? { id: id2 } : {},
      ...path !== void 0 ? { path } : {},
      sessionIds
    });
  }
  return rows;
}
function parseArchivedIds(input2) {
  const unwrapped = rpcOk(input2);
  const body = unwrapped.ok ? unwrapped.value : input2;
  const rec = asRecord(body);
  const ids = rec !== void 0 && Array.isArray(rec.archivedSessionIds) ? rec.archivedSessionIds : [];
  return ids.filter((value) => typeof value === "string" && value !== "");
}
function createdSessionId(input2) {
  const unwrapped = rpcOk(input2);
  const body = unwrapped.ok ? unwrapped.value : input2;
  const rec = asRecord(body);
  if (rec === void 0) return void 0;
  if (typeof rec.sessionId === "string" && rec.sessionId !== "") return rec.sessionId;
  if (typeof rec.id === "string" && rec.id !== "") return rec.id;
  return void 0;
}
function pickWorkspaceSession(input2) {
  const project = input2.project !== void 0 ? normalizeDir(input2.project) : "";
  if (project === "") return void 0;
  const archived = new Set(input2.archivedIds ?? []);
  const workspace = (input2.workspaces ?? []).find((item3) => sameDir(item3.path, project));
  const byId = new Map(input2.sessions.map((item3) => [item3.id, item3]));
  const candidates = /* @__PURE__ */ new Map();
  for (const row3 of input2.sessions) {
    if (sameDir(row3.cwd, project) && !archived.has(row3.id)) candidates.set(row3.id, row3);
  }
  for (const id2 of workspace?.sessionIds ?? []) {
    if (archived.has(id2) || candidates.has(id2)) continue;
    const row3 = byId.get(id2);
    candidates.set(id2, row3 ?? { id: id2, cwd: project });
  }
  if (candidates.size === 0) return void 0;
  const preferred = input2.preferredId;
  if (preferred !== void 0 && candidates.has(preferred)) return candidates.get(preferred);
  return [...candidates.values()].sort((left, right) => {
    const blank = Number(left.blank === true) - Number(right.blank === true);
    if (blank !== 0) return blank;
    return (right.updatedAt ?? 0) - (left.updatedAt ?? 0);
  })[0];
}
function sessionRunningFromList(input2, sessionId) {
  for (const row3 of rpcItems(input2)) {
    const item3 = asRecord(row3);
    if (item3 === void 0) continue;
    const id2 = typeof item3.sessionId === "string" ? item3.sessionId : typeof item3.id === "string" ? item3.id : void 0;
    if (id2 !== sessionId) continue;
    if (typeof item3.running === "boolean") return item3.running;
  }
  return void 0;
}

// src/client/stage/project.ts
var STORAGE_KEY = "directorx-project";
var activeProject;
function getClientProject() {
  if (activeProject !== void 0 && activeProject !== "") return activeProject;
  try {
    const stored = localStorage.getItem(STORAGE_KEY);
    return stored !== null && stored !== "" ? stored : void 0;
  } catch {
    return void 0;
  }
}
function setClientProject(path) {
  activeProject = path;
  try {
    localStorage.setItem(STORAGE_KEY, path);
  } catch {
  }
}
function withProject(url) {
  const project = getClientProject();
  if (project === void 0) return url;
  const sep = url.includes("?") ? "&" : "?";
  return `${url}${sep}project=${encodeURIComponent(project)}`;
}
function projectHeaders() {
  const project = getClientProject();
  return project === void 0 ? {} : { "x-directorx-project": encodeURIComponent(project) };
}
function inferProjectFromWorkspaceUi(projects) {
  const byTitle = (title) => projects.find((item3) => item3.title === title)?.path;
  const afterLabel = (document.body?.innerText ?? "").split("\u5DE5\u4F5C\u533A\n")[1] ?? "";
  const currentTitle = afterLabel.split("\n").map((line) => line.trim()).find((line) => line !== "");
  if (currentTitle !== void 0) {
    const matched = byTitle(currentTitle);
    if (matched !== void 0) return matched;
  }
  const selected2 = document.querySelector('[role="tree"] [aria-current="true"], [role="tree"] [aria-selected="true"]');
  const selectedTitle = selected2?.textContent?.trim().split("\n")[0] ?? "";
  return selectedTitle !== "" ? byTitle(selectedTitle) : void 0;
}
function pickDefaultProject(projects, preferred) {
  if (projects.length === 0) return void 0;
  if (preferred !== void 0 && preferred !== "" && projects.some((item3) => item3.path === preferred)) return preferred;
  const fromUi = inferProjectFromWorkspaceUi(projects);
  if (fromUi !== void 0) return fromUi;
  const stored = getClientProject();
  if (stored !== void 0 && projects.some((item3) => item3.path === stored)) return stored;
  return projects[0]?.path;
}

// src/client/DirectorxSettingsSection.tsx
var import_jsx_runtime = require("react/jsx-runtime");
var DEFAULT_DRAFT = {
  vision: { enabled: true, mode: "deepseek-chat", baseURL: "https://api.deepseek.com", model: "deepseek-v4-flash-vision-exp", resolution: "1K", apiKey: "", klingAk: "", klingSk: "", runwayVersion: "" },
  image: { enabled: true, mode: "openai-images", baseURL: "https://api.modelverse.cn/v1", model: "gpt-image-2", resolution: "1K", apiKey: "", klingAk: "", klingSk: "", runwayVersion: "" },
  video: { enabled: true, mode: "modelverse-tasks", baseURL: "https://api.modelverse.cn/v1", model: "MiniMax-H3", resolution: "2K", apiKey: "", klingAk: "", klingSk: "", runwayVersion: "2024-11-06" },
  audio: { enabled: true, mode: "openai-tts", baseURL: "https://api.modelverse.cn/v1", model: "gpt-4o-mini-tts", resolution: "1K", apiKey: "", klingAk: "", klingSk: "", runwayVersion: "" }
};
var MODES = {
  vision: ["deepseek-chat", "openai-chat", "mock"],
  image: ["openai-images", "modelverse-tasks", "generic-rest", "mock"],
  video: ["openai-videos", "modelverse-tasks", "kling", "kling-v3", "runway", "minimax-h3", "vidu", "veo", "generic-rest", "mock"],
  audio: ["openai-tts", "generic-rest", "mock"]
};
var CAPABILITY_LABEL = {
  vision: "\u89C6\u89C9 / Vision",
  image: "\u56FE\u50CF\u751F\u6210 / Image",
  video: "\u89C6\u9891\u751F\u6210 / Video",
  audio: "\u97F3\u9891\u751F\u6210 / Audio"
};
function readDraft(value) {
  const next = { ...DEFAULT_DRAFT };
  for (const key of Object.keys(next)) {
    const raw = value?.[key];
    if (raw !== null && typeof raw === "object") {
      const record = raw;
      const auth = record.auth !== null && typeof record.auth === "object" ? record.auth : {};
      next[key] = {
        enabled: typeof record.enabled === "boolean" ? record.enabled : next[key].enabled,
        mode: typeof record.mode === "string" ? record.mode : next[key].mode,
        baseURL: typeof record.baseURL === "string" ? record.baseURL : next[key].baseURL,
        model: typeof record.model === "string" ? record.model : next[key].model,
        resolution: typeof record.resolution === "string" ? record.resolution : next[key].resolution,
        apiKey: "",
        klingAk: "",
        klingSk: "",
        runwayVersion: typeof auth.runwayVersion === "string" ? auth.runwayVersion : next[key].runwayVersion
      };
    }
  }
  return next;
}
function messageOf(error) {
  return error instanceof Error ? error.message : String(error);
}
var settingsRoot = {
  width: "100%",
  maxWidth: 1040,
  boxSizing: "border-box",
  padding: "clamp(14px, 2.2vw, 24px)",
  color: "var(--dsw-alias-label-primary)",
  fontSize: 13,
  overflowWrap: "anywhere"
};
var card = {
  minWidth: 0,
  boxSizing: "border-box",
  border: "1px solid var(--dsw-alias-border-l2)",
  borderRadius: 12,
  padding: "clamp(12px, 1.8vw, 16px)",
  marginBottom: 12,
  background: "var(--dsw-alias-bg-layer-2)"
};
var capabilityCard = { ...card, marginBottom: 0 };
var capabilityGrid = {
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(min(100%, 360px), 1fr))",
  gap: 12,
  alignItems: "start"
};
var cardHeader = {
  display: "flex",
  alignItems: "flex-start",
  justifyContent: "space-between",
  gap: 12,
  minWidth: 0,
  paddingBottom: 10,
  marginBottom: 12,
  borderBottom: "1px solid var(--dsw-alias-border-l2)"
};
var cardSub = { display: "block", marginTop: 3, fontSize: 11, color: "var(--dsw-alias-label-tertiary)" };
var row = {
  display: "grid",
  gridTemplateColumns: "minmax(84px, .42fr) minmax(0, 1fr)",
  gap: "6px 12px",
  marginBottom: 10,
  alignItems: "center",
  minWidth: 0
};
var label = { minWidth: 0, fontSize: 12, color: "var(--dsw-alias-label-secondary)", overflowWrap: "anywhere" };
var input = {
  width: "100%",
  minWidth: 0,
  minHeight: 32,
  boxSizing: "border-box",
  padding: "6px 9px",
  borderRadius: 8,
  border: "1px solid var(--dsw-alias-border-l2)",
  background: "var(--dsw-alias-bg-layer-1)",
  color: "var(--dsw-alias-label-primary)",
  font: "inherit",
  lineHeight: 1.35,
  outline: "none"
};
var button = {
  minHeight: 32,
  boxSizing: "border-box",
  display: "inline-flex",
  alignItems: "center",
  justifyContent: "center",
  gap: 6,
  padding: "0 12px",
  borderRadius: 8,
  border: "1px solid var(--dsw-alias-brand-primary)",
  background: "var(--dsw-alias-bg-layer-1)",
  color: "var(--dsw-alias-label-primary)",
  font: "inherit",
  fontSize: 12,
  cursor: "pointer"
};
var sectionTitle = { fontSize: 18, lineHeight: 1.25, fontWeight: 600, letterSpacing: -0.2, margin: 0 };
var hint = { fontSize: 12, color: "var(--dsw-alias-label-tertiary)", lineHeight: 1.55, margin: "6px 0 0" };
var eyebrow = { fontSize: 10, lineHeight: 1.3, letterSpacing: 0.8, color: "var(--dsw-alias-label-caption)", textTransform: "uppercase" };
var notice = { minWidth: 0, boxSizing: "border-box", padding: "8px 10px", borderRadius: 8, fontSize: 12, lineHeight: 1.45, margin: "0 0 10px" };
var errorNotice = { ...notice, color: "var(--dsw-alias-state-error-primary)", background: "var(--dsw-alias-interactive-bg-hover-danger)", border: "1px solid var(--dsw-alias-state-error-primary)" };
var successNotice = { ...notice, color: "var(--dsw-alias-state-success-primary)", background: "var(--dsw-alias-interactive-bg-hover)", border: "1px solid var(--dsw-alias-state-success-primary)" };
var warningNotice = { ...notice, color: "var(--dsw-alias-state-warn-primary)", background: "var(--dsw-alias-interactive-bg-hover)", border: "1px solid var(--dsw-alias-state-warn-primary)" };
var danger = { border: "1px solid var(--dsw-alias-state-error-primary)", background: "var(--dsw-alias-bg-layer-1)", color: "var(--dsw-alias-state-error-primary)", cursor: "pointer" };
function CapabilityCard(props) {
  const { draft } = props;
  const [testState, setTestState] = (0, import_react.useState)("idle");
  const [testMessage, setTestMessage] = (0, import_react.useState)(void 0);
  async function testConnection() {
    setTestState("testing");
    setTestMessage(void 0);
    try {
      const response = await fetch("/directorx/settings/test", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ capability: props.capability })
      });
      const data = await response.json();
      setTestState(data.ok ? "ok" : "fail");
      setTestMessage(data.message);
    } catch (cause) {
      setTestState("fail");
      setTestMessage(cause instanceof Error ? cause.message : String(cause));
    }
  }
  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: capabilityCard, children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: cardHeader, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { minWidth: 0 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: eyebrow, children: [
          props.capability,
          " capability"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { style: { display: "block", marginTop: 4, fontSize: 14, lineHeight: 1.3 }, children: props.title }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: cardSub, children: draft.enabled ? "\u5DF2\u542F\u7528 \xB7 \u914D\u7F6E\u5373\u65F6\u751F\u6548" : "\u5DF2\u505C\u7528 \xB7 \u4E0D\u6CE8\u518C\u5BF9\u5E94\u5DE5\u5177" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", { style: { flexShrink: 0, display: "inline-flex", gap: 7, alignItems: "center", fontSize: 12, color: "var(--dsw-alias-label-secondary)", cursor: "pointer" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "checkbox", checked: draft.enabled, onChange: (event) => props.onChange({ ...draft, enabled: event.target.checked }) }),
        "\u542F\u7528"
      ] })
    ] }),
    draft.enabled ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u914D\u7F6E\u65B9\u5F0F / Mode" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("select", { style: input, value: draft.mode, onChange: (event) => props.onChange({ ...draft, mode: event.target.value }), children: props.modes.map((mode) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: mode, children: mode }, mode)) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "Base URL" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, value: draft.baseURL, placeholder: "https://api.openai.com/v1", onChange: (event) => props.onChange({ ...draft, baseURL: event.target.value }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "API Key" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, type: "password", value: draft.apiKey, autoComplete: "off", placeholder: draft.baseURL.startsWith("http://localhost") || draft.baseURL.includes("127.0.0.1") ? "\u672C\u5730\u7AEF\u70B9\u53EF\u7559\u7A7A" : "sk-...\uFF08\u7559\u7A7A\u5219\u4E0D\u4FEE\u6539\uFF09", onChange: (event) => props.onChange({ ...draft, apiKey: event.target.value }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "Model" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { minWidth: 0 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
            "input",
            {
              style: input,
              list: `dx-models-${props.capability}`,
              value: draft.model,
              placeholder: "model id",
              onChange: (event) => props.onChange({ ...draft, model: event.target.value })
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("datalist", { id: `dx-models-${props.capability}`, children: [...props.capability === "vision" ? DEEPSEEK_FIRST_PARTY_VISION : [], ...MODELVERSE_BY_CAPABILITY[props.capability], ...props.extraModels ?? []].filter((id2, index2, all) => all.indexOf(id2) === index2).map((id2) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: id2 }, id2)) })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap", margin: "2px 0 12px" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u8FDE\u63A5\u6D4B\u8BD5" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { className: "dx-hit", style: { ...button, minHeight: 30, padding: "0 10px", opacity: testState === "testing" ? 0.6 : 1 }, onClick: () => void testConnection(), disabled: testState === "testing", children: testState === "testing" ? "\u6D4B\u8BD5\u4E2D\u2026" : "\u6D4B\u8BD5\u8FDE\u63A5" }),
        testMessage !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { role: testState === "fail" ? "alert" : "status", style: { fontSize: 12, color: testState === "ok" ? "var(--dsw-alias-state-success-primary)" : "var(--dsw-alias-state-error-primary)", overflowWrap: "anywhere" }, children: testMessage }) : null
      ] }),
      props.title === CAPABILITY_LABEL.video ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "Resolution" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, value: draft.resolution, placeholder: "2K / 720p / 1080p", onChange: (event) => props.onChange({ ...draft, resolution: event.target.value }) })
        ] }),
        draft.mode === "kling" ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u53EF\u7075 AccessKey" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, type: "password", autoComplete: "off", value: draft.klingAk, placeholder: "AK\u2026\uFF08\u7559\u7A7A\u5219\u4E0D\u4FEE\u6539\uFF09", onChange: (event) => props.onChange({ ...draft, klingAk: event.target.value }) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u53EF\u7075 SecretKey" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, type: "password", autoComplete: "off", value: draft.klingSk, placeholder: "SK\u2026\uFF08\u7559\u7A7A\u5219\u4E0D\u4FEE\u6539\uFF09", onChange: (event) => props.onChange({ ...draft, klingSk: event.target.value }) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: hint, children: "\u53EF\u7075\uFF08Kling\uFF09\u6A21\u5F0F\u4F7F\u7528 AK/SK \u505A JWT \u7B7E\u540D\u9274\u6743\uFF0C\u4E0D\u4F7F\u7528\u4E0A\u9762\u7684 API Key\uFF1BBase URL \u586B https://api-beijing.klingai.com\uFF08\u56FD\u5185\uFF09\u6216 https://api.klingai.com\uFF08\u5168\u7403\uFF09\u3002\u6A21\u578B\u5EFA\u8BAE kling-v2\u3002" })
        ] }) : null,
        draft.mode === "generic-rest" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: hint, children: "generic-rest \u8D70\u7528\u6237\u5165\u9A7B\u7684 AdapterSpec\u3002\u5728\u4E0B\u65B9\u300C\u63A5\u5165\u65B0\u6A21\u578B\u300D\u6216\u4F1A\u8BDD\u91CC\u4EA4\u7ED9 DSH\uFF1Aingest \u2192 classify \u2192 draft \u2192 smoke \u2192 commit\u3002" }) : null,
        draft.mode === "runway" ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "API \u7248\u672C\u5934" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, value: draft.runwayVersion, placeholder: "\u5982 2024-11-06\uFF08\u7559\u7A7A\u4E0D\u53D1\u9001\uFF09", onChange: (event) => props.onChange({ ...draft, runwayVersion: event.target.value }) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: hint, children: "Runway \u6A21\u5F0F\u4F7F\u7528\u4E0A\u9762\u7684 API Key\uFF1BBase URL \u586B https://api.dev.runwayml.com\u3002\u6A21\u578B\u5EFA\u8BAE gen4.5\uFF08\u6587\u751F/\u56FE\u751F\uFF09\u6216 gen4_turbo\uFF08\u56FE\u751F\uFF09\uFF1BHailuo 3.0 \u53EF\u901A\u8FC7\u6A21\u578B hailuo3 \u8C03\u7528\u3002" })
        ] }) : null
      ] }) : null
    ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: hint, children: "\u5173\u95ED\u540E\uFF0CDirectorX \u4E0D\u4F1A\u6CE8\u518C\u6B64\u80FD\u529B\u5BF9\u5E94\u7684\u5DE5\u5177\u3002\u5176\u5B83\u80FD\u529B\u4E0D\u53D7\u5F71\u54CD\u3002" })
  ] });
}
function viewFromScope(snapshot2) {
  if (snapshot2.status !== "ready" || snapshot2.value === void 0) return void 0;
  return {
    writable: snapshot2.writable,
    view: {
      ns: "directorx",
      schema: void 0,
      value: snapshot2.value,
      applies: "live",
      secrets: [],
      revision: snapshot2.revision ?? 0
    }
  };
}
function DirectorxSettingsSection(props) {
  const api = props.api;
  const scope = props.scope;
  const [status, setStatus] = (0, import_react.useState)("idle");
  const [draft, setDraft] = (0, import_react.useState)(DEFAULT_DRAFT);
  const [view, setView] = (0, import_react.useState)(void 0);
  const [writable, setWritable] = (0, import_react.useState)(false);
  const [saving, setSaving] = (0, import_react.useState)(false);
  const [saved, setSaved] = (0, import_react.useState)(false);
  const [error, setError] = (0, import_react.useState)(void 0);
  const [canvasInfo, setCanvasInfo] = (0, import_react.useState)(void 0);
  const [canvasAction, setCanvasAction] = (0, import_react.useState)(void 0);
  const [confirmReset, setConfirmReset] = (0, import_react.useState)(false);
  const [initiative, setInitiative] = (0, import_react.useState)("\u534F\u540C");
  const [adapters, setAdapters] = (0, import_react.useState)([]);
  const [onboard, setOnboard] = (0, import_react.useState)({ capability: "video", model: "", baseURL: "", apiDoc: "", apiDocUrl: "", apiKey: "" });
  const [onboardMsg, setOnboardMsg] = (0, import_react.useState)(void 0);
  async function refreshAdapters() {
    try {
      const response = await fetch(withProject("/directorx/adapters"), { headers: projectHeaders() });
      if (!response.ok) return;
      const body = await response.json();
      setAdapters(Array.isArray(body.adapters) ? body.adapters : []);
    } catch {
    }
  }
  async function submitOnboard() {
    setOnboardMsg("\u63D0\u4EA4\u4E2D\u2026");
    try {
      const response = await fetch(withProject("/directorx/adapters"), {
        method: "POST",
        headers: { ...projectHeaders(), "content-type": "application/json" },
        body: JSON.stringify(onboard)
      });
      const body = await response.json();
      if (!response.ok || body.ok === false) throw new Error(body.message ?? String(response.status));
      setOnboardMsg(body.hint ?? `\u5DF2\u6536\u6599 ${body.id}\u3002\u5728\u4F1A\u8BDD\u91CC\u8BF4\uFF1A\u7EE7\u7EED\u63A5\u5165 ${body.id}`);
      setOnboard((current) => ({ ...current, apiKey: "" }));
      await refreshAdapters();
    } catch (cause) {
      setOnboardMsg(`\u5931\u8D25\uFF1A${cause instanceof Error ? cause.message : String(cause)}`);
    }
  }
  async function refreshCanvas() {
    try {
      const response = await fetch(withProject("/directorx/canvas"), { headers: projectHeaders() });
      if (!response.ok) return;
      const doc = await response.json();
      setCanvasInfo({ nodes: doc.nodes.length, edges: doc.edges.length, title: doc.title });
    } catch {
    }
  }
  async function resetCanvas() {
    setCanvasAction("\u91CD\u7F6E\u4E2D\u2026");
    try {
      const response = await fetch(withProject("/directorx/canvas/reset"), { method: "POST", headers: projectHeaders() });
      if (!response.ok) throw new Error(String(response.status));
      setCanvasAction("\u5DF2\u91CD\u7F6E\uFF08\u65E7\u753B\u5E03\u5DF2\u5907\u4EFD\u4E3A canvas.json.bak-<\u65F6\u95F4\u6233>\uFF09");
      await refreshCanvas();
    } catch (cause) {
      setCanvasAction(`\u91CD\u7F6E\u5931\u8D25\uFF1A${cause instanceof Error ? cause.message : String(cause)}`);
    }
  }
  function applyNamespace(target, writableFlag) {
    setView(target);
    setDraft(readDraft(target.value));
    const mode = target.value.initiative;
    setInitiative(mode === "\u4E25\u683C" || mode === "\u81EA\u52A8" || mode === "\u534F\u540C" ? mode : "\u534F\u540C");
    setWritable(writableFlag);
    setStatus("ready");
    setError(void 0);
    void refreshCanvas();
    void refreshAdapters();
  }
  function applyScopeSnapshot(snapshot2) {
    if (snapshot2.status === "loading") {
      setStatus((prev) => prev === "idle" ? "loading" : prev);
      return;
    }
    const next = viewFromScope(snapshot2);
    if (next === void 0) {
      setStatus("ready");
      setWritable(snapshot2.writable);
      setError("\u672A\u627E\u5230 directorx \u8BBE\u7F6E\u547D\u540D\u7A7A\u95F4\u3002\u8BF7\u786E\u8BA4\u63D2\u4EF6\u5DF2\u5B89\u88C5\u5E76\u91CD\u542F DSH\u3002");
      return;
    }
    applyNamespace(next.view, next.writable);
  }
  async function load() {
    if (scope !== void 0) {
      applyScopeSnapshot(scope.getSnapshot());
      return;
    }
    if (api === void 0) return;
    setStatus((prev) => prev === "idle" ? "loading" : prev);
    setError(void 0);
    try {
      const parsed = rpcOk(await api.describe({}));
      if (!parsed.ok) throw new Error(parsed.message);
      const target = parsed.value.namespaces.find((entry) => entry.ns === "directorx");
      if (target === void 0) {
        setStatus("ready");
        setWritable(parsed.value.writable);
        setError("\u672A\u627E\u5230 directorx \u8BBE\u7F6E\u547D\u540D\u7A7A\u95F4\u3002\u8BF7\u786E\u8BA4\u63D2\u4EF6\u5DF2\u5B89\u88C5\u5E76\u91CD\u542F DSH\u3002");
        return;
      }
      applyNamespace(target, parsed.value.writable);
    } catch (loadError) {
      setStatus("error");
      setError(messageOf(loadError));
    }
  }
  (0, import_react.useEffect)(() => {
    if (scope !== void 0) {
      applyScopeSnapshot(scope.getSnapshot());
      return scope.subscribe(() => {
        applyScopeSnapshot(scope.getSnapshot());
      });
    }
    void load();
    return void 0;
  }, []);
  async function save() {
    if (saving) return;
    const revision = scope?.getSnapshot().revision ?? view?.revision;
    if (scope?.mutate === void 0 && api === void 0) {
      setError("\u5F53\u524D Host \u65E0\u6CD5\u6309\u8DEF\u5F84\u5199\u5165\u8BBE\u7F6E\uFF08\u9700\u8981 settings.mutate\uFF09\u3002");
      return;
    }
    setSaving(true);
    setSaved(false);
    setError(void 0);
    try {
      const ops = [
        { op: "set", path: ["persona"], value: "\u6210\u7247" },
        { op: "set", path: ["initiative"], value: initiative }
      ];
      for (const key of Object.keys(draft)) {
        const capability = draft[key];
        ops.push(
          { op: "set", path: [key, "enabled"], value: capability.enabled },
          { op: "set", path: [key, "mode"], value: capability.mode },
          { op: "set", path: [key, "baseURL"], value: capability.baseURL },
          { op: "set", path: [key, "model"], value: capability.model },
          { op: "set", path: [key, "resolution"], value: capability.resolution },
          { op: "set", path: [key, "auth", "runwayVersion"], value: capability.runwayVersion }
        );
        if (capability.apiKey.trim() !== "") {
          ops.push({ op: "set", path: [key, "apiKey"], value: capability.apiKey.trim() });
        }
        if (capability.klingAk.trim() !== "") {
          ops.push({ op: "set", path: [key, "auth", "klingAk"], value: capability.klingAk.trim() });
        }
        if (capability.klingSk.trim() !== "") {
          ops.push({ op: "set", path: [key, "auth", "klingSk"], value: capability.klingSk.trim() });
        }
      }
      if (scope?.mutate !== void 0) {
        await scope.mutate(ops, revision);
        if (scope !== void 0) applyScopeSnapshot(scope.getSnapshot());
      } else {
        if (api === void 0) throw new Error("\u5F53\u524D Host \u65E0\u6CD5\u6309\u8DEF\u5F84\u5199\u5165\u8BBE\u7F6E\uFF08\u9700\u8981 settings.mutate\uFF09\u3002");
        const parsed = rpcOk(await api.mutate({
          ns: "directorx",
          ops,
          ...revision === void 0 ? {} : { expectedRevision: revision }
        }));
        if (!parsed.ok) throw new Error(parsed.message);
        applyNamespace(parsed.value, writable);
      }
      setSaved(true);
      window.setTimeout(() => setSaved(false), 2500);
    } catch (saveError) {
      setError(messageOf(saveError));
    } finally {
      setSaving(false);
    }
  }
  if (api === void 0 && scope === void 0) return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: settingsRoot, children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: eyebrow, children: "DIRECTORX / SETTINGS" }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", { style: sectionTitle, children: "DirectorX \u6A21\u578B" }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: notice, role: "status", children: "Host \u6CA1\u6709\u628A settings \u9762\u6CE8\u5165\u8FDB\u6765\u3002\u628A\u63D2\u4EF6\u5207\u5230 SettingsScope \u9A71\u52A8\u7684\u7248\u672C\uFF0C\u5B83\u5C31\u4E0D\u4F1A\u8D70\u8FD9\u6761\u5206\u8DEF\u3002" })
  ] });
  if (status === "loading" || status === "idle") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { ...settingsRoot, color: "var(--dsw-alias-label-tertiary)" }, role: "status", children: "\u6B63\u5728\u52A0\u8F7D DirectorX \u6A21\u578B\u914D\u7F6E\u2026" });
  if (status === "error") return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: settingsRoot, children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: errorNotice, role: "alert", children: error ?? "\u52A0\u8F7D\u8BBE\u7F6E\u5931\u8D25" }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { style: button, onClick: () => void load(), children: "\u91CD\u8BD5" })
  ] });
  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: settingsRoot, children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 18 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: eyebrow, children: "DIRECTORX / SETTINGS" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", { style: sectionTitle, children: "DirectorX \u6A21\u578B" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: hint, children: "\u914D\u7F6E\u89C6\u89C9\u3001\u56FE\u50CF\u3001\u89C6\u9891\u4E0E\u97F3\u9891\u80FD\u529B\u3002\u8BBE\u7F6E\u7531 DSH settings \u6301\u6709\uFF0C\u4FDD\u5B58\u540E\u5373\u65F6\u66F4\u65B0\u5DE5\u5177\u6CE8\u518C\u3002" })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: card, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: cardHeader, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { minWidth: 0 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: eyebrow, children: "WORKFLOW CONTROL" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { style: { display: "block", marginTop: 4, fontSize: 14 }, children: "\u6210\u7247 persona \xB7 \u4E3B\u52A8\u6027" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: cardSub, children: "\u51B3\u5B9A DSH \u5728\u751F\u6210\u524D\u540E\u7684\u786E\u8BA4\u4E0E\u6267\u884C\u8282\u594F" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { flexShrink: 0, padding: "3px 8px", borderRadius: 999, border: "1px solid var(--dsw-alias-border-l2)", color: "var(--dsw-alias-label-secondary)", fontSize: 11 }, children: "DSH loop" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: { ...hint, marginTop: -2, marginBottom: 12 }, children: "\u4E25\u683C\uFF1A\u591A\u786E\u8BA4\u3001\u4E0D\u751F\u6210\uFF1B\u81EA\u52A8\uFF1A\u9884\u7B97\u5185\u76F4\u63A5\u6267\u884C\uFF1B\u534F\u540C\uFF1A\u5148\u7ED9\u63D0\u793A\u8BCD\u4E0E\u5360\u4F4D\uFF0C\u5BA1\u9605\u540E\u6267\u884C\u3002" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { ...row, marginBottom: 0 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u4E3B\u52A8\u6027" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("select", { style: input, value: initiative, onChange: (event) => setInitiative(event.target.value), children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "\u4E25\u683C", children: "\u4E25\u683C" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "\u81EA\u52A8", children: "\u81EA\u52A8" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "\u534F\u540C", children: "\u534F\u540C" })
        ] })
      ] })
    ] }),
    !writable ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: warningNotice, role: "status", children: "\u5F53\u524D settings \u540E\u7AEF\u53EA\u8BFB\uFF0C\u65E0\u6CD5\u4FDD\u5B58\u4FEE\u6539\u3002" }) : null,
    error !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: errorNotice, role: "alert", children: error }) : null,
    saved ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: successNotice, role: "status", children: "\u5DF2\u4FDD\u5B58\u5E76\u70ED\u66F4\u65B0\u5DE5\u5177\u6CE8\u518C\u3002" }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { ...card, padding: 0, border: 0, background: "transparent" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { ...eyebrow, margin: "2px 0 8px" }, children: "CAPABILITIES" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: capabilityGrid, children: Object.keys(draft).map((key) => {
        const capabilityKey = key;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          CapabilityCard,
          {
            title: CAPABILITY_LABEL[capabilityKey],
            capability: capabilityKey,
            draft: draft[capabilityKey],
            modes: MODES[capabilityKey],
            extraModels: adapters.filter((item3) => item3.capability === capabilityKey).map((item3) => item3.model),
            onChange: (next) => {
              setDraft((current) => ({ ...current, [capabilityKey]: next }));
            }
          },
          capabilityKey
        );
      }) })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: card, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: cardHeader, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { minWidth: 0 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: eyebrow, children: "ADAPTER INTAKE" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { style: { display: "block", marginTop: 4, fontSize: 14 }, children: "\u63A5\u5165\u65B0\u6A21\u578B" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: cardSub, children: "\u8FD9\u91CC\u53EA\u6536\u6599\uFF1BDSH \u6309 ingest \u2192 classify \u2192 draft \u2192 smoke \u2192 commit \u5B8C\u6210\u5165\u9A7B" })
      ] }) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u80FD\u529B" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("select", { style: input, value: onboard.capability, onChange: (event) => setOnboard((current) => ({ ...current, capability: event.target.value })), children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "video", children: "\u89C6\u9891" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "image", children: "\u56FE\u50CF" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "audio", children: "\u97F3\u9891" })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "Model" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, value: onboard.model, placeholder: "\u4E0A\u6E38 model id", onChange: (event) => setOnboard((current) => ({ ...current, model: event.target.value })) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "Base URL" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, value: onboard.baseURL, placeholder: "https://api.example.com/v1", onChange: (event) => setOnboard((current) => ({ ...current, baseURL: event.target.value })) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u6587\u6863 URL" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, value: onboard.apiDocUrl, placeholder: "\u53EF\u9009", onChange: (event) => setOnboard((current) => ({ ...current, apiDocUrl: event.target.value })) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "\u6587\u6863\u6B63\u6587" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("textarea", { style: { ...input, minHeight: 88, resize: "vertical" }, value: onboard.apiDoc, placeholder: "\u7C98\u8D34 API \u6587\u6863\u5173\u952E\u7AE0\u8282", onChange: (event) => setOnboard((current) => ({ ...current, apiDoc: event.target.value })) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: label, children: "API Key" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { style: input, type: "password", autoComplete: "off", value: onboard.apiKey, placeholder: "\u4E0D\u4F1A\u56DE\u5199\u5230\u4F1A\u8BDD", onChange: (event) => setOnboard((current) => ({ ...current, apiKey: event.target.value })) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap", marginTop: 2 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { style: button, onClick: () => void submitOnboard(), children: "\u4EA4\u7ED9 DSH \u5165\u9A7B" }),
        onboardMsg !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: hint, children: onboardMsg }) : null
      ] }),
      adapters.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gap: 6, marginTop: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: eyebrow, children: "REGISTERED ADAPTERS" }),
        adapters.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6, alignItems: "center", minWidth: 0, padding: "7px 9px", borderRadius: 8, background: "var(--dsw-alias-bg-layer-1)", border: "1px solid var(--dsw-alias-border-l2)", fontSize: 12 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: item3.model }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: "var(--dsw-alias-label-tertiary)" }, children: [
            item3.capability,
            " \xB7 ",
            item3.mode
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { marginLeft: "auto", color: "var(--dsw-alias-label-secondary)" }, children: item3.status })
        ] }, item3.id))
      ] }) : null
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: card, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: cardHeader, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { minWidth: 0 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: eyebrow, children: "CANVAS STORAGE" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { style: { display: "block", marginTop: 4, fontSize: 14 }, children: "\u753B\u5E03" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: cardSub, children: "DSH canvas \u6301\u4E45\u5316\u4E0E\u5907\u4EFD\u72B6\u6001" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { flexShrink: 0, color: "var(--dsw-alias-label-caption)", fontSize: 11 }, children: "canvas.json" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gap: 6, fontSize: 12, lineHeight: 1.5, color: "var(--dsw-alias-label-secondary)" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          "\u6587\u6863\u4F4D\u7F6E\uFF1A",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "var(--dsw-alias-label-tertiary)", overflowWrap: "anywhere" }, children: "directorx_output/canvas.json" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          "\u5F53\u524D\u72B6\u6001\uFF1A",
          canvasInfo === void 0 ? "\u8BFB\u53D6\u4E2D\u2026" : `${canvasInfo.nodes} \u4E2A\u8282\u70B9 \xB7 ${canvasInfo.edges} \u6761\u8FDE\u7EBF${canvasInfo.title !== void 0 && canvasInfo.title !== "" ? ` \xB7 \u6807\u9898\u300C${canvasInfo.title}\u300D` : ""}`
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", marginTop: 12, flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { style: { ...button, borderColor: "var(--dsw-alias-border-l2)" }, onClick: () => {
          void refreshCanvas();
        }, children: "\u5237\u65B0\u72B6\u6001" }),
        confirmReset ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { style: { ...button, ...danger }, onClick: () => {
            setConfirmReset(false);
            void resetCanvas();
          }, children: "\u786E\u8BA4\u91CD\u7F6E\uFF08\u5907\u4EFD\u540E\u6E05\u7A7A\uFF09" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { style: { ...button, borderColor: "var(--dsw-alias-border-l2)" }, onClick: () => setConfirmReset(false), children: "\u53D6\u6D88" })
        ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { style: { ...button, ...danger }, onClick: () => setConfirmReset(true), children: "\u91CD\u7F6E\u753B\u5E03\uFF08\u81EA\u52A8\u5907\u4EFD\uFF09" }),
        canvasAction !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: hint, children: canvasAction }) : null
      ] })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", justifyContent: "flex-end", gap: 10, flexWrap: "wrap" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { style: { ...button, minWidth: 144, background: "var(--dsw-alias-button-contrast-fill)", color: "var(--dsw-alias-label-primary-inverted)", borderColor: "var(--dsw-alias-button-contrast-fill)" }, disabled: saving || !writable, onClick: () => void save(), children: saving ? "\u4FDD\u5B58\u4E2D\u2026" : "\u4FDD\u5B58\u5168\u90E8\u914D\u7F6E" }) })
  ] });
}

// src/client/DirectorxToolRow.tsx
var import_react2 = require("react");

// src/client/editor.ts
var snapshot = { open: false, tab: "canvas", kind: null, path: null, look: null, workspace: null };
var listeners = /* @__PURE__ */ new Set();
var WORKSPACE_KEY = "directorx-editor-workspace";
function persistWorkspace(workspace) {
  try {
    if (typeof sessionStorage === "undefined") return;
    if (workspace === null) sessionStorage.removeItem(WORKSPACE_KEY);
    else sessionStorage.setItem(WORKSPACE_KEY, JSON.stringify(workspace));
  } catch {
  }
}
function editorSnapshot() {
  return snapshot;
}
function subscribeEditor(listener) {
  listeners.add(listener);
  return () => {
    listeners.delete(listener);
  };
}
function update(next) {
  snapshot = { ...snapshot, ...next };
  if ("workspace" in next) persistWorkspace(snapshot.workspace);
  for (const listener of [...listeners]) listener();
}
function openEditor(kind, path, extras) {
  update({ open: true, tab: kind, kind, path, look: extras?.look ?? null, workspace: null });
}
function openDirectorStage(nodeId) {
  if (nodeId === void 0 || nodeId === "") {
    update({ open: true, tab: "canvas" });
    return;
  }
  update({ open: true, tab: "canvas", kind: null, path: null, look: null, workspace: { kind: "director-stage", nodeId } });
}
function openEdit(nodeId) {
  if (nodeId === void 0 || nodeId === "") {
    update({ open: true, tab: "canvas" });
    return;
  }
  update({ open: true, tab: "canvas", kind: null, path: null, look: null, workspace: { kind: "edit", nodeId } });
}
function closeWorkspace() {
  update({ workspace: null, tab: "canvas" });
}
function ensureEditorOpen() {
  if (snapshot.open && snapshot.tab === "canvas") return;
  update({ open: true, tab: snapshot.tab === "image" || snapshot.tab === "video" ? snapshot.tab : "canvas" });
}
function openCanvas() {
  update({ open: true, tab: "canvas", workspace: null });
}
function setEditorTab(tab) {
  if (tab === "director-stage" || tab === "edit") {
    update({ open: true, tab: "canvas" });
    return;
  }
  update({ open: true, tab, workspace: tab === "canvas" ? null : snapshot.workspace });
}
function closeEditor() {
  update({ open: false });
}

// src/client/DirectorxToolRow.tsx
var import_jsx_runtime2 = require("react/jsx-runtime");
var TASK_STATE_LABEL = {
  submitted: "\u5DF2\u63D0\u4EA4\u6392\u961F",
  succeeded: "\u5DF2\u5B8C\u6210",
  failed: "\u5931\u8D25",
  cancelled: "\u5DF2\u53D6\u6D88"
};
var META = {
  directorx_view_image: { title: "\u770B\u56FE \xB7 Vision", kind: "image" },
  directorx_generate_image: { title: "\u751F\u6210\u56FE\u50CF \xB7 Image", kind: "image" },
  directorx_generate_video: { title: "\u751F\u6210\u89C6\u9891 \xB7 Video", kind: "video" },
  directorx_generate_audio: { title: "\u751F\u6210\u97F3\u9891 \xB7 Audio", kind: "audio" },
  directorx_transcribe_audio: { title: "\u97F3\u9891\u8F6C\u5199 \xB7 Transcribe", kind: "audio" },
  directorx_probe_media: { title: "\u5A92\u4F53\u63A2\u6D4B \xB7 Probe", kind: "video" },
  directorx_extract_frames: { title: "\u63D0\u53D6\u5E27 \xB7 Frames", kind: "image" },
  directorx_task_status: { title: "\u4EFB\u52A1\u72B6\u6001 \xB7 Tasks", kind: "video" },
  directorx_cancel_task: { title: "\u53D6\u6D88\u4EFB\u52A1 \xB7 Cancel", kind: "video" },
  directorx_edits: { title: "\u7F16\u8F91\u4EA7\u7269 \xB7 Edits", kind: "image" },
  directorx_edit_plan: { title: "\u7F16\u8F91\u8DEF\u7531 \xB7 Plan", kind: "image" },
  directorx_image_edit: { title: "\u56FE\u7247\u7F16\u8F91 \xB7 Image edit", kind: "image" },
  directorx_edit: { title: "\u526A\u8F91 \xB7 Edit", kind: "video" },
  directorx_video_process: { title: "\u89C6\u9891\u5904\u7406 \xB7 Process", kind: "video" },
  directorx_canvas_intents: { title: "\u753B\u5E03\u6307\u4EE4 \xB7 Intent", kind: "image" },
  directorx_canvas_intent_ack: { title: "\u753B\u5E03\u56DE\u6267 \xB7 Ack", kind: "image" },
  directorx_canvas_continue: { title: "\u753B\u5E03\u7EED\u5199 \xB7 Continue", kind: "image" },
  directorx_canvas_add: { title: "\u753B\u5E03\u52A0\u70B9 \xB7 Add", kind: "image" },
  directorx_canvas_connect: { title: "\u753B\u5E03\u8FDE\u7EBF \xB7 Connect", kind: "image" },
  directorx_canvas_get: { title: "\u753B\u5E03\u8BFB\u53D6 \xB7 Get", kind: "image" },
  directorx_canvas_shotlist: { title: "\u955C\u5934\u8868 \xB7 Shotlist", kind: "image" },
  directorx_canvas_update: { title: "\u753B\u5E03\u66F4\u65B0 \xB7 Update", kind: "image" },
  directorx_canvas_batch: { title: "\u753B\u5E03\u6279\u91CF \xB7 Batch", kind: "image" },
  directorx_canvas_pack: { title: "\u5408\u6210\u89C6\u9891 \xB7 Pack", kind: "video" },
  directorx_canvas_sheet: { title: "\u4E5D\u5BAB\u683C \xB7 Sheet", kind: "image" },
  directorx_canvas_split: { title: "\u62C6\u5206\u5BAB\u683C \xB7 Split", kind: "image" },
  directorx_canvas_join: { title: "\u5408\u5E76\u5BAB\u683C \xB7 Join", kind: "image" },
  directorx_canvas_stack: { title: "\u5206\u5C4F \xB7 Stack", kind: "video" },
  directorx_canvas_desub: { title: "\u53BB\u5B57\u5E55 \xB7 Desub", kind: "video" },
  directorx_canvas_extend: { title: "\u89C6\u9891\u5EF6\u957F \xB7 Extend", kind: "video" },
  directorx_canvas_gif: { title: "\u5BFC\u51FA GIF \xB7 GIF", kind: "image" },
  directorx_stage_snapshot: { title: "\u5BFC\u6F14\u53F0\u622A\u56FE \xB7 Snapshot", kind: "image" },
  directorx_stage: { title: "\u5236\u7247\u9636\u6BB5 \xB7 Stage", kind: "image" },
  directorx_blocking: { title: "\u573A\u9762\u63A7\u5236\u8868 \xB7 Blocking", kind: "image" },
  directorx_studio: { title: "\u7F16\u8F91\u53F0 \xB7 Studio", kind: "image" },
  directorx_skill_search: { title: "\u68C0\u7D22\u6280\u80FD \xB7 Skills", kind: "image" },
  directorx_skill_route: { title: "\u6280\u80FD\u8DEF\u7531 \xB7 Route", kind: "image" },
  directorx_skill_read: { title: "\u8BFB\u6280\u80FD \xB7 Skill", kind: "image" },
  directorx_prompt_plan: { title: "\u63D0\u793A\u8BCD\u7F16\u6392 \xB7 Plan", kind: "image" },
  directorx_ip_scan: { title: "\u7248\u6743\u626B\u63CF \xB7 IP", kind: "image" },
  directorx_ip_rewrite: { title: "\u7248\u6743\u6539\u5199 \xB7 IP", kind: "image" }
};
var card2 = {
  minWidth: 0,
  boxSizing: "border-box",
  border: "1px solid var(--dsw-alias-border-l2)",
  borderRadius: 12,
  padding: "12px 14px",
  marginBottom: 10,
  background: "var(--dsw-alias-bg-layer-2)",
  color: "var(--dsw-alias-label-primary)",
  overflow: "hidden"
};
var head = { display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 12, minWidth: 0 };
var statusBase = { flexShrink: 0, fontSize: 11, lineHeight: 1.3, whiteSpace: "nowrap", padding: "4px 8px", borderRadius: 999, border: "1px solid var(--dsw-alias-border-l2)", color: "var(--dsw-alias-label-secondary)", background: "var(--dsw-alias-bg-layer-1)" };
var statusRunning = { ...statusBase, color: "var(--dsw-alias-state-warn-primary)", borderColor: "var(--dsw-alias-state-warn-primary)", background: "var(--dsw-alias-interactive-bg-hover)" };
var statusFailed = { ...statusBase, color: "var(--dsw-alias-state-error-primary)", borderColor: "var(--dsw-alias-state-error-primary)", background: "var(--dsw-alias-interactive-bg-hover-danger)" };
var statusDone = { ...statusBase, color: "var(--dsw-alias-state-success-primary)", borderColor: "var(--dsw-alias-state-success-primary)", background: "var(--dsw-alias-interactive-bg-hover)" };
var summary = { minWidth: 0, fontSize: 12.5, color: "var(--dsw-alias-label-secondary)", marginTop: 8, lineHeight: 1.55, overflowWrap: "anywhere" };
var metaLine = { minWidth: 0, fontSize: 11, color: "var(--dsw-alias-label-tertiary)", marginTop: 10, overflowWrap: "anywhere" };
var mediaBox = { minWidth: 0, marginTop: 12 };
var errorText = { minWidth: 0, fontSize: 12.5, color: "var(--dsw-alias-state-error-primary)", background: "var(--dsw-alias-interactive-bg-hover-danger)", borderRadius: 8, padding: "8px 10px", marginTop: 10, overflowWrap: "anywhere" };
var IMG = { display: "block", maxWidth: "100%", maxHeight: 280, borderRadius: 8, border: "1px solid var(--dsw-alias-border-l2)" };
var VIDEO = { display: "block", width: "100%", maxWidth: 480, maxHeight: 300, borderRadius: 8, background: "var(--dsw-alias-bg-base)" };
var AUDIO = { display: "block", width: "100%", maxWidth: 480, marginTop: 4 };
var LINK = { minWidth: 0, fontSize: 12, color: "var(--dsw-alias-label-secondary)", overflowWrap: "anywhere" };
function parseArgs(raw) {
  if (raw === void 0 || raw === "") return {};
  try {
    const parsed = JSON.parse(raw);
    return typeof parsed === "object" && parsed !== null ? parsed : {};
  } catch {
    return {};
  }
}
function argsOf(block) {
  return parseArgs(block.argsRaw ?? block.call?.argsRaw);
}
function textContentOf(block) {
  const parts = [];
  for (const item3 of block.content ?? []) {
    if (item3.type === "text" && typeof item3.text === "string") parts.push(item3.text);
  }
  return parts.join("\n");
}
function resultOf(block) {
  const text = textContentOf(block);
  if (text === "") return null;
  try {
    const parsed = JSON.parse(text);
    return typeof parsed === "object" && parsed !== null ? parsed : null;
  } catch {
    return null;
  }
}
function mediaSrc(file) {
  if (typeof file.url === "string" && (/^https?:\/\//i.test(file.url) || /^data:/i.test(file.url))) return file.url;
  if (typeof file.path === "string" && file.path !== "") {
    return withProject(`/directorx/media?path=${encodeURIComponent(file.path)}`);
  }
  return void 0;
}
function kindOf(file, fallback) {
  const mime = file.mimeType ?? "";
  if (mime.startsWith("video/")) return "video";
  if (mime.startsWith("audio/")) return "audio";
  if (mime.startsWith("image/")) return "image";
  return fallback;
}
function MediaPreview({ file, fallback }) {
  const src = mediaSrc(file);
  const label2 = file.path ?? file.url ?? "";
  if (src === void 0) {
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: LINK, children: [
      "\u8F93\u51FA\u6587\u4EF6\uFF1A",
      label2
    ] });
  }
  const kind = kindOf(file, fallback);
  if (kind === "image") {
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("a", { href: src, target: "_blank", rel: "noreferrer", style: { display: "inline-block" }, children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("img", { src, alt: label2, style: IMG }) });
  }
  if (kind === "video") {
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("video", { src, controls: true, preload: "metadata", style: VIDEO }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: LINK, children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("a", { href: src, target: "_blank", rel: "noreferrer", children: "\u5728\u6D4F\u89C8\u5668\u4E2D\u6253\u5F00" }) })
    ] });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("audio", { src, controls: true, preload: "metadata", style: AUDIO }) });
}
var kvRow = { display: "grid", gridTemplateColumns: "minmax(70px, 92px) minmax(0, 1fr)", gap: "2px 10px", fontSize: 12, marginTop: 7, alignItems: "baseline", minWidth: 0 };
var kvLabel = { color: "var(--dsw-alias-label-tertiary)" };
var thumbGrid = { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(104px, 1fr))", gap: 8, marginTop: 10, minWidth: 0 };
var thumb = { width: "100%", height: 74, objectFit: "cover", borderRadius: 7, border: "1px solid var(--dsw-alias-border-l2)", display: "block" };
var itemRow = { minWidth: 0, fontSize: 12, marginBottom: 7, lineHeight: 1.45, overflowWrap: "anywhere", color: "var(--dsw-alias-label-secondary)" };
function fmtBytesLocal(bytes) {
  if (bytes === void 0) return "\u2014";
  return bytes > 1024 * 1024 ? `${(bytes / 1024 / 1024).toFixed(1)} MB` : `${Math.round(bytes / 1024)} KB`;
}
function FramesGrid({ files }) {
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: thumbGrid, children: files.slice(0, 12).map((file, index2) => {
    const src = mediaSrc(file);
    return src !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("a", { href: src, target: "_blank", rel: "noreferrer", children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("img", { src, alt: file.path ?? `frame-${index2}`, style: thumb }) }, index2) : /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: LINK, children: file.path }, index2);
  }) });
}
function TranscribeBody({ result }) {
  const srtFiles = (result.files ?? []).filter((file) => file.mimeType === "application/x-subrip");
  const otherFiles = (result.files ?? []).filter((file) => file.mimeType !== "application/x-subrip");
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    typeof result.text === "string" && result.text !== "" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: summary, children: [
      result.text.slice(0, 900),
      result.text.length > 900 ? "\u2026" : ""
    ] }) : null,
    result.srt !== void 0 && result.srt !== "" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: summary, children: [
      String(result.srt).slice(0, 400),
      String(result.srt).length > 400 ? "\u2026" : ""
    ] }) : null,
    srtFiles.map((file, index2) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: LINK, children: [
      "\u5B57\u5E55\u6587\u4EF6\uFF1A",
      file.path
    ] }, index2)),
    otherFiles.map((file, index2) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: LINK, children: [
      "\u6587\u672C\u6587\u4EF6\uFF1A",
      file.path
    ] }, index2))
  ] });
}
function ProbeBody({ result }) {
  const streams = Array.isArray(result.streams) ? result.streams : [];
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { children: [
    /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: kvRow, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { style: kvLabel, children: "\u5BB9\u5668" }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { children: result.format ?? "\u2014" })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: kvRow, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { style: kvLabel, children: "\u65F6\u957F" }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { children: result.durationSec !== void 0 ? `${result.durationSec.toFixed(2)}s` : "\u2014" })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: kvRow, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { style: kvLabel, children: "\u5927\u5C0F" }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { children: fmtBytesLocal(result.sizeBytes) })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: kvRow, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { style: kvLabel, children: "\u6D41" }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { children: streams.map((stream, index2) => {
        const parts = [String(stream.type ?? ""), String(stream.codec ?? "")].filter((part) => part !== "");
        if (stream.width !== void 0 && stream.height !== void 0) parts.push(`${stream.width}\xD7${stream.height}`);
        if (stream.fps !== void 0) parts.push(`${stream.fps} fps`);
        return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { children: parts.join(" \xB7 ") }, index2);
      }) })
    ] })
  ] });
}
function TaskRows({ result }) {
  const tasks = Array.isArray(result.tasks) ? result.tasks : result.task !== void 0 ? [result.task] : [];
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: { marginTop: 6 }, children: [
    tasks.map((task, index2) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: { ...itemRow, display: "flex", alignItems: "baseline", gap: 6, flexWrap: "wrap" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { style: { fontWeight: 600, color: task.state === "failed" ? "var(--dsw-alias-state-error-primary)" : task.state === "succeeded" ? "var(--dsw-alias-state-success-primary)" : "var(--dsw-alias-label-secondary)" }, children: TASK_STATE_LABEL[task.state ?? ""] ?? task.state ?? "\u2014" }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { children: task.taskId ?? result.task_id ?? "" }),
      task.model !== void 0 && task.model !== "" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { style: { color: "var(--dsw-alias-label-tertiary)" }, children: task.model }) : null
    ] }, index2)),
    tasks.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: itemRow, children: "\u65E0\u4EFB\u52A1\u8BB0\u5F55" }) : null
  ] });
}
function EditsBody({ result }) {
  const edits = Array.isArray(result.edits) ? result.edits : [];
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: { marginTop: 6 }, children: [
    edits.map((edit, index2) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: itemRow, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { style: { fontWeight: 600 }, children: edit.name ?? edit.path?.split("/").pop() }),
      " \xB7 ",
      fmtBytesLocal(edit.bytes),
      typeof edit.path === "string" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: { opacity: 0.55 }, children: edit.path }) : null
    ] }, index2)),
    edits.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: itemRow, children: "\u8FD8\u6CA1\u6709\u7F16\u8F91\u4EA7\u7269\u3002\u5728\u751F\u6210\u5361\u7247\u4E0A\u70B9\u300C\u7F16\u8F91\u300D\uFF0C\u4FDD\u5B58\u540E\u4F1A\u51FA\u73B0\u5728\u8FD9\u91CC\u3002" }) : null
  ] });
}
function DirectorxToolRow(props) {
  const meta = META[props.toolName];
  if (meta === void 0) return null;
  const block = props.block;
  const settled = block.kind === "tool-result";
  const args = argsOf(block);
  const result = (0, import_react2.useMemo)(() => settled ? resultOf(block) : null, [block]);
  const isSpecial = props.toolName === "directorx_transcribe_audio" || props.toolName === "directorx_probe_media" || props.toolName === "directorx_extract_frames" || props.toolName === "directorx_task_status" || props.toolName === "directorx_cancel_task" || props.toolName === "directorx_edits";
  const prompt = typeof args.question === "string" && args.question !== "" ? args.question : typeof args.prompt === "string" ? args.prompt : typeof args.source === "string" ? args.source : typeof args.task_id === "string" ? args.task_id : typeof args.text === "string" ? args.text : "";
  const failed = settled && (block.isError === true || result === null && textContentOf(block) !== "");
  const [taskState, setTaskState] = (0, import_react2.useState)(void 0);
  (0, import_react2.useEffect)(() => {
    if (props.toolName !== "directorx_generate_video" || settled || prompt === "") return;
    let live = true;
    let timer2;
    const poll = async () => {
      try {
        const response = await fetch(withProject("/directorx/media/tasks"));
        if (!response.ok) return;
        const data = await response.json();
        const match = data.tasks.find((task) => typeof task.prompt === "string" && (task.prompt === prompt || prompt.includes(task.prompt)));
        if (live && match !== void 0) setTaskState(match.state);
      } catch {
      }
    };
    void poll();
    timer2 = window.setInterval(() => {
      void poll();
    }, 3e3);
    return () => {
      live = false;
      if (timer2 !== void 0) window.clearInterval(timer2);
    };
  }, [props.toolName, settled, prompt]);
  (0, import_react2.useEffect)(() => {
    if (!settled || result === null || result.openStudio !== true || typeof result.path !== "string" || result.path === "") return;
    openEditor(result.kind === "video" ? "video" : "image", result.path, result.look !== void 0 ? { look: result.look } : void 0);
  }, [settled, result]);
  const files = result?.files?.slice(0, 4) ?? (typeof result?.path === "string" && result.path !== "" ? [{ path: result.path }] : []);
  const details = [
    result?.model !== void 0 && result.model !== "" ? result.model : "",
    result?.mode !== void 0 && result.mode !== "" ? result.mode : "",
    files.length > 0 ? `${files.length} \u4E2A\u6587\u4EF6` : ""
  ].filter((part) => part !== "").join(" \xB7 ");
  const editableKind = props.toolName === "directorx_generate_image" || props.toolName === "directorx_image_edit" || props.toolName === "directorx_studio" && result?.kind !== "video" ? "image" : props.toolName === "directorx_generate_video" || props.toolName === "directorx_edit" || props.toolName === "directorx_video_process" || props.toolName === "directorx_studio" && result?.kind === "video" ? "video" : null;
  const editablePath = editableKind !== null && typeof files[0]?.path === "string" && files[0].path !== "" ? files[0].path : null;
  let statusLabel = "\u8FDB\u884C\u4E2D\u2026";
  if (failed) statusLabel = "\u5931\u8D25";
  else if (settled) statusLabel = result?.status ?? "\u5B8C\u6210";
  else if (taskState !== void 0) statusLabel = `\u751F\u6210\u4E2D\u2026\uFF08${TASK_STATE_LABEL[taskState] ?? taskState}\uFF09`;
  const visionSource = props.toolName === "directorx_view_image" && result?.source !== void 0 ? result.source : void 0;
  const visionPreview = visionSource !== void 0 && visionSource !== "" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(MediaPreview, { file: { path: visionSource }, fallback: "image" }) : null;
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: card2, children: [
    /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: head, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: { minWidth: 0 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: { fontSize: 10, lineHeight: 1.3, letterSpacing: 0.65, color: "var(--dsw-alias-label-caption)", textTransform: "uppercase", overflowWrap: "anywhere" }, children: props.toolName.replace(/^directorx_/, "").replaceAll("_", " ") }),
        /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("strong", { style: { display: "block", minWidth: 0, marginTop: 3, fontSize: 13, lineHeight: 1.35, overflowWrap: "anywhere" }, children: meta.title })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { role: "status", style: failed ? statusFailed : settled ? statusDone : statusRunning, children: statusLabel })
    ] }),
    prompt !== "" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: summary, children: prompt.length > 220 ? `${prompt.slice(0, 220)}\u2026` : prompt }) : null,
    !failed ? /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
      visionPreview,
      result?.answer !== void 0 && result.answer !== "" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: summary, children: result.answer.slice(0, 1200) }) : null,
      isSpecial && result !== null ? /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: mediaBox, children: [
        props.toolName === "directorx_transcribe_audio" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(TranscribeBody, { result }) : null,
        props.toolName === "directorx_probe_media" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(ProbeBody, { result }) : null,
        props.toolName === "directorx_extract_frames" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(FramesGrid, { files: result.files ?? [] }) : null,
        props.toolName === "directorx_task_status" || props.toolName === "directorx_cancel_task" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(TaskRows, { result }) : null,
        props.toolName === "directorx_edits" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(EditsBody, { result }) : null
      ] }) : /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: mediaBox, children: [
        files.map((file, index2) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(MediaPreview, { file, fallback: meta.kind }, index2)),
        files.length === 0 && result !== null && props.toolName.startsWith("directorx_canvas_") ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: summary, children: [
          typeof result.canvasTitle === "string" ? result.canvasTitle : "",
          result.nodeCount !== void 0 ? `${String(result.nodeCount)} \u8282\u70B9` : "",
          typeof result.nodeId === "string" ? `node ${result.nodeId}` : "",
          Array.isArray(result.summary) ? result.summary.slice(0, 4).join(" \xB7 ") : ""
        ].filter((part) => part !== "").join(" \xB7 ") || textContentOf(block).slice(0, 240) }) : null
      ] }),
      editableKind !== null && editablePath !== null ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { className: "dx-hit", style: { marginTop: 10, minHeight: 30, padding: "0 10px", borderRadius: 8, border: "1px solid var(--dsw-alias-brand-primary)", background: "var(--dsw-alias-bg-layer-1)", color: "var(--dsw-alias-label-primary)", fontSize: 12, cursor: "pointer" }, onClick: () => openEditor(editableKind, editablePath), children: "\u5728\u753B\u5E03\u4E2D\u7F16\u8F91" }) : null
    ] }) : /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { style: errorText, role: "alert", children: [
      block.error?.code !== void 0 && block.error.code !== "" ? `${block.error.code}: ` : "",
      textContentOf(block).slice(0, 400) || "\u751F\u6210\u5931\u8D25"
    ] }),
    details !== "" ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { style: metaLine, children: details }) : null
  ] });
}
var DIRECTORX_TOOLVIEW_KEYS = [
  "directorx_view_image",
  "directorx_generate_image",
  "directorx_generate_video",
  "directorx_generate_audio",
  "directorx_transcribe_audio",
  "directorx_probe_media",
  "directorx_extract_frames",
  "directorx_task_status",
  "directorx_cancel_task",
  "directorx_edits",
  "directorx_edit_plan",
  "directorx_image_edit",
  "directorx_edit",
  "directorx_video_process",
  "directorx_canvas_intents",
  "directorx_canvas_intent_ack",
  "directorx_canvas_continue",
  "directorx_canvas_add",
  "directorx_canvas_connect",
  "directorx_canvas_get",
  "directorx_canvas_shotlist",
  "directorx_canvas_update",
  "directorx_canvas_batch",
  "directorx_studio",
  "directorx_skill_search",
  "directorx_skill_route",
  "directorx_skill_read"
];

// src/client/EditorDock.tsx
var import_react21 = require("react");

// src/client/stage/Stage.tsx
var import_react19 = require("react");

// node_modules/@xyflow/react/dist/esm/index.js
var import_jsx_runtime3 = require("react/jsx-runtime");
var import_react4 = require("react");

// node_modules/classcat/index.js
function cc(names) {
  if (typeof names === "string" || typeof names === "number") return "" + names;
  let out = "";
  if (Array.isArray(names)) {
    for (let i2 = 0, tmp; i2 < names.length; i2++) {
      if ((tmp = cc(names[i2])) !== "") {
        out += (out && " ") + tmp;
      }
    }
  } else {
    for (let k2 in names) {
      if (names[k2]) out += (out && " ") + k2;
    }
  }
  return out;
}

// node_modules/d3-dispatch/src/dispatch.js
var noop = { value: () => {
} };
function dispatch() {
  for (var i2 = 0, n2 = arguments.length, _3 = {}, t; i2 < n2; ++i2) {
    if (!(t = arguments[i2] + "") || t in _3 || /[\s.]/.test(t)) throw new Error("illegal type: " + t);
    _3[t] = [];
  }
  return new Dispatch(_3);
}
function Dispatch(_3) {
  this._ = _3;
}
function parseTypenames(typenames, types) {
  return typenames.trim().split(/^|\s+/).map(function(t) {
    var name2 = "", i2 = t.indexOf(".");
    if (i2 >= 0) name2 = t.slice(i2 + 1), t = t.slice(0, i2);
    if (t && !types.hasOwnProperty(t)) throw new Error("unknown type: " + t);
    return { type: t, name: name2 };
  });
}
Dispatch.prototype = dispatch.prototype = {
  constructor: Dispatch,
  on: function(typename, callback) {
    var _3 = this._, T3 = parseTypenames(typename + "", _3), t, i2 = -1, n2 = T3.length;
    if (arguments.length < 2) {
      while (++i2 < n2) if ((t = (typename = T3[i2]).type) && (t = get(_3[t], typename.name))) return t;
      return;
    }
    if (callback != null && typeof callback !== "function") throw new Error("invalid callback: " + callback);
    while (++i2 < n2) {
      if (t = (typename = T3[i2]).type) _3[t] = set(_3[t], typename.name, callback);
      else if (callback == null) for (t in _3) _3[t] = set(_3[t], typename.name, null);
    }
    return this;
  },
  copy: function() {
    var copy = {}, _3 = this._;
    for (var t in _3) copy[t] = _3[t].slice();
    return new Dispatch(copy);
  },
  call: function(type, that) {
    if ((n2 = arguments.length - 2) > 0) for (var args = new Array(n2), i2 = 0, n2, t; i2 < n2; ++i2) args[i2] = arguments[i2 + 2];
    if (!this._.hasOwnProperty(type)) throw new Error("unknown type: " + type);
    for (t = this._[type], i2 = 0, n2 = t.length; i2 < n2; ++i2) t[i2].value.apply(that, args);
  },
  apply: function(type, that, args) {
    if (!this._.hasOwnProperty(type)) throw new Error("unknown type: " + type);
    for (var t = this._[type], i2 = 0, n2 = t.length; i2 < n2; ++i2) t[i2].value.apply(that, args);
  }
};
function get(type, name2) {
  for (var i2 = 0, n2 = type.length, c; i2 < n2; ++i2) {
    if ((c = type[i2]).name === name2) {
      return c.value;
    }
  }
}
function set(type, name2, callback) {
  for (var i2 = 0, n2 = type.length; i2 < n2; ++i2) {
    if (type[i2].name === name2) {
      type[i2] = noop, type = type.slice(0, i2).concat(type.slice(i2 + 1));
      break;
    }
  }
  if (callback != null) type.push({ name: name2, value: callback });
  return type;
}
var dispatch_default = dispatch;

// node_modules/d3-selection/src/namespaces.js
var xhtml = "http://www.w3.org/1999/xhtml";
var namespaces_default = {
  svg: "http://www.w3.org/2000/svg",
  xhtml,
  xlink: "http://www.w3.org/1999/xlink",
  xml: "http://www.w3.org/XML/1998/namespace",
  xmlns: "http://www.w3.org/2000/xmlns/"
};

// node_modules/d3-selection/src/namespace.js
function namespace_default(name2) {
  var prefix = name2 += "", i2 = prefix.indexOf(":");
  if (i2 >= 0 && (prefix = name2.slice(0, i2)) !== "xmlns") name2 = name2.slice(i2 + 1);
  return namespaces_default.hasOwnProperty(prefix) ? { space: namespaces_default[prefix], local: name2 } : name2;
}

// node_modules/d3-selection/src/creator.js
function creatorInherit(name2) {
  return function() {
    var document2 = this.ownerDocument, uri = this.namespaceURI;
    return uri === xhtml && document2.documentElement.namespaceURI === xhtml ? document2.createElement(name2) : document2.createElementNS(uri, name2);
  };
}
function creatorFixed(fullname) {
  return function() {
    return this.ownerDocument.createElementNS(fullname.space, fullname.local);
  };
}
function creator_default(name2) {
  var fullname = namespace_default(name2);
  return (fullname.local ? creatorFixed : creatorInherit)(fullname);
}

// node_modules/d3-selection/src/selector.js
function none() {
}
function selector_default(selector) {
  return selector == null ? none : function() {
    return this.querySelector(selector);
  };
}

// node_modules/d3-selection/src/selection/select.js
function select_default(select) {
  if (typeof select !== "function") select = selector_default(select);
  for (var groups = this._groups, m2 = groups.length, subgroups = new Array(m2), j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, subgroup = subgroups[j3] = new Array(n2), node, subnode, i2 = 0; i2 < n2; ++i2) {
      if ((node = group[i2]) && (subnode = select.call(node, node.__data__, i2, group))) {
        if ("__data__" in node) subnode.__data__ = node.__data__;
        subgroup[i2] = subnode;
      }
    }
  }
  return new Selection(subgroups, this._parents);
}

// node_modules/d3-selection/src/array.js
function array(x3) {
  return x3 == null ? [] : Array.isArray(x3) ? x3 : Array.from(x3);
}

// node_modules/d3-selection/src/selectorAll.js
function empty() {
  return [];
}
function selectorAll_default(selector) {
  return selector == null ? empty : function() {
    return this.querySelectorAll(selector);
  };
}

// node_modules/d3-selection/src/selection/selectAll.js
function arrayAll(select) {
  return function() {
    return array(select.apply(this, arguments));
  };
}
function selectAll_default(select) {
  if (typeof select === "function") select = arrayAll(select);
  else select = selectorAll_default(select);
  for (var groups = this._groups, m2 = groups.length, subgroups = [], parents = [], j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, node, i2 = 0; i2 < n2; ++i2) {
      if (node = group[i2]) {
        subgroups.push(select.call(node, node.__data__, i2, group));
        parents.push(node);
      }
    }
  }
  return new Selection(subgroups, parents);
}

// node_modules/d3-selection/src/matcher.js
function matcher_default(selector) {
  return function() {
    return this.matches(selector);
  };
}
function childMatcher(selector) {
  return function(node) {
    return node.matches(selector);
  };
}

// node_modules/d3-selection/src/selection/selectChild.js
var find = Array.prototype.find;
function childFind(match) {
  return function() {
    return find.call(this.children, match);
  };
}
function childFirst() {
  return this.firstElementChild;
}
function selectChild_default(match) {
  return this.select(match == null ? childFirst : childFind(typeof match === "function" ? match : childMatcher(match)));
}

// node_modules/d3-selection/src/selection/selectChildren.js
var filter = Array.prototype.filter;
function children() {
  return Array.from(this.children);
}
function childrenFilter(match) {
  return function() {
    return filter.call(this.children, match);
  };
}
function selectChildren_default(match) {
  return this.selectAll(match == null ? children : childrenFilter(typeof match === "function" ? match : childMatcher(match)));
}

// node_modules/d3-selection/src/selection/filter.js
function filter_default(match) {
  if (typeof match !== "function") match = matcher_default(match);
  for (var groups = this._groups, m2 = groups.length, subgroups = new Array(m2), j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, subgroup = subgroups[j3] = [], node, i2 = 0; i2 < n2; ++i2) {
      if ((node = group[i2]) && match.call(node, node.__data__, i2, group)) {
        subgroup.push(node);
      }
    }
  }
  return new Selection(subgroups, this._parents);
}

// node_modules/d3-selection/src/selection/sparse.js
function sparse_default(update2) {
  return new Array(update2.length);
}

// node_modules/d3-selection/src/selection/enter.js
function enter_default() {
  return new Selection(this._enter || this._groups.map(sparse_default), this._parents);
}
function EnterNode(parent, datum2) {
  this.ownerDocument = parent.ownerDocument;
  this.namespaceURI = parent.namespaceURI;
  this._next = null;
  this._parent = parent;
  this.__data__ = datum2;
}
EnterNode.prototype = {
  constructor: EnterNode,
  appendChild: function(child) {
    return this._parent.insertBefore(child, this._next);
  },
  insertBefore: function(child, next) {
    return this._parent.insertBefore(child, next);
  },
  querySelector: function(selector) {
    return this._parent.querySelector(selector);
  },
  querySelectorAll: function(selector) {
    return this._parent.querySelectorAll(selector);
  }
};

// node_modules/d3-selection/src/constant.js
function constant_default(x3) {
  return function() {
    return x3;
  };
}

// node_modules/d3-selection/src/selection/data.js
function bindIndex(parent, group, enter, update2, exit, data) {
  var i2 = 0, node, groupLength = group.length, dataLength = data.length;
  for (; i2 < dataLength; ++i2) {
    if (node = group[i2]) {
      node.__data__ = data[i2];
      update2[i2] = node;
    } else {
      enter[i2] = new EnterNode(parent, data[i2]);
    }
  }
  for (; i2 < groupLength; ++i2) {
    if (node = group[i2]) {
      exit[i2] = node;
    }
  }
}
function bindKey(parent, group, enter, update2, exit, data, key) {
  var i2, node, nodeByKeyValue = /* @__PURE__ */ new Map(), groupLength = group.length, dataLength = data.length, keyValues = new Array(groupLength), keyValue;
  for (i2 = 0; i2 < groupLength; ++i2) {
    if (node = group[i2]) {
      keyValues[i2] = keyValue = key.call(node, node.__data__, i2, group) + "";
      if (nodeByKeyValue.has(keyValue)) {
        exit[i2] = node;
      } else {
        nodeByKeyValue.set(keyValue, node);
      }
    }
  }
  for (i2 = 0; i2 < dataLength; ++i2) {
    keyValue = key.call(parent, data[i2], i2, data) + "";
    if (node = nodeByKeyValue.get(keyValue)) {
      update2[i2] = node;
      node.__data__ = data[i2];
      nodeByKeyValue.delete(keyValue);
    } else {
      enter[i2] = new EnterNode(parent, data[i2]);
    }
  }
  for (i2 = 0; i2 < groupLength; ++i2) {
    if ((node = group[i2]) && nodeByKeyValue.get(keyValues[i2]) === node) {
      exit[i2] = node;
    }
  }
}
function datum(node) {
  return node.__data__;
}
function data_default(value, key) {
  if (!arguments.length) return Array.from(this, datum);
  var bind = key ? bindKey : bindIndex, parents = this._parents, groups = this._groups;
  if (typeof value !== "function") value = constant_default(value);
  for (var m2 = groups.length, update2 = new Array(m2), enter = new Array(m2), exit = new Array(m2), j3 = 0; j3 < m2; ++j3) {
    var parent = parents[j3], group = groups[j3], groupLength = group.length, data = arraylike(value.call(parent, parent && parent.__data__, j3, parents)), dataLength = data.length, enterGroup = enter[j3] = new Array(dataLength), updateGroup = update2[j3] = new Array(dataLength), exitGroup = exit[j3] = new Array(groupLength);
    bind(parent, group, enterGroup, updateGroup, exitGroup, data, key);
    for (var i0 = 0, i1 = 0, previous, next; i0 < dataLength; ++i0) {
      if (previous = enterGroup[i0]) {
        if (i0 >= i1) i1 = i0 + 1;
        while (!(next = updateGroup[i1]) && ++i1 < dataLength) ;
        previous._next = next || null;
      }
    }
  }
  update2 = new Selection(update2, parents);
  update2._enter = enter;
  update2._exit = exit;
  return update2;
}
function arraylike(data) {
  return typeof data === "object" && "length" in data ? data : Array.from(data);
}

// node_modules/d3-selection/src/selection/exit.js
function exit_default() {
  return new Selection(this._exit || this._groups.map(sparse_default), this._parents);
}

// node_modules/d3-selection/src/selection/join.js
function join_default(onenter, onupdate, onexit) {
  var enter = this.enter(), update2 = this, exit = this.exit();
  if (typeof onenter === "function") {
    enter = onenter(enter);
    if (enter) enter = enter.selection();
  } else {
    enter = enter.append(onenter + "");
  }
  if (onupdate != null) {
    update2 = onupdate(update2);
    if (update2) update2 = update2.selection();
  }
  if (onexit == null) exit.remove();
  else onexit(exit);
  return enter && update2 ? enter.merge(update2).order() : update2;
}

// node_modules/d3-selection/src/selection/merge.js
function merge_default(context) {
  var selection2 = context.selection ? context.selection() : context;
  for (var groups0 = this._groups, groups1 = selection2._groups, m0 = groups0.length, m1 = groups1.length, m2 = Math.min(m0, m1), merges = new Array(m0), j3 = 0; j3 < m2; ++j3) {
    for (var group0 = groups0[j3], group1 = groups1[j3], n2 = group0.length, merge = merges[j3] = new Array(n2), node, i2 = 0; i2 < n2; ++i2) {
      if (node = group0[i2] || group1[i2]) {
        merge[i2] = node;
      }
    }
  }
  for (; j3 < m0; ++j3) {
    merges[j3] = groups0[j3];
  }
  return new Selection(merges, this._parents);
}

// node_modules/d3-selection/src/selection/order.js
function order_default() {
  for (var groups = this._groups, j3 = -1, m2 = groups.length; ++j3 < m2; ) {
    for (var group = groups[j3], i2 = group.length - 1, next = group[i2], node; --i2 >= 0; ) {
      if (node = group[i2]) {
        if (next && node.compareDocumentPosition(next) ^ 4) next.parentNode.insertBefore(node, next);
        next = node;
      }
    }
  }
  return this;
}

// node_modules/d3-selection/src/selection/sort.js
function sort_default(compare) {
  if (!compare) compare = ascending;
  function compareNode(a, b) {
    return a && b ? compare(a.__data__, b.__data__) : !a - !b;
  }
  for (var groups = this._groups, m2 = groups.length, sortgroups = new Array(m2), j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, sortgroup = sortgroups[j3] = new Array(n2), node, i2 = 0; i2 < n2; ++i2) {
      if (node = group[i2]) {
        sortgroup[i2] = node;
      }
    }
    sortgroup.sort(compareNode);
  }
  return new Selection(sortgroups, this._parents).order();
}
function ascending(a, b) {
  return a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
}

// node_modules/d3-selection/src/selection/call.js
function call_default() {
  var callback = arguments[0];
  arguments[0] = this;
  callback.apply(null, arguments);
  return this;
}

// node_modules/d3-selection/src/selection/nodes.js
function nodes_default() {
  return Array.from(this);
}

// node_modules/d3-selection/src/selection/node.js
function node_default() {
  for (var groups = this._groups, j3 = 0, m2 = groups.length; j3 < m2; ++j3) {
    for (var group = groups[j3], i2 = 0, n2 = group.length; i2 < n2; ++i2) {
      var node = group[i2];
      if (node) return node;
    }
  }
  return null;
}

// node_modules/d3-selection/src/selection/size.js
function size_default() {
  let size = 0;
  for (const node of this) ++size;
  return size;
}

// node_modules/d3-selection/src/selection/empty.js
function empty_default() {
  return !this.node();
}

// node_modules/d3-selection/src/selection/each.js
function each_default(callback) {
  for (var groups = this._groups, j3 = 0, m2 = groups.length; j3 < m2; ++j3) {
    for (var group = groups[j3], i2 = 0, n2 = group.length, node; i2 < n2; ++i2) {
      if (node = group[i2]) callback.call(node, node.__data__, i2, group);
    }
  }
  return this;
}

// node_modules/d3-selection/src/selection/attr.js
function attrRemove(name2) {
  return function() {
    this.removeAttribute(name2);
  };
}
function attrRemoveNS(fullname) {
  return function() {
    this.removeAttributeNS(fullname.space, fullname.local);
  };
}
function attrConstant(name2, value) {
  return function() {
    this.setAttribute(name2, value);
  };
}
function attrConstantNS(fullname, value) {
  return function() {
    this.setAttributeNS(fullname.space, fullname.local, value);
  };
}
function attrFunction(name2, value) {
  return function() {
    var v2 = value.apply(this, arguments);
    if (v2 == null) this.removeAttribute(name2);
    else this.setAttribute(name2, v2);
  };
}
function attrFunctionNS(fullname, value) {
  return function() {
    var v2 = value.apply(this, arguments);
    if (v2 == null) this.removeAttributeNS(fullname.space, fullname.local);
    else this.setAttributeNS(fullname.space, fullname.local, v2);
  };
}
function attr_default(name2, value) {
  var fullname = namespace_default(name2);
  if (arguments.length < 2) {
    var node = this.node();
    return fullname.local ? node.getAttributeNS(fullname.space, fullname.local) : node.getAttribute(fullname);
  }
  return this.each((value == null ? fullname.local ? attrRemoveNS : attrRemove : typeof value === "function" ? fullname.local ? attrFunctionNS : attrFunction : fullname.local ? attrConstantNS : attrConstant)(fullname, value));
}

// node_modules/d3-selection/src/window.js
function window_default(node) {
  return node.ownerDocument && node.ownerDocument.defaultView || node.document && node || node.defaultView;
}

// node_modules/d3-selection/src/selection/style.js
function styleRemove(name2) {
  return function() {
    this.style.removeProperty(name2);
  };
}
function styleConstant(name2, value, priority) {
  return function() {
    this.style.setProperty(name2, value, priority);
  };
}
function styleFunction(name2, value, priority) {
  return function() {
    var v2 = value.apply(this, arguments);
    if (v2 == null) this.style.removeProperty(name2);
    else this.style.setProperty(name2, v2, priority);
  };
}
function style_default(name2, value, priority) {
  return arguments.length > 1 ? this.each((value == null ? styleRemove : typeof value === "function" ? styleFunction : styleConstant)(name2, value, priority == null ? "" : priority)) : styleValue(this.node(), name2);
}
function styleValue(node, name2) {
  return node.style.getPropertyValue(name2) || window_default(node).getComputedStyle(node, null).getPropertyValue(name2);
}

// node_modules/d3-selection/src/selection/property.js
function propertyRemove(name2) {
  return function() {
    delete this[name2];
  };
}
function propertyConstant(name2, value) {
  return function() {
    this[name2] = value;
  };
}
function propertyFunction(name2, value) {
  return function() {
    var v2 = value.apply(this, arguments);
    if (v2 == null) delete this[name2];
    else this[name2] = v2;
  };
}
function property_default(name2, value) {
  return arguments.length > 1 ? this.each((value == null ? propertyRemove : typeof value === "function" ? propertyFunction : propertyConstant)(name2, value)) : this.node()[name2];
}

// node_modules/d3-selection/src/selection/classed.js
function classArray(string) {
  return string.trim().split(/^|\s+/);
}
function classList(node) {
  return node.classList || new ClassList(node);
}
function ClassList(node) {
  this._node = node;
  this._names = classArray(node.getAttribute("class") || "");
}
ClassList.prototype = {
  add: function(name2) {
    var i2 = this._names.indexOf(name2);
    if (i2 < 0) {
      this._names.push(name2);
      this._node.setAttribute("class", this._names.join(" "));
    }
  },
  remove: function(name2) {
    var i2 = this._names.indexOf(name2);
    if (i2 >= 0) {
      this._names.splice(i2, 1);
      this._node.setAttribute("class", this._names.join(" "));
    }
  },
  contains: function(name2) {
    return this._names.indexOf(name2) >= 0;
  }
};
function classedAdd(node, names) {
  var list = classList(node), i2 = -1, n2 = names.length;
  while (++i2 < n2) list.add(names[i2]);
}
function classedRemove(node, names) {
  var list = classList(node), i2 = -1, n2 = names.length;
  while (++i2 < n2) list.remove(names[i2]);
}
function classedTrue(names) {
  return function() {
    classedAdd(this, names);
  };
}
function classedFalse(names) {
  return function() {
    classedRemove(this, names);
  };
}
function classedFunction(names, value) {
  return function() {
    (value.apply(this, arguments) ? classedAdd : classedRemove)(this, names);
  };
}
function classed_default(name2, value) {
  var names = classArray(name2 + "");
  if (arguments.length < 2) {
    var list = classList(this.node()), i2 = -1, n2 = names.length;
    while (++i2 < n2) if (!list.contains(names[i2])) return false;
    return true;
  }
  return this.each((typeof value === "function" ? classedFunction : value ? classedTrue : classedFalse)(names, value));
}

// node_modules/d3-selection/src/selection/text.js
function textRemove() {
  this.textContent = "";
}
function textConstant(value) {
  return function() {
    this.textContent = value;
  };
}
function textFunction(value) {
  return function() {
    var v2 = value.apply(this, arguments);
    this.textContent = v2 == null ? "" : v2;
  };
}
function text_default(value) {
  return arguments.length ? this.each(value == null ? textRemove : (typeof value === "function" ? textFunction : textConstant)(value)) : this.node().textContent;
}

// node_modules/d3-selection/src/selection/html.js
function htmlRemove() {
  this.innerHTML = "";
}
function htmlConstant(value) {
  return function() {
    this.innerHTML = value;
  };
}
function htmlFunction(value) {
  return function() {
    var v2 = value.apply(this, arguments);
    this.innerHTML = v2 == null ? "" : v2;
  };
}
function html_default(value) {
  return arguments.length ? this.each(value == null ? htmlRemove : (typeof value === "function" ? htmlFunction : htmlConstant)(value)) : this.node().innerHTML;
}

// node_modules/d3-selection/src/selection/raise.js
function raise() {
  if (this.nextSibling) this.parentNode.appendChild(this);
}
function raise_default() {
  return this.each(raise);
}

// node_modules/d3-selection/src/selection/lower.js
function lower() {
  if (this.previousSibling) this.parentNode.insertBefore(this, this.parentNode.firstChild);
}
function lower_default() {
  return this.each(lower);
}

// node_modules/d3-selection/src/selection/append.js
function append_default(name2) {
  var create2 = typeof name2 === "function" ? name2 : creator_default(name2);
  return this.select(function() {
    return this.appendChild(create2.apply(this, arguments));
  });
}

// node_modules/d3-selection/src/selection/insert.js
function constantNull() {
  return null;
}
function insert_default(name2, before) {
  var create2 = typeof name2 === "function" ? name2 : creator_default(name2), select = before == null ? constantNull : typeof before === "function" ? before : selector_default(before);
  return this.select(function() {
    return this.insertBefore(create2.apply(this, arguments), select.apply(this, arguments) || null);
  });
}

// node_modules/d3-selection/src/selection/remove.js
function remove() {
  var parent = this.parentNode;
  if (parent) parent.removeChild(this);
}
function remove_default() {
  return this.each(remove);
}

// node_modules/d3-selection/src/selection/clone.js
function selection_cloneShallow() {
  var clone = this.cloneNode(false), parent = this.parentNode;
  return parent ? parent.insertBefore(clone, this.nextSibling) : clone;
}
function selection_cloneDeep() {
  var clone = this.cloneNode(true), parent = this.parentNode;
  return parent ? parent.insertBefore(clone, this.nextSibling) : clone;
}
function clone_default(deep) {
  return this.select(deep ? selection_cloneDeep : selection_cloneShallow);
}

// node_modules/d3-selection/src/selection/datum.js
function datum_default(value) {
  return arguments.length ? this.property("__data__", value) : this.node().__data__;
}

// node_modules/d3-selection/src/selection/on.js
function contextListener(listener) {
  return function(event) {
    listener.call(this, event, this.__data__);
  };
}
function parseTypenames2(typenames) {
  return typenames.trim().split(/^|\s+/).map(function(t) {
    var name2 = "", i2 = t.indexOf(".");
    if (i2 >= 0) name2 = t.slice(i2 + 1), t = t.slice(0, i2);
    return { type: t, name: name2 };
  });
}
function onRemove(typename) {
  return function() {
    var on = this.__on;
    if (!on) return;
    for (var j3 = 0, i2 = -1, m2 = on.length, o2; j3 < m2; ++j3) {
      if (o2 = on[j3], (!typename.type || o2.type === typename.type) && o2.name === typename.name) {
        this.removeEventListener(o2.type, o2.listener, o2.options);
      } else {
        on[++i2] = o2;
      }
    }
    if (++i2) on.length = i2;
    else delete this.__on;
  };
}
function onAdd(typename, value, options) {
  return function() {
    var on = this.__on, o2, listener = contextListener(value);
    if (on) for (var j3 = 0, m2 = on.length; j3 < m2; ++j3) {
      if ((o2 = on[j3]).type === typename.type && o2.name === typename.name) {
        this.removeEventListener(o2.type, o2.listener, o2.options);
        this.addEventListener(o2.type, o2.listener = listener, o2.options = options);
        o2.value = value;
        return;
      }
    }
    this.addEventListener(typename.type, listener, options);
    o2 = { type: typename.type, name: typename.name, value, listener, options };
    if (!on) this.__on = [o2];
    else on.push(o2);
  };
}
function on_default(typename, value, options) {
  var typenames = parseTypenames2(typename + ""), i2, n2 = typenames.length, t;
  if (arguments.length < 2) {
    var on = this.node().__on;
    if (on) for (var j3 = 0, m2 = on.length, o2; j3 < m2; ++j3) {
      for (i2 = 0, o2 = on[j3]; i2 < n2; ++i2) {
        if ((t = typenames[i2]).type === o2.type && t.name === o2.name) {
          return o2.value;
        }
      }
    }
    return;
  }
  on = value ? onAdd : onRemove;
  for (i2 = 0; i2 < n2; ++i2) this.each(on(typenames[i2], value, options));
  return this;
}

// node_modules/d3-selection/src/selection/dispatch.js
function dispatchEvent(node, type, params) {
  var window2 = window_default(node), event = window2.CustomEvent;
  if (typeof event === "function") {
    event = new event(type, params);
  } else {
    event = window2.document.createEvent("Event");
    if (params) event.initEvent(type, params.bubbles, params.cancelable), event.detail = params.detail;
    else event.initEvent(type, false, false);
  }
  node.dispatchEvent(event);
}
function dispatchConstant(type, params) {
  return function() {
    return dispatchEvent(this, type, params);
  };
}
function dispatchFunction(type, params) {
  return function() {
    return dispatchEvent(this, type, params.apply(this, arguments));
  };
}
function dispatch_default2(type, params) {
  return this.each((typeof params === "function" ? dispatchFunction : dispatchConstant)(type, params));
}

// node_modules/d3-selection/src/selection/iterator.js
function* iterator_default() {
  for (var groups = this._groups, j3 = 0, m2 = groups.length; j3 < m2; ++j3) {
    for (var group = groups[j3], i2 = 0, n2 = group.length, node; i2 < n2; ++i2) {
      if (node = group[i2]) yield node;
    }
  }
}

// node_modules/d3-selection/src/selection/index.js
var root = [null];
function Selection(groups, parents) {
  this._groups = groups;
  this._parents = parents;
}
function selection() {
  return new Selection([[document.documentElement]], root);
}
function selection_selection() {
  return this;
}
Selection.prototype = selection.prototype = {
  constructor: Selection,
  select: select_default,
  selectAll: selectAll_default,
  selectChild: selectChild_default,
  selectChildren: selectChildren_default,
  filter: filter_default,
  data: data_default,
  enter: enter_default,
  exit: exit_default,
  join: join_default,
  merge: merge_default,
  selection: selection_selection,
  order: order_default,
  sort: sort_default,
  call: call_default,
  nodes: nodes_default,
  node: node_default,
  size: size_default,
  empty: empty_default,
  each: each_default,
  attr: attr_default,
  style: style_default,
  property: property_default,
  classed: classed_default,
  text: text_default,
  html: html_default,
  raise: raise_default,
  lower: lower_default,
  append: append_default,
  insert: insert_default,
  remove: remove_default,
  clone: clone_default,
  datum: datum_default,
  on: on_default,
  dispatch: dispatch_default2,
  [Symbol.iterator]: iterator_default
};
var selection_default = selection;

// node_modules/d3-selection/src/select.js
function select_default2(selector) {
  return typeof selector === "string" ? new Selection([[document.querySelector(selector)]], [document.documentElement]) : new Selection([[selector]], root);
}

// node_modules/d3-selection/src/sourceEvent.js
function sourceEvent_default(event) {
  let sourceEvent;
  while (sourceEvent = event.sourceEvent) event = sourceEvent;
  return event;
}

// node_modules/d3-selection/src/pointer.js
function pointer_default(event, node) {
  event = sourceEvent_default(event);
  if (node === void 0) node = event.currentTarget;
  if (node) {
    var svg = node.ownerSVGElement || node;
    if (svg.createSVGPoint) {
      var point = svg.createSVGPoint();
      point.x = event.clientX, point.y = event.clientY;
      point = point.matrixTransform(node.getScreenCTM().inverse());
      return [point.x, point.y];
    }
    if (node.getBoundingClientRect) {
      var rect = node.getBoundingClientRect();
      return [event.clientX - rect.left - node.clientLeft, event.clientY - rect.top - node.clientTop];
    }
  }
  return [event.pageX, event.pageY];
}

// node_modules/d3-drag/src/noevent.js
var nonpassive = { passive: false };
var nonpassivecapture = { capture: true, passive: false };
function nopropagation(event) {
  event.stopImmediatePropagation();
}
function noevent_default(event) {
  event.preventDefault();
  event.stopImmediatePropagation();
}

// node_modules/d3-drag/src/nodrag.js
function nodrag_default(view) {
  var root2 = view.document.documentElement, selection2 = select_default2(view).on("dragstart.drag", noevent_default, nonpassivecapture);
  if ("onselectstart" in root2) {
    selection2.on("selectstart.drag", noevent_default, nonpassivecapture);
  } else {
    root2.__noselect = root2.style.MozUserSelect;
    root2.style.MozUserSelect = "none";
  }
}
function yesdrag(view, noclick) {
  var root2 = view.document.documentElement, selection2 = select_default2(view).on("dragstart.drag", null);
  if (noclick) {
    selection2.on("click.drag", noevent_default, nonpassivecapture);
    setTimeout(function() {
      selection2.on("click.drag", null);
    }, 0);
  }
  if ("onselectstart" in root2) {
    selection2.on("selectstart.drag", null);
  } else {
    root2.style.MozUserSelect = root2.__noselect;
    delete root2.__noselect;
  }
}

// node_modules/d3-drag/src/constant.js
var constant_default2 = (x3) => () => x3;

// node_modules/d3-drag/src/event.js
function DragEvent(type, {
  sourceEvent,
  subject,
  target,
  identifier,
  active,
  x: x3,
  y: y2,
  dx: dx2,
  dy,
  dispatch: dispatch2
}) {
  Object.defineProperties(this, {
    type: { value: type, enumerable: true, configurable: true },
    sourceEvent: { value: sourceEvent, enumerable: true, configurable: true },
    subject: { value: subject, enumerable: true, configurable: true },
    target: { value: target, enumerable: true, configurable: true },
    identifier: { value: identifier, enumerable: true, configurable: true },
    active: { value: active, enumerable: true, configurable: true },
    x: { value: x3, enumerable: true, configurable: true },
    y: { value: y2, enumerable: true, configurable: true },
    dx: { value: dx2, enumerable: true, configurable: true },
    dy: { value: dy, enumerable: true, configurable: true },
    _: { value: dispatch2 }
  });
}
DragEvent.prototype.on = function() {
  var value = this._.on.apply(this._, arguments);
  return value === this._ ? this : value;
};

// node_modules/d3-drag/src/drag.js
function defaultFilter(event) {
  return !event.ctrlKey && !event.button;
}
function defaultContainer() {
  return this.parentNode;
}
function defaultSubject(event, d2) {
  return d2 == null ? { x: event.x, y: event.y } : d2;
}
function defaultTouchable() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function drag_default() {
  var filter2 = defaultFilter, container = defaultContainer, subject = defaultSubject, touchable = defaultTouchable, gestures = {}, listeners2 = dispatch_default("start", "drag", "end"), active = 0, mousedownx, mousedowny, mousemoving, touchending, clickDistance2 = 0;
  function drag(selection2) {
    selection2.on("mousedown.drag", mousedowned).filter(touchable).on("touchstart.drag", touchstarted).on("touchmove.drag", touchmoved, nonpassive).on("touchend.drag touchcancel.drag", touchended).style("touch-action", "none").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  function mousedowned(event, d2) {
    if (touchending || !filter2.call(this, event, d2)) return;
    var gesture = beforestart(this, container.call(this, event, d2), event, d2, "mouse");
    if (!gesture) return;
    select_default2(event.view).on("mousemove.drag", mousemoved, nonpassivecapture).on("mouseup.drag", mouseupped, nonpassivecapture);
    nodrag_default(event.view);
    nopropagation(event);
    mousemoving = false;
    mousedownx = event.clientX;
    mousedowny = event.clientY;
    gesture("start", event);
  }
  function mousemoved(event) {
    noevent_default(event);
    if (!mousemoving) {
      var dx2 = event.clientX - mousedownx, dy = event.clientY - mousedowny;
      mousemoving = dx2 * dx2 + dy * dy > clickDistance2;
    }
    gestures.mouse("drag", event);
  }
  function mouseupped(event) {
    select_default2(event.view).on("mousemove.drag mouseup.drag", null);
    yesdrag(event.view, mousemoving);
    noevent_default(event);
    gestures.mouse("end", event);
  }
  function touchstarted(event, d2) {
    if (!filter2.call(this, event, d2)) return;
    var touches = event.changedTouches, c = container.call(this, event, d2), n2 = touches.length, i2, gesture;
    for (i2 = 0; i2 < n2; ++i2) {
      if (gesture = beforestart(this, c, event, d2, touches[i2].identifier, touches[i2])) {
        nopropagation(event);
        gesture("start", event, touches[i2]);
      }
    }
  }
  function touchmoved(event) {
    var touches = event.changedTouches, n2 = touches.length, i2, gesture;
    for (i2 = 0; i2 < n2; ++i2) {
      if (gesture = gestures[touches[i2].identifier]) {
        noevent_default(event);
        gesture("drag", event, touches[i2]);
      }
    }
  }
  function touchended(event) {
    var touches = event.changedTouches, n2 = touches.length, i2, gesture;
    if (touchending) clearTimeout(touchending);
    touchending = setTimeout(function() {
      touchending = null;
    }, 500);
    for (i2 = 0; i2 < n2; ++i2) {
      if (gesture = gestures[touches[i2].identifier]) {
        nopropagation(event);
        gesture("end", event, touches[i2]);
      }
    }
  }
  function beforestart(that, container2, event, d2, identifier, touch) {
    var dispatch2 = listeners2.copy(), p2 = pointer_default(touch || event, container2), dx2, dy, s;
    if ((s = subject.call(that, new DragEvent("beforestart", {
      sourceEvent: event,
      target: drag,
      identifier,
      active,
      x: p2[0],
      y: p2[1],
      dx: 0,
      dy: 0,
      dispatch: dispatch2
    }), d2)) == null) return;
    dx2 = s.x - p2[0] || 0;
    dy = s.y - p2[1] || 0;
    return function gesture(type, event2, touch2) {
      var p0 = p2, n2;
      switch (type) {
        case "start":
          gestures[identifier] = gesture, n2 = active++;
          break;
        case "end":
          delete gestures[identifier], --active;
        // falls through
        case "drag":
          p2 = pointer_default(touch2 || event2, container2), n2 = active;
          break;
      }
      dispatch2.call(
        type,
        that,
        new DragEvent(type, {
          sourceEvent: event2,
          subject: s,
          target: drag,
          identifier,
          active: n2,
          x: p2[0] + dx2,
          y: p2[1] + dy,
          dx: p2[0] - p0[0],
          dy: p2[1] - p0[1],
          dispatch: dispatch2
        }),
        d2
      );
    };
  }
  drag.filter = function(_3) {
    return arguments.length ? (filter2 = typeof _3 === "function" ? _3 : constant_default2(!!_3), drag) : filter2;
  };
  drag.container = function(_3) {
    return arguments.length ? (container = typeof _3 === "function" ? _3 : constant_default2(_3), drag) : container;
  };
  drag.subject = function(_3) {
    return arguments.length ? (subject = typeof _3 === "function" ? _3 : constant_default2(_3), drag) : subject;
  };
  drag.touchable = function(_3) {
    return arguments.length ? (touchable = typeof _3 === "function" ? _3 : constant_default2(!!_3), drag) : touchable;
  };
  drag.on = function() {
    var value = listeners2.on.apply(listeners2, arguments);
    return value === listeners2 ? drag : value;
  };
  drag.clickDistance = function(_3) {
    return arguments.length ? (clickDistance2 = (_3 = +_3) * _3, drag) : Math.sqrt(clickDistance2);
  };
  return drag;
}

// node_modules/d3-color/src/define.js
function define_default(constructor, factory, prototype) {
  constructor.prototype = factory.prototype = prototype;
  prototype.constructor = constructor;
}
function extend(parent, definition) {
  var prototype = Object.create(parent.prototype);
  for (var key in definition) prototype[key] = definition[key];
  return prototype;
}

// node_modules/d3-color/src/color.js
function Color() {
}
var darker = 0.7;
var brighter = 1 / darker;
var reI = "\\s*([+-]?\\d+)\\s*";
var reN = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
var reP = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
var reHex = /^#([0-9a-f]{3,8})$/;
var reRgbInteger = new RegExp(`^rgb\\(${reI},${reI},${reI}\\)$`);
var reRgbPercent = new RegExp(`^rgb\\(${reP},${reP},${reP}\\)$`);
var reRgbaInteger = new RegExp(`^rgba\\(${reI},${reI},${reI},${reN}\\)$`);
var reRgbaPercent = new RegExp(`^rgba\\(${reP},${reP},${reP},${reN}\\)$`);
var reHslPercent = new RegExp(`^hsl\\(${reN},${reP},${reP}\\)$`);
var reHslaPercent = new RegExp(`^hsla\\(${reN},${reP},${reP},${reN}\\)$`);
var named = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
define_default(Color, color, {
  copy(channels) {
    return Object.assign(new this.constructor(), this, channels);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: color_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: color_formatHex,
  formatHex8: color_formatHex8,
  formatHsl: color_formatHsl,
  formatRgb: color_formatRgb,
  toString: color_formatRgb
});
function color_formatHex() {
  return this.rgb().formatHex();
}
function color_formatHex8() {
  return this.rgb().formatHex8();
}
function color_formatHsl() {
  return hslConvert(this).formatHsl();
}
function color_formatRgb() {
  return this.rgb().formatRgb();
}
function color(format) {
  var m2, l2;
  format = (format + "").trim().toLowerCase();
  return (m2 = reHex.exec(format)) ? (l2 = m2[1].length, m2 = parseInt(m2[1], 16), l2 === 6 ? rgbn(m2) : l2 === 3 ? new Rgb(m2 >> 8 & 15 | m2 >> 4 & 240, m2 >> 4 & 15 | m2 & 240, (m2 & 15) << 4 | m2 & 15, 1) : l2 === 8 ? rgba(m2 >> 24 & 255, m2 >> 16 & 255, m2 >> 8 & 255, (m2 & 255) / 255) : l2 === 4 ? rgba(m2 >> 12 & 15 | m2 >> 8 & 240, m2 >> 8 & 15 | m2 >> 4 & 240, m2 >> 4 & 15 | m2 & 240, ((m2 & 15) << 4 | m2 & 15) / 255) : null) : (m2 = reRgbInteger.exec(format)) ? new Rgb(m2[1], m2[2], m2[3], 1) : (m2 = reRgbPercent.exec(format)) ? new Rgb(m2[1] * 255 / 100, m2[2] * 255 / 100, m2[3] * 255 / 100, 1) : (m2 = reRgbaInteger.exec(format)) ? rgba(m2[1], m2[2], m2[3], m2[4]) : (m2 = reRgbaPercent.exec(format)) ? rgba(m2[1] * 255 / 100, m2[2] * 255 / 100, m2[3] * 255 / 100, m2[4]) : (m2 = reHslPercent.exec(format)) ? hsla(m2[1], m2[2] / 100, m2[3] / 100, 1) : (m2 = reHslaPercent.exec(format)) ? hsla(m2[1], m2[2] / 100, m2[3] / 100, m2[4]) : named.hasOwnProperty(format) ? rgbn(named[format]) : format === "transparent" ? new Rgb(NaN, NaN, NaN, 0) : null;
}
function rgbn(n2) {
  return new Rgb(n2 >> 16 & 255, n2 >> 8 & 255, n2 & 255, 1);
}
function rgba(r, g2, b, a) {
  if (a <= 0) r = g2 = b = NaN;
  return new Rgb(r, g2, b, a);
}
function rgbConvert(o2) {
  if (!(o2 instanceof Color)) o2 = color(o2);
  if (!o2) return new Rgb();
  o2 = o2.rgb();
  return new Rgb(o2.r, o2.g, o2.b, o2.opacity);
}
function rgb(r, g2, b, opacity) {
  return arguments.length === 1 ? rgbConvert(r) : new Rgb(r, g2, b, opacity == null ? 1 : opacity);
}
function Rgb(r, g2, b, opacity) {
  this.r = +r;
  this.g = +g2;
  this.b = +b;
  this.opacity = +opacity;
}
define_default(Rgb, rgb, extend(Color, {
  brighter(k2) {
    k2 = k2 == null ? brighter : Math.pow(brighter, k2);
    return new Rgb(this.r * k2, this.g * k2, this.b * k2, this.opacity);
  },
  darker(k2) {
    k2 = k2 == null ? darker : Math.pow(darker, k2);
    return new Rgb(this.r * k2, this.g * k2, this.b * k2, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Rgb(clampi(this.r), clampi(this.g), clampi(this.b), clampa(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && (-0.5 <= this.g && this.g < 255.5) && (-0.5 <= this.b && this.b < 255.5) && (0 <= this.opacity && this.opacity <= 1);
  },
  hex: rgb_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: rgb_formatHex,
  formatHex8: rgb_formatHex8,
  formatRgb: rgb_formatRgb,
  toString: rgb_formatRgb
}));
function rgb_formatHex() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}`;
}
function rgb_formatHex8() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}${hex((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function rgb_formatRgb() {
  const a = clampa(this.opacity);
  return `${a === 1 ? "rgb(" : "rgba("}${clampi(this.r)}, ${clampi(this.g)}, ${clampi(this.b)}${a === 1 ? ")" : `, ${a})`}`;
}
function clampa(opacity) {
  return isNaN(opacity) ? 1 : Math.max(0, Math.min(1, opacity));
}
function clampi(value) {
  return Math.max(0, Math.min(255, Math.round(value) || 0));
}
function hex(value) {
  value = clampi(value);
  return (value < 16 ? "0" : "") + value.toString(16);
}
function hsla(h, s, l2, a) {
  if (a <= 0) h = s = l2 = NaN;
  else if (l2 <= 0 || l2 >= 1) h = s = NaN;
  else if (s <= 0) h = NaN;
  return new Hsl(h, s, l2, a);
}
function hslConvert(o2) {
  if (o2 instanceof Hsl) return new Hsl(o2.h, o2.s, o2.l, o2.opacity);
  if (!(o2 instanceof Color)) o2 = color(o2);
  if (!o2) return new Hsl();
  if (o2 instanceof Hsl) return o2;
  o2 = o2.rgb();
  var r = o2.r / 255, g2 = o2.g / 255, b = o2.b / 255, min = Math.min(r, g2, b), max = Math.max(r, g2, b), h = NaN, s = max - min, l2 = (max + min) / 2;
  if (s) {
    if (r === max) h = (g2 - b) / s + (g2 < b) * 6;
    else if (g2 === max) h = (b - r) / s + 2;
    else h = (r - g2) / s + 4;
    s /= l2 < 0.5 ? max + min : 2 - max - min;
    h *= 60;
  } else {
    s = l2 > 0 && l2 < 1 ? 0 : h;
  }
  return new Hsl(h, s, l2, o2.opacity);
}
function hsl(h, s, l2, opacity) {
  return arguments.length === 1 ? hslConvert(h) : new Hsl(h, s, l2, opacity == null ? 1 : opacity);
}
function Hsl(h, s, l2, opacity) {
  this.h = +h;
  this.s = +s;
  this.l = +l2;
  this.opacity = +opacity;
}
define_default(Hsl, hsl, extend(Color, {
  brighter(k2) {
    k2 = k2 == null ? brighter : Math.pow(brighter, k2);
    return new Hsl(this.h, this.s, this.l * k2, this.opacity);
  },
  darker(k2) {
    k2 = k2 == null ? darker : Math.pow(darker, k2);
    return new Hsl(this.h, this.s, this.l * k2, this.opacity);
  },
  rgb() {
    var h = this.h % 360 + (this.h < 0) * 360, s = isNaN(h) || isNaN(this.s) ? 0 : this.s, l2 = this.l, m2 = l2 + (l2 < 0.5 ? l2 : 1 - l2) * s, m1 = 2 * l2 - m2;
    return new Rgb(
      hsl2rgb(h >= 240 ? h - 240 : h + 120, m1, m2),
      hsl2rgb(h, m1, m2),
      hsl2rgb(h < 120 ? h + 240 : h - 120, m1, m2),
      this.opacity
    );
  },
  clamp() {
    return new Hsl(clamph(this.h), clampt(this.s), clampt(this.l), clampa(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && (0 <= this.l && this.l <= 1) && (0 <= this.opacity && this.opacity <= 1);
  },
  formatHsl() {
    const a = clampa(this.opacity);
    return `${a === 1 ? "hsl(" : "hsla("}${clamph(this.h)}, ${clampt(this.s) * 100}%, ${clampt(this.l) * 100}%${a === 1 ? ")" : `, ${a})`}`;
  }
}));
function clamph(value) {
  value = (value || 0) % 360;
  return value < 0 ? value + 360 : value;
}
function clampt(value) {
  return Math.max(0, Math.min(1, value || 0));
}
function hsl2rgb(h, m1, m2) {
  return (h < 60 ? m1 + (m2 - m1) * h / 60 : h < 180 ? m2 : h < 240 ? m1 + (m2 - m1) * (240 - h) / 60 : m1) * 255;
}

// node_modules/d3-interpolate/src/basis.js
function basis(t1, v0, v1, v2, v3) {
  var t2 = t1 * t1, t3 = t2 * t1;
  return ((1 - 3 * t1 + 3 * t2 - t3) * v0 + (4 - 6 * t2 + 3 * t3) * v1 + (1 + 3 * t1 + 3 * t2 - 3 * t3) * v2 + t3 * v3) / 6;
}
function basis_default(values) {
  var n2 = values.length - 1;
  return function(t) {
    var i2 = t <= 0 ? t = 0 : t >= 1 ? (t = 1, n2 - 1) : Math.floor(t * n2), v1 = values[i2], v2 = values[i2 + 1], v0 = i2 > 0 ? values[i2 - 1] : 2 * v1 - v2, v3 = i2 < n2 - 1 ? values[i2 + 2] : 2 * v2 - v1;
    return basis((t - i2 / n2) * n2, v0, v1, v2, v3);
  };
}

// node_modules/d3-interpolate/src/basisClosed.js
function basisClosed_default(values) {
  var n2 = values.length;
  return function(t) {
    var i2 = Math.floor(((t %= 1) < 0 ? ++t : t) * n2), v0 = values[(i2 + n2 - 1) % n2], v1 = values[i2 % n2], v2 = values[(i2 + 1) % n2], v3 = values[(i2 + 2) % n2];
    return basis((t - i2 / n2) * n2, v0, v1, v2, v3);
  };
}

// node_modules/d3-interpolate/src/constant.js
var constant_default3 = (x3) => () => x3;

// node_modules/d3-interpolate/src/color.js
function linear(a, d2) {
  return function(t) {
    return a + t * d2;
  };
}
function exponential(a, b, y2) {
  return a = Math.pow(a, y2), b = Math.pow(b, y2) - a, y2 = 1 / y2, function(t) {
    return Math.pow(a + t * b, y2);
  };
}
function gamma(y2) {
  return (y2 = +y2) === 1 ? nogamma : function(a, b) {
    return b - a ? exponential(a, b, y2) : constant_default3(isNaN(a) ? b : a);
  };
}
function nogamma(a, b) {
  var d2 = b - a;
  return d2 ? linear(a, d2) : constant_default3(isNaN(a) ? b : a);
}

// node_modules/d3-interpolate/src/rgb.js
var rgb_default = (function rgbGamma(y2) {
  var color2 = gamma(y2);
  function rgb2(start2, end) {
    var r = color2((start2 = rgb(start2)).r, (end = rgb(end)).r), g2 = color2(start2.g, end.g), b = color2(start2.b, end.b), opacity = nogamma(start2.opacity, end.opacity);
    return function(t) {
      start2.r = r(t);
      start2.g = g2(t);
      start2.b = b(t);
      start2.opacity = opacity(t);
      return start2 + "";
    };
  }
  rgb2.gamma = rgbGamma;
  return rgb2;
})(1);
function rgbSpline(spline) {
  return function(colors) {
    var n2 = colors.length, r = new Array(n2), g2 = new Array(n2), b = new Array(n2), i2, color2;
    for (i2 = 0; i2 < n2; ++i2) {
      color2 = rgb(colors[i2]);
      r[i2] = color2.r || 0;
      g2[i2] = color2.g || 0;
      b[i2] = color2.b || 0;
    }
    r = spline(r);
    g2 = spline(g2);
    b = spline(b);
    color2.opacity = 1;
    return function(t) {
      color2.r = r(t);
      color2.g = g2(t);
      color2.b = b(t);
      return color2 + "";
    };
  };
}
var rgbBasis = rgbSpline(basis_default);
var rgbBasisClosed = rgbSpline(basisClosed_default);

// node_modules/d3-interpolate/src/numberArray.js
function numberArray_default(a, b) {
  if (!b) b = [];
  var n2 = a ? Math.min(b.length, a.length) : 0, c = b.slice(), i2;
  return function(t) {
    for (i2 = 0; i2 < n2; ++i2) c[i2] = a[i2] * (1 - t) + b[i2] * t;
    return c;
  };
}
function isNumberArray(x3) {
  return ArrayBuffer.isView(x3) && !(x3 instanceof DataView);
}

// node_modules/d3-interpolate/src/array.js
function genericArray(a, b) {
  var nb = b ? b.length : 0, na = a ? Math.min(nb, a.length) : 0, x3 = new Array(na), c = new Array(nb), i2;
  for (i2 = 0; i2 < na; ++i2) x3[i2] = value_default(a[i2], b[i2]);
  for (; i2 < nb; ++i2) c[i2] = b[i2];
  return function(t) {
    for (i2 = 0; i2 < na; ++i2) c[i2] = x3[i2](t);
    return c;
  };
}

// node_modules/d3-interpolate/src/date.js
function date_default(a, b) {
  var d2 = /* @__PURE__ */ new Date();
  return a = +a, b = +b, function(t) {
    return d2.setTime(a * (1 - t) + b * t), d2;
  };
}

// node_modules/d3-interpolate/src/number.js
function number_default(a, b) {
  return a = +a, b = +b, function(t) {
    return a * (1 - t) + b * t;
  };
}

// node_modules/d3-interpolate/src/object.js
function object_default(a, b) {
  var i2 = {}, c = {}, k2;
  if (a === null || typeof a !== "object") a = {};
  if (b === null || typeof b !== "object") b = {};
  for (k2 in b) {
    if (k2 in a) {
      i2[k2] = value_default(a[k2], b[k2]);
    } else {
      c[k2] = b[k2];
    }
  }
  return function(t) {
    for (k2 in i2) c[k2] = i2[k2](t);
    return c;
  };
}

// node_modules/d3-interpolate/src/string.js
var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var reB = new RegExp(reA.source, "g");
function zero(b) {
  return function() {
    return b;
  };
}
function one(b) {
  return function(t) {
    return b(t) + "";
  };
}
function string_default(a, b) {
  var bi = reA.lastIndex = reB.lastIndex = 0, am, bm, bs, i2 = -1, s = [], q3 = [];
  a = a + "", b = b + "";
  while ((am = reA.exec(a)) && (bm = reB.exec(b))) {
    if ((bs = bm.index) > bi) {
      bs = b.slice(bi, bs);
      if (s[i2]) s[i2] += bs;
      else s[++i2] = bs;
    }
    if ((am = am[0]) === (bm = bm[0])) {
      if (s[i2]) s[i2] += bm;
      else s[++i2] = bm;
    } else {
      s[++i2] = null;
      q3.push({ i: i2, x: number_default(am, bm) });
    }
    bi = reB.lastIndex;
  }
  if (bi < b.length) {
    bs = b.slice(bi);
    if (s[i2]) s[i2] += bs;
    else s[++i2] = bs;
  }
  return s.length < 2 ? q3[0] ? one(q3[0].x) : zero(b) : (b = q3.length, function(t) {
    for (var i3 = 0, o2; i3 < b; ++i3) s[(o2 = q3[i3]).i] = o2.x(t);
    return s.join("");
  });
}

// node_modules/d3-interpolate/src/value.js
function value_default(a, b) {
  var t = typeof b, c;
  return b == null || t === "boolean" ? constant_default3(b) : (t === "number" ? number_default : t === "string" ? (c = color(b)) ? (b = c, rgb_default) : string_default : b instanceof color ? rgb_default : b instanceof Date ? date_default : isNumberArray(b) ? numberArray_default : Array.isArray(b) ? genericArray : typeof b.valueOf !== "function" && typeof b.toString !== "function" || isNaN(b) ? object_default : number_default)(a, b);
}

// node_modules/d3-interpolate/src/transform/decompose.js
var degrees = 180 / Math.PI;
var identity = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function decompose_default(a, b, c, d2, e, f2) {
  var scaleX, scaleY, skewX;
  if (scaleX = Math.sqrt(a * a + b * b)) a /= scaleX, b /= scaleX;
  if (skewX = a * c + b * d2) c -= a * skewX, d2 -= b * skewX;
  if (scaleY = Math.sqrt(c * c + d2 * d2)) c /= scaleY, d2 /= scaleY, skewX /= scaleY;
  if (a * d2 < b * c) a = -a, b = -b, skewX = -skewX, scaleX = -scaleX;
  return {
    translateX: e,
    translateY: f2,
    rotate: Math.atan2(b, a) * degrees,
    skewX: Math.atan(skewX) * degrees,
    scaleX,
    scaleY
  };
}

// node_modules/d3-interpolate/src/transform/parse.js
var svgNode;
function parseCss(value) {
  const m2 = new (typeof DOMMatrix === "function" ? DOMMatrix : WebKitCSSMatrix)(value + "");
  return m2.isIdentity ? identity : decompose_default(m2.a, m2.b, m2.c, m2.d, m2.e, m2.f);
}
function parseSvg(value) {
  if (value == null) return identity;
  if (!svgNode) svgNode = document.createElementNS("http://www.w3.org/2000/svg", "g");
  svgNode.setAttribute("transform", value);
  if (!(value = svgNode.transform.baseVal.consolidate())) return identity;
  value = value.matrix;
  return decompose_default(value.a, value.b, value.c, value.d, value.e, value.f);
}

// node_modules/d3-interpolate/src/transform/index.js
function interpolateTransform(parse, pxComma, pxParen, degParen) {
  function pop2(s) {
    return s.length ? s.pop() + " " : "";
  }
  function translate(xa, ya, xb, yb, s, q3) {
    if (xa !== xb || ya !== yb) {
      var i2 = s.push("translate(", null, pxComma, null, pxParen);
      q3.push({ i: i2 - 4, x: number_default(xa, xb) }, { i: i2 - 2, x: number_default(ya, yb) });
    } else if (xb || yb) {
      s.push("translate(" + xb + pxComma + yb + pxParen);
    }
  }
  function rotate(a, b, s, q3) {
    if (a !== b) {
      if (a - b > 180) b += 360;
      else if (b - a > 180) a += 360;
      q3.push({ i: s.push(pop2(s) + "rotate(", null, degParen) - 2, x: number_default(a, b) });
    } else if (b) {
      s.push(pop2(s) + "rotate(" + b + degParen);
    }
  }
  function skewX(a, b, s, q3) {
    if (a !== b) {
      q3.push({ i: s.push(pop2(s) + "skewX(", null, degParen) - 2, x: number_default(a, b) });
    } else if (b) {
      s.push(pop2(s) + "skewX(" + b + degParen);
    }
  }
  function scale(xa, ya, xb, yb, s, q3) {
    if (xa !== xb || ya !== yb) {
      var i2 = s.push(pop2(s) + "scale(", null, ",", null, ")");
      q3.push({ i: i2 - 4, x: number_default(xa, xb) }, { i: i2 - 2, x: number_default(ya, yb) });
    } else if (xb !== 1 || yb !== 1) {
      s.push(pop2(s) + "scale(" + xb + "," + yb + ")");
    }
  }
  return function(a, b) {
    var s = [], q3 = [];
    a = parse(a), b = parse(b);
    translate(a.translateX, a.translateY, b.translateX, b.translateY, s, q3);
    rotate(a.rotate, b.rotate, s, q3);
    skewX(a.skewX, b.skewX, s, q3);
    scale(a.scaleX, a.scaleY, b.scaleX, b.scaleY, s, q3);
    a = b = null;
    return function(t) {
      var i2 = -1, n2 = q3.length, o2;
      while (++i2 < n2) s[(o2 = q3[i2]).i] = o2.x(t);
      return s.join("");
    };
  };
}
var interpolateTransformCss = interpolateTransform(parseCss, "px, ", "px)", "deg)");
var interpolateTransformSvg = interpolateTransform(parseSvg, ", ", ")", ")");

// node_modules/d3-interpolate/src/zoom.js
var epsilon2 = 1e-12;
function cosh(x3) {
  return ((x3 = Math.exp(x3)) + 1 / x3) / 2;
}
function sinh(x3) {
  return ((x3 = Math.exp(x3)) - 1 / x3) / 2;
}
function tanh(x3) {
  return ((x3 = Math.exp(2 * x3)) - 1) / (x3 + 1);
}
var zoom_default = (function zoomRho(rho, rho2, rho4) {
  function zoom(p0, p1) {
    var ux0 = p0[0], uy0 = p0[1], w0 = p0[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx2 = ux1 - ux0, dy = uy1 - uy0, d2 = dx2 * dx2 + dy * dy, i2, S3;
    if (d2 < epsilon2) {
      S3 = Math.log(w1 / w0) / rho;
      i2 = function(t) {
        return [
          ux0 + t * dx2,
          uy0 + t * dy,
          w0 * Math.exp(rho * t * S3)
        ];
      };
    } else {
      var d1 = Math.sqrt(d2), b0 = (w1 * w1 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1), b1 = (w1 * w1 - w0 * w0 - rho4 * d2) / (2 * w1 * rho2 * d1), r0 = Math.log(Math.sqrt(b0 * b0 + 1) - b0), r1 = Math.log(Math.sqrt(b1 * b1 + 1) - b1);
      S3 = (r1 - r0) / rho;
      i2 = function(t) {
        var s = t * S3, coshr0 = cosh(r0), u2 = w0 / (rho2 * d1) * (coshr0 * tanh(rho * s + r0) - sinh(r0));
        return [
          ux0 + u2 * dx2,
          uy0 + u2 * dy,
          w0 * coshr0 / cosh(rho * s + r0)
        ];
      };
    }
    i2.duration = S3 * 1e3 * rho / Math.SQRT2;
    return i2;
  }
  zoom.rho = function(_3) {
    var _1 = Math.max(1e-3, +_3), _22 = _1 * _1, _4 = _22 * _22;
    return zoomRho(_1, _22, _4);
  };
  return zoom;
})(Math.SQRT2, 2, 4);

// node_modules/d3-timer/src/timer.js
var frame = 0;
var timeout = 0;
var interval = 0;
var pokeDelay = 1e3;
var taskHead;
var taskTail;
var clockLast = 0;
var clockNow = 0;
var clockSkew = 0;
var clock = typeof performance === "object" && performance.now ? performance : Date;
var setFrame = typeof window === "object" && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function(f2) {
  setTimeout(f2, 17);
};
function now() {
  return clockNow || (setFrame(clearNow), clockNow = clock.now() + clockSkew);
}
function clearNow() {
  clockNow = 0;
}
function Timer() {
  this._call = this._time = this._next = null;
}
Timer.prototype = timer.prototype = {
  constructor: Timer,
  restart: function(callback, delay, time) {
    if (typeof callback !== "function") throw new TypeError("callback is not a function");
    time = (time == null ? now() : +time) + (delay == null ? 0 : +delay);
    if (!this._next && taskTail !== this) {
      if (taskTail) taskTail._next = this;
      else taskHead = this;
      taskTail = this;
    }
    this._call = callback;
    this._time = time;
    sleep();
  },
  stop: function() {
    if (this._call) {
      this._call = null;
      this._time = Infinity;
      sleep();
    }
  }
};
function timer(callback, delay, time) {
  var t = new Timer();
  t.restart(callback, delay, time);
  return t;
}
function timerFlush() {
  now();
  ++frame;
  var t = taskHead, e;
  while (t) {
    if ((e = clockNow - t._time) >= 0) t._call.call(void 0, e);
    t = t._next;
  }
  --frame;
}
function wake() {
  clockNow = (clockLast = clock.now()) + clockSkew;
  frame = timeout = 0;
  try {
    timerFlush();
  } finally {
    frame = 0;
    nap();
    clockNow = 0;
  }
}
function poke() {
  var now2 = clock.now(), delay = now2 - clockLast;
  if (delay > pokeDelay) clockSkew -= delay, clockLast = now2;
}
function nap() {
  var t0, t1 = taskHead, t2, time = Infinity;
  while (t1) {
    if (t1._call) {
      if (time > t1._time) time = t1._time;
      t0 = t1, t1 = t1._next;
    } else {
      t2 = t1._next, t1._next = null;
      t1 = t0 ? t0._next = t2 : taskHead = t2;
    }
  }
  taskTail = t0;
  sleep(time);
}
function sleep(time) {
  if (frame) return;
  if (timeout) timeout = clearTimeout(timeout);
  var delay = time - clockNow;
  if (delay > 24) {
    if (time < Infinity) timeout = setTimeout(wake, time - clock.now() - clockSkew);
    if (interval) interval = clearInterval(interval);
  } else {
    if (!interval) clockLast = clock.now(), interval = setInterval(poke, pokeDelay);
    frame = 1, setFrame(wake);
  }
}

// node_modules/d3-timer/src/timeout.js
function timeout_default(callback, delay, time) {
  var t = new Timer();
  delay = delay == null ? 0 : +delay;
  t.restart((elapsed) => {
    t.stop();
    callback(elapsed + delay);
  }, delay, time);
  return t;
}

// node_modules/d3-transition/src/transition/schedule.js
var emptyOn = dispatch_default("start", "end", "cancel", "interrupt");
var emptyTween = [];
var CREATED = 0;
var SCHEDULED = 1;
var STARTING = 2;
var STARTED = 3;
var RUNNING = 4;
var ENDING = 5;
var ENDED = 6;
function schedule_default(node, name2, id2, index2, group, timing) {
  var schedules = node.__transition;
  if (!schedules) node.__transition = {};
  else if (id2 in schedules) return;
  create(node, id2, {
    name: name2,
    index: index2,
    // For context during callback.
    group,
    // For context during callback.
    on: emptyOn,
    tween: emptyTween,
    time: timing.time,
    delay: timing.delay,
    duration: timing.duration,
    ease: timing.ease,
    timer: null,
    state: CREATED
  });
}
function init(node, id2) {
  var schedule = get2(node, id2);
  if (schedule.state > CREATED) throw new Error("too late; already scheduled");
  return schedule;
}
function set2(node, id2) {
  var schedule = get2(node, id2);
  if (schedule.state > STARTED) throw new Error("too late; already running");
  return schedule;
}
function get2(node, id2) {
  var schedule = node.__transition;
  if (!schedule || !(schedule = schedule[id2])) throw new Error("transition not found");
  return schedule;
}
function create(node, id2, self2) {
  var schedules = node.__transition, tween;
  schedules[id2] = self2;
  self2.timer = timer(schedule, 0, self2.time);
  function schedule(elapsed) {
    self2.state = SCHEDULED;
    self2.timer.restart(start2, self2.delay, self2.time);
    if (self2.delay <= elapsed) start2(elapsed - self2.delay);
  }
  function start2(elapsed) {
    var i2, j3, n2, o2;
    if (self2.state !== SCHEDULED) return stop();
    for (i2 in schedules) {
      o2 = schedules[i2];
      if (o2.name !== self2.name) continue;
      if (o2.state === STARTED) return timeout_default(start2);
      if (o2.state === RUNNING) {
        o2.state = ENDED;
        o2.timer.stop();
        o2.on.call("interrupt", node, node.__data__, o2.index, o2.group);
        delete schedules[i2];
      } else if (+i2 < id2) {
        o2.state = ENDED;
        o2.timer.stop();
        o2.on.call("cancel", node, node.__data__, o2.index, o2.group);
        delete schedules[i2];
      }
    }
    timeout_default(function() {
      if (self2.state === STARTED) {
        self2.state = RUNNING;
        self2.timer.restart(tick, self2.delay, self2.time);
        tick(elapsed);
      }
    });
    self2.state = STARTING;
    self2.on.call("start", node, node.__data__, self2.index, self2.group);
    if (self2.state !== STARTING) return;
    self2.state = STARTED;
    tween = new Array(n2 = self2.tween.length);
    for (i2 = 0, j3 = -1; i2 < n2; ++i2) {
      if (o2 = self2.tween[i2].value.call(node, node.__data__, self2.index, self2.group)) {
        tween[++j3] = o2;
      }
    }
    tween.length = j3 + 1;
  }
  function tick(elapsed) {
    var t = elapsed < self2.duration ? self2.ease.call(null, elapsed / self2.duration) : (self2.timer.restart(stop), self2.state = ENDING, 1), i2 = -1, n2 = tween.length;
    while (++i2 < n2) {
      tween[i2].call(node, t);
    }
    if (self2.state === ENDING) {
      self2.on.call("end", node, node.__data__, self2.index, self2.group);
      stop();
    }
  }
  function stop() {
    self2.state = ENDED;
    self2.timer.stop();
    delete schedules[id2];
    for (var i2 in schedules) return;
    delete node.__transition;
  }
}

// node_modules/d3-transition/src/interrupt.js
function interrupt_default(node, name2) {
  var schedules = node.__transition, schedule, active, empty2 = true, i2;
  if (!schedules) return;
  name2 = name2 == null ? null : name2 + "";
  for (i2 in schedules) {
    if ((schedule = schedules[i2]).name !== name2) {
      empty2 = false;
      continue;
    }
    active = schedule.state > STARTING && schedule.state < ENDING;
    schedule.state = ENDED;
    schedule.timer.stop();
    schedule.on.call(active ? "interrupt" : "cancel", node, node.__data__, schedule.index, schedule.group);
    delete schedules[i2];
  }
  if (empty2) delete node.__transition;
}

// node_modules/d3-transition/src/selection/interrupt.js
function interrupt_default2(name2) {
  return this.each(function() {
    interrupt_default(this, name2);
  });
}

// node_modules/d3-transition/src/transition/tween.js
function tweenRemove(id2, name2) {
  var tween0, tween1;
  return function() {
    var schedule = set2(this, id2), tween = schedule.tween;
    if (tween !== tween0) {
      tween1 = tween0 = tween;
      for (var i2 = 0, n2 = tween1.length; i2 < n2; ++i2) {
        if (tween1[i2].name === name2) {
          tween1 = tween1.slice();
          tween1.splice(i2, 1);
          break;
        }
      }
    }
    schedule.tween = tween1;
  };
}
function tweenFunction(id2, name2, value) {
  var tween0, tween1;
  if (typeof value !== "function") throw new Error();
  return function() {
    var schedule = set2(this, id2), tween = schedule.tween;
    if (tween !== tween0) {
      tween1 = (tween0 = tween).slice();
      for (var t = { name: name2, value }, i2 = 0, n2 = tween1.length; i2 < n2; ++i2) {
        if (tween1[i2].name === name2) {
          tween1[i2] = t;
          break;
        }
      }
      if (i2 === n2) tween1.push(t);
    }
    schedule.tween = tween1;
  };
}
function tween_default(name2, value) {
  var id2 = this._id;
  name2 += "";
  if (arguments.length < 2) {
    var tween = get2(this.node(), id2).tween;
    for (var i2 = 0, n2 = tween.length, t; i2 < n2; ++i2) {
      if ((t = tween[i2]).name === name2) {
        return t.value;
      }
    }
    return null;
  }
  return this.each((value == null ? tweenRemove : tweenFunction)(id2, name2, value));
}
function tweenValue(transition2, name2, value) {
  var id2 = transition2._id;
  transition2.each(function() {
    var schedule = set2(this, id2);
    (schedule.value || (schedule.value = {}))[name2] = value.apply(this, arguments);
  });
  return function(node) {
    return get2(node, id2).value[name2];
  };
}

// node_modules/d3-transition/src/transition/interpolate.js
function interpolate_default(a, b) {
  var c;
  return (typeof b === "number" ? number_default : b instanceof color ? rgb_default : (c = color(b)) ? (b = c, rgb_default) : string_default)(a, b);
}

// node_modules/d3-transition/src/transition/attr.js
function attrRemove2(name2) {
  return function() {
    this.removeAttribute(name2);
  };
}
function attrRemoveNS2(fullname) {
  return function() {
    this.removeAttributeNS(fullname.space, fullname.local);
  };
}
function attrConstant2(name2, interpolate, value1) {
  var string00, string1 = value1 + "", interpolate0;
  return function() {
    var string0 = this.getAttribute(name2);
    return string0 === string1 ? null : string0 === string00 ? interpolate0 : interpolate0 = interpolate(string00 = string0, value1);
  };
}
function attrConstantNS2(fullname, interpolate, value1) {
  var string00, string1 = value1 + "", interpolate0;
  return function() {
    var string0 = this.getAttributeNS(fullname.space, fullname.local);
    return string0 === string1 ? null : string0 === string00 ? interpolate0 : interpolate0 = interpolate(string00 = string0, value1);
  };
}
function attrFunction2(name2, interpolate, value) {
  var string00, string10, interpolate0;
  return function() {
    var string0, value1 = value(this), string1;
    if (value1 == null) return void this.removeAttribute(name2);
    string0 = this.getAttribute(name2);
    string1 = value1 + "";
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : (string10 = string1, interpolate0 = interpolate(string00 = string0, value1));
  };
}
function attrFunctionNS2(fullname, interpolate, value) {
  var string00, string10, interpolate0;
  return function() {
    var string0, value1 = value(this), string1;
    if (value1 == null) return void this.removeAttributeNS(fullname.space, fullname.local);
    string0 = this.getAttributeNS(fullname.space, fullname.local);
    string1 = value1 + "";
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : (string10 = string1, interpolate0 = interpolate(string00 = string0, value1));
  };
}
function attr_default2(name2, value) {
  var fullname = namespace_default(name2), i2 = fullname === "transform" ? interpolateTransformSvg : interpolate_default;
  return this.attrTween(name2, typeof value === "function" ? (fullname.local ? attrFunctionNS2 : attrFunction2)(fullname, i2, tweenValue(this, "attr." + name2, value)) : value == null ? (fullname.local ? attrRemoveNS2 : attrRemove2)(fullname) : (fullname.local ? attrConstantNS2 : attrConstant2)(fullname, i2, value));
}

// node_modules/d3-transition/src/transition/attrTween.js
function attrInterpolate(name2, i2) {
  return function(t) {
    this.setAttribute(name2, i2.call(this, t));
  };
}
function attrInterpolateNS(fullname, i2) {
  return function(t) {
    this.setAttributeNS(fullname.space, fullname.local, i2.call(this, t));
  };
}
function attrTweenNS(fullname, value) {
  var t0, i0;
  function tween() {
    var i2 = value.apply(this, arguments);
    if (i2 !== i0) t0 = (i0 = i2) && attrInterpolateNS(fullname, i2);
    return t0;
  }
  tween._value = value;
  return tween;
}
function attrTween(name2, value) {
  var t0, i0;
  function tween() {
    var i2 = value.apply(this, arguments);
    if (i2 !== i0) t0 = (i0 = i2) && attrInterpolate(name2, i2);
    return t0;
  }
  tween._value = value;
  return tween;
}
function attrTween_default(name2, value) {
  var key = "attr." + name2;
  if (arguments.length < 2) return (key = this.tween(key)) && key._value;
  if (value == null) return this.tween(key, null);
  if (typeof value !== "function") throw new Error();
  var fullname = namespace_default(name2);
  return this.tween(key, (fullname.local ? attrTweenNS : attrTween)(fullname, value));
}

// node_modules/d3-transition/src/transition/delay.js
function delayFunction(id2, value) {
  return function() {
    init(this, id2).delay = +value.apply(this, arguments);
  };
}
function delayConstant(id2, value) {
  return value = +value, function() {
    init(this, id2).delay = value;
  };
}
function delay_default(value) {
  var id2 = this._id;
  return arguments.length ? this.each((typeof value === "function" ? delayFunction : delayConstant)(id2, value)) : get2(this.node(), id2).delay;
}

// node_modules/d3-transition/src/transition/duration.js
function durationFunction(id2, value) {
  return function() {
    set2(this, id2).duration = +value.apply(this, arguments);
  };
}
function durationConstant(id2, value) {
  return value = +value, function() {
    set2(this, id2).duration = value;
  };
}
function duration_default(value) {
  var id2 = this._id;
  return arguments.length ? this.each((typeof value === "function" ? durationFunction : durationConstant)(id2, value)) : get2(this.node(), id2).duration;
}

// node_modules/d3-transition/src/transition/ease.js
function easeConstant(id2, value) {
  if (typeof value !== "function") throw new Error();
  return function() {
    set2(this, id2).ease = value;
  };
}
function ease_default(value) {
  var id2 = this._id;
  return arguments.length ? this.each(easeConstant(id2, value)) : get2(this.node(), id2).ease;
}

// node_modules/d3-transition/src/transition/easeVarying.js
function easeVarying(id2, value) {
  return function() {
    var v2 = value.apply(this, arguments);
    if (typeof v2 !== "function") throw new Error();
    set2(this, id2).ease = v2;
  };
}
function easeVarying_default(value) {
  if (typeof value !== "function") throw new Error();
  return this.each(easeVarying(this._id, value));
}

// node_modules/d3-transition/src/transition/filter.js
function filter_default2(match) {
  if (typeof match !== "function") match = matcher_default(match);
  for (var groups = this._groups, m2 = groups.length, subgroups = new Array(m2), j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, subgroup = subgroups[j3] = [], node, i2 = 0; i2 < n2; ++i2) {
      if ((node = group[i2]) && match.call(node, node.__data__, i2, group)) {
        subgroup.push(node);
      }
    }
  }
  return new Transition(subgroups, this._parents, this._name, this._id);
}

// node_modules/d3-transition/src/transition/merge.js
function merge_default2(transition2) {
  if (transition2._id !== this._id) throw new Error();
  for (var groups0 = this._groups, groups1 = transition2._groups, m0 = groups0.length, m1 = groups1.length, m2 = Math.min(m0, m1), merges = new Array(m0), j3 = 0; j3 < m2; ++j3) {
    for (var group0 = groups0[j3], group1 = groups1[j3], n2 = group0.length, merge = merges[j3] = new Array(n2), node, i2 = 0; i2 < n2; ++i2) {
      if (node = group0[i2] || group1[i2]) {
        merge[i2] = node;
      }
    }
  }
  for (; j3 < m0; ++j3) {
    merges[j3] = groups0[j3];
  }
  return new Transition(merges, this._parents, this._name, this._id);
}

// node_modules/d3-transition/src/transition/on.js
function start(name2) {
  return (name2 + "").trim().split(/^|\s+/).every(function(t) {
    var i2 = t.indexOf(".");
    if (i2 >= 0) t = t.slice(0, i2);
    return !t || t === "start";
  });
}
function onFunction(id2, name2, listener) {
  var on0, on1, sit = start(name2) ? init : set2;
  return function() {
    var schedule = sit(this, id2), on = schedule.on;
    if (on !== on0) (on1 = (on0 = on).copy()).on(name2, listener);
    schedule.on = on1;
  };
}
function on_default2(name2, listener) {
  var id2 = this._id;
  return arguments.length < 2 ? get2(this.node(), id2).on.on(name2) : this.each(onFunction(id2, name2, listener));
}

// node_modules/d3-transition/src/transition/remove.js
function removeFunction(id2) {
  return function() {
    var parent = this.parentNode;
    for (var i2 in this.__transition) if (+i2 !== id2) return;
    if (parent) parent.removeChild(this);
  };
}
function remove_default2() {
  return this.on("end.remove", removeFunction(this._id));
}

// node_modules/d3-transition/src/transition/select.js
function select_default3(select) {
  var name2 = this._name, id2 = this._id;
  if (typeof select !== "function") select = selector_default(select);
  for (var groups = this._groups, m2 = groups.length, subgroups = new Array(m2), j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, subgroup = subgroups[j3] = new Array(n2), node, subnode, i2 = 0; i2 < n2; ++i2) {
      if ((node = group[i2]) && (subnode = select.call(node, node.__data__, i2, group))) {
        if ("__data__" in node) subnode.__data__ = node.__data__;
        subgroup[i2] = subnode;
        schedule_default(subgroup[i2], name2, id2, i2, subgroup, get2(node, id2));
      }
    }
  }
  return new Transition(subgroups, this._parents, name2, id2);
}

// node_modules/d3-transition/src/transition/selectAll.js
function selectAll_default2(select) {
  var name2 = this._name, id2 = this._id;
  if (typeof select !== "function") select = selectorAll_default(select);
  for (var groups = this._groups, m2 = groups.length, subgroups = [], parents = [], j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, node, i2 = 0; i2 < n2; ++i2) {
      if (node = group[i2]) {
        for (var children2 = select.call(node, node.__data__, i2, group), child, inherit2 = get2(node, id2), k2 = 0, l2 = children2.length; k2 < l2; ++k2) {
          if (child = children2[k2]) {
            schedule_default(child, name2, id2, k2, children2, inherit2);
          }
        }
        subgroups.push(children2);
        parents.push(node);
      }
    }
  }
  return new Transition(subgroups, parents, name2, id2);
}

// node_modules/d3-transition/src/transition/selection.js
var Selection2 = selection_default.prototype.constructor;
function selection_default2() {
  return new Selection2(this._groups, this._parents);
}

// node_modules/d3-transition/src/transition/style.js
function styleNull(name2, interpolate) {
  var string00, string10, interpolate0;
  return function() {
    var string0 = styleValue(this, name2), string1 = (this.style.removeProperty(name2), styleValue(this, name2));
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : interpolate0 = interpolate(string00 = string0, string10 = string1);
  };
}
function styleRemove2(name2) {
  return function() {
    this.style.removeProperty(name2);
  };
}
function styleConstant2(name2, interpolate, value1) {
  var string00, string1 = value1 + "", interpolate0;
  return function() {
    var string0 = styleValue(this, name2);
    return string0 === string1 ? null : string0 === string00 ? interpolate0 : interpolate0 = interpolate(string00 = string0, value1);
  };
}
function styleFunction2(name2, interpolate, value) {
  var string00, string10, interpolate0;
  return function() {
    var string0 = styleValue(this, name2), value1 = value(this), string1 = value1 + "";
    if (value1 == null) string1 = value1 = (this.style.removeProperty(name2), styleValue(this, name2));
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : (string10 = string1, interpolate0 = interpolate(string00 = string0, value1));
  };
}
function styleMaybeRemove(id2, name2) {
  var on0, on1, listener0, key = "style." + name2, event = "end." + key, remove2;
  return function() {
    var schedule = set2(this, id2), on = schedule.on, listener = schedule.value[key] == null ? remove2 || (remove2 = styleRemove2(name2)) : void 0;
    if (on !== on0 || listener0 !== listener) (on1 = (on0 = on).copy()).on(event, listener0 = listener);
    schedule.on = on1;
  };
}
function style_default2(name2, value, priority) {
  var i2 = (name2 += "") === "transform" ? interpolateTransformCss : interpolate_default;
  return value == null ? this.styleTween(name2, styleNull(name2, i2)).on("end.style." + name2, styleRemove2(name2)) : typeof value === "function" ? this.styleTween(name2, styleFunction2(name2, i2, tweenValue(this, "style." + name2, value))).each(styleMaybeRemove(this._id, name2)) : this.styleTween(name2, styleConstant2(name2, i2, value), priority).on("end.style." + name2, null);
}

// node_modules/d3-transition/src/transition/styleTween.js
function styleInterpolate(name2, i2, priority) {
  return function(t) {
    this.style.setProperty(name2, i2.call(this, t), priority);
  };
}
function styleTween(name2, value, priority) {
  var t, i0;
  function tween() {
    var i2 = value.apply(this, arguments);
    if (i2 !== i0) t = (i0 = i2) && styleInterpolate(name2, i2, priority);
    return t;
  }
  tween._value = value;
  return tween;
}
function styleTween_default(name2, value, priority) {
  var key = "style." + (name2 += "");
  if (arguments.length < 2) return (key = this.tween(key)) && key._value;
  if (value == null) return this.tween(key, null);
  if (typeof value !== "function") throw new Error();
  return this.tween(key, styleTween(name2, value, priority == null ? "" : priority));
}

// node_modules/d3-transition/src/transition/text.js
function textConstant2(value) {
  return function() {
    this.textContent = value;
  };
}
function textFunction2(value) {
  return function() {
    var value1 = value(this);
    this.textContent = value1 == null ? "" : value1;
  };
}
function text_default2(value) {
  return this.tween("text", typeof value === "function" ? textFunction2(tweenValue(this, "text", value)) : textConstant2(value == null ? "" : value + ""));
}

// node_modules/d3-transition/src/transition/textTween.js
function textInterpolate(i2) {
  return function(t) {
    this.textContent = i2.call(this, t);
  };
}
function textTween(value) {
  var t0, i0;
  function tween() {
    var i2 = value.apply(this, arguments);
    if (i2 !== i0) t0 = (i0 = i2) && textInterpolate(i2);
    return t0;
  }
  tween._value = value;
  return tween;
}
function textTween_default(value) {
  var key = "text";
  if (arguments.length < 1) return (key = this.tween(key)) && key._value;
  if (value == null) return this.tween(key, null);
  if (typeof value !== "function") throw new Error();
  return this.tween(key, textTween(value));
}

// node_modules/d3-transition/src/transition/transition.js
function transition_default() {
  var name2 = this._name, id0 = this._id, id1 = newId();
  for (var groups = this._groups, m2 = groups.length, j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, node, i2 = 0; i2 < n2; ++i2) {
      if (node = group[i2]) {
        var inherit2 = get2(node, id0);
        schedule_default(node, name2, id1, i2, group, {
          time: inherit2.time + inherit2.delay + inherit2.duration,
          delay: 0,
          duration: inherit2.duration,
          ease: inherit2.ease
        });
      }
    }
  }
  return new Transition(groups, this._parents, name2, id1);
}

// node_modules/d3-transition/src/transition/end.js
function end_default() {
  var on0, on1, that = this, id2 = that._id, size = that.size();
  return new Promise(function(resolve, reject) {
    var cancel = { value: reject }, end = { value: function() {
      if (--size === 0) resolve();
    } };
    that.each(function() {
      var schedule = set2(this, id2), on = schedule.on;
      if (on !== on0) {
        on1 = (on0 = on).copy();
        on1._.cancel.push(cancel);
        on1._.interrupt.push(cancel);
        on1._.end.push(end);
      }
      schedule.on = on1;
    });
    if (size === 0) resolve();
  });
}

// node_modules/d3-transition/src/transition/index.js
var id = 0;
function Transition(groups, parents, name2, id2) {
  this._groups = groups;
  this._parents = parents;
  this._name = name2;
  this._id = id2;
}
function transition(name2) {
  return selection_default().transition(name2);
}
function newId() {
  return ++id;
}
var selection_prototype = selection_default.prototype;
Transition.prototype = transition.prototype = {
  constructor: Transition,
  select: select_default3,
  selectAll: selectAll_default2,
  selectChild: selection_prototype.selectChild,
  selectChildren: selection_prototype.selectChildren,
  filter: filter_default2,
  merge: merge_default2,
  selection: selection_default2,
  transition: transition_default,
  call: selection_prototype.call,
  nodes: selection_prototype.nodes,
  node: selection_prototype.node,
  size: selection_prototype.size,
  empty: selection_prototype.empty,
  each: selection_prototype.each,
  on: on_default2,
  attr: attr_default2,
  attrTween: attrTween_default,
  style: style_default2,
  styleTween: styleTween_default,
  text: text_default2,
  textTween: textTween_default,
  remove: remove_default2,
  tween: tween_default,
  delay: delay_default,
  duration: duration_default,
  ease: ease_default,
  easeVarying: easeVarying_default,
  end: end_default,
  [Symbol.iterator]: selection_prototype[Symbol.iterator]
};

// node_modules/d3-ease/src/cubic.js
function cubicInOut(t) {
  return ((t *= 2) <= 1 ? t * t * t : (t -= 2) * t * t + 2) / 2;
}

// node_modules/d3-transition/src/selection/transition.js
var defaultTiming = {
  time: null,
  // Set on use.
  delay: 0,
  duration: 250,
  ease: cubicInOut
};
function inherit(node, id2) {
  var timing;
  while (!(timing = node.__transition) || !(timing = timing[id2])) {
    if (!(node = node.parentNode)) {
      throw new Error(`transition ${id2} not found`);
    }
  }
  return timing;
}
function transition_default2(name2) {
  var id2, timing;
  if (name2 instanceof Transition) {
    id2 = name2._id, name2 = name2._name;
  } else {
    id2 = newId(), (timing = defaultTiming).time = now(), name2 = name2 == null ? null : name2 + "";
  }
  for (var groups = this._groups, m2 = groups.length, j3 = 0; j3 < m2; ++j3) {
    for (var group = groups[j3], n2 = group.length, node, i2 = 0; i2 < n2; ++i2) {
      if (node = group[i2]) {
        schedule_default(node, name2, id2, i2, group, timing || inherit(node, id2));
      }
    }
  }
  return new Transition(groups, this._parents, name2, id2);
}

// node_modules/d3-transition/src/selection/index.js
selection_default.prototype.interrupt = interrupt_default2;
selection_default.prototype.transition = transition_default2;

// node_modules/d3-zoom/src/constant.js
var constant_default4 = (x3) => () => x3;

// node_modules/d3-zoom/src/event.js
function ZoomEvent(type, {
  sourceEvent,
  target,
  transform: transform2,
  dispatch: dispatch2
}) {
  Object.defineProperties(this, {
    type: { value: type, enumerable: true, configurable: true },
    sourceEvent: { value: sourceEvent, enumerable: true, configurable: true },
    target: { value: target, enumerable: true, configurable: true },
    transform: { value: transform2, enumerable: true, configurable: true },
    _: { value: dispatch2 }
  });
}

// node_modules/d3-zoom/src/transform.js
function Transform(k2, x3, y2) {
  this.k = k2;
  this.x = x3;
  this.y = y2;
}
Transform.prototype = {
  constructor: Transform,
  scale: function(k2) {
    return k2 === 1 ? this : new Transform(this.k * k2, this.x, this.y);
  },
  translate: function(x3, y2) {
    return x3 === 0 & y2 === 0 ? this : new Transform(this.k, this.x + this.k * x3, this.y + this.k * y2);
  },
  apply: function(point) {
    return [point[0] * this.k + this.x, point[1] * this.k + this.y];
  },
  applyX: function(x3) {
    return x3 * this.k + this.x;
  },
  applyY: function(y2) {
    return y2 * this.k + this.y;
  },
  invert: function(location) {
    return [(location[0] - this.x) / this.k, (location[1] - this.y) / this.k];
  },
  invertX: function(x3) {
    return (x3 - this.x) / this.k;
  },
  invertY: function(y2) {
    return (y2 - this.y) / this.k;
  },
  rescaleX: function(x3) {
    return x3.copy().domain(x3.range().map(this.invertX, this).map(x3.invert, x3));
  },
  rescaleY: function(y2) {
    return y2.copy().domain(y2.range().map(this.invertY, this).map(y2.invert, y2));
  },
  toString: function() {
    return "translate(" + this.x + "," + this.y + ") scale(" + this.k + ")";
  }
};
var identity2 = new Transform(1, 0, 0);
transform.prototype = Transform.prototype;
function transform(node) {
  while (!node.__zoom) if (!(node = node.parentNode)) return identity2;
  return node.__zoom;
}

// node_modules/d3-zoom/src/noevent.js
function nopropagation2(event) {
  event.stopImmediatePropagation();
}
function noevent_default2(event) {
  event.preventDefault();
  event.stopImmediatePropagation();
}

// node_modules/d3-zoom/src/zoom.js
function defaultFilter2(event) {
  return (!event.ctrlKey || event.type === "wheel") && !event.button;
}
function defaultExtent() {
  var e = this;
  if (e instanceof SVGElement) {
    e = e.ownerSVGElement || e;
    if (e.hasAttribute("viewBox")) {
      e = e.viewBox.baseVal;
      return [[e.x, e.y], [e.x + e.width, e.y + e.height]];
    }
    return [[0, 0], [e.width.baseVal.value, e.height.baseVal.value]];
  }
  return [[0, 0], [e.clientWidth, e.clientHeight]];
}
function defaultTransform() {
  return this.__zoom || identity2;
}
function defaultWheelDelta(event) {
  return -event.deltaY * (event.deltaMode === 1 ? 0.05 : event.deltaMode ? 1 : 2e-3) * (event.ctrlKey ? 10 : 1);
}
function defaultTouchable2() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function defaultConstrain(transform2, extent, translateExtent) {
  var dx0 = transform2.invertX(extent[0][0]) - translateExtent[0][0], dx1 = transform2.invertX(extent[1][0]) - translateExtent[1][0], dy0 = transform2.invertY(extent[0][1]) - translateExtent[0][1], dy1 = transform2.invertY(extent[1][1]) - translateExtent[1][1];
  return transform2.translate(
    dx1 > dx0 ? (dx0 + dx1) / 2 : Math.min(0, dx0) || Math.max(0, dx1),
    dy1 > dy0 ? (dy0 + dy1) / 2 : Math.min(0, dy0) || Math.max(0, dy1)
  );
}
function zoom_default2() {
  var filter2 = defaultFilter2, extent = defaultExtent, constrain = defaultConstrain, wheelDelta2 = defaultWheelDelta, touchable = defaultTouchable2, scaleExtent = [0, Infinity], translateExtent = [[-Infinity, -Infinity], [Infinity, Infinity]], duration = 250, interpolate = zoom_default, listeners2 = dispatch_default("start", "zoom", "end"), touchstarting, touchfirst, touchending, touchDelay = 500, wheelDelay = 150, clickDistance2 = 0, tapDistance = 10;
  function zoom(selection2) {
    selection2.property("__zoom", defaultTransform).on("wheel.zoom", wheeled, { passive: false }).on("mousedown.zoom", mousedowned).on("dblclick.zoom", dblclicked).filter(touchable).on("touchstart.zoom", touchstarted).on("touchmove.zoom", touchmoved).on("touchend.zoom touchcancel.zoom", touchended).style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  zoom.transform = function(collection, transform2, point, event) {
    var selection2 = collection.selection ? collection.selection() : collection;
    selection2.property("__zoom", defaultTransform);
    if (collection !== selection2) {
      schedule(collection, transform2, point, event);
    } else {
      selection2.interrupt().each(function() {
        gesture(this, arguments).event(event).start().zoom(null, typeof transform2 === "function" ? transform2.apply(this, arguments) : transform2).end();
      });
    }
  };
  zoom.scaleBy = function(selection2, k2, p2, event) {
    zoom.scaleTo(selection2, function() {
      var k0 = this.__zoom.k, k1 = typeof k2 === "function" ? k2.apply(this, arguments) : k2;
      return k0 * k1;
    }, p2, event);
  };
  zoom.scaleTo = function(selection2, k2, p2, event) {
    zoom.transform(selection2, function() {
      var e = extent.apply(this, arguments), t0 = this.__zoom, p0 = p2 == null ? centroid(e) : typeof p2 === "function" ? p2.apply(this, arguments) : p2, p1 = t0.invert(p0), k1 = typeof k2 === "function" ? k2.apply(this, arguments) : k2;
      return constrain(translate(scale(t0, k1), p0, p1), e, translateExtent);
    }, p2, event);
  };
  zoom.translateBy = function(selection2, x3, y2, event) {
    zoom.transform(selection2, function() {
      return constrain(this.__zoom.translate(
        typeof x3 === "function" ? x3.apply(this, arguments) : x3,
        typeof y2 === "function" ? y2.apply(this, arguments) : y2
      ), extent.apply(this, arguments), translateExtent);
    }, null, event);
  };
  zoom.translateTo = function(selection2, x3, y2, p2, event) {
    zoom.transform(selection2, function() {
      var e = extent.apply(this, arguments), t = this.__zoom, p0 = p2 == null ? centroid(e) : typeof p2 === "function" ? p2.apply(this, arguments) : p2;
      return constrain(identity2.translate(p0[0], p0[1]).scale(t.k).translate(
        typeof x3 === "function" ? -x3.apply(this, arguments) : -x3,
        typeof y2 === "function" ? -y2.apply(this, arguments) : -y2
      ), e, translateExtent);
    }, p2, event);
  };
  function scale(transform2, k2) {
    k2 = Math.max(scaleExtent[0], Math.min(scaleExtent[1], k2));
    return k2 === transform2.k ? transform2 : new Transform(k2, transform2.x, transform2.y);
  }
  function translate(transform2, p0, p1) {
    var x3 = p0[0] - p1[0] * transform2.k, y2 = p0[1] - p1[1] * transform2.k;
    return x3 === transform2.x && y2 === transform2.y ? transform2 : new Transform(transform2.k, x3, y2);
  }
  function centroid(extent2) {
    return [(+extent2[0][0] + +extent2[1][0]) / 2, (+extent2[0][1] + +extent2[1][1]) / 2];
  }
  function schedule(transition2, transform2, point, event) {
    transition2.on("start.zoom", function() {
      gesture(this, arguments).event(event).start();
    }).on("interrupt.zoom end.zoom", function() {
      gesture(this, arguments).event(event).end();
    }).tween("zoom", function() {
      var that = this, args = arguments, g2 = gesture(that, args).event(event), e = extent.apply(that, args), p2 = point == null ? centroid(e) : typeof point === "function" ? point.apply(that, args) : point, w = Math.max(e[1][0] - e[0][0], e[1][1] - e[0][1]), a = that.__zoom, b = typeof transform2 === "function" ? transform2.apply(that, args) : transform2, i2 = interpolate(a.invert(p2).concat(w / a.k), b.invert(p2).concat(w / b.k));
      return function(t) {
        if (t === 1) t = b;
        else {
          var l2 = i2(t), k2 = w / l2[2];
          t = new Transform(k2, p2[0] - l2[0] * k2, p2[1] - l2[1] * k2);
        }
        g2.zoom(null, t);
      };
    });
  }
  function gesture(that, args, clean) {
    return !clean && that.__zooming || new Gesture(that, args);
  }
  function Gesture(that, args) {
    this.that = that;
    this.args = args;
    this.active = 0;
    this.sourceEvent = null;
    this.extent = extent.apply(that, args);
    this.taps = 0;
  }
  Gesture.prototype = {
    event: function(event) {
      if (event) this.sourceEvent = event;
      return this;
    },
    start: function() {
      if (++this.active === 1) {
        this.that.__zooming = this;
        this.emit("start");
      }
      return this;
    },
    zoom: function(key, transform2) {
      if (this.mouse && key !== "mouse") this.mouse[1] = transform2.invert(this.mouse[0]);
      if (this.touch0 && key !== "touch") this.touch0[1] = transform2.invert(this.touch0[0]);
      if (this.touch1 && key !== "touch") this.touch1[1] = transform2.invert(this.touch1[0]);
      this.that.__zoom = transform2;
      this.emit("zoom");
      return this;
    },
    end: function() {
      if (--this.active === 0) {
        delete this.that.__zooming;
        this.emit("end");
      }
      return this;
    },
    emit: function(type) {
      var d2 = select_default2(this.that).datum();
      listeners2.call(
        type,
        this.that,
        new ZoomEvent(type, {
          sourceEvent: this.sourceEvent,
          target: zoom,
          type,
          transform: this.that.__zoom,
          dispatch: listeners2
        }),
        d2
      );
    }
  };
  function wheeled(event, ...args) {
    if (!filter2.apply(this, arguments)) return;
    var g2 = gesture(this, args).event(event), t = this.__zoom, k2 = Math.max(scaleExtent[0], Math.min(scaleExtent[1], t.k * Math.pow(2, wheelDelta2.apply(this, arguments)))), p2 = pointer_default(event);
    if (g2.wheel) {
      if (g2.mouse[0][0] !== p2[0] || g2.mouse[0][1] !== p2[1]) {
        g2.mouse[1] = t.invert(g2.mouse[0] = p2);
      }
      clearTimeout(g2.wheel);
    } else if (t.k === k2) return;
    else {
      g2.mouse = [p2, t.invert(p2)];
      interrupt_default(this);
      g2.start();
    }
    noevent_default2(event);
    g2.wheel = setTimeout(wheelidled, wheelDelay);
    g2.zoom("mouse", constrain(translate(scale(t, k2), g2.mouse[0], g2.mouse[1]), g2.extent, translateExtent));
    function wheelidled() {
      g2.wheel = null;
      g2.end();
    }
  }
  function mousedowned(event, ...args) {
    if (touchending || !filter2.apply(this, arguments)) return;
    var currentTarget = event.currentTarget, g2 = gesture(this, args, true).event(event), v2 = select_default2(event.view).on("mousemove.zoom", mousemoved, true).on("mouseup.zoom", mouseupped, true), p2 = pointer_default(event, currentTarget), x0 = event.clientX, y0 = event.clientY;
    nodrag_default(event.view);
    nopropagation2(event);
    g2.mouse = [p2, this.__zoom.invert(p2)];
    interrupt_default(this);
    g2.start();
    function mousemoved(event2) {
      noevent_default2(event2);
      if (!g2.moved) {
        var dx2 = event2.clientX - x0, dy = event2.clientY - y0;
        g2.moved = dx2 * dx2 + dy * dy > clickDistance2;
      }
      g2.event(event2).zoom("mouse", constrain(translate(g2.that.__zoom, g2.mouse[0] = pointer_default(event2, currentTarget), g2.mouse[1]), g2.extent, translateExtent));
    }
    function mouseupped(event2) {
      v2.on("mousemove.zoom mouseup.zoom", null);
      yesdrag(event2.view, g2.moved);
      noevent_default2(event2);
      g2.event(event2).end();
    }
  }
  function dblclicked(event, ...args) {
    if (!filter2.apply(this, arguments)) return;
    var t0 = this.__zoom, p0 = pointer_default(event.changedTouches ? event.changedTouches[0] : event, this), p1 = t0.invert(p0), k1 = t0.k * (event.shiftKey ? 0.5 : 2), t1 = constrain(translate(scale(t0, k1), p0, p1), extent.apply(this, args), translateExtent);
    noevent_default2(event);
    if (duration > 0) select_default2(this).transition().duration(duration).call(schedule, t1, p0, event);
    else select_default2(this).call(zoom.transform, t1, p0, event);
  }
  function touchstarted(event, ...args) {
    if (!filter2.apply(this, arguments)) return;
    var touches = event.touches, n2 = touches.length, g2 = gesture(this, args, event.changedTouches.length === n2).event(event), started, i2, t, p2;
    nopropagation2(event);
    for (i2 = 0; i2 < n2; ++i2) {
      t = touches[i2], p2 = pointer_default(t, this);
      p2 = [p2, this.__zoom.invert(p2), t.identifier];
      if (!g2.touch0) g2.touch0 = p2, started = true, g2.taps = 1 + !!touchstarting;
      else if (!g2.touch1 && g2.touch0[2] !== p2[2]) g2.touch1 = p2, g2.taps = 0;
    }
    if (touchstarting) touchstarting = clearTimeout(touchstarting);
    if (started) {
      if (g2.taps < 2) touchfirst = p2[0], touchstarting = setTimeout(function() {
        touchstarting = null;
      }, touchDelay);
      interrupt_default(this);
      g2.start();
    }
  }
  function touchmoved(event, ...args) {
    if (!this.__zooming) return;
    var g2 = gesture(this, args).event(event), touches = event.changedTouches, n2 = touches.length, i2, t, p2, l2;
    noevent_default2(event);
    for (i2 = 0; i2 < n2; ++i2) {
      t = touches[i2], p2 = pointer_default(t, this);
      if (g2.touch0 && g2.touch0[2] === t.identifier) g2.touch0[0] = p2;
      else if (g2.touch1 && g2.touch1[2] === t.identifier) g2.touch1[0] = p2;
    }
    t = g2.that.__zoom;
    if (g2.touch1) {
      var p0 = g2.touch0[0], l0 = g2.touch0[1], p1 = g2.touch1[0], l1 = g2.touch1[1], dp = (dp = p1[0] - p0[0]) * dp + (dp = p1[1] - p0[1]) * dp, dl = (dl = l1[0] - l0[0]) * dl + (dl = l1[1] - l0[1]) * dl;
      t = scale(t, Math.sqrt(dp / dl));
      p2 = [(p0[0] + p1[0]) / 2, (p0[1] + p1[1]) / 2];
      l2 = [(l0[0] + l1[0]) / 2, (l0[1] + l1[1]) / 2];
    } else if (g2.touch0) p2 = g2.touch0[0], l2 = g2.touch0[1];
    else return;
    g2.zoom("touch", constrain(translate(t, p2, l2), g2.extent, translateExtent));
  }
  function touchended(event, ...args) {
    if (!this.__zooming) return;
    var g2 = gesture(this, args).event(event), touches = event.changedTouches, n2 = touches.length, i2, t;
    nopropagation2(event);
    if (touchending) clearTimeout(touchending);
    touchending = setTimeout(function() {
      touchending = null;
    }, touchDelay);
    for (i2 = 0; i2 < n2; ++i2) {
      t = touches[i2];
      if (g2.touch0 && g2.touch0[2] === t.identifier) delete g2.touch0;
      else if (g2.touch1 && g2.touch1[2] === t.identifier) delete g2.touch1;
    }
    if (g2.touch1 && !g2.touch0) g2.touch0 = g2.touch1, delete g2.touch1;
    if (g2.touch0) g2.touch0[1] = this.__zoom.invert(g2.touch0[0]);
    else {
      g2.end();
      if (g2.taps === 2) {
        t = pointer_default(t, this);
        if (Math.hypot(touchfirst[0] - t[0], touchfirst[1] - t[1]) < tapDistance) {
          var p2 = select_default2(this).on("dblclick.zoom");
          if (p2) p2.apply(this, arguments);
        }
      }
    }
  }
  zoom.wheelDelta = function(_3) {
    return arguments.length ? (wheelDelta2 = typeof _3 === "function" ? _3 : constant_default4(+_3), zoom) : wheelDelta2;
  };
  zoom.filter = function(_3) {
    return arguments.length ? (filter2 = typeof _3 === "function" ? _3 : constant_default4(!!_3), zoom) : filter2;
  };
  zoom.touchable = function(_3) {
    return arguments.length ? (touchable = typeof _3 === "function" ? _3 : constant_default4(!!_3), zoom) : touchable;
  };
  zoom.extent = function(_3) {
    return arguments.length ? (extent = typeof _3 === "function" ? _3 : constant_default4([[+_3[0][0], +_3[0][1]], [+_3[1][0], +_3[1][1]]]), zoom) : extent;
  };
  zoom.scaleExtent = function(_3) {
    return arguments.length ? (scaleExtent[0] = +_3[0], scaleExtent[1] = +_3[1], zoom) : [scaleExtent[0], scaleExtent[1]];
  };
  zoom.translateExtent = function(_3) {
    return arguments.length ? (translateExtent[0][0] = +_3[0][0], translateExtent[1][0] = +_3[1][0], translateExtent[0][1] = +_3[0][1], translateExtent[1][1] = +_3[1][1], zoom) : [[translateExtent[0][0], translateExtent[0][1]], [translateExtent[1][0], translateExtent[1][1]]];
  };
  zoom.constrain = function(_3) {
    return arguments.length ? (constrain = _3, zoom) : constrain;
  };
  zoom.duration = function(_3) {
    return arguments.length ? (duration = +_3, zoom) : duration;
  };
  zoom.interpolate = function(_3) {
    return arguments.length ? (interpolate = _3, zoom) : interpolate;
  };
  zoom.on = function() {
    var value = listeners2.on.apply(listeners2, arguments);
    return value === listeners2 ? zoom : value;
  };
  zoom.clickDistance = function(_3) {
    return arguments.length ? (clickDistance2 = (_3 = +_3) * _3, zoom) : Math.sqrt(clickDistance2);
  };
  zoom.tapDistance = function(_3) {
    return arguments.length ? (tapDistance = +_3, zoom) : tapDistance;
  };
  return zoom;
}

// node_modules/@xyflow/system/dist/esm/index.js
var errorMessages = {
  error001: (lib = "react") => `Seems like you have not used ${lib === "svelte" ? "SvelteFlowProvider" : "ReactFlowProvider"} as an ancestor. Help: https://${lib}flow.dev/error#001`,
  error002: () => "It looks like you've created a new nodeTypes or edgeTypes object. If this wasn't on purpose please define the nodeTypes/edgeTypes outside of the component or memoize them.",
  error003: (nodeType) => `Node type "${nodeType}" not found. Using fallback type "default".`,
  error004: () => "The parent container needs a width and a height to render the graph.",
  error005: () => "Only child nodes can use a parent extent.",
  error006: () => "Can't create edge. An edge needs a source and a target.",
  error007: (id2) => `The old edge with id=${id2} does not exist.`,
  error009: (type) => `Marker type "${type}" doesn't exist.`,
  error008: (handleType, { id: id2, sourceHandle, targetHandle }) => `Couldn't create edge for ${handleType} handle id: "${handleType === "source" ? sourceHandle : targetHandle}", edge id: ${id2}.`,
  error010: () => "Handle: No node id found. Make sure to only use a Handle inside a custom Node.",
  error011: (edgeType) => `Edge type "${edgeType}" not found. Using fallback type "default".`,
  error012: (id2) => `Node with id "${id2}" does not exist, it may have been removed. This can happen when a node is deleted before the "onNodeClick" handler is called.`,
  error013: (lib = "react") => `It seems that you haven't loaded the styles. Please import '@xyflow/${lib}/dist/style.css' or base.css to make sure everything is working properly.`,
  error014: () => "useNodeConnections: No node ID found. Call useNodeConnections inside a custom Node or provide a node ID.",
  error015: () => "It seems that you are trying to drag a node that is not initialized. Please use onNodesChange as explained in the docs.",
  error016: (id2) => `Edge with id "${id2}" does not exist, it may have been removed. This can happen when an edge is deleted before the "onEdgeClick" handler is called.`
};
var infiniteExtent = [
  [Number.NEGATIVE_INFINITY, Number.NEGATIVE_INFINITY],
  [Number.POSITIVE_INFINITY, Number.POSITIVE_INFINITY]
];
var elementSelectionKeys = ["Enter", " ", "Escape"];
var defaultAriaLabelConfig = {
  "node.a11yDescription.default": "Press enter or space to select a node. Press delete to remove it and escape to cancel.",
  "node.a11yDescription.keyboardDisabled": "Press enter or space to select a node. You can then use the arrow keys to move the node around. Press delete to remove it and escape to cancel.",
  "node.a11yDescription.ariaLiveMessage": ({ direction, x: x3, y: y2 }) => `Moved selected node ${direction}. New position, x: ${x3}, y: ${y2}`,
  "edge.a11yDescription.default": "Press enter or space to select an edge. You can then press delete to remove it or escape to cancel.",
  // Control elements
  "controls.ariaLabel": "Control Panel",
  "controls.zoomIn.ariaLabel": "Zoom In",
  "controls.zoomOut.ariaLabel": "Zoom Out",
  "controls.fitView.ariaLabel": "Fit View",
  "controls.interactive.ariaLabel": "Toggle Interactivity",
  // Mini map
  "minimap.ariaLabel": "Mini Map",
  // Handle
  "handle.ariaLabel": "Handle"
};
var ConnectionMode;
(function(ConnectionMode2) {
  ConnectionMode2["Strict"] = "strict";
  ConnectionMode2["Loose"] = "loose";
})(ConnectionMode || (ConnectionMode = {}));
var PanOnScrollMode;
(function(PanOnScrollMode2) {
  PanOnScrollMode2["Free"] = "free";
  PanOnScrollMode2["Vertical"] = "vertical";
  PanOnScrollMode2["Horizontal"] = "horizontal";
})(PanOnScrollMode || (PanOnScrollMode = {}));
var SelectionMode;
(function(SelectionMode2) {
  SelectionMode2["Partial"] = "partial";
  SelectionMode2["Full"] = "full";
})(SelectionMode || (SelectionMode = {}));
var initialConnection = {
  inProgress: false,
  isValid: null,
  from: null,
  fromHandle: null,
  fromPosition: null,
  fromNode: null,
  to: null,
  toHandle: null,
  toPosition: null,
  toNode: null,
  pointer: null
};
var ConnectionLineType;
(function(ConnectionLineType2) {
  ConnectionLineType2["Bezier"] = "default";
  ConnectionLineType2["Straight"] = "straight";
  ConnectionLineType2["Step"] = "step";
  ConnectionLineType2["SmoothStep"] = "smoothstep";
  ConnectionLineType2["SimpleBezier"] = "simplebezier";
})(ConnectionLineType || (ConnectionLineType = {}));
var MarkerType;
(function(MarkerType2) {
  MarkerType2["Arrow"] = "arrow";
  MarkerType2["ArrowClosed"] = "arrowclosed";
})(MarkerType || (MarkerType = {}));
var Position;
(function(Position2) {
  Position2["Left"] = "left";
  Position2["Top"] = "top";
  Position2["Right"] = "right";
  Position2["Bottom"] = "bottom";
})(Position || (Position = {}));
var oppositePosition = {
  [Position.Left]: Position.Right,
  [Position.Right]: Position.Left,
  [Position.Top]: Position.Bottom,
  [Position.Bottom]: Position.Top
};
function getConnectionStatus(isValid) {
  return isValid === null ? null : isValid ? "valid" : "invalid";
}
var isEdgeBase = (element) => !!element && typeof element === "object" && "id" in element && "source" in element && "target" in element;
var isNodeBase = (element) => !!element && typeof element === "object" && "id" in element && "position" in element && !("source" in element) && !("target" in element);
var isInternalNodeBase = (element) => !!element && typeof element === "object" && "id" in element && "internals" in element && !("source" in element) && !("target" in element);
var getNodePositionWithOrigin = (node, nodeOrigin = [0, 0]) => {
  const { width, height } = getNodeDimensions(node);
  const origin = node.origin ?? nodeOrigin;
  const offsetX = width * origin[0];
  const offsetY = height * origin[1];
  return {
    x: node.position.x - offsetX,
    y: node.position.y - offsetY
  };
};
var getNodesBounds = (nodes, params = { nodeOrigin: [0, 0] }) => {
  if (!params.nodeLookup) {
    console.warn("Please use `getNodesBounds` from `useReactFlow`/`useSvelteFlow` hook to ensure correct values for sub flows. If not possible, you have to provide a nodeLookup to support sub flows.");
  }
  if (nodes.length === 0) {
    return { x: 0, y: 0, width: 0, height: 0 };
  }
  let hasNode = false;
  const box2 = nodes.reduce((currBox, nodeOrId) => {
    const isId = typeof nodeOrId === "string";
    let currentNode = !params.nodeLookup && !isId ? nodeOrId : void 0;
    if (params.nodeLookup) {
      currentNode = isId ? params.nodeLookup.get(nodeOrId) : !isInternalNodeBase(nodeOrId) ? params.nodeLookup.get(nodeOrId.id) : nodeOrId;
    }
    if (!currentNode) {
      return currBox;
    }
    hasNode = true;
    return getBoundsOfBoxes(currBox, nodeToBox(currentNode, params.nodeOrigin));
  }, { x: Infinity, y: Infinity, x2: -Infinity, y2: -Infinity });
  return hasNode ? boxToRect(box2) : { x: 0, y: 0, width: 0, height: 0 };
};
var getInternalNodesBounds = (nodeLookup, params = {}) => {
  let box2 = { x: Infinity, y: Infinity, x2: -Infinity, y2: -Infinity };
  let hasVisibleNodes = false;
  nodeLookup.forEach((node) => {
    if (params.filter === void 0 || params.filter(node)) {
      box2 = getBoundsOfBoxes(box2, nodeToBox(node));
      hasVisibleNodes = true;
    }
  });
  return hasVisibleNodes ? boxToRect(box2) : { x: 0, y: 0, width: 0, height: 0 };
};
var getNodesInside = (nodes, rect, [tx, ty, tScale] = [0, 0, 1], partially = false, excludeNonSelectableNodes = false) => {
  const paneX = (rect.x - tx) / tScale;
  const paneY = (rect.y - ty) / tScale;
  const paneWidth = rect.width / tScale;
  const paneHeight = rect.height / tScale;
  const visibleNodes = [];
  for (const node of nodes.values()) {
    const { measured, selectable = true, hidden = false } = node;
    if (excludeNonSelectableNodes && !selectable || hidden) {
      continue;
    }
    const width = measured.width ?? node.width ?? node.initialWidth ?? 0;
    const height = measured.height ?? node.height ?? node.initialHeight ?? 0;
    const { x: x3, y: y2 } = node.internals.positionAbsolute;
    const overlappingArea = getRectsOverlappingArea(paneX, paneY, paneWidth, paneHeight, x3, y2, width, height);
    const area = width * height;
    const partiallyVisible = partially && overlappingArea > 0;
    const forceInitialRender = !node.internals.handleBounds;
    const isVisible = forceInitialRender || partiallyVisible || overlappingArea >= area;
    if (isVisible || node.dragging) {
      visibleNodes.push(node);
    }
  }
  return visibleNodes;
};
var getConnectedEdges = (nodes, edges) => {
  const nodeIds = /* @__PURE__ */ new Set();
  nodes.forEach((node) => {
    nodeIds.add(node.id);
  });
  return edges.filter((edge) => nodeIds.has(edge.source) || nodeIds.has(edge.target));
};
function getFitViewNodes(nodeLookup, options) {
  const fitViewNodes = /* @__PURE__ */ new Map();
  const optionNodeIds = options?.nodes ? new Set(options.nodes.map((node) => node.id)) : null;
  nodeLookup.forEach((n2) => {
    let isVisible;
    if (options?.includeHiddenNodes) {
      const { width, height } = getNodeDimensions(n2);
      isVisible = width > 0 && height > 0;
    } else {
      isVisible = Boolean(n2.measured.width && n2.measured.height && !n2.hidden);
    }
    if (isVisible && (!optionNodeIds || optionNodeIds.has(n2.id))) {
      fitViewNodes.set(n2.id, n2);
    }
  });
  return fitViewNodes;
}
async function fitViewport({ nodes, width, height, panZoom, minZoom, maxZoom }, options) {
  if (nodes.size === 0) {
    return true;
  }
  const nodesToFit = getFitViewNodes(nodes, options);
  const bounds = getInternalNodesBounds(nodesToFit);
  const viewport = getViewportForBounds(bounds, width, height, options?.minZoom ?? minZoom, options?.maxZoom ?? maxZoom, options?.padding ?? 0.1);
  await panZoom.setViewport(viewport, {
    duration: options?.duration,
    ease: options?.ease,
    interpolate: options?.interpolate
  });
  return true;
}
function calculateNodePosition({ nodeId, nextPosition, nodeLookup, nodeOrigin = [0, 0], nodeExtent, onError }) {
  const node = nodeLookup.get(nodeId);
  const parentNode = node.parentId ? nodeLookup.get(node.parentId) : void 0;
  const { x: parentX, y: parentY } = parentNode ? parentNode.internals.positionAbsolute : { x: 0, y: 0 };
  const origin = node.origin ?? nodeOrigin;
  let extent = node.extent || nodeExtent;
  if (node.extent === "parent" && !node.expandParent) {
    if (!parentNode) {
      onError?.("005", errorMessages["error005"]());
    } else {
      const { width: parentWidth, height: parentHeight } = getNodeDimensions(parentNode);
      if (parentWidth && parentHeight) {
        extent = [
          [parentX, parentY],
          [parentX + parentWidth, parentY + parentHeight]
        ];
      }
    }
  } else if (parentNode && isCoordinateExtent(node.extent)) {
    extent = [
      [node.extent[0][0] + parentX, node.extent[0][1] + parentY],
      [node.extent[1][0] + parentX, node.extent[1][1] + parentY]
    ];
  }
  const positionAbsolute = isCoordinateExtent(extent) ? clampPosition(nextPosition, extent, node.measured) : nextPosition;
  if (node.measured.width === void 0 || node.measured.height === void 0) {
    onError?.("015", errorMessages["error015"]());
  }
  return {
    position: {
      x: positionAbsolute.x - parentX + (node.measured.width ?? 0) * origin[0],
      y: positionAbsolute.y - parentY + (node.measured.height ?? 0) * origin[1]
    },
    positionAbsolute
  };
}
async function getElementsToRemove({ nodesToRemove = [], edgesToRemove = [], nodes, edges, onBeforeDelete }) {
  const nodeIds = new Set(nodesToRemove.map((node) => node.id));
  const matchingNodes = [];
  for (const node of nodes) {
    if (node.deletable === false) {
      continue;
    }
    const isIncluded = nodeIds.has(node.id);
    const parentHit = !isIncluded && node.parentId && matchingNodes.find((n2) => n2.id === node.parentId);
    if (isIncluded || parentHit) {
      matchingNodes.push(node);
    }
  }
  const edgeIds = new Set(edgesToRemove.map((edge) => edge.id));
  const deletableEdges = edges.filter((edge) => edge.deletable !== false);
  const connectedEdges = getConnectedEdges(matchingNodes, deletableEdges);
  const matchingEdges = connectedEdges;
  for (const edge of deletableEdges) {
    const isIncluded = edgeIds.has(edge.id);
    if (isIncluded && !matchingEdges.find((e) => e.id === edge.id)) {
      matchingEdges.push(edge);
    }
  }
  if (!onBeforeDelete) {
    return {
      edges: matchingEdges,
      nodes: matchingNodes
    };
  }
  const onBeforeDeleteResult = await onBeforeDelete({
    nodes: matchingNodes,
    edges: matchingEdges
  });
  if (typeof onBeforeDeleteResult === "boolean") {
    return onBeforeDeleteResult ? { edges: matchingEdges, nodes: matchingNodes } : { edges: [], nodes: [] };
  }
  return onBeforeDeleteResult;
}
var clamp = (val, min = 0, max = 1) => Math.min(Math.max(val, min), max);
var clampPosition = (position = { x: 0, y: 0 }, extent, dimensions) => ({
  x: clamp(position.x, extent[0][0], extent[1][0] - (dimensions?.width ?? 0)),
  y: clamp(position.y, extent[0][1], extent[1][1] - (dimensions?.height ?? 0))
});
function clampPositionToParent(childPosition, childDimensions, parent) {
  const { width: parentWidth, height: parentHeight } = getNodeDimensions(parent);
  const { x: parentX, y: parentY } = parent.internals.positionAbsolute;
  return clampPosition(childPosition, [
    [parentX, parentY],
    [parentX + parentWidth, parentY + parentHeight]
  ], childDimensions);
}
var calcAutoPanVelocity = (value, min, max) => {
  if (value < min) {
    return clamp(Math.abs(value - min), 1, min) / min;
  } else if (value > max) {
    return -clamp(Math.abs(value - max), 1, min) / min;
  }
  return 0;
};
var calcAutoPan = (pos, bounds, speed = 15, distance2 = 40) => {
  const xMovement = calcAutoPanVelocity(pos.x, distance2, bounds.width - distance2) * speed;
  const yMovement = calcAutoPanVelocity(pos.y, distance2, bounds.height - distance2) * speed;
  return [xMovement, yMovement];
};
var getBoundsOfBoxes = (box1, box2) => ({
  x: Math.min(box1.x, box2.x),
  y: Math.min(box1.y, box2.y),
  x2: Math.max(box1.x2, box2.x2),
  y2: Math.max(box1.y2, box2.y2)
});
var rectToBox = ({ x: x3, y: y2, width, height }) => ({
  x: x3,
  y: y2,
  x2: x3 + width,
  y2: y2 + height
});
var boxToRect = ({ x: x3, y: y2, x2: x22, y2: y22 }) => ({
  x: x3,
  y: y2,
  width: x22 - x3,
  height: y22 - y2
});
var nodeToRect = (node, nodeOrigin = [0, 0]) => {
  const { x: x3, y: y2 } = isInternalNodeBase(node) ? node.internals.positionAbsolute : getNodePositionWithOrigin(node, nodeOrigin);
  return {
    x: x3,
    y: y2,
    width: node.measured?.width ?? node.width ?? node.initialWidth ?? 0,
    height: node.measured?.height ?? node.height ?? node.initialHeight ?? 0
  };
};
var nodeToBox = (node, nodeOrigin = [0, 0]) => {
  const { x: x3, y: y2 } = isInternalNodeBase(node) ? node.internals.positionAbsolute : getNodePositionWithOrigin(node, nodeOrigin);
  return {
    x: x3,
    y: y2,
    x2: x3 + (node.measured?.width ?? node.width ?? node.initialWidth ?? 0),
    y2: y2 + (node.measured?.height ?? node.height ?? node.initialHeight ?? 0)
  };
};
var getBoundsOfRects = (rect1, rect2) => boxToRect(getBoundsOfBoxes(rectToBox(rect1), rectToBox(rect2)));
var getRectsOverlappingArea = (aX, aY, aWidth, aHeight, bX, bY, bWidth, bHeight) => {
  const xOverlap = Math.max(0, Math.min(aX + aWidth, bX + bWidth) - Math.max(aX, bX));
  const yOverlap = Math.max(0, Math.min(aY + aHeight, bY + bHeight) - Math.max(aY, bY));
  return Math.ceil(xOverlap * yOverlap);
};
var getOverlappingArea = (rectA, rectB) => getRectsOverlappingArea(rectA.x, rectA.y, rectA.width, rectA.height, rectB.x, rectB.y, rectB.width, rectB.height);
var isRectObject = (obj) => isNumeric(obj.width) && isNumeric(obj.height) && isNumeric(obj.x) && isNumeric(obj.y);
var isNumeric = (n2) => !isNaN(n2) && isFinite(n2);
var createDevWarn = (lib, helpUrl) => (id2, message) => {
  if (true) {
    console.warn(`[${lib}]: ${message} Help: ${helpUrl}error#${id2}`);
  }
};
var snapPosition = (position, snapGrid = [1, 1]) => {
  return {
    x: snapGrid[0] * Math.round(position.x / snapGrid[0]),
    y: snapGrid[1] * Math.round(position.y / snapGrid[1])
  };
};
var pointToRendererPoint = ({ x: x3, y: y2 }, [tx, ty, tScale], snapToGrid = false, snapGrid = [1, 1]) => {
  const position = {
    x: (x3 - tx) / tScale,
    y: (y2 - ty) / tScale
  };
  return snapToGrid ? snapPosition(position, snapGrid) : position;
};
var rendererPointToPoint = ({ x: x3, y: y2 }, [tx, ty, tScale]) => {
  return {
    x: x3 * tScale + tx,
    y: y2 * tScale + ty
  };
};
function parsePadding(padding, viewport) {
  if (typeof padding === "number") {
    return Math.floor((viewport - viewport / (1 + padding)) * 0.5);
  }
  if (typeof padding === "string" && padding.endsWith("px")) {
    const paddingValue = parseFloat(padding);
    if (!Number.isNaN(paddingValue)) {
      return Math.floor(paddingValue);
    }
  }
  if (typeof padding === "string" && padding.endsWith("%")) {
    const paddingValue = parseFloat(padding);
    if (!Number.isNaN(paddingValue)) {
      return Math.floor(viewport * paddingValue * 0.01);
    }
  }
  console.error(`The padding value "${padding}" is invalid. Please provide a number or a string with a valid unit (px or %).`);
  return 0;
}
function parsePaddings(padding, width, height) {
  if (typeof padding === "string" || typeof padding === "number") {
    const paddingY = parsePadding(padding, height);
    const paddingX = parsePadding(padding, width);
    return {
      top: paddingY,
      right: paddingX,
      bottom: paddingY,
      left: paddingX,
      x: paddingX * 2,
      y: paddingY * 2
    };
  }
  if (typeof padding === "object") {
    const top = parsePadding(padding.top ?? padding.y ?? 0, height);
    const bottom = parsePadding(padding.bottom ?? padding.y ?? 0, height);
    const left = parsePadding(padding.left ?? padding.x ?? 0, width);
    const right = parsePadding(padding.right ?? padding.x ?? 0, width);
    return { top, right, bottom, left, x: left + right, y: top + bottom };
  }
  return { top: 0, right: 0, bottom: 0, left: 0, x: 0, y: 0 };
}
function calculateAppliedPaddings(bounds, x3, y2, zoom, width, height) {
  const { x: left, y: top } = rendererPointToPoint(bounds, [x3, y2, zoom]);
  const { x: boundRight, y: boundBottom } = rendererPointToPoint({ x: bounds.x + bounds.width, y: bounds.y + bounds.height }, [x3, y2, zoom]);
  const right = width - boundRight;
  const bottom = height - boundBottom;
  return {
    left: Math.floor(left),
    top: Math.floor(top),
    right: Math.floor(right),
    bottom: Math.floor(bottom)
  };
}
var getViewportForBounds = (bounds, width, height, minZoom, maxZoom, padding) => {
  const p2 = parsePaddings(padding, width, height);
  const xZoom = (width - p2.x) / bounds.width;
  const yZoom = (height - p2.y) / bounds.height;
  const zoom = Math.min(xZoom, yZoom);
  const clampedZoom = clamp(zoom, minZoom, maxZoom);
  const boundsCenterX = bounds.x + bounds.width / 2;
  const boundsCenterY = bounds.y + bounds.height / 2;
  const x3 = width / 2 - boundsCenterX * clampedZoom;
  const y2 = height / 2 - boundsCenterY * clampedZoom;
  const newPadding = calculateAppliedPaddings(bounds, x3, y2, clampedZoom, width, height);
  const offset = {
    left: Math.min(newPadding.left - p2.left, 0),
    top: Math.min(newPadding.top - p2.top, 0),
    right: Math.min(newPadding.right - p2.right, 0),
    bottom: Math.min(newPadding.bottom - p2.bottom, 0)
  };
  return {
    x: x3 - offset.left + offset.right,
    y: y2 - offset.top + offset.bottom,
    zoom: clampedZoom
  };
};
var isMacOs = () => typeof navigator !== "undefined" && navigator?.userAgent?.indexOf("Mac") >= 0;
function isCoordinateExtent(extent) {
  return extent !== void 0 && extent !== null && extent !== "parent";
}
function getNodeDimensions(node) {
  return {
    width: node.measured?.width ?? node.width ?? node.initialWidth ?? 0,
    height: node.measured?.height ?? node.height ?? node.initialHeight ?? 0
  };
}
function nodeHasDimensions(node) {
  return (node.measured?.width ?? node.width ?? node.initialWidth) !== void 0 && (node.measured?.height ?? node.height ?? node.initialHeight) !== void 0;
}
function evaluateAbsolutePosition(position, dimensions = { width: 0, height: 0 }, parentId, nodeLookup, nodeOrigin) {
  const positionAbsolute = { ...position };
  const parent = nodeLookup.get(parentId);
  if (parent) {
    const origin = parent.origin || nodeOrigin;
    positionAbsolute.x += parent.internals.positionAbsolute.x - (dimensions.width ?? 0) * origin[0];
    positionAbsolute.y += parent.internals.positionAbsolute.y - (dimensions.height ?? 0) * origin[1];
  }
  return positionAbsolute;
}
function areSetsEqual(a, b) {
  if (a.size !== b.size) {
    return false;
  }
  for (const item3 of a) {
    if (!b.has(item3)) {
      return false;
    }
  }
  return true;
}
function withResolvers() {
  let resolve;
  let reject;
  const promise = new Promise((res, rej) => {
    resolve = res;
    reject = rej;
  });
  return { promise, resolve, reject };
}
function mergeAriaLabelConfig(partial) {
  return { ...defaultAriaLabelConfig, ...partial || {} };
}
function getPointerPosition(event, { snapGrid = [0, 0], snapToGrid = false, transform: transform2, containerBounds }) {
  const { x: x3, y: y2 } = getEventPosition(event);
  const pointerPos = pointToRendererPoint({ x: x3 - (containerBounds?.left ?? 0), y: y2 - (containerBounds?.top ?? 0) }, transform2);
  const { x: xSnapped, y: ySnapped } = snapToGrid ? snapPosition(pointerPos, snapGrid) : pointerPos;
  return {
    xSnapped,
    ySnapped,
    ...pointerPos
  };
}
var getDimensions = (node) => ({
  width: node.offsetWidth,
  height: node.offsetHeight
});
var getHostForElement = (element) => element?.getRootNode?.() || window?.document;
var inputTags = ["INPUT", "SELECT", "TEXTAREA"];
function isInputDOMNode(event) {
  const target = event.composedPath?.()?.[0] || event.target;
  if (target?.nodeType !== 1)
    return false;
  const isInput = inputTags.includes(target.nodeName) || target.hasAttribute("contenteditable");
  return isInput || !!target.closest(".nokey");
}
var isMouseEvent = (event) => "clientX" in event;
var getEventPosition = (event, bounds) => {
  const isMouse = isMouseEvent(event);
  const evtX = isMouse ? event.clientX : event.touches?.[0].clientX;
  const evtY = isMouse ? event.clientY : event.touches?.[0].clientY;
  return {
    x: evtX - (bounds?.left ?? 0),
    y: evtY - (bounds?.top ?? 0)
  };
};
var getHandleBounds = (type, nodeElement, nodeBounds, zoom, nodeId) => {
  const handles = nodeElement.querySelectorAll(`.${type}`);
  if (!handles || !handles.length) {
    return null;
  }
  return Array.from(handles).map((handle2) => {
    const handleBounds = handle2.getBoundingClientRect();
    return {
      id: handle2.getAttribute("data-handleid"),
      type,
      nodeId,
      position: handle2.getAttribute("data-handlepos"),
      x: (handleBounds.left - nodeBounds.left) / zoom,
      y: (handleBounds.top - nodeBounds.top) / zoom,
      ...getDimensions(handle2)
    };
  });
};
function getBezierEdgeCenter({ sourceX, sourceY, targetX, targetY, sourceControlX, sourceControlY, targetControlX, targetControlY }) {
  const centerX = sourceX * 0.125 + sourceControlX * 0.375 + targetControlX * 0.375 + targetX * 0.125;
  const centerY = sourceY * 0.125 + sourceControlY * 0.375 + targetControlY * 0.375 + targetY * 0.125;
  const offsetX = Math.abs(centerX - sourceX);
  const offsetY = Math.abs(centerY - sourceY);
  return [centerX, centerY, offsetX, offsetY];
}
function calculateControlOffset(distance2, curvature) {
  if (distance2 >= 0) {
    return 0.5 * distance2;
  }
  return curvature * 25 * Math.sqrt(-distance2);
}
function getControlWithCurvature({ pos, x1, y1, x2: x22, y2, c }) {
  switch (pos) {
    case Position.Left:
      return [x1 - calculateControlOffset(x1 - x22, c), y1];
    case Position.Right:
      return [x1 + calculateControlOffset(x22 - x1, c), y1];
    case Position.Top:
      return [x1, y1 - calculateControlOffset(y1 - y2, c)];
    case Position.Bottom:
      return [x1, y1 + calculateControlOffset(y2 - y1, c)];
  }
}
function getBezierPath({ sourceX, sourceY, sourcePosition = Position.Bottom, targetX, targetY, targetPosition = Position.Top, curvature = 0.25 }) {
  const [sourceControlX, sourceControlY] = getControlWithCurvature({
    pos: sourcePosition,
    x1: sourceX,
    y1: sourceY,
    x2: targetX,
    y2: targetY,
    c: curvature
  });
  const [targetControlX, targetControlY] = getControlWithCurvature({
    pos: targetPosition,
    x1: targetX,
    y1: targetY,
    x2: sourceX,
    y2: sourceY,
    c: curvature
  });
  const [labelX, labelY, offsetX, offsetY] = getBezierEdgeCenter({
    sourceX,
    sourceY,
    targetX,
    targetY,
    sourceControlX,
    sourceControlY,
    targetControlX,
    targetControlY
  });
  return [
    `M${sourceX},${sourceY} C${sourceControlX},${sourceControlY} ${targetControlX},${targetControlY} ${targetX},${targetY}`,
    labelX,
    labelY,
    offsetX,
    offsetY
  ];
}
function getEdgeCenter({ sourceX, sourceY, targetX, targetY }) {
  const xOffset = Math.abs(targetX - sourceX) / 2;
  const centerX = targetX < sourceX ? targetX + xOffset : targetX - xOffset;
  const yOffset = Math.abs(targetY - sourceY) / 2;
  const centerY = targetY < sourceY ? targetY + yOffset : targetY - yOffset;
  return [centerX, centerY, xOffset, yOffset];
}
function getElevatedEdgeZIndex({ sourceNode, targetNode, selected: selected2 = false, zIndex = 0, elevateOnSelect = false, zIndexMode = "basic" }) {
  if (zIndexMode === "manual") {
    return zIndex;
  }
  const edgeZ = elevateOnSelect && selected2 ? zIndex + 1e3 : zIndex;
  const nodeZ = Math.max(sourceNode.parentId || elevateOnSelect && sourceNode.selected ? sourceNode.internals.z : 0, targetNode.parentId || elevateOnSelect && targetNode.selected ? targetNode.internals.z : 0);
  return edgeZ + nodeZ;
}
function isEdgeVisible({ sourceNode, targetNode, width, height, transform: transform2 }) {
  const edgeBox = getBoundsOfBoxes(nodeToBox(sourceNode), nodeToBox(targetNode));
  if (edgeBox.x === edgeBox.x2) {
    edgeBox.x2 += 1;
  }
  if (edgeBox.y === edgeBox.y2) {
    edgeBox.y2 += 1;
  }
  const viewRect = {
    x: -transform2[0] / transform2[2],
    y: -transform2[1] / transform2[2],
    width: width / transform2[2],
    height: height / transform2[2]
  };
  return getOverlappingArea(viewRect, boxToRect(edgeBox)) > 0;
}
var getEdgeId = ({ source, sourceHandle, target, targetHandle }) => `xy-edge__${source}${sourceHandle || ""}-${target}${targetHandle || ""}`;
var connectionExists = (edge, edges) => {
  return edges.some((el) => el.source === edge.source && el.target === edge.target && (el.sourceHandle === edge.sourceHandle || !el.sourceHandle && !edge.sourceHandle) && (el.targetHandle === edge.targetHandle || !el.targetHandle && !edge.targetHandle));
};
var addEdge = (edgeParams, edges, options = {}) => {
  if (!edgeParams.source || !edgeParams.target) {
    options.onError?.("006", errorMessages["error006"]());
    return edges;
  }
  const edgeIdGenerator = options.getEdgeId || getEdgeId;
  let edge;
  if (isEdgeBase(edgeParams)) {
    edge = { ...edgeParams };
  } else {
    edge = {
      ...edgeParams,
      id: edgeIdGenerator(edgeParams)
    };
  }
  if (connectionExists(edge, edges)) {
    return edges;
  }
  if (edge.sourceHandle === null) {
    delete edge.sourceHandle;
  }
  if (edge.targetHandle === null) {
    delete edge.targetHandle;
  }
  return edges.concat(edge);
};
var reconnectEdge = (oldEdge, newConnection, edges, options = { shouldReplaceId: true }) => {
  const { id: oldEdgeId, ...rest } = oldEdge;
  if (!newConnection.source || !newConnection.target) {
    options.onError?.("006", errorMessages["error006"]());
    return edges;
  }
  const foundEdge = edges.find((e) => e.id === oldEdge.id);
  if (!foundEdge) {
    options.onError?.("007", errorMessages["error007"](oldEdgeId));
    return edges;
  }
  const edgeIdGenerator = options.getEdgeId || getEdgeId;
  const edge = {
    ...rest,
    id: options.shouldReplaceId ? edgeIdGenerator(newConnection) : oldEdgeId,
    source: newConnection.source,
    target: newConnection.target,
    sourceHandle: newConnection.sourceHandle,
    targetHandle: newConnection.targetHandle
  };
  return edges.filter((e) => e.id !== oldEdgeId).concat(edge);
};
function getStraightPath({ sourceX, sourceY, targetX, targetY }) {
  const [labelX, labelY, offsetX, offsetY] = getEdgeCenter({
    sourceX,
    sourceY,
    targetX,
    targetY
  });
  return [`M ${sourceX},${sourceY}L ${targetX},${targetY}`, labelX, labelY, offsetX, offsetY];
}
var handleDirections = {
  [Position.Left]: { x: -1, y: 0 },
  [Position.Right]: { x: 1, y: 0 },
  [Position.Top]: { x: 0, y: -1 },
  [Position.Bottom]: { x: 0, y: 1 }
};
var getDirection = ({ source, sourcePosition = Position.Bottom, target }) => {
  if (sourcePosition === Position.Left || sourcePosition === Position.Right) {
    return source.x < target.x ? { x: 1, y: 0 } : { x: -1, y: 0 };
  }
  return source.y < target.y ? { x: 0, y: 1 } : { x: 0, y: -1 };
};
var distance = (a, b) => Math.sqrt(Math.pow(b.x - a.x, 2) + Math.pow(b.y - a.y, 2));
function getPoints({ source, sourcePosition = Position.Bottom, target, targetPosition = Position.Top, center, offset, stepPosition }) {
  const sourceDir = handleDirections[sourcePosition];
  const targetDir = handleDirections[targetPosition];
  const sourceGapped = { x: source.x + sourceDir.x * offset, y: source.y + sourceDir.y * offset };
  const targetGapped = { x: target.x + targetDir.x * offset, y: target.y + targetDir.y * offset };
  const dir = getDirection({
    source: sourceGapped,
    sourcePosition,
    target: targetGapped
  });
  const dirAccessor = dir.x !== 0 ? "x" : "y";
  const currDir = dir[dirAccessor];
  let points = [];
  let centerX, centerY;
  const sourceGapOffset = { x: 0, y: 0 };
  const targetGapOffset = { x: 0, y: 0 };
  const [, , defaultOffsetX, defaultOffsetY] = getEdgeCenter({
    sourceX: source.x,
    sourceY: source.y,
    targetX: target.x,
    targetY: target.y
  });
  if (sourceDir[dirAccessor] * targetDir[dirAccessor] === -1) {
    if (dirAccessor === "x") {
      centerX = center.x ?? sourceGapped.x + (targetGapped.x - sourceGapped.x) * stepPosition;
      centerY = center.y ?? (sourceGapped.y + targetGapped.y) / 2;
    } else {
      centerX = center.x ?? (sourceGapped.x + targetGapped.x) / 2;
      centerY = center.y ?? sourceGapped.y + (targetGapped.y - sourceGapped.y) * stepPosition;
    }
    const verticalSplit = [
      { x: centerX, y: sourceGapped.y },
      { x: centerX, y: targetGapped.y }
    ];
    const horizontalSplit = [
      { x: sourceGapped.x, y: centerY },
      { x: targetGapped.x, y: centerY }
    ];
    if (sourceDir[dirAccessor] === currDir) {
      points = dirAccessor === "x" ? verticalSplit : horizontalSplit;
    } else {
      points = dirAccessor === "x" ? horizontalSplit : verticalSplit;
    }
  } else {
    const sourceTarget = [{ x: sourceGapped.x, y: targetGapped.y }];
    const targetSource = [{ x: targetGapped.x, y: sourceGapped.y }];
    if (dirAccessor === "x") {
      points = sourceDir.x === currDir ? targetSource : sourceTarget;
    } else {
      points = sourceDir.y === currDir ? sourceTarget : targetSource;
    }
    if (sourcePosition === targetPosition) {
      const diff = Math.abs(source[dirAccessor] - target[dirAccessor]);
      if (diff <= offset) {
        const gapOffset = Math.min(offset - 1, offset - diff);
        if (sourceDir[dirAccessor] === currDir) {
          sourceGapOffset[dirAccessor] = (sourceGapped[dirAccessor] > source[dirAccessor] ? -1 : 1) * gapOffset;
        } else {
          targetGapOffset[dirAccessor] = (targetGapped[dirAccessor] > target[dirAccessor] ? -1 : 1) * gapOffset;
        }
      }
    }
    if (sourcePosition !== targetPosition) {
      const dirAccessorOpposite = dirAccessor === "x" ? "y" : "x";
      const isSameDir = sourceDir[dirAccessor] === targetDir[dirAccessorOpposite];
      const sourceGtTargetOppo = sourceGapped[dirAccessorOpposite] > targetGapped[dirAccessorOpposite];
      const sourceLtTargetOppo = sourceGapped[dirAccessorOpposite] < targetGapped[dirAccessorOpposite];
      const flipSourceTarget = sourceDir[dirAccessor] === 1 && (!isSameDir && sourceGtTargetOppo || isSameDir && sourceLtTargetOppo) || sourceDir[dirAccessor] !== 1 && (!isSameDir && sourceLtTargetOppo || isSameDir && sourceGtTargetOppo);
      if (flipSourceTarget) {
        points = dirAccessor === "x" ? sourceTarget : targetSource;
      }
    }
    const sourceGapPoint = { x: sourceGapped.x + sourceGapOffset.x, y: sourceGapped.y + sourceGapOffset.y };
    const targetGapPoint = { x: targetGapped.x + targetGapOffset.x, y: targetGapped.y + targetGapOffset.y };
    const maxXDistance = Math.max(Math.abs(sourceGapPoint.x - points[0].x), Math.abs(targetGapPoint.x - points[0].x));
    const maxYDistance = Math.max(Math.abs(sourceGapPoint.y - points[0].y), Math.abs(targetGapPoint.y - points[0].y));
    if (maxXDistance >= maxYDistance) {
      centerX = (sourceGapPoint.x + targetGapPoint.x) / 2;
      centerY = points[0].y;
    } else {
      centerX = points[0].x;
      centerY = (sourceGapPoint.y + targetGapPoint.y) / 2;
    }
  }
  const gappedSource = { x: sourceGapped.x + sourceGapOffset.x, y: sourceGapped.y + sourceGapOffset.y };
  const gappedTarget = { x: targetGapped.x + targetGapOffset.x, y: targetGapped.y + targetGapOffset.y };
  const pathPoints = [
    source,
    // we only want to add the gapped source/target if they are different from the first/last point to avoid duplicates which can cause issues with the bends
    ...gappedSource.x !== points[0].x || gappedSource.y !== points[0].y ? [gappedSource] : [],
    ...points,
    ...gappedTarget.x !== points[points.length - 1].x || gappedTarget.y !== points[points.length - 1].y ? [gappedTarget] : [],
    target
  ];
  return [pathPoints, centerX, centerY, defaultOffsetX, defaultOffsetY];
}
function getBend(a, b, c, size) {
  const bendSize = Math.min(distance(a, b) / 2, distance(b, c) / 2, size);
  const { x: x3, y: y2 } = b;
  if (a.x === x3 && x3 === c.x || a.y === y2 && y2 === c.y) {
    return `L${x3} ${y2}`;
  }
  if (a.y === y2) {
    const xDir2 = a.x < c.x ? -1 : 1;
    const yDir2 = a.y < c.y ? 1 : -1;
    return `L ${x3 + bendSize * xDir2},${y2}Q ${x3},${y2} ${x3},${y2 + bendSize * yDir2}`;
  }
  const xDir = a.x < c.x ? 1 : -1;
  const yDir = a.y < c.y ? -1 : 1;
  return `L ${x3},${y2 + bendSize * yDir}Q ${x3},${y2} ${x3 + bendSize * xDir},${y2}`;
}
function getSmoothStepPath({ sourceX, sourceY, sourcePosition = Position.Bottom, targetX, targetY, targetPosition = Position.Top, borderRadius = 5, centerX, centerY, offset = 20, stepPosition = 0.5 }) {
  const [points, labelX, labelY, offsetX, offsetY] = getPoints({
    source: { x: sourceX, y: sourceY },
    sourcePosition,
    target: { x: targetX, y: targetY },
    targetPosition,
    center: { x: centerX, y: centerY },
    offset,
    stepPosition
  });
  let path = `M${points[0].x} ${points[0].y}`;
  for (let i2 = 1; i2 < points.length - 1; i2++) {
    path += getBend(points[i2 - 1], points[i2], points[i2 + 1], borderRadius);
  }
  path += `L${points[points.length - 1].x} ${points[points.length - 1].y}`;
  return [path, labelX, labelY, offsetX, offsetY];
}
function isNodeInitialized(node) {
  return node && !!(node.internals.handleBounds || node.handles?.length) && !!(node.measured.width || node.width || node.initialWidth);
}
function getEdgePosition(params) {
  const { sourceNode, targetNode } = params;
  if (!isNodeInitialized(sourceNode) || !isNodeInitialized(targetNode)) {
    return null;
  }
  const sourceHandleBounds = sourceNode.internals.handleBounds || toHandleBounds(sourceNode.handles);
  const targetHandleBounds = targetNode.internals.handleBounds || toHandleBounds(targetNode.handles);
  const sourceHandle = getHandle$1(sourceHandleBounds?.source ?? [], params.sourceHandle);
  const targetHandle = getHandle$1(
    // when connection type is loose we can define all handles as sources and connect source -> source
    params.connectionMode === ConnectionMode.Strict ? targetHandleBounds?.target ?? [] : (targetHandleBounds?.target ?? []).concat(targetHandleBounds?.source ?? []),
    params.targetHandle
  );
  if (!sourceHandle || !targetHandle) {
    params.onError?.("008", errorMessages["error008"](!sourceHandle ? "source" : "target", {
      id: params.id,
      sourceHandle: params.sourceHandle,
      targetHandle: params.targetHandle
    }));
    return null;
  }
  const sourcePosition = sourceHandle?.position || Position.Bottom;
  const targetPosition = targetHandle?.position || Position.Top;
  const source = getHandlePosition(sourceNode, sourceHandle, sourcePosition);
  const target = getHandlePosition(targetNode, targetHandle, targetPosition);
  return {
    sourceX: source.x,
    sourceY: source.y,
    targetX: target.x,
    targetY: target.y,
    sourcePosition,
    targetPosition
  };
}
function toHandleBounds(handles) {
  if (!handles) {
    return null;
  }
  const source = [];
  const target = [];
  for (const handle2 of handles) {
    handle2.width = handle2.width ?? 1;
    handle2.height = handle2.height ?? 1;
    if (handle2.type === "source") {
      source.push(handle2);
    } else if (handle2.type === "target") {
      target.push(handle2);
    }
  }
  return {
    source,
    target
  };
}
function getHandlePosition(node, handle2, fallbackPosition = Position.Left, center = false) {
  const x3 = (handle2?.x ?? 0) + node.internals.positionAbsolute.x;
  const y2 = (handle2?.y ?? 0) + node.internals.positionAbsolute.y;
  const { width, height } = handle2 ?? getNodeDimensions(node);
  if (center) {
    return { x: x3 + width / 2, y: y2 + height / 2 };
  }
  const position = handle2?.position ?? fallbackPosition;
  switch (position) {
    case Position.Top:
      return { x: x3 + width / 2, y: y2 };
    case Position.Right:
      return { x: x3 + width, y: y2 + height / 2 };
    case Position.Bottom:
      return { x: x3 + width / 2, y: y2 + height };
    case Position.Left:
      return { x: x3, y: y2 + height / 2 };
  }
}
function getHandle$1(bounds, handleId) {
  if (!bounds) {
    return null;
  }
  return (!handleId ? bounds[0] : bounds.find((d2) => d2.id === handleId)) || null;
}
function getMarkerId(marker, id2) {
  if (!marker) {
    return "";
  }
  if (typeof marker === "string") {
    return marker;
  }
  const idPrefix = id2 ? `${id2}__` : "";
  return `${idPrefix}${Object.keys(marker).sort().map((key) => `${key}=${marker[key]}`).join("&")}`;
}
function createMarkerIds(edges, { id: id2, defaultColor, defaultMarkerStart, defaultMarkerEnd }) {
  const ids = /* @__PURE__ */ new Set();
  return edges.reduce((markers, edge) => {
    [edge.markerStart || defaultMarkerStart, edge.markerEnd || defaultMarkerEnd].forEach((marker) => {
      if (marker && typeof marker === "object") {
        const markerId = getMarkerId(marker, id2);
        if (!ids.has(markerId)) {
          markers.push({ id: markerId, color: marker.color || defaultColor, ...marker });
          ids.add(markerId);
        }
      }
    });
    return markers;
  }, []).sort((a, b) => a.id.localeCompare(b.id));
}
var SELECTED_NODE_Z = 1e3;
var ROOT_PARENT_Z_INCREMENT = 10;
var defaultOptions = {
  nodeOrigin: [0, 0],
  nodeExtent: infiniteExtent,
  elevateNodesOnSelect: true,
  zIndexMode: "basic",
  defaults: {}
};
var adoptUserNodesDefaultOptions = {
  ...defaultOptions,
  checkEquality: true
};
function mergeObjects(base2, incoming) {
  const result = { ...base2 };
  for (const key in incoming) {
    if (incoming[key] !== void 0) {
      result[key] = incoming[key];
    }
  }
  return result;
}
function updateAbsolutePositions(nodeLookup, parentLookup, options) {
  const _options = mergeObjects(defaultOptions, options);
  for (const node of nodeLookup.values()) {
    if (node.parentId) {
      updateChildNode(node, nodeLookup, parentLookup, _options);
    } else {
      const positionWithOrigin = getNodePositionWithOrigin(node, _options.nodeOrigin);
      const extent = isCoordinateExtent(node.extent) ? node.extent : _options.nodeExtent;
      const clampedPosition = clampPosition(positionWithOrigin, extent, getNodeDimensions(node));
      node.internals.positionAbsolute = clampedPosition;
    }
  }
}
function parseHandles(userNode, internalNode) {
  if (!userNode.handles) {
    return !userNode.measured ? void 0 : internalNode?.internals.handleBounds;
  }
  const source = [];
  const target = [];
  for (const handle2 of userNode.handles) {
    const handleBounds = {
      id: handle2.id,
      width: handle2.width ?? 1,
      height: handle2.height ?? 1,
      nodeId: userNode.id,
      x: handle2.x,
      y: handle2.y,
      position: handle2.position,
      type: handle2.type
    };
    if (handle2.type === "source") {
      source.push(handleBounds);
    } else if (handle2.type === "target") {
      target.push(handleBounds);
    }
  }
  return {
    source,
    target
  };
}
function isManualZIndexMode(zIndexMode) {
  return zIndexMode === "manual";
}
function adoptUserNodes(nodes, nodeLookup, parentLookup, options = {}) {
  const _options = mergeObjects(adoptUserNodesDefaultOptions, options);
  const rootParentIndex = { i: 0 };
  const tmpLookup = new Map(nodeLookup);
  const selectedNodeZ = _options?.elevateNodesOnSelect && !isManualZIndexMode(_options.zIndexMode) ? SELECTED_NODE_Z : 0;
  let nodesInitialized = nodes.length > 0;
  let hasSelectedNodes = false;
  nodeLookup.clear();
  parentLookup.clear();
  for (const userNode of nodes) {
    let internalNode = tmpLookup.get(userNode.id);
    if (_options.checkEquality && userNode === internalNode?.internals.userNode) {
      nodeLookup.set(userNode.id, internalNode);
    } else {
      const positionWithOrigin = getNodePositionWithOrigin(userNode, _options.nodeOrigin);
      const extent = isCoordinateExtent(userNode.extent) ? userNode.extent : _options.nodeExtent;
      const clampedPosition = clampPosition(positionWithOrigin, extent, getNodeDimensions(userNode));
      internalNode = {
        ..._options.defaults,
        ...userNode,
        measured: {
          width: userNode.measured?.width,
          height: userNode.measured?.height
        },
        internals: {
          positionAbsolute: clampedPosition,
          // if user re-initializes the node or removes `measured` for whatever reason, we reset the handleBounds so that the node gets re-measured
          handleBounds: parseHandles(userNode, internalNode),
          z: calculateZ(userNode, selectedNodeZ, _options.zIndexMode),
          userNode
        }
      };
      nodeLookup.set(userNode.id, internalNode);
    }
    if ((internalNode.measured === void 0 || internalNode.measured.width === void 0 || internalNode.measured.height === void 0) && !internalNode.hidden) {
      nodesInitialized = false;
    }
    if (userNode.parentId) {
      updateChildNode(internalNode, nodeLookup, parentLookup, options, rootParentIndex);
    }
    hasSelectedNodes ||= userNode.selected ?? false;
  }
  return { nodesInitialized, hasSelectedNodes };
}
function updateParentLookup(node, parentLookup) {
  if (!node.parentId) {
    return;
  }
  const childNodes = parentLookup.get(node.parentId);
  if (childNodes) {
    childNodes.set(node.id, node);
  } else {
    parentLookup.set(node.parentId, /* @__PURE__ */ new Map([[node.id, node]]));
  }
}
function updateChildNode(node, nodeLookup, parentLookup, options, rootParentIndex) {
  const { elevateNodesOnSelect, nodeOrigin, nodeExtent, zIndexMode } = mergeObjects(defaultOptions, options);
  const parentId = node.parentId;
  const parentNode = nodeLookup.get(parentId);
  if (!parentNode) {
    console.warn(`Parent node ${parentId} not found. Please make sure that parent nodes are in front of their child nodes in the nodes array.`);
    return;
  }
  updateParentLookup(node, parentLookup);
  if (rootParentIndex && !parentNode.parentId && parentNode.internals.rootParentIndex === void 0 && zIndexMode === "auto") {
    parentNode.internals.rootParentIndex = ++rootParentIndex.i;
    parentNode.internals.z = parentNode.internals.z + rootParentIndex.i * ROOT_PARENT_Z_INCREMENT;
  }
  if (rootParentIndex && parentNode.internals.rootParentIndex !== void 0) {
    rootParentIndex.i = parentNode.internals.rootParentIndex;
  }
  const selectedNodeZ = elevateNodesOnSelect && !isManualZIndexMode(zIndexMode) ? SELECTED_NODE_Z : 0;
  const { x: x3, y: y2, z: z3 } = calculateChildXYZ(node, parentNode, nodeOrigin, nodeExtent, selectedNodeZ, zIndexMode);
  const { positionAbsolute } = node.internals;
  const positionChanged = x3 !== positionAbsolute.x || y2 !== positionAbsolute.y;
  if (positionChanged || z3 !== node.internals.z) {
    nodeLookup.set(node.id, {
      ...node,
      internals: {
        ...node.internals,
        positionAbsolute: positionChanged ? { x: x3, y: y2 } : positionAbsolute,
        z: z3
      }
    });
  }
}
function calculateZ(node, selectedNodeZ, zIndexMode) {
  const zIndex = isNumeric(node.zIndex) ? node.zIndex : 0;
  if (isManualZIndexMode(zIndexMode)) {
    return zIndex;
  }
  return zIndex + (node.selected ? selectedNodeZ : 0);
}
function calculateChildXYZ(childNode, parentNode, nodeOrigin, nodeExtent, selectedNodeZ, zIndexMode) {
  const { x: parentX, y: parentY } = parentNode.internals.positionAbsolute;
  const childDimensions = getNodeDimensions(childNode);
  const positionWithOrigin = getNodePositionWithOrigin(childNode, nodeOrigin);
  const clampedPosition = isCoordinateExtent(childNode.extent) ? clampPosition(positionWithOrigin, childNode.extent, childDimensions) : positionWithOrigin;
  let absolutePosition = clampPosition({ x: parentX + clampedPosition.x, y: parentY + clampedPosition.y }, nodeExtent, childDimensions);
  if (childNode.extent === "parent") {
    absolutePosition = clampPositionToParent(absolutePosition, childDimensions, parentNode);
  }
  const childZ = calculateZ(childNode, selectedNodeZ, zIndexMode);
  const parentZ = parentNode.internals.z ?? 0;
  return {
    x: absolutePosition.x,
    y: absolutePosition.y,
    z: parentZ >= childZ ? parentZ + 1 : childZ
  };
}
function handleExpandParent(children2, nodeLookup, parentLookup, nodeOrigin = [0, 0]) {
  const changes = [];
  const parentExpansions = /* @__PURE__ */ new Map();
  for (const child of children2) {
    const parent = nodeLookup.get(child.parentId);
    if (!parent) {
      continue;
    }
    const parentRect = parentExpansions.get(child.parentId)?.expandedRect ?? nodeToRect(parent);
    const expandedRect = getBoundsOfRects(parentRect, child.rect);
    parentExpansions.set(child.parentId, { expandedRect, parent });
  }
  if (parentExpansions.size > 0) {
    parentExpansions.forEach(({ expandedRect, parent }, parentId) => {
      const positionAbsolute = parent.internals.positionAbsolute;
      const dimensions = getNodeDimensions(parent);
      const origin = parent.origin ?? nodeOrigin;
      const xChange = expandedRect.x < positionAbsolute.x ? Math.round(Math.abs(positionAbsolute.x - expandedRect.x)) : 0;
      const yChange = expandedRect.y < positionAbsolute.y ? Math.round(Math.abs(positionAbsolute.y - expandedRect.y)) : 0;
      const newWidth = Math.max(dimensions.width, Math.round(expandedRect.width));
      const newHeight = Math.max(dimensions.height, Math.round(expandedRect.height));
      const widthChange = (newWidth - dimensions.width) * origin[0];
      const heightChange = (newHeight - dimensions.height) * origin[1];
      if (xChange > 0 || yChange > 0 || widthChange || heightChange) {
        changes.push({
          id: parentId,
          type: "position",
          position: {
            x: parent.position.x - xChange + widthChange,
            y: parent.position.y - yChange + heightChange
          }
        });
        parentLookup.get(parentId)?.forEach((childNode) => {
          if (!children2.some((child) => child.id === childNode.id)) {
            changes.push({
              id: childNode.id,
              type: "position",
              position: {
                x: childNode.position.x + xChange,
                y: childNode.position.y + yChange
              }
            });
          }
        });
      }
      if (dimensions.width < expandedRect.width || dimensions.height < expandedRect.height || xChange || yChange) {
        changes.push({
          id: parentId,
          type: "dimensions",
          setAttributes: true,
          dimensions: {
            width: newWidth + (xChange ? origin[0] * xChange - widthChange : 0),
            height: newHeight + (yChange ? origin[1] * yChange - heightChange : 0)
          }
        });
      }
    });
  }
  return changes;
}
function updateNodeInternals(updates, nodeLookup, parentLookup, domNode, nodeOrigin, nodeExtent, zIndexMode) {
  const viewportNode = domNode?.querySelector(".xyflow__viewport");
  let updatedInternals = false;
  if (!viewportNode) {
    return { changes: [], updatedInternals };
  }
  const changes = [];
  const style2 = window.getComputedStyle(viewportNode);
  const { m22: zoom } = new window.DOMMatrixReadOnly(style2.transform);
  const parentExpandChildren = [];
  for (const update2 of updates.values()) {
    const node = nodeLookup.get(update2.id);
    if (!node) {
      continue;
    }
    if (node.hidden) {
      nodeLookup.set(node.id, {
        ...node,
        internals: {
          ...node.internals,
          handleBounds: void 0
        }
      });
      updatedInternals = true;
      continue;
    }
    const dimensions = getDimensions(update2.nodeElement);
    const dimensionChanged = node.measured.width !== dimensions.width || node.measured.height !== dimensions.height;
    const doUpdate = !!(dimensions.width && dimensions.height && (dimensionChanged || !node.internals.handleBounds || update2.force));
    if (doUpdate) {
      const nodeBounds = update2.nodeElement.getBoundingClientRect();
      const extent = isCoordinateExtent(node.extent) ? node.extent : nodeExtent;
      let { positionAbsolute } = node.internals;
      if (node.parentId && node.extent === "parent") {
        const parentNode = nodeLookup.get(node.parentId);
        if (parentNode) {
          positionAbsolute = clampPositionToParent(positionAbsolute, dimensions, parentNode);
        }
      } else if (extent) {
        positionAbsolute = clampPosition(positionAbsolute, extent, dimensions);
      }
      const newNode = {
        ...node,
        measured: dimensions,
        internals: {
          ...node.internals,
          positionAbsolute,
          handleBounds: {
            source: getHandleBounds("source", update2.nodeElement, nodeBounds, zoom, node.id),
            target: getHandleBounds("target", update2.nodeElement, nodeBounds, zoom, node.id)
          }
        }
      };
      nodeLookup.set(node.id, newNode);
      if (node.parentId) {
        updateChildNode(newNode, nodeLookup, parentLookup, { nodeOrigin, zIndexMode });
      }
      updatedInternals = true;
      if (dimensionChanged) {
        changes.push({
          id: node.id,
          type: "dimensions",
          dimensions
        });
        if (node.expandParent && node.parentId) {
          parentExpandChildren.push({
            id: node.id,
            parentId: node.parentId,
            rect: nodeToRect(newNode, nodeOrigin)
          });
        }
      }
    }
  }
  if (parentExpandChildren.length > 0) {
    const parentExpandChanges = handleExpandParent(parentExpandChildren, nodeLookup, parentLookup, nodeOrigin);
    changes.push(...parentExpandChanges);
  }
  return { changes, updatedInternals };
}
async function panBy({ delta, panZoom, transform: transform2, translateExtent, width, height }) {
  if (!panZoom || !delta.x && !delta.y) {
    return false;
  }
  const nextViewport = await panZoom.setViewportConstrained({
    x: transform2[0] + delta.x,
    y: transform2[1] + delta.y,
    zoom: transform2[2]
  }, [
    [0, 0],
    [width, height]
  ], translateExtent);
  const transformChanged = !!nextViewport && (nextViewport.x !== transform2[0] || nextViewport.y !== transform2[1] || nextViewport.k !== transform2[2]);
  return transformChanged;
}
function addConnectionToLookup(type, connection, connectionKey, connectionLookup, nodeId, handleId) {
  let key = nodeId;
  const nodeMap = connectionLookup.get(key) || /* @__PURE__ */ new Map();
  connectionLookup.set(key, nodeMap.set(connectionKey, connection));
  key = `${nodeId}-${type}`;
  const typeMap = connectionLookup.get(key) || /* @__PURE__ */ new Map();
  connectionLookup.set(key, typeMap.set(connectionKey, connection));
  if (handleId) {
    key = `${nodeId}-${type}-${handleId}`;
    const handleMap = connectionLookup.get(key) || /* @__PURE__ */ new Map();
    connectionLookup.set(key, handleMap.set(connectionKey, connection));
  }
}
function updateConnectionLookup(connectionLookup, edgeLookup, edges) {
  connectionLookup.clear();
  edgeLookup.clear();
  for (const edge of edges) {
    const { source: sourceNode, target: targetNode, sourceHandle = null, targetHandle = null } = edge;
    const connection = { edgeId: edge.id, source: sourceNode, target: targetNode, sourceHandle, targetHandle };
    const sourceKey = `${sourceNode}-${sourceHandle}--${targetNode}-${targetHandle}`;
    const targetKey = `${targetNode}-${targetHandle}--${sourceNode}-${sourceHandle}`;
    addConnectionToLookup("source", connection, targetKey, connectionLookup, sourceNode, sourceHandle);
    addConnectionToLookup("target", connection, sourceKey, connectionLookup, targetNode, targetHandle);
    edgeLookup.set(edge.id, edge);
  }
}
function isParentSelected(node, nodeLookup) {
  if (!node.parentId) {
    return false;
  }
  const parentNode = nodeLookup.get(node.parentId);
  if (!parentNode) {
    return false;
  }
  if (parentNode.selected) {
    return true;
  }
  return isParentSelected(parentNode, nodeLookup);
}
function hasSelector(target, selector, domNode) {
  let current = target;
  do {
    if (current?.matches?.(selector))
      return true;
    if (current === domNode)
      return false;
    current = current?.parentElement;
  } while (current);
  return false;
}
function getDragItems(nodeLookup, nodesDraggable, mousePos, nodeId) {
  const dragItems = /* @__PURE__ */ new Map();
  for (const [id2, node] of nodeLookup) {
    if ((node.selected || node.id === nodeId) && (!node.parentId || !isParentSelected(node, nodeLookup)) && (node.draggable || nodesDraggable && typeof node.draggable === "undefined")) {
      const internalNode = nodeLookup.get(id2);
      if (internalNode) {
        dragItems.set(id2, {
          id: id2,
          position: internalNode.position || { x: 0, y: 0 },
          distance: {
            x: mousePos.x - internalNode.internals.positionAbsolute.x,
            y: mousePos.y - internalNode.internals.positionAbsolute.y
          },
          extent: internalNode.extent,
          parentId: internalNode.parentId,
          origin: internalNode.origin,
          expandParent: internalNode.expandParent,
          internals: {
            positionAbsolute: internalNode.internals.positionAbsolute || { x: 0, y: 0 }
          },
          measured: {
            width: internalNode.measured.width ?? 0,
            height: internalNode.measured.height ?? 0
          }
        });
      }
    }
  }
  return dragItems;
}
function getEventHandlerParams({ nodeId, dragItems, nodeLookup, dragging = true }) {
  const nodesFromDragItems = [];
  for (const [id2, dragItem] of dragItems) {
    const node2 = nodeLookup.get(id2)?.internals.userNode;
    if (node2) {
      nodesFromDragItems.push({
        ...node2,
        position: dragItem.position,
        dragging
      });
    }
  }
  if (!nodeId) {
    return [nodesFromDragItems[0], nodesFromDragItems];
  }
  const node = nodeLookup.get(nodeId)?.internals.userNode;
  return [
    !node ? nodesFromDragItems[0] : {
      ...node,
      position: dragItems.get(nodeId)?.position || node.position,
      dragging
    },
    nodesFromDragItems
  ];
}
function calculateSnapOffset({ dragItems, snapGrid, x: x3, y: y2 }) {
  const refDragItem = dragItems.values().next().value;
  if (!refDragItem) {
    return null;
  }
  const refPos = {
    x: x3 - refDragItem.distance.x,
    y: y2 - refDragItem.distance.y
  };
  const refPosSnapped = snapPosition(refPos, snapGrid);
  return {
    x: refPosSnapped.x - refPos.x,
    y: refPosSnapped.y - refPos.y
  };
}
function XYDrag({ onNodeMouseDown, getStoreItems, onDragStart, onDrag, onDragStop }) {
  let lastPos = { x: null, y: null };
  let autoPanId = 0;
  let dragItems = /* @__PURE__ */ new Map();
  let autoPanStarted = false;
  let mousePosition = { x: 0, y: 0 };
  let containerBounds = null;
  let dragStarted = false;
  let d3Selection = null;
  let abortDrag = false;
  let nodePositionsChanged = false;
  let dragEvent = null;
  function update2({ noDragClassName, handleSelector, domNode, isSelectable, nodeId, nodeClickDistance = 0 }) {
    d3Selection = select_default2(domNode);
    function updateNodes({ x: x3, y: y2 }) {
      const { nodeLookup, nodeExtent, snapGrid, snapToGrid, nodeOrigin, onNodeDrag, onSelectionDrag, onError, updateNodePositions } = getStoreItems();
      lastPos = { x: x3, y: y2 };
      let hasChange = false;
      const isMultiDrag = dragItems.size > 1;
      const nodesBox = isMultiDrag && nodeExtent ? rectToBox(getInternalNodesBounds(dragItems)) : null;
      const multiDragSnapOffset = isMultiDrag && snapToGrid ? calculateSnapOffset({
        dragItems,
        snapGrid,
        x: x3,
        y: y2
      }) : null;
      for (const [id2, dragItem] of dragItems) {
        if (!nodeLookup.has(id2)) {
          continue;
        }
        let nextPosition = { x: x3 - dragItem.distance.x, y: y2 - dragItem.distance.y };
        if (snapToGrid) {
          nextPosition = multiDragSnapOffset ? {
            x: Math.round(nextPosition.x + multiDragSnapOffset.x),
            y: Math.round(nextPosition.y + multiDragSnapOffset.y)
          } : snapPosition(nextPosition, snapGrid);
        }
        let adjustedNodeExtent = null;
        if (isMultiDrag && nodeExtent && !dragItem.extent && nodesBox) {
          const { positionAbsolute: positionAbsolute2 } = dragItem.internals;
          const x1 = positionAbsolute2.x - nodesBox.x + nodeExtent[0][0];
          const x22 = positionAbsolute2.x + dragItem.measured.width - nodesBox.x2 + nodeExtent[1][0];
          const y1 = positionAbsolute2.y - nodesBox.y + nodeExtent[0][1];
          const y22 = positionAbsolute2.y + dragItem.measured.height - nodesBox.y2 + nodeExtent[1][1];
          adjustedNodeExtent = [
            [x1, y1],
            [x22, y22]
          ];
        }
        const { position, positionAbsolute } = calculateNodePosition({
          nodeId: id2,
          nextPosition,
          nodeLookup,
          nodeExtent: adjustedNodeExtent ? adjustedNodeExtent : nodeExtent,
          nodeOrigin,
          onError
        });
        hasChange = hasChange || dragItem.position.x !== position.x || dragItem.position.y !== position.y;
        dragItem.position = position;
        dragItem.internals.positionAbsolute = positionAbsolute;
      }
      nodePositionsChanged = nodePositionsChanged || hasChange;
      if (!hasChange) {
        return;
      }
      updateNodePositions(dragItems, true);
      if (dragEvent && (onDrag || onNodeDrag || !nodeId && onSelectionDrag)) {
        const [currentNode, currentNodes] = getEventHandlerParams({
          nodeId,
          dragItems,
          nodeLookup
        });
        onDrag?.(dragEvent, dragItems, currentNode, currentNodes);
        onNodeDrag?.(dragEvent, currentNode, currentNodes);
        if (!nodeId) {
          onSelectionDrag?.(dragEvent, currentNodes);
        }
      }
    }
    async function autoPan() {
      if (!containerBounds) {
        return;
      }
      const { transform: transform2, panBy: panBy2, autoPanSpeed, autoPanOnNodeDrag } = getStoreItems();
      if (!autoPanOnNodeDrag) {
        autoPanStarted = false;
        cancelAnimationFrame(autoPanId);
        return;
      }
      const [xMovement, yMovement] = calcAutoPan(mousePosition, containerBounds, autoPanSpeed);
      if (xMovement !== 0 || yMovement !== 0) {
        lastPos.x = (lastPos.x ?? 0) - xMovement / transform2[2];
        lastPos.y = (lastPos.y ?? 0) - yMovement / transform2[2];
        if (await panBy2({ x: xMovement, y: yMovement })) {
          updateNodes(lastPos);
        }
      }
      autoPanId = requestAnimationFrame(autoPan);
    }
    function startDrag(event) {
      const { nodeLookup, multiSelectionActive, nodesDraggable, transform: transform2, snapGrid, snapToGrid, selectNodesOnDrag, onNodeDragStart, onSelectionDragStart, unselectNodesAndEdges } = getStoreItems();
      dragStarted = true;
      if ((!selectNodesOnDrag || !isSelectable) && !multiSelectionActive && nodeId) {
        if (!nodeLookup.get(nodeId)?.selected) {
          unselectNodesAndEdges();
        }
      }
      if (isSelectable && selectNodesOnDrag && nodeId) {
        onNodeMouseDown?.(nodeId);
      }
      const pointerPos = getPointerPosition(event.sourceEvent, { transform: transform2, snapGrid, snapToGrid, containerBounds });
      lastPos = pointerPos;
      dragItems = getDragItems(nodeLookup, nodesDraggable, pointerPos, nodeId);
      if (dragItems.size > 0 && (onDragStart || onNodeDragStart || !nodeId && onSelectionDragStart)) {
        const [currentNode, currentNodes] = getEventHandlerParams({
          nodeId,
          dragItems,
          nodeLookup
        });
        onDragStart?.(event.sourceEvent, dragItems, currentNode, currentNodes);
        onNodeDragStart?.(event.sourceEvent, currentNode, currentNodes);
        if (!nodeId) {
          onSelectionDragStart?.(event.sourceEvent, currentNodes);
        }
      }
    }
    const d3DragInstance = drag_default().clickDistance(nodeClickDistance).on("start", (event) => {
      const { domNode: domNode2, nodeDragThreshold, transform: transform2, snapGrid, snapToGrid } = getStoreItems();
      containerBounds = domNode2?.getBoundingClientRect() || null;
      abortDrag = false;
      nodePositionsChanged = false;
      dragEvent = event.sourceEvent;
      if (nodeDragThreshold === 0) {
        startDrag(event);
      }
      const pointerPos = getPointerPosition(event.sourceEvent, { transform: transform2, snapGrid, snapToGrid, containerBounds });
      lastPos = pointerPos;
      mousePosition = getEventPosition(event.sourceEvent, containerBounds);
    }).on("drag", (event) => {
      const { autoPanOnNodeDrag, transform: transform2, snapGrid, snapToGrid, nodeDragThreshold, nodeLookup } = getStoreItems();
      const pointerPos = getPointerPosition(event.sourceEvent, { transform: transform2, snapGrid, snapToGrid, containerBounds });
      dragEvent = event.sourceEvent;
      if (event.sourceEvent.type === "touchmove" && event.sourceEvent.touches.length > 1 || // if user deletes a node while dragging, we need to abort the drag to prevent errors
      nodeId && !nodeLookup.has(nodeId)) {
        abortDrag = true;
      }
      if (abortDrag) {
        return;
      }
      if (!autoPanStarted && autoPanOnNodeDrag && dragStarted) {
        autoPanStarted = true;
        autoPan();
      }
      if (!dragStarted) {
        const currentMousePosition = getEventPosition(event.sourceEvent, containerBounds);
        const x3 = currentMousePosition.x - mousePosition.x;
        const y2 = currentMousePosition.y - mousePosition.y;
        const distance2 = Math.sqrt(x3 * x3 + y2 * y2);
        if (distance2 > nodeDragThreshold) {
          startDrag(event);
        }
      }
      if ((lastPos.x !== pointerPos.xSnapped || lastPos.y !== pointerPos.ySnapped) && dragItems && dragStarted) {
        mousePosition = getEventPosition(event.sourceEvent, containerBounds);
        updateNodes(pointerPos);
      }
    }).on("end", (event) => {
      if (!dragStarted || abortDrag) {
        if (abortDrag && dragItems.size > 0) {
          getStoreItems().updateNodePositions(dragItems, false);
        }
        return;
      }
      autoPanStarted = false;
      dragStarted = false;
      cancelAnimationFrame(autoPanId);
      if (dragItems.size > 0) {
        const { nodeLookup, updateNodePositions, onNodeDragStop, onSelectionDragStop } = getStoreItems();
        if (nodePositionsChanged) {
          updateNodePositions(dragItems, false);
          nodePositionsChanged = false;
        }
        if (onDragStop || onNodeDragStop || !nodeId && onSelectionDragStop) {
          const [currentNode, currentNodes] = getEventHandlerParams({
            nodeId,
            dragItems,
            nodeLookup,
            dragging: false
          });
          onDragStop?.(event.sourceEvent, dragItems, currentNode, currentNodes);
          onNodeDragStop?.(event.sourceEvent, currentNode, currentNodes);
          if (!nodeId) {
            onSelectionDragStop?.(event.sourceEvent, currentNodes);
          }
        }
      }
    }).filter((event) => {
      const target = event.target;
      const isDraggable = !event.button && (!noDragClassName || !hasSelector(target, `.${noDragClassName}`, domNode)) && (!handleSelector || hasSelector(target, handleSelector, domNode));
      return isDraggable;
    });
    d3Selection.call(d3DragInstance);
  }
  function destroy() {
    d3Selection?.on(".drag", null);
  }
  return {
    update: update2,
    destroy
  };
}
function getNodesWithinDistance(position, nodeLookup, distance2) {
  const nodes = [];
  const rect = {
    x: position.x - distance2,
    y: position.y - distance2,
    width: distance2 * 2,
    height: distance2 * 2
  };
  for (const node of nodeLookup.values()) {
    if (getOverlappingArea(rect, nodeToRect(node)) > 0) {
      nodes.push(node);
    }
  }
  return nodes;
}
var ADDITIONAL_DISTANCE = 250;
function getClosestHandle(position, connectionRadius, nodeLookup, fromHandle) {
  let closestHandles = [];
  let minDistance = Infinity;
  const closeNodes = getNodesWithinDistance(position, nodeLookup, connectionRadius + ADDITIONAL_DISTANCE);
  for (const node of closeNodes) {
    const allHandles = [...node.internals.handleBounds?.source ?? [], ...node.internals.handleBounds?.target ?? []];
    for (const handle2 of allHandles) {
      if (fromHandle.nodeId === handle2.nodeId && fromHandle.type === handle2.type && fromHandle.id === handle2.id) {
        continue;
      }
      const { x: x3, y: y2 } = getHandlePosition(node, handle2, handle2.position, true);
      const distance2 = Math.sqrt(Math.pow(x3 - position.x, 2) + Math.pow(y2 - position.y, 2));
      if (distance2 > connectionRadius) {
        continue;
      }
      if (distance2 < minDistance) {
        closestHandles = [{ ...handle2, x: x3, y: y2 }];
        minDistance = distance2;
      } else if (distance2 === minDistance) {
        closestHandles.push({ ...handle2, x: x3, y: y2 });
      }
    }
  }
  if (!closestHandles.length) {
    return null;
  }
  if (closestHandles.length > 1) {
    const oppositeHandleType = fromHandle.type === "source" ? "target" : "source";
    return closestHandles.find((handle2) => handle2.type === oppositeHandleType) ?? closestHandles[0];
  }
  return closestHandles[0];
}
function getHandle(nodeId, handleType, handleId, nodeLookup, connectionMode, withAbsolutePosition = false) {
  const node = nodeLookup.get(nodeId);
  if (!node) {
    return null;
  }
  const handles = connectionMode === "strict" ? node.internals.handleBounds?.[handleType] : [...node.internals.handleBounds?.source ?? [], ...node.internals.handleBounds?.target ?? []];
  const handle2 = (handleId ? handles?.find((h) => h.id === handleId) : handles?.[0]) ?? null;
  return handle2 && withAbsolutePosition ? { ...handle2, ...getHandlePosition(node, handle2, handle2.position, true) } : handle2;
}
function getHandleType(edgeUpdaterType, handleDomNode) {
  if (edgeUpdaterType) {
    return edgeUpdaterType;
  } else if (handleDomNode?.classList.contains("target")) {
    return "target";
  } else if (handleDomNode?.classList.contains("source")) {
    return "source";
  }
  return null;
}
function isConnectionValid(isInsideConnectionRadius, isHandleValid) {
  let isValid = null;
  if (isHandleValid) {
    isValid = true;
  } else if (isInsideConnectionRadius && !isHandleValid) {
    isValid = false;
  }
  return isValid;
}
var alwaysValid = () => true;
function onPointerDown(event, { connectionMode, connectionRadius, handleId, nodeId, edgeUpdaterType, isTarget, domNode, nodeLookup, lib, autoPanOnConnect, flowId, panBy: panBy2, cancelConnection, onConnectStart, onConnect, onConnectEnd, isValidConnection = alwaysValid, onReconnectEnd, updateConnection, getTransform, getFromHandle, autoPanSpeed, dragThreshold = 1, handleDomNode }) {
  const doc = getHostForElement(event.target);
  let autoPanId = 0;
  let closestHandle;
  const { x: x3, y: y2 } = getEventPosition(event);
  const handleType = getHandleType(edgeUpdaterType, handleDomNode);
  const containerBounds = domNode?.getBoundingClientRect();
  let connectionStarted = false;
  if (!containerBounds || !handleType) {
    return;
  }
  const fromHandleInternal = getHandle(nodeId, handleType, handleId, nodeLookup, connectionMode);
  if (!fromHandleInternal) {
    return;
  }
  let position = getEventPosition(event, containerBounds);
  let autoPanStarted = false;
  let connection = null;
  let isValid = false;
  let resultHandleDomNode = null;
  function autoPan() {
    if (!autoPanOnConnect || !containerBounds) {
      return;
    }
    const [x4, y3] = calcAutoPan(position, containerBounds, autoPanSpeed);
    panBy2({ x: x4, y: y3 });
    autoPanId = requestAnimationFrame(autoPan);
  }
  const fromHandle = {
    ...fromHandleInternal,
    nodeId,
    type: handleType,
    position: fromHandleInternal.position
  };
  const fromInternalNode = nodeLookup.get(nodeId);
  const from = getHandlePosition(fromInternalNode, fromHandle, Position.Left, true);
  let previousConnection = {
    inProgress: true,
    isValid: null,
    from,
    fromHandle,
    fromPosition: fromHandle.position,
    fromNode: fromInternalNode,
    to: position,
    toHandle: null,
    toPosition: oppositePosition[fromHandle.position],
    toNode: null,
    pointer: position
  };
  function startConnection() {
    connectionStarted = true;
    updateConnection(previousConnection);
    onConnectStart?.(event, { nodeId, handleId, handleType });
  }
  if (dragThreshold === 0) {
    startConnection();
  }
  function onPointerMove(event2) {
    if (!connectionStarted) {
      const { x: evtX, y: evtY } = getEventPosition(event2);
      const dx2 = evtX - x3;
      const dy = evtY - y2;
      const nextConnectionStarted = dx2 * dx2 + dy * dy > dragThreshold * dragThreshold;
      if (!nextConnectionStarted) {
        return;
      }
      startConnection();
    }
    if (!getFromHandle() || !fromHandle) {
      onPointerUp(event2);
      return;
    }
    const transform2 = getTransform();
    position = getEventPosition(event2, containerBounds);
    closestHandle = getClosestHandle(pointToRendererPoint(position, transform2, false, [1, 1]), connectionRadius, nodeLookup, fromHandle);
    if (!autoPanStarted) {
      autoPan();
      autoPanStarted = true;
    }
    const result = isValidHandle(event2, {
      handle: closestHandle,
      connectionMode,
      fromNodeId: nodeId,
      fromHandleId: handleId,
      fromType: isTarget ? "target" : "source",
      isValidConnection,
      doc,
      lib,
      flowId,
      nodeLookup
    });
    resultHandleDomNode = result.handleDomNode;
    connection = result.connection;
    isValid = isConnectionValid(!!closestHandle, result.isValid);
    const fromInternalNode2 = nodeLookup.get(nodeId);
    const from2 = fromInternalNode2 ? getHandlePosition(fromInternalNode2, fromHandle, Position.Left, true) : previousConnection.from;
    const newConnection = {
      ...previousConnection,
      from: from2,
      isValid,
      to: result.toHandle && isValid ? rendererPointToPoint({ x: result.toHandle.x, y: result.toHandle.y }, transform2) : position,
      toHandle: result.toHandle,
      toPosition: isValid && result.toHandle ? result.toHandle.position : oppositePosition[fromHandle.position],
      toNode: result.toHandle ? nodeLookup.get(result.toHandle.nodeId) : null,
      pointer: position
    };
    updateConnection(newConnection);
    previousConnection = newConnection;
  }
  function onPointerUp(event2) {
    if ("touches" in event2 && event2.touches.length > 0) {
      return;
    }
    if (connectionStarted) {
      if ((closestHandle || resultHandleDomNode) && connection && isValid) {
        onConnect?.(connection);
      }
      const { inProgress, ...connectionState } = previousConnection;
      const finalConnectionState = {
        ...connectionState,
        toPosition: previousConnection.toHandle ? previousConnection.toPosition : null
      };
      onConnectEnd?.(event2, finalConnectionState);
      if (edgeUpdaterType) {
        onReconnectEnd?.(event2, finalConnectionState);
      }
    }
    cancelConnection();
    cancelAnimationFrame(autoPanId);
    autoPanStarted = false;
    isValid = false;
    connection = null;
    resultHandleDomNode = null;
    doc.removeEventListener("mousemove", onPointerMove);
    doc.removeEventListener("mouseup", onPointerUp);
    doc.removeEventListener("touchmove", onPointerMove);
    doc.removeEventListener("touchend", onPointerUp);
  }
  doc.addEventListener("mousemove", onPointerMove);
  doc.addEventListener("mouseup", onPointerUp);
  doc.addEventListener("touchmove", onPointerMove);
  doc.addEventListener("touchend", onPointerUp);
}
function isValidHandle(event, { handle: handle2, connectionMode, fromNodeId, fromHandleId, fromType, doc, lib, flowId, isValidConnection = alwaysValid, nodeLookup }) {
  const isTarget = fromType === "target";
  const handleDomNode = handle2 ? doc.querySelector(`.${lib}-flow__handle[data-id="${flowId}-${handle2?.nodeId}-${handle2?.id}-${handle2?.type}"]`) : null;
  const { x: x3, y: y2 } = getEventPosition(event);
  const handleBelow = doc.elementFromPoint(x3, y2);
  const handleToCheck = handleBelow?.classList.contains(`${lib}-flow__handle`) ? handleBelow : handleDomNode;
  const result = {
    handleDomNode: handleToCheck,
    isValid: false,
    connection: null,
    toHandle: null
  };
  if (handleToCheck) {
    const handleType = getHandleType(void 0, handleToCheck);
    const handleNodeId = handleToCheck.getAttribute("data-nodeid");
    const handleId = handleToCheck.getAttribute("data-handleid");
    const connectable = handleToCheck.classList.contains("connectable");
    const connectableEnd = handleToCheck.classList.contains("connectableend");
    if (!handleNodeId || !handleType) {
      return result;
    }
    const connection = {
      source: isTarget ? handleNodeId : fromNodeId,
      sourceHandle: isTarget ? handleId : fromHandleId,
      target: isTarget ? fromNodeId : handleNodeId,
      targetHandle: isTarget ? fromHandleId : handleId
    };
    result.connection = connection;
    const isConnectable = connectable && connectableEnd;
    const isValid = isConnectable && (connectionMode === ConnectionMode.Strict ? isTarget && handleType === "source" || !isTarget && handleType === "target" : handleNodeId !== fromNodeId || handleId !== fromHandleId);
    result.isValid = isValid && isValidConnection(connection);
    result.toHandle = getHandle(handleNodeId, handleType, handleId, nodeLookup, connectionMode, true);
  }
  return result;
}
var XYHandle = {
  onPointerDown,
  isValid: isValidHandle
};
function XYMinimap({ domNode, panZoom, getTransform, getViewScale }) {
  const selection2 = select_default2(domNode);
  function update2({ translateExtent, width, height, zoomStep = 1, pannable = true, zoomable = true, inversePan = false }) {
    const zoomHandler = (event) => {
      if (event.sourceEvent.type !== "wheel" || !panZoom) {
        return;
      }
      const transform2 = getTransform();
      const factor = event.sourceEvent.ctrlKey && isMacOs() ? 10 : 1;
      const pinchDelta = -event.sourceEvent.deltaY * (event.sourceEvent.deltaMode === 1 ? 0.05 : event.sourceEvent.deltaMode ? 1 : 2e-3) * zoomStep;
      const nextZoom = transform2[2] * Math.pow(2, pinchDelta * factor);
      panZoom.scaleTo(nextZoom);
    };
    let panStart = [0, 0];
    const panStartHandler = (event) => {
      if (event.sourceEvent.type === "mousedown" || event.sourceEvent.type === "touchstart") {
        panStart = [
          event.sourceEvent.clientX ?? event.sourceEvent.touches[0].clientX,
          event.sourceEvent.clientY ?? event.sourceEvent.touches[0].clientY
        ];
      }
    };
    const panHandler = (event) => {
      const transform2 = getTransform();
      if (event.sourceEvent.type !== "mousemove" && event.sourceEvent.type !== "touchmove" || !panZoom) {
        return;
      }
      const panCurrent = [
        event.sourceEvent.clientX ?? event.sourceEvent.touches[0].clientX,
        event.sourceEvent.clientY ?? event.sourceEvent.touches[0].clientY
      ];
      const panDelta = [panCurrent[0] - panStart[0], panCurrent[1] - panStart[1]];
      panStart = panCurrent;
      const moveScale = getViewScale() * Math.max(transform2[2], Math.log(transform2[2])) * (inversePan ? -1 : 1);
      const position = {
        x: transform2[0] - panDelta[0] * moveScale,
        y: transform2[1] - panDelta[1] * moveScale
      };
      const extent = [
        [0, 0],
        [width, height]
      ];
      panZoom.setViewportConstrained({
        x: position.x,
        y: position.y,
        zoom: transform2[2]
      }, extent, translateExtent);
    };
    const zoomAndPanHandler = zoom_default2().on("start", panStartHandler).on("zoom", pannable ? panHandler : null).on("zoom.wheel", zoomable ? zoomHandler : null);
    selection2.call(zoomAndPanHandler, {});
  }
  function destroy() {
    selection2.on("zoom", null);
  }
  return {
    update: update2,
    destroy,
    pointer: pointer_default
  };
}
var transformToViewport = (transform2) => ({
  x: transform2.x,
  y: transform2.y,
  zoom: transform2.k
});
var viewportToTransform = ({ x: x3, y: y2, zoom }) => identity2.translate(x3, y2).scale(zoom);
var isWrappedWithClass = (event, className) => event.target.closest(`.${className}`);
var isRightClickPan = (panOnDrag, usedButton) => usedButton === 2 && Array.isArray(panOnDrag) && panOnDrag.includes(2);
var defaultEase = (t) => ((t *= 2) <= 1 ? t * t * t : (t -= 2) * t * t + 2) / 2;
var getD3Transition = (selection2, duration = 0, ease = defaultEase, onEnd = () => {
}) => {
  const hasDuration = typeof duration === "number" && duration > 0;
  if (!hasDuration) {
    onEnd();
  }
  return hasDuration ? selection2.transition().duration(duration).ease(ease).on("end", onEnd) : selection2;
};
var wheelDelta = (event) => {
  const factor = event.ctrlKey && isMacOs() ? 10 : 1;
  return -event.deltaY * (event.deltaMode === 1 ? 0.05 : event.deltaMode ? 1 : 2e-3) * factor;
};
function createPanOnScrollHandler({ zoomPanValues, noWheelClassName, d3Selection, d3Zoom, panOnScrollMode, panOnScrollSpeed, zoomOnPinch, onPanZoomStart, onPanZoom, onPanZoomEnd }) {
  return (event) => {
    if (isWrappedWithClass(event, noWheelClassName)) {
      if (event.ctrlKey) {
        event.preventDefault();
      }
      return false;
    }
    event.preventDefault();
    event.stopImmediatePropagation();
    const currentZoom = d3Selection.property("__zoom").k || 1;
    if (event.ctrlKey && zoomOnPinch) {
      const point = pointer_default(event);
      const pinchDelta = wheelDelta(event);
      const zoom = currentZoom * Math.pow(2, pinchDelta);
      d3Zoom.scaleTo(d3Selection, zoom, point, event);
      return;
    }
    const deltaNormalize = event.deltaMode === 1 ? 20 : 1;
    let deltaX = panOnScrollMode === PanOnScrollMode.Vertical ? 0 : event.deltaX * deltaNormalize;
    let deltaY = panOnScrollMode === PanOnScrollMode.Horizontal ? 0 : event.deltaY * deltaNormalize;
    if (!isMacOs() && event.shiftKey && panOnScrollMode !== PanOnScrollMode.Vertical) {
      deltaX = event.deltaY * deltaNormalize;
      deltaY = 0;
    }
    d3Zoom.translateBy(
      d3Selection,
      -(deltaX / currentZoom) * panOnScrollSpeed,
      -(deltaY / currentZoom) * panOnScrollSpeed,
      // @ts-ignore
      { internal: true }
    );
    const nextViewport = transformToViewport(d3Selection.property("__zoom"));
    clearTimeout(zoomPanValues.panScrollTimeout);
    if (!zoomPanValues.isPanScrolling) {
      zoomPanValues.isPanScrolling = true;
      onPanZoomStart?.(event, nextViewport);
    } else {
      onPanZoom?.(event, nextViewport);
    }
    zoomPanValues.panScrollTimeout = setTimeout(() => {
      onPanZoomEnd?.(event, nextViewport);
      zoomPanValues.isPanScrolling = false;
    }, 150);
  };
}
function createZoomOnScrollHandler({ noWheelClassName, preventScrolling, d3ZoomHandler }) {
  return function(event, d2) {
    const isWheel = event.type === "wheel";
    const preventZoom = !preventScrolling && isWheel && !event.ctrlKey;
    const hasNoWheelClass = isWrappedWithClass(event, noWheelClassName);
    if (event.ctrlKey && isWheel && hasNoWheelClass) {
      event.preventDefault();
    }
    if (preventZoom || hasNoWheelClass) {
      return null;
    }
    event.preventDefault();
    d3ZoomHandler.call(this, event, d2);
  };
}
function createPanZoomStartHandler({ zoomPanValues, onDraggingChange, onPanZoomStart }) {
  return (event) => {
    if (event.sourceEvent?.internal) {
      return;
    }
    const viewport = transformToViewport(event.transform);
    zoomPanValues.mouseButton = event.sourceEvent?.button || 0;
    zoomPanValues.isZoomingOrPanning = true;
    zoomPanValues.prevViewport = viewport;
    if (event.sourceEvent?.type === "mousedown") {
      onDraggingChange(true);
    }
    if (onPanZoomStart) {
      onPanZoomStart?.(event.sourceEvent, viewport);
    }
  };
}
function createPanZoomHandler({ zoomPanValues, panOnDrag, onPaneContextMenu, onTransformChange, onPanZoom }) {
  return (event) => {
    zoomPanValues.usedRightMouseButton = !!(onPaneContextMenu && isRightClickPan(panOnDrag, zoomPanValues.mouseButton ?? 0));
    if (!event.sourceEvent?.sync) {
      onTransformChange([event.transform.x, event.transform.y, event.transform.k]);
    }
    if (onPanZoom && !event.sourceEvent?.internal) {
      onPanZoom?.(event.sourceEvent, transformToViewport(event.transform));
    }
  };
}
function createPanZoomEndHandler({ zoomPanValues, panOnDrag, panOnScroll, onDraggingChange, onPanZoomEnd, onPaneContextMenu }) {
  return (event) => {
    if (event.sourceEvent?.internal) {
      return;
    }
    zoomPanValues.isZoomingOrPanning = false;
    if (onPaneContextMenu && isRightClickPan(panOnDrag, zoomPanValues.mouseButton ?? 0) && !zoomPanValues.usedRightMouseButton && event.sourceEvent) {
      onPaneContextMenu(event.sourceEvent);
    }
    zoomPanValues.usedRightMouseButton = false;
    onDraggingChange(false);
    if (onPanZoomEnd) {
      const viewport = transformToViewport(event.transform);
      zoomPanValues.prevViewport = viewport;
      clearTimeout(zoomPanValues.timerId);
      zoomPanValues.timerId = setTimeout(
        () => {
          onPanZoomEnd?.(event.sourceEvent, viewport);
        },
        // we need a setTimeout for panOnScroll to suppress multiple end events fired during scroll
        panOnScroll ? 150 : 0
      );
    }
  };
}
function createFilter({ panActivationKeyPressed, zoomActivationKeyPressed, zoomOnScroll, zoomOnPinch, panOnDrag, panOnScroll, zoomOnDoubleClick, userSelectionActive, noWheelClassName, noPanClassName, lib, connectionInProgress }) {
  return (event) => {
    const zoomScroll = zoomActivationKeyPressed || zoomOnScroll;
    const pinchZoom = zoomOnPinch && event.ctrlKey;
    const isWheelEvent = event.type === "wheel";
    if (event.button === 1 && event.type === "mousedown" && (isWrappedWithClass(event, `${lib}-flow__node`) || isWrappedWithClass(event, `${lib}-flow__edge`) || isWrappedWithClass(event, `${lib}-flow__selection`) || isWrappedWithClass(event, `${lib}-flow__nodesselection`))) {
      return true;
    }
    if (!panOnDrag && !zoomScroll && !panOnScroll && !zoomOnDoubleClick && !zoomOnPinch) {
      return false;
    }
    if (userSelectionActive) {
      return false;
    }
    if (connectionInProgress && !isWheelEvent) {
      return false;
    }
    if (isWrappedWithClass(event, noWheelClassName) && isWheelEvent) {
      return false;
    }
    if (isWrappedWithClass(event, noPanClassName) && (!isWheelEvent || panOnScroll && isWheelEvent && !zoomActivationKeyPressed)) {
      return false;
    }
    if (!zoomOnPinch && event.ctrlKey && isWheelEvent) {
      return false;
    }
    if (!zoomOnPinch && event.type === "touchstart" && event.touches?.length > 1) {
      event.preventDefault();
      return false;
    }
    if (!zoomScroll && !panOnScroll && !pinchZoom && isWheelEvent) {
      return false;
    }
    if (!panOnDrag && (event.type === "mousedown" || event.type === "touchstart")) {
      return false;
    }
    if (Array.isArray(panOnDrag) && !panOnDrag.includes(event.button) && event.type === "mousedown") {
      return false;
    }
    const buttonAllowed = Array.isArray(panOnDrag) && panOnDrag.includes(event.button) || !event.button || event.button <= 1;
    return (!event.ctrlKey || isWheelEvent || panActivationKeyPressed) && buttonAllowed;
  };
}
function XYPanZoom({ domNode, minZoom, maxZoom, translateExtent, viewport, onPanZoom, onPanZoomStart, onPanZoomEnd, onDraggingChange }) {
  const zoomPanValues = {
    isZoomingOrPanning: false,
    usedRightMouseButton: false,
    prevViewport: {},
    mouseButton: 0,
    timerId: void 0,
    panScrollTimeout: void 0,
    isPanScrolling: false
  };
  const bbox = domNode.getBoundingClientRect();
  let cachedExtent = [
    [0, 0],
    [bbox.width, bbox.height]
  ];
  const extentResizeObserver = typeof ResizeObserver !== "undefined" ? new ResizeObserver((entries) => {
    const entry = entries[0];
    if (entry) {
      cachedExtent = [
        [0, 0],
        [entry.contentRect.width, entry.contentRect.height]
      ];
    }
  }) : null;
  extentResizeObserver?.observe(domNode);
  const d3ZoomInstance = zoom_default2().extent(() => cachedExtent).scaleExtent([minZoom, maxZoom]).translateExtent(translateExtent);
  const d3Selection = select_default2(domNode).call(d3ZoomInstance);
  setViewportConstrained({
    x: viewport.x,
    y: viewport.y,
    zoom: clamp(viewport.zoom, minZoom, maxZoom)
  }, [
    [0, 0],
    [bbox.width, bbox.height]
  ], translateExtent);
  const d3ZoomHandler = d3Selection.on("wheel.zoom");
  const d3DblClickZoomHandler = d3Selection.on("dblclick.zoom");
  d3ZoomInstance.wheelDelta(wheelDelta);
  async function setTransform(transform2, options) {
    if (d3Selection) {
      return new Promise((resolve) => {
        d3ZoomInstance?.interpolate(options?.interpolate === "linear" ? value_default : zoom_default).transform(getD3Transition(d3Selection, options?.duration, options?.ease, () => resolve(true)), transform2);
      });
    }
    return false;
  }
  function update2({ noWheelClassName, noPanClassName, onPaneContextMenu, userSelectionActive, panOnScroll, panOnDrag, panOnScrollMode, panOnScrollSpeed, preventScrolling, zoomOnPinch, zoomOnScroll, zoomOnDoubleClick, panActivationKeyPressed = false, zoomActivationKeyPressed, lib, onTransformChange, connectionInProgress, paneClickDistance, selectionOnDrag }) {
    if (userSelectionActive && !zoomPanValues.isZoomingOrPanning) {
      destroy();
    }
    const isPanOnScroll = panOnScroll && !zoomActivationKeyPressed && !userSelectionActive;
    d3ZoomInstance.clickDistance(selectionOnDrag ? Infinity : !isNumeric(paneClickDistance) || paneClickDistance < 0 ? 0 : paneClickDistance);
    const wheelHandler = isPanOnScroll ? createPanOnScrollHandler({
      zoomPanValues,
      noWheelClassName,
      d3Selection,
      d3Zoom: d3ZoomInstance,
      panOnScrollMode,
      panOnScrollSpeed,
      zoomOnPinch,
      onPanZoomStart,
      onPanZoom,
      onPanZoomEnd
    }) : createZoomOnScrollHandler({
      noWheelClassName,
      preventScrolling,
      d3ZoomHandler
    });
    d3Selection.on("wheel.zoom", wheelHandler, { passive: false });
    const startHandler = createPanZoomStartHandler({
      zoomPanValues,
      onDraggingChange,
      onPanZoomStart
    });
    d3ZoomInstance.on("start", startHandler);
    const panZoomHandler = createPanZoomHandler({
      zoomPanValues,
      panOnDrag,
      onPaneContextMenu: !!onPaneContextMenu,
      onPanZoom,
      onTransformChange
    });
    d3ZoomInstance.on("zoom", panZoomHandler);
    const panZoomEndHandler = createPanZoomEndHandler({
      zoomPanValues,
      panOnDrag,
      panOnScroll,
      onPaneContextMenu,
      onPanZoomEnd,
      onDraggingChange
    });
    d3ZoomInstance.on("end", panZoomEndHandler);
    const filter2 = createFilter({
      panActivationKeyPressed,
      zoomActivationKeyPressed,
      panOnDrag,
      zoomOnScroll,
      panOnScroll,
      zoomOnDoubleClick,
      zoomOnPinch,
      userSelectionActive,
      noPanClassName,
      noWheelClassName,
      lib,
      connectionInProgress
    });
    d3ZoomInstance.filter(filter2);
    if (zoomOnDoubleClick) {
      d3Selection.on("dblclick.zoom", d3DblClickZoomHandler);
    } else {
      d3Selection.on("dblclick.zoom", null);
    }
  }
  function destroy() {
    d3ZoomInstance.on("zoom", null);
  }
  async function setViewportConstrained(viewport2, extent, translateExtent2) {
    const nextTransform = viewportToTransform(viewport2);
    const contrainedTransform = d3ZoomInstance?.constrain()(nextTransform, extent, translateExtent2);
    if (contrainedTransform) {
      await setTransform(contrainedTransform);
    }
    return contrainedTransform;
  }
  async function setViewport(viewport2, options) {
    const nextTransform = viewportToTransform(viewport2);
    await setTransform(nextTransform, options);
    return nextTransform;
  }
  function syncViewport(viewport2) {
    if (d3Selection) {
      const nextTransform = viewportToTransform(viewport2);
      const currentTransform = d3Selection.property("__zoom");
      if (currentTransform.k !== viewport2.zoom || currentTransform.x !== viewport2.x || currentTransform.y !== viewport2.y) {
        d3ZoomInstance?.transform(d3Selection, nextTransform, null, { sync: true });
      }
    }
  }
  function getViewport() {
    const transform2 = d3Selection ? transform(d3Selection.node()) : { x: 0, y: 0, k: 1 };
    return { x: transform2.x, y: transform2.y, zoom: transform2.k };
  }
  async function scaleTo(zoom, options) {
    if (d3Selection) {
      return new Promise((resolve) => {
        d3ZoomInstance?.interpolate(options?.interpolate === "linear" ? value_default : zoom_default).scaleTo(getD3Transition(d3Selection, options?.duration, options?.ease, () => resolve(true)), zoom);
      });
    }
    return false;
  }
  async function scaleBy(factor, options) {
    if (d3Selection) {
      return new Promise((resolve) => {
        d3ZoomInstance?.interpolate(options?.interpolate === "linear" ? value_default : zoom_default).scaleBy(getD3Transition(d3Selection, options?.duration, options?.ease, () => resolve(true)), factor);
      });
    }
    return false;
  }
  function setScaleExtent(scaleExtent) {
    d3ZoomInstance?.scaleExtent(scaleExtent);
  }
  function setTranslateExtent(translateExtent2) {
    d3ZoomInstance?.translateExtent(translateExtent2);
  }
  function setClickDistance(distance2) {
    const validDistance = !isNumeric(distance2) || distance2 < 0 ? 0 : distance2;
    d3ZoomInstance?.clickDistance(validDistance);
  }
  return {
    update: update2,
    destroy,
    setViewport,
    setViewportConstrained,
    getViewport,
    scaleTo,
    scaleBy,
    setScaleExtent,
    setTranslateExtent,
    syncViewport,
    setClickDistance
  };
}
var ResizeControlVariant;
(function(ResizeControlVariant2) {
  ResizeControlVariant2["Line"] = "line";
  ResizeControlVariant2["Handle"] = "handle";
})(ResizeControlVariant || (ResizeControlVariant = {}));
var XY_RESIZER_HANDLE_POSITIONS = ["top-left", "top-right", "bottom-left", "bottom-right"];
var XY_RESIZER_LINE_POSITIONS = ["top", "right", "bottom", "left"];
function getResizeDirection({ width, prevWidth, height, prevHeight, affectsX, affectsY }) {
  const deltaWidth = width - prevWidth;
  const deltaHeight = height - prevHeight;
  const direction = [deltaWidth > 0 ? 1 : deltaWidth < 0 ? -1 : 0, deltaHeight > 0 ? 1 : deltaHeight < 0 ? -1 : 0];
  if (deltaWidth && affectsX) {
    direction[0] = direction[0] * -1;
  }
  if (deltaHeight && affectsY) {
    direction[1] = direction[1] * -1;
  }
  return direction;
}
function getControlDirection(controlPosition) {
  const isHorizontal = controlPosition.includes("right") || controlPosition.includes("left");
  const isVertical = controlPosition.includes("bottom") || controlPosition.includes("top");
  const affectsX = controlPosition.includes("left");
  const affectsY = controlPosition.includes("top");
  return {
    isHorizontal,
    isVertical,
    affectsX,
    affectsY
  };
}
function getLowerExtentClamp(lowerExtent, lowerBound) {
  return Math.max(0, lowerBound - lowerExtent);
}
function getUpperExtentClamp(upperExtent, upperBound) {
  return Math.max(0, upperExtent - upperBound);
}
function getSizeClamp(size, minSize, maxSize) {
  return Math.max(0, minSize - size, size - maxSize);
}
function xor(a, b) {
  return a ? !b : b;
}
function getDimensionsAfterResize(startValues, controlDirection, pointerPosition, boundaries, keepAspectRatio, nodeOrigin, extent, childExtent) {
  let { affectsX, affectsY } = controlDirection;
  const { isHorizontal, isVertical } = controlDirection;
  const isDiagonal = isHorizontal && isVertical;
  const { xSnapped, ySnapped } = pointerPosition;
  const { minWidth, maxWidth, minHeight, maxHeight } = boundaries;
  const { x: startX, y: startY, width: startWidth, height: startHeight, aspectRatio: aspectRatio2 } = startValues;
  let distX = Math.floor(isHorizontal ? xSnapped - startValues.pointerX : 0);
  let distY = Math.floor(isVertical ? ySnapped - startValues.pointerY : 0);
  const newWidth = startWidth + (affectsX ? -distX : distX);
  const newHeight = startHeight + (affectsY ? -distY : distY);
  const originOffsetX = -nodeOrigin[0] * startWidth;
  const originOffsetY = -nodeOrigin[1] * startHeight;
  let clampX = getSizeClamp(newWidth, minWidth, maxWidth);
  let clampY = getSizeClamp(newHeight, minHeight, maxHeight);
  if (extent) {
    let xExtentClamp = 0;
    let yExtentClamp = 0;
    if (affectsX && distX < 0) {
      xExtentClamp = getLowerExtentClamp(startX + distX + originOffsetX, extent[0][0]);
    } else if (!affectsX && distX > 0) {
      xExtentClamp = getUpperExtentClamp(startX + newWidth + originOffsetX, extent[1][0]);
    }
    if (affectsY && distY < 0) {
      yExtentClamp = getLowerExtentClamp(startY + distY + originOffsetY, extent[0][1]);
    } else if (!affectsY && distY > 0) {
      yExtentClamp = getUpperExtentClamp(startY + newHeight + originOffsetY, extent[1][1]);
    }
    clampX = Math.max(clampX, xExtentClamp);
    clampY = Math.max(clampY, yExtentClamp);
  }
  if (childExtent) {
    let xExtentClamp = 0;
    let yExtentClamp = 0;
    if (affectsX && distX > 0) {
      xExtentClamp = getUpperExtentClamp(startX + distX, childExtent[0][0]);
    } else if (!affectsX && distX < 0) {
      xExtentClamp = getLowerExtentClamp(startX + newWidth, childExtent[1][0]);
    }
    if (affectsY && distY > 0) {
      yExtentClamp = getUpperExtentClamp(startY + distY, childExtent[0][1]);
    } else if (!affectsY && distY < 0) {
      yExtentClamp = getLowerExtentClamp(startY + newHeight, childExtent[1][1]);
    }
    clampX = Math.max(clampX, xExtentClamp);
    clampY = Math.max(clampY, yExtentClamp);
  }
  if (keepAspectRatio) {
    if (isHorizontal) {
      const aspectHeightClamp = getSizeClamp(newWidth / aspectRatio2, minHeight, maxHeight) * aspectRatio2;
      clampX = Math.max(clampX, aspectHeightClamp);
      if (extent) {
        let aspectExtentClamp = 0;
        if (!affectsX && !affectsY || affectsX && !affectsY && isDiagonal) {
          aspectExtentClamp = getUpperExtentClamp(startY + originOffsetY + newWidth / aspectRatio2, extent[1][1]) * aspectRatio2;
        } else {
          aspectExtentClamp = getLowerExtentClamp(startY + originOffsetY + (affectsX ? distX : -distX) / aspectRatio2, extent[0][1]) * aspectRatio2;
        }
        clampX = Math.max(clampX, aspectExtentClamp);
      }
      if (childExtent) {
        let aspectExtentClamp = 0;
        if (!affectsX && !affectsY || affectsX && !affectsY && isDiagonal) {
          aspectExtentClamp = getLowerExtentClamp(startY + newWidth / aspectRatio2, childExtent[1][1]) * aspectRatio2;
        } else {
          aspectExtentClamp = getUpperExtentClamp(startY + (affectsX ? distX : -distX) / aspectRatio2, childExtent[0][1]) * aspectRatio2;
        }
        clampX = Math.max(clampX, aspectExtentClamp);
      }
    }
    if (isVertical) {
      const aspectWidthClamp = getSizeClamp(newHeight * aspectRatio2, minWidth, maxWidth) / aspectRatio2;
      clampY = Math.max(clampY, aspectWidthClamp);
      if (extent) {
        let aspectExtentClamp = 0;
        if (!affectsX && !affectsY || affectsY && !affectsX && isDiagonal) {
          aspectExtentClamp = getUpperExtentClamp(startX + newHeight * aspectRatio2 + originOffsetX, extent[1][0]) / aspectRatio2;
        } else {
          aspectExtentClamp = getLowerExtentClamp(startX + (affectsY ? distY : -distY) * aspectRatio2 + originOffsetX, extent[0][0]) / aspectRatio2;
        }
        clampY = Math.max(clampY, aspectExtentClamp);
      }
      if (childExtent) {
        let aspectExtentClamp = 0;
        if (!affectsX && !affectsY || affectsY && !affectsX && isDiagonal) {
          aspectExtentClamp = getLowerExtentClamp(startX + newHeight * aspectRatio2, childExtent[1][0]) / aspectRatio2;
        } else {
          aspectExtentClamp = getUpperExtentClamp(startX + (affectsY ? distY : -distY) * aspectRatio2, childExtent[0][0]) / aspectRatio2;
        }
        clampY = Math.max(clampY, aspectExtentClamp);
      }
    }
  }
  distY = distY + (distY < 0 ? clampY : -clampY);
  distX = distX + (distX < 0 ? clampX : -clampX);
  if (keepAspectRatio) {
    if (isDiagonal) {
      if (newWidth > newHeight * aspectRatio2) {
        distY = (xor(affectsX, affectsY) ? -distX : distX) / aspectRatio2;
      } else {
        distX = (xor(affectsX, affectsY) ? -distY : distY) * aspectRatio2;
      }
    } else {
      if (isHorizontal) {
        distY = distX / aspectRatio2;
        affectsY = affectsX;
      } else {
        distX = distY * aspectRatio2;
        affectsX = affectsY;
      }
    }
  }
  const x3 = affectsX ? startX + distX : startX;
  const y2 = affectsY ? startY + distY : startY;
  return {
    width: startWidth + (affectsX ? -distX : distX),
    height: startHeight + (affectsY ? -distY : distY),
    x: nodeOrigin[0] * distX * (!affectsX ? 1 : -1) + x3,
    y: nodeOrigin[1] * distY * (!affectsY ? 1 : -1) + y2
  };
}
var initPrevValues = { width: 0, height: 0, x: 0, y: 0 };
var initStartValues = {
  ...initPrevValues,
  pointerX: 0,
  pointerY: 0,
  aspectRatio: 1
};
function nodeToChildExtent(child, parent, nodeOrigin) {
  const x3 = parent.position.x + child.position.x;
  const y2 = parent.position.y + child.position.y;
  const width = child.measured.width ?? 0;
  const height = child.measured.height ?? 0;
  const originOffsetX = nodeOrigin[0] * width;
  const originOffsetY = nodeOrigin[1] * height;
  return [
    [x3 - originOffsetX, y2 - originOffsetY],
    [x3 + width - originOffsetX, y2 + height - originOffsetY]
  ];
}
function XYResizer({ domNode, nodeId, getStoreItems, onChange, onEnd }) {
  const selection2 = select_default2(domNode);
  let params = {
    controlDirection: getControlDirection("bottom-right"),
    boundaries: {
      minWidth: 0,
      minHeight: 0,
      maxWidth: Number.MAX_VALUE,
      maxHeight: Number.MAX_VALUE
    },
    resizeDirection: void 0,
    keepAspectRatio: false
  };
  function update2({ controlPosition, boundaries, keepAspectRatio, resizeDirection, onResizeStart, onResize, onResizeEnd, shouldResize }) {
    let prevValues = { ...initPrevValues };
    let startValues = { ...initStartValues };
    params = {
      boundaries,
      resizeDirection,
      keepAspectRatio,
      controlDirection: getControlDirection(controlPosition)
    };
    let node = void 0;
    let containerBounds = null;
    let childNodes = [];
    let parentNode = void 0;
    let nodeExtent = void 0;
    let childExtent = void 0;
    let resizeDetected = false;
    const dragHandler = drag_default().on("start", (event) => {
      const { nodeLookup, transform: transform2, snapGrid, snapToGrid, nodeOrigin, paneDomNode } = getStoreItems();
      node = nodeLookup.get(nodeId);
      if (!node) {
        return;
      }
      containerBounds = paneDomNode?.getBoundingClientRect() ?? null;
      const { xSnapped, ySnapped } = getPointerPosition(event.sourceEvent, {
        transform: transform2,
        snapGrid,
        snapToGrid,
        containerBounds
      });
      prevValues = {
        width: node.measured.width ?? 0,
        height: node.measured.height ?? 0,
        x: node.position.x ?? 0,
        y: node.position.y ?? 0
      };
      startValues = {
        ...prevValues,
        pointerX: xSnapped,
        pointerY: ySnapped,
        aspectRatio: prevValues.width / prevValues.height
      };
      parentNode = void 0;
      nodeExtent = isCoordinateExtent(node.extent) ? node.extent : void 0;
      if (node.parentId && (node.extent === "parent" || node.expandParent)) {
        parentNode = nodeLookup.get(node.parentId);
      }
      if (parentNode && node.extent === "parent") {
        nodeExtent = [
          [0, 0],
          [parentNode.measured.width, parentNode.measured.height]
        ];
      }
      childNodes = [];
      childExtent = void 0;
      for (const [childId, child] of nodeLookup) {
        if (child.parentId === nodeId) {
          childNodes.push({
            id: childId,
            position: { ...child.position },
            extent: child.extent
          });
          if (child.extent === "parent" || child.expandParent) {
            const extent = nodeToChildExtent(child, node, child.origin ?? nodeOrigin);
            if (childExtent) {
              childExtent = [
                [Math.min(extent[0][0], childExtent[0][0]), Math.min(extent[0][1], childExtent[0][1])],
                [Math.max(extent[1][0], childExtent[1][0]), Math.max(extent[1][1], childExtent[1][1])]
              ];
            } else {
              childExtent = extent;
            }
          }
        }
      }
      onResizeStart?.(event, { ...prevValues });
    }).on("drag", (event) => {
      const { transform: transform2, snapGrid, snapToGrid, nodeOrigin: storeNodeOrigin } = getStoreItems();
      const pointerPosition = getPointerPosition(event.sourceEvent, {
        transform: transform2,
        snapGrid,
        snapToGrid,
        containerBounds
      });
      const childChanges = [];
      if (!node) {
        return;
      }
      const { x: prevX, y: prevY, width: prevWidth, height: prevHeight } = prevValues;
      const change = {};
      const nodeOrigin = node.origin ?? storeNodeOrigin;
      const { width, height, x: x3, y: y2 } = getDimensionsAfterResize(startValues, params.controlDirection, pointerPosition, params.boundaries, params.keepAspectRatio, nodeOrigin, nodeExtent, childExtent);
      const isWidthChange = width !== prevWidth;
      const isHeightChange = height !== prevHeight;
      const isXPosChange = x3 !== prevX && isWidthChange;
      const isYPosChange = y2 !== prevY && isHeightChange;
      if (!isXPosChange && !isYPosChange && !isWidthChange && !isHeightChange) {
        return;
      }
      if (isXPosChange || isYPosChange || nodeOrigin[0] === 1 || nodeOrigin[1] === 1) {
        change.x = isXPosChange ? x3 : prevValues.x;
        change.y = isYPosChange ? y2 : prevValues.y;
        prevValues.x = change.x;
        prevValues.y = change.y;
        if (childNodes.length > 0) {
          const xChange = x3 - prevX;
          const yChange = y2 - prevY;
          for (const childNode of childNodes) {
            childNode.position = {
              x: childNode.position.x - xChange + nodeOrigin[0] * (width - prevWidth),
              y: childNode.position.y - yChange + nodeOrigin[1] * (height - prevHeight)
            };
            childChanges.push(childNode);
          }
        }
      }
      if (isWidthChange || isHeightChange) {
        change.width = isWidthChange && (!params.resizeDirection || params.resizeDirection === "horizontal") ? width : prevValues.width;
        change.height = isHeightChange && (!params.resizeDirection || params.resizeDirection === "vertical") ? height : prevValues.height;
        prevValues.width = change.width;
        prevValues.height = change.height;
      }
      if (parentNode && node.expandParent) {
        const xLimit = nodeOrigin[0] * (change.width ?? 0);
        if (change.x && change.x < xLimit) {
          prevValues.x = xLimit;
          startValues.x = startValues.x - (change.x - xLimit);
        }
        const yLimit = nodeOrigin[1] * (change.height ?? 0);
        if (change.y && change.y < yLimit) {
          prevValues.y = yLimit;
          startValues.y = startValues.y - (change.y - yLimit);
        }
      }
      const direction = getResizeDirection({
        width: prevValues.width,
        prevWidth,
        height: prevValues.height,
        prevHeight,
        affectsX: params.controlDirection.affectsX,
        affectsY: params.controlDirection.affectsY
      });
      const nextValues = { ...prevValues, direction };
      const callResize = shouldResize?.(event, nextValues);
      if (callResize === false) {
        return;
      }
      resizeDetected = true;
      onResize?.(event, nextValues);
      onChange(change, childChanges);
    }).on("end", (event) => {
      if (!resizeDetected) {
        return;
      }
      onResizeEnd?.(event, { ...prevValues });
      onEnd?.({ ...prevValues });
      resizeDetected = false;
    });
    selection2.call(dragHandler);
  }
  function destroy() {
    selection2.on(".drag", null);
  }
  return {
    update: update2,
    destroy
  };
}

// node_modules/zustand/esm/traditional.mjs
var import_react3 = __toESM(require("react"), 1);
var import_with_selector = __toESM(require_with_selector(), 1);

// node_modules/zustand/esm/vanilla.mjs
var import_meta = {};
var createStoreImpl = (createState) => {
  let state;
  const listeners2 = /* @__PURE__ */ new Set();
  const setState = (partial, replace) => {
    const nextState = typeof partial === "function" ? partial(state) : partial;
    if (!Object.is(nextState, state)) {
      const previousState = state;
      state = (replace != null ? replace : typeof nextState !== "object" || nextState === null) ? nextState : Object.assign({}, state, nextState);
      listeners2.forEach((listener) => listener(state, previousState));
    }
  };
  const getState = () => state;
  const getInitialState2 = () => initialState;
  const subscribe = (listener) => {
    listeners2.add(listener);
    return () => listeners2.delete(listener);
  };
  const destroy = () => {
    if ((import_meta.env ? import_meta.env.MODE : void 0) !== "production") {
      console.warn(
        "[DEPRECATED] The `destroy` method will be unsupported in a future version. Instead use unsubscribe function returned by subscribe. Everything will be garbage-collected if store is garbage-collected."
      );
    }
    listeners2.clear();
  };
  const api = { setState, getState, getInitialState: getInitialState2, subscribe, destroy };
  const initialState = state = createState(setState, getState, api);
  return api;
};
var createStore = (createState) => createState ? createStoreImpl(createState) : createStoreImpl;

// node_modules/zustand/esm/traditional.mjs
var { useDebugValue } = import_react3.default;
var { useSyncExternalStoreWithSelector } = import_with_selector.default;
var identity3 = (arg) => arg;
function useStoreWithEqualityFn(api, selector = identity3, equalityFn) {
  const slice = useSyncExternalStoreWithSelector(
    api.subscribe,
    api.getState,
    api.getServerState || api.getInitialState,
    selector,
    equalityFn
  );
  useDebugValue(slice);
  return slice;
}
var createWithEqualityFnImpl = (createState, defaultEqualityFn) => {
  const api = createStore(createState);
  const useBoundStoreWithEqualityFn = (selector, equalityFn = defaultEqualityFn) => useStoreWithEqualityFn(api, selector, equalityFn);
  Object.assign(useBoundStoreWithEqualityFn, api);
  return useBoundStoreWithEqualityFn;
};
var createWithEqualityFn = (createState, defaultEqualityFn) => createState ? createWithEqualityFnImpl(createState, defaultEqualityFn) : createWithEqualityFnImpl;

// node_modules/zustand/esm/shallow.mjs
function shallow$1(objA, objB) {
  if (Object.is(objA, objB)) {
    return true;
  }
  if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) {
    return false;
  }
  if (objA instanceof Map && objB instanceof Map) {
    if (objA.size !== objB.size) return false;
    for (const [key, value] of objA) {
      if (!Object.is(value, objB.get(key))) {
        return false;
      }
    }
    return true;
  }
  if (objA instanceof Set && objB instanceof Set) {
    if (objA.size !== objB.size) return false;
    for (const value of objA) {
      if (!objB.has(value)) {
        return false;
      }
    }
    return true;
  }
  const keysA = Object.keys(objA);
  if (keysA.length !== Object.keys(objB).length) {
    return false;
  }
  for (const keyA of keysA) {
    if (!Object.prototype.hasOwnProperty.call(objB, keyA) || !Object.is(objA[keyA], objB[keyA])) {
      return false;
    }
  }
  return true;
}

// node_modules/@xyflow/react/dist/esm/index.js
var import_react_dom = require("react-dom");
var StoreContext = (0, import_react4.createContext)(null);
var Provider$1 = StoreContext.Provider;
var zustandErrorMessage = errorMessages["error001"]("react");
function useStore(selector, equalityFn) {
  const store = (0, import_react4.useContext)(StoreContext);
  if (store === null) {
    throw new Error(zustandErrorMessage);
  }
  return useStoreWithEqualityFn(store, selector, equalityFn);
}
function useStoreApi() {
  const store = (0, import_react4.useContext)(StoreContext);
  if (store === null) {
    throw new Error(zustandErrorMessage);
  }
  return (0, import_react4.useMemo)(() => ({
    getState: store.getState,
    setState: store.setState,
    subscribe: store.subscribe
  }), [store]);
}
var style = { display: "none" };
var ariaLiveStyle = {
  position: "absolute",
  width: 1,
  height: 1,
  margin: -1,
  border: 0,
  padding: 0,
  overflow: "hidden",
  clip: "rect(0px, 0px, 0px, 0px)",
  clipPath: "inset(100%)"
};
var ARIA_NODE_DESC_KEY = "react-flow__node-desc";
var ARIA_EDGE_DESC_KEY = "react-flow__edge-desc";
var ARIA_LIVE_MESSAGE = "react-flow__aria-live";
var ariaLiveSelector = (s) => s.ariaLiveMessage;
var ariaLabelConfigSelector = (s) => s.ariaLabelConfig;
function AriaLiveMessage({ rfId }) {
  const ariaLiveMessage = useStore(ariaLiveSelector);
  return (0, import_jsx_runtime3.jsx)("div", { id: `${ARIA_LIVE_MESSAGE}-${rfId}`, "aria-live": "assertive", "aria-atomic": "true", style: ariaLiveStyle, children: ariaLiveMessage });
}
function A11yDescriptions({ rfId, disableKeyboardA11y }) {
  const ariaLabelConfig = useStore(ariaLabelConfigSelector);
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [(0, import_jsx_runtime3.jsx)("div", { id: `${ARIA_NODE_DESC_KEY}-${rfId}`, style, children: disableKeyboardA11y ? ariaLabelConfig["node.a11yDescription.default"] : ariaLabelConfig["node.a11yDescription.keyboardDisabled"] }), (0, import_jsx_runtime3.jsx)("div", { id: `${ARIA_EDGE_DESC_KEY}-${rfId}`, style, children: ariaLabelConfig["edge.a11yDescription.default"] }), !disableKeyboardA11y && (0, import_jsx_runtime3.jsx)(AriaLiveMessage, { rfId })] });
}
var Panel = (0, import_react4.forwardRef)(({ position = "top-left", children: children2, className, style: style2, ...rest }, ref) => {
  const positionClasses = `${position}`.split("-");
  return (0, import_jsx_runtime3.jsx)("div", { className: cc(["react-flow__panel", className, ...positionClasses]), style: style2, ref, ...rest, children: children2 });
});
Panel.displayName = "Panel";
var link = `https://reactflow.dev${false ? "?utm_source=attribution" : "/attribution"}`;
function Attribution({ proOptions, position = "bottom-right" }) {
  if (proOptions?.hideAttribution) {
    return null;
  }
  return (0, import_jsx_runtime3.jsx)(Panel, { position, className: "react-flow__attribution", "data-message": `Please only hide this attribution when you are subscribed to React Flow Pro: ${link}`, children: (0, import_jsx_runtime3.jsx)("a", { href: link, target: "_blank", rel: "noopener noreferrer", "aria-label": "React Flow attribution", children: "React Flow" }) });
}
var selector$l = (s) => {
  const selectedNodes = [];
  const selectedEdges = [];
  for (const [, node] of s.nodeLookup) {
    if (node.selected) {
      selectedNodes.push(node.internals.userNode);
    }
  }
  for (const [, edge] of s.edgeLookup) {
    if (edge.selected) {
      selectedEdges.push(edge);
    }
  }
  return { selectedNodes, selectedEdges };
};
var selectId = (obj) => obj.id;
function areEqual$1(a, b) {
  return shallow$1(a.selectedNodes.map(selectId), b.selectedNodes.map(selectId)) && shallow$1(a.selectedEdges.map(selectId), b.selectedEdges.map(selectId));
}
function SelectionListenerInner({ onSelectionChange }) {
  const store = useStoreApi();
  const { selectedNodes, selectedEdges } = useStore(selector$l, areEqual$1);
  (0, import_react4.useEffect)(() => {
    const params = { nodes: selectedNodes, edges: selectedEdges };
    onSelectionChange?.(params);
    store.getState().onSelectionChangeHandlers.forEach((fn) => fn(params));
  }, [selectedNodes, selectedEdges, onSelectionChange]);
  return null;
}
var changeSelector = (s) => !!s.onSelectionChangeHandlers;
function SelectionListener({ onSelectionChange }) {
  const storeHasSelectionChangeHandlers = useStore(changeSelector);
  if (onSelectionChange || storeHasSelectionChangeHandlers) {
    return (0, import_jsx_runtime3.jsx)(SelectionListenerInner, { onSelectionChange });
  }
  return null;
}
var defaultNodeOrigin = [0, 0];
var defaultViewport = { x: 0, y: 0, zoom: 1 };
var reactFlowFieldsToTrack = [
  "nodes",
  "edges",
  "defaultNodes",
  "defaultEdges",
  "onConnect",
  "onConnectStart",
  "onConnectEnd",
  "onClickConnectStart",
  "onClickConnectEnd",
  "nodesDraggable",
  "autoPanOnNodeFocus",
  "nodesConnectable",
  "nodesFocusable",
  "edgesFocusable",
  "edgesReconnectable",
  "elevateNodesOnSelect",
  "elevateEdgesOnSelect",
  "minZoom",
  "maxZoom",
  "nodeExtent",
  "onNodesChange",
  "onEdgesChange",
  "elementsSelectable",
  "connectionMode",
  "snapGrid",
  "snapToGrid",
  "translateExtent",
  "connectOnClick",
  "defaultEdgeOptions",
  "fitView",
  "fitViewOptions",
  "onNodesDelete",
  "onEdgesDelete",
  "onDelete",
  "onNodeDrag",
  "onNodeDragStart",
  "onNodeDragStop",
  "onSelectionDrag",
  "onSelectionDragStart",
  "onSelectionDragStop",
  "onMoveStart",
  "onMove",
  "onMoveEnd",
  "noPanClassName",
  "nodeOrigin",
  "autoPanOnConnect",
  "autoPanOnNodeDrag",
  "onError",
  "connectionRadius",
  "isValidConnection",
  "selectNodesOnDrag",
  "nodeDragThreshold",
  "connectionDragThreshold",
  "onBeforeDelete",
  "debug",
  "autoPanSpeed",
  "ariaLabelConfig",
  "zIndexMode"
];
var fieldsToTrack = [...reactFlowFieldsToTrack, "rfId"];
var selector$k = (s) => ({
  setNodes: s.setNodes,
  setEdges: s.setEdges,
  setMinZoom: s.setMinZoom,
  setMaxZoom: s.setMaxZoom,
  setTranslateExtent: s.setTranslateExtent,
  setNodeExtent: s.setNodeExtent,
  reset: s.reset,
  setDefaultNodesAndEdges: s.setDefaultNodesAndEdges
});
var initPrevValues2 = {
  /*
   * these are values that are also passed directly to other components
   * than the StoreUpdater. We can reduce the number of setStore calls
   * by setting the same values here as prev fields.
   */
  translateExtent: infiniteExtent,
  nodeOrigin: defaultNodeOrigin,
  minZoom: 0.5,
  maxZoom: 2,
  elementsSelectable: true,
  noPanClassName: "nopan",
  rfId: "1"
};
function StoreUpdater(props) {
  const { setNodes, setEdges, setMinZoom, setMaxZoom, setTranslateExtent, setNodeExtent, reset, setDefaultNodesAndEdges } = useStore(selector$k, shallow$1);
  const store = useStoreApi();
  (0, import_react4.useEffect)(() => {
    setDefaultNodesAndEdges(props.defaultNodes, props.defaultEdges);
    return () => {
      previousFields.current = initPrevValues2;
      reset();
    };
  }, []);
  const previousFields = (0, import_react4.useRef)(initPrevValues2);
  (0, import_react4.useEffect)(
    () => {
      for (const fieldName of fieldsToTrack) {
        const fieldValue = props[fieldName];
        const previousFieldValue = previousFields.current[fieldName];
        if (fieldValue === previousFieldValue)
          continue;
        if (typeof props[fieldName] === "undefined")
          continue;
        if (fieldName === "nodes")
          setNodes(fieldValue);
        else if (fieldName === "edges")
          setEdges(fieldValue);
        else if (fieldName === "minZoom")
          setMinZoom(fieldValue);
        else if (fieldName === "maxZoom")
          setMaxZoom(fieldValue);
        else if (fieldName === "translateExtent")
          setTranslateExtent(fieldValue);
        else if (fieldName === "nodeExtent")
          setNodeExtent(fieldValue);
        else if (fieldName === "ariaLabelConfig")
          store.setState({ ariaLabelConfig: mergeAriaLabelConfig(fieldValue) });
        else if (fieldName === "fitView")
          store.setState({ fitViewQueued: fieldValue });
        else if (fieldName === "fitViewOptions")
          store.setState({ fitViewOptions: fieldValue });
        else
          store.setState({ [fieldName]: fieldValue });
      }
      previousFields.current = props;
    },
    // Only re-run the effect if one of the fields we track changes
    fieldsToTrack.map((fieldName) => props[fieldName])
  );
  return null;
}
function getMediaQuery() {
  if (typeof window === "undefined" || !window.matchMedia) {
    return null;
  }
  return window.matchMedia("(prefers-color-scheme: dark)");
}
function useColorModeClass(colorMode) {
  const [colorModeClass, setColorModeClass] = (0, import_react4.useState)(colorMode === "system" ? null : colorMode);
  (0, import_react4.useEffect)(() => {
    if (colorMode !== "system") {
      setColorModeClass(colorMode);
      return;
    }
    const mediaQuery = getMediaQuery();
    const updateColorModeClass = () => setColorModeClass(mediaQuery?.matches ? "dark" : "light");
    updateColorModeClass();
    mediaQuery?.addEventListener("change", updateColorModeClass);
    return () => {
      mediaQuery?.removeEventListener("change", updateColorModeClass);
    };
  }, [colorMode]);
  return colorModeClass !== null ? colorModeClass : getMediaQuery()?.matches ? "dark" : "light";
}
var defaultDoc = typeof document !== "undefined" ? document : null;
function useKeyPress(keyCode = null, options = { target: defaultDoc, actInsideInputWithModifier: true }) {
  const [keyPressed, setKeyPressed] = (0, import_react4.useState)(false);
  const modifierPressed = (0, import_react4.useRef)(false);
  const pressedKeys = (0, import_react4.useRef)(/* @__PURE__ */ new Set([]));
  const [keyCodes, keysToWatch] = (0, import_react4.useMemo)(() => {
    if (keyCode !== null) {
      const keyCodeArr = Array.isArray(keyCode) ? keyCode : [keyCode];
      const keys = keyCodeArr.filter((kc) => typeof kc === "string").map((kc) => kc.replace(/\+/g, "\n").replace("\n\n", "\n+").split("\n"));
      const keysFlat = keys.reduce((res, item3) => res.concat(...item3), []);
      return [keys, keysFlat];
    }
    return [[], []];
  }, [keyCode]);
  (0, import_react4.useEffect)(() => {
    const target = options?.target ?? defaultDoc;
    const actInsideInputWithModifier = options?.actInsideInputWithModifier ?? true;
    if (keyCode !== null) {
      const downHandler = (event) => {
        modifierPressed.current = event.ctrlKey || event.metaKey || event.shiftKey || event.altKey;
        const preventAction = (!modifierPressed.current || modifierPressed.current && !actInsideInputWithModifier) && isInputDOMNode(event);
        if (preventAction) {
          return false;
        }
        const keyOrCode = useKeyOrCode(event.code, keysToWatch);
        pressedKeys.current.add(event[keyOrCode]);
        if (isMatchingKey(keyCodes, pressedKeys.current, false)) {
          const target2 = event.composedPath?.()?.[0] || event.target;
          const isInteractiveElement = target2?.nodeName === "BUTTON" || target2?.nodeName === "A";
          if (options.preventDefault !== false && (modifierPressed.current || !isInteractiveElement)) {
            event.preventDefault();
          }
          setKeyPressed(true);
        }
      };
      const upHandler = (event) => {
        const keyOrCode = useKeyOrCode(event.code, keysToWatch);
        if (isMatchingKey(keyCodes, pressedKeys.current, true)) {
          setKeyPressed(false);
          pressedKeys.current.clear();
        } else {
          pressedKeys.current.delete(event[keyOrCode]);
        }
        if (event.key === "Meta") {
          pressedKeys.current.clear();
        }
        modifierPressed.current = false;
      };
      const resetHandler = () => {
        pressedKeys.current.clear();
        setKeyPressed(false);
      };
      target?.addEventListener("keydown", downHandler);
      target?.addEventListener("keyup", upHandler);
      window.addEventListener("blur", resetHandler);
      window.addEventListener("contextmenu", resetHandler);
      return () => {
        target?.removeEventListener("keydown", downHandler);
        target?.removeEventListener("keyup", upHandler);
        window.removeEventListener("blur", resetHandler);
        window.removeEventListener("contextmenu", resetHandler);
      };
    }
  }, [keyCode, setKeyPressed]);
  return keyPressed;
}
function isMatchingKey(keyCodes, pressedKeys, isUp) {
  return keyCodes.filter((keys) => isUp || keys.length === pressedKeys.size).some((keys) => keys.every((k2) => pressedKeys.has(k2)));
}
function useKeyOrCode(eventCode, keysToWatch) {
  return keysToWatch.includes(eventCode) ? "code" : "key";
}
var useViewportHelper = () => {
  const store = useStoreApi();
  return (0, import_react4.useMemo)(() => {
    return {
      zoomIn: async (options) => {
        const { panZoom } = store.getState();
        return panZoom ? panZoom.scaleBy(1.2, options) : false;
      },
      zoomOut: async (options) => {
        const { panZoom } = store.getState();
        return panZoom ? panZoom.scaleBy(1 / 1.2, options) : false;
      },
      zoomTo: async (zoomLevel, options) => {
        const { panZoom } = store.getState();
        return panZoom ? panZoom.scaleTo(zoomLevel, options) : false;
      },
      getZoom: () => store.getState().transform[2],
      setViewport: async (viewport, options) => {
        const { transform: [tX, tY, tZoom], panZoom } = store.getState();
        if (!panZoom) {
          return false;
        }
        await panZoom.setViewport({
          x: viewport.x ?? tX,
          y: viewport.y ?? tY,
          zoom: viewport.zoom ?? tZoom
        }, options);
        return true;
      },
      getViewport: () => {
        const [x3, y2, zoom] = store.getState().transform;
        return { x: x3, y: y2, zoom };
      },
      setCenter: async (x3, y2, options) => {
        return store.getState().setCenter(x3, y2, options);
      },
      fitBounds: async (bounds, options) => {
        const { width, height, minZoom, maxZoom, panZoom } = store.getState();
        const viewport = getViewportForBounds(bounds, width, height, minZoom, maxZoom, options?.padding ?? 0.1);
        if (!panZoom) {
          return false;
        }
        await panZoom.setViewport(viewport, {
          duration: options?.duration,
          ease: options?.ease,
          interpolate: options?.interpolate
        });
        return true;
      },
      screenToFlowPosition: (clientPosition, options = {}) => {
        const { transform: transform2, snapGrid, snapToGrid, domNode } = store.getState();
        if (!domNode) {
          return clientPosition;
        }
        const { x: domX, y: domY } = domNode.getBoundingClientRect();
        const correctedPosition = {
          x: clientPosition.x - domX,
          y: clientPosition.y - domY
        };
        const _snapGrid = options.snapGrid ?? snapGrid;
        const _snapToGrid = options.snapToGrid ?? snapToGrid;
        return pointToRendererPoint(correctedPosition, transform2, _snapToGrid, _snapGrid);
      },
      flowToScreenPosition: (flowPosition) => {
        const { transform: transform2, domNode } = store.getState();
        if (!domNode) {
          return flowPosition;
        }
        const { x: domX, y: domY } = domNode.getBoundingClientRect();
        const rendererPosition = rendererPointToPoint(flowPosition, transform2);
        return {
          x: rendererPosition.x + domX,
          y: rendererPosition.y + domY
        };
      }
    };
  }, []);
};
function applyChanges(changes, elements) {
  const updatedElements = [];
  const changesMap = /* @__PURE__ */ new Map();
  const addItemChanges = [];
  for (const change of changes) {
    if (change.type === "add") {
      addItemChanges.push(change);
      continue;
    } else if (change.type === "remove" || change.type === "replace") {
      changesMap.set(change.id, [change]);
    } else {
      const elementChanges = changesMap.get(change.id);
      if (elementChanges) {
        elementChanges.push(change);
      } else {
        changesMap.set(change.id, [change]);
      }
    }
  }
  for (const element of elements) {
    const changes2 = changesMap.get(element.id);
    if (!changes2) {
      updatedElements.push(element);
      continue;
    }
    if (changes2[0].type === "remove") {
      continue;
    }
    if (changes2[0].type === "replace") {
      updatedElements.push({ ...changes2[0].item });
      continue;
    }
    const updatedElement = { ...element };
    for (const change of changes2) {
      applyChange(change, updatedElement);
    }
    updatedElements.push(updatedElement);
  }
  if (addItemChanges.length) {
    addItemChanges.forEach((change) => {
      if (change.index !== void 0) {
        updatedElements.splice(change.index, 0, { ...change.item });
      } else {
        updatedElements.push({ ...change.item });
      }
    });
  }
  return updatedElements;
}
function applyChange(change, element) {
  switch (change.type) {
    case "select": {
      element.selected = change.selected;
      break;
    }
    case "position": {
      if (typeof change.position !== "undefined") {
        element.position = change.position;
      }
      if (typeof change.dragging !== "undefined") {
        element.dragging = change.dragging;
      }
      break;
    }
    case "dimensions": {
      if (typeof change.dimensions !== "undefined") {
        element.measured = {
          ...change.dimensions
        };
        if (change.setAttributes) {
          if (change.setAttributes === true || change.setAttributes === "width") {
            element.width = change.dimensions.width;
          }
          if (change.setAttributes === true || change.setAttributes === "height") {
            element.height = change.dimensions.height;
          }
        }
      }
      if (typeof change.resizing === "boolean") {
        element.resizing = change.resizing;
      }
      break;
    }
  }
}
function applyNodeChanges(changes, nodes) {
  return applyChanges(changes, nodes);
}
function applyEdgeChanges(changes, edges) {
  return applyChanges(changes, edges);
}
function createSelectionChange(id2, selected2) {
  return {
    id: id2,
    type: "select",
    selected: selected2
  };
}
function getSelectionChanges(items, selectedIds = /* @__PURE__ */ new Set(), mutateItem = false) {
  const changes = [];
  for (const [id2, item3] of items) {
    const willBeSelected = selectedIds.has(id2);
    if (!(item3.selected === void 0 && !willBeSelected) && item3.selected !== willBeSelected) {
      if (mutateItem) {
        item3.selected = willBeSelected;
      }
      changes.push(createSelectionChange(item3.id, willBeSelected));
    }
  }
  return changes;
}
function getElementsDiffChanges({ items = [], lookup }) {
  const changes = [];
  const itemsLookup = new Map(items.map((item3) => [item3.id, item3]));
  for (const [index2, item3] of items.entries()) {
    const lookupItem = lookup.get(item3.id);
    const storeItem = lookupItem?.internals?.userNode ?? lookupItem;
    if (storeItem !== void 0 && storeItem !== item3) {
      changes.push({ id: item3.id, item: item3, type: "replace" });
    }
    if (storeItem === void 0) {
      changes.push({ item: item3, type: "add", index: index2 });
    }
  }
  for (const [id2] of lookup) {
    const nextNode = itemsLookup.get(id2);
    if (nextNode === void 0) {
      changes.push({ id: id2, type: "remove" });
    }
  }
  return changes;
}
function elementToRemoveChange(item3) {
  return {
    id: item3.id,
    type: "remove"
  };
}
var defaultOnError = createDevWarn("React Flow", "https://reactflow.dev/");
function addEdge2(edgeParams, edges, options = {}) {
  return addEdge(edgeParams, edges, {
    ...options,
    onError: options.onError ?? defaultOnError
  });
}
function reconnectEdge2(oldEdge, newConnection, edges, options = { shouldReplaceId: true }) {
  return reconnectEdge(oldEdge, newConnection, edges, {
    ...options,
    onError: options.onError ?? defaultOnError
  });
}
var isNode = (element) => isNodeBase(element);
var isEdge = (element) => isEdgeBase(element);
function fixedForwardRef(render) {
  return (0, import_react4.forwardRef)(render);
}
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? import_react4.useLayoutEffect : import_react4.useEffect;
function useQueue(runQueue) {
  const [serial, setSerial] = (0, import_react4.useState)(BigInt(0));
  const [queue] = (0, import_react4.useState)(() => createQueue(() => setSerial((n2) => n2 + BigInt(1))));
  useIsomorphicLayoutEffect(() => {
    const queueItems = queue.get();
    if (queueItems.length) {
      runQueue(queueItems);
      queue.reset();
    }
  }, [serial]);
  return queue;
}
function createQueue(cb) {
  let queue = [];
  return {
    get: () => queue,
    reset: () => {
      queue = [];
    },
    push: (item3) => {
      queue.push(item3);
      cb();
    }
  };
}
var BatchContext = (0, import_react4.createContext)(null);
function BatchProvider({ children: children2 }) {
  const store = useStoreApi();
  const nodeQueueHandler = (0, import_react4.useCallback)((queueItems) => {
    const { nodes = [], setNodes, hasDefaultNodes, onNodesChange, nodeLookup, fitViewQueued, onNodesChangeMiddlewareMap } = store.getState();
    let next = nodes;
    for (const payload of queueItems) {
      next = typeof payload === "function" ? payload(next) : payload;
    }
    let changes = getElementsDiffChanges({
      items: next,
      lookup: nodeLookup
    });
    for (const middleware of onNodesChangeMiddlewareMap.values()) {
      changes = middleware(changes);
    }
    if (hasDefaultNodes) {
      setNodes(next);
    }
    if (changes.length > 0) {
      onNodesChange?.(changes);
    } else if (fitViewQueued) {
      window.requestAnimationFrame(() => {
        const { fitViewQueued: fitViewQueued2, nodes: nodes2, setNodes: setNodes2 } = store.getState();
        if (fitViewQueued2) {
          setNodes2(nodes2);
        }
      });
    }
  }, []);
  const nodeQueue = useQueue(nodeQueueHandler);
  const edgeQueueHandler = (0, import_react4.useCallback)((queueItems) => {
    const { edges = [], setEdges, hasDefaultEdges, onEdgesChange, edgeLookup } = store.getState();
    let next = edges;
    for (const payload of queueItems) {
      next = typeof payload === "function" ? payload(next) : payload;
    }
    if (hasDefaultEdges) {
      setEdges(next);
    } else if (onEdgesChange) {
      onEdgesChange(getElementsDiffChanges({
        items: next,
        lookup: edgeLookup
      }));
    }
  }, []);
  const edgeQueue = useQueue(edgeQueueHandler);
  const value = (0, import_react4.useMemo)(() => ({ nodeQueue, edgeQueue }), []);
  return (0, import_jsx_runtime3.jsx)(BatchContext.Provider, { value, children: children2 });
}
function useBatchContext() {
  const batchContext = (0, import_react4.useContext)(BatchContext);
  if (!batchContext) {
    throw new Error("useBatchContext must be used within a BatchProvider");
  }
  return batchContext;
}
var selector$j = (s) => !!s.panZoom;
function useReactFlow() {
  const viewportHelper = useViewportHelper();
  const store = useStoreApi();
  const batchContext = useBatchContext();
  const viewportInitialized = useStore(selector$j);
  const generalHelper = (0, import_react4.useMemo)(() => {
    const getInternalNode = (id2) => store.getState().nodeLookup.get(id2);
    const setNodes = (payload) => {
      batchContext.nodeQueue.push(payload);
    };
    const setEdges = (payload) => {
      batchContext.edgeQueue.push(payload);
    };
    const getNodeRect = (node) => {
      const { nodeLookup, nodeOrigin } = store.getState();
      const nodeToUse = isNode(node) ? node : nodeLookup.get(node.id);
      const position = nodeToUse.parentId ? evaluateAbsolutePosition(nodeToUse.position, nodeToUse.measured, nodeToUse.parentId, nodeLookup, nodeOrigin) : nodeToUse.position;
      const nodeWithPosition = {
        ...nodeToUse,
        position,
        width: nodeToUse.measured?.width ?? nodeToUse.width,
        height: nodeToUse.measured?.height ?? nodeToUse.height
      };
      return nodeToRect(nodeWithPosition);
    };
    const updateNode = (id2, nodeUpdate, options = { replace: false }) => {
      setNodes((prevNodes) => prevNodes.map((node) => {
        if (node.id === id2) {
          const nextNode = typeof nodeUpdate === "function" ? nodeUpdate(node) : nodeUpdate;
          return options.replace && isNode(nextNode) ? nextNode : { ...node, ...nextNode };
        }
        return node;
      }));
    };
    const updateEdge = (id2, edgeUpdate, options = { replace: false }) => {
      setEdges((prevEdges) => prevEdges.map((edge) => {
        if (edge.id === id2) {
          const nextEdge = typeof edgeUpdate === "function" ? edgeUpdate(edge) : edgeUpdate;
          return options.replace && isEdge(nextEdge) ? nextEdge : { ...edge, ...nextEdge };
        }
        return edge;
      }));
    };
    return {
      getNodes: () => store.getState().nodes.map((n2) => ({ ...n2 })),
      getNode: (id2) => getInternalNode(id2)?.internals.userNode,
      getInternalNode,
      getEdges: () => {
        const { edges = [] } = store.getState();
        return edges.map((e) => ({ ...e }));
      },
      getEdge: (id2) => store.getState().edgeLookup.get(id2),
      setNodes,
      setEdges,
      addNodes: (payload) => {
        const newNodes = Array.isArray(payload) ? payload : [payload];
        batchContext.nodeQueue.push((nodes) => [...nodes, ...newNodes]);
      },
      addEdges: (payload) => {
        const newEdges = Array.isArray(payload) ? payload : [payload];
        batchContext.edgeQueue.push((edges) => [...edges, ...newEdges]);
      },
      toObject: () => {
        const { nodes = [], edges = [], transform: transform2 } = store.getState();
        const [x3, y2, zoom] = transform2;
        return {
          nodes: nodes.map((n2) => ({ ...n2 })),
          edges: edges.map((e) => ({ ...e })),
          viewport: {
            x: x3,
            y: y2,
            zoom
          }
        };
      },
      deleteElements: async ({ nodes: nodesToRemove = [], edges: edgesToRemove = [] }) => {
        const { nodes, edges, onNodesDelete, onEdgesDelete, triggerNodeChanges, triggerEdgeChanges, onDelete, onBeforeDelete } = store.getState();
        const { nodes: matchingNodes, edges: matchingEdges } = await getElementsToRemove({
          nodesToRemove,
          edgesToRemove,
          nodes,
          edges,
          onBeforeDelete
        });
        const hasMatchingEdges = matchingEdges.length > 0;
        const hasMatchingNodes = matchingNodes.length > 0;
        if (hasMatchingEdges) {
          const edgeChanges = matchingEdges.map(elementToRemoveChange);
          onEdgesDelete?.(matchingEdges);
          triggerEdgeChanges(edgeChanges);
        }
        if (hasMatchingNodes) {
          const nodeChanges = matchingNodes.map(elementToRemoveChange);
          onNodesDelete?.(matchingNodes);
          triggerNodeChanges(nodeChanges);
        }
        if (hasMatchingNodes || hasMatchingEdges) {
          onDelete?.({ nodes: matchingNodes, edges: matchingEdges });
        }
        return { deletedNodes: matchingNodes, deletedEdges: matchingEdges };
      },
      /**
       * Partial is defined as "the 2 nodes/areas are intersecting partially".
       * If a is contained in b or b is contained in a, they are both
       * considered fully intersecting.
       */
      getIntersectingNodes: (nodeOrRect, partially = true, nodes) => {
        const isRect = isRectObject(nodeOrRect);
        const nodeRect = isRect ? nodeOrRect : getNodeRect(nodeOrRect);
        const hasNodesOption = nodes !== void 0;
        if (!nodeRect) {
          return [];
        }
        return (nodes || store.getState().nodes).filter((n2) => {
          const internalNode = store.getState().nodeLookup.get(n2.id);
          if (internalNode && !isRect && (n2.id === nodeOrRect.id || !internalNode.internals.positionAbsolute)) {
            return false;
          }
          const currNodeRect = nodeToRect(hasNodesOption ? n2 : internalNode);
          const overlappingArea = getOverlappingArea(currNodeRect, nodeRect);
          const partiallyVisible = partially && overlappingArea > 0;
          return partiallyVisible || overlappingArea >= currNodeRect.width * currNodeRect.height || overlappingArea >= nodeRect.width * nodeRect.height;
        });
      },
      isNodeIntersecting: (nodeOrRect, area, partially = true) => {
        const isRect = isRectObject(nodeOrRect);
        const nodeRect = isRect ? nodeOrRect : getNodeRect(nodeOrRect);
        if (!nodeRect) {
          return false;
        }
        const overlappingArea = getOverlappingArea(nodeRect, area);
        const partiallyVisible = partially && overlappingArea > 0;
        return partiallyVisible || overlappingArea >= area.width * area.height || overlappingArea >= nodeRect.width * nodeRect.height;
      },
      updateNode,
      updateNodeData: (id2, dataUpdate, options = { replace: false }) => {
        updateNode(id2, (node) => {
          const nextData = typeof dataUpdate === "function" ? dataUpdate(node) : dataUpdate;
          return options.replace ? { ...node, data: nextData } : { ...node, data: { ...node.data, ...nextData } };
        }, options);
      },
      updateEdge,
      updateEdgeData: (id2, dataUpdate, options = { replace: false }) => {
        updateEdge(id2, (edge) => {
          const nextData = typeof dataUpdate === "function" ? dataUpdate(edge) : dataUpdate;
          return options.replace ? { ...edge, data: nextData } : { ...edge, data: { ...edge.data, ...nextData } };
        }, options);
      },
      getNodesBounds: (nodes) => {
        const { nodeLookup, nodeOrigin } = store.getState();
        return getNodesBounds(nodes, { nodeLookup, nodeOrigin });
      },
      getHandleConnections: ({ type, id: id2, nodeId }) => Array.from(store.getState().connectionLookup.get(`${nodeId}-${type}${id2 ? `-${id2}` : ""}`)?.values() ?? []),
      getNodeConnections: ({ type, handleId, nodeId }) => Array.from(store.getState().connectionLookup.get(`${nodeId}${type ? handleId ? `-${type}-${handleId}` : `-${type}` : ""}`)?.values() ?? []),
      fitView: async (options) => {
        const fitViewResolver = store.getState().fitViewResolver ?? withResolvers();
        store.setState({ fitViewQueued: true, fitViewOptions: options, fitViewResolver });
        batchContext.nodeQueue.push((nodes) => [...nodes]);
        return fitViewResolver.promise;
      }
    };
  }, []);
  return (0, import_react4.useMemo)(() => {
    return {
      ...generalHelper,
      ...viewportHelper,
      viewportInitialized
    };
  }, [viewportInitialized]);
}
var selected = (item3) => item3.selected;
var win$1 = typeof window !== "undefined" ? window : void 0;
function useGlobalKeyHandler({ deleteKeyCode, multiSelectionKeyCode }) {
  const store = useStoreApi();
  const { deleteElements } = useReactFlow();
  const deleteKeyPressed = useKeyPress(deleteKeyCode, { actInsideInputWithModifier: false });
  const multiSelectionKeyPressed = useKeyPress(multiSelectionKeyCode, { target: win$1 });
  (0, import_react4.useEffect)(() => {
    if (deleteKeyPressed) {
      const { edges, nodes } = store.getState();
      deleteElements({ nodes: nodes.filter(selected), edges: edges.filter(selected) });
      store.setState({ nodesSelectionActive: false });
    }
  }, [deleteKeyPressed]);
  (0, import_react4.useEffect)(() => {
    store.setState({ multiSelectionActive: multiSelectionKeyPressed });
  }, [multiSelectionKeyPressed]);
}
function useResizeHandler(domNode) {
  const store = useStoreApi();
  (0, import_react4.useEffect)(() => {
    const updateDimensions = () => {
      if (!domNode.current || !(domNode.current.checkVisibility?.() ?? true)) {
        return false;
      }
      const size = getDimensions(domNode.current);
      if (size.height === 0 || size.width === 0) {
        store.getState().onError?.("004", errorMessages["error004"]());
      }
      store.setState({ width: size.width || 500, height: size.height || 500 });
    };
    if (domNode.current) {
      updateDimensions();
      window.addEventListener("resize", updateDimensions);
      const resizeObserver = new ResizeObserver(() => updateDimensions());
      resizeObserver.observe(domNode.current);
      return () => {
        window.removeEventListener("resize", updateDimensions);
        if (resizeObserver && domNode.current) {
          resizeObserver.unobserve(domNode.current);
        }
      };
    }
  }, []);
}
var containerStyle = {
  position: "absolute",
  width: "100%",
  height: "100%",
  top: 0,
  left: 0
};
var selector$i = (s) => ({
  userSelectionActive: s.userSelectionActive,
  lib: s.lib,
  connectionInProgress: s.connection.inProgress
});
function ZoomPane({ onPaneContextMenu, zoomOnScroll = true, zoomOnPinch = true, panOnScroll = false, panActivationKeyPressed, panOnScrollSpeed = 0.5, panOnScrollMode = PanOnScrollMode.Free, zoomOnDoubleClick = true, panOnDrag = true, defaultViewport: defaultViewport2, translateExtent, minZoom, maxZoom, zoomActivationKeyCode, preventScrolling = true, children: children2, noWheelClassName, noPanClassName, onViewportChange, isControlledViewport, paneClickDistance, selectionOnDrag }) {
  const store = useStoreApi();
  const zoomPane = (0, import_react4.useRef)(null);
  const { userSelectionActive, lib, connectionInProgress } = useStore(selector$i, shallow$1);
  const zoomActivationKeyPressed = useKeyPress(zoomActivationKeyCode);
  const panZoom = (0, import_react4.useRef)();
  useResizeHandler(zoomPane);
  const onTransformChange = (0, import_react4.useCallback)((transform2) => {
    onViewportChange?.({ x: transform2[0], y: transform2[1], zoom: transform2[2] });
    if (!isControlledViewport) {
      store.setState({ transform: transform2 });
    }
  }, [onViewportChange, isControlledViewport]);
  (0, import_react4.useEffect)(() => {
    if (zoomPane.current) {
      panZoom.current = XYPanZoom({
        domNode: zoomPane.current,
        minZoom,
        maxZoom,
        translateExtent,
        viewport: defaultViewport2,
        onDraggingChange: (paneDragging) => store.setState((prevState) => prevState.paneDragging === paneDragging ? prevState : { paneDragging }),
        onPanZoomStart: (event, vp) => {
          const { onViewportChangeStart, onMoveStart } = store.getState();
          onMoveStart?.(event, vp);
          onViewportChangeStart?.(vp);
        },
        onPanZoom: (event, vp) => {
          const { onViewportChange: onViewportChange2, onMove } = store.getState();
          onMove?.(event, vp);
          onViewportChange2?.(vp);
        },
        onPanZoomEnd: (event, vp) => {
          const { onViewportChangeEnd, onMoveEnd } = store.getState();
          onMoveEnd?.(event, vp);
          onViewportChangeEnd?.(vp);
        }
      });
      const { x: x3, y: y2, zoom } = panZoom.current.getViewport();
      store.setState({
        panZoom: panZoom.current,
        transform: [x3, y2, zoom],
        domNode: zoomPane.current.closest(".react-flow")
      });
      return () => {
        panZoom.current?.destroy();
      };
    }
  }, []);
  (0, import_react4.useEffect)(() => {
    panZoom.current?.update({
      onPaneContextMenu,
      zoomOnScroll,
      zoomOnPinch,
      panOnScroll,
      panActivationKeyPressed,
      panOnScrollSpeed,
      panOnScrollMode,
      zoomOnDoubleClick,
      panOnDrag,
      zoomActivationKeyPressed,
      preventScrolling,
      noPanClassName,
      userSelectionActive,
      noWheelClassName,
      lib,
      onTransformChange,
      connectionInProgress,
      selectionOnDrag,
      paneClickDistance
    });
  }, [
    onPaneContextMenu,
    zoomOnScroll,
    zoomOnPinch,
    panOnScroll,
    panActivationKeyPressed,
    panOnScrollSpeed,
    panOnScrollMode,
    zoomOnDoubleClick,
    panOnDrag,
    zoomActivationKeyPressed,
    preventScrolling,
    noPanClassName,
    userSelectionActive,
    noWheelClassName,
    lib,
    onTransformChange,
    connectionInProgress,
    selectionOnDrag,
    paneClickDistance
  ]);
  return (0, import_jsx_runtime3.jsx)("div", { className: "react-flow__renderer", ref: zoomPane, style: containerStyle, children: children2 });
}
var selector$h = (s) => ({
  userSelectionActive: s.userSelectionActive,
  userSelectionRect: s.userSelectionRect
});
function UserSelection() {
  const { userSelectionActive, userSelectionRect } = useStore(selector$h, shallow$1);
  const isActive = userSelectionActive && userSelectionRect;
  if (!isActive) {
    return null;
  }
  return (0, import_jsx_runtime3.jsx)("div", { className: "react-flow__selection react-flow__container", style: {
    width: userSelectionRect.width,
    height: userSelectionRect.height,
    transform: `translate(${userSelectionRect.x}px, ${userSelectionRect.y}px)`
  } });
}
var wrapHandler = (handler, containerRef) => {
  return (event) => {
    if (event.target !== containerRef.current) {
      return;
    }
    handler?.(event);
  };
};
var selector$g = (s) => ({
  userSelectionActive: s.userSelectionActive,
  elementsSelectable: s.elementsSelectable,
  dragging: s.paneDragging,
  panBy: s.panBy,
  autoPanSpeed: s.autoPanSpeed
});
function Pane({ isSelecting, selectionKeyPressed, selectionMode = SelectionMode.Full, panOnDrag, autoPanOnSelection, paneClickDistance, selectionOnDrag, onSelectionStart, onSelectionEnd, onPaneClick, onPaneContextMenu, onPaneScroll, onPaneMouseEnter, onPaneMouseMove, onPaneMouseLeave, children: children2 }) {
  const autoPanId = (0, import_react4.useRef)(0);
  const store = useStoreApi();
  const { userSelectionActive, elementsSelectable, dragging, panBy: panBy2, autoPanSpeed } = useStore(selector$g, shallow$1);
  const isSelectionEnabled = elementsSelectable && (isSelecting || userSelectionActive);
  const container = (0, import_react4.useRef)(null);
  const containerBounds = (0, import_react4.useRef)();
  const selectedNodeIds = (0, import_react4.useRef)(/* @__PURE__ */ new Set());
  const selectedEdgeIds = (0, import_react4.useRef)(/* @__PURE__ */ new Set());
  const connectionEndedOnPane = (0, import_react4.useRef)(false);
  const selectionInProgress = (0, import_react4.useRef)(false);
  const position = (0, import_react4.useRef)({ x: 0, y: 0 });
  const autoPanStarted = (0, import_react4.useRef)(false);
  const onClick = (event) => {
    if (selectionInProgress.current || connectionEndedOnPane.current || store.getState().connection.inProgress) {
      selectionInProgress.current = false;
      connectionEndedOnPane.current = false;
      return;
    }
    onPaneClick?.(event);
    store.getState().resetSelectedElements();
    store.setState({ nodesSelectionActive: false });
  };
  const onContextMenu = (event) => {
    if (Array.isArray(panOnDrag) && panOnDrag?.includes(2)) {
      event.preventDefault();
      return;
    }
    onPaneContextMenu?.(event);
  };
  const onWheel = onPaneScroll ? (event) => onPaneScroll(event) : void 0;
  const onClickCapture = (event) => {
    if (selectionInProgress.current) {
      event.stopPropagation();
      selectionInProgress.current = false;
    }
  };
  const onPointerDownCapture = (event) => {
    if (event.pointerType === "touch" && panOnDrag !== false && !selectionKeyPressed) {
      return;
    }
    const { domNode, transform: transform2 } = store.getState();
    containerBounds.current = domNode?.getBoundingClientRect();
    if (!containerBounds.current)
      return;
    const eventTargetIsContainer = event.target === container.current;
    const isNoKeyEvent = !eventTargetIsContainer && !!event.target.closest(".nokey");
    const isSelectionActive = selectionOnDrag && eventTargetIsContainer || selectionKeyPressed;
    if (isNoKeyEvent || !isSelecting || !isSelectionActive || event.button !== 0 || !event.isPrimary) {
      return;
    }
    event.target?.setPointerCapture?.(event.pointerId);
    selectionInProgress.current = false;
    const { x: x3, y: y2 } = getEventPosition(event.nativeEvent, containerBounds.current);
    const userSelectionStartPosition = pointToRendererPoint({ x: x3, y: y2 }, transform2);
    store.setState({
      userSelectionRect: {
        width: 0,
        height: 0,
        startX: userSelectionStartPosition.x,
        startY: userSelectionStartPosition.y,
        x: x3,
        y: y2
      }
    });
    if (!eventTargetIsContainer) {
      event.stopPropagation();
      event.preventDefault();
    }
  };
  function commitUserSelectionRect(mouseX, mouseY) {
    const { userSelectionRect } = store.getState();
    if (!userSelectionRect) {
      return;
    }
    const { transform: transform2, nodeLookup, edgeLookup, connectionLookup, triggerNodeChanges, triggerEdgeChanges, defaultEdgeOptions } = store.getState();
    const userStartPosition = { x: userSelectionRect.startX, y: userSelectionRect.startY };
    const { x: screenStartX, y: screenStartY } = rendererPointToPoint(userStartPosition, transform2);
    const nextUserSelectRect = {
      startX: userStartPosition.x,
      startY: userStartPosition.y,
      x: mouseX < screenStartX ? mouseX : screenStartX,
      y: mouseY < screenStartY ? mouseY : screenStartY,
      width: Math.abs(mouseX - screenStartX),
      height: Math.abs(mouseY - screenStartY)
    };
    const prevSelectedNodeIds = selectedNodeIds.current;
    const prevSelectedEdgeIds = selectedEdgeIds.current;
    selectedNodeIds.current = new Set(getNodesInside(nodeLookup, nextUserSelectRect, transform2, selectionMode === SelectionMode.Partial, true).map((node) => node.id));
    selectedEdgeIds.current = /* @__PURE__ */ new Set();
    const edgesSelectable = defaultEdgeOptions?.selectable ?? true;
    for (const nodeId of selectedNodeIds.current) {
      const connections = connectionLookup.get(nodeId);
      if (!connections)
        continue;
      for (const { edgeId } of connections.values()) {
        const edge = edgeLookup.get(edgeId);
        if (edge && (edge.selectable ?? edgesSelectable)) {
          selectedEdgeIds.current.add(edgeId);
        }
      }
    }
    if (!areSetsEqual(prevSelectedNodeIds, selectedNodeIds.current)) {
      const changes = getSelectionChanges(nodeLookup, selectedNodeIds.current, true);
      triggerNodeChanges(changes);
    }
    if (!areSetsEqual(prevSelectedEdgeIds, selectedEdgeIds.current)) {
      const changes = getSelectionChanges(edgeLookup, selectedEdgeIds.current);
      triggerEdgeChanges(changes);
    }
    store.setState({
      userSelectionRect: nextUserSelectRect,
      userSelectionActive: true,
      nodesSelectionActive: false
    });
  }
  function autoPan() {
    if (!autoPanOnSelection || !containerBounds.current) {
      return;
    }
    const [x3, y2] = calcAutoPan(position.current, containerBounds.current, autoPanSpeed);
    panBy2({ x: x3, y: y2 }).then((panned) => {
      if (!selectionInProgress.current || !panned) {
        autoPanId.current = requestAnimationFrame(autoPan);
        return;
      }
      const { x: mx, y: my } = position.current;
      commitUserSelectionRect(mx, my);
      autoPanId.current = requestAnimationFrame(autoPan);
    });
  }
  const cleanupAutoPan = () => {
    cancelAnimationFrame(autoPanId.current);
    autoPanId.current = 0;
    autoPanStarted.current = false;
  };
  (0, import_react4.useEffect)(() => {
    return () => cleanupAutoPan();
  }, []);
  const onPointerMove = (event) => {
    const { userSelectionRect, transform: transform2, resetSelectedElements } = store.getState();
    if (!containerBounds.current || !userSelectionRect) {
      return;
    }
    const { x: mouseX, y: mouseY } = getEventPosition(event.nativeEvent, containerBounds.current);
    position.current = { x: mouseX, y: mouseY };
    const screenStart = rendererPointToPoint({ x: userSelectionRect.startX, y: userSelectionRect.startY }, transform2);
    if (!selectionInProgress.current) {
      const requiredDistance = selectionKeyPressed ? 0 : paneClickDistance;
      const distance2 = Math.hypot(mouseX - screenStart.x, mouseY - screenStart.y);
      if (distance2 <= requiredDistance) {
        return;
      }
      resetSelectedElements();
      onSelectionStart?.(event);
    }
    selectionInProgress.current = true;
    if (!autoPanStarted.current) {
      autoPan();
      autoPanStarted.current = true;
    }
    commitUserSelectionRect(mouseX, mouseY);
  };
  const onPointerUp = (event) => {
    if (!isSelectionEnabled) {
      if (event.target === container.current && store.getState().connection.inProgress) {
        connectionEndedOnPane.current = true;
      }
      return;
    }
    if (event.button !== 0) {
      return;
    }
    event.target?.releasePointerCapture?.(event.pointerId);
    if (!userSelectionActive && event.target === container.current && store.getState().userSelectionRect) {
      onClick?.(event);
    }
    store.setState({
      userSelectionActive: false,
      userSelectionRect: null
    });
    if (selectionInProgress.current) {
      onSelectionEnd?.(event);
      store.setState({
        nodesSelectionActive: selectedNodeIds.current.size > 0
      });
    }
    cleanupAutoPan();
  };
  const onPointerCancel = (event) => {
    event.target?.releasePointerCapture?.(event.pointerId);
    cleanupAutoPan();
  };
  const draggable = panOnDrag === true || Array.isArray(panOnDrag) && panOnDrag.includes(0);
  return (0, import_jsx_runtime3.jsxs)("div", { className: cc(["react-flow__pane", { draggable, dragging, selection: isSelecting }]), onClick: isSelectionEnabled ? void 0 : wrapHandler(onClick, container), onContextMenu: wrapHandler(onContextMenu, container), onWheel: wrapHandler(onWheel, container), onPointerEnter: isSelectionEnabled ? void 0 : onPaneMouseEnter, onPointerMove: isSelectionEnabled ? onPointerMove : onPaneMouseMove, onPointerUp, onPointerCancel: isSelectionEnabled ? onPointerCancel : void 0, onPointerDownCapture: isSelectionEnabled ? onPointerDownCapture : void 0, onClickCapture: isSelectionEnabled ? onClickCapture : void 0, onPointerLeave: onPaneMouseLeave, ref: container, style: containerStyle, children: [children2, (0, import_jsx_runtime3.jsx)(UserSelection, {})] });
}
function handleNodeClick({ id: id2, store, unselect = false, nodeRef }) {
  const { addSelectedNodes, unselectNodesAndEdges, multiSelectionActive, nodeLookup, onError } = store.getState();
  const node = nodeLookup.get(id2);
  if (!node) {
    onError?.("012", errorMessages["error012"](id2));
    return;
  }
  store.setState({ nodesSelectionActive: false });
  if (!node.selected) {
    addSelectedNodes([id2]);
  } else if (unselect || node.selected && multiSelectionActive) {
    unselectNodesAndEdges({ nodes: [node], edges: [] });
    requestAnimationFrame(() => nodeRef?.current?.blur());
  }
}
function useDrag({ nodeRef, disabled = false, noDragClassName, handleSelector, nodeId, isSelectable, nodeClickDistance }) {
  const store = useStoreApi();
  const [dragging, setDragging] = (0, import_react4.useState)(false);
  const xyDrag = (0, import_react4.useRef)();
  (0, import_react4.useEffect)(() => {
    if (disabled) {
      return;
    }
    xyDrag.current = XYDrag({
      getStoreItems: () => store.getState(),
      onNodeMouseDown: (id2) => {
        handleNodeClick({
          id: id2,
          store,
          nodeRef
        });
      },
      onDragStart: () => {
        setDragging(true);
      },
      onDragStop: () => {
        setDragging(false);
      }
    });
    return () => {
      xyDrag.current?.destroy();
      xyDrag.current = void 0;
    };
  }, [disabled, store, nodeRef]);
  (0, import_react4.useEffect)(() => {
    if (disabled || !nodeRef.current || !xyDrag.current) {
      return;
    }
    xyDrag.current.update({
      noDragClassName,
      handleSelector,
      domNode: nodeRef.current,
      isSelectable,
      nodeId,
      nodeClickDistance
    });
  }, [noDragClassName, handleSelector, disabled, isSelectable, nodeRef, nodeId, nodeClickDistance]);
  return dragging;
}
var selectedAndDraggable = (nodesDraggable) => (n2) => n2.selected && (n2.draggable || nodesDraggable && typeof n2.draggable === "undefined");
function useMoveSelectedNodes() {
  const store = useStoreApi();
  const moveSelectedNodes = (0, import_react4.useCallback)((params) => {
    const { nodeExtent, snapToGrid, snapGrid, nodesDraggable, onError, updateNodePositions, nodeLookup, nodeOrigin } = store.getState();
    const nodeUpdates = /* @__PURE__ */ new Map();
    const isSelected = selectedAndDraggable(nodesDraggable);
    const xVelo = snapToGrid ? snapGrid[0] : 5;
    const yVelo = snapToGrid ? snapGrid[1] : 5;
    const xDiff = params.direction.x * xVelo * params.factor;
    const yDiff = params.direction.y * yVelo * params.factor;
    for (const [, node] of nodeLookup) {
      if (!isSelected(node)) {
        continue;
      }
      let nextPosition = {
        x: node.internals.positionAbsolute.x + xDiff,
        y: node.internals.positionAbsolute.y + yDiff
      };
      if (snapToGrid) {
        nextPosition = snapPosition(nextPosition, snapGrid);
      }
      const { position, positionAbsolute } = calculateNodePosition({
        nodeId: node.id,
        nextPosition,
        nodeLookup,
        nodeExtent,
        nodeOrigin,
        onError
      });
      node.position = position;
      node.internals.positionAbsolute = positionAbsolute;
      nodeUpdates.set(node.id, node);
    }
    updateNodePositions(nodeUpdates);
  }, []);
  return moveSelectedNodes;
}
var NodeIdContext = (0, import_react4.createContext)(null);
var Provider = NodeIdContext.Provider;
NodeIdContext.Consumer;
var useNodeId = () => {
  const nodeId = (0, import_react4.useContext)(NodeIdContext);
  return nodeId;
};
var selector$f = (s) => ({
  connectOnClick: s.connectOnClick,
  noPanClassName: s.noPanClassName,
  rfId: s.rfId
});
var HandleConfigContext = (0, import_react4.createContext)(null);
function HandleConfigProvider({ children: children2 }) {
  const config = useStore(selector$f, shallow$1);
  return (0, import_jsx_runtime3.jsx)(HandleConfigContext.Provider, { value: config, children: children2 });
}
function useHandleConfig() {
  const config = (0, import_react4.useContext)(HandleConfigContext);
  if (!config) {
    throw new Error("useHandleConfig must be used within a HandleConfigProvider");
  }
  return config;
}
var idleConnectingState = {
  connectingFrom: false,
  connectingTo: false,
  clickConnecting: false,
  isPossibleEndHandle: true,
  connectionInProcess: false,
  clickConnectionInProcess: false,
  valid: false
};
var connectingSelector = (nodeId, handleId, type) => (state) => {
  const { connectionClickStartHandle: clickHandle, connectionMode, connection } = state;
  const { fromHandle, toHandle, isValid } = connection;
  if (!fromHandle && !clickHandle) {
    return idleConnectingState;
  }
  const connectingTo = toHandle?.nodeId === nodeId && toHandle?.id === handleId && toHandle?.type === type;
  return {
    connectingFrom: fromHandle?.nodeId === nodeId && fromHandle?.id === handleId && fromHandle?.type === type,
    connectingTo,
    clickConnecting: clickHandle?.nodeId === nodeId && clickHandle?.id === handleId && clickHandle?.type === type,
    isPossibleEndHandle: connectionMode === ConnectionMode.Strict ? fromHandle?.type !== type : nodeId !== fromHandle?.nodeId || handleId !== fromHandle?.id,
    connectionInProcess: !!fromHandle,
    clickConnectionInProcess: !!clickHandle,
    valid: connectingTo && isValid
  };
};
function HandleComponent({ type = "source", position = Position.Top, isValidConnection, isConnectable = true, isConnectableStart = true, isConnectableEnd = true, id: id2, onConnect, children: children2, className, onMouseDown, onTouchStart, ...rest }, ref) {
  const handleId = id2 || null;
  const isTarget = type === "target";
  const store = useStoreApi();
  const nodeId = useNodeId();
  const { connectOnClick, noPanClassName, rfId } = useHandleConfig();
  const { connectingFrom, connectingTo, clickConnecting, isPossibleEndHandle, connectionInProcess, clickConnectionInProcess, valid } = useStore(connectingSelector(nodeId, handleId, type), shallow$1);
  if (!nodeId) {
    store.getState().onError?.("010", errorMessages["error010"]());
  }
  const onConnectExtended = (params) => {
    const { defaultEdgeOptions, onConnect: onConnectAction, hasDefaultEdges } = store.getState();
    const edgeParams = {
      ...defaultEdgeOptions,
      ...params
    };
    if (hasDefaultEdges) {
      const { edges, setEdges, onError } = store.getState();
      setEdges(addEdge2(edgeParams, edges, { onError }));
    }
    onConnectAction?.(edgeParams);
    onConnect?.(edgeParams);
  };
  const onPointerDown2 = (event) => {
    if (!nodeId) {
      return;
    }
    const isMouseTriggered = isMouseEvent(event.nativeEvent);
    if (isConnectableStart && (isMouseTriggered && event.button === 0 || !isMouseTriggered)) {
      const currentStore = store.getState();
      XYHandle.onPointerDown(event.nativeEvent, {
        handleDomNode: event.currentTarget,
        autoPanOnConnect: currentStore.autoPanOnConnect,
        connectionMode: currentStore.connectionMode,
        connectionRadius: currentStore.connectionRadius,
        domNode: currentStore.domNode,
        nodeLookup: currentStore.nodeLookup,
        lib: currentStore.lib,
        isTarget,
        handleId,
        nodeId,
        flowId: currentStore.rfId,
        panBy: currentStore.panBy,
        cancelConnection: currentStore.cancelConnection,
        onConnectStart: currentStore.onConnectStart,
        onConnectEnd: (...args) => store.getState().onConnectEnd?.(...args),
        updateConnection: currentStore.updateConnection,
        onConnect: onConnectExtended,
        isValidConnection: isValidConnection || ((...args) => store.getState().isValidConnection?.(...args) ?? true),
        getTransform: () => store.getState().transform,
        getFromHandle: () => store.getState().connection.fromHandle,
        autoPanSpeed: currentStore.autoPanSpeed,
        dragThreshold: currentStore.connectionDragThreshold
      });
    }
    if (isMouseTriggered) {
      onMouseDown?.(event);
    } else {
      onTouchStart?.(event);
    }
  };
  const onClick = (event) => {
    const { onClickConnectStart, onClickConnectEnd, connectionClickStartHandle, connectionMode, isValidConnection: isValidConnectionStore, lib, rfId: flowId, nodeLookup, connection: connectionState } = store.getState();
    if (!nodeId || !connectionClickStartHandle && !isConnectableStart) {
      return;
    }
    if (!connectionClickStartHandle) {
      onClickConnectStart?.(event.nativeEvent, { nodeId, handleId, handleType: type });
      store.setState({ connectionClickStartHandle: { nodeId, type, id: handleId } });
      return;
    }
    const doc = getHostForElement(event.target);
    const isValidConnectionHandler = isValidConnection || isValidConnectionStore;
    const { connection, isValid } = XYHandle.isValid(event.nativeEvent, {
      handle: {
        nodeId,
        id: handleId,
        type
      },
      connectionMode,
      fromNodeId: connectionClickStartHandle.nodeId,
      fromHandleId: connectionClickStartHandle.id || null,
      fromType: connectionClickStartHandle.type,
      isValidConnection: isValidConnectionHandler,
      flowId,
      doc,
      lib,
      nodeLookup
    });
    if (isValid && connection) {
      onConnectExtended(connection);
    }
    const connectionClone = structuredClone(connectionState);
    delete connectionClone.inProgress;
    connectionClone.toPosition = connectionClone.toHandle ? connectionClone.toHandle.position : null;
    onClickConnectEnd?.(event, connectionClone);
    store.setState({ connectionClickStartHandle: null });
  };
  return (0, import_jsx_runtime3.jsx)("div", { "data-handleid": handleId, "data-nodeid": nodeId, "data-handlepos": position, "data-id": `${rfId}-${nodeId}-${handleId}-${type}`, className: cc([
    "react-flow__handle",
    `react-flow__handle-${position}`,
    "nodrag",
    noPanClassName,
    className,
    {
      source: !isTarget,
      target: isTarget,
      connectable: isConnectable,
      connectablestart: isConnectableStart,
      connectableend: isConnectableEnd,
      clickconnecting: clickConnecting,
      connectingfrom: connectingFrom,
      connectingto: connectingTo,
      valid,
      /*
       * shows where you can start a connection from
       * and where you can end it while connecting
       */
      connectionindicator: isConnectable && (!connectionInProcess || isPossibleEndHandle) && (connectionInProcess || clickConnectionInProcess ? isConnectableEnd : isConnectableStart)
    }
  ]), onMouseDown: onPointerDown2, onTouchStart: onPointerDown2, onClick: connectOnClick ? onClick : void 0, ref, ...rest, children: children2 });
}
var Handle = (0, import_react4.memo)(fixedForwardRef(HandleComponent));
function InputNode({ data, isConnectable, sourcePosition = Position.Bottom }) {
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [data?.label, (0, import_jsx_runtime3.jsx)(Handle, { type: "source", position: sourcePosition, isConnectable })] });
}
function DefaultNode({ data, isConnectable, targetPosition = Position.Top, sourcePosition = Position.Bottom }) {
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [(0, import_jsx_runtime3.jsx)(Handle, { type: "target", position: targetPosition, isConnectable }), data?.label, (0, import_jsx_runtime3.jsx)(Handle, { type: "source", position: sourcePosition, isConnectable })] });
}
function GroupNode() {
  return null;
}
function OutputNode({ data, isConnectable, targetPosition = Position.Top }) {
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [(0, import_jsx_runtime3.jsx)(Handle, { type: "target", position: targetPosition, isConnectable }), data?.label] });
}
var arrowKeyDiffs = {
  ArrowUp: { x: 0, y: -1 },
  ArrowDown: { x: 0, y: 1 },
  ArrowLeft: { x: -1, y: 0 },
  ArrowRight: { x: 1, y: 0 }
};
var builtinNodeTypes = {
  input: InputNode,
  default: DefaultNode,
  output: OutputNode,
  group: GroupNode
};
function getNodeInlineStyleDimensions(node) {
  if (node.internals.handleBounds === void 0) {
    return {
      width: node.width ?? node.initialWidth ?? node.style?.width,
      height: node.height ?? node.initialHeight ?? node.style?.height
    };
  }
  return {
    width: node.width ?? node.style?.width,
    height: node.height ?? node.style?.height
  };
}
var selector$e = (s) => {
  const { width, height, x: x3, y: y2 } = getInternalNodesBounds(s.nodeLookup, {
    filter: (node) => !!node.selected
  });
  return {
    width: isNumeric(width) ? width : null,
    height: isNumeric(height) ? height : null,
    userSelectionActive: s.userSelectionActive,
    transformString: `translate(${s.transform[0]}px,${s.transform[1]}px) scale(${s.transform[2]}) translate(${x3}px,${y2}px)`
  };
};
function NodesSelection({ onSelectionContextMenu, noPanClassName, disableKeyboardA11y }) {
  const store = useStoreApi();
  const { width, height, transformString, userSelectionActive } = useStore(selector$e, shallow$1);
  const moveSelectedNodes = useMoveSelectedNodes();
  const nodeRef = (0, import_react4.useRef)(null);
  (0, import_react4.useEffect)(() => {
    if (!disableKeyboardA11y) {
      nodeRef.current?.focus({
        preventScroll: true
      });
    }
  }, [disableKeyboardA11y]);
  const shouldRender = !userSelectionActive && width !== null && height !== null;
  useDrag({
    nodeRef,
    disabled: !shouldRender
  });
  if (!shouldRender) {
    return null;
  }
  const onContextMenu = onSelectionContextMenu ? (event) => {
    const selectedNodes = store.getState().nodes.filter((n2) => n2.selected);
    onSelectionContextMenu(event, selectedNodes);
  } : void 0;
  const onKeyDown = (event) => {
    if (Object.prototype.hasOwnProperty.call(arrowKeyDiffs, event.key)) {
      event.preventDefault();
      moveSelectedNodes({
        direction: arrowKeyDiffs[event.key],
        factor: event.shiftKey ? 4 : 1
      });
    }
  };
  return (0, import_jsx_runtime3.jsx)("div", { className: cc(["react-flow__nodesselection", "react-flow__container", noPanClassName]), style: {
    transform: transformString
  }, children: (0, import_jsx_runtime3.jsx)("div", { ref: nodeRef, className: "react-flow__nodesselection-rect", onContextMenu, tabIndex: disableKeyboardA11y ? void 0 : -1, onKeyDown: disableKeyboardA11y ? void 0 : onKeyDown, style: {
    width,
    height
  } }) });
}
var win = typeof window !== "undefined" ? window : void 0;
var selector$d = (s) => {
  return { nodesSelectionActive: s.nodesSelectionActive, userSelectionActive: s.userSelectionActive };
};
function FlowRendererComponent({ children: children2, onPaneClick, onPaneMouseEnter, onPaneMouseMove, onPaneMouseLeave, onPaneContextMenu, onPaneScroll, paneClickDistance, deleteKeyCode, selectionKeyCode, selectionOnDrag, selectionMode, onSelectionStart, onSelectionEnd, multiSelectionKeyCode, panActivationKeyCode, zoomActivationKeyCode, elementsSelectable, zoomOnScroll, zoomOnPinch, panOnScroll: _panOnScroll, panOnScrollSpeed, panOnScrollMode, zoomOnDoubleClick, panOnDrag: _panOnDrag, autoPanOnSelection, defaultViewport: defaultViewport2, translateExtent, minZoom, maxZoom, preventScrolling, onSelectionContextMenu, noWheelClassName, noPanClassName, disableKeyboardA11y, onViewportChange, isControlledViewport }) {
  const { nodesSelectionActive, userSelectionActive } = useStore(selector$d, shallow$1);
  const selectionKeyPressed = useKeyPress(selectionKeyCode, { target: win });
  const panActivationKeyPressed = useKeyPress(panActivationKeyCode, { target: win });
  const panOnDrag = panActivationKeyPressed || _panOnDrag;
  const panOnScroll = panActivationKeyPressed || _panOnScroll;
  const _selectionOnDrag = selectionOnDrag && panOnDrag !== true;
  const isSelecting = selectionKeyPressed || userSelectionActive || _selectionOnDrag;
  useGlobalKeyHandler({ deleteKeyCode, multiSelectionKeyCode });
  return (0, import_jsx_runtime3.jsx)(ZoomPane, { onPaneContextMenu, elementsSelectable, zoomOnScroll, zoomOnPinch, panOnScroll, panActivationKeyPressed, panOnScrollSpeed, panOnScrollMode, zoomOnDoubleClick, panOnDrag: !selectionKeyPressed && panOnDrag, defaultViewport: defaultViewport2, translateExtent, minZoom, maxZoom, zoomActivationKeyCode, preventScrolling, noWheelClassName, noPanClassName, onViewportChange, isControlledViewport, paneClickDistance, selectionOnDrag: _selectionOnDrag, children: (0, import_jsx_runtime3.jsxs)(Pane, { onSelectionStart, onSelectionEnd, onPaneClick, onPaneMouseEnter, onPaneMouseMove, onPaneMouseLeave, onPaneContextMenu, onPaneScroll, panOnDrag, autoPanOnSelection, isSelecting: !!isSelecting, selectionMode, selectionKeyPressed, paneClickDistance, selectionOnDrag: _selectionOnDrag, children: [children2, nodesSelectionActive && (0, import_jsx_runtime3.jsx)(NodesSelection, { onSelectionContextMenu, noPanClassName, disableKeyboardA11y })] }) });
}
FlowRendererComponent.displayName = "FlowRenderer";
var FlowRenderer = (0, import_react4.memo)(FlowRendererComponent);
var selector$c = (onlyRenderVisible) => (s) => {
  return onlyRenderVisible ? getNodesInside(s.nodeLookup, { x: 0, y: 0, width: s.width, height: s.height }, s.transform, true).map((node) => node.id) : Array.from(s.nodeLookup.keys());
};
function useVisibleNodeIds(onlyRenderVisible) {
  const nodeIds = useStore((0, import_react4.useCallback)(selector$c(onlyRenderVisible), [onlyRenderVisible]), shallow$1);
  return nodeIds;
}
var selector$b = (s) => s.updateNodeInternals;
function useResizeObserver() {
  const updateNodeInternals2 = useStore(selector$b);
  const [resizeObserver] = (0, import_react4.useState)(() => {
    if (typeof ResizeObserver === "undefined") {
      return null;
    }
    return new ResizeObserver((entries) => {
      const updates = /* @__PURE__ */ new Map();
      entries.forEach((entry) => {
        const id2 = entry.target.getAttribute("data-id");
        updates.set(id2, {
          id: id2,
          nodeElement: entry.target,
          force: true
        });
      });
      updateNodeInternals2(updates);
    });
  });
  (0, import_react4.useEffect)(() => {
    return () => {
      resizeObserver?.disconnect();
    };
  }, [resizeObserver]);
  return resizeObserver;
}
function useNodeObserver({ node, nodeType, hasDimensions, resizeObserver }) {
  const store = useStoreApi();
  const nodeRef = (0, import_react4.useRef)(null);
  const observedNode = (0, import_react4.useRef)(null);
  const prevSourcePosition = (0, import_react4.useRef)(node.sourcePosition);
  const prevTargetPosition = (0, import_react4.useRef)(node.targetPosition);
  const prevType = (0, import_react4.useRef)(nodeType);
  const isInitialized = hasDimensions && !!node.internals.handleBounds;
  (0, import_react4.useEffect)(() => {
    if (nodeRef.current && !node.hidden && (!isInitialized || observedNode.current !== nodeRef.current)) {
      if (observedNode.current) {
        resizeObserver?.unobserve(observedNode.current);
      }
      resizeObserver?.observe(nodeRef.current);
      observedNode.current = nodeRef.current;
    }
  }, [isInitialized, node.hidden]);
  (0, import_react4.useEffect)(() => {
    return () => {
      if (observedNode.current) {
        resizeObserver?.unobserve(observedNode.current);
        observedNode.current = null;
      }
    };
  }, []);
  (0, import_react4.useEffect)(() => {
    if (nodeRef.current) {
      const typeChanged = prevType.current !== nodeType;
      const sourcePosChanged = prevSourcePosition.current !== node.sourcePosition;
      const targetPosChanged = prevTargetPosition.current !== node.targetPosition;
      if (typeChanged || sourcePosChanged || targetPosChanged) {
        prevType.current = nodeType;
        prevSourcePosition.current = node.sourcePosition;
        prevTargetPosition.current = node.targetPosition;
        store.getState().updateNodeInternals(/* @__PURE__ */ new Map([[node.id, { id: node.id, nodeElement: nodeRef.current, force: true }]]));
      }
    }
  }, [node.id, nodeType, node.sourcePosition, node.targetPosition]);
  return nodeRef;
}
function NodeWrapper({ id: id2, onClick, onMouseEnter, onMouseMove, onMouseLeave, onContextMenu, onDoubleClick, nodesDraggable, elementsSelectable, nodesConnectable, nodesFocusable, resizeObserver, noDragClassName, noPanClassName, disableKeyboardA11y, rfId, nodeTypes, nodeClickDistance, onError }) {
  const { node, internals, isParent } = useStore((s) => {
    const node2 = s.nodeLookup.get(id2);
    const isParent2 = s.parentLookup.has(id2);
    return {
      node: node2,
      internals: node2.internals,
      isParent: isParent2
    };
  }, shallow$1);
  let nodeType = node.type || "default";
  let NodeComponent = nodeTypes?.[nodeType] || builtinNodeTypes[nodeType];
  if (NodeComponent === void 0) {
    onError?.("003", errorMessages["error003"](nodeType));
    nodeType = "default";
    NodeComponent = nodeTypes?.["default"] || builtinNodeTypes.default;
  }
  const isDraggable = !!(node.draggable || nodesDraggable && typeof node.draggable === "undefined");
  const isSelectable = !!(node.selectable || elementsSelectable && typeof node.selectable === "undefined");
  const isConnectable = !!(node.connectable || nodesConnectable && typeof node.connectable === "undefined");
  const isFocusable = !!(node.focusable || nodesFocusable && typeof node.focusable === "undefined");
  const store = useStoreApi();
  const hasDimensions = nodeHasDimensions(node);
  const nodeRef = useNodeObserver({ node, nodeType, hasDimensions, resizeObserver });
  const dragging = useDrag({
    nodeRef,
    disabled: node.hidden || !isDraggable,
    noDragClassName,
    handleSelector: node.dragHandle,
    nodeId: id2,
    isSelectable,
    nodeClickDistance
  });
  const moveSelectedNodes = useMoveSelectedNodes();
  if (node.hidden) {
    return null;
  }
  const nodeDimensions = getNodeDimensions(node);
  const inlineDimensions = getNodeInlineStyleDimensions(node);
  const hasPointerEvents = isSelectable || isDraggable || onClick || onMouseEnter || onMouseMove || onMouseLeave;
  const onMouseEnterHandler = onMouseEnter ? (event) => onMouseEnter(event, { ...internals.userNode }) : void 0;
  const onMouseMoveHandler = onMouseMove ? (event) => onMouseMove(event, { ...internals.userNode }) : void 0;
  const onMouseLeaveHandler = onMouseLeave ? (event) => onMouseLeave(event, { ...internals.userNode }) : void 0;
  const onContextMenuHandler = onContextMenu ? (event) => onContextMenu(event, { ...internals.userNode }) : void 0;
  const onDoubleClickHandler = onDoubleClick ? (event) => onDoubleClick(event, { ...internals.userNode }) : void 0;
  const onSelectNodeHandler = (event) => {
    const { selectNodesOnDrag, nodeDragThreshold } = store.getState();
    if (isSelectable && (!selectNodesOnDrag || !isDraggable || nodeDragThreshold > 0)) {
      handleNodeClick({
        id: id2,
        store,
        nodeRef
      });
    }
    if (onClick) {
      onClick(event, { ...internals.userNode });
    }
  };
  const onKeyDown = (event) => {
    if (isInputDOMNode(event.nativeEvent) || disableKeyboardA11y) {
      return;
    }
    if (elementSelectionKeys.includes(event.key) && isSelectable) {
      const unselect = event.key === "Escape";
      handleNodeClick({
        id: id2,
        store,
        unselect,
        nodeRef
      });
    } else if (isDraggable && node.selected && Object.prototype.hasOwnProperty.call(arrowKeyDiffs, event.key)) {
      event.preventDefault();
      const { ariaLabelConfig } = store.getState();
      store.setState({
        ariaLiveMessage: ariaLabelConfig["node.a11yDescription.ariaLiveMessage"]({
          direction: event.key.replace("Arrow", "").toLowerCase(),
          x: ~~internals.positionAbsolute.x,
          y: ~~internals.positionAbsolute.y
        })
      });
      moveSelectedNodes({
        direction: arrowKeyDiffs[event.key],
        factor: event.shiftKey ? 4 : 1
      });
    }
  };
  const onFocus = () => {
    if (disableKeyboardA11y || !nodeRef.current?.matches(":focus-visible")) {
      return;
    }
    const { transform: transform2, width, height, autoPanOnNodeFocus, setCenter } = store.getState();
    if (!autoPanOnNodeFocus) {
      return;
    }
    const withinViewport = getNodesInside(/* @__PURE__ */ new Map([[id2, node]]), { x: 0, y: 0, width, height }, transform2, true).length > 0;
    if (!withinViewport) {
      setCenter(node.position.x + nodeDimensions.width / 2, node.position.y + nodeDimensions.height / 2, {
        zoom: transform2[2]
      });
    }
  };
  return (0, import_jsx_runtime3.jsx)("div", { className: cc([
    "react-flow__node",
    `react-flow__node-${nodeType}`,
    {
      // this is overwritable by passing `nopan` as a class name
      [noPanClassName]: isDraggable
    },
    node.className,
    {
      selected: node.selected,
      selectable: isSelectable,
      parent: isParent,
      draggable: isDraggable,
      dragging
    }
  ]), ref: nodeRef, style: {
    zIndex: internals.z,
    transform: `translate(${internals.positionAbsolute.x}px,${internals.positionAbsolute.y}px)`,
    pointerEvents: hasPointerEvents ? "all" : "none",
    visibility: hasDimensions ? "visible" : "hidden",
    ...node.style,
    ...inlineDimensions
  }, "data-id": id2, "data-testid": `rf__node-${id2}`, onMouseEnter: onMouseEnterHandler, onMouseMove: onMouseMoveHandler, onMouseLeave: onMouseLeaveHandler, onContextMenu: onContextMenuHandler, onClick: onSelectNodeHandler, onDoubleClick: onDoubleClickHandler, onKeyDown: isFocusable ? onKeyDown : void 0, tabIndex: isFocusable ? 0 : void 0, onFocus: isFocusable ? onFocus : void 0, role: node.ariaRole ?? (isFocusable ? "group" : void 0), "aria-roledescription": "node", "aria-describedby": disableKeyboardA11y ? void 0 : `${ARIA_NODE_DESC_KEY}-${rfId}`, "aria-label": node.ariaLabel, ...node.domAttributes, children: (0, import_jsx_runtime3.jsx)(Provider, { value: id2, children: (0, import_jsx_runtime3.jsx)(NodeComponent, { id: id2, data: node.data, type: nodeType, positionAbsoluteX: internals.positionAbsolute.x, positionAbsoluteY: internals.positionAbsolute.y, selected: node.selected ?? false, selectable: isSelectable, draggable: isDraggable, deletable: node.deletable ?? true, isConnectable, sourcePosition: node.sourcePosition, targetPosition: node.targetPosition, dragging, dragHandle: node.dragHandle, zIndex: internals.z, parentId: node.parentId, ...nodeDimensions }) }) });
}
var NodeWrapper$1 = (0, import_react4.memo)(NodeWrapper);
var selector$a = (s) => ({
  nodesConnectable: s.nodesConnectable,
  nodesFocusable: s.nodesFocusable,
  elementsSelectable: s.elementsSelectable,
  onError: s.onError
});
function NodeRendererComponent(props) {
  const { nodesConnectable, nodesFocusable, elementsSelectable, onError } = useStore(selector$a, shallow$1);
  const nodeIds = useVisibleNodeIds(props.onlyRenderVisibleElements);
  const resizeObserver = useResizeObserver();
  return (0, import_jsx_runtime3.jsx)("div", { className: "react-flow__nodes", style: containerStyle, children: nodeIds.map((nodeId) => {
    return (
      /*
       * The split of responsibilities between NodeRenderer and
       * NodeComponentWrapper may appear weird. However, it’s designed to
       * minimize the cost of updates when individual nodes change.
       *
       * For example, when you’re dragging a single node, that node gets
       * updated multiple times per second. If `NodeRenderer` were to update
       * every time, it would have to re-run the `nodes.map()` loop every
       * time. This gets pricey with hundreds of nodes, especially if every
       * loop cycle does more than just rendering a JSX element!
       *
       * As a result of this choice, we took the following implementation
       * decisions:
       * - NodeRenderer subscribes *only* to node IDs – and therefore
       *   rerender *only* when visible nodes are added or removed.
       * - NodeRenderer performs all operations the result of which can be
       *   shared between nodes (such as creating the `ResizeObserver`
       *   instance, or subscribing to `selector`). This means extra prop
       *   drilling into `NodeComponentWrapper`, but it means we need to run
       *   these operations only once – instead of once per node.
       * - Any operations that you’d normally write inside `nodes.map` are
       *   moved into `NodeComponentWrapper`. This ensures they are
       *   memorized – so if `NodeRenderer` *has* to rerender, it only
       *   needs to regenerate the list of nodes, nothing else.
       */
      (0, import_jsx_runtime3.jsx)(NodeWrapper$1, { id: nodeId, nodeTypes: props.nodeTypes, nodeExtent: props.nodeExtent, onClick: props.onNodeClick, onMouseEnter: props.onNodeMouseEnter, onMouseMove: props.onNodeMouseMove, onMouseLeave: props.onNodeMouseLeave, onContextMenu: props.onNodeContextMenu, onDoubleClick: props.onNodeDoubleClick, noDragClassName: props.noDragClassName, noPanClassName: props.noPanClassName, rfId: props.rfId, disableKeyboardA11y: props.disableKeyboardA11y, resizeObserver, nodesDraggable: props.nodesDraggable ?? true, nodesConnectable, nodesFocusable, elementsSelectable, nodeClickDistance: props.nodeClickDistance, onError }, nodeId)
    );
  }) });
}
NodeRendererComponent.displayName = "NodeRenderer";
var NodeRenderer = (0, import_react4.memo)(NodeRendererComponent);
function useVisibleEdgeIds(onlyRenderVisible) {
  const edgeIds = useStore((0, import_react4.useCallback)((s) => {
    if (!onlyRenderVisible) {
      return s.edges.map((edge) => edge.id);
    }
    const visibleEdgeIds = [];
    if (s.width && s.height) {
      for (const edge of s.edges) {
        const sourceNode = s.nodeLookup.get(edge.source);
        const targetNode = s.nodeLookup.get(edge.target);
        if (sourceNode && targetNode && isEdgeVisible({
          sourceNode,
          targetNode,
          width: s.width,
          height: s.height,
          transform: s.transform
        })) {
          visibleEdgeIds.push(edge.id);
        }
      }
    }
    return visibleEdgeIds;
  }, [onlyRenderVisible]), shallow$1);
  return edgeIds;
}
var ArrowSymbol = ({ color: color2 = "none", strokeWidth = 1 }) => {
  const style2 = {
    strokeWidth,
    ...color2 && { stroke: color2 }
  };
  return (0, import_jsx_runtime3.jsx)("polyline", { className: "arrow", style: style2, strokeLinecap: "round", fill: "none", strokeLinejoin: "round", points: "-5,-4 0,0 -5,4" });
};
var ArrowClosedSymbol = ({ color: color2 = "none", strokeWidth = 1 }) => {
  const style2 = {
    strokeWidth,
    ...color2 && { stroke: color2, fill: color2 }
  };
  return (0, import_jsx_runtime3.jsx)("polyline", { className: "arrowclosed", style: style2, strokeLinecap: "round", strokeLinejoin: "round", points: "-5,-4 0,0 -5,4 -5,-4" });
};
var MarkerSymbols = {
  [MarkerType.Arrow]: ArrowSymbol,
  [MarkerType.ArrowClosed]: ArrowClosedSymbol
};
function useMarkerSymbol(type) {
  const store = useStoreApi();
  const symbol = (0, import_react4.useMemo)(() => {
    const symbolExists = Object.prototype.hasOwnProperty.call(MarkerSymbols, type);
    if (!symbolExists) {
      store.getState().onError?.("009", errorMessages["error009"](type));
      return null;
    }
    return MarkerSymbols[type];
  }, [type]);
  return symbol;
}
var Marker = ({ id: id2, type, color: color2, width = 12.5, height = 12.5, markerUnits = "strokeWidth", strokeWidth, orient = "auto-start-reverse" }) => {
  const Symbol2 = useMarkerSymbol(type);
  if (!Symbol2) {
    return null;
  }
  return (0, import_jsx_runtime3.jsx)("marker", { className: "react-flow__arrowhead", id: id2, markerWidth: `${width}`, markerHeight: `${height}`, viewBox: "-10 -10 20 20", markerUnits, orient, refX: "0", refY: "0", children: (0, import_jsx_runtime3.jsx)(Symbol2, { color: color2, strokeWidth }) });
};
var MarkerDefinitions = ({ defaultColor, rfId }) => {
  const edges = useStore((s) => s.edges);
  const defaultEdgeOptions = useStore((s) => s.defaultEdgeOptions);
  const markers = (0, import_react4.useMemo)(() => {
    const markers2 = createMarkerIds(edges, {
      id: rfId,
      defaultColor,
      defaultMarkerStart: defaultEdgeOptions?.markerStart,
      defaultMarkerEnd: defaultEdgeOptions?.markerEnd
    });
    return markers2;
  }, [edges, defaultEdgeOptions, rfId, defaultColor]);
  if (!markers.length) {
    return null;
  }
  return (0, import_jsx_runtime3.jsx)("svg", { className: "react-flow__marker", "aria-hidden": "true", children: (0, import_jsx_runtime3.jsx)("defs", { children: markers.map((marker) => (0, import_jsx_runtime3.jsx)(Marker, { id: marker.id, type: marker.type, color: marker.color, width: marker.width, height: marker.height, markerUnits: marker.markerUnits, strokeWidth: marker.strokeWidth, orient: marker.orient }, marker.id)) }) });
};
MarkerDefinitions.displayName = "MarkerDefinitions";
var MarkerDefinitions$1 = (0, import_react4.memo)(MarkerDefinitions);
function EdgeTextComponent({ x: x3, y: y2, label: label2, labelStyle, labelShowBg = true, labelBgStyle, labelBgPadding = [2, 4], labelBgBorderRadius = 2, children: children2, className, ...rest }) {
  const [edgeTextBbox, setEdgeTextBbox] = (0, import_react4.useState)({ x: 1, y: 0, width: 0, height: 0 });
  const edgeTextClasses = cc(["react-flow__edge-textwrapper", className]);
  const edgeTextRef = (0, import_react4.useRef)(null);
  (0, import_react4.useEffect)(() => {
    if (edgeTextRef.current) {
      const textBbox = edgeTextRef.current.getBBox();
      setEdgeTextBbox({
        x: textBbox.x,
        y: textBbox.y,
        width: textBbox.width,
        height: textBbox.height
      });
    }
  }, [label2]);
  if (!label2) {
    return null;
  }
  return (0, import_jsx_runtime3.jsxs)("g", { transform: `translate(${x3 - edgeTextBbox.width / 2} ${y2 - edgeTextBbox.height / 2})`, className: edgeTextClasses, visibility: edgeTextBbox.width ? "visible" : "hidden", ...rest, children: [labelShowBg && (0, import_jsx_runtime3.jsx)("rect", { width: edgeTextBbox.width + 2 * labelBgPadding[0], x: -labelBgPadding[0], y: -labelBgPadding[1], height: edgeTextBbox.height + 2 * labelBgPadding[1], className: "react-flow__edge-textbg", style: labelBgStyle, rx: labelBgBorderRadius, ry: labelBgBorderRadius }), (0, import_jsx_runtime3.jsx)("text", { className: "react-flow__edge-text", y: edgeTextBbox.height / 2, dy: "0.3em", ref: edgeTextRef, style: labelStyle, children: label2 }), children2] });
}
EdgeTextComponent.displayName = "EdgeText";
var EdgeText = (0, import_react4.memo)(EdgeTextComponent);
function BaseEdge({ path, labelX, labelY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, interactionWidth = 20, ...props }) {
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [(0, import_jsx_runtime3.jsx)("path", { ...props, d: path, fill: "none", className: cc(["react-flow__edge-path", props.className]) }), interactionWidth ? (0, import_jsx_runtime3.jsx)("path", { d: path, fill: "none", strokeOpacity: 0, strokeWidth: interactionWidth, className: "react-flow__edge-interaction" }) : null, label2 && isNumeric(labelX) && isNumeric(labelY) ? (0, import_jsx_runtime3.jsx)(EdgeText, { x: labelX, y: labelY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius }) : null] });
}
function getControl({ pos, x1, y1, x2: x22, y2 }) {
  if (pos === Position.Left || pos === Position.Right) {
    return [0.5 * (x1 + x22), y1];
  }
  return [x1, 0.5 * (y1 + y2)];
}
function getSimpleBezierPath({ sourceX, sourceY, sourcePosition = Position.Bottom, targetX, targetY, targetPosition = Position.Top }) {
  const [sourceControlX, sourceControlY] = getControl({
    pos: sourcePosition,
    x1: sourceX,
    y1: sourceY,
    x2: targetX,
    y2: targetY
  });
  const [targetControlX, targetControlY] = getControl({
    pos: targetPosition,
    x1: targetX,
    y1: targetY,
    x2: sourceX,
    y2: sourceY
  });
  const [labelX, labelY, offsetX, offsetY] = getBezierEdgeCenter({
    sourceX,
    sourceY,
    targetX,
    targetY,
    sourceControlX,
    sourceControlY,
    targetControlX,
    targetControlY
  });
  return [
    `M${sourceX},${sourceY} C${sourceControlX},${sourceControlY} ${targetControlX},${targetControlY} ${targetX},${targetY}`,
    labelX,
    labelY,
    offsetX,
    offsetY
  ];
}
function createSimpleBezierEdge(params) {
  return (0, import_react4.memo)(({ id: id2, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, markerEnd, markerStart, interactionWidth }) => {
    const [path, labelX, labelY] = getSimpleBezierPath({
      sourceX,
      sourceY,
      sourcePosition,
      targetX,
      targetY,
      targetPosition
    });
    const _id = params.isInternal ? void 0 : id2;
    return (0, import_jsx_runtime3.jsx)(BaseEdge, { id: _id, path, labelX, labelY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, markerEnd, markerStart, interactionWidth });
  });
}
var SimpleBezierEdge = createSimpleBezierEdge({ isInternal: false });
var SimpleBezierEdgeInternal = createSimpleBezierEdge({ isInternal: true });
SimpleBezierEdge.displayName = "SimpleBezierEdge";
SimpleBezierEdgeInternal.displayName = "SimpleBezierEdgeInternal";
function createSmoothStepEdge(params) {
  return (0, import_react4.memo)(({ id: id2, sourceX, sourceY, targetX, targetY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, sourcePosition = Position.Bottom, targetPosition = Position.Top, markerEnd, markerStart, pathOptions, interactionWidth }) => {
    const [path, labelX, labelY] = getSmoothStepPath({
      sourceX,
      sourceY,
      sourcePosition,
      targetX,
      targetY,
      targetPosition,
      borderRadius: pathOptions?.borderRadius,
      offset: pathOptions?.offset,
      stepPosition: pathOptions?.stepPosition
    });
    const _id = params.isInternal ? void 0 : id2;
    return (0, import_jsx_runtime3.jsx)(BaseEdge, { id: _id, path, labelX, labelY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, markerEnd, markerStart, interactionWidth });
  });
}
var SmoothStepEdge = createSmoothStepEdge({ isInternal: false });
var SmoothStepEdgeInternal = createSmoothStepEdge({ isInternal: true });
SmoothStepEdge.displayName = "SmoothStepEdge";
SmoothStepEdgeInternal.displayName = "SmoothStepEdgeInternal";
function createStepEdge(params) {
  return (0, import_react4.memo)(({ id: id2, ...props }) => {
    const _id = params.isInternal ? void 0 : id2;
    return (0, import_jsx_runtime3.jsx)(SmoothStepEdge, { ...props, id: _id, pathOptions: (0, import_react4.useMemo)(() => ({ borderRadius: 0, offset: props.pathOptions?.offset }), [props.pathOptions?.offset]) });
  });
}
var StepEdge = createStepEdge({ isInternal: false });
var StepEdgeInternal = createStepEdge({ isInternal: true });
StepEdge.displayName = "StepEdge";
StepEdgeInternal.displayName = "StepEdgeInternal";
function createStraightEdge(params) {
  return (0, import_react4.memo)(({ id: id2, sourceX, sourceY, targetX, targetY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, markerEnd, markerStart, interactionWidth }) => {
    const [path, labelX, labelY] = getStraightPath({ sourceX, sourceY, targetX, targetY });
    const _id = params.isInternal ? void 0 : id2;
    return (0, import_jsx_runtime3.jsx)(BaseEdge, { id: _id, path, labelX, labelY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, markerEnd, markerStart, interactionWidth });
  });
}
var StraightEdge = createStraightEdge({ isInternal: false });
var StraightEdgeInternal = createStraightEdge({ isInternal: true });
StraightEdge.displayName = "StraightEdge";
StraightEdgeInternal.displayName = "StraightEdgeInternal";
function createBezierEdge(params) {
  return (0, import_react4.memo)(({ id: id2, sourceX, sourceY, targetX, targetY, sourcePosition = Position.Bottom, targetPosition = Position.Top, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, markerEnd, markerStart, pathOptions, interactionWidth }) => {
    const [path, labelX, labelY] = getBezierPath({
      sourceX,
      sourceY,
      sourcePosition,
      targetX,
      targetY,
      targetPosition,
      curvature: pathOptions?.curvature
    });
    const _id = params.isInternal ? void 0 : id2;
    return (0, import_jsx_runtime3.jsx)(BaseEdge, { id: _id, path, labelX, labelY, label: label2, labelStyle, labelShowBg, labelBgStyle, labelBgPadding, labelBgBorderRadius, style: style2, markerEnd, markerStart, interactionWidth });
  });
}
var BezierEdge = createBezierEdge({ isInternal: false });
var BezierEdgeInternal = createBezierEdge({ isInternal: true });
BezierEdge.displayName = "BezierEdge";
BezierEdgeInternal.displayName = "BezierEdgeInternal";
var builtinEdgeTypes = {
  default: BezierEdgeInternal,
  straight: StraightEdgeInternal,
  step: StepEdgeInternal,
  smoothstep: SmoothStepEdgeInternal,
  simplebezier: SimpleBezierEdgeInternal
};
var nullPosition = {
  sourceX: null,
  sourceY: null,
  targetX: null,
  targetY: null,
  sourcePosition: null,
  targetPosition: null,
  zIndex: void 0
};
var shiftX = (x3, shift, position) => {
  if (position === Position.Left)
    return x3 - shift;
  if (position === Position.Right)
    return x3 + shift;
  return x3;
};
var shiftY = (y2, shift, position) => {
  if (position === Position.Top)
    return y2 - shift;
  if (position === Position.Bottom)
    return y2 + shift;
  return y2;
};
var EdgeUpdaterClassName = "react-flow__edgeupdater";
function EdgeAnchor({ position, centerX, centerY, radius = 10, onMouseDown, onMouseEnter, onMouseOut, type }) {
  return (0, import_jsx_runtime3.jsx)("circle", { onMouseDown, onMouseEnter, onMouseOut, className: cc([EdgeUpdaterClassName, `${EdgeUpdaterClassName}-${type}`]), cx: shiftX(centerX, radius, position), cy: shiftY(centerY, radius, position), r: radius, stroke: "transparent", fill: "transparent" });
}
function EdgeUpdateAnchors({ isReconnectable, reconnectRadius, edge, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, onReconnect, onReconnectStart, onReconnectEnd, setReconnecting, setUpdateHover }) {
  const store = useStoreApi();
  const handleEdgeUpdater = (event, oppositeHandle) => {
    if (event.button !== 0) {
      return;
    }
    const { autoPanOnConnect, domNode, connectionMode, connectionRadius, lib, onConnectStart, cancelConnection, nodeLookup, rfId: flowId, panBy: panBy2, updateConnection } = store.getState();
    const isTarget = oppositeHandle.type === "target";
    const _onReconnectEnd = (evt, connectionState) => {
      setReconnecting(false);
      onReconnectEnd?.(evt, edge, oppositeHandle.type, connectionState);
    };
    const onConnectEdge = (connection) => onReconnect?.(edge, connection);
    const _onConnectStart = (_event, params) => {
      setReconnecting(true);
      onReconnectStart?.(event, edge, oppositeHandle.type);
      onConnectStart?.(_event, params);
    };
    XYHandle.onPointerDown(event.nativeEvent, {
      autoPanOnConnect,
      connectionMode,
      connectionRadius,
      domNode,
      handleId: oppositeHandle.id,
      nodeId: oppositeHandle.nodeId,
      nodeLookup,
      isTarget,
      edgeUpdaterType: oppositeHandle.type,
      lib,
      flowId,
      cancelConnection,
      panBy: panBy2,
      isValidConnection: (...args) => store.getState().isValidConnection?.(...args) ?? true,
      onConnect: onConnectEdge,
      onConnectStart: _onConnectStart,
      onConnectEnd: (...args) => store.getState().onConnectEnd?.(...args),
      onReconnectEnd: _onReconnectEnd,
      updateConnection,
      getTransform: () => store.getState().transform,
      getFromHandle: () => store.getState().connection.fromHandle,
      dragThreshold: store.getState().connectionDragThreshold,
      handleDomNode: event.currentTarget
    });
  };
  const onReconnectSourceMouseDown = (event) => handleEdgeUpdater(event, { nodeId: edge.target, id: edge.targetHandle ?? null, type: "target" });
  const onReconnectTargetMouseDown = (event) => handleEdgeUpdater(event, { nodeId: edge.source, id: edge.sourceHandle ?? null, type: "source" });
  const onReconnectMouseEnter = () => setUpdateHover(true);
  const onReconnectMouseOut = () => setUpdateHover(false);
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [(isReconnectable === true || isReconnectable === "source") && (0, import_jsx_runtime3.jsx)(EdgeAnchor, { position: sourcePosition, centerX: sourceX, centerY: sourceY, radius: reconnectRadius, onMouseDown: onReconnectSourceMouseDown, onMouseEnter: onReconnectMouseEnter, onMouseOut: onReconnectMouseOut, type: "source" }), (isReconnectable === true || isReconnectable === "target") && (0, import_jsx_runtime3.jsx)(EdgeAnchor, { position: targetPosition, centerX: targetX, centerY: targetY, radius: reconnectRadius, onMouseDown: onReconnectTargetMouseDown, onMouseEnter: onReconnectMouseEnter, onMouseOut: onReconnectMouseOut, type: "target" })] });
}
function EdgeWrapper({ id: id2, edgesFocusable, edgesReconnectable, elementsSelectable, onClick, onDoubleClick, onContextMenu, onMouseEnter, onMouseMove, onMouseLeave, reconnectRadius, onReconnect, onReconnectStart, onReconnectEnd, rfId, edgeTypes, noPanClassName, onError, disableKeyboardA11y }) {
  let edge = useStore((s) => s.edgeLookup.get(id2));
  const defaultEdgeOptions = useStore((s) => s.defaultEdgeOptions);
  edge = defaultEdgeOptions ? { ...defaultEdgeOptions, ...edge } : edge;
  let edgeType = edge.type || "default";
  let EdgeComponent = edgeTypes?.[edgeType] || builtinEdgeTypes[edgeType];
  if (EdgeComponent === void 0) {
    onError?.("011", errorMessages["error011"](edgeType));
    edgeType = "default";
    EdgeComponent = edgeTypes?.["default"] || builtinEdgeTypes.default;
  }
  const isFocusable = !!(edge.focusable || edgesFocusable && typeof edge.focusable === "undefined");
  const isReconnectable = typeof onReconnect !== "undefined" && (edge.reconnectable || edgesReconnectable && typeof edge.reconnectable === "undefined");
  const isSelectable = !!(edge.selectable || elementsSelectable && typeof edge.selectable === "undefined");
  const edgeRef = (0, import_react4.useRef)(null);
  const [updateHover, setUpdateHover] = (0, import_react4.useState)(false);
  const [reconnecting, setReconnecting] = (0, import_react4.useState)(false);
  const store = useStoreApi();
  const { zIndex = edge.zIndex, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition } = useStore((0, import_react4.useCallback)((store2) => {
    const sourceNode = store2.nodeLookup.get(edge.source);
    const targetNode = store2.nodeLookup.get(edge.target);
    if (!sourceNode || !targetNode) {
      return nullPosition;
    }
    const edgePosition = getEdgePosition({
      id: id2,
      sourceNode,
      targetNode,
      sourceHandle: edge.sourceHandle || null,
      targetHandle: edge.targetHandle || null,
      connectionMode: store2.connectionMode,
      onError
    });
    const zIndex2 = getElevatedEdgeZIndex({
      selected: edge.selected,
      zIndex: edge.zIndex,
      sourceNode,
      targetNode,
      elevateOnSelect: store2.elevateEdgesOnSelect,
      zIndexMode: store2.zIndexMode
    });
    return {
      ...edgePosition || nullPosition,
      zIndex: zIndex2
    };
  }, [edge.source, edge.target, edge.sourceHandle, edge.targetHandle, edge.selected, edge.zIndex, onError]), shallow$1);
  const markerStartUrl = (0, import_react4.useMemo)(() => edge.markerStart ? `url('#${getMarkerId(edge.markerStart, rfId)}')` : void 0, [edge.markerStart, rfId]);
  const markerEndUrl = (0, import_react4.useMemo)(() => edge.markerEnd ? `url('#${getMarkerId(edge.markerEnd, rfId)}')` : void 0, [edge.markerEnd, rfId]);
  if (edge.hidden || sourceX === null || sourceY === null || targetX === null || targetY === null) {
    return null;
  }
  const onEdgeClick = (event) => {
    const { addSelectedEdges, unselectNodesAndEdges, multiSelectionActive } = store.getState();
    if (isSelectable) {
      store.setState({ nodesSelectionActive: false });
      if (edge.selected && multiSelectionActive) {
        unselectNodesAndEdges({ nodes: [], edges: [edge] });
        edgeRef.current?.blur();
      } else {
        addSelectedEdges([id2]);
      }
    }
    if (onClick) {
      onClick(event, edge);
    }
  };
  const onEdgeDoubleClick = onDoubleClick ? (event) => {
    onDoubleClick(event, { ...edge });
  } : void 0;
  const onEdgeContextMenu = onContextMenu ? (event) => {
    onContextMenu(event, { ...edge });
  } : void 0;
  const onEdgeMouseEnter = onMouseEnter ? (event) => {
    onMouseEnter(event, { ...edge });
  } : void 0;
  const onEdgeMouseMove = onMouseMove ? (event) => {
    onMouseMove(event, { ...edge });
  } : void 0;
  const onEdgeMouseLeave = onMouseLeave ? (event) => {
    onMouseLeave(event, { ...edge });
  } : void 0;
  const onKeyDown = (event) => {
    if (!disableKeyboardA11y && elementSelectionKeys.includes(event.key) && isSelectable) {
      const { unselectNodesAndEdges, addSelectedEdges } = store.getState();
      const unselect = event.key === "Escape";
      if (unselect) {
        edgeRef.current?.blur();
        unselectNodesAndEdges({ edges: [edge] });
      } else {
        addSelectedEdges([id2]);
      }
    }
  };
  return (0, import_jsx_runtime3.jsx)("svg", { style: { zIndex }, children: (0, import_jsx_runtime3.jsxs)("g", { className: cc([
    "react-flow__edge",
    `react-flow__edge-${edgeType}`,
    edge.className,
    noPanClassName,
    {
      selected: edge.selected,
      animated: edge.animated,
      inactive: !isSelectable && !onClick,
      updating: updateHover,
      selectable: isSelectable
    }
  ]), onClick: onEdgeClick, onDoubleClick: onEdgeDoubleClick, onContextMenu: onEdgeContextMenu, onMouseEnter: onEdgeMouseEnter, onMouseMove: onEdgeMouseMove, onMouseLeave: onEdgeMouseLeave, onKeyDown: isFocusable ? onKeyDown : void 0, tabIndex: isFocusable ? 0 : void 0, role: edge.ariaRole ?? (isFocusable ? "group" : "img"), "aria-roledescription": "edge", "data-id": id2, "data-testid": `rf__edge-${id2}`, "aria-label": edge.ariaLabel === null ? void 0 : edge.ariaLabel || `Edge from ${edge.source} to ${edge.target}`, "aria-describedby": isFocusable ? `${ARIA_EDGE_DESC_KEY}-${rfId}` : void 0, ref: edgeRef, ...edge.domAttributes, children: [!reconnecting && (0, import_jsx_runtime3.jsx)(EdgeComponent, { id: id2, source: edge.source, target: edge.target, type: edge.type, selected: edge.selected, animated: edge.animated, selectable: isSelectable, deletable: edge.deletable ?? true, label: edge.label, labelStyle: edge.labelStyle, labelShowBg: edge.labelShowBg, labelBgStyle: edge.labelBgStyle, labelBgPadding: edge.labelBgPadding, labelBgBorderRadius: edge.labelBgBorderRadius, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, data: edge.data, style: edge.style, sourceHandleId: edge.sourceHandle, targetHandleId: edge.targetHandle, markerStart: markerStartUrl, markerEnd: markerEndUrl, pathOptions: "pathOptions" in edge ? edge.pathOptions : void 0, interactionWidth: edge.interactionWidth }), isReconnectable && (0, import_jsx_runtime3.jsx)(EdgeUpdateAnchors, { edge, isReconnectable, reconnectRadius, onReconnect, onReconnectStart, onReconnectEnd, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, setUpdateHover, setReconnecting })] }) });
}
var EdgeWrapper$1 = (0, import_react4.memo)(EdgeWrapper);
var selector$9 = (s) => ({
  edgesFocusable: s.edgesFocusable,
  edgesReconnectable: s.edgesReconnectable,
  elementsSelectable: s.elementsSelectable,
  connectionMode: s.connectionMode,
  onError: s.onError
});
function EdgeRendererComponent({ defaultMarkerColor, onlyRenderVisibleElements, rfId, edgeTypes, noPanClassName, onReconnect, onEdgeContextMenu, onEdgeMouseEnter, onEdgeMouseMove, onEdgeMouseLeave, onEdgeClick, reconnectRadius, onEdgeDoubleClick, onReconnectStart, onReconnectEnd, disableKeyboardA11y }) {
  const { edgesFocusable, edgesReconnectable, elementsSelectable, onError } = useStore(selector$9, shallow$1);
  const edgeIds = useVisibleEdgeIds(onlyRenderVisibleElements);
  return (0, import_jsx_runtime3.jsxs)("div", { className: "react-flow__edges", children: [(0, import_jsx_runtime3.jsx)(MarkerDefinitions$1, { defaultColor: defaultMarkerColor, rfId }), edgeIds.map((id2) => {
    return (0, import_jsx_runtime3.jsx)(EdgeWrapper$1, { id: id2, edgesFocusable, edgesReconnectable, elementsSelectable, noPanClassName, onReconnect, onContextMenu: onEdgeContextMenu, onMouseEnter: onEdgeMouseEnter, onMouseMove: onEdgeMouseMove, onMouseLeave: onEdgeMouseLeave, onClick: onEdgeClick, reconnectRadius, onDoubleClick: onEdgeDoubleClick, onReconnectStart, onReconnectEnd, rfId, onError, edgeTypes, disableKeyboardA11y }, id2);
  })] });
}
EdgeRendererComponent.displayName = "EdgeRenderer";
var EdgeRenderer = (0, import_react4.memo)(EdgeRendererComponent);
var toTransformString = (transform2) => `translate(${transform2[0]}px,${transform2[1]}px) scale(${transform2[2]})`;
function Viewport({ children: children2 }) {
  const store = useStoreApi();
  const viewportRef = (0, import_react4.useRef)(null);
  const [initialTransform] = (0, import_react4.useState)(() => store.getState().transform);
  useIsomorphicLayoutEffect(() => {
    let prevTransform = null;
    const applyTransform = () => {
      const transform2 = store.getState().transform;
      if (prevTransform && transform2[0] === prevTransform[0] && transform2[1] === prevTransform[1] && transform2[2] === prevTransform[2]) {
        return;
      }
      prevTransform = transform2;
      if (viewportRef.current) {
        viewportRef.current.style.transform = toTransformString(transform2);
      }
    };
    applyTransform();
    return store.subscribe(applyTransform);
  }, [store]);
  return (0, import_jsx_runtime3.jsx)("div", { ref: viewportRef, className: "react-flow__viewport xyflow__viewport react-flow__container", style: { transform: toTransformString(initialTransform) }, children: children2 });
}
function useOnInitHandler(onInit) {
  const rfInstance = useReactFlow();
  const isInitialized = (0, import_react4.useRef)(false);
  (0, import_react4.useEffect)(() => {
    if (!isInitialized.current && rfInstance.viewportInitialized && onInit) {
      setTimeout(() => onInit(rfInstance), 1);
      isInitialized.current = true;
    }
  }, [onInit, rfInstance.viewportInitialized]);
}
var selector$8 = (state) => state.panZoom?.syncViewport;
function useViewportSync(viewport) {
  const syncViewport = useStore(selector$8);
  const store = useStoreApi();
  (0, import_react4.useEffect)(() => {
    if (viewport) {
      syncViewport?.(viewport);
      store.setState({ transform: [viewport.x, viewport.y, viewport.zoom] });
    }
  }, [viewport, syncViewport]);
  return null;
}
function storeSelector$1(s) {
  return s.connection.inProgress ? { ...s.connection, to: pointToRendererPoint(s.connection.to, s.transform) } : { ...s.connection };
}
function getSelector(connectionSelector) {
  if (connectionSelector) {
    const combinedSelector = (s) => {
      const connection = storeSelector$1(s);
      return connectionSelector(connection);
    };
    return combinedSelector;
  }
  return storeSelector$1;
}
function useConnection(connectionSelector) {
  const combinedSelector = getSelector(connectionSelector);
  return useStore(combinedSelector, shallow$1);
}
var selector$7 = (s) => ({
  nodesConnectable: s.nodesConnectable,
  isValid: s.connection.isValid,
  inProgress: s.connection.inProgress,
  width: s.width,
  height: s.height
});
function ConnectionLineWrapper({ containerStyle: containerStyle2, style: style2, type, component }) {
  const { nodesConnectable, width, height, isValid, inProgress } = useStore(selector$7, shallow$1);
  const renderConnection = !!(width && nodesConnectable && inProgress);
  if (!renderConnection) {
    return null;
  }
  return (0, import_jsx_runtime3.jsx)("svg", { style: containerStyle2, width, height, className: "react-flow__connectionline react-flow__container", children: (0, import_jsx_runtime3.jsx)("g", { className: cc(["react-flow__connection", getConnectionStatus(isValid)]), children: (0, import_jsx_runtime3.jsx)(ConnectionLine, { style: style2, type, CustomComponent: component, isValid }) }) });
}
var ConnectionLine = ({ style: style2, type = ConnectionLineType.Bezier, CustomComponent, isValid }) => {
  const { inProgress, from, fromNode, fromHandle, fromPosition, to, toNode, toHandle, toPosition, pointer } = useConnection();
  if (!inProgress) {
    return;
  }
  if (CustomComponent) {
    return (0, import_jsx_runtime3.jsx)(CustomComponent, { connectionLineType: type, connectionLineStyle: style2, fromNode, fromHandle, fromX: from.x, fromY: from.y, toX: to.x, toY: to.y, fromPosition, toPosition, connectionStatus: getConnectionStatus(isValid), toNode, toHandle, pointer });
  }
  let path = "";
  const pathParams = {
    sourceX: from.x,
    sourceY: from.y,
    sourcePosition: fromPosition,
    targetX: to.x,
    targetY: to.y,
    targetPosition: toPosition
  };
  switch (type) {
    case ConnectionLineType.Bezier:
      [path] = getBezierPath(pathParams);
      break;
    case ConnectionLineType.SimpleBezier:
      [path] = getSimpleBezierPath(pathParams);
      break;
    case ConnectionLineType.Step:
      [path] = getSmoothStepPath({
        ...pathParams,
        borderRadius: 0
      });
      break;
    case ConnectionLineType.SmoothStep:
      [path] = getSmoothStepPath(pathParams);
      break;
    default:
      [path] = getStraightPath(pathParams);
  }
  return (0, import_jsx_runtime3.jsx)("path", { d: path, fill: "none", className: "react-flow__connection-path", style: style2 });
};
ConnectionLine.displayName = "ConnectionLine";
var emptyTypes = {};
function useNodeOrEdgeTypesWarning(nodeOrEdgeTypes = emptyTypes) {
  const typesRef = (0, import_react4.useRef)(nodeOrEdgeTypes);
  const store = useStoreApi();
  (0, import_react4.useEffect)(() => {
    if (true) {
      const usedKeys = /* @__PURE__ */ new Set([...Object.keys(typesRef.current), ...Object.keys(nodeOrEdgeTypes)]);
      for (const key of usedKeys) {
        if (typesRef.current[key] !== nodeOrEdgeTypes[key]) {
          store.getState().onError?.("002", errorMessages["error002"]());
          break;
        }
      }
      typesRef.current = nodeOrEdgeTypes;
    }
  }, [nodeOrEdgeTypes]);
}
function useStylesLoadedWarning() {
  const store = useStoreApi();
  const checked = (0, import_react4.useRef)(false);
  (0, import_react4.useEffect)(() => {
    if (true) {
      if (!checked.current) {
        const pane = document.querySelector(".react-flow__pane");
        if (pane && !(window.getComputedStyle(pane).zIndex === "1")) {
          store.getState().onError?.("013", errorMessages["error013"]("react"));
        }
        checked.current = true;
      }
    }
  }, []);
}
function GraphViewComponent({ nodeTypes, edgeTypes, onInit, onNodeClick, onEdgeClick, onNodeDoubleClick, onEdgeDoubleClick, onNodeMouseEnter, onNodeMouseMove, onNodeMouseLeave, onNodeContextMenu, onSelectionContextMenu, onSelectionStart, onSelectionEnd, connectionLineType, connectionLineStyle, connectionLineComponent, connectionLineContainerStyle, selectionKeyCode, selectionOnDrag, selectionMode, multiSelectionKeyCode, panActivationKeyCode, zoomActivationKeyCode, deleteKeyCode, onlyRenderVisibleElements, elementsSelectable, defaultViewport: defaultViewport2, translateExtent, minZoom, maxZoom, preventScrolling, defaultMarkerColor, zoomOnScroll, zoomOnPinch, panOnScroll, panOnScrollSpeed, panOnScrollMode, zoomOnDoubleClick, panOnDrag, autoPanOnSelection, onPaneClick, onPaneMouseEnter, onPaneMouseMove, onPaneMouseLeave, onPaneScroll, onPaneContextMenu, paneClickDistance, nodeClickDistance, onEdgeContextMenu, onEdgeMouseEnter, onEdgeMouseMove, onEdgeMouseLeave, reconnectRadius, onReconnect, onReconnectStart, onReconnectEnd, noDragClassName, noWheelClassName, noPanClassName, disableKeyboardA11y, nodeExtent, rfId, viewport, onViewportChange, nodesDraggable }) {
  useNodeOrEdgeTypesWarning(nodeTypes);
  useNodeOrEdgeTypesWarning(edgeTypes);
  useStylesLoadedWarning();
  useOnInitHandler(onInit);
  useViewportSync(viewport);
  return (0, import_jsx_runtime3.jsx)(FlowRenderer, { onPaneClick, onPaneMouseEnter, onPaneMouseMove, onPaneMouseLeave, onPaneContextMenu, onPaneScroll, paneClickDistance, deleteKeyCode, selectionKeyCode, selectionOnDrag, selectionMode, onSelectionStart, onSelectionEnd, multiSelectionKeyCode, panActivationKeyCode, zoomActivationKeyCode, elementsSelectable, zoomOnScroll, zoomOnPinch, zoomOnDoubleClick, panOnScroll, panOnScrollSpeed, panOnScrollMode, panOnDrag, autoPanOnSelection, defaultViewport: defaultViewport2, translateExtent, minZoom, maxZoom, onSelectionContextMenu, preventScrolling, noDragClassName, noWheelClassName, noPanClassName, disableKeyboardA11y, onViewportChange, isControlledViewport: !!viewport, children: (0, import_jsx_runtime3.jsxs)(Viewport, { children: [(0, import_jsx_runtime3.jsx)(EdgeRenderer, { edgeTypes, onEdgeClick, onEdgeDoubleClick, onReconnect, onReconnectStart, onReconnectEnd, onlyRenderVisibleElements, onEdgeContextMenu, onEdgeMouseEnter, onEdgeMouseMove, onEdgeMouseLeave, reconnectRadius, defaultMarkerColor, noPanClassName, disableKeyboardA11y, rfId }), (0, import_jsx_runtime3.jsx)(ConnectionLineWrapper, { style: connectionLineStyle, type: connectionLineType, component: connectionLineComponent, containerStyle: connectionLineContainerStyle }), (0, import_jsx_runtime3.jsx)("div", { className: "react-flow__edgelabel-renderer" }), (0, import_jsx_runtime3.jsx)(NodeRenderer, { nodeTypes, onNodeClick, onNodeDoubleClick, onNodeMouseEnter, onNodeMouseMove, onNodeMouseLeave, onNodeContextMenu, nodeClickDistance, onlyRenderVisibleElements, noPanClassName, noDragClassName, disableKeyboardA11y, nodeExtent, rfId, nodesDraggable }), (0, import_jsx_runtime3.jsx)("div", { className: "react-flow__viewport-portal" })] }) });
}
GraphViewComponent.displayName = "GraphView";
var GraphView = (0, import_react4.memo)(GraphViewComponent);
var devWarn = createDevWarn("React Flow", "https://reactflow.dev/");
var getInitialState = ({ nodes, edges, defaultNodes, defaultEdges, width, height, fitView, fitViewOptions, minZoom = 0.5, maxZoom = 2, nodeOrigin, nodeExtent, zIndexMode = "basic" } = {}) => {
  const nodeLookup = /* @__PURE__ */ new Map();
  const parentLookup = /* @__PURE__ */ new Map();
  const connectionLookup = /* @__PURE__ */ new Map();
  const edgeLookup = /* @__PURE__ */ new Map();
  const storeEdges = defaultEdges ?? edges ?? [];
  const storeNodes = defaultNodes ?? nodes ?? [];
  const storeNodeOrigin = nodeOrigin ?? [0, 0];
  const storeNodeExtent = nodeExtent ?? infiniteExtent;
  updateConnectionLookup(connectionLookup, edgeLookup, storeEdges);
  const { nodesInitialized } = adoptUserNodes(storeNodes, nodeLookup, parentLookup, {
    nodeOrigin: storeNodeOrigin,
    nodeExtent: storeNodeExtent,
    zIndexMode
  });
  let transform2 = [0, 0, 1];
  if (fitView && width && height) {
    const bounds = getInternalNodesBounds(nodeLookup, {
      filter: (node) => !!((node.width || node.initialWidth) && (node.height || node.initialHeight))
    });
    const { x: x3, y: y2, zoom } = getViewportForBounds(bounds, width, height, minZoom, maxZoom, fitViewOptions?.padding ?? 0.1);
    transform2 = [x3, y2, zoom];
  }
  return {
    rfId: "1",
    width: width ?? 0,
    height: height ?? 0,
    transform: transform2,
    nodes: storeNodes,
    nodesInitialized,
    nodeLookup,
    parentLookup,
    edges: storeEdges,
    edgeLookup,
    connectionLookup,
    onNodesChange: null,
    onEdgesChange: null,
    hasDefaultNodes: defaultNodes !== void 0,
    hasDefaultEdges: defaultEdges !== void 0,
    panZoom: null,
    minZoom,
    maxZoom,
    translateExtent: infiniteExtent,
    nodeExtent: storeNodeExtent,
    nodesSelectionActive: false,
    userSelectionActive: false,
    userSelectionRect: null,
    connectionMode: ConnectionMode.Strict,
    domNode: null,
    paneDragging: false,
    noPanClassName: "nopan",
    nodeOrigin: storeNodeOrigin,
    nodeDragThreshold: 1,
    connectionDragThreshold: 1,
    snapGrid: [15, 15],
    snapToGrid: false,
    nodesDraggable: true,
    nodesConnectable: true,
    nodesFocusable: true,
    edgesFocusable: true,
    edgesReconnectable: true,
    elementsSelectable: true,
    elevateNodesOnSelect: true,
    elevateEdgesOnSelect: true,
    selectNodesOnDrag: true,
    multiSelectionActive: false,
    fitViewQueued: fitView ?? false,
    fitViewOptions,
    fitViewResolver: null,
    connection: { ...initialConnection },
    connectionClickStartHandle: null,
    connectOnClick: true,
    ariaLiveMessage: "",
    autoPanOnConnect: true,
    autoPanOnNodeDrag: true,
    autoPanOnNodeFocus: true,
    autoPanSpeed: 15,
    connectionRadius: 20,
    onError: devWarn,
    isValidConnection: void 0,
    onSelectionChangeHandlers: [],
    lib: "react",
    debug: false,
    ariaLabelConfig: defaultAriaLabelConfig,
    zIndexMode,
    onNodesChangeMiddlewareMap: /* @__PURE__ */ new Map(),
    onEdgesChangeMiddlewareMap: /* @__PURE__ */ new Map()
  };
};
var createStore2 = ({ nodes, edges, defaultNodes, defaultEdges, width, height, fitView, fitViewOptions, minZoom, maxZoom, nodeOrigin, nodeExtent, zIndexMode }) => createWithEqualityFn((set3, get3) => {
  async function resolveFitView() {
    const { nodeLookup, panZoom, fitViewOptions: fitViewOptions2, fitViewResolver, width: width2, height: height2, minZoom: minZoom2, maxZoom: maxZoom2 } = get3();
    if (!panZoom) {
      return;
    }
    await fitViewport({
      nodes: nodeLookup,
      width: width2,
      height: height2,
      panZoom,
      minZoom: minZoom2,
      maxZoom: maxZoom2
    }, fitViewOptions2);
    fitViewResolver?.resolve(true);
    set3({ fitViewResolver: null });
  }
  return {
    ...getInitialState({
      nodes,
      edges,
      width,
      height,
      fitView,
      fitViewOptions,
      minZoom,
      maxZoom,
      nodeOrigin,
      nodeExtent,
      defaultNodes,
      defaultEdges,
      zIndexMode
    }),
    setNodes: (nodes2) => {
      const { nodeLookup, parentLookup, nodeOrigin: nodeOrigin2, nodeExtent: nodeExtent2, elevateNodesOnSelect, fitViewQueued, zIndexMode: zIndexMode2, nodesSelectionActive } = get3();
      const { nodesInitialized, hasSelectedNodes } = adoptUserNodes(nodes2, nodeLookup, parentLookup, {
        nodeOrigin: nodeOrigin2,
        nodeExtent: nodeExtent2,
        elevateNodesOnSelect,
        checkEquality: true,
        zIndexMode: zIndexMode2
      });
      const nextNodesSelectionActive = nodesSelectionActive && hasSelectedNodes;
      if (fitViewQueued && nodesInitialized) {
        resolveFitView();
        set3({
          nodes: nodes2,
          nodesInitialized,
          fitViewQueued: false,
          fitViewOptions: void 0,
          nodesSelectionActive: nextNodesSelectionActive
        });
      } else {
        set3({ nodes: nodes2, nodesInitialized, nodesSelectionActive: nextNodesSelectionActive });
      }
    },
    setEdges: (edges2) => {
      const { connectionLookup, edgeLookup } = get3();
      updateConnectionLookup(connectionLookup, edgeLookup, edges2);
      set3({ edges: edges2 });
    },
    setDefaultNodesAndEdges: (nodes2, edges2) => {
      if (nodes2) {
        const { setNodes } = get3();
        setNodes(nodes2);
        set3({ hasDefaultNodes: true });
      }
      if (edges2) {
        const { setEdges } = get3();
        setEdges(edges2);
        set3({ hasDefaultEdges: true });
      }
    },
    /*
     * Every node gets registered at a ResizeObserver. Whenever a node
     * changes its dimensions, this function is called to measure the
     * new dimensions and update the nodes.
     */
    updateNodeInternals: (updates) => {
      const { triggerNodeChanges, nodeLookup, parentLookup, domNode, nodeOrigin: nodeOrigin2, nodeExtent: nodeExtent2, debug, fitViewQueued, zIndexMode: zIndexMode2 } = get3();
      const { changes, updatedInternals } = updateNodeInternals(updates, nodeLookup, parentLookup, domNode, nodeOrigin2, nodeExtent2, zIndexMode2);
      if (!updatedInternals) {
        return;
      }
      updateAbsolutePositions(nodeLookup, parentLookup, { nodeOrigin: nodeOrigin2, nodeExtent: nodeExtent2, zIndexMode: zIndexMode2 });
      if (fitViewQueued) {
        resolveFitView();
        set3({ fitViewQueued: false, fitViewOptions: void 0 });
      } else {
        set3({});
      }
      if (changes?.length > 0) {
        if (debug) {
          console.log("React Flow: trigger node changes", changes);
        }
        triggerNodeChanges?.(changes);
      }
    },
    updateNodePositions: (nodeDragItems, dragging = false) => {
      const parentExpandChildren = [];
      let changes = [];
      const { nodeLookup, triggerNodeChanges, connection, updateConnection, onNodesChangeMiddlewareMap } = get3();
      for (const [id2, dragItem] of nodeDragItems) {
        const node = nodeLookup.get(id2);
        const expandParent = !!(node?.expandParent && node?.parentId && dragItem?.position);
        const change = {
          id: id2,
          type: "position",
          position: expandParent ? {
            x: Math.max(0, dragItem.position.x),
            y: Math.max(0, dragItem.position.y)
          } : dragItem.position,
          dragging
        };
        if (node && connection.inProgress && connection.fromNode.id === node.id) {
          const updatedFrom = getHandlePosition(node, connection.fromHandle, Position.Left, true);
          updateConnection({ ...connection, from: updatedFrom });
        }
        if (expandParent && node.parentId) {
          parentExpandChildren.push({
            id: id2,
            parentId: node.parentId,
            rect: {
              ...dragItem.internals.positionAbsolute,
              width: dragItem.measured.width ?? 0,
              height: dragItem.measured.height ?? 0
            }
          });
        }
        changes.push(change);
      }
      if (parentExpandChildren.length > 0) {
        const { parentLookup, nodeOrigin: nodeOrigin2 } = get3();
        const parentExpandChanges = handleExpandParent(parentExpandChildren, nodeLookup, parentLookup, nodeOrigin2);
        changes.push(...parentExpandChanges);
      }
      for (const middleware of onNodesChangeMiddlewareMap.values()) {
        changes = middleware(changes);
      }
      triggerNodeChanges(changes);
    },
    triggerNodeChanges: (changes) => {
      const { onNodesChange, setNodes, nodes: nodes2, hasDefaultNodes, debug } = get3();
      if (changes?.length) {
        if (hasDefaultNodes) {
          const updatedNodes = applyNodeChanges(changes, nodes2);
          setNodes(updatedNodes);
        }
        if (debug) {
          console.log("React Flow: trigger node changes", changes);
        }
        onNodesChange?.(changes);
      }
    },
    triggerEdgeChanges: (changes) => {
      const { onEdgesChange, setEdges, edges: edges2, hasDefaultEdges, debug } = get3();
      if (changes?.length) {
        if (hasDefaultEdges) {
          const updatedEdges = applyEdgeChanges(changes, edges2);
          setEdges(updatedEdges);
        }
        if (debug) {
          console.log("React Flow: trigger edge changes", changes);
        }
        onEdgesChange?.(changes);
      }
    },
    addSelectedNodes: (selectedNodeIds) => {
      const { multiSelectionActive, edgeLookup, nodeLookup, triggerNodeChanges, triggerEdgeChanges } = get3();
      if (multiSelectionActive) {
        const nodeChanges = selectedNodeIds.map((nodeId) => createSelectionChange(nodeId, true));
        triggerNodeChanges(nodeChanges);
        return;
      }
      triggerNodeChanges(getSelectionChanges(nodeLookup, /* @__PURE__ */ new Set([...selectedNodeIds]), true));
      triggerEdgeChanges(getSelectionChanges(edgeLookup));
    },
    addSelectedEdges: (selectedEdgeIds) => {
      const { multiSelectionActive, edgeLookup, nodeLookup, triggerNodeChanges, triggerEdgeChanges } = get3();
      if (multiSelectionActive) {
        const changedEdges = selectedEdgeIds.map((edgeId) => createSelectionChange(edgeId, true));
        triggerEdgeChanges(changedEdges);
        return;
      }
      triggerEdgeChanges(getSelectionChanges(edgeLookup, /* @__PURE__ */ new Set([...selectedEdgeIds])));
      triggerNodeChanges(getSelectionChanges(nodeLookup, /* @__PURE__ */ new Set(), true));
    },
    unselectNodesAndEdges: ({ nodes: nodes2, edges: edges2 } = {}) => {
      const { edges: storeEdges, nodes: storeNodes, nodeLookup, triggerNodeChanges, triggerEdgeChanges } = get3();
      const nodesToUnselect = nodes2 ? nodes2 : storeNodes;
      const edgesToUnselect = edges2 ? edges2 : storeEdges;
      const nodeChanges = [];
      for (const node of nodesToUnselect) {
        if (!node.selected) {
          continue;
        }
        const internalNode = nodeLookup.get(node.id);
        if (internalNode) {
          internalNode.selected = false;
        }
        nodeChanges.push(createSelectionChange(node.id, false));
      }
      const edgeChanges = [];
      for (const edge of edgesToUnselect) {
        if (!edge.selected) {
          continue;
        }
        edgeChanges.push(createSelectionChange(edge.id, false));
      }
      triggerNodeChanges(nodeChanges);
      triggerEdgeChanges(edgeChanges);
    },
    setMinZoom: (minZoom2) => {
      const { panZoom, maxZoom: maxZoom2 } = get3();
      panZoom?.setScaleExtent([minZoom2, maxZoom2]);
      set3({ minZoom: minZoom2 });
    },
    setMaxZoom: (maxZoom2) => {
      const { panZoom, minZoom: minZoom2 } = get3();
      panZoom?.setScaleExtent([minZoom2, maxZoom2]);
      set3({ maxZoom: maxZoom2 });
    },
    setTranslateExtent: (translateExtent) => {
      get3().panZoom?.setTranslateExtent(translateExtent);
      set3({ translateExtent });
    },
    resetSelectedElements: () => {
      const { edges: edges2, nodes: nodes2, triggerNodeChanges, triggerEdgeChanges, elementsSelectable } = get3();
      if (!elementsSelectable) {
        return;
      }
      const nodeChanges = nodes2.reduce((res, node) => node.selected ? [...res, createSelectionChange(node.id, false)] : res, []);
      const edgeChanges = edges2.reduce((res, edge) => edge.selected ? [...res, createSelectionChange(edge.id, false)] : res, []);
      triggerNodeChanges(nodeChanges);
      triggerEdgeChanges(edgeChanges);
    },
    setNodeExtent: (nextNodeExtent) => {
      const { nodes: nodes2, nodeLookup, parentLookup, nodeOrigin: nodeOrigin2, elevateNodesOnSelect, nodeExtent: nodeExtent2, zIndexMode: zIndexMode2 } = get3();
      if (nextNodeExtent[0][0] === nodeExtent2[0][0] && nextNodeExtent[0][1] === nodeExtent2[0][1] && nextNodeExtent[1][0] === nodeExtent2[1][0] && nextNodeExtent[1][1] === nodeExtent2[1][1]) {
        return;
      }
      adoptUserNodes(nodes2, nodeLookup, parentLookup, {
        nodeOrigin: nodeOrigin2,
        nodeExtent: nextNodeExtent,
        elevateNodesOnSelect,
        checkEquality: false,
        zIndexMode: zIndexMode2
      });
      set3({ nodeExtent: nextNodeExtent });
    },
    panBy: (delta) => {
      const { transform: transform2, width: width2, height: height2, panZoom, translateExtent } = get3();
      return panBy({ delta, panZoom, transform: transform2, translateExtent, width: width2, height: height2 });
    },
    setCenter: async (x3, y2, options) => {
      const { width: width2, height: height2, maxZoom: maxZoom2, panZoom } = get3();
      if (!panZoom) {
        return false;
      }
      const nextZoom = typeof options?.zoom !== "undefined" ? options.zoom : maxZoom2;
      await panZoom.setViewport({
        x: width2 / 2 - x3 * nextZoom,
        y: height2 / 2 - y2 * nextZoom,
        zoom: nextZoom
      }, { duration: options?.duration, ease: options?.ease, interpolate: options?.interpolate });
      return true;
    },
    cancelConnection: () => {
      set3({
        connection: { ...initialConnection }
      });
    },
    updateConnection: (connection) => {
      set3({ connection });
    },
    reset: () => set3({ ...getInitialState() })
  };
}, Object.is);
function ReactFlowProvider({ initialNodes: nodes, initialEdges: edges, defaultNodes, defaultEdges, initialWidth: width, initialHeight: height, initialMinZoom: minZoom, initialMaxZoom: maxZoom, initialFitViewOptions: fitViewOptions, fitView, nodeOrigin, nodeExtent, zIndexMode, children: children2 }) {
  const [store] = (0, import_react4.useState)(() => createStore2({
    nodes,
    edges,
    defaultNodes,
    defaultEdges,
    width,
    height,
    fitView,
    minZoom,
    maxZoom,
    fitViewOptions,
    nodeOrigin,
    nodeExtent,
    zIndexMode
  }));
  return (0, import_jsx_runtime3.jsx)(Provider$1, { value: store, children: (0, import_jsx_runtime3.jsx)(BatchProvider, { children: (0, import_jsx_runtime3.jsx)(HandleConfigProvider, { children: children2 }) }) });
}
function Wrapper({ children: children2, nodes, edges, defaultNodes, defaultEdges, width, height, fitView, fitViewOptions, minZoom, maxZoom, nodeOrigin, nodeExtent, zIndexMode }) {
  const isWrapped = (0, import_react4.useContext)(StoreContext);
  if (isWrapped) {
    return (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: children2 });
  }
  return (0, import_jsx_runtime3.jsx)(ReactFlowProvider, { initialNodes: nodes, initialEdges: edges, defaultNodes, defaultEdges, initialWidth: width, initialHeight: height, fitView, initialFitViewOptions: fitViewOptions, initialMinZoom: minZoom, initialMaxZoom: maxZoom, nodeOrigin, nodeExtent, zIndexMode, children: children2 });
}
var wrapperStyle = {
  width: "100%",
  height: "100%",
  overflow: "hidden",
  position: "relative",
  zIndex: 0
};
function ReactFlow({ nodes, edges, defaultNodes, defaultEdges, className, nodeTypes, edgeTypes, onNodeClick, onEdgeClick, onInit, onMove, onMoveStart, onMoveEnd, onConnect, onConnectStart, onConnectEnd, onClickConnectStart, onClickConnectEnd, onNodeMouseEnter, onNodeMouseMove, onNodeMouseLeave, onNodeContextMenu, onNodeDoubleClick, onNodeDragStart, onNodeDrag, onNodeDragStop, onNodesDelete, onEdgesDelete, onDelete, onSelectionChange, onSelectionDragStart, onSelectionDrag, onSelectionDragStop, onSelectionContextMenu, onSelectionStart, onSelectionEnd, onBeforeDelete, connectionMode, connectionLineType = ConnectionLineType.Bezier, connectionLineStyle, connectionLineComponent, connectionLineContainerStyle, deleteKeyCode = "Backspace", selectionKeyCode = "Shift", selectionOnDrag = false, selectionMode = SelectionMode.Full, panActivationKeyCode = "Space", multiSelectionKeyCode = isMacOs() ? "Meta" : "Control", zoomActivationKeyCode = isMacOs() ? "Meta" : "Control", snapToGrid, snapGrid, onlyRenderVisibleElements = false, selectNodesOnDrag, nodesDraggable, autoPanOnNodeFocus, nodesConnectable, nodesFocusable, nodeOrigin = defaultNodeOrigin, edgesFocusable, edgesReconnectable, elementsSelectable = true, defaultViewport: defaultViewport$1 = defaultViewport, minZoom = 0.5, maxZoom = 2, translateExtent = infiniteExtent, preventScrolling = true, nodeExtent, defaultMarkerColor = "#b1b1b7", zoomOnScroll = true, zoomOnPinch = true, panOnScroll = false, panOnScrollSpeed = 0.5, panOnScrollMode = PanOnScrollMode.Free, zoomOnDoubleClick = true, panOnDrag = true, onPaneClick, onPaneMouseEnter, onPaneMouseMove, onPaneMouseLeave, onPaneScroll, onPaneContextMenu, paneClickDistance = 1, nodeClickDistance = 0, children: children2, onReconnect, onReconnectStart, onReconnectEnd, onEdgeContextMenu, onEdgeDoubleClick, onEdgeMouseEnter, onEdgeMouseMove, onEdgeMouseLeave, reconnectRadius = 10, onNodesChange, onEdgesChange, noDragClassName = "nodrag", noWheelClassName = "nowheel", noPanClassName = "nopan", fitView, fitViewOptions, connectOnClick, attributionPosition, proOptions, defaultEdgeOptions, elevateNodesOnSelect = true, elevateEdgesOnSelect = false, disableKeyboardA11y = false, autoPanOnConnect, autoPanOnNodeDrag, autoPanOnSelection = true, autoPanSpeed, connectionRadius, isValidConnection, onError, style: style2, id: id2, nodeDragThreshold, connectionDragThreshold, viewport, onViewportChange, width, height, colorMode = "light", debug, onScroll, ariaLabelConfig, zIndexMode = "basic", ...rest }, ref) {
  const rfId = id2 || "1";
  const colorModeClassName = useColorModeClass(colorMode);
  const wrapperOnScroll = (0, import_react4.useCallback)((e) => {
    e.currentTarget.scrollTo({ top: 0, left: 0, behavior: "instant" });
    onScroll?.(e);
  }, [onScroll]);
  return (0, import_jsx_runtime3.jsx)("div", { "data-testid": "rf__wrapper", ...rest, onScroll: wrapperOnScroll, style: { ...style2, ...wrapperStyle }, ref, className: cc(["react-flow", className, colorModeClassName]), id: id2, role: "application", children: (0, import_jsx_runtime3.jsxs)(Wrapper, { nodes, edges, width, height, fitView, fitViewOptions, minZoom, maxZoom, nodeOrigin, nodeExtent, zIndexMode, children: [(0, import_jsx_runtime3.jsx)(StoreUpdater, { nodes, edges, defaultNodes, defaultEdges, onConnect, onConnectStart, onConnectEnd, onClickConnectStart, onClickConnectEnd, nodesDraggable, autoPanOnNodeFocus, nodesConnectable, nodesFocusable, edgesFocusable, edgesReconnectable, elementsSelectable, elevateNodesOnSelect, elevateEdgesOnSelect, minZoom, maxZoom, nodeExtent, onNodesChange, onEdgesChange, snapToGrid, snapGrid, connectionMode, translateExtent, connectOnClick, defaultEdgeOptions, fitView, fitViewOptions, onNodesDelete, onEdgesDelete, onDelete, onNodeDragStart, onNodeDrag, onNodeDragStop, onSelectionDrag, onSelectionDragStart, onSelectionDragStop, onMove, onMoveStart, onMoveEnd, noPanClassName, nodeOrigin, rfId, autoPanOnConnect, autoPanOnNodeDrag, autoPanSpeed, onError, connectionRadius, isValidConnection, selectNodesOnDrag, nodeDragThreshold, connectionDragThreshold, onBeforeDelete, debug, ariaLabelConfig, zIndexMode }), (0, import_jsx_runtime3.jsx)(GraphView, { onInit, onNodeClick, onEdgeClick, onNodeMouseEnter, onNodeMouseMove, onNodeMouseLeave, onNodeContextMenu, onNodeDoubleClick, nodeTypes, edgeTypes, connectionLineType, connectionLineStyle, connectionLineComponent, connectionLineContainerStyle, selectionKeyCode, selectionOnDrag, selectionMode, deleteKeyCode, multiSelectionKeyCode, panActivationKeyCode, zoomActivationKeyCode, onlyRenderVisibleElements, defaultViewport: defaultViewport$1, translateExtent, minZoom, maxZoom, preventScrolling, zoomOnScroll, zoomOnPinch, zoomOnDoubleClick, panOnScroll, panOnScrollSpeed, panOnScrollMode, panOnDrag, autoPanOnSelection, onPaneClick, onPaneMouseEnter, onPaneMouseMove, onPaneMouseLeave, onPaneScroll, onPaneContextMenu, paneClickDistance, nodeClickDistance, onSelectionContextMenu, onSelectionStart, onSelectionEnd, onReconnect, onReconnectStart, onReconnectEnd, onEdgeContextMenu, onEdgeDoubleClick, onEdgeMouseEnter, onEdgeMouseMove, onEdgeMouseLeave, reconnectRadius, defaultMarkerColor, noDragClassName, noWheelClassName, noPanClassName, rfId, disableKeyboardA11y, nodeExtent, viewport, onViewportChange, nodesDraggable }), (0, import_jsx_runtime3.jsx)(SelectionListener, { onSelectionChange }), children2, (0, import_jsx_runtime3.jsx)(Attribution, { proOptions, position: attributionPosition }), (0, import_jsx_runtime3.jsx)(A11yDescriptions, { rfId, disableKeyboardA11y })] }) });
}
var index = fixedForwardRef(ReactFlow);
var selector$5 = (s) => s.domNode?.querySelector(".react-flow__viewport-portal");
function ViewportPortal({ children: children2 }) {
  const viewPortalDiv = useStore(selector$5);
  if (!viewPortalDiv) {
    return null;
  }
  return (0, import_react_dom.createPortal)(children2, viewPortalDiv);
}
var viewportSelector = (state) => ({
  x: state.transform[0],
  y: state.transform[1],
  zoom: state.transform[2]
});
function useViewport() {
  const viewport = useStore(viewportSelector, shallow$1);
  return viewport;
}
var error014 = errorMessages["error014"]();
function useInternalNode(id2) {
  const node = useStore((0, import_react4.useCallback)((s) => s.nodeLookup.get(id2), [id2]), shallow$1);
  return node;
}
function LinePattern({ dimensions, lineWidth, variant, className }) {
  return (0, import_jsx_runtime3.jsx)("path", { strokeWidth: lineWidth, d: `M${dimensions[0] / 2} 0 V${dimensions[1]} M0 ${dimensions[1] / 2} H${dimensions[0]}`, className: cc(["react-flow__background-pattern", variant, className]) });
}
function DotPattern({ radius, className }) {
  return (0, import_jsx_runtime3.jsx)("circle", { cx: radius, cy: radius, r: radius, className: cc(["react-flow__background-pattern", "dots", className]) });
}
var BackgroundVariant;
(function(BackgroundVariant2) {
  BackgroundVariant2["Lines"] = "lines";
  BackgroundVariant2["Dots"] = "dots";
  BackgroundVariant2["Cross"] = "cross";
})(BackgroundVariant || (BackgroundVariant = {}));
var defaultSize = {
  [BackgroundVariant.Dots]: 1,
  [BackgroundVariant.Lines]: 1,
  [BackgroundVariant.Cross]: 6
};
var selector$3 = (s) => ({ transform: s.transform, patternId: `pattern-${s.rfId}` });
function BackgroundComponent({
  id: id2,
  variant = BackgroundVariant.Dots,
  // only used for dots and cross
  gap = 20,
  // only used for lines and cross
  size,
  lineWidth = 1,
  offset = 0,
  color: color2,
  bgColor,
  style: style2,
  className,
  patternClassName
}) {
  const ref = (0, import_react4.useRef)(null);
  const { transform: transform2, patternId } = useStore(selector$3, shallow$1);
  const patternSize = size || defaultSize[variant];
  const isDots = variant === BackgroundVariant.Dots;
  const isCross = variant === BackgroundVariant.Cross;
  const gapXY = Array.isArray(gap) ? gap : [gap, gap];
  const scaledGap = [gapXY[0] * transform2[2] || 1, gapXY[1] * transform2[2] || 1];
  const scaledSize = patternSize * transform2[2];
  const offsetXY = Array.isArray(offset) ? offset : [offset, offset];
  const patternDimensions = isCross ? [scaledSize, scaledSize] : scaledGap;
  const scaledOffset = [
    offsetXY[0] * transform2[2] + patternDimensions[0] / 2,
    offsetXY[1] * transform2[2] + patternDimensions[1] / 2
  ];
  const _patternId = `${patternId}${id2 ? id2 : ""}`;
  return (0, import_jsx_runtime3.jsxs)("svg", { className: cc(["react-flow__background", className]), style: {
    ...style2,
    ...containerStyle,
    "--xy-background-color-props": bgColor,
    "--xy-background-pattern-color-props": color2
  }, ref, "data-testid": "rf__background", children: [(0, import_jsx_runtime3.jsx)("pattern", { id: _patternId, x: transform2[0] % scaledGap[0], y: transform2[1] % scaledGap[1], width: scaledGap[0], height: scaledGap[1], patternUnits: "userSpaceOnUse", patternTransform: `translate(-${scaledOffset[0]},-${scaledOffset[1]})`, children: isDots ? (0, import_jsx_runtime3.jsx)(DotPattern, { radius: scaledSize / 2, className: patternClassName }) : (0, import_jsx_runtime3.jsx)(LinePattern, { dimensions: patternDimensions, lineWidth, variant, className: patternClassName }) }), (0, import_jsx_runtime3.jsx)("rect", { x: "0", y: "0", width: "100%", height: "100%", fill: `url(#${_patternId})` })] });
}
BackgroundComponent.displayName = "Background";
var Background = (0, import_react4.memo)(BackgroundComponent);
function PlusIcon() {
  return (0, import_jsx_runtime3.jsx)("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 32 32", children: (0, import_jsx_runtime3.jsx)("path", { d: "M32 18.133H18.133V32h-4.266V18.133H0v-4.266h13.867V0h4.266v13.867H32z" }) });
}
function MinusIcon() {
  return (0, import_jsx_runtime3.jsx)("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 32 5", children: (0, import_jsx_runtime3.jsx)("path", { d: "M0 0h32v4.2H0z" }) });
}
function FitViewIcon() {
  return (0, import_jsx_runtime3.jsx)("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 32 30", children: (0, import_jsx_runtime3.jsx)("path", { d: "M3.692 4.63c0-.53.4-.938.939-.938h5.215V0H4.708C2.13 0 0 2.054 0 4.63v5.216h3.692V4.631zM27.354 0h-5.2v3.692h5.17c.53 0 .984.4.984.939v5.215H32V4.631A4.624 4.624 0 0027.354 0zm.954 24.83c0 .532-.4.94-.939.94h-5.215v3.768h5.215c2.577 0 4.631-2.13 4.631-4.707v-5.139h-3.692v5.139zm-23.677.94c-.531 0-.939-.4-.939-.94v-5.138H0v5.139c0 2.577 2.13 4.707 4.708 4.707h5.138V25.77H4.631z" }) });
}
function LockIcon() {
  return (0, import_jsx_runtime3.jsx)("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 25 32", children: (0, import_jsx_runtime3.jsx)("path", { d: "M21.333 10.667H19.81V7.619C19.81 3.429 16.38 0 12.19 0 8 0 4.571 3.429 4.571 7.619v3.048H3.048A3.056 3.056 0 000 13.714v15.238A3.056 3.056 0 003.048 32h18.285a3.056 3.056 0 003.048-3.048V13.714a3.056 3.056 0 00-3.048-3.047zM12.19 24.533a3.056 3.056 0 01-3.047-3.047 3.056 3.056 0 013.047-3.048 3.056 3.056 0 013.048 3.048 3.056 3.056 0 01-3.048 3.047zm4.724-13.866H7.467V7.619c0-2.59 2.133-4.724 4.723-4.724 2.591 0 4.724 2.133 4.724 4.724v3.048z" }) });
}
function UnlockIcon() {
  return (0, import_jsx_runtime3.jsx)("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 25 32", children: (0, import_jsx_runtime3.jsx)("path", { d: "M21.333 10.667H19.81V7.619C19.81 3.429 16.38 0 12.19 0c-4.114 1.828-1.37 2.133.305 2.438 1.676.305 4.42 2.59 4.42 5.181v3.048H3.047A3.056 3.056 0 000 13.714v15.238A3.056 3.056 0 003.048 32h18.285a3.056 3.056 0 003.048-3.048V13.714a3.056 3.056 0 00-3.048-3.047zM12.19 24.533a3.056 3.056 0 01-3.047-3.047 3.056 3.056 0 013.047-3.048 3.056 3.056 0 013.048 3.048 3.056 3.056 0 01-3.048 3.047z" }) });
}
function ControlButton({ children: children2, className, ...rest }) {
  return (0, import_jsx_runtime3.jsx)("button", { type: "button", className: cc(["react-flow__controls-button", className]), ...rest, children: children2 });
}
var selector$2 = (s) => ({
  isInteractive: s.nodesDraggable || s.nodesConnectable || s.elementsSelectable,
  minZoomReached: s.transform[2] <= s.minZoom,
  maxZoomReached: s.transform[2] >= s.maxZoom,
  ariaLabelConfig: s.ariaLabelConfig
});
function ControlsComponent({ style: style2, showZoom = true, showFitView = true, showInteractive = true, fitViewOptions, onZoomIn, onZoomOut, onFitView, onInteractiveChange, className, children: children2, position = "bottom-left", orientation = "vertical", "aria-label": ariaLabel }) {
  const store = useStoreApi();
  const { isInteractive, minZoomReached, maxZoomReached, ariaLabelConfig } = useStore(selector$2, shallow$1);
  const { zoomIn, zoomOut, fitView } = useReactFlow();
  const onZoomInHandler = () => {
    zoomIn();
    onZoomIn?.();
  };
  const onZoomOutHandler = () => {
    zoomOut();
    onZoomOut?.();
  };
  const onFitViewHandler = () => {
    fitView(fitViewOptions);
    onFitView?.();
  };
  const onToggleInteractivity = () => {
    store.setState({
      nodesDraggable: !isInteractive,
      nodesConnectable: !isInteractive,
      elementsSelectable: !isInteractive
    });
    onInteractiveChange?.(!isInteractive);
  };
  const orientationClass = orientation === "horizontal" ? "horizontal" : "vertical";
  return (0, import_jsx_runtime3.jsxs)(Panel, { className: cc(["react-flow__controls", orientationClass, className]), position, style: style2, "data-testid": "rf__controls", "aria-label": ariaLabel ?? ariaLabelConfig["controls.ariaLabel"], children: [showZoom && (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [(0, import_jsx_runtime3.jsx)(ControlButton, { onClick: onZoomInHandler, className: "react-flow__controls-zoomin", title: ariaLabelConfig["controls.zoomIn.ariaLabel"], "aria-label": ariaLabelConfig["controls.zoomIn.ariaLabel"], disabled: maxZoomReached, children: (0, import_jsx_runtime3.jsx)(PlusIcon, {}) }), (0, import_jsx_runtime3.jsx)(ControlButton, { onClick: onZoomOutHandler, className: "react-flow__controls-zoomout", title: ariaLabelConfig["controls.zoomOut.ariaLabel"], "aria-label": ariaLabelConfig["controls.zoomOut.ariaLabel"], disabled: minZoomReached, children: (0, import_jsx_runtime3.jsx)(MinusIcon, {}) })] }), showFitView && (0, import_jsx_runtime3.jsx)(ControlButton, { className: "react-flow__controls-fitview", onClick: onFitViewHandler, title: ariaLabelConfig["controls.fitView.ariaLabel"], "aria-label": ariaLabelConfig["controls.fitView.ariaLabel"], children: (0, import_jsx_runtime3.jsx)(FitViewIcon, {}) }), showInteractive && (0, import_jsx_runtime3.jsx)(ControlButton, { className: "react-flow__controls-interactive", onClick: onToggleInteractivity, title: ariaLabelConfig["controls.interactive.ariaLabel"], "aria-label": ariaLabelConfig["controls.interactive.ariaLabel"], children: isInteractive ? (0, import_jsx_runtime3.jsx)(UnlockIcon, {}) : (0, import_jsx_runtime3.jsx)(LockIcon, {}) }), children2] });
}
ControlsComponent.displayName = "Controls";
var Controls = (0, import_react4.memo)(ControlsComponent);
function MiniMapNodeComponent({ id: id2, x: x3, y: y2, width, height, style: style2, color: color2, strokeColor, strokeWidth, className, borderRadius, shapeRendering, selected: selected2, onClick }) {
  const { background, backgroundColor } = style2 || {};
  const fill = color2 || background || backgroundColor;
  return (0, import_jsx_runtime3.jsx)("rect", { className: cc(["react-flow__minimap-node", { selected: selected2 }, className]), x: x3, y: y2, rx: borderRadius, ry: borderRadius, width, height, style: {
    fill,
    stroke: strokeColor,
    strokeWidth
  }, shapeRendering, onClick: onClick ? (event) => onClick(event, id2) : void 0 });
}
var MiniMapNode = (0, import_react4.memo)(MiniMapNodeComponent);
var selectorNodeIds = (s) => s.nodes.map((node) => node.id);
var getAttrFunction = (func) => func instanceof Function ? func : () => func;
function MiniMapNodes({
  nodeStrokeColor,
  nodeColor,
  nodeClassName = "",
  nodeBorderRadius = 5,
  nodeStrokeWidth,
  /*
   * We need to rename the prop to be `CapitalCase` so that JSX will render it as
   * a component properly.
   */
  nodeComponent: NodeComponent = MiniMapNode,
  onClick
}) {
  const nodeIds = useStore(selectorNodeIds, shallow$1);
  const nodeColorFunc = getAttrFunction(nodeColor);
  const nodeStrokeColorFunc = getAttrFunction(nodeStrokeColor);
  const nodeClassNameFunc = getAttrFunction(nodeClassName);
  const shapeRendering = typeof window === "undefined" || !!window.chrome ? "crispEdges" : "geometricPrecision";
  return (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: nodeIds.map((nodeId) => (
    /*
     * The split of responsibilities between MiniMapNodes and
     * NodeComponentWrapper may appear weird. However, it’s designed to
     * minimize the cost of updates when individual nodes change.
     *
     * For more details, see a similar commit in `NodeRenderer/index.tsx`.
     */
    (0, import_jsx_runtime3.jsx)(NodeComponentWrapper, { id: nodeId, nodeColorFunc, nodeStrokeColorFunc, nodeClassNameFunc, nodeBorderRadius, nodeStrokeWidth, NodeComponent, onClick, shapeRendering }, nodeId)
  )) });
}
function NodeComponentWrapperInner({ id: id2, nodeColorFunc, nodeStrokeColorFunc, nodeClassNameFunc, nodeBorderRadius, nodeStrokeWidth, shapeRendering, NodeComponent, onClick }) {
  const { node, x: x3, y: y2, width, height } = useStore((s) => {
    const node2 = s.nodeLookup.get(id2);
    if (!node2) {
      return { node: void 0, x: 0, y: 0, width: 0, height: 0 };
    }
    const userNode = node2.internals.userNode;
    const { x: x4, y: y3 } = node2.internals.positionAbsolute;
    const { width: width2, height: height2 } = getNodeDimensions(userNode);
    return {
      node: userNode,
      x: x4,
      y: y3,
      width: width2,
      height: height2
    };
  }, shallow$1);
  if (!node || node.hidden || !nodeHasDimensions(node)) {
    return null;
  }
  return (0, import_jsx_runtime3.jsx)(NodeComponent, { x: x3, y: y2, width, height, style: node.style, selected: !!node.selected, className: nodeClassNameFunc(node), color: nodeColorFunc(node), borderRadius: nodeBorderRadius, strokeColor: nodeStrokeColorFunc(node), strokeWidth: nodeStrokeWidth, shapeRendering, onClick, id: node.id });
}
var NodeComponentWrapper = (0, import_react4.memo)(NodeComponentWrapperInner);
var MiniMapNodes$1 = (0, import_react4.memo)(MiniMapNodes);
var defaultWidth = 200;
var defaultHeight = 150;
var filterHidden = (node) => !node.hidden;
var selector$1 = (s) => {
  const viewBB = {
    x: -s.transform[0] / s.transform[2],
    y: -s.transform[1] / s.transform[2],
    width: s.width / s.transform[2],
    height: s.height / s.transform[2]
  };
  return {
    viewBB,
    boundingRect: s.nodeLookup.size > 0 ? getBoundsOfRects(getInternalNodesBounds(s.nodeLookup, { filter: filterHidden }), viewBB) : viewBB,
    rfId: s.rfId,
    panZoom: s.panZoom,
    translateExtent: s.translateExtent,
    flowWidth: s.width,
    flowHeight: s.height,
    ariaLabelConfig: s.ariaLabelConfig
  };
};
var rectEqual = (a, b) => a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
var areEqual = (a, b) => rectEqual(a.viewBB, b.viewBB) && rectEqual(a.boundingRect, b.boundingRect) && a.rfId === b.rfId && a.panZoom === b.panZoom && a.translateExtent === b.translateExtent && a.flowWidth === b.flowWidth && a.flowHeight === b.flowHeight && a.ariaLabelConfig === b.ariaLabelConfig;
var ARIA_LABEL_KEY = "react-flow__minimap-desc";
function MiniMapComponent({
  style: style2,
  className,
  nodeStrokeColor,
  nodeColor,
  nodeClassName = "",
  nodeBorderRadius = 5,
  nodeStrokeWidth,
  /*
   * We need to rename the prop to be `CapitalCase` so that JSX will render it as
   * a component properly.
   */
  nodeComponent,
  bgColor,
  maskColor,
  maskStrokeColor,
  maskStrokeWidth,
  position = "bottom-right",
  onClick,
  onNodeClick,
  pannable = false,
  zoomable = false,
  ariaLabel,
  inversePan,
  zoomStep = 1,
  offsetScale = 5
}) {
  const store = useStoreApi();
  const svg = (0, import_react4.useRef)(null);
  const { boundingRect, viewBB, rfId, panZoom, translateExtent, flowWidth, flowHeight, ariaLabelConfig } = useStore(selector$1, areEqual);
  const elementWidth = style2?.width ?? defaultWidth;
  const elementHeight = style2?.height ?? defaultHeight;
  const scaledWidth = boundingRect.width / elementWidth;
  const scaledHeight = boundingRect.height / elementHeight;
  const viewScale = Math.max(scaledWidth, scaledHeight);
  const viewWidth = viewScale * elementWidth;
  const viewHeight = viewScale * elementHeight;
  const offset = offsetScale * viewScale;
  const x3 = boundingRect.x - (viewWidth - boundingRect.width) / 2 - offset;
  const y2 = boundingRect.y - (viewHeight - boundingRect.height) / 2 - offset;
  const width = viewWidth + offset * 2;
  const height = viewHeight + offset * 2;
  const labelledBy = `${ARIA_LABEL_KEY}-${rfId}`;
  const viewScaleRef = (0, import_react4.useRef)(0);
  const minimapInstance = (0, import_react4.useRef)();
  viewScaleRef.current = viewScale;
  (0, import_react4.useEffect)(() => {
    if (svg.current && panZoom) {
      minimapInstance.current = XYMinimap({
        domNode: svg.current,
        panZoom,
        getTransform: () => store.getState().transform,
        getViewScale: () => viewScaleRef.current
      });
      return () => {
        minimapInstance.current?.destroy();
      };
    }
  }, [panZoom]);
  (0, import_react4.useEffect)(() => {
    minimapInstance.current?.update({
      translateExtent,
      width: flowWidth,
      height: flowHeight,
      inversePan,
      pannable,
      zoomStep,
      zoomable
    });
  }, [pannable, zoomable, inversePan, zoomStep, translateExtent, flowWidth, flowHeight]);
  const onSvgClick = onClick ? (event) => {
    const [x4, y3] = minimapInstance.current?.pointer(event) || [0, 0];
    onClick(event, { x: x4, y: y3 });
  } : void 0;
  const onSvgNodeClick = onNodeClick ? (0, import_react4.useCallback)((event, nodeId) => {
    const node = store.getState().nodeLookup.get(nodeId).internals.userNode;
    onNodeClick(event, node);
  }, []) : void 0;
  const _ariaLabel = ariaLabel ?? ariaLabelConfig["minimap.ariaLabel"];
  return (0, import_jsx_runtime3.jsx)(Panel, { position, style: {
    ...style2,
    "--xy-minimap-background-color-props": typeof bgColor === "string" ? bgColor : void 0,
    "--xy-minimap-mask-background-color-props": typeof maskColor === "string" ? maskColor : void 0,
    "--xy-minimap-mask-stroke-color-props": typeof maskStrokeColor === "string" ? maskStrokeColor : void 0,
    "--xy-minimap-mask-stroke-width-props": typeof maskStrokeWidth === "number" ? maskStrokeWidth * viewScale : void 0,
    "--xy-minimap-node-background-color-props": typeof nodeColor === "string" ? nodeColor : void 0,
    "--xy-minimap-node-stroke-color-props": typeof nodeStrokeColor === "string" ? nodeStrokeColor : void 0,
    "--xy-minimap-node-stroke-width-props": typeof nodeStrokeWidth === "number" ? nodeStrokeWidth : void 0
  }, className: cc(["react-flow__minimap", className]), "data-testid": "rf__minimap", children: (0, import_jsx_runtime3.jsxs)("svg", { width: elementWidth, height: elementHeight, viewBox: `${x3} ${y2} ${width} ${height}`, className: "react-flow__minimap-svg", role: "img", "aria-labelledby": labelledBy, ref: svg, onClick: onSvgClick, children: [_ariaLabel && (0, import_jsx_runtime3.jsx)("title", { id: labelledBy, children: _ariaLabel }), (0, import_jsx_runtime3.jsx)(MiniMapNodes$1, { onClick: onSvgNodeClick, nodeColor, nodeStrokeColor, nodeBorderRadius, nodeClassName, nodeStrokeWidth, nodeComponent }), (0, import_jsx_runtime3.jsx)("path", { className: "react-flow__minimap-mask", d: `M${x3 - offset},${y2 - offset}h${width + offset * 2}v${height + offset * 2}h${-width - offset * 2}z
        M${viewBB.x},${viewBB.y}h${viewBB.width}v${viewBB.height}h${-viewBB.width}z`, fillRule: "evenodd", pointerEvents: "none" })] }) });
}
MiniMapComponent.displayName = "MiniMap";
var MiniMap = (0, import_react4.memo)(MiniMapComponent);
var scaleSelector = (calculateScale) => (store) => calculateScale ? `${Math.max(1 / store.transform[2], 1)}` : void 0;
var defaultPositions = {
  [ResizeControlVariant.Line]: "right",
  [ResizeControlVariant.Handle]: "bottom-right"
};
function ResizeControl({ nodeId, position, variant = ResizeControlVariant.Handle, className, style: style2 = void 0, children: children2, color: color2, minWidth = 10, minHeight = 10, maxWidth = Number.MAX_VALUE, maxHeight = Number.MAX_VALUE, keepAspectRatio = false, resizeDirection, autoScale = true, shouldResize, onResizeStart, onResize, onResizeEnd }) {
  const contextNodeId = useNodeId();
  const id2 = typeof nodeId === "string" ? nodeId : contextNodeId;
  const store = useStoreApi();
  const resizeControlRef = (0, import_react4.useRef)(null);
  const isHandleControl = variant === ResizeControlVariant.Handle;
  const scale = useStore((0, import_react4.useCallback)(scaleSelector(isHandleControl && autoScale), [isHandleControl, autoScale]), shallow$1);
  const resizer = (0, import_react4.useRef)(null);
  const controlPosition = position ?? defaultPositions[variant];
  (0, import_react4.useEffect)(() => {
    if (!resizeControlRef.current || !id2) {
      return;
    }
    if (!resizer.current) {
      resizer.current = XYResizer({
        domNode: resizeControlRef.current,
        nodeId: id2,
        getStoreItems: () => {
          const { nodeLookup, transform: transform2, snapGrid, snapToGrid, nodeOrigin, domNode } = store.getState();
          return {
            nodeLookup,
            transform: transform2,
            snapGrid,
            snapToGrid,
            nodeOrigin,
            paneDomNode: domNode
          };
        },
        onChange: (change, childChanges) => {
          const { triggerNodeChanges, nodeLookup, parentLookup, nodeOrigin } = store.getState();
          const changes = [];
          const nextPosition = { x: change.x, y: change.y };
          const node = nodeLookup.get(id2);
          if (node && node.expandParent && node.parentId) {
            const origin = node.origin ?? nodeOrigin;
            const width = change.width ?? node.measured.width ?? 0;
            const height = change.height ?? node.measured.height ?? 0;
            const child = {
              id: node.id,
              parentId: node.parentId,
              rect: {
                width,
                height,
                ...evaluateAbsolutePosition({
                  x: change.x ?? node.position.x,
                  y: change.y ?? node.position.y
                }, { width, height }, node.parentId, nodeLookup, origin)
              }
            };
            const parentExpandChanges = handleExpandParent([child], nodeLookup, parentLookup, nodeOrigin);
            changes.push(...parentExpandChanges);
            nextPosition.x = change.x ? Math.max(origin[0] * width, change.x) : void 0;
            nextPosition.y = change.y ? Math.max(origin[1] * height, change.y) : void 0;
          }
          if (nextPosition.x !== void 0 && nextPosition.y !== void 0) {
            const positionChange = {
              id: id2,
              type: "position",
              position: { ...nextPosition }
            };
            changes.push(positionChange);
          }
          if (change.width !== void 0 && change.height !== void 0) {
            const setAttributes = !resizeDirection ? true : resizeDirection === "horizontal" ? "width" : "height";
            const dimensionChange = {
              id: id2,
              type: "dimensions",
              resizing: true,
              setAttributes,
              dimensions: {
                width: change.width,
                height: change.height
              }
            };
            changes.push(dimensionChange);
          }
          for (const childChange of childChanges) {
            const positionChange = {
              ...childChange,
              type: "position"
            };
            changes.push(positionChange);
          }
          triggerNodeChanges(changes);
        },
        onEnd: ({ width, height }) => {
          const dimensionChange = {
            id: id2,
            type: "dimensions",
            resizing: false,
            dimensions: {
              width,
              height
            }
          };
          store.getState().triggerNodeChanges([dimensionChange]);
        }
      });
    }
    resizer.current.update({
      controlPosition,
      boundaries: {
        minWidth,
        minHeight,
        maxWidth,
        maxHeight
      },
      keepAspectRatio,
      resizeDirection,
      onResizeStart,
      onResize,
      onResizeEnd,
      shouldResize
    });
    return () => {
      resizer.current?.destroy();
    };
  }, [
    controlPosition,
    minWidth,
    minHeight,
    maxWidth,
    maxHeight,
    keepAspectRatio,
    onResizeStart,
    onResize,
    onResizeEnd,
    shouldResize
  ]);
  const positionClassNames = controlPosition.split("-");
  return (0, import_jsx_runtime3.jsx)("div", { className: cc(["react-flow__resize-control", "nodrag", ...positionClassNames, variant, className]), ref: resizeControlRef, style: {
    ...style2,
    scale,
    ...color2 && { [isHandleControl ? "backgroundColor" : "borderColor"]: color2 }
  }, children: children2 });
}
var NodeResizeControl = (0, import_react4.memo)(ResizeControl);
function NodeResizer({ nodeId, isVisible = true, handleClassName, handleStyle: handleStyle3, lineClassName, lineStyle, color: color2, minWidth = 10, minHeight = 10, maxWidth = Number.MAX_VALUE, maxHeight = Number.MAX_VALUE, keepAspectRatio = false, autoScale = true, shouldResize, onResizeStart, onResize, onResizeEnd }) {
  if (!isVisible) {
    return null;
  }
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [XY_RESIZER_LINE_POSITIONS.map((position) => (0, import_jsx_runtime3.jsx)(NodeResizeControl, { className: lineClassName, style: lineStyle, nodeId, position, variant: ResizeControlVariant.Line, color: color2, minWidth, minHeight, maxWidth, maxHeight, onResizeStart, keepAspectRatio, autoScale, shouldResize, onResize, onResizeEnd }, position)), XY_RESIZER_HANDLE_POSITIONS.map((position) => (0, import_jsx_runtime3.jsx)(NodeResizeControl, { className: handleClassName, style: handleStyle3, nodeId, position, color: color2, minWidth, minHeight, maxWidth, maxHeight, onResizeStart, keepAspectRatio, autoScale, shouldResize, onResize, onResizeEnd }, position))] });
}

// src/client/stage/ImageStudio.tsx
var import_react6 = require("react");

// src/client/canvas-theme.ts
var dx = {
  black: "#000000",
  field: "#000000",
  chrome: "rgba(20, 20, 20, .92)",
  ink: "#f5f5f5",
  mute: "#9a9a9a",
  dim: "#6a6a6a",
  ghost: "rgba(255,255,255,.06)",
  ghostStrong: "rgba(255,255,255,.11)",
  hairline: "rgba(255,255,255,.10)",
  hairlineStrong: "rgba(255,255,255,.16)",
  selected: "rgb(122,122,122)",
  glow: "none",
  radiusCard: 16,
  radiusBtn: 10,
  radiusRail: 9999,
  font: '"Inter", "SF Pro Text", "PingFang SC", system-ui, sans-serif',
  mediaW: 400,
  mediaH: 220,
  mediaThumb: 250,
  textW: 250,
  textH: 250,
  groupW: 640,
  groupH: 460,
  minZoom: 0.15,
  maxZoom: 2
};
var dxChrome = {
  background: dx.chrome,
  border: `1px solid ${dx.hairline}`,
  boxShadow: "none",
  color: dx.ink,
  fontFamily: dx.font,
  backdropFilter: "blur(16px)",
  WebkitBackdropFilter: "blur(16px)"
};
var dxGhostBtn = {
  width: 36,
  height: 36,
  borderRadius: 11,
  border: "1px solid transparent",
  background: "transparent",
  color: "#ececec",
  cursor: "pointer",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  padding: 0
};
var dxPill = {
  width: 38,
  height: 38,
  borderRadius: 9999,
  border: "none",
  background: "#f3f3f3",
  color: "#141414",
  cursor: "pointer",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  padding: 0
};
var INTER_HREF = "https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap";

// src/client/stage/icons.tsx
var import_jsx_runtime4 = require("react/jsx-runtime");
var base = { display: "block", flexShrink: 0 };
function Svg(props) {
  const size = props.size ?? 16;
  const rest = {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: props.filled === true ? "currentColor" : "none",
    stroke: props.filled === true ? "none" : "currentColor",
    strokeWidth: 1.75,
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: base,
    className: props.className,
    "aria-hidden": true
  };
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("svg", { ...rest, children: props.children });
}
function IconPlus(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M5 12h14M12 5v14" }) });
}
function IconMinus(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M5 12h14" }) });
}
function IconSearch(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "11", cy: "11", r: "7" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m20 20-3.5-3.5" })
  ] });
}
function IconGrid(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "3", y: "3", width: "7", height: "7", rx: "1.2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "14", y: "3", width: "7", height: "7", rx: "1.2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "3", y: "14", width: "7", height: "7", rx: "1.2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "14", y: "14", width: "7", height: "7", rx: "1.2" })
  ] });
}
function IconUpload(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M12 16V5M7 9l5-5 5 5" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M5 19h14" })
  ] });
}
function IconImage(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "3", y: "5", width: "18", height: "14", rx: "2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "8.5", cy: "10", r: "1.4" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m21 16-5.2-5.2L5 21" })
  ] });
}
function IconVideo(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "3", y: "6", width: "12", height: "12", rx: "2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m15 10 5-2.5v9L15 14Z" })
  ] });
}
function IconText(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M5 6h14M12 6v12M8 18h8" }) });
}
function IconGroup(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "4", y: "4", width: "16", height: "16", rx: "3", strokeDasharray: "3.2 2.4" }) });
}
function IconClose(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M6 6l12 12M18 6 6 18" }) });
}
function IconSpark(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M12 3 13.6 8.4 19 10l-5.4 1.6L12 17l-1.6-5.4L5 10l5.4-1.6Z" }) });
}
function IconSend(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M4 12h13M13 7l6 5-6 5" }) });
}
function IconStop(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, filled: true, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "6.5", y: "6.5", width: "11", height: "11", rx: "2" }) });
}
function IconLeave(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M9 5H6a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h3M12 12h9M17 8l4 4-4 4" }) });
}
function IconEdit(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M13 5.5 18.5 11M4 20l1.6-6.2L15.2 4.2a1.8 1.8 0 0 1 2.6 0l1.9 1.9a1.8 1.8 0 0 1 0 2.6L10.2 18.4Z" }) });
}
function IconCopy(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "8", y: "8", width: "11", height: "11", rx: "2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M5 16V6a2 2 0 0 1 2-2h10" })
  ] });
}
function IconTrash(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M4 7h16M9 7V5a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2M7 7l.8 12a2 2 0 0 0 2 1.8h4.4a2 2 0 0 0 2-1.8L17 7" }) });
}
function IconCompare(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "3", y: "5", width: "8", height: "14", rx: "1.6" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "13", y: "5", width: "8", height: "14", rx: "1.6" })
  ] });
}
function IconFit(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M4 9V4h5M15 4h5v5M20 15v5h-5M9 20H4v-5" }) });
}
function IconChevron(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m7 10 5 5 5-5" }) });
}
function IconFolder(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M3 7h5l2 2h11v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z" }) });
}
function IconLock(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "5", y: "11", width: "14", height: "10", rx: "2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M8 11V8a4 4 0 0 1 8 0v3" })
  ] });
}
function IconUnlock(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "5", y: "11", width: "14", height: "10", rx: "2" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M8 11V8a4 4 0 0 1 8 0" })
  ] });
}
function IconDownload(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M12 4v11M7 11l5 5 5-5" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M5 20h14" })
  ] });
}
function IconHelp(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "12", cy: "12", r: "9" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M9.5 9.2a2.6 2.6 0 1 1 3.2 3.4V14" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M12 17.2h.01" })
  ] });
}
function IconAlign(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M4 6h16M4 12h11M4 18h16" }) });
}
function IconCheck(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m5 12.5 4.2 4.3L19 7" }) });
}
function IconUnlink(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m9 15-2 2a3.2 3.2 0 0 1-4.5-4.5l2-2M15 9l2-2a3.2 3.2 0 0 1 4.5 4.5l-2 2M10 14l4-4" }) });
}
function IconPlay(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, filled: true, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M8 5.5 19 12 8 18.5Z" }) });
}
function IconPause(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M8 5h3v14H8ZM13 5h3v14h-3Z" }) });
}
function IconUndo(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M5 10h8.5a4.5 4.5 0 1 1 0 9H11" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M9 6 5 10l4 4" })
  ] });
}
function IconRedo(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M19 10H10.5a4.5 4.5 0 1 0 0 9H13" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m15 6 4 4-4 4" })
  ] });
}
function IconRotate(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M20 12a8 8 0 1 1-2.2-5.5" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M20 5v5h-5" })
  ] });
}
function IconCrop(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M6 3v14h14M18 21V7H4" }) });
}
function IconBrush(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m15 4 5 5-9 9H6v-5Z" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m13 6 5 5" })
  ] });
}
function IconSliders(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M4 7h16M4 12h16M4 17h16" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "8", cy: "7", r: "1.6" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "16", cy: "12", r: "1.6" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "11", cy: "17", r: "1.6" })
  ] });
}
function IconScissors(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "6", cy: "6", r: "2.4" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "6", cy: "18", r: "2.4" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "m8 8 12 11M8 16 20 5" })
  ] });
}
function IconSkipBack(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M6 5v14M19 6 10 12l9 6Z" }) });
}
function IconSkipFwd(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M18 5v14M5 6l9 6-9 6Z" }) });
}
function IconBox(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M12 3 20 7.5v9L12 21 4 16.5v-9Z" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M12 21V12M4 7.5 12 12l8-4.5" })
  ] });
}
function IconAudio(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M4 10v4M8 7v10M12 4v16M16 7v10M20 10v4" }) });
}
function IconMore(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(Svg, { ...props, filled: true, children: [
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "6", cy: "12", r: "1.4" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "12", cy: "12", r: "1.4" }),
    /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("circle", { cx: "18", cy: "12", r: "1.4" })
  ] });
}
function IconMax(props = {}) {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Svg, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M4 9V4h5M15 4h5v5M20 15v5h-5M9 20H4v-5" }) });
}
function KindGlyph(props) {
  if (props.kind === "video") return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(IconVideo, { size: props.size });
  if (props.kind === "image") return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(IconImage, { size: props.size });
  if (props.kind === "audio") return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(IconAudio, { size: props.size });
  if (props.kind === "group") return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(IconGroup, { size: props.size });
  if (props.kind === "director-stage") return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(IconBox, { size: props.size });
  if (props.kind === "edit") return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(IconScissors, { size: props.size });
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(IconText, { size: props.size });
}

// src/client/stage/studio-chrome.tsx
var import_react5 = require("react");
var import_jsx_runtime5 = require("react/jsx-runtime");
var studioBtn = {
  ...dxGhostBtn,
  width: "auto",
  height: 32,
  padding: "0 10px",
  gap: 6,
  fontSize: 12,
  fontFamily: dx.font,
  color: dx.ink,
  border: `1px solid ${dx.hairline}`,
  background: "rgba(255,255,255,.04)"
};
var studioPrimary = {
  ...dxPill,
  width: "auto",
  height: 34,
  padding: "0 14px",
  gap: 6,
  fontSize: 12,
  fontWeight: 600,
  fontFamily: dx.font
};
var railBtn = {
  ...dxGhostBtn,
  width: 36,
  height: 36
};
function StudioShell(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "dx-studio", style: {
    position: "absolute",
    inset: 0,
    zIndex: 40,
    display: "flex",
    flexDirection: "column",
    background: dx.black,
    color: dx.ink,
    fontFamily: dx.font
  }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { style: {
      ...dxChrome,
      display: "flex",
      alignItems: "center",
      gap: 10,
      padding: "10px 14px",
      borderRadius: 0,
      borderLeft: "none",
      borderRight: "none",
      borderTop: "none"
    }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { style: { fontSize: 13, fontWeight: 600 }, children: props.title }),
        /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { style: { fontSize: 11, color: dx.mute, marginTop: 2, maxWidth: 360, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: props.filename })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("span", { style: { fontSize: 11, color: dx.dim }, children: props.meta }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("span", { style: { flex: 1 } }),
      props.onUndo !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("button", { className: "dx-hit", style: studioBtn, disabled: props.canUndo !== true, onClick: props.onUndo, children: "\u64A4\u9500" }) : null,
      props.onRedo !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("button", { className: "dx-hit", style: studioBtn, disabled: props.canRedo !== true, onClick: props.onRedo, children: "\u91CD\u505A" }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("span", { style: { fontSize: 11, color: dx.dim, letterSpacing: 0.2 }, children: "Esc \u5B8C\u6210 \xB7 \u2318S \u4FDD\u5B58" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("button", { className: "dx-hit dx-cta", style: studioPrimary, disabled: props.saveDisabled === true, onClick: props.onSave, children: props.saveLabel }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: props.onClose, children: props.closeLabel ?? "\u5B8C\u6210" })
    ] }),
    props.error !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { style: { padding: "8px 16px", color: "#ff9b8f", fontSize: 12, background: "rgba(40,16,14,.85)" }, children: props.error }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { style: { flex: 1, minHeight: 0, display: "flex" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { style: {
        ...dxChrome,
        width: 52,
        margin: 10,
        marginRight: 0,
        borderRadius: 18,
        padding: 8,
        display: "flex",
        flexDirection: "column",
        gap: 4
      }, children: props.tools.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
        "button",
        {
          className: "dx-hit",
          title: item3.label,
          onClick: () => props.onTool(item3.id),
          style: {
            ...railBtn,
            background: props.tool === item3.id ? "rgba(255,255,255,.14)" : "transparent"
          },
          children: item3.icon
        },
        item3.id
      )) }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { style: { flex: 1, minWidth: 0, minHeight: 0, position: "relative", display: "flex", flexDirection: "column" }, children: props.children }),
      props.inspector !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { style: {
        ...dxChrome,
        width: 268,
        margin: 10,
        marginLeft: 0,
        borderRadius: 18,
        padding: 14,
        overflow: "auto"
      }, children: props.inspector }) : null
    ] }),
    props.footer
  ] });
}
function StudioField(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("label", { style: { display: "block", marginBottom: 12, fontSize: 12, color: dx.mute }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { style: { marginBottom: 6 }, children: props.label }),
    props.children
  ] });
}
function studioRange() {
  return { width: "100%", accentColor: "#f3f3f3" };
}
var StudioErrorBoundary = class extends import_react5.Component {
  constructor(props) {
    super(props);
    this.state = { message: "" };
  }
  static getDerivedStateFromError(error) {
    const text = error instanceof Error ? error.stack ?? error.message : String(error);
    return { message: text || String(error) };
  }
  componentDidCatch(error, info) {
    console.error("DirectorX studio", error, info.componentStack);
  }
  render() {
    if (this.state.message !== "") {
      return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("pre", { "data-stage-crash": "1", style: { position: "absolute", inset: 0, overflow: "auto", color: "#ff9b8f", fontFamily: dx.font, fontSize: 12, padding: 24, whiteSpace: "pre-wrap", background: "#111", margin: 0 }, children: this.state.message });
    }
    return this.props.children;
  }
};

// src/providers/grade-catalog.ts
var GRADE_LOOKS = [
  "wasteland",
  "teal-orange",
  "bleach-bypass",
  "cyber",
  "film-fade",
  "vintage",
  "kodachrome",
  "portra",
  "cross-process",
  "color-negative",
  "bw-contrast",
  "muted",
  "night",
  "cold",
  "warm",
  "golden"
];
var GRADE_FAMILIES = [
  { id: "cinema", label: "\u7535\u5F71" },
  { id: "film", label: "\u80F6\u7247" },
  { id: "tone", label: "\u5F71\u8C03" }
];
var GRADE_TABLE = {
  wasteland: {
    id: "wasteland",
    label: "\u8352\u571F",
    family: "cinema",
    vf: "format=yuv420p10le,colorbalance=rs=0.16:gs=0.05:bs=-0.18:rh=0.14:gh=0.02:bh=-0.12,eq=contrast=1.16:saturation=0.68:brightness=-0.05:gamma=1.08,vignette=angle=PI/6,format=yuv420p",
    css: "sepia(0.38) saturate(0.72) contrast(1.16) brightness(0.94) hue-rotate(-14deg)",
    pattern: /末日|荒土|废土|黄沙|沙尘|dust|wasteland|desert|apocalyp/i,
    source: "DirectorX 36-color-luts \u6218\u4E89/\u672B\u65E5\u6761"
  },
  "teal-orange": {
    id: "teal-orange",
    label: "\u9752\u6A59",
    family: "cinema",
    vf: "format=yuv420p10le,colorbalance=rs=-0.12:gs=0.06:bs=0.12:rh=0.12:gh=0.03:bh=-0.12:rm=0.03:bm=-0.03,eq=contrast=1.08:saturation=1.2,format=yuv420p",
    css: "saturate(1.18) contrast(1.08) hue-rotate(8deg)",
    pattern: /青橙|teal|orange|好莱坞/i,
    source: "36-color-luts \u9752\u6A59 + Pixflow \u516C\u5F00\u6559\u7A0B"
  },
  "bleach-bypass": {
    id: "bleach-bypass",
    label: "\u6F02\u767D",
    family: "cinema",
    vf: "format=yuv420p10le,eq=saturation=0.42:contrast=1.38:brightness=-0.04:gamma=1.06,colorbalance=bs=0.06:rs=-0.04:bh=0.04,curves=preset=strong_contrast,format=yuv420p",
    css: "grayscale(0.35) contrast(1.32) saturate(0.55) brightness(0.96)",
    pattern: /漂白|bypass|银盐|skip.?bleach/i,
    source: "36-color-luts \u6F02\u767D\u65C1\u8DEF + FFmpeg curves=strong_contrast"
  },
  cyber: {
    id: "cyber",
    label: "\u9713\u8679",
    family: "cinema",
    vf: "format=yuv420p10le,colorbalance=bs=0.16:rs=-0.08:rh=0.14:bh=-0.06:gm=-0.04:bm=0.08,eq=contrast=1.18:saturation=1.35:gamma=0.98,format=yuv420p",
    css: "saturate(1.42) contrast(1.18) hue-rotate(-12deg) brightness(1.02)",
    pattern: /赛博|霓虹|cyber|neon|品红青|品红/i,
    source: "yeun/open-color MIT \u9752/\u7C89\u951A + chromotome MIT \u9713\u8679\u677F\u6C14\u8D28"
  },
  "film-fade": {
    id: "film-fade",
    label: "\u80F6\u7247",
    family: "film",
    vf: "format=yuv420p10le,curves=master='0/0.05 0.2/0.24 0.6/0.58 1/0.96':interp=pchip,eq=saturation=0.85:gamma=1.05,colorbalance=rh=0.06:bh=-0.05,vignette=angle=PI/7,noise=alls=5:allf=t+u,format=yuv420p",
    css: "sepia(0.18) saturate(0.86) contrast(1.05) brightness(1.02)",
    pattern: /胶片|褪色|film.?fade|fade.?film/i,
    source: "36-color-luts \u80F6\u7247\u6A21\u62DF"
  },
  vintage: {
    id: "vintage",
    label: "\u590D\u53E4",
    family: "film",
    vf: "format=yuv420p10le,curves=preset=vintage,eq=saturation=0.82:gamma=1.06,vignette=angle=PI/6,noise=alls=6:allf=t+u,format=yuv420p",
    css: "sepia(0.32) saturate(0.78) contrast(1.08) brightness(1.04)",
    pattern: /vintage|复古|怀旧|老照片/i,
    source: "FFmpeg libavfilter curves=preset=vintage\uFF08\u8FD0\u884C\u65F6\uFF0C\u4E0D\u6346\u7ED1\u6E90\u7801\uFF09"
  },
  kodachrome: {
    id: "kodachrome",
    label: "\u514B\u7F57\u59C6",
    family: "film",
    vf: "format=yuv420p10le,colorbalance=rs=0.1:rh=0.12:bs=-0.06:bh=0.08:gs=-0.02,eq=contrast=1.22:saturation=1.28:gamma=0.96,curves=preset=increase_contrast,format=yuv420p",
    css: "saturate(1.28) contrast(1.2) brightness(0.98) hue-rotate(-6deg)",
    pattern: /kodachrome|克罗姆|柯达正片|velvia|正片/i,
    source: "colour-science/colour BSD-3 \u6B63\u7247\u7279\u5F81\u8FD1\u4F3C\uFF0C\u975E\u5546\u6807\u80F6\u7247\u62F7\u8D1D"
  },
  portra: {
    id: "portra",
    label: "\u4EBA\u50CF\u8D1F\u7247",
    family: "film",
    vf: "format=yuv420p10le,curves=master='0/0.04 0.25/0.28 0.6/0.62 1/0.97':interp=pchip,colorbalance=rs=0.08:gs=0.04:bs=-0.04:rh=0.06:gh=0.02,eq=saturation=0.92:contrast=0.98:gamma=1.04,format=yuv420p",
    css: "saturate(0.92) contrast(0.98) brightness(1.04) sepia(0.08)",
    pattern: /portra|波特拉|人像胶片|人像负片|肤色胶片/i,
    source: "colour-science/colour BSD-3 \u8D1F\u7247\u80A4\u8272\u503E\u5411\u8FD1\u4F3C\uFF1BFilm-Luts MIT \u6C14\u8D28\u5BF9\u7167\uFF08\u672A\u6346\u7ED1 .cube\uFF09"
  },
  "cross-process": {
    id: "cross-process",
    label: "\u4EA4\u53C9\u51B2\u5370",
    family: "film",
    vf: "format=yuv420p10le,curves=preset=cross_process,eq=saturation=1.12:contrast=1.08,format=yuv420p",
    css: "saturate(1.2) contrast(1.1) hue-rotate(-18deg)",
    pattern: /交叉冲印|cross.?process|冲印/i,
    source: "FFmpeg curves=preset=cross_process + 36-color-luts \u4EA4\u53C9\u51B2\u5370\u6761"
  },
  "color-negative": {
    id: "color-negative",
    label: "\u8D1F\u7247",
    family: "film",
    vf: "format=yuv420p10le,curves=preset=color_negative,eq=contrast=1.05,format=yuv420p",
    css: "invert(1) hue-rotate(180deg) contrast(1.05)",
    pattern: /负片|color.?negative|invert|反相|底片/i,
    source: "FFmpeg curves=preset=color_negative"
  },
  "bw-contrast": {
    id: "bw-contrast",
    label: "\u9ED1\u767D",
    family: "film",
    vf: "format=yuv420p10le,hue=s=0,curves=preset=strong_contrast,eq=contrast=1.15:brightness=-0.02,noise=alls=4:allf=t,format=yuv420p",
    css: "grayscale(1) contrast(1.18) brightness(0.98)",
    pattern: /黑白|noir|mono|grayscale|b\s*[&\/]\s*w/i,
    source: "FFmpeg curves=preset=strong_contrast + hue=s=0"
  },
  muted: {
    id: "muted",
    label: "\u4F4E\u9971\u548C",
    family: "tone",
    vf: "format=yuv420p10le,eq=saturation=0.55:contrast=0.96:brightness=0.02:gamma=1.04,colorbalance=rs=0.03:bs=0.02,format=yuv420p",
    css: "saturate(0.58) contrast(0.96) brightness(1.02)",
    pattern: /低饱和|muted|desat|去饱和|淡彩|北欧|冷淡/i,
    source: "36-color-luts \u4F4E\u9971\u548C\u6761 + d3 ColorBrewer \u4F4E\u5F69\u5E8F\u5217\u6C14\u8D28"
  },
  night: {
    id: "night",
    label: "\u591C\u8272",
    family: "tone",
    vf: "format=yuv420p10le,colorbalance=bs=0.18:gs=0.04:rs=-0.14:bh=0.1:rh=-0.08,eq=contrast=1.12:saturation=0.78:brightness=-0.08:gamma=1.08,format=yuv420p",
    css: "saturate(0.78) contrast(1.12) brightness(0.88) hue-rotate(196deg)",
    pattern: /夜色|月光|夜景|night|moonlight|深夜/i,
    source: "yeun/open-color MIT indigo/blue \u951A"
  },
  cold: {
    id: "cold",
    label: "\u51B7\u8C03",
    family: "tone",
    vf: "format=yuv420p10le,colorbalance=bs=0.14:gs=0.04:rs=-0.1:bh=0.08:rh=-0.06,eq=contrast=1.06:saturation=0.9:gamma=1.04,format=yuv420p",
    css: "saturate(0.9) contrast(1.06) hue-rotate(168deg) brightness(1.02)",
    pattern: /冷调|冷色|青冷|steel|cyan|ice/i,
    source: "36-color-luts \u51B7\u79D1\u6280\u6761"
  },
  warm: {
    id: "warm",
    label: "\u6696\u8C03",
    family: "tone",
    vf: "format=yuv420p10le,colorbalance=rs=0.12:gs=0.04:bs=-0.1:rh=0.1:bh=-0.06,eq=contrast=1.05:saturation=1.08:gamma=1.03,format=yuv420p",
    css: "sepia(0.16) saturate(1.08) contrast(1.05) brightness(1.03) hue-rotate(-8deg)",
    pattern: /暖调|暖色|warm/i,
    source: "36-color-luts \u6696\u8BB0\u5FC6\u6761"
  },
  golden: {
    id: "golden",
    label: "\u91D1\u9EC4\u660F",
    family: "tone",
    vf: "format=yuv420p10le,colorbalance=rs=0.16:gs=0.08:bs=-0.14:rh=0.14:gh=0.04:bh=-0.1,eq=contrast=1.08:saturation=1.12:gamma=1.04:brightness=0.03,format=yuv420p",
    css: "sepia(0.22) saturate(1.16) contrast(1.08) brightness(1.06) hue-rotate(-12deg)",
    pattern: /金色|黄昏|golden|sunset|magic.?hour|黄金时刻|日落/i,
    source: "yeun/open-color MIT yellow/orange \u951A + 36-color-luts \u6696\u8BB0\u5FC6"
  }
};
var GRADE_LOOK_LIST = GRADE_LOOKS.map((id2) => GRADE_TABLE[id2]);
var GRADE_ALIASES = [
  { look: "bleach-bypass", pattern: GRADE_TABLE["bleach-bypass"].pattern },
  { look: "cross-process", pattern: GRADE_TABLE["cross-process"].pattern },
  { look: "portra", pattern: GRADE_TABLE.portra.pattern },
  { look: "kodachrome", pattern: GRADE_TABLE.kodachrome.pattern },
  { look: "color-negative", pattern: GRADE_TABLE["color-negative"].pattern },
  { look: "teal-orange", pattern: GRADE_TABLE["teal-orange"].pattern },
  { look: "wasteland", pattern: GRADE_TABLE.wasteland.pattern },
  { look: "vintage", pattern: GRADE_TABLE.vintage.pattern },
  { look: "cyber", pattern: GRADE_TABLE.cyber.pattern },
  { look: "golden", pattern: GRADE_TABLE.golden.pattern },
  { look: "night", pattern: GRADE_TABLE.night.pattern },
  { look: "muted", pattern: GRADE_TABLE.muted.pattern },
  { look: "bw-contrast", pattern: GRADE_TABLE["bw-contrast"].pattern },
  { look: "film-fade", pattern: GRADE_TABLE["film-fade"].pattern },
  { look: "cold", pattern: GRADE_TABLE.cold.pattern },
  { look: "warm", pattern: GRADE_TABLE.warm.pattern }
];
function looksByFamily(family) {
  return GRADE_LOOK_LIST.filter((item3) => item3.family === family);
}

// src/client/stage/ImageStudio.tsx
var import_jsx_runtime6 = require("react/jsx-runtime");
var LOOKS = GRADE_LOOK_LIST;
var ASPECTS = [
  { id: "free", label: "\u81EA\u7531" },
  { id: "1", label: "1:1", value: 1 },
  { id: "16:9", label: "16:9", value: 16 / 9 },
  { id: "9:16", label: "9:16", value: 9 / 16 },
  { id: "4:3", label: "4:3", value: 4 / 3 }
];
function cloneCanvas(source) {
  const next = document.createElement("canvas");
  next.width = source.width;
  next.height = source.height;
  next.getContext("2d")?.drawImage(source, 0, 0);
  return next;
}
function constrainRect(x0, y0, x1, y1, aspect) {
  let w = x1 - x0;
  let h = y1 - y0;
  if (aspect !== void 0 && aspect > 0) {
    const aw = Math.abs(w);
    const ah = Math.abs(w) / aspect;
    w = w < 0 ? -aw : aw;
    h = h < 0 ? -ah : ah;
  }
  return {
    x: Math.min(x0, x0 + w),
    y: Math.min(y0, y0 + h),
    w: Math.abs(w),
    h: Math.abs(h)
  };
}
function ImageStudio(props) {
  const stageRef = (0, import_react6.useRef)(null);
  const frameRef = (0, import_react6.useRef)(null);
  const workRef = (0, import_react6.useRef)(null);
  const historyRef = (0, import_react6.useRef)([]);
  const redoRef = (0, import_react6.useRef)([]);
  const dragRef = (0, import_react6.useRef)(void 0);
  const [tool, setTool] = (0, import_react6.useState)("move");
  const [error, setError] = (0, import_react6.useState)(void 0);
  const [busy, setBusy] = (0, import_react6.useState)(true);
  const [size, setSize] = (0, import_react6.useState)({ width: 0, height: 0 });
  const [zoom, setZoom] = (0, import_react6.useState)(1);
  const [pan, setPan] = (0, import_react6.useState)({ x: 0, y: 0 });
  const [brush, setBrush] = (0, import_react6.useState)(16);
  const [color2, setColor] = (0, import_react6.useState)("#ffffff");
  const [fontSize, setFontSize] = (0, import_react6.useState)(48);
  const [adjust, setAdjust] = (0, import_react6.useState)({ brightness: 1, contrast: 1, saturate: 1 });
  const [lookId, setLookId] = (0, import_react6.useState)(void 0);
  const [crop, setCrop] = (0, import_react6.useState)(void 0);
  const [aspect, setAspect] = (0, import_react6.useState)("free");
  const [textDraft, setTextDraft] = (0, import_react6.useState)(void 0);
  const [canUndo, setCanUndo] = (0, import_react6.useState)(false);
  const [canRedo, setCanRedo] = (0, import_react6.useState)(false);
  const paint = () => {
    const view = stageRef.current;
    const work = workRef.current;
    const frame2 = frameRef.current;
    if (view === null || work === null) return;
    const box2 = frame2?.getBoundingClientRect();
    const maxW = Math.max(240, Math.round(box2?.width ?? 800) - 32);
    const maxH = Math.max(200, Math.round(box2?.height ?? 520) - 32);
    const scale = Math.min(maxW / work.width, maxH / work.height, 1);
    view.width = Math.max(1, Math.round(work.width * scale));
    view.height = Math.max(1, Math.round(work.height * scale));
    const ctx = view.getContext("2d");
    if (ctx === null) return;
    const tone = LOOKS.find((item3) => item3.id === lookId)?.css;
    const filter2 = [tone, `brightness(${adjust.brightness})`, `contrast(${adjust.contrast})`, `saturate(${adjust.saturate})`].filter(Boolean).join(" ");
    ctx.filter = filter2;
    ctx.drawImage(work, 0, 0, view.width, view.height);
    ctx.filter = "none";
    if (crop !== void 0) {
      ctx.fillStyle = "rgba(0,0,0,.48)";
      ctx.fillRect(0, 0, view.width, view.height);
      ctx.filter = filter2;
      ctx.drawImage(
        work,
        crop.x / view.width * work.width,
        crop.y / view.height * work.height,
        crop.w / view.width * work.width,
        crop.h / view.height * work.height,
        crop.x,
        crop.y,
        crop.w,
        crop.h
      );
      ctx.filter = "none";
      ctx.strokeStyle = "#fff";
      ctx.lineWidth = 1;
      ctx.setLineDash([6, 4]);
      ctx.strokeRect(crop.x + 0.5, crop.y + 0.5, crop.w, crop.h);
      ctx.setLineDash([]);
    }
  };
  const syncStacks = () => {
    setCanUndo(historyRef.current.length > 0);
    setCanRedo(redoRef.current.length > 0);
  };
  (0, import_react6.useEffect)(() => {
    let live = true;
    const image = new Image();
    image.crossOrigin = "anonymous";
    image.onload = () => {
      if (!live) return;
      const canvas = document.createElement("canvas");
      canvas.width = image.naturalWidth || 1;
      canvas.height = image.naturalHeight || 1;
      canvas.getContext("2d")?.drawImage(image, 0, 0);
      workRef.current = canvas;
      historyRef.current = [];
      redoRef.current = [];
      setSize({ width: canvas.width, height: canvas.height });
      setBusy(false);
      setZoom(1);
      setPan({ x: 0, y: 0 });
      paint();
      syncStacks();
    };
    image.onerror = () => {
      if (live) {
        setBusy(false);
        setError("\u56FE\u7247\u52A0\u8F7D\u5931\u8D25");
      }
    };
    image.src = props.source;
    return () => {
      live = false;
    };
  }, [props.source]);
  (0, import_react6.useEffect)(() => {
    if (props.look === void 0 || props.look === null || props.look === "") return;
    setLookId(props.look);
    setTool("adjust");
  }, [props.look, props.source]);
  (0, import_react6.useEffect)(() => {
    paint();
  }, [adjust, crop, tool, size, lookId]);
  (0, import_react6.useEffect)(() => {
    const frame2 = frameRef.current;
    if (frame2 === null) return;
    const observer = new ResizeObserver(() => paint());
    observer.observe(frame2);
    return () => observer.disconnect();
  }, []);
  const snapshot2 = () => {
    const work = workRef.current;
    if (work === null) return;
    historyRef.current.push(cloneCanvas(work));
    if (historyRef.current.length > 30) historyRef.current.shift();
    redoRef.current = [];
    syncStacks();
  };
  const commit = (next) => {
    snapshot2();
    workRef.current = next;
    setSize({ width: next.width, height: next.height });
    setCrop(void 0);
    paint();
  };
  const mapPoint = (event) => {
    const view = stageRef.current;
    if (view === null) return { x: 0, y: 0 };
    const rect = view.getBoundingClientRect();
    return {
      x: (event.clientX - rect.left) / rect.width * view.width,
      y: (event.clientY - rect.top) / rect.height * view.height
    };
  };
  const applyAdjust = () => {
    const work = workRef.current;
    if (work === null) return;
    const next = document.createElement("canvas");
    next.width = work.width;
    next.height = work.height;
    const ctx = next.getContext("2d");
    if (ctx === null) return;
    const tone = LOOKS.find((item3) => item3.id === lookId)?.css;
    ctx.filter = [tone, `brightness(${adjust.brightness})`, `contrast(${adjust.contrast})`, `saturate(${adjust.saturate})`].filter(Boolean).join(" ");
    ctx.drawImage(work, 0, 0);
    setAdjust({ brightness: 1, contrast: 1, saturate: 1 });
    setLookId(void 0);
    commit(next);
  };
  const rotate = () => {
    const work = workRef.current;
    if (work === null) return;
    const next = document.createElement("canvas");
    next.width = work.height;
    next.height = work.width;
    const ctx = next.getContext("2d");
    if (ctx === null) return;
    ctx.translate(next.width, 0);
    ctx.rotate(Math.PI / 2);
    ctx.drawImage(work, 0, 0);
    commit(next);
  };
  const flip = (axis) => {
    const work = workRef.current;
    if (work === null) return;
    const next = document.createElement("canvas");
    next.width = work.width;
    next.height = work.height;
    const ctx = next.getContext("2d");
    if (ctx === null) return;
    if (axis === "h") {
      ctx.translate(next.width, 0);
      ctx.scale(-1, 1);
    } else {
      ctx.translate(0, next.height);
      ctx.scale(1, -1);
    }
    ctx.drawImage(work, 0, 0);
    commit(next);
  };
  const applyCrop = () => {
    const work = workRef.current;
    const view = stageRef.current;
    if (work === null || view === null || crop === void 0 || crop.w < 4 || crop.h < 4) return;
    const sx = crop.x / view.width * work.width;
    const sy = crop.y / view.height * work.height;
    const sw = crop.w / view.width * work.width;
    const sh = crop.h / view.height * work.height;
    const next = document.createElement("canvas");
    next.width = Math.max(1, Math.round(sw));
    next.height = Math.max(1, Math.round(sh));
    next.getContext("2d")?.drawImage(work, sx, sy, sw, sh, 0, 0, next.width, next.height);
    commit(next);
  };
  const undo = () => {
    const prev = historyRef.current.pop();
    const work = workRef.current;
    if (prev === void 0 || work === null) return;
    redoRef.current.push(cloneCanvas(work));
    workRef.current = prev;
    setSize({ width: prev.width, height: prev.height });
    setCrop(void 0);
    paint();
    syncStacks();
  };
  const redo = () => {
    const next = redoRef.current.pop();
    const work = workRef.current;
    if (next === void 0 || work === null) return;
    historyRef.current.push(cloneCanvas(work));
    workRef.current = next;
    setSize({ width: next.width, height: next.height });
    setCrop(void 0);
    paint();
    syncStacks();
  };
  const commitText = () => {
    const draft = textDraft;
    const work = workRef.current;
    const view = stageRef.current;
    if (draft === void 0 || draft.value.trim() === "" || work === null || view === null) {
      setTextDraft(void 0);
      return;
    }
    const next = cloneCanvas(work);
    const ctx = next.getContext("2d");
    if (ctx === null) return;
    ctx.fillStyle = color2;
    ctx.font = `${Math.round(fontSize * (work.width / Math.max(view.width, 1)))}px ${dx.font}`;
    ctx.fillText(draft.value, draft.x / view.width * work.width, draft.y / view.height * work.height);
    setTextDraft(void 0);
    commit(next);
  };
  const exportPng = () => {
    const work = workRef.current;
    if (work === null) return;
    work.toBlob((blob) => {
      if (blob === null) setError("\u5BFC\u51FA\u5931\u8D25");
      else props.onExport(blob, "image/png");
    }, "image/png");
  };
  (0, import_react6.useEffect)(() => {
    const onKey = (event) => {
      const target = event.target;
      const typing = typeof target?.matches === "function" && target.matches("input, textarea");
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "z") {
        event.preventDefault();
        if (event.shiftKey) redo();
        else undo();
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "s") {
        event.preventDefault();
        exportPng();
        return;
      }
      if (typing) return;
      if (event.key === "Enter" && tool === "crop") applyCrop();
      if (event.key === "Enter" && textDraft !== void 0) commitText();
      if (event.key === "Escape") {
        event.preventDefault();
        if (crop !== void 0 || textDraft !== void 0) {
          setCrop(void 0);
          setTextDraft(void 0);
          return;
        }
        props.onClose();
        return;
      }
      if (event.key === "v" || event.key === "V") setTool("move");
      if (event.key === "c" || event.key === "C") setTool("crop");
      if (event.key === "b" || event.key === "B") setTool("draw");
      if (event.key === "t" || event.key === "T") setTool("text");
      if (event.key === "a" || event.key === "A") setTool("adjust");
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  });
  const ratio = ASPECTS.find((item3) => item3.id === aspect)?.value;
  const filename = props.name.split("/").pop() ?? props.name;
  return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
    StudioShell,
    {
      title: "\u56FE\u7247\u7F16\u8F91",
      filename,
      meta: [
        size.width > 0 ? `${size.width}\xD7${size.height}` : "",
        `${Math.round(zoom * 100)}%`
      ].filter((part) => part !== "").join(" \xB7 "),
      error,
      saveLabel: "\u4FDD\u5B58\u5230\u753B\u5E03",
      saveDisabled: busy || workRef.current === null,
      onSave: exportPng,
      onClose: props.onClose,
      canUndo,
      canRedo,
      onUndo: undo,
      onRedo: redo,
      tools: [
        { id: "move", label: "\u79FB\u52A8", icon: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(IconFit, { size: 15 }) },
        { id: "crop", label: "\u88C1\u526A", icon: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(IconCrop, { size: 15 }) },
        { id: "draw", label: "\u753B\u7B14", icon: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(IconBrush, { size: 15 }) },
        { id: "text", label: "\u6587\u5B57", icon: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(IconText, { size: 15 }) },
        { id: "adjust", label: "\u8C03\u8282", icon: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(IconSliders, { size: 15 }) }
      ],
      tool,
      onTool: (id2) => setTool(id2),
      inspector: /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, { children: [
        tool === "crop" ? /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: "\u753B\u5E45", children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: ASPECTS.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
            "button",
            {
              className: "dx-hit",
              style: { ...studioBtn, background: aspect === item3.id ? "rgba(255,255,255,.14)" : studioBtn.background },
              onClick: () => setAspect(item3.id),
              children: item3.label
            },
            item3.id
          )) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("button", { className: "dx-hit", style: studioBtn, onClick: applyCrop, children: [
            /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(IconCheck, { size: 13 }),
            "\u5E94\u7528\u88C1\u526A"
          ] })
        ] }) : null,
        tool === "draw" ? /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: "\u989C\u8272", children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("input", { type: "color", value: color2, onChange: (event) => setColor(event.target.value), style: { width: "100%", height: 32, border: "none", background: "transparent" } }) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: `\u7B14\u5237 ${brush}px`, children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("input", { type: "range", min: 2, max: 72, value: brush, onChange: (event) => setBrush(Number(event.target.value)), style: studioRange() }) })
        ] }) : null,
        tool === "text" ? /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: "\u989C\u8272", children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("input", { type: "color", value: color2, onChange: (event) => setColor(event.target.value), style: { width: "100%", height: 32, border: "none", background: "transparent" } }) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: `\u5B57\u53F7 ${fontSize}`, children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("input", { type: "range", min: 16, max: 160, value: fontSize, onChange: (event) => setFontSize(Number(event.target.value)), style: studioRange() }) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("div", { style: { fontSize: 11, color: dx.dim }, children: "\u70B9\u51FB\u753B\u9762\u653E\u7F6E\u6587\u5B57\uFF0C\u56DE\u8F66\u786E\u8BA4\u3002" })
        ] }) : null,
        tool === "adjust" ? /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 14 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("button", { className: "dx-hit", style: studioBtn, onClick: rotate, children: [
              /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(IconRotate, { size: 13 }),
              "\u65CB\u8F6C"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: () => flip("h"), children: "\u6C34\u5E73\u7FFB\u8F6C" }),
            /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: () => flip("v"), children: "\u5782\u76F4\u7FFB\u8F6C" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: "\u8272\u8C03", children: GRADE_FAMILIES.map((family) => /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { style: { marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("div", { style: { fontSize: 10, color: dx.mute, marginBottom: 4 }, children: family.label }),
            /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: looksByFamily(family.id).map((item3) => /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
              "button",
              {
                className: "dx-hit",
                title: item3.source,
                style: { ...studioBtn, background: lookId === item3.id ? "rgba(255,255,255,.14)" : studioBtn.background },
                onClick: () => setLookId((current) => current === item3.id ? void 0 : item3.id),
                children: item3.label
              },
              item3.id
            )) })
          ] }, family.id)) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: `\u4EAE\u5EA6 ${adjust.brightness.toFixed(2)}`, children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("input", { type: "range", min: 0.4, max: 1.8, step: 0.02, value: adjust.brightness, onChange: (event) => setAdjust((current) => ({ ...current, brightness: Number(event.target.value) })), style: studioRange() }) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: `\u5BF9\u6BD4 ${adjust.contrast.toFixed(2)}`, children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("input", { type: "range", min: 0.4, max: 1.8, step: 0.02, value: adjust.contrast, onChange: (event) => setAdjust((current) => ({ ...current, contrast: Number(event.target.value) })), style: studioRange() }) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(StudioField, { label: `\u9971\u548C ${adjust.saturate.toFixed(2)}`, children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("input", { type: "range", min: 0, max: 2, step: 0.02, value: adjust.saturate, onChange: (event) => setAdjust((current) => ({ ...current, saturate: Number(event.target.value) })), style: studioRange() }) }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: applyAdjust, children: "\u5E94\u7528\u8C03\u8282" })
        ] }) : null,
        tool === "move" ? /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("button", { className: "dx-hit", style: { ...studioBtn, marginBottom: 12 }, onClick: () => {
            setZoom(1);
            setPan({ x: 0, y: 0 });
          }, children: "\u590D\u4F4D\u89C6\u56FE" }),
          /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("div", { style: { fontSize: 12, color: dx.dim, lineHeight: 1.6 }, children: "\u6EDA\u8F6E\u7F29\u653E\uFF0C\u62D6\u62FD\u5E73\u79FB\u3002V \u79FB\u52A8 \xB7 C \u88C1\u526A \xB7 B \u753B\u7B14 \xB7 T \u6587\u5B57 \xB7 A \u8C03\u8282\u3002Esc \u53D6\u6D88\u5F53\u524D\u64CD\u4F5C\u6216\u5B8C\u6210\u3002" })
        ] }) : null
      ] }),
      children: /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(
        "div",
        {
          ref: frameRef,
          onWheel: (event) => {
            event.preventDefault();
            const next = Math.min(4, Math.max(0.25, zoom * (event.deltaY > 0 ? 0.9 : 1.1)));
            setZoom(next);
          },
          style: {
            flex: 1,
            minHeight: 0,
            overflow: "hidden",
            position: "relative",
            background: "repeating-conic-gradient(#1a1a1a 0% 25%, #111 0% 50%) 50% / 18px 18px"
          },
          children: [
            busy ? /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("div", { style: { position: "absolute", inset: 0, display: "grid", placeItems: "center", color: dx.mute, fontSize: 13 }, children: "\u6B63\u5728\u52A0\u8F7D\u56FE\u7247\u2026" }) : null,
            /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("div", { style: {
              position: "absolute",
              inset: 0,
              display: "grid",
              placeItems: "center",
              transform: `translate(${pan.x}px, ${pan.y}px) scale(${zoom})`
            }, children: /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
              "canvas",
              {
                ref: stageRef,
                style: {
                  maxWidth: "100%",
                  cursor: tool === "draw" || tool === "crop" || tool === "text" ? "crosshair" : tool === "move" ? "grab" : "default",
                  boxShadow: "0 18px 48px rgba(0,0,0,.45)"
                },
                onPointerDown: (event) => {
                  const point = mapPoint(event);
                  dragRef.current = point;
                  event.currentTarget.setPointerCapture(event.pointerId);
                  if (tool === "crop") setCrop({ x: point.x, y: point.y, w: 0, h: 0 });
                  if (tool === "draw") snapshot2();
                  if (tool === "text") setTextDraft({ x: event.clientX, y: event.clientY, value: "" });
                },
                onPointerMove: (event) => {
                  const start2 = dragRef.current;
                  if (start2 === void 0) return;
                  const point = mapPoint(event);
                  if (tool === "move") {
                    setPan((current) => ({
                      x: current.x + event.movementX,
                      y: current.y + event.movementY
                    }));
                  }
                  if (tool === "crop") setCrop(constrainRect(start2.x, start2.y, point.x, point.y, ratio));
                  if (tool === "draw") {
                    const work = workRef.current;
                    const view = stageRef.current;
                    if (work === null || view === null) return;
                    const ctx = work.getContext("2d");
                    if (ctx === null) return;
                    ctx.strokeStyle = color2;
                    ctx.lineWidth = brush * (work.width / view.width);
                    ctx.lineCap = "round";
                    ctx.lineJoin = "round";
                    ctx.beginPath();
                    ctx.moveTo(start2.x / view.width * work.width, start2.y / view.height * work.height);
                    ctx.lineTo(point.x / view.width * work.width, point.y / view.height * work.height);
                    ctx.stroke();
                    dragRef.current = point;
                    paint();
                  }
                },
                onPointerUp: () => {
                  dragRef.current = void 0;
                }
              }
            ) }),
            textDraft !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
              "textarea",
              {
                autoFocus: true,
                value: textDraft.value,
                onChange: (event) => setTextDraft((current) => current === void 0 ? current : { ...current, value: event.target.value }),
                onBlur: commitText,
                onKeyDown: (event) => {
                  if (event.key === "Enter" && !event.shiftKey) {
                    event.preventDefault();
                    commitText();
                  }
                  if (event.key === "Escape") setTextDraft(void 0);
                },
                style: {
                  position: "fixed",
                  left: textDraft.x,
                  top: textDraft.y,
                  minWidth: 160,
                  minHeight: 36,
                  padding: "6px 8px",
                  border: "1px solid rgba(255,255,255,.28)",
                  borderRadius: 8,
                  background: "rgba(12,12,12,.92)",
                  color: color2,
                  fontSize: 14,
                  fontFamily: dx.font,
                  outline: "none",
                  zIndex: 50
                }
              }
            ) : null
          ]
        }
      )
    }
  );
}

// src/client/stage/VideoStudio.tsx
var import_react7 = require("react");

// node_modules/@webav/av-cliper/dist/av-cliper.js
var import_mp4box2 = __toESM(require_mp4box_all(), 1);

// node_modules/@webav/internal-utils/dist/internal-utils.js
var import_mp4box = __toESM(require_mp4box_all(), 1);
var M = class {
  /**
   * 在两个 EventTool 实例间转发消息
   * @param from
   * @param to
   * @param evtTypes 需转发的消息类型
   *
   * @example
   * EventTool.forwardEvent(from, to, ['evtName']),
   */
  static forwardEvent(t, n2, r) {
    const o2 = r.map((s) => {
      const [i2, l2] = Array.isArray(s) ? s : [s, s];
      return t.on(i2, (...c) => {
        n2.emit(l2, ...c);
      });
    });
    return () => {
      o2.forEach((s) => s());
    };
  }
  #e = /* @__PURE__ */ new Map();
  /**
   * 监听 EventType 中定义的事件
   */
  on = (t, n2) => {
    const r = this.#e.get(t) ?? /* @__PURE__ */ new Set();
    return r.add(n2), this.#e.has(t) || this.#e.set(t, r), () => {
      r.delete(n2), r.size === 0 && this.#e.delete(t);
    };
  };
  /**
   * 监听事件，首次触发后自动移除监听
   *
   * 期望回调一次的事件，使用 once; 期望多次回调使用 on
   */
  once = (t, n2) => {
    const r = this.on(t, (...o2) => {
      r(), n2(...o2);
    });
    return r;
  };
  /**
   * 触发事件
   * @param type
   * @param args
   * @returns
   */
  emit = (t, ...n2) => {
    const r = this.#e.get(t);
    r?.forEach((o2) => o2(...n2));
  };
  destroy() {
    this.#e.clear();
  }
};
var L = () => {
  let e, t = 16.6;
  self.onmessage = (n2) => {
    n2.data.event === "start" && (self.clearInterval(e), e = self.setInterval(() => {
      self.postMessage({});
    }, t)), n2.data.event === "stop" && self.clearInterval(e);
  };
};
var V = () => {
  const e = new Blob([`(${L.toString()})()`]), t = URL.createObjectURL(e);
  return new Worker(t);
};
var E = /* @__PURE__ */ new Map();
var B = 1;
var z = null;
globalThis.Worker != null && (z = V(), z.onmessage = () => {
  B += 1;
  for (const [e, t] of E)
    if (B % e === 0) for (const n2 of t) n2();
});
var _ = (e, t) => {
  const n2 = Math.round(t / 16.6), r = E.get(n2) ?? /* @__PURE__ */ new Set();
  return r.add(e), E.set(n2, r), E.size === 1 && r.size === 1 && z?.postMessage({ event: "start" }), () => {
    r.delete(e), r.size === 0 && E.delete(n2), E.size === 0 && (B = 0, z?.postMessage({ event: "stop" }));
  };
};
function Z(e, t, n2) {
  let r = 0, o2 = 0;
  const s = e.boxes;
  let i2 = false;
  const l2 = () => {
    if (!i2)
      if (s.find((g2) => g2.type === "moof") != null)
        i2 = true;
      else
        return null;
    if (o2 >= s.length) return null;
    const f2 = new import_mp4box.default.DataStream();
    f2.endianness = import_mp4box.default.DataStream.BIG_ENDIAN;
    let m2 = o2;
    try {
      for (; m2 < s.length; )
        s[m2].write(f2), delete s[m2], m2 += 1;
    } catch (g2) {
      const y2 = s[m2];
      throw g2 instanceof Error && y2 != null ? Error(
        `${g2.message} | deltaBuf( boxType: ${y2.type}, boxSize: ${y2.size}, boxDataLen: ${y2.data?.length ?? -1})`
      ) : g2;
    }
    return P(e), o2 = s.length, new Uint8Array(f2.buffer);
  };
  let c = false, a = false, d2 = null;
  return {
    stream: new ReadableStream({
      start(f2) {
        r = self.setInterval(() => {
          const m2 = l2();
          m2 != null && !a && f2.enqueue(m2);
        }, t), d2 = (m2) => {
          if (clearInterval(r), e.flush(), m2 != null) {
            f2.error(m2);
            return;
          }
          const g2 = l2();
          g2 != null && !a && f2.enqueue(g2), a || f2.close();
        }, c && d2();
      },
      cancel() {
        a = true, clearInterval(r), n2?.();
      }
    }),
    stop: (f2) => {
      c || (c = true, d2?.(f2));
    }
  };
}
function P(e) {
  if (e.moov != null) {
    for (var t = 0; t < e.moov.traks.length; t++)
      e.moov.traks[t].samples = [];
    e.mdats = [], e.moofs = [];
  }
}
function F(e) {
  return e instanceof Error ? String(e) : typeof e == "object" ? JSON.stringify(e, (t, n2) => n2 instanceof Error ? String(n2) : n2) : String(e);
}
function O() {
  const e = /* @__PURE__ */ new Date();
  return `${e.getHours()}:${e.getMinutes()}:${e.getSeconds()}.${e.getMilliseconds()}`;
}
var C = 1;
var $ = [];
var U = ["debug", "info", "warn", "error"].reduce(
  (e, t, n2) => Object.assign(e, {
    [t]: (...r) => {
      C <= n2 && (console[t](...r), $.push({
        lvName: t,
        timeStr: O(),
        args: r
      }));
    }
  }),
  {}
);
var A = /* @__PURE__ */ new Map();
var S = {
  /**
   * 设置记录日志的级别
   *
   * @example
   * Log.setLogLevel(Log.warn) // 记录 warn，error 日志
   */
  setLogLevel: (e) => {
    C = A.get(e) ?? 1;
  },
  ...U,
  /**
   * 生成一个 log 实例，所有输出前都会附加 tag
   *
   * @example
   * const log = Log.create('<prefix>')
   * log.info('xxx') // '<prefix> xxx'
   */
  create: (e) => Object.fromEntries(
    Object.entries(U).map(([t, n2]) => [
      t,
      (...r) => n2(e, ...r)
    ])
  ),
  /**
   * 将所有日志导出为一个字符串
   *
   * @example
   * Log.dump() // => [level][time]  内容...
   *
   */
  async dump() {
    return $.reduce(
      (e, { lvName: t, timeStr: n2, args: r }) => e + `[${t}][${n2}]  ${r.map((o2) => F(o2)).join(" ")}
`,
      ""
    );
  }
};
A.set(S.debug, 0);
A.set(S.info, 1);
A.set(S.warn, 2);
A.set(S.error, 3);
(async function() {
  if (await Promise.resolve(), !(globalThis.navigator == null || globalThis.document == null) && (S.info(
    `@webav version: 1.2.8, date: ${(/* @__PURE__ */ new Date()).toLocaleDateString()}`
  ), S.info(globalThis.navigator.userAgent), document.addEventListener("visibilitychange", () => {
    S.info(`visibilitychange: ${document.visibilityState}`);
  }), "PressureObserver" in globalThis)) {
    let t = "";
    new PressureObserver((r) => {
      const o2 = JSON.stringify(r.map((s) => s.state));
      o2 !== t && (S.info(`cpu state change: ${o2}`), t = o2);
    }).observe("cpu");
  }
})();
var R = (e, t) => {
  const n2 = new Uint8Array(8);
  new DataView(n2.buffer).setUint32(0, t);
  for (let o2 = 0; o2 < 4; o2++)
    n2[4 + o2] = e.charCodeAt(o2);
  return n2;
};
var N = () => {
  const e = new TextEncoder(), t = e.encode("mdta"), n2 = e.encode("mp4 handler"), r = 32 + n2.byteLength + 1, o2 = new Uint8Array(r), s = new DataView(o2.buffer);
  return o2.set(R("hdlr", r), 0), s.setUint32(8, 0), o2.set(t, 16), o2.set(n2, 32), o2;
};
var Q = (e) => {
  const t = new TextEncoder(), n2 = t.encode("mdta"), r = e.map((a) => {
    const d2 = t.encode(a), h = 8 + d2.byteLength, f2 = new Uint8Array(h);
    return new DataView(f2.buffer).setUint32(0, h), f2.set(n2, 4), f2.set(d2, 4 + n2.byteLength), f2;
  }), s = 16 + r.reduce((a, d2) => a + d2.byteLength, 0), i2 = new Uint8Array(s), l2 = new DataView(i2.buffer);
  i2.set(R("keys", s), 0), l2.setUint32(8, 0), l2.setUint32(12, e.length);
  let c = 16;
  for (const a of r)
    i2.set(a, c), c += a.byteLength;
  return i2;
};
var H = (e) => {
  const t = new TextEncoder(), n2 = t.encode("data"), r = Object.entries(e).map(([c, a], d2) => {
    const h = d2 + 1, f2 = t.encode(a), m2 = 24 + f2.byteLength, g2 = new Uint8Array(m2), y2 = new DataView(g2.buffer);
    return y2.setUint32(0, m2), y2.setUint32(4, h), y2.setUint32(8, 16 + f2.byteLength), g2.set(n2, 12), y2.setUint32(16, 1), g2.set(f2, 24), g2;
  }), s = 8 + r.reduce((c, a) => c + a.byteLength, 0), i2 = new Uint8Array(s);
  i2.set(R("ilst", s), 0);
  let l2 = 8;
  for (const c of r)
    i2.set(c, l2), l2 += c.byteLength;
  return i2;
};
var J = (e) => {
  const t = N(), n2 = Q(Object.keys(e)), r = H(e), o2 = t.length + n2.length + r.length, s = new Uint8Array(o2);
  return s.set(t, 0), s.set(n2, t.length), s.set(r, t.length + n2.length), s;
};
function ee(e) {
  S.info("recodemux opts:", e);
  const t = import_mp4box.default.createFile(), n2 = new M(), r = (c, a) => {
    const h = c.add("udta").add("meta");
    h.data = J(a), h.size = h.data.byteLength;
  };
  let o2 = false;
  const s = () => {
    t.moov == null || o2 || (o2 = true, e.metaDataTags != null && r(t.moov, e.metaDataTags), e.duration != null && (t.moov.mvhd.duration = e.duration));
  };
  n2.once("VideoReady", s), n2.once("AudioReady", s);
  let i2 = e.video != null ? W(e.video, t, n2) : null, l2 = e.audio != null ? q(e.audio, t, n2) : null;
  return e.video == null && n2.emit("VideoReady"), e.audio == null && n2.emit("AudioReady"), {
    encodeVideo: (c, a) => {
      i2?.encode(c, a), c.close();
    },
    encodeAudio: (c) => {
      if (l2 != null)
        try {
          l2.encode(c), c.close();
        } catch (a) {
          const d2 = `encode audio chunk error: ${a.message}, state: ${JSON.stringify(
            {
              qSize: l2.encodeQueueSize,
              state: l2.state
            }
          )}`;
          throw S.error(d2), Error(d2);
        }
    },
    getEncodeQueueSize: () => i2?.encodeQueueSize ?? l2?.encodeQueueSize ?? 0,
    flush: async () => {
      await Promise.all([
        i2?.flush(),
        l2?.state === "configured" ? l2.flush() : null
      ]);
    },
    close: () => {
      n2.destroy(), i2?.close(), l2?.state === "configured" && l2.close();
    },
    mp4file: t
  };
}
function W(e, t, n2) {
  const r = {
    // 微秒
    timescale: 1e6,
    width: e.width,
    height: e.height,
    brands: ["isom", "iso2", "avc1", "mp42", "mp41"],
    avcDecoderConfigRecord: null,
    hevcDecoderConfigRecord: null,
    vpcDecoderConfigRecord: null,
    type: "avc1",
    name: "Track created with WebAV"
  };
  let o2 = -1, s = false;
  n2.once("AudioReady", () => {
    s = true;
  });
  const i2 = {
    encoder0: [],
    encoder1: []
  }, l2 = (u2, b, p2) => {
    if (o2 === -1 && p2 != null) {
      let w = p2.decoderConfig?.description;
      e.codec.startsWith("avc1") ? j(w) : e.codec.startsWith("vp09") && p2.decoderConfig && (r.type = "vp09", w = K(p2.decoderConfig));
      const x3 = [
        ["avc1", "avcDecoderConfigRecord"],
        ["hvc1", "hevcDecoderConfigRecord"],
        ["vp09", "vpcDecoderConfigRecord"]
      ].find(([D2]) => e.codec.startsWith(D2))?.[1];
      x3 != null && w != null && (r[x3] = w), o2 = t.addTrack(r), n2.emit("VideoReady"), S.info("VideoEncoder, video track ready, trackId:", o2);
    }
    i2[u2].push(T(b));
  };
  let c = "encoder1", a = 0;
  const d2 = Math.floor(1e3 / e.expectFPS * 1e3);
  function h() {
    if (!s) return;
    const u2 = c === "encoder1" ? "encoder0" : "encoder1", b = i2[c], p2 = i2[u2];
    if (b.length === 0 && p2.length === 0) return;
    let w = b[0];
    if (w != null && (!w.is_sync || w.cts - a < d2)) {
      const D2 = f2(b);
      D2 > a && (a = D2);
    }
    const x3 = p2[0];
    if (x3?.is_sync && x3.cts - a < d2) {
      c = u2, h();
      return;
    }
    if (w?.is_sync && x3?.is_sync)
      if (w.cts <= x3.cts) {
        const D2 = f2(b);
        D2 > a && (a = D2);
      } else {
        c = u2, h();
        return;
      }
  }
  function f2(u2) {
    let b = -1, p2 = 0;
    for (; p2 < u2.length; p2++) {
      const w = u2[p2];
      if (p2 > 0 && w.is_sync) break;
      t.addSample(o2, w.data, w), b = w.cts + w.duration;
    }
    return u2.splice(0, p2), b;
  }
  const m2 = _(h, 15), g2 = I(
    e,
    (u2, b) => l2("encoder0", u2, b)
  ), y2 = I(
    e,
    (u2, b) => l2("encoder1", u2, b)
  );
  let v2 = 0;
  return {
    get encodeQueueSize() {
      return g2.encodeQueueSize + y2.encodeQueueSize;
    },
    encode: (u2, b) => {
      try {
        b.keyFrame && (v2 += 1), (v2 % 2 === 0 ? g2 : y2).encode(u2, b);
      } catch (p2) {
        const w = `encode video frame error: ${p2.message}, state: ${JSON.stringify(
          {
            ts: u2.timestamp,
            keyFrame: b.keyFrame,
            duration: u2.duration,
            gopId: v2
          }
        )}`;
        throw S.error(w), Error(w);
      }
    },
    flush: async () => {
      await Promise.all([
        g2.state === "configured" ? await g2.flush() : null,
        y2.state === "configured" ? await y2.flush() : null
      ]), m2(), h();
    },
    close: () => {
      g2.state === "configured" && g2.close(), y2.state === "configured" && y2.close();
    }
  };
}
function j(e) {
  const t = new Uint8Array(e);
  t[2].toString(2).slice(-2).includes("1") && (t[2] = 0);
}
function I(e, t) {
  const n2 = {
    codec: e.codec,
    framerate: e.expectFPS,
    hardwareAcceleration: e.__unsafe_hardwareAcceleration__,
    // 码率
    bitrate: e.bitrate,
    width: e.width,
    height: e.height,
    // H264 不支持背景透明度
    alpha: "discard",
    // macos 自带播放器只支持avc
    avc: { format: "avc" }
    // mp4box.js 无法解析 annexb 的 mimeCodec ，只会显示 avc1
    // avc: { format: 'annexb' }
  }, r = new VideoEncoder({
    error: (o2) => {
      const s = `VideoEncoder error: ${o2.message}, config: ${JSON.stringify(n2)}, state: ${JSON.stringify(
        {
          qSize: r.encodeQueueSize,
          state: r.state
        }
      )}`;
      throw S.error(s), Error(s);
    },
    output: t
  });
  return r.configure(n2), r;
}
function q(e, t, n2) {
  const r = {
    timescale: 1e6,
    samplerate: e.sampleRate,
    channel_count: e.channelCount,
    hdlr: "soun",
    type: e.codec === "aac" ? "mp4a" : "Opus",
    name: "Track created with WebAV"
  };
  let o2 = -1, s = [], i2 = false;
  n2.once("VideoReady", () => {
    i2 = true, s.forEach((a) => {
      const d2 = T(a);
      t.addSample(o2, d2.data, d2);
    }), s = [];
  });
  const l2 = {
    codec: e.codec === "aac" ? "mp4a.40.2" : "opus",
    sampleRate: e.sampleRate,
    numberOfChannels: e.channelCount,
    bitrate: 128e3
  }, c = new AudioEncoder({
    error: (a) => {
      const d2 = `AudioEncoder error: ${a.message}, config: ${JSON.stringify(
        l2
      )}, state: ${JSON.stringify({
        qSize: c.encodeQueueSize,
        state: c.state
      })}`;
      throw S.error(d2), Error(d2);
    },
    output: (a, d2) => {
      if (o2 === -1) {
        const h = d2?.decoderConfig?.description;
        o2 = t.addTrack({
          ...r,
          description: h == null ? void 0 : G(h)
        }), n2.emit("AudioReady"), S.info("AudioEncoder, audio track ready, trackId:", o2);
      }
      if (i2) {
        const h = T(a);
        t.addSample(o2, h.data, h);
      } else
        s.push(a);
    }
  });
  return c.configure(l2), c;
}
function G(e) {
  const t = e.byteLength, n2 = new Uint8Array([
    0,
    // version 0
    0,
    0,
    0,
    // flags
    3,
    // descriptor_type
    23 + t,
    // length
    0,
    // 0x01, // es_id
    2,
    // es_id
    0,
    // stream_priority
    4,
    // descriptor_type
    18 + t,
    // length
    64,
    // codec : mpeg4_audio
    21,
    // stream_type
    0,
    0,
    0,
    // buffer_size
    0,
    0,
    0,
    0,
    // maxBitrate
    0,
    0,
    0,
    0,
    // avgBitrate
    5,
    // descriptor_type
    t,
    ...new Uint8Array(e instanceof ArrayBuffer ? e : e.buffer),
    6,
    1,
    2
  ]), r = new import_mp4box.default.BoxParser.esdsBox(n2.byteLength);
  return r.hdr_size = 0, r.parse(new import_mp4box.default.DataStream(n2, 0, import_mp4box.default.DataStream.BIG_ENDIAN)), r;
}
function T(e) {
  const t = new ArrayBuffer(e.byteLength);
  e.copyTo(t);
  const n2 = e.timestamp;
  return {
    duration: e.duration ?? 0,
    dts: n2,
    cts: n2,
    is_sync: e.type === "key",
    data: t
  };
}
function K(e) {
  const n2 = e.codec.split("."), r = parseInt(n2[1] || "0", 10), o2 = parseInt(n2[2] || "40", 10), s = parseInt(n2[3] || "08", 10), i2 = {
    bt709: 1,
    bt601: 5,
    bt2020: 9
  }, l2 = {
    bt709: 1,
    srgb: 13,
    pq: 16,
    hlg: 18
  }, c = {
    bt709: 1,
    bt601: 5,
    bt2020: 9
  }, a = i2[e.colorSpace?.primaries || "bt709"] || 1, d2 = l2[e.colorSpace?.transfer || "bt709"] || 1, h = c[e.colorSpace?.matrix || "bt709"] || 1, f2 = e.colorSpace?.fullRange ? 1 : 0, m2 = 1, g2 = 0, y2 = new ArrayBuffer(12), v2 = new DataView(y2);
  let u2 = 0;
  return v2.setUint32(u2, 1 << 24), u2 += 4, v2.setUint8(u2++, r), v2.setUint8(u2++, o2), v2.setUint8(
    u2++,
    s << 4 | m2 << 1 | f2
  ), v2.setUint8(u2++, a), v2.setUint8(u2++, d2), v2.setUint8(u2++, h), v2.setUint16(u2, g2), y2;
}

// node_modules/wave-resampler/lib/interpolator.js
var Interpolator = class {
  /**
   * @param {number} scaleFrom the length of the original array.
   * @param {number} scaleTo The length of the new array.
   * @param {?Object} details The extra configuration, if needed.
   */
  constructor(scaleFrom, scaleTo, details) {
    this.length_ = scaleFrom;
    this.scaleFactor_ = (scaleFrom - 1) / scaleTo;
    this.interpolate = this.cubic;
    if (details.method === "point") {
      this.interpolate = this.point;
    } else if (details.method === "linear") {
      this.interpolate = this.linear;
    } else if (details.method === "sinc") {
      this.interpolate = this.sinc;
    }
    this.tangentFactor_ = 1 - Math.max(0, Math.min(1, details.tension || 0));
    this.sincFilterSize_ = details.sincFilterSize || 1;
    this.kernel_ = sincKernel_(details.sincWindow || window_);
  }
  /**
   * @param {number} t The index to interpolate.
   * @param {Array|TypedArray} samples the original array.
   * @return {number} The interpolated value.
   */
  point(t, samples) {
    return this.getClippedInput_(Math.round(this.scaleFactor_ * t), samples);
  }
  /**
   * @param {number} t The index to interpolate.
   * @param {Array|TypedArray} samples the original array.
   * @return {number} The interpolated value.
   */
  linear(t, samples) {
    t = this.scaleFactor_ * t;
    let k2 = Math.floor(t);
    t -= k2;
    return (1 - t) * this.getClippedInput_(k2, samples) + t * this.getClippedInput_(k2 + 1, samples);
  }
  /**
   * @param {number} t The index to interpolate.
   * @param {Array|TypedArray} samples the original array.
   * @return {number} The interpolated value.
   */
  cubic(t, samples) {
    t = this.scaleFactor_ * t;
    let k2 = Math.floor(t);
    let m2 = [this.getTangent_(k2, samples), this.getTangent_(k2 + 1, samples)];
    let p2 = [
      this.getClippedInput_(k2, samples),
      this.getClippedInput_(k2 + 1, samples)
    ];
    t -= k2;
    let t2 = t * t;
    let t3 = t * t2;
    return (2 * t3 - 3 * t2 + 1) * p2[0] + (t3 - 2 * t2 + t) * m2[0] + (-2 * t3 + 3 * t2) * p2[1] + (t3 - t2) * m2[1];
  }
  /**
   * @param {number} t The index to interpolate.
   * @param {Array|TypedArray} samples the original array.
   * @return {number} The interpolated value.
   */
  sinc(t, samples) {
    t = this.scaleFactor_ * t;
    let k2 = Math.floor(t);
    let ref = k2 - this.sincFilterSize_ + 1;
    let ref1 = k2 + this.sincFilterSize_;
    let sum = 0;
    for (let n2 = ref; n2 <= ref1; n2++) {
      sum += this.kernel_(t - n2) * this.getClippedInput_(n2, samples);
    }
    return sum;
  }
  /**
   * @param {number} k The scaled index to interpolate.
   * @param {Array|TypedArray} samples the original array.
   * @return {number} The tangent.
   * @private
   */
  getTangent_(k2, samples) {
    return this.tangentFactor_ * (this.getClippedInput_(k2 + 1, samples) - this.getClippedInput_(k2 - 1, samples)) / 2;
  }
  /**
   * @param {number} t The scaled index to interpolate.
   * @param {Array|TypedArray} samples the original array.
   * @return {number} The interpolated value.
   * @private
   */
  getClippedInput_(t, samples) {
    if (0 <= t && t < this.length_) {
      return samples[t];
    }
    return 0;
  }
};
function window_(x3) {
  return Math.exp(-x3 / 2 * x3 / 2);
}
function sincKernel_(window2) {
  return function(x3) {
    return sinc_(x3) * window2(x3);
  };
}
function sinc_(x3) {
  if (x3 === 0) {
    return 1;
  }
  return Math.sin(Math.PI * x3) / (Math.PI * x3);
}

// node_modules/wave-resampler/lib/fir-lpf.js
var FIRLPF = class {
  /**
   * @param {number} order The order of the filter.
   * @param {number} sampleRate The sample rate.
   * @param {number} cutOff The cut off frequency.
   */
  constructor(order, sampleRate, cutOff) {
    let omega = 2 * Math.PI * cutOff / sampleRate;
    let dc = 0;
    this.filters = [];
    for (let i2 = 0; i2 <= order; i2++) {
      if (i2 - order / 2 === 0) {
        this.filters[i2] = omega;
      } else {
        this.filters[i2] = Math.sin(omega * (i2 - order / 2)) / (i2 - order / 2);
        this.filters[i2] *= 0.54 - 0.46 * Math.cos(2 * Math.PI * i2 / order);
      }
      dc = dc + this.filters[i2];
    }
    for (let i2 = 0; i2 <= order; i2++) {
      this.filters[i2] /= dc;
    }
    this.z = this.initZ_();
  }
  /**
   * @param {number} sample A sample of a sequence.
   * @return {number}
   */
  filter(sample) {
    this.z.buf[this.z.pointer] = sample;
    let out = 0;
    for (let i2 = 0, len = this.z.buf.length; i2 < len; i2++) {
      out += this.filters[i2] * this.z.buf[(this.z.pointer + i2) % this.z.buf.length];
    }
    this.z.pointer = (this.z.pointer + 1) % this.z.buf.length;
    return out;
  }
  /**
   * Reset the filter.
   */
  reset() {
    this.z = this.initZ_();
  }
  /**
   * Return the default value for z.
   * @private
   */
  initZ_() {
    let r = [];
    for (let i2 = 0; i2 < this.filters.length - 1; i2++) {
      r.push(0);
    }
    return {
      buf: r,
      pointer: 0
    };
  }
};

// node_modules/wave-resampler/lib/butterworth-lpf.js
var ButterworthLPF = class {
  /**
   * @param {number} order The order of the filter.
   * @param {number} sampleRate The sample rate.
   * @param {number} cutOff The cut off frequency.
   */
  constructor(order, sampleRate, cutOff) {
    let filters = [];
    for (let i2 = 0; i2 < order; i2++) {
      filters.push(this.getCoeffs_({
        Fs: sampleRate,
        Fc: cutOff,
        Q: 0.5 / Math.sin(Math.PI / (order * 2) * (i2 + 0.5))
      }));
    }
    this.stages = [];
    for (let i2 = 0; i2 < filters.length; i2++) {
      this.stages[i2] = {
        b0: filters[i2].b[0],
        b1: filters[i2].b[1],
        b2: filters[i2].b[2],
        a1: filters[i2].a[0],
        a2: filters[i2].a[1],
        k: filters[i2].k,
        z: [0, 0]
      };
    }
  }
  /**
   * @param {number} sample A sample of a sequence.
   * @return {number}
   */
  filter(sample) {
    let out = sample;
    for (let i2 = 0, len = this.stages.length; i2 < len; i2++) {
      out = this.runStage_(i2, out);
    }
    return out;
  }
  getCoeffs_(params) {
    let coeffs = {};
    coeffs.z = [0, 0];
    coeffs.a = [];
    coeffs.b = [];
    let p2 = this.preCalc_(params, coeffs);
    coeffs.k = 1;
    coeffs.b.push((1 - p2.cw) / (2 * p2.a0));
    coeffs.b.push(2 * coeffs.b[0]);
    coeffs.b.push(coeffs.b[0]);
    return coeffs;
  }
  preCalc_(params, coeffs) {
    let pre = {};
    let w = 2 * Math.PI * params.Fc / params.Fs;
    pre.alpha = Math.sin(w) / (2 * params.Q);
    pre.cw = Math.cos(w);
    pre.a0 = 1 + pre.alpha;
    coeffs.a0 = pre.a0;
    coeffs.a.push(-2 * pre.cw / pre.a0);
    coeffs.k = 1;
    coeffs.a.push((1 - pre.alpha) / pre.a0);
    return pre;
  }
  runStage_(i2, input2) {
    let temp = input2 * this.stages[i2].k - this.stages[i2].a1 * this.stages[i2].z[0] - this.stages[i2].a2 * this.stages[i2].z[1];
    let out = this.stages[i2].b0 * temp + this.stages[i2].b1 * this.stages[i2].z[0] + this.stages[i2].b2 * this.stages[i2].z[1];
    this.stages[i2].z[1] = this.stages[i2].z[0];
    this.stages[i2].z[0] = temp;
    return out;
  }
  /**
   * Reset the filter.
   */
  reset() {
    for (let i2 = 0; i2 < this.stages.length; i2++) {
      this.stages[i2].z = [0, 0];
    }
  }
};

// node_modules/wave-resampler/index.js
var DEFAULT_LPF_USE = {
  "point": false,
  "linear": false,
  "cubic": true,
  "sinc": true
};
var DEFAULT_LPF_ORDER = {
  "IIR": 16,
  "FIR": 71
};
var DEFAULT_LPF = {
  "IIR": ButterworthLPF,
  "FIR": FIRLPF
};
function resample(samples, oldSampleRate, sampleRate, details = {}) {
  let rate = (sampleRate - oldSampleRate) / oldSampleRate + 1;
  let newSamples = new Float64Array(samples.length * rate);
  details.method = details.method || "cubic";
  let interpolator = new Interpolator(
    samples.length,
    newSamples.length,
    {
      method: details.method,
      tension: details.tension || 0,
      sincFilterSize: details.sincFilterSize || 6,
      sincWindow: details.sincWindow || void 0
    }
  );
  if (details.LPF === void 0) {
    details.LPF = DEFAULT_LPF_USE[details.method];
  }
  if (details.LPF) {
    details.LPFType = details.LPFType || "IIR";
    const LPF = DEFAULT_LPF[details.LPFType];
    if (sampleRate > oldSampleRate) {
      let filter2 = new LPF(
        details.LPFOrder || DEFAULT_LPF_ORDER[details.LPFType],
        sampleRate,
        oldSampleRate / 2
      );
      upsample_(
        samples,
        newSamples,
        interpolator,
        filter2
      );
    } else {
      let filter2 = new LPF(
        details.LPFOrder || DEFAULT_LPF_ORDER[details.LPFType],
        oldSampleRate,
        sampleRate / 2
      );
      downsample_(
        samples,
        newSamples,
        interpolator,
        filter2
      );
    }
  } else {
    resample_(samples, newSamples, interpolator);
  }
  return newSamples;
}
function resample_(samples, newSamples, interpolator) {
  for (let i2 = 0, len = newSamples.length; i2 < len; i2++) {
    newSamples[i2] = interpolator.interpolate(i2, samples);
  }
}
function upsample_(samples, newSamples, interpolator, filter2) {
  for (let i2 = 0, len = newSamples.length; i2 < len; i2++) {
    newSamples[i2] = filter2.filter(interpolator.interpolate(i2, samples));
  }
  filter2.reset();
  for (let i2 = newSamples.length - 1; i2 >= 0; i2--) {
    newSamples[i2] = filter2.filter(newSamples[i2]);
  }
}
function downsample_(samples, newSamples, interpolator, filter2) {
  for (let i2 = 0, len = samples.length; i2 < len; i2++) {
    samples[i2] = filter2.filter(samples[i2]);
  }
  filter2.reset();
  for (let i2 = samples.length - 1; i2 >= 0; i2--) {
    samples[i2] = filter2.filter(samples[i2]);
  }
  resample_(samples, newSamples, interpolator);
}

// node_modules/opfs-tools/dist/opfs-tools.js
var z2 = (r) => {
  throw TypeError(r);
};
var j2 = (r, e, t) => e.has(r) || z2("Cannot " + t);
var n = (r, e, t) => (j2(r, e, "read from private field"), t ? t.call(r) : e.get(r));
var o = (r, e, t) => e.has(r) ? z2("Cannot add the same private member more than once") : e instanceof WeakSet ? e.add(r) : e.set(r, t);
var l = (r, e, t, a) => (j2(r, e, "write to private field"), a ? a.call(r, t) : e.set(r, t), t);
var J2 = "";
var D = (r) => Uint8Array.from(atob(r), (e) => e.charCodeAt(0));
var K2 = typeof self < "u" && self.Blob && new Blob([D(J2)], { type: "text/javascript;charset=utf-8" });
function M2(r) {
  let e;
  try {
    if (e = K2 && (self.URL || self.webkitURL).createObjectURL(K2), !e) throw "";
    const t = new Worker(e, {
      name: r == null ? void 0 : r.name
    });
    return t.addEventListener("error", () => {
      (self.URL || self.webkitURL).revokeObjectURL(e);
    }), t;
  } catch {
    return new Worker(
      "data:text/javascript;base64," + J2,
      {
        name: r == null ? void 0 : r.name
      }
    );
  } finally {
    e && (self.URL || self.webkitURL).revokeObjectURL(e);
  }
}
async function _2(r, e, t) {
  const a = A2();
  return await a("register", { fileId: r, filePath: e, mode: t }), {
    read: async (i2, s) => await a("read", {
      fileId: r,
      offset: i2,
      size: s
    }),
    write: async (i2, s) => await a(
      "write",
      {
        fileId: r,
        data: i2,
        opts: s
      },
      [ArrayBuffer.isView(i2) ? i2.buffer : i2]
    ),
    close: async () => await a("close", {
      fileId: r
    }),
    truncate: async (i2) => await a("truncate", {
      fileId: r,
      newSize: i2
    }),
    getSize: async () => await a("getSize", {
      fileId: r
    }),
    flush: async () => await a("flush", {
      fileId: r
    })
  };
}
var v = [];
var x = 0;
function A2() {
  if (v.length < 3) {
    const e = r();
    return v.push(e), e;
  } else {
    const e = v[x];
    return x = (x + 1) % v.length, e;
  }
  function r() {
    const e = new M2();
    let t = 0, a = {};
    return e.onmessage = ({
      data: i2
    }) => {
      var s, c;
      i2.evtType === "callback" ? (s = a[i2.cbId]) == null || s.resolve(i2.returnVal) : i2.evtType === "throwError" && ((c = a[i2.cbId]) == null || c.reject(Error(i2.errMsg))), delete a[i2.cbId];
    }, async function(s, c, h = []) {
      t += 1;
      const w = new Promise((b, k2) => {
        a[t] = { resolve: b, reject: k2 };
      });
      return e.postMessage(
        {
          cbId: t,
          evtType: s,
          args: c
        },
        h
      ), w;
    };
  }
}
function V2(r) {
  if (r === "/") return { parent: null, name: "" };
  const e = r.split("/").filter((i2) => i2.length > 0);
  if (e.length === 0) throw Error("Invalid path");
  const t = e[e.length - 1], a = "/" + e.slice(0, -1).join("/");
  return { name: t, parent: a };
}
async function m(r, e) {
  const { parent: t, name: a } = V2(r);
  if (t == null) return await navigator.storage.getDirectory();
  const i2 = t.split("/").filter((s) => s.length > 0);
  try {
    let s = await navigator.storage.getDirectory();
    for (const c of i2)
      s = await s.getDirectoryHandle(c, {
        create: e.create
      });
    return e.isFile ? await s.getFileHandle(a, {
      create: e.create
    }) : await s.getDirectoryHandle(a, {
      create: e.create
    });
  } catch (s) {
    if (s.name === "NotFoundError")
      return null;
    throw s;
  }
}
async function L2(r) {
  const { parent: e, name: t } = V2(r);
  if (e == null) {
    const i2 = await navigator.storage.getDirectory();
    for await (const s of i2.keys())
      await i2.removeEntry(s, { recursive: true });
    return;
  }
  const a = await m(e, {
    create: false,
    isFile: false
  });
  if (a != null)
    try {
      await a.removeEntry(t, { recursive: true });
    } catch (i2) {
      if (i2.name === "NotFoundError") return;
      throw i2;
    }
}
function E2(r, e) {
  return `${r}/${e}`.replace("//", "/");
}
function g(r) {
  return new T2(r);
}
var f;
var S2;
var p;
var C2 = class C3 {
  constructor(e) {
    o(this, f);
    o(this, S2);
    o(this, p);
    l(this, f, e);
    const { parent: t, name: a } = V2(e);
    l(this, S2, a), l(this, p, t);
  }
  get kind() {
    return "dir";
  }
  get name() {
    return n(this, S2);
  }
  get path() {
    return n(this, f);
  }
  get parent() {
    return n(this, p) == null ? null : g(n(this, p));
  }
  /**
   * Creates the directory.
   * return A promise that resolves when the directory is created.
   */
  async create() {
    return await m(n(this, f), {
      create: true,
      isFile: false
    }), g(n(this, f));
  }
  /**
   * Checks if the directory exists.
   * return A promise that resolves to true if the directory exists, otherwise false.
   */
  async exists() {
    return await m(n(this, f), {
      create: false,
      isFile: false
    }) instanceof FileSystemDirectoryHandle;
  }
  /**
   * Removes the directory.
   * return A promise that resolves when the directory is removed.
   */
  async remove(e = {}) {
    for (const t of await this.children())
      try {
        await t.remove(e);
      } catch (a) {
        console.warn(a);
      }
    try {
      await L2(n(this, f));
    } catch (t) {
      console.warn(t);
    }
  }
  /**
   * Retrieves the children of the directory.
   * return A promise that resolves to an array of objects representing the children.
   */
  async children() {
    const e = await m(n(this, f), {
      create: false,
      isFile: false
    });
    if (e == null) return [];
    const t = [];
    for await (const a of e.values())
      t.push((a.kind === "file" ? F2 : g)(E2(n(this, f), a.name)));
    return t;
  }
  async copyTo(e) {
    if (!await this.exists())
      throw Error(`dir ${this.path} not exists`);
    if (e instanceof C3) {
      const t = await e.exists() ? g(E2(e.path, this.name)) : e;
      return await t.create(), await Promise.all((await this.children()).map((a) => a.copyTo(t))), t;
    } else if (e instanceof FileSystemDirectoryHandle)
      return await Promise.all(
        (await this.children()).map(async (t) => {
          t.kind === "file" ? await t.copyTo(
            await e.getFileHandle(t.name, { create: true })
          ) : await t.copyTo(
            await e.getDirectoryHandle(t.name, { create: true })
          );
        })
      ), null;
    throw Error("Illegal target type");
  }
  /**
   * move directory, copy then remove current
   */
  async moveTo(e) {
    const t = await this.copyTo(e);
    return await this.remove(), t;
  }
};
f = /* @__PURE__ */ new WeakMap(), S2 = /* @__PURE__ */ new WeakMap(), p = /* @__PURE__ */ new WeakMap();
var T2 = C2;
var P2 = /* @__PURE__ */ new Map();
function F2(r, e = "rw") {
  if (e === "rw") {
    const t = P2.get(r) ?? new W2(r, e);
    return P2.set(r, t), t;
  }
  return new W2(r, e);
}
async function B2(r, e, t = { overwrite: true }) {
  if (e instanceof W2) {
    await B2(r, await e.stream(), t);
    return;
  }
  const a = await (r instanceof W2 ? r : F2(r, "rw")).createWriter();
  try {
    if (t.overwrite && await a.truncate(0), e instanceof ReadableStream) {
      const i2 = e.getReader();
      for (; ; ) {
        const { done: s, value: c } = await i2.read();
        if (s) break;
        await a.write(c);
      }
    } else
      await a.write(e);
  } catch (i2) {
    throw i2;
  } finally {
    await a.close();
  }
}
var $2 = 0;
var q2 = () => ++$2;
var u;
var Z2;
var G2;
var Y;
var X;
var d;
var R2;
var I2;
var y;
var O2 = class O3 {
  constructor(e, t) {
    o(this, u);
    o(this, Z2);
    o(this, G2);
    o(this, Y);
    o(this, X);
    o(this, d, 0);
    o(this, R2, async () => {
    });
    o(this, I2, /* @__PURE__ */ (() => {
      let e2 = null;
      return () => (l(this, d, n(this, d) + 1), e2 != null || (e2 = new Promise(async (t2, a2) => {
        try {
          const i3 = await _2(
            n(this, X),
            n(this, u),
            n(this, Y)
          );
          l(this, R2, async () => {
            e2 != null && (e2 = null, l(this, d, 0), await i3.close().catch(console.error));
          }), t2([
            i3,
            async () => {
              l(this, d, n(this, d) - 1), !(n(this, d) > 0) && (e2 = null, await i3.close());
            }
          ]);
        } catch (i3) {
          a2(i3);
        }
      })), e2);
    })());
    o(this, y, false);
    l(this, X, q2()), l(this, u, e), l(this, Y, {
      r: "read-only",
      rw: "readwrite",
      "rw-unsafe": "readwrite-unsafe"
    }[t]);
    const { parent: a, name: i2 } = V2(e);
    if (a == null) throw Error("Invalid path");
    l(this, G2, i2), l(this, Z2, a);
  }
  get kind() {
    return "file";
  }
  get path() {
    return n(this, u);
  }
  get name() {
    return n(this, G2);
  }
  get parent() {
    return n(this, Z2) == null ? null : g(n(this, Z2));
  }
  /**
   * Random write to file
   */
  async createWriter() {
    if (n(this, Y) === "read-only") throw Error("file is read-only");
    if (n(this, y)) throw Error("Other writer have not been closed");
    l(this, y, true);
    try {
      const e = new TextEncoder(), [t, a] = await n(this, I2).call(this);
      let i2 = await t.getSize(), s = false;
      return {
        write: async (c, h = {}) => {
          if (s) throw Error("Writer is closed");
          const w = typeof c == "string" ? e.encode(c) : c, b = h.at ?? i2, k2 = w.byteLength;
          return i2 = b + k2, await t.write(w, { at: b });
        },
        truncate: async (c) => {
          if (s) throw Error("Writer is closed");
          await t.truncate(c), i2 > c && (i2 = c);
        },
        flush: async () => {
          if (s) throw Error("Writer is closed");
          await t.flush();
        },
        close: async () => {
          if (s) throw Error("Writer is closed");
          s = true, l(this, y, false), await a();
        }
      };
    } catch (e) {
      throw l(this, y, false), e;
    }
  }
  /**
   * Random access to file
   */
  async createReader() {
    const [e, t] = await n(this, I2).call(this);
    let a = false, i2 = 0;
    return {
      read: async (s, c = {}) => {
        if (a) throw Error("Reader is closed");
        const h = c.at ?? i2, w = await e.read(h, s);
        return i2 = h + w.byteLength, w;
      },
      getSize: async () => {
        if (a) throw Error("Reader is closed");
        return await e.getSize();
      },
      close: async () => {
        a || (a = true, await t());
      }
    };
  }
  async text() {
    return new TextDecoder().decode(await this.arrayBuffer());
  }
  async arrayBuffer() {
    const e = await m(n(this, u), { create: false, isFile: true });
    return e == null ? new ArrayBuffer(0) : (await e.getFile()).arrayBuffer();
  }
  async stream() {
    const e = await this.getOriginFile();
    return e == null ? new ReadableStream({
      pull: (t) => {
        t.close();
      }
    }) : e.stream();
  }
  async getOriginFile() {
    var e;
    return (e = await m(n(this, u), { create: false, isFile: true })) == null ? void 0 : e.getFile();
  }
  async getSize() {
    const e = await m(n(this, u), { create: false, isFile: true });
    return e == null ? 0 : (await e.getFile()).size;
  }
  async exists() {
    return await m(n(this, u), {
      create: false,
      isFile: true
    }) instanceof FileSystemFileHandle;
  }
  async remove(e = {}) {
    if (e.force === true) {
      await n(this, R2).call(this), await L2(n(this, u)), P2.delete(n(this, u));
      return;
    }
    if (n(this, d) > 0) throw Error("exists unclosed reader/writer");
    await L2(n(this, u));
  }
  async copyTo(e) {
    if (e instanceof O3)
      return e.path === this.path ? this : (await B2(e, this), e);
    if (e instanceof T2) {
      if (!await this.exists())
        throw Error(`file ${this.path} not exists`);
      return await this.copyTo(F2(E2(e.path, this.name)));
    } else if (e instanceof FileSystemFileHandle)
      return await (await this.stream()).pipeTo(await e.createWritable()), null;
    throw Error("Illegal target type");
  }
  /**
   * move file, copy then remove current
   */
  async moveTo(e) {
    const t = await this.copyTo(e);
    return await this.remove(), t;
  }
};
u = /* @__PURE__ */ new WeakMap(), Z2 = /* @__PURE__ */ new WeakMap(), G2 = /* @__PURE__ */ new WeakMap(), Y = /* @__PURE__ */ new WeakMap(), X = /* @__PURE__ */ new WeakMap(), d = /* @__PURE__ */ new WeakMap(), R2 = /* @__PURE__ */ new WeakMap(), I2 = /* @__PURE__ */ new WeakMap(), y = /* @__PURE__ */ new WeakMap();
var W2 = O2;
var U2 = "/.opfs-tools-temp-dir";
async function Q2(r) {
  try {
    if (r.kind === "file") {
      if (!await r.exists()) return true;
      const e = await r.createWriter();
      await e.truncate(0), await e.close(), await r.remove();
    } else
      await r.remove();
    return true;
  } catch (e) {
    return console.warn(e), false;
  }
}
function ee2() {
  setInterval(async () => {
    for (const e of await g(U2).children()) {
      const t = /^\d+-(\d+)$/.exec(e.name);
      (t == null || Date.now() - Number(t[1]) > 2592e5) && await Q2(e);
    }
  }, 60 * 1e3);
}
var H2 = [];
var N2 = false;
async function te() {
  if (globalThis.localStorage == null) return;
  const r = "OPFS_TOOLS_EXPIRES_TMP_FILES";
  N2 || (N2 = true, globalThis.addEventListener("unload", () => {
    H2.length !== 0 && localStorage.setItem(
      r,
      `${localStorage.getItem(r) ?? ""},${H2.join(",")}`
    );
  }));
  let e = localStorage.getItem(r) ?? "";
  for (const t of e.split(","))
    t.length !== 0 && await Q2(F2(`${U2}/${t}`)) && (e = e.replace(t, ""));
  localStorage.setItem(r, e.replace(/,{2,}/g, ","));
}
(async function() {
  var e;
  globalThis.__opfs_tools_tmpfile_init__ !== true && (globalThis.__opfs_tools_tmpfile_init__ = true, !(globalThis.FileSystemDirectoryHandle == null || globalThis.FileSystemFileHandle == null || ((e = globalThis.navigator) == null ? void 0 : e.storage.getDirectory) == null) && (ee2(), await te()));
})();
function ae() {
  const r = `${Math.random().toString().slice(2)}-${Date.now()}`;
  return H2.push(r), F2(`${U2}/${r}`);
}

// node_modules/@webav/av-cliper/dist/av-cliper.js
function et(s) {
  if (s.format === "f32-planar") {
    const t = [];
    for (let e = 0; e < s.numberOfChannels; e += 1) {
      const i2 = s.allocationSize({ planeIndex: e }), n2 = new ArrayBuffer(i2);
      s.copyTo(n2, { planeIndex: e }), t.push(new Float32Array(n2));
    }
    return t;
  } else if (s.format === "f32") {
    const t = new ArrayBuffer(s.allocationSize({ planeIndex: 0 }));
    return s.copyTo(t, { planeIndex: 0 }), Et(new Float32Array(t), s.numberOfChannels);
  } else if (s.format === "s16") {
    const t = new ArrayBuffer(s.allocationSize({ planeIndex: 0 }));
    return s.copyTo(t, { planeIndex: 0 }), Rt(new Int16Array(t), s.numberOfChannels);
  }
  throw Error("Unsupported audio data format");
}
function Rt(s, t) {
  const e = s.length / t, i2 = Array.from(
    { length: t },
    () => new Float32Array(e)
  );
  for (let n2 = 0; n2 < e; n2++)
    for (let a = 0; a < t; a++) {
      const r = s[n2 * t + a];
      i2[a][n2] = r / 32768;
    }
  return i2;
}
function Et(s, t) {
  const e = s.length / t, i2 = Array.from(
    { length: t },
    () => new Float32Array(e)
  );
  for (let n2 = 0; n2 < e; n2++)
    for (let a = 0; a < t; a++)
      i2[a][n2] = s[n2 * t + a];
  return i2;
}
function $3(s) {
  return Array(s.numberOfChannels).fill(0).map((t, e) => s.getChannelData(e));
}
async function Pt(s, t, e) {
  const i2 = s.length, n2 = Array(e.chanCount).fill(0).map(() => new Float32Array(0));
  if (i2 === 0) return n2;
  const a = Math.max(...s.map((l2) => l2.length));
  if (a === 0) return n2;
  if (globalThis.OfflineAudioContext == null)
    return s.map(
      (l2) => new Float32Array(
        resample(l2, t, e.rate, {
          method: "sinc",
          LPF: false
        })
      )
    );
  const r = new globalThis.OfflineAudioContext(
    e.chanCount,
    a * e.rate / t,
    e.rate
  ), o2 = r.createBufferSource(), c = r.createBuffer(i2, a, t);
  return s.forEach((l2, h) => c.copyToChannel(l2, h)), o2.buffer = c, o2.connect(r.destination), o2.start(), $3(await r.startRendering());
}
function H3(s) {
  return new Promise((t) => {
    const e = _(() => {
      e(), t();
    }, s);
  });
}
function it(s, t) {
  const e = Math.floor(s.length / t), i2 = new Float32Array(e);
  for (let n2 = 0; n2 < e; n2++) {
    const a = n2 * t, r = Math.floor(a), o2 = a - r;
    r + 1 < s.length ? i2[n2] = s[r] * (1 - o2) + s[r + 1] * o2 : i2[n2] = s[r];
  }
  return i2;
}
var x2 = {
  sampleRate: 48e3,
  channelCount: 2,
  codec: "mp4a.40.2"
};
function W3(s, t) {
  const e = t.videoTracks[0], i2 = {};
  if (e != null) {
    const a = Mt(s.getTrackById(e.id))?.buffer, { descKey: r, type: o2 } = e.codec.startsWith("avc1") ? { descKey: "avcDecoderConfigRecord", type: "avc1" } : e.codec.startsWith("hvc1") ? { descKey: "hevcDecoderConfigRecord", type: "hvc1" } : { descKey: "", type: "" };
    r !== "" && (i2.videoTrackConf = {
      timescale: e.timescale,
      duration: e.duration,
      width: e.video.width,
      height: e.video.height,
      brands: t.brands,
      type: o2,
      [r]: a
    }), i2.videoDecoderConf = {
      codec: e.codec,
      codedHeight: e.video.height,
      codedWidth: e.video.width,
      description: a
    };
  }
  const n2 = t.audioTracks[0];
  if (n2 != null) {
    const a = Bt(s), r = a == null ? {} : _t(a);
    i2.audioTrackConf = {
      timescale: n2.timescale,
      samplerate: r.sampleRate ?? n2.audio.sample_rate,
      channel_count: r.numberOfChannels ?? n2.audio.channel_count,
      hdlr: "soun",
      type: n2.codec.startsWith("mp4a") ? "mp4a" : n2.codec,
      description: a
    }, i2.audioDecoderConf = {
      codec: r.codec ?? x2.codec,
      numberOfChannels: r.numberOfChannels ?? n2.audio.channel_count,
      sampleRate: r.sampleRate ?? n2.audio.sample_rate
    };
  }
  return i2;
}
function Mt(s) {
  for (const t of s.mdia.minf.stbl.stsd.entries) {
    const e = t.avcC ?? t.hvcC ?? t.av1C ?? t.vpcC;
    if (e != null) {
      const i2 = new import_mp4box2.default.DataStream(
        void 0,
        0,
        import_mp4box2.default.DataStream.BIG_ENDIAN
      );
      return e.write(i2), new Uint8Array(i2.buffer.slice(8));
    }
  }
}
function Bt(s, t = "mp4a") {
  return s.moov?.traks.map((i2) => i2.mdia.minf.stbl.stsd.entries).flat().find(({ type: i2 }) => i2 === t)?.esds;
}
function _t(s) {
  let t = "mp4a";
  const e = s.esd.descs[0];
  if (e == null) return {};
  t += "." + e.oti.toString(16);
  const i2 = e.descs[0];
  if (i2 == null)
    return t.endsWith("40") && (t += ".2"), { codec: t };
  const n2 = (i2.data[0] & 248) >> 3;
  t += "." + n2;
  const [a, r] = i2.data, o2 = ((a & 7) << 1) + (r >> 7), c = (r & 127) >> 3;
  return {
    codec: t,
    sampleRate: [
      96e3,
      88200,
      64e3,
      48e3,
      44100,
      32e3,
      24e3,
      22050,
      16e3,
      12e3,
      11025,
      8e3,
      7350
    ][o2],
    numberOfChannels: c
  };
}
async function Ot(s, t, e) {
  const i2 = import_mp4box2.default.createFile(false);
  i2.onReady = (a) => {
    t({ mp4boxFile: i2, info: a });
    const r = a.videoTracks[0]?.id;
    r != null && i2.setExtractionOptions(r, "video", { nbSamples: 100 });
    const o2 = a.audioTracks[0]?.id;
    o2 != null && i2.setExtractionOptions(o2, "audio", { nbSamples: 100 }), i2.start();
  }, i2.onSamples = e, await n2();
  async function n2() {
    let a = 0;
    const r = 30 * 1024 * 1024;
    for (; ; ) {
      const o2 = await s.read(r, {
        at: a
      });
      if (o2.byteLength === 0) break;
      o2.fileStart = a;
      const c = i2.appendBuffer(o2);
      if (c == null) break;
      a = c;
    }
    i2.stop();
  }
}
function zt(s) {
  if (s?.length !== 9) return {};
  const t = new Int32Array(s.buffer), e = t[0] / 65536, i2 = t[1] / 65536, n2 = t[3] / 65536, a = t[4] / 65536, r = t[6] / 65536, o2 = t[7] / 65536, c = t[8] / (1 << 30), l2 = Math.sqrt(e * e + n2 * n2), h = Math.sqrt(i2 * i2 + a * a), u2 = Math.atan2(n2, e), d2 = u2 * 180 / Math.PI;
  return {
    scaleX: l2,
    scaleY: h,
    rotationRad: u2,
    rotationDeg: d2,
    translateX: r,
    translateY: o2,
    perspective: c
  };
}
function Vt(s, t, e) {
  const i2 = (Math.round(e / 90) * 90 + 360) % 360;
  if (i2 === 0) return (c) => c;
  const n2 = i2 === 90 || i2 === 270 ? t : s, a = i2 === 90 || i2 === 270 ? s : t, r = new OffscreenCanvas(n2, a), o2 = r.getContext("2d");
  return o2.translate(n2 / 2, a / 2), o2.rotate(-i2 * Math.PI / 180), o2.translate(-s / 2, -t / 2), (c) => {
    if (c == null) return null;
    o2.drawImage(c, 0, 0);
    const l2 = new VideoFrame(r, {
      timestamp: c.timestamp,
      duration: c.duration ?? void 0
    });
    return c.close(), l2;
  };
}
var X2 = 0;
function B3(s) {
  return s.kind === "file" && s.createReader instanceof Function;
}
var I3 = class _I {
  #t = X2++;
  #n = S.create(`MP4Clip id:${this.#t},`);
  ready;
  #e = false;
  #s = {
    // 微秒
    duration: 0,
    width: 0,
    height: 0,
    audioSampleRate: 0,
    audioChanCount: 0
  };
  get meta() {
    return { ...this.#s };
  }
  #a;
  /** 存储视频头（box: ftyp, moov）的二进制数据 */
  #r = [];
  /**
   * 提供视频头（box: ftyp, moov）的二进制数据
   * 使用任意 mp4 demxer 解析即可获得详细的视频信息
   * 单元测试包含使用 mp4box.js 解析示例代码
   */
  async getFileHeaderBinData() {
    await this.ready;
    const t = await this.#a.getOriginFile();
    if (t == null) throw Error("MP4Clip localFile is not origin file");
    return await new Blob(
      this.#r.map(
        ({ start: e, size: i2 }) => t.slice(e, e + i2)
      )
    ).arrayBuffer();
  }
  /**存储视频平移旋转信息，目前只还原旋转 */
  #i = {
    perspective: 1,
    rotationRad: 0,
    rotationDeg: 0,
    scaleX: 1,
    scaleY: 1,
    translateX: 0,
    translateY: 0
  };
  #o = (t) => t;
  #l = 1;
  #c = [];
  #d = [];
  #m = null;
  #u = null;
  #f = {
    video: null,
    audio: null
  };
  #h = { audio: true };
  constructor(t, e = {}) {
    if (!(t instanceof ReadableStream) && !B3(t) && !Array.isArray(t.videoSamples))
      throw Error("Illegal argument");
    this.#h = { audio: true, ...e }, this.#l = typeof e.audio == "object" && "volume" in e.audio ? e.audio.volume : 1;
    const i2 = async (n2) => (await B2(this.#a, n2), this.#a);
    this.#a = B3(t) ? t : "localFile" in t ? t.localFile : ae(), this.ready = (t instanceof ReadableStream ? i2(t).then(
      (n2) => J3(n2, this.#h)
    ) : B3(t) ? J3(t, this.#h) : Promise.resolve(t)).then(
      async ({
        videoSamples: n2,
        audioSamples: a,
        decoderConf: r,
        headerBoxPos: o2,
        parsedMatrix: c
      }) => {
        this.#c = n2, this.#d = a, this.#f = r, this.#r = o2, this.#i = c;
        const { videoFrameFinder: l2, audioFrameFinder: h } = Ut(
          {
            video: r.video == null ? null : {
              ...r.video,
              hardwareAcceleration: this.#h.__unsafe_hardwareAcceleration__
            },
            audio: r.audio
          },
          await this.#a.createReader(),
          n2,
          a,
          this.#h.audio !== false ? this.#l : 0
        );
        this.#m = l2, this.#u = h;
        const { codedWidth: u2, codedHeight: d2 } = r.video ?? {};
        return u2 && d2 && (this.#o = Vt(
          u2,
          d2,
          c.rotationDeg
        )), this.#s = Lt(
          r,
          n2,
          a,
          c.rotationDeg
        ), this.#n.info("MP4Clip meta:", this.#s), { ...this.#s };
      }
    );
  }
  /**
   * 拦截 {@link MP4Clip.tick} 方法返回的数据，用于对图像、音频数据二次处理
   * @param time 调用 tick 的时间
   * @param tickRet tick 返回的数据
   *
   * @see [移除视频绿幕背景](https://webav-tech.github.io/WebAV/demo/3_2-chromakey-video)
   */
  tickInterceptor = async (t, e) => e;
  /**
   * 获取素材指定时刻的图像帧、音频数据
   * @param time 微秒
   */
  async tick(t) {
    if (t >= this.#s.duration)
      return await this.tickInterceptor(t, {
        audio: await this.#u?.find(t) ?? [],
        state: "done"
      });
    const [e, i2] = await Promise.all([
      this.#u?.find(t) ?? [],
      this.#m?.find(t).then(this.#o)
    ]);
    return i2 == null ? await this.tickInterceptor(t, {
      audio: e,
      state: "success"
    }) : await this.tickInterceptor(t, {
      video: i2,
      audio: e,
      state: "success"
    });
  }
  #p = new AbortController();
  /**
   * 生成缩略图，默认每个关键帧生成一个 100px 宽度的缩略图。
   *
   * @param imgWidth 缩略图宽度，默认 100
   * @param opts Partial<ThumbnailOpts>
   * @returns Promise<Array<{ ts: number; img: Blob }>>
   */
  async thumbnails(t = 100, e) {
    this.#p.abort(), this.#p = new AbortController();
    const i2 = this.#p.signal;
    await this.ready;
    const n2 = "generate thumbnails aborted";
    if (i2.aborted) throw Error(n2);
    const { width: a, height: r } = this.#s, o2 = Xt(
      t,
      Math.round(r * (t / a)),
      { quality: 0.1, type: "image/png" }
    );
    return new Promise(
      async (c, l2) => {
        let h = [];
        const u2 = this.#f.video;
        if (u2 == null || this.#c.length === 0) {
          d2();
          return;
        }
        i2.addEventListener("abort", () => {
          l2(Error(n2));
        });
        async function d2() {
          i2.aborted || c(
            await Promise.all(
              h.map(async (p2) => ({
                ts: p2.ts,
                img: await p2.img
              }))
            )
          );
        }
        function y2(p2) {
          h.push({
            ts: p2.timestamp,
            img: o2(p2)
          });
        }
        const { start: m2 = 0, end: f2 = this.#s.duration, step: w } = e ?? {};
        if (w) {
          let p2 = m2;
          const g2 = new nt(
            await this.#a.createReader(),
            this.#c,
            {
              ...u2,
              hardwareAcceleration: this.#h.__unsafe_hardwareAcceleration__
            }
          );
          for (; p2 <= f2 && !i2.aborted; ) {
            const b = await g2.find(p2);
            b && y2(b), p2 += w;
          }
          g2.destroy(), d2();
        } else
          await Jt(
            this.#c,
            this.#a,
            u2,
            i2,
            { start: m2, end: f2 },
            (p2, g2) => {
              p2 != null && y2(p2), g2 && d2();
            }
          );
      }
    );
  }
  async split(t) {
    if (await this.ready, t <= 0 || t >= this.#s.duration)
      throw Error('"time" out of bounds');
    const [e, i2] = Yt(
      this.#c,
      t
    ), [n2, a] = Gt(
      this.#d,
      t
    ), r = new _I(
      {
        localFile: this.#a,
        videoSamples: e ?? [],
        audioSamples: n2 ?? [],
        decoderConf: this.#f,
        headerBoxPos: this.#r,
        parsedMatrix: this.#i
      },
      this.#h
    ), o2 = new _I(
      {
        localFile: this.#a,
        videoSamples: i2 ?? [],
        audioSamples: a ?? [],
        decoderConf: this.#f,
        headerBoxPos: this.#r,
        parsedMatrix: this.#i
      },
      this.#h
    );
    return await Promise.all([r.ready, o2.ready]), [r, o2];
  }
  async clone() {
    await this.ready;
    const t = new _I(
      {
        localFile: this.#a,
        videoSamples: [...this.#c],
        audioSamples: [...this.#d],
        decoderConf: this.#f,
        headerBoxPos: this.#r,
        parsedMatrix: this.#i
      },
      this.#h
    );
    return await t.ready, t.tickInterceptor = this.tickInterceptor, t;
  }
  /**
   * 拆分 MP4Clip 为仅包含视频轨道和音频轨道的 MP4Clip
   * @returns Mp4CLip[]
   */
  async splitTrack() {
    await this.ready;
    const t = [];
    if (this.#c.length > 0) {
      const e = new _I(
        {
          localFile: this.#a,
          videoSamples: [...this.#c],
          audioSamples: [],
          decoderConf: {
            video: this.#f.video,
            audio: null
          },
          headerBoxPos: this.#r,
          parsedMatrix: this.#i
        },
        this.#h
      );
      await e.ready, e.tickInterceptor = this.tickInterceptor, t.push(e);
    }
    if (this.#d.length > 0) {
      const e = new _I(
        {
          localFile: this.#a,
          videoSamples: [],
          audioSamples: [...this.#d],
          decoderConf: {
            audio: this.#f.audio,
            video: null
          },
          headerBoxPos: this.#r,
          parsedMatrix: this.#i
        },
        this.#h
      );
      await e.ready, e.tickInterceptor = this.tickInterceptor, t.push(e);
    }
    return t;
  }
  destroy() {
    this.#e || (this.#n.info("MP4Clip destroy"), this.#e = true, this.#m?.destroy(), this.#u?.destroy());
  }
};
function Lt(s, t, e, i2) {
  const n2 = {
    duration: 0,
    width: 0,
    height: 0,
    audioSampleRate: 0,
    audioChanCount: 0
  };
  if (s.video != null && t.length > 0) {
    n2.width = s.video.codedWidth ?? 0, n2.height = s.video.codedHeight ?? 0;
    const o2 = (Math.round(i2 / 90) * 90 + 360) % 360;
    (o2 === 90 || o2 === 270) && ([n2.width, n2.height] = [n2.height, n2.width]);
  }
  s.audio != null && e.length > 0 && (n2.audioSampleRate = x2.sampleRate, n2.audioChanCount = x2.channelCount);
  let a = 0, r = 0;
  if (t.length > 0)
    for (let o2 = t.length - 1; o2 >= 0; o2--) {
      const c = t[o2];
      if (!c.deleted) {
        a = c.cts + c.duration;
        break;
      }
    }
  if (e.length > 0) {
    const o2 = e.at(-1);
    r = o2.cts + o2.duration;
  }
  return n2.duration = Math.max(a, r), n2;
}
function Ut(s, t, e, i2, n2) {
  return {
    audioFrameFinder: n2 === 0 || s.audio == null || i2.length === 0 ? null : new $t(
      t,
      i2,
      s.audio,
      {
        volume: n2,
        targetSampleRate: x2.sampleRate
      }
    ),
    videoFrameFinder: s.video == null || e.length === 0 ? null : new nt(
      t,
      e,
      s.video
    )
  };
}
async function J3(s, t = {}) {
  let e = null;
  const i2 = { video: null, audio: null };
  let n2 = [], a = [], r = [];
  const o2 = {
    perspective: 1,
    rotationRad: 0,
    rotationDeg: 0,
    scaleX: 1,
    scaleY: 1,
    translateX: 0,
    translateY: 0
  };
  let c = -1, l2 = -1;
  const h = await s.createReader();
  await Ot(
    h,
    async (d2) => {
      e = d2.info;
      const y2 = d2.mp4boxFile.ftyp;
      r.push({ start: y2.start, size: y2.size });
      const m2 = d2.mp4boxFile.moov;
      r.push({ start: m2.start, size: m2.size }), Object.assign(o2, zt(e.videoTracks[0]?.matrix));
      let { videoDecoderConf: f2, audioDecoderConf: w } = W3(
        d2.mp4boxFile,
        d2.info
      );
      if (i2.video = f2 ?? null, i2.audio = w ?? null, f2 == null && w == null && S.error("MP4Clip no video and audio track"), w != null) {
        const { supported: p2 } = await AudioDecoder.isConfigSupported(w);
        p2 || S.error(`MP4Clip audio codec is not supported: ${w.codec}`);
      }
      if (f2 != null) {
        const { supported: p2 } = await VideoDecoder.isConfigSupported(f2);
        p2 || S.error(`MP4Clip video codec is not supported: ${f2.codec}`);
      }
      S.info(
        "mp4BoxFile moov ready",
        {
          ...d2.info,
          tracks: null,
          videoTracks: null,
          audioTracks: null
        },
        i2
      );
    },
    (d2, y2, m2) => {
      if (y2 === "video") {
        c === -1 && (c = m2[0].dts);
        for (const f2 of m2)
          n2.push(Q3(f2, c, "video"));
      } else if (y2 === "audio" && t.audio) {
        l2 === -1 && (l2 = m2[0].dts);
        for (const f2 of m2)
          a.push(Q3(f2, l2, "audio"));
      }
    }
  ), await h.close();
  const u2 = n2.at(-1) ?? a.at(-1);
  if (e == null)
    throw Error("MP4Clip stream is done, but not emit ready");
  if (u2 == null)
    throw Error("MP4Clip stream not contain any sample");
  return L3(n2), S.info("mp4 stream parsed"), {
    videoSamples: n2,
    audioSamples: a,
    decoderConf: i2,
    headerBoxPos: r,
    parsedMatrix: o2
  };
}
function Q3(s, t = 0, e) {
  let i2 = s.offset;
  const n2 = e === "video" && s.is_sync ? jt(s.data, s.description.type) : -1;
  let a = s.size;
  return n2 > 0 && (i2 += n2, a -= n2), {
    ...s,
    is_idr: n2 >= 0,
    offset: i2,
    size: a,
    cts: (s.cts - t) / s.timescale * 1e6,
    dts: (s.dts - t) / s.timescale * 1e6,
    duration: s.duration / s.timescale * 1e6,
    timescale: 1e6,
    // 音频数据量可控，直接保存在内存中
    data: e === "video" ? null : s.data
  };
}
var nt = class {
  constructor(t, e, i2) {
    this.localFileReader = t, this.samples = e, this.conf = i2;
  }
  #t = null;
  #n = 0;
  #e = { abort: false, st: performance.now() };
  find = async (t) => {
    (this.#t == null || this.#t.state === "closed" || t <= this.#n || t - this.#n > 3e6) && this.#h(t), this.#e.abort = true, this.#n = t, this.#e = { abort: false, st: performance.now() };
    const e = await this.#m(t, this.#t, this.#e);
    return this.#c = 0, e;
  };
  // fix VideoFrame duration is null
  #s = 0;
  #a = false;
  #r = 0;
  #i = [];
  #o = 0;
  #l = 0;
  #c = 0;
  #d = false;
  #m = async (t, e, i2) => {
    if (e == null || e.state === "closed" || i2.abort) return null;
    if (this.#i.length > 0) {
      const n2 = this.#i[0];
      return t < n2.timestamp ? null : (this.#i.shift(), t > n2.timestamp + (n2.duration ?? 0) ? (n2.close(), await this.#m(t, e, i2)) : (!this.#d && this.#i.length < 10 && this.#f(e).catch((a) => {
        throw this.#d = true, this.#h(t), a;
      }), n2));
    }
    if (this.#u || this.#o < this.#l && e.decodeQueueSize > 0) {
      if (performance.now() - i2.st > 6e3)
        throw Error(
          `MP4Clip.tick video timeout, ${JSON.stringify(this.#p())}`
        );
      this.#c += 1, await H3(15);
    } else {
      if (this.#r >= this.samples.length)
        return null;
      try {
        await this.#f(e);
      } catch (n2) {
        throw this.#h(t), n2;
      }
    }
    return await this.#m(t, e, i2);
  };
  #u = false;
  #f = async (t) => {
    if (this.#u || t.decodeQueueSize > 600) return;
    let e = this.#r + 1;
    if (e > this.samples.length) return;
    this.#u = true;
    let i2 = false;
    for (; e < this.samples.length; e++) {
      const n2 = this.samples[e];
      if (!i2 && !n2.deleted && (i2 = true), n2.is_idr) break;
    }
    if (i2) {
      const n2 = this.samples.slice(this.#r, e);
      if (n2[0]?.is_idr !== true)
        S.warn("First sample not idr frame");
      else {
        const a = performance.now(), r = await st(n2, this.localFileReader), o2 = performance.now() - a;
        if (o2 > 1e3) {
          const c = n2[0], l2 = n2.at(-1), h = l2.offset + l2.size - c.offset;
          S.warn(
            `Read video samples time cost: ${Math.round(o2)}ms, file chunk size: ${h}`
          );
        }
        if (t.state === "closed") return;
        this.#s = r[0]?.duration ?? 0, V3(t, r, {
          onDecodingError: (c) => {
            if (this.#a)
              throw c;
            this.#o === 0 && (this.#a = true, S.warn("Downgrade to software decode"), this.#h());
          }
        }), this.#l += r.length;
      }
    }
    this.#r = e, this.#u = false;
  };
  #h = (t) => {
    if (this.#u = false, this.#i.forEach((i2) => i2.close()), this.#i = [], t == null || t === 0)
      this.#r = 0;
    else {
      let i2 = 0;
      for (let n2 = 0; n2 < this.samples.length; n2++) {
        const a = this.samples[n2];
        if (a.is_idr && (i2 = n2), !(a.cts < t)) {
          this.#r = i2;
          break;
        }
      }
    }
    this.#l = 0, this.#o = 0, this.#t?.state !== "closed" && this.#t?.close();
    const e = {
      ...this.conf,
      ...this.#a ? { hardwareAcceleration: "prefer-software" } : {}
    };
    this.#t = new VideoDecoder({
      output: (i2) => {
        if (this.#o += 1, i2.timestamp === -1) {
          i2.close();
          return;
        }
        let n2 = i2;
        i2.duration == null && (n2 = new VideoFrame(i2, {
          duration: this.#s
        }), i2.close()), this.#i.push(n2);
      },
      error: (i2) => {
        if (i2.message.includes("Codec reclaimed due to inactivity")) {
          this.#t = null, S.warn(i2.message);
          return;
        }
        const n2 = `VideoFinder VideoDecoder err: ${i2.message}, config: ${JSON.stringify(e)}, state: ${JSON.stringify(this.#p())}`;
        throw S.error(n2), Error(n2);
      }
    }), this.#t.configure(e);
  };
  #p = () => ({
    time: this.#n,
    decState: this.#t?.state,
    decQSize: this.#t?.decodeQueueSize,
    decCusorIdx: this.#r,
    sampleLen: this.samples.length,
    inputCnt: this.#l,
    outputCnt: this.#o,
    cacheFrameLen: this.#i.length,
    softDeocde: this.#a,
    clipIdCnt: X2,
    sleepCnt: this.#c,
    memInfo: at()
  });
  destroy = () => {
    this.#t?.state !== "closed" && this.#t?.close(), this.#t = null, this.#e.abort = true, this.#i.forEach((t) => t.close()), this.#i = [], this.localFileReader.close();
  };
};
function Nt(s, t) {
  for (let e = 0; e < t.length; e++) {
    const i2 = t[e];
    if (s >= i2.cts && s < i2.cts + i2.duration)
      return e;
    if (i2.cts > s) break;
  }
  return 0;
}
var $t = class {
  constructor(t, e, i2, n2) {
    this.localFileReader = t, this.samples = e, this.conf = i2, this.#t = n2.volume, this.#n = n2.targetSampleRate;
  }
  #t = 1;
  #n;
  #e = null;
  #s = { abort: false, st: performance.now() };
  find = async (t) => {
    const e = t <= this.#a || t - this.#a > 1e5;
    (this.#e == null || this.#e.state === "closed" || e) && this.#d(), e && (this.#a = t, this.#r = Nt(t, this.samples)), this.#s.abort = true;
    const i2 = t - this.#a;
    this.#a = t, this.#s = { abort: false, st: performance.now() };
    const n2 = await this.#l(
      Math.ceil(i2 * (this.#n / 1e6)),
      this.#e,
      this.#s
    );
    return this.#o = 0, n2;
  };
  #a = 0;
  #r = 0;
  #i = {
    frameCnt: 0,
    data: []
  };
  #o = 0;
  #l = async (t, e = null, i2) => {
    if (e == null || i2.abort || e.state === "closed" || t === 0)
      return [];
    const n2 = this.#i.frameCnt - t;
    if (n2 > 0)
      return n2 < x2.sampleRate / 10 && this.#c(e), K3(this.#i, t);
    if (e.decoding) {
      if (performance.now() - i2.st > 3e3)
        throw i2.abort = true, Error(
          `MP4Clip.tick audio timeout, ${JSON.stringify(this.#m())}`
        );
      this.#o += 1, await H3(15);
    } else {
      if (this.#r >= this.samples.length - 1)
        return K3(this.#i, this.#i.frameCnt);
      this.#c(e);
    }
    return this.#l(t, e, i2);
  };
  #c = (t) => {
    if (t.decodeQueueSize > 10) return;
    const i2 = [];
    let n2 = this.#r;
    for (; n2 < this.samples.length; ) {
      const a = this.samples[n2];
      if (n2 += 1, !a.deleted && (i2.push(a), i2.length >= 10))
        break;
    }
    this.#r = n2, t.decode(
      i2.map(
        (a) => new EncodedAudioChunk({
          type: "key",
          timestamp: a.cts,
          duration: a.duration,
          data: a.data
        })
      )
    );
  };
  #d = () => {
    this.#a = 0, this.#r = 0, this.#i = {
      frameCnt: 0,
      data: []
    }, this.#e?.close(), this.#e = Ht(
      this.conf,
      {
        resampleRate: x2.sampleRate,
        volume: this.#t
      },
      (t) => {
        this.#i.data.push(t), this.#i.frameCnt += t[0].length;
      }
    );
  };
  #m = () => ({
    time: this.#a,
    decState: this.#e?.state,
    decQSize: this.#e?.decodeQueueSize,
    decCusorIdx: this.#r,
    sampleLen: this.samples.length,
    pcmLen: this.#i.frameCnt,
    clipIdCnt: X2,
    sleepCnt: this.#o,
    memInfo: at()
  });
  destroy = () => {
    this.#e = null, this.#s.abort = true, this.#i = {
      frameCnt: 0,
      data: []
    }, this.localFileReader.close();
  };
};
function Ht(s, t, e) {
  let i2 = 0, n2 = 0;
  const a = (h) => {
    if (n2 += 1, h.length !== 0) {
      if (t.volume !== 1)
        for (const u2 of h)
          for (let d2 = 0; d2 < u2.length; d2++) u2[d2] *= t.volume;
      h.length === 1 && (h = [h[0], h[0]]), e(h);
    }
  }, r = Wt(a), o2 = t.resampleRate !== s.sampleRate;
  let c = new AudioDecoder({
    output: (h) => {
      const u2 = et(h);
      o2 ? r(
        () => Pt(u2, h.sampleRate, {
          rate: t.resampleRate,
          chanCount: h.numberOfChannels
        })
      ) : a(u2), h.close();
    },
    error: (h) => {
      h.message.includes("Codec reclaimed due to inactivity") || l2("MP4Clip AudioDecoder err", h);
    }
  });
  c.configure(s);
  function l2(h, u2) {
    const d2 = `${h}: ${u2.message}, state: ${JSON.stringify(
      {
        qSize: c.decodeQueueSize,
        state: c.state,
        inputCnt: i2,
        outputCnt: n2
      }
    )}`;
    throw S.error(d2), Error(d2);
  }
  return {
    decode(h) {
      i2 += h.length;
      try {
        for (const u2 of h) c.decode(u2);
      } catch (u2) {
        l2("decode audio chunk error", u2);
      }
    },
    close() {
      c.state !== "closed" && c.close();
    },
    get decoding() {
      return i2 > n2 && c.decodeQueueSize > 0;
    },
    get state() {
      return c.state;
    },
    get decodeQueueSize() {
      return c.decodeQueueSize;
    }
  };
}
function Wt(s) {
  const t = [];
  let e = 0;
  function i2(r, o2) {
    t[o2] = r, n2();
  }
  function n2() {
    const r = t[e];
    r != null && (s(r), e += 1, n2());
  }
  let a = 0;
  return (r) => {
    const o2 = a;
    a += 1, r().then((c) => i2(c, o2)).catch((c) => i2(c, o2));
  };
}
function K3(s, t) {
  const e = [new Float32Array(t), new Float32Array(t)];
  let i2 = 0, n2 = 0;
  for (; n2 < s.data.length; ) {
    const [a, r] = s.data[n2];
    if (i2 + a.length > t) {
      const o2 = t - i2;
      e[0].set(a.subarray(0, o2), i2), e[1].set(r.subarray(0, o2), i2), s.data[n2][0] = a.subarray(o2, a.length), s.data[n2][1] = r.subarray(o2, r.length);
      break;
    } else
      e[0].set(a, i2), e[1].set(r, i2), i2 += a.length, n2++;
  }
  return s.data = s.data.slice(n2), s.frameCnt -= t, e;
}
async function st(s, t) {
  const e = s[0], i2 = s.at(-1);
  if (i2 == null) return [];
  const n2 = i2.offset + i2.size - e.offset;
  if (n2 < 3e7) {
    const a = new Uint8Array(
      await t.read(n2, { at: e.offset })
    );
    return s.map((r) => {
      const o2 = r.offset - e.offset;
      return new EncodedVideoChunk({
        type: r.is_sync ? "key" : "delta",
        timestamp: r.cts,
        duration: r.duration,
        data: a.subarray(o2, o2 + r.size)
      });
    });
  }
  return await Promise.all(
    s.map(async (a) => new EncodedVideoChunk({
      type: a.is_sync ? "key" : "delta",
      timestamp: a.cts,
      duration: a.duration,
      data: await t.read(a.size, {
        at: a.offset
      })
    }))
  );
}
function Xt(s, t, e) {
  const i2 = new OffscreenCanvas(s, t), n2 = i2.getContext("2d");
  return async (a) => (n2.drawImage(a, 0, 0, s, t), a.close(), await i2.convertToBlob(e));
}
function Yt(s, t) {
  if (s.length === 0) return [];
  let e = 0, i2 = 0, n2 = -1;
  for (let c = 0; c < s.length; c++) {
    const l2 = s[c];
    if (n2 === -1 && t < l2.cts && (n2 = c - 1), l2.is_idr)
      if (n2 === -1)
        e = c;
      else {
        i2 = c;
        break;
      }
  }
  const a = s[n2];
  if (a == null) throw Error("Not found video sample by time");
  const r = s.slice(0, i2 === 0 ? s.length : i2).map((c) => ({ ...c }));
  for (let c = e; c < r.length; c++) {
    const l2 = r[c];
    t < l2.cts && (l2.deleted = true, l2.cts = -1);
  }
  L3(r);
  const o2 = s.slice(a.is_idr ? n2 : e).map((c) => ({ ...c, cts: c.cts - t }));
  for (const c of o2)
    c.cts < 0 && (c.deleted = true, c.cts = -1);
  return L3(o2), [r, o2];
}
function Gt(s, t) {
  if (s.length === 0) return [void 0, void 0];
  if (s[0].cts >= t)
    return [void 0, s.map((r) => ({ ...r }))];
  if (s[s.length - 1].cts < t)
    return [s.map((r) => ({ ...r })), void 0];
  let i2 = -1;
  for (let r = 0; r < s.length; r++) {
    const o2 = s[r];
    if (!(t > o2.cts)) {
      i2 = r;
      break;
    }
  }
  if (i2 === -1) throw Error("Not found audio sample by time");
  const n2 = s.slice(0, i2).map((r) => ({ ...r })), a = s.slice(i2).map((r) => ({ ...r, cts: r.cts - t }));
  return [n2, a];
}
function V3(s, t, e) {
  if (s.state === "configured") {
    for (let i2 = 0; i2 < t.length; i2++) s.decode(t[i2]);
    s.flush().catch((i2) => {
      if (!(i2 instanceof Error)) throw i2;
      if (i2.message.includes("Decoding error") && e.onDecodingError != null) {
        e.onDecodingError(i2);
        return;
      }
      if (!i2.message.includes("Aborted due to close"))
        throw i2;
    });
  }
}
function jt(s, t) {
  if (t !== "avc1" && t !== "hvc1") return 0;
  const e = new DataView(s.buffer);
  for (let i2 = 0; i2 < s.byteLength - 4; ) {
    if (t === "avc1") {
      const n2 = e.getUint8(i2 + 4) & 31;
      if (n2 === 5 || n2 === 7 || n2 === 8) return i2;
    } else if (t === "hvc1") {
      const n2 = e.getUint8(i2 + 4) >> 1 & 63;
      if (n2 === 19 || n2 === 20 || n2 === 32 || n2 === 33 || n2 === 34)
        return i2;
    }
    i2 += e.getUint32(i2) + 4;
  }
  return -1;
}
async function Jt(s, t, e, i2, n2, a) {
  const r = await t.createReader(), o2 = await st(
    s.filter(
      (h) => !h.deleted && h.is_sync && h.cts >= n2.start && h.cts <= n2.end
    ),
    r
  );
  if (o2.length === 0 || i2.aborted) {
    a(null, true);
    return;
  }
  let c = 0;
  V3(l2(), o2, {
    onDecodingError: (h) => {
      S.warn("thumbnailsByKeyFrame", h), c === 0 ? V3(l2(true), o2, {
        onDecodingError: (u2) => {
          r.close(), S.error("thumbnailsByKeyFrame retry soft deocde", u2);
        }
      }) : (a(null, true), r.close());
    }
  });
  function l2(h = false) {
    const u2 = {
      ...e,
      ...h ? { hardwareAcceleration: "prefer-software" } : {}
    }, d2 = new VideoDecoder({
      output: (y2) => {
        c += 1;
        const m2 = c === o2.length;
        a(y2, m2), m2 && (r.close(), d2.state !== "closed" && d2.close());
      },
      error: (y2) => {
        const m2 = `thumbnails decoder error: ${y2.message}, config: ${JSON.stringify(u2)}, state: ${JSON.stringify(
          {
            qSize: d2.decodeQueueSize,
            state: d2.state,
            outputCnt: c,
            inputCnt: o2.length
          }
        )}`;
        throw S.error(m2), Error(m2);
      }
    });
    return i2.addEventListener("abort", () => {
      r.close(), d2.state !== "closed" && d2.close();
    }), d2.configure(u2), d2;
  }
}
function L3(s) {
  let t = 0, e = null;
  for (const i2 of s)
    if (!i2.deleted) {
      if (i2.is_sync && (t += 1), t >= 2) break;
      (e == null || i2.cts < e.cts) && (e = i2);
    }
  e != null && e.cts < 2e5 && (e.duration += e.cts, e.cts = 0);
}
function at() {
  try {
    const s = performance.memory;
    return {
      jsHeapSizeLimit: s.jsHeapSizeLimit,
      totalJSHeapSize: s.totalJSHeapSize,
      usedJSHeapSize: s.usedJSHeapSize,
      percentUsed: (s.usedJSHeapSize / s.jsHeapSizeLimit).toFixed(3),
      percentTotal: (s.totalJSHeapSize / s.jsHeapSizeLimit).toFixed(3)
    };
  } catch {
    return {};
  }
}
var oe = 0;
async function ct(s) {
  s() > 50 && (await H3(15), await ct(s));
}
var xe = class {
  /**
   * 检测当前环境的兼容性
   * @param args.videoCodec 指定视频编码格式，默认 avc1.42E032
   * @param args.width 指定视频宽度，默认 1920
   * @param args.height 指定视频高度，默认 1080
   * @param args.bitrate 指定视频比特率，默认 5e6
   */
  static async isSupported(t = {}) {
    return (self.OffscreenCanvas != null && self.VideoEncoder != null && self.VideoDecoder != null && self.VideoFrame != null && self.AudioEncoder != null && self.AudioDecoder != null && self.AudioData != null && ((await self.VideoEncoder.isConfigSupported({
      codec: t.videoCodec ?? "avc1.42E032",
      width: t.width ?? 1920,
      height: t.height ?? 1080,
      bitrate: t.bitrate ?? 7e6
    })).supported ?? false) && (await self.AudioEncoder.isConfigSupported({
      codec: x2.codec,
      sampleRate: x2.sampleRate,
      numberOfChannels: x2.channelCount
    })).supported) ?? false;
  }
  #t = S.create(`id:${oe++},`);
  #n = false;
  #e = [];
  #s;
  #a;
  // 中断输出
  #r = null;
  #i;
  #o;
  #l = new M();
  on = this.#l.on;
  /**
   * 根据配置创建合成器实例
   * @param opts ICombinatorOpts
   */
  constructor(t = {}) {
    const { width: e = 0, height: i2 = 0 } = t;
    this.#s = new OffscreenCanvas(e, i2);
    const n2 = this.#s.getContext("2d", { alpha: false });
    if (n2 == null) throw Error("Can not create 2d offscreen context");
    this.#a = n2, this.#i = Object.assign(
      {
        bgColor: "#000",
        width: 0,
        height: 0,
        videoCodec: "avc1.42E032",
        audio: true,
        bitrate: 5e6,
        fps: 30,
        metaDataTags: null
      },
      t
    ), this.#o = e * i2 > 0;
  }
  /**
   * 添加用于合成视频的 Sprite，视频时长默认取所有素材 duration 字段的最大值
   * @param os Sprite
   * @param opts.main 如果 main 为 true，视频时长为该素材的 duration 值
   */
  async addSprite(t, e = {}) {
    const i2 = {
      rect: de(["x", "y", "w", "h"], t.rect),
      time: { ...t.time },
      zIndex: t.zIndex
    };
    this.#t.info("Combinator add sprite", i2);
    const n2 = await t.clone();
    this.#t.info("Combinator add sprite ready"), this.#e.push(
      Object.assign(n2, {
        main: e.main ?? false,
        expired: false
      })
    ), this.#e.sort((a, r) => a.zIndex - r.zIndex);
  }
  #c(t) {
    const { fps: e, width: i2, height: n2, videoCodec: a, bitrate: r, audio: o2, metaDataTags: c } = this.#i;
    return ee({
      video: this.#o ? {
        width: i2,
        height: n2,
        expectFPS: e,
        codec: a,
        bitrate: r,
        __unsafe_hardwareAcceleration__: this.#i.__unsafe_hardwareAcceleration__
      } : null,
      audio: o2 === false ? null : {
        codec: "aac",
        sampleRate: x2.sampleRate,
        channelCount: x2.channelCount
      },
      duration: t,
      metaDataTags: c
    });
  }
  /**
   * 输出视频文件二进制流
   * @param opts.maxTime 允许输出视频的最大时长，超过时长的素材内容会被忽略
   */
  output(t = {}) {
    if (this.#e.length === 0) throw Error("No sprite added");
    const e = this.#e.find((l2) => l2.main), i2 = t.maxTime ?? (e != null ? e.time.offset + e.time.duration : Math.max(
      ...this.#e.map((l2) => l2.time.offset + l2.time.duration)
    ));
    if (i2 === 1 / 0)
      throw Error(
        "Unable to determine the end time, please specify a main sprite, or limit the duration of ImgClip, AudioCli"
      );
    i2 === -1 && this.#t.warn(
      "Unable to determine the end time, process value don't update"
    ), this.#t.info(`start combinate video, maxTime:${i2}`);
    const n2 = this.#c(i2);
    let a = performance.now();
    const r = this.#d(n2, i2, {
      onProgress: (l2) => {
        this.#t.debug("OutputProgress:", l2), this.#l.emit("OutputProgress", l2);
      },
      onEnded: async () => {
        await n2.flush(), this.#t.info(
          "===== output ended =====, cost:",
          performance.now() - a
        ), this.#l.emit("OutputProgress", 1), this.destroy();
      },
      onError: (l2) => {
        this.#l.emit("error", l2), c(l2), this.destroy();
      }
    });
    this.#r = () => {
      r(), n2.close(), c();
    };
    const { stream: o2, stop: c } = Z(
      n2.mp4file,
      500,
      this.destroy
    );
    return o2;
  }
  /**
   * 销毁实例，释放资源
   */
  destroy() {
    this.#n || (this.#n = true, this.#r?.(), this.#l.destroy());
  }
  #d(t, e, {
    onProgress: i2,
    onEnded: n2,
    onError: a
  }) {
    let r = 0;
    const o2 = { aborted: false };
    let c = null;
    (async () => {
      const { fps: d2, bgColor: y2, audio: m2 } = this.#i, f2 = Math.round(1e6 / d2), w = this.#a, p2 = ce({
        ctx: w,
        bgColor: y2,
        sprites: this.#e,
        aborter: o2
      }), g2 = le({
        remux: t,
        ctx: w,
        cvs: this.#s,
        outputAudio: m2,
        hasVideoTrack: this.#o,
        timeSlice: f2,
        fps: d2
      });
      let b = 0;
      for (; ; ) {
        if (c != null) return;
        if (o2.aborted || e !== -1 && b > e || this.#e.length === 0) {
          u2(), await n2();
          return;
        }
        r = b / e;
        const { audios: v2, mainSprDone: S3 } = await p2(b);
        if (S3) {
          u2(), await n2();
          return;
        }
        if (o2.aborted) return;
        g2(b, v2), b += f2, await ct(t.getEncodeQueueSize);
      }
    })().catch((d2) => {
      c = d2, this.#t.error(d2), u2(), a(d2);
    });
    const h = setInterval(() => {
      i2(r);
    }, 500), u2 = () => {
      o2.aborted || (o2.aborted = true, clearInterval(h), this.#e.forEach((d2) => d2.destroy()));
    };
    return u2;
  }
};
function ce(s) {
  const { ctx: t, bgColor: e, sprites: i2, aborter: n2 } = s, { width: a, height: r } = t.canvas;
  return async (o2) => {
    t.fillStyle = e, t.fillRect(0, 0, a, r);
    const c = [];
    let l2 = false;
    for (const h of i2) {
      if (n2.aborted) break;
      if (o2 < h.time.offset || h.expired) continue;
      t.save();
      const { audio: u2, done: d2 } = await h.offscreenRender(t, o2 - h.time.offset);
      c.push(u2), t.restore(), (h.time.duration > 0 && o2 > h.time.offset + h.time.duration || d2) && (h.main && (l2 = true), h.destroy(), h.expired = true);
    }
    return {
      audios: c,
      mainSprDone: l2
    };
  };
}
function le(s) {
  const { ctx: t, cvs: e, outputAudio: i2, remux: n2, hasVideoTrack: a, timeSlice: r } = s, { width: o2, height: c } = e;
  let l2 = 0;
  const h = Math.floor(3 * s.fps), u2 = he(1024);
  return (d2, y2) => {
    if (i2 !== false)
      for (const m2 of u2(d2, y2)) n2.encodeAudio(m2);
    if (a) {
      const m2 = new VideoFrame(e, {
        duration: r,
        timestamp: d2
      });
      n2.encodeVideo(m2, {
        keyFrame: l2 % h === 0
      }), t.resetTransform(), t.clearRect(0, 0, o2, c), l2 += 1;
    }
  };
}
function he(s) {
  const t = s * x2.channelCount, e = new Float32Array(t * 3);
  let i2 = 0, n2 = 0;
  const a = s / x2.sampleRate * 1e6, r = new Float32Array(t), o2 = (c) => {
    let l2 = 0;
    const h = Math.floor(i2 / t), u2 = [];
    for (let d2 = 0; d2 < h; d2++)
      u2.push(
        new AudioData({
          timestamp: n2,
          numberOfChannels: x2.channelCount,
          numberOfFrames: s,
          sampleRate: x2.sampleRate,
          format: "f32",
          data: e.subarray(l2, l2 + t)
        })
      ), l2 += t, n2 += a;
    for (e.set(e.subarray(l2, i2), 0), i2 -= l2; c - n2 > a; )
      u2.push(
        new AudioData({
          timestamp: n2,
          numberOfChannels: x2.channelCount,
          numberOfFrames: s,
          sampleRate: x2.sampleRate,
          format: "f32",
          data: r
        })
      ), n2 += a;
    return u2;
  };
  return (c, l2) => {
    const h = Math.max(...l2.map((u2) => u2[0]?.length ?? 0));
    for (let u2 = 0; u2 < h; u2++) {
      let d2 = 0, y2 = 0;
      for (let m2 = 0; m2 < l2.length; m2++) {
        const f2 = l2[m2][0]?.[u2] ?? 0, w = l2[m2][1]?.[u2] ?? f2;
        d2 += f2, y2 += w;
      }
      e[i2] = d2, e[i2 + 1] = y2, i2 += 2;
    }
    return o2(c);
  };
}
function de(s, t) {
  return s.reduce(
    (e, i2) => (e[i2] = t[i2], e),
    {}
  );
}
var Y3 = class _Y {
  #t = new M();
  /**
   * 监听属性变更事件
   * @example
   * rect.on('propsChange', (changedProps) => {})
   */
  on = this.#t.on;
  #n = 0;
  /**
   * x 坐标
   */
  get x() {
    return this.#n;
  }
  set x(t) {
    this.#i("x", t);
  }
  #e = 0;
  get y() {
    return this.#e;
  }
  /**
   * y 坐标
   */
  set y(t) {
    this.#i("y", t);
  }
  #s = 0;
  /**
   * 宽
   */
  get w() {
    return this.#s;
  }
  set w(t) {
    this.#i("w", t);
  }
  #a = 0;
  /**
   * 高
   */
  get h() {
    return this.#a;
  }
  set h(t) {
    this.#i("h", t);
  }
  #r = 0;
  /**
   * 旋转角度
   * @see [MDN Canvas rotate](https://developer.mozilla.org/docs/Web/API/CanvasRenderingContext2D/rotate)
   */
  get angle() {
    return this.#r;
  }
  set angle(t) {
    this.#i("angle", t);
  }
  #i(t, e) {
    const i2 = this[t] !== e;
    switch (t) {
      case "x":
        this.#n = e;
        break;
      case "y":
        this.#e = e;
        break;
      case "w":
        this.#s = e;
        break;
      case "h":
        this.#a = e;
        break;
      case "angle":
        this.#r = e;
        break;
    }
    i2 && this.#t.emit("propsChange", { [t]: e });
  }
  /**
   * 如果当前实例是 Rect 控制点之一，`master` 将指向该 Rect
   *
   * 控制点的坐标是相对于它的 `master` 定位
   */
  #o = null;
  constructor(t, e, i2, n2, a) {
    this.x = t ?? 0, this.y = e ?? 0, this.w = i2 ?? 0, this.h = n2 ?? 0, this.#o = a ?? null;
  }
  /**
   * 根据坐标、宽高计算出来的矩形中心点
   */
  get center() {
    const { x: t, y: e, w: i2, h: n2 } = this;
    return { x: t + i2 / 2, y: e + n2 / 2 };
  }
  /**
   * 是否保持固定宽高比例，禁止变形缩放
   *
   * 值为 true 时，将缺少上下左右四个控制点
   */
  fixedAspectRatio = false;
  /**
   * 是否固定中心点进行缩放
   * 值为 true 时，固定中心点不变进行缩放
   * 值为 false 时，固定对角点不变进行缩放
   */
  fixedScaleCenter = false;
  clone() {
    const { x: t, y: e, w: i2, h: n2 } = this, a = new _Y(t, e, i2, n2, this.#o);
    return a.angle = this.angle, a.fixedAspectRatio = this.fixedAspectRatio, a.fixedScaleCenter = this.fixedScaleCenter, a;
  }
  /**
   * 检测目标坐标是否命中当前实例
   * @param tx 目标点 x 坐标
   * @param ty 目标点 y 坐标
   */
  checkHit(t, e) {
    let { angle: i2, center: n2, x: a, y: r, w: o2, h: c } = this;
    const l2 = this.#o?.center ?? n2, h = this.#o?.angle ?? i2;
    this.#o == null && (a = a - l2.x, r = r - l2.y);
    const u2 = t - l2.x, d2 = e - l2.y;
    let y2 = u2, m2 = d2;
    return h !== 0 && (y2 = u2 * Math.cos(h) + d2 * Math.sin(h), m2 = d2 * Math.cos(h) - u2 * Math.sin(h)), !(y2 < a || y2 > a + o2 || m2 < r || m2 > r + c);
  }
};
var lt = class {
  /**
   * 控制素材在视频中的空间属性（坐标、旋转、缩放）
   */
  rect = new Y3();
  /**
   * 控制素材在的时间偏移、时长、播放速率，常用于剪辑场景时间轴（轨道）模块
   * duration 不能大于引用 {@link IClip} 的时长，单位 微秒
   *
   * playbackRate 控制当前素材的播放速率，1 表示正常播放；
   * **注意**
   *    1. 设置 playbackRate 时需要主动修正 duration
   *    2. 音频使用最简单的插值算法来改变速率，所以改变速率后音调会产生变化，自定义算法请使用 {@link MP4Clip.tickInterceptor} 配合实现
   *
   */
  #t = {
    offset: 0,
    duration: 0,
    playbackRate: 1
  };
  get time() {
    return this.#t;
  }
  set time(t) {
    Object.assign(this.#t, t);
  }
  #n = new M();
  /**
   * 监听属性变更事件
   * @example
   * sprite.on('propsChange', (changedProps) => {})
   */
  on = this.#n.on;
  #e = 0;
  get zIndex() {
    return this.#e;
  }
  /**
   * 控制素材间的层级关系，zIndex 值较小的素材会被遮挡
   */
  set zIndex(t) {
    const e = this.#e !== t;
    this.#e = t, e && this.#n.emit("propsChange", { zIndex: t });
  }
  /**
   * 不透明度
   */
  opacity = 1;
  /**
   * 水平或垂直方向翻转素材
   */
  flip = null;
  #s = null;
  #a = null;
  /**
   * @see {@link IClip.ready}
   */
  ready = Promise.resolve();
  constructor() {
    this.rect.on("propsChange", (t) => {
      this.#n.emit("propsChange", { rect: t });
    });
  }
  _render(t) {
    const {
      rect: { center: e, angle: i2 }
    } = this;
    t.setTransform(
      // 水平 缩放、倾斜
      this.flip === "horizontal" ? -1 : 1,
      0,
      // 垂直 倾斜、缩放
      0,
      this.flip === "vertical" ? -1 : 1,
      // 坐标原点偏移 x y
      e.x,
      e.y
    ), t.rotate((this.flip == null ? 1 : -1) * i2), t.globalAlpha = this.opacity;
  }
  /**
   * 给素材添加动画，使用方法参考 css animation
   *
   * @example
   * sprite.setAnimation(
   *   {
   *     '0%': { x: 0, y: 0 },
   *     '25%': { x: 1200, y: 680 },
   *     '50%': { x: 1200, y: 0 },
   *     '75%': { x: 0, y: 680 },
   *     '100%': { x: 0, y: 0 },
   *   },
   *   { duration: 4e6, iterCount: 1 },
   * );
   *
   * @see [视频水印动画](https://webav-tech.github.io/WebAV/demo/2_1-concat-video)
   */
  setAnimation(t, e) {
    this.#s = Object.entries(t).map(([i2, n2]) => {
      const a = { from: 0, to: 100 }[i2] ?? Number(i2.slice(0, -1));
      if (isNaN(a) || a > 100 || a < 0)
        throw Error("keyFrame must between 0~100");
      return [a / 100, n2];
    }), this.#a = Object.assign({}, this.#a, {
      duration: e.duration,
      delay: e.delay ?? 0,
      iterCount: e.iterCount ?? 1 / 0
    });
  }
  /**
   * 如果当前 sprite 已被设置动画，将 sprite 的动画属性设定到指定时间的状态
   */
  animate(t) {
    if (this.#s == null || this.#a == null || t < this.#a.delay)
      return;
    const e = ue(
      t,
      this.#s,
      this.#a
    );
    for (const i2 in e)
      switch (i2) {
        case "opacity":
          this.opacity = e[i2];
          break;
        case "x":
        case "y":
        case "w":
        case "h":
        case "angle":
          this.rect[i2] = e[i2];
          break;
      }
  }
  /**
   * 将当前 sprite 的属性赋值到目标
   *
   * 用于 clone，或 {@link VisibleSprite} 与 {@link OffscreenSprite} 实例间的类型转换
   */
  copyStateTo(t) {
    t.#s = this.#s, t.#a = this.#a, t.zIndex = this.zIndex, t.opacity = this.opacity, t.flip = this.flip, t.rect = this.rect.clone(), t.time = { ...this.time };
  }
  destroy() {
    this.#n.destroy();
  }
};
function ue(s, t, e) {
  const i2 = s - e.delay, n2 = i2 % e.duration, a = i2 / e.duration >= e.iterCount || i2 === e.duration ? 1 : n2 / e.duration, r = t.findIndex((y2) => y2[0] >= a);
  if (r === -1) return {};
  const o2 = t[r - 1], c = t[r], l2 = c[1];
  if (o2 == null) return l2;
  const h = o2[1], u2 = {}, d2 = (a - o2[0]) / (c[0] - o2[0]);
  for (const y2 in l2) {
    const m2 = y2;
    h[m2] != null && (u2[m2] = (l2[m2] - h[m2]) * d2 + h[m2]);
  }
  return u2;
}
var ht = class _ht extends lt {
  #t;
  // 保持最近一帧，若 clip 在当前帧无数据，则绘制最近一帧
  #n = null;
  #e = false;
  constructor(t) {
    super(), this.#t = t, this.ready = t.ready.then(({ width: e, height: i2, duration: n2 }) => {
      this.rect.w = this.rect.w === 0 ? e : this.rect.w, this.rect.h = this.rect.h === 0 ? i2 : this.rect.h, this.time.duration = this.time.duration === 0 ? n2 : this.time.duration;
    });
  }
  /**
   * 绘制素材指定时刻的图像到 canvas 上下文，并返回对应的音频数据
   * @param time 指定时刻，微秒
   */
  async offscreenRender(t, e) {
    const i2 = e * this.time.playbackRate;
    this.animate(i2), super._render(t);
    const { w: n2, h: a } = this.rect, { video: r, audio: o2, state: c } = await this.#t.tick(i2);
    let l2 = o2 ?? [];
    if (o2 != null && this.time.playbackRate !== 1 && (l2 = o2.map(
      (u2) => it(u2, this.time.playbackRate)
    )), c === "done")
      return {
        audio: l2,
        done: true
      };
    const h = r ?? this.#n;
    return h != null && t.drawImage(h, -n2 / 2, -a / 2, n2, a), r != null && (this.#n?.close(), this.#n = r), {
      audio: l2,
      done: false
    };
  }
  async clone() {
    const t = new _ht(await this.#t.clone());
    return await t.ready, this.copyStateTo(t), t;
  }
  destroy() {
    this.#e || (this.#e = true, S.info("OffscreenSprite destroy"), super.destroy(), this.#n?.close(), this.#n = null, this.#t.destroy());
  }
};

// src/client/stage/timeline-edit.ts
function clipPlayDuration(clip2) {
  const span = Math.max(0.05, clip2.sourceOut - clip2.sourceIn);
  return span / Math.max(0.25, clip2.speed);
}
function sequenceDuration(clips) {
  return clips.reduce((sum, clip2) => sum + clipPlayDuration(clip2), 0);
}
function clipStarts(clips) {
  const starts = [];
  let cursor = 0;
  for (const clip2 of clips) {
    starts.push(cursor);
    cursor += clipPlayDuration(clip2);
  }
  return starts;
}
function hitTest(clips, seqTime) {
  const starts = clipStarts(clips);
  for (let index2 = 0; index2 < clips.length; index2 += 1) {
    const clip2 = clips[index2];
    const start2 = starts[index2] ?? 0;
    const end = start2 + clipPlayDuration(clip2);
    if (seqTime >= start2 && seqTime < end - 1e-4 || index2 === clips.length - 1 && seqTime >= start2 && seqTime <= end + 1e-4) {
      const local = Math.min(clipPlayDuration(clip2), Math.max(0, seqTime - start2));
      return { clip: clip2, index: index2, start: start2, local, sourceTime: clip2.sourceIn + local * clip2.speed };
    }
  }
  const last = clips[clips.length - 1];
  if (last === void 0) return void 0;
  return {
    clip: last,
    index: clips.length - 1,
    start: starts[clips.length - 1] ?? 0,
    local: clipPlayDuration(last),
    sourceTime: last.sourceOut
  };
}
function fromSource(duration, id2 = 1) {
  const end = Math.max(0.05, duration);
  return [{ id: id2, sourceIn: 0, sourceOut: end, speed: 1, fadeIn: 0, fadeOut: 0, muted: false }];
}
function nextClipId(clips) {
  return clips.reduce((max, clip2) => Math.max(max, clip2.id), 0) + 1;
}
function splitAt(clips, seqTime, id2) {
  const hit = hitTest(clips, seqTime);
  if (hit === void 0) return clips;
  if (hit.local < 0.05 || hit.local > clipPlayDuration(hit.clip) - 0.05) return clips;
  const cut = hit.sourceTime;
  return clips.flatMap((clip2) => {
    if (clip2.id !== hit.clip.id) return [clip2];
    return [
      { ...clip2, sourceOut: cut },
      { ...clip2, id: id2, sourceIn: cut, fadeIn: 0 }
    ];
  });
}
function removeClip(clips, id2) {
  return clips.filter((clip2) => clip2.id !== id2);
}
function duplicateClip(clips, id2, nextId) {
  const index2 = clips.findIndex((clip3) => clip3.id === id2);
  const clip2 = clips[index2];
  if (clip2 === void 0) return clips;
  const copy = { ...clip2, id: nextId };
  return [...clips.slice(0, index2 + 1), copy, ...clips.slice(index2 + 1)];
}
function moveClip(clips, id2, dir) {
  const index2 = clips.findIndex((clip2) => clip2.id === id2);
  return moveTo(clips, id2, index2 + dir);
}
function moveTo(clips, id2, toIndex) {
  const from = clips.findIndex((clip2) => clip2.id === id2);
  if (from < 0 || toIndex < 0 || toIndex >= clips.length || from === toIndex) return clips;
  const copy = clips.slice();
  const [item3] = copy.splice(from, 1);
  if (item3 === void 0) return clips;
  copy.splice(toIndex, 0, item3);
  return copy;
}
function patchClip(clips, id2, patch) {
  return clips.map((clip2) => clip2.id === id2 ? { ...clip2, ...patch, id: clip2.id } : clip2);
}
function trimClip(clips, id2, edge, sourceTime, sourceDuration) {
  return clips.map((clip2) => {
    if (clip2.id !== id2) return clip2;
    if (edge === "in") {
      const sourceIn = Math.max(0, Math.min(sourceTime, clip2.sourceOut - 0.05));
      return { ...clip2, sourceIn };
    }
    const sourceOut = Math.min(sourceDuration, Math.max(sourceTime, clip2.sourceIn + 0.05));
    return { ...clip2, sourceOut };
  });
}
function evenPx(value) {
  return Math.max(2, Math.round(value / 2) * 2);
}
function exportSize(width, height, scale) {
  if (scale === "orig" || width <= 0 || height <= 0) return { width: evenPx(width || 1280), height: evenPx(height || 720) };
  const maxH = scale === "1080" ? 1080 : scale === "720" ? 720 : 480;
  const factor = Math.min(1, maxH / height);
  return { width: evenPx(width * factor), height: evenPx(height * factor) };
}
function exportBitrate(quality) {
  if (quality === "high") return 8e6;
  if (quality === "draft") return 15e5;
  return 4e6;
}
function fmtClock(seconds) {
  const total = Math.max(0, seconds);
  const m2 = Math.floor(total / 60);
  const s = (total % 60).toFixed(1);
  return `${m2}:${s.padStart(4, "0")}`;
}
function tickStep(pxPerSec) {
  if (pxPerSec >= 160) return 0.5;
  if (pxPerSec >= 80) return 1;
  if (pxPerSec >= 40) return 2;
  return 5;
}

// src/client/stage/VideoStudio.tsx
var import_jsx_runtime7 = require("react/jsx-runtime");
var LOOKS2 = GRADE_LOOK_LIST;
var SPEEDS = [0.5, 1, 1.5, 2];
function VideoStudio(props) {
  const videoRef = (0, import_react7.useRef)(null);
  const trackRef = (0, import_react7.useRef)(null);
  const clipRef = (0, import_react7.useRef)(null);
  const playRef = (0, import_react7.useRef)(void 0);
  const historyRef = (0, import_react7.useRef)([]);
  const redoRef = (0, import_react7.useRef)([]);
  const [sourceDuration, setSourceDuration] = (0, import_react7.useState)(0);
  const [size, setSize] = (0, import_react7.useState)({ width: 1280, height: 720 });
  const [time, setTime] = (0, import_react7.useState)(0);
  const [playing, setPlaying] = (0, import_react7.useState)(false);
  const [clips, setClips] = (0, import_react7.useState)([]);
  const [selected2, setSelected] = (0, import_react7.useState)(void 0);
  const [busy, setBusy] = (0, import_react7.useState)(false);
  const [error, setError] = (0, import_react7.useState)(void 0);
  const [progress, setProgress] = (0, import_react7.useState)(void 0);
  const [exportReady, setExportReady] = (0, import_react7.useState)(false);
  const [loopSelected, setLoopSelected] = (0, import_react7.useState)(false);
  const [drag, setDrag] = (0, import_react7.useState)(void 0);
  const [lookId, setLookId] = (0, import_react7.useState)(void 0);
  const [gradeBusy, setGradeBusy] = (0, import_react7.useState)(false);
  const [mode, setMode] = (0, import_react7.useState)("cut");
  const [mediaSrc2, setMediaSrc] = (0, import_react7.useState)(props.source);
  const [pxPerSec, setPxPerSec] = (0, import_react7.useState)(80);
  const [canUndo, setCanUndo] = (0, import_react7.useState)(false);
  const [canRedo, setCanRedo] = (0, import_react7.useState)(false);
  const [exportScale, setExportScale] = (0, import_react7.useState)("orig");
  const [exportQuality, setExportQuality] = (0, import_react7.useState)("standard");
  const [exportFps, setExportFps] = (0, import_react7.useState)(30);
  const [exportAudio, setExportAudio] = (0, import_react7.useState)(true);
  const [transition2, setTransition] = (0, import_react7.useState)("cut");
  const seqLen = sequenceDuration(clips);
  const starts = clipStarts(clips);
  const selectedClip = clips.find((clip2) => clip2.id === selected2);
  const syncStacks = (history, redo2) => {
    setCanUndo(history.length > 0);
    setCanRedo(redo2.length > 0);
  };
  const commitClips = (0, import_react7.useCallback)((next) => {
    setClips((current) => {
      const resolved = typeof next === "function" ? next(current) : next;
      if (resolved === current) return current;
      historyRef.current.push(current);
      if (historyRef.current.length > 40) historyRef.current.shift();
      redoRef.current = [];
      syncStacks(historyRef.current, redoRef.current);
      return resolved;
    });
  }, []);
  const undo = (0, import_react7.useCallback)(() => {
    const prev = historyRef.current.pop();
    if (prev === void 0) return;
    setClips((current) => {
      redoRef.current.push(current);
      syncStacks(historyRef.current, redoRef.current);
      return prev;
    });
  }, []);
  const redo = (0, import_react7.useCallback)(() => {
    const next = redoRef.current.pop();
    if (next === void 0) return;
    setClips((current) => {
      historyRef.current.push(current);
      syncStacks(historyRef.current, redoRef.current);
      return next;
    });
  }, []);
  (0, import_react7.useEffect)(() => {
    let live = true;
    void (async () => {
      try {
        const response = await fetch(props.source);
        if (!response.ok) throw new Error(`\u89C6\u9891\u52A0\u8F7D\u5931\u8D25 (HTTP ${response.status})`);
        const body = response.body;
        if (body === null) throw new Error("\u89C6\u9891\u6D41\u4E0D\u53EF\u7528");
        const clip2 = new I3(body);
        await clip2.ready;
        if (!live) {
          clip2.destroy();
          return;
        }
        clipRef.current = clip2;
        const nextDuration = clip2.meta.duration / 1e6;
        setSourceDuration(nextDuration);
        setSize({ width: clip2.meta.width, height: clip2.meta.height });
        setClips((current) => current.length === 0 ? fromSource(nextDuration) : current);
        setExportReady(true);
      } catch (cause) {
        if (live) setError(cause instanceof Error ? cause.message : `\u5BFC\u51FA\u5F15\u64CE\u672A\u5C31\u7EEA\uFF1A${String(cause)}`);
      }
    })();
    return () => {
      live = false;
      clipRef.current?.destroy();
      clipRef.current = null;
    };
  }, [props.source]);
  (0, import_react7.useEffect)(() => {
    setMediaSrc(props.source);
  }, [props.source]);
  (0, import_react7.useEffect)(() => {
    if (props.look === void 0 || props.look === null || props.look === "") return;
    setLookId(props.look);
  }, [props.look, props.source]);
  const applySeqTime = (0, import_react7.useCallback)((seqTime, play = false) => {
    const total = sequenceDuration(clips);
    const clamped = Math.min(Math.max(0, seqTime), Math.max(0, total));
    setTime(clamped);
    const hit = hitTest(clips, clamped);
    const video = videoRef.current;
    if (hit === void 0 || video === null) return;
    video.playbackRate = hit.clip.speed;
    video.muted = hit.clip.muted || !exportAudio;
    if (Math.abs(video.currentTime - hit.sourceTime) > 0.05) video.currentTime = hit.sourceTime;
    if (play && video.paused) void video.play();
  }, [clips, exportAudio]);
  const seek = (0, import_react7.useCallback)((seqTime) => {
    applySeqTime(seqTime, false);
  }, [applySeqTime]);
  const togglePlay = (0, import_react7.useCallback)(() => {
    const video = videoRef.current;
    if (video === null) return;
    if (playing) {
      video.pause();
      playRef.current = void 0;
      setPlaying(false);
      return;
    }
    playRef.current = { origin: performance.now(), seq: time };
    setPlaying(true);
    applySeqTime(time, true);
  }, [applySeqTime, playing, time]);
  (0, import_react7.useEffect)(() => {
    if (!playing) return;
    let frame2 = 0;
    const tick = (now2) => {
      const mark = playRef.current;
      if (mark === void 0) return;
      let next = mark.seq + (now2 - mark.origin) / 1e3;
      const total = sequenceDuration(clips);
      const hit = hitTest(clips, Math.min(next, total));
      if (loopSelected && selected2 !== void 0 && hit?.clip.id === selected2 && hit.local >= clipPlayDuration(hit.clip) - 0.04) {
        next = hit.start;
        playRef.current = { origin: now2, seq: next };
      } else if (next >= total - 0.02) {
        videoRef.current?.pause();
        playRef.current = void 0;
        setPlaying(false);
        setTime(total);
        return;
      }
      applySeqTime(next, true);
      frame2 = requestAnimationFrame(tick);
    };
    frame2 = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(frame2);
  }, [applySeqTime, clips, loopSelected, playing, selected2]);
  const split = (0, import_react7.useCallback)(() => {
    commitClips((current) => splitAt(current, time, nextClipId(current)));
  }, [commitClips, time]);
  const remove2 = (0, import_react7.useCallback)(() => {
    if (selected2 === void 0) return;
    commitClips((current) => removeClip(current, selected2));
    setSelected(void 0);
  }, [commitClips, selected2]);
  const duplicate = (0, import_react7.useCallback)(() => {
    if (selected2 === void 0) return;
    commitClips((current) => duplicateClip(current, selected2, nextClipId(current)));
  }, [commitClips, selected2]);
  const applyLook = (0, import_react7.useCallback)(async () => {
    if (lookId === void 0) return;
    setGradeBusy(true);
    setError(void 0);
    try {
      const response = await fetch(withProject("/directorx/studio"), {
        method: "POST",
        headers: { "content-type": "application/json", ...projectHeaders() },
        body: JSON.stringify({
          path: props.name,
          look: lookId,
          kind: "video",
          ...props.nodeId !== void 0 ? { nodeId: props.nodeId } : {}
        })
      });
      const body = await response.json();
      if (!response.ok || body.ok === false) throw new Error(body.message ?? `\u8C03\u8272\u5931\u8D25 (${response.status})`);
      if (typeof body.path === "string" && body.path !== "") {
        const blob = await fetch(body.path.startsWith("http") ? body.path : withProject(`/directorx/media?path=${encodeURIComponent(body.path)}`)).then((item3) => item3.blob());
        const next = URL.createObjectURL(blob);
        setMediaSrc((current) => {
          if (current.startsWith("blob:")) URL.revokeObjectURL(current);
          return next;
        });
        const clip2 = new I3(blob.stream());
        await clip2.ready;
        clipRef.current?.destroy();
        clipRef.current = clip2;
        setLookId(void 0);
      }
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    } finally {
      setGradeBusy(false);
    }
  }, [lookId, props]);
  const exportMp4 = (0, import_react7.useCallback)(async (mode2 = "canvas") => {
    const source = clipRef.current;
    if (source === null || clips.length === 0) return;
    setBusy(true);
    setError(void 0);
    setProgress(void 0);
    try {
      const out = exportSize(size.width, size.height, exportScale);
      if (!await xe.isSupported({ width: out.width, height: out.height })) {
        throw new Error("\u5F53\u524D\u6D4F\u89C8\u5668\u4E0D\u652F\u6301 WebCodecs \u5BFC\u51FA\uFF08\u9700\u8981 Chrome/Edge\uFF09");
      }
      const combinator = new xe({
        width: out.width,
        height: out.height,
        bitrate: exportBitrate(exportQuality),
        fps: exportFps,
        ...exportAudio && !clips.every((clip2) => clip2.muted) ? {} : { audio: false }
      });
      let offset = 0;
      const fadePad = transition2 === "fade" ? 0.3 : 0;
      for (const clip2 of clips) {
        const span = Math.max(0.05, clip2.sourceOut - clip2.sourceIn);
        const play = clipPlayDuration(clip2);
        const cloned = await source.clone();
        const cutIn = Math.round(clip2.sourceIn * 1e6);
        let piece = cloned;
        if (cutIn > 0) {
          const [head2, tail] = await cloned.split(cutIn);
          head2.destroy();
          piece = tail;
        }
        const cutLen = Math.round(span * 1e6);
        if (cutLen > 0 && cutLen < piece.meta.duration) {
          const [keep, rest] = await piece.split(cutLen);
          rest.destroy();
          piece = keep;
        }
        const sprite = new ht(piece);
        sprite.rect.w = out.width;
        sprite.rect.h = out.height;
        sprite.time = {
          offset: Math.round(offset * 1e6),
          duration: Math.round(play * 1e6),
          playbackRate: clip2.speed
        };
        const fadeIn = Math.max(clip2.fadeIn, fadePad);
        const fadeOut = Math.max(clip2.fadeOut, fadePad);
        if (fadeIn > 0 || fadeOut > 0) {
          const inPct = Math.min(40, fadeIn / play * 100);
          const outPct = Math.max(inPct + 10, 100 - Math.min(40, fadeOut / play * 100));
          sprite.setAnimation({
            "0%": { opacity: fadeIn > 0 ? 0 : 1 },
            [`${Math.round(inPct)}%`]: { opacity: 1 },
            [`${Math.round(outPct)}%`]: { opacity: 1 },
            "100%": { opacity: fadeOut > 0 ? 0 : 1 }
          }, { duration: Math.round(play * 1e6) });
        }
        await combinator.addSprite(sprite);
        offset += play;
      }
      combinator.on("OutputProgress", (value) => setProgress(value));
      const blob = await new Response(combinator.output()).blob();
      if (mode2 === "file") {
        const url = URL.createObjectURL(blob);
        const link2 = document.createElement("a");
        const stem = (props.name.split("/").pop() ?? "edit").replace(/\.[^.]+$/, "");
        link2.href = url;
        link2.download = `${stem}-cut.mp4`;
        link2.click();
        window.setTimeout(() => URL.revokeObjectURL(url), 4e3);
      } else {
        props.onExport(blob, "video/mp4");
      }
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    } finally {
      setBusy(false);
    }
  }, [clips, exportAudio, exportFps, exportQuality, exportScale, props, size.height, size.width, transition2]);
  (0, import_react7.useEffect)(() => {
    const onKey = (event) => {
      const target = event.target;
      const typing = typeof target?.matches === "function" && target.matches("input, textarea");
      if (typing) return;
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "z") {
        event.preventDefault();
        if (event.shiftKey) redo();
        else undo();
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "s") {
        event.preventDefault();
        void exportMp4("canvas");
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "d") {
        event.preventDefault();
        duplicate();
        return;
      }
      if (event.key === "Escape") {
        event.preventDefault();
        props.onClose();
        return;
      }
      if (event.key === " ") {
        event.preventDefault();
        togglePlay();
      }
      if (event.key.toLowerCase() === "s" && !event.metaKey) split();
      if (event.key === "Delete" || event.key === "Backspace") remove2();
      if (event.key === "ArrowLeft") seek(time - (event.shiftKey ? 1 : 1 / 30));
      if (event.key === "ArrowRight") seek(time + (event.shiftKey ? 1 : 1 / 30));
      if (event.key === "[") commitClips((current) => moveClip(current, selected2 ?? -1, -1));
      if (event.key === "]") commitClips((current) => moveClip(current, selected2 ?? -1, 1));
      if (event.key === "=" || event.key === "+") setPxPerSec((value) => Math.min(240, value + 20));
      if (event.key === "-") setPxPerSec((value) => Math.max(32, value - 20));
      if (event.key.toLowerCase() === "i" && selectedClip !== void 0) {
        const hit = hitTest(clips, time);
        if (hit !== void 0) commitClips((current) => trimClip(current, selectedClip.id, "in", hit.sourceTime, sourceDuration));
      }
      if (event.key.toLowerCase() === "o" && selectedClip !== void 0) {
        const hit = hitTest(clips, time);
        if (hit !== void 0) commitClips((current) => trimClip(current, selectedClip.id, "out", hit.sourceTime, sourceDuration));
      }
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  });
  const seqAt = (event) => {
    const track = trackRef.current;
    if (track === null) return 0;
    const rect = track.getBoundingClientRect();
    const x3 = event.clientX - rect.left + track.scrollLeft - 56;
    return Math.max(0, x3 / pxPerSec);
  };
  const filename = props.name.split("/").pop() ?? props.name;
  const kept = seqLen;
  const outSize = exportSize(size.width, size.height, exportScale);
  const timelineWidth = Math.max(640, seqLen * pxPerSec + 80);
  const step = tickStep(pxPerSec);
  return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
    StudioShell,
    {
      title: "\u65F6\u95F4\u7EBF",
      filename,
      meta: `${fmtClock(time)} / ${fmtClock(seqLen)} \xB7 ${clips.length} \u6BB5 \xB7 ${size.width}\xD7${size.height}`,
      error,
      saveLabel: busy ? `\u5BFC\u51FA\u4E2D\u2026${progress !== void 0 ? ` ${Math.round(progress * 100)}%` : ""}` : "\u4FDD\u5B58\u5230\u753B\u5E03",
      saveDisabled: busy || clips.length === 0 || !exportReady,
      onSave: () => {
        void exportMp4("canvas");
      },
      onClose: props.onClose,
      canUndo,
      canRedo,
      onUndo: undo,
      onRedo: redo,
      tools: [
        { id: "cut", label: "\u526A\u8F91", icon: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconScissors, { size: 15 }) },
        { id: "grade", label: "\u8C03\u8272", icon: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconSliders, { size: 15 }) },
        { id: "export", label: "\u5BFC\u51FA", icon: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconDownload, { size: 15 }) }
      ],
      tool: mode,
      onTool: (id2) => setMode(id2 === "grade" ? "grade" : id2 === "export" ? "export" : "cut"),
      inspector: mode === "grade" ? /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { fontSize: 11, letterSpacing: 0.3, textTransform: "uppercase", color: dx.mute, marginBottom: 12 }, children: "\u8272\u8C03" }),
        GRADE_FAMILIES.map((family) => /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { marginBottom: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { fontSize: 10, color: dx.mute, marginBottom: 4 }, children: family.label }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: looksByFamily(family.id).map((item3) => /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
            "button",
            {
              className: "dx-hit",
              title: item3.source,
              style: { ...studioBtn, background: lookId === item3.id ? "rgba(255,255,255,.14)" : studioBtn.background },
              onClick: () => setLookId((current) => current === item3.id ? void 0 : item3.id),
              children: item3.label
            },
            item3.id
          )) })
        ] }, family.id)),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, disabled: lookId === void 0 || gradeBusy, onClick: () => {
          void applyLook();
        }, children: gradeBusy ? "\u8C03\u8272\u4E2D\u2026" : "\u5E94\u7528\u5230\u9884\u89C8" }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { marginTop: 10, fontSize: 11, color: dx.dim, lineHeight: 1.5 }, children: "\u8C03\u8272\u53EA\u6539\u9884\u89C8\uFF0C\u786E\u8BA4\u540E\u5230\u300C\u5BFC\u51FA\u300D\u4FDD\u5B58\u6216\u4E0B\u8F7D\u3002" })
      ] }) : mode === "export" ? /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: "\u5206\u8FA8\u7387", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: [
          ["orig", "\u539F\u59CB"],
          ["1080", "1080p"],
          ["720", "720p"],
          ["480", "480p"]
        ].map(([id2, label2]) => /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: { ...studioBtn, background: exportScale === id2 ? "rgba(255,255,255,.14)" : studioBtn.background }, onClick: () => setExportScale(id2), children: label2 }, id2)) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: "\u753B\u8D28", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: [
          ["high", "\u9AD8"],
          ["standard", "\u6807\u51C6"],
          ["draft", "\u8349\u7A3F"]
        ].map(([id2, label2]) => /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: { ...studioBtn, background: exportQuality === id2 ? "rgba(255,255,255,.14)" : studioBtn.background }, onClick: () => setExportQuality(id2), children: label2 }, id2)) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: "\u5E27\u7387", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { display: "flex", gap: 6 }, children: [24, 30].map((fps) => /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("button", { className: "dx-hit", style: { ...studioBtn, background: exportFps === fps ? "rgba(255,255,255,.14)" : studioBtn.background }, onClick: () => setExportFps(fps), children: [
          fps,
          " fps"
        ] }, fps)) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: "\u8F6C\u573A", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { display: "flex", gap: 6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: { ...studioBtn, background: transition2 === "cut" ? "rgba(255,255,255,.14)" : studioBtn.background }, onClick: () => setTransition("cut"), children: "\u786C\u5207" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: { ...studioBtn, background: transition2 === "fade" ? "rgba(255,255,255,.14)" : studioBtn.background }, onClick: () => setTransition("fade"), children: "\u53E0\u5316" })
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("label", { style: { display: "flex", alignItems: "center", gap: 8, fontSize: 12, color: dx.mute, marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("input", { type: "checkbox", checked: exportAudio, onChange: (event) => setExportAudio(event.target.checked) }),
          "\u5BFC\u51FA\u97F3\u8F68"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { fontSize: 11, color: dx.dim, marginBottom: 12 }, children: [
          outSize.width,
          "\xD7",
          outSize.height,
          " \xB7 ",
          fmtClock(kept)
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, disabled: busy || clips.length === 0, onClick: () => {
            void exportMp4("canvas");
          }, children: "\u4FDD\u5B58\u5230\u753B\u5E03" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, disabled: busy || clips.length === 0, onClick: () => {
            void exportMp4("file");
          }, children: "\u5BFC\u51FA\u6587\u4EF6" })
        ] }),
        !exportReady ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { marginTop: 12, fontSize: 11, color: dx.mute }, children: "\u9884\u89C8\u53EF\u7528\u3002\u5BFC\u51FA\u5F15\u64CE\u4ECD\u5728\u51C6\u5907\u3002" }) : null
      ] }) : /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { fontSize: 11, letterSpacing: 0.3, textTransform: "uppercase", color: dx.mute, marginBottom: 12 }, children: "\u526A\u8F91" }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("button", { className: "dx-hit", style: studioBtn, onClick: () => seek(Math.max(0, time - 1)), children: [
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconSkipBack, { size: 13 }),
            " -1s"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: togglePlay, children: playing ? "\u6682\u505C" : "\u64AD\u653E" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("button", { className: "dx-hit", style: studioBtn, onClick: () => seek(time + 1), children: [
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconSkipFwd, { size: 13 }),
            " +1s"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: split, children: "\u5206\u5272" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, disabled: selected2 === void 0, onClick: duplicate, children: "\u590D\u5236" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("button", { className: "dx-hit", style: studioBtn, disabled: selected2 === void 0, onClick: remove2, children: [
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconTrash, { size: 13 }),
            "\u5220\u9664"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: "\u6210\u7247\u65F6\u957F", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { color: dx.ink }, children: [
          fmtClock(kept),
          " \xB7 \u6E90 ",
          fmtClock(sourceDuration)
        ] }) }),
        selectedClip !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: "\u5165\u70B9 / \u51FA\u70B9", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { color: dx.ink }, children: [
            fmtClock(selectedClip.sourceIn),
            " \u2192 ",
            fmtClock(selectedClip.sourceOut)
          ] }) }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: "\u901F\u5EA6", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: SPEEDS.map((speed) => /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(
            "button",
            {
              className: "dx-hit",
              style: { ...studioBtn, background: selectedClip.speed === speed ? "rgba(255,255,255,.14)" : studioBtn.background },
              onClick: () => commitClips((current) => patchClip(current, selectedClip.id, { speed })),
              children: [
                speed,
                "\xD7"
              ]
            },
            speed
          )) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: `\u6DE1\u5165 ${selectedClip.fadeIn.toFixed(1)}s`, children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("input", { type: "range", min: 0, max: 2, step: 0.1, value: selectedClip.fadeIn, onChange: (event) => commitClips((current) => patchClip(current, selectedClip.id, { fadeIn: Number(event.target.value) })), style: studioRange() }) }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(StudioField, { label: `\u6DE1\u51FA ${selectedClip.fadeOut.toFixed(1)}s`, children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("input", { type: "range", min: 0, max: 2, step: 0.1, value: selectedClip.fadeOut, onChange: (event) => commitClips((current) => patchClip(current, selectedClip.id, { fadeOut: Number(event.target.value) })), style: studioRange() }) }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 12 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: () => commitClips((current) => moveClip(current, selectedClip.id, -1)), children: "\u5DE6\u79FB" }),
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: () => commitClips((current) => moveClip(current, selectedClip.id, 1)), children: "\u53F3\u79FB" }),
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: () => commitClips((current) => patchClip(current, selectedClip.id, { muted: !selectedClip.muted })), children: selectedClip.muted ? "\u53D6\u6D88\u9759\u97F3" : "\u9759\u97F3" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("label", { style: { display: "flex", alignItems: "center", gap: 8, fontSize: 12, color: dx.mute }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("input", { type: "checkbox", checked: loopSelected, onChange: (event) => setLoopSelected(event.target.checked) }),
            "\u5FAA\u73AF\u5F53\u524D\u7247\u6BB5"
          ] })
        ] }) : /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { fontSize: 12, color: dx.dim, lineHeight: 1.6 }, children: "\u7A7A\u683C\u64AD\u653E\uFF0CS \u5206\u5272\uFF0CI / O \u8BBE\u5165\u51FA\u70B9\uFF0C[ ] \u8C03\u5E8F\uFF0CDelete \u5220\u9664\u3002\u6EDA\u8F6E\u7F29\u653E\u65F6\u95F4\u7EBF\u3002" })
      ] }),
      footer: /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { className: "dx-timeline", style: { borderTop: `1px solid ${dx.hairline}`, background: "#0b0b0b" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "8px 12px 6px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: { ...studioBtn, width: 32, height: 28, padding: 0 }, onClick: togglePlay, title: "\u64AD\u653E", children: playing ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconPause, { size: 13 }) : /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconPlay, { size: 13 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("span", { style: { fontSize: 11, color: dx.mute, fontVariantNumeric: "tabular-nums", minWidth: 92 }, children: [
            fmtClock(time),
            " / ",
            fmtClock(seqLen)
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, onClick: split, children: "\u5206\u5272" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("button", { className: "dx-hit", style: studioBtn, disabled: selected2 === void 0, onClick: remove2, children: "\u5220\u9664" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { style: { flex: 1 } }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { style: { fontSize: 11, color: dx.dim }, children: "\u7F29\u653E" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
            "input",
            {
              type: "range",
              min: 32,
              max: 240,
              value: pxPerSec,
              onChange: (event) => setPxPerSec(Number(event.target.value)),
              style: { width: 100, accentColor: "#f3f3f3" }
            }
          )
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
          "div",
          {
            ref: trackRef,
            className: "dx-timeline-board",
            onWheel: (event) => {
              if (!event.ctrlKey && !event.metaKey) return;
              event.preventDefault();
              setPxPerSec((value) => Math.min(240, Math.max(32, value + (event.deltaY > 0 ? -16 : 16))));
            },
            onPointerDown: (event) => {
              if (event.target.closest("[data-clip]")) return;
              const at2 = seqAt(event);
              seek(at2);
              setDrag({ kind: "head" });
              event.currentTarget.setPointerCapture(event.pointerId);
            },
            onPointerMove: (event) => {
              if (drag === void 0) return;
              const at2 = seqAt(event);
              if (drag.kind === "head") {
                seek(at2);
                return;
              }
              const origin = drag.origin;
              const clip2 = origin.find((item3) => item3.id === drag.id);
              if (clip2 === void 0) return;
              const index2 = origin.findIndex((item3) => item3.id === drag.id);
              const start2 = clipStarts(origin)[index2] ?? 0;
              const play = clipPlayDuration(clip2);
              if (drag.kind === "in") {
                const nextPlay = Math.max(0.05, start2 + play - at2);
                setClips(trimClip(origin, clip2.id, "in", clip2.sourceOut - nextPlay * clip2.speed, sourceDuration));
                return;
              }
              if (drag.kind === "out") {
                const nextPlay = Math.max(0.05, at2 - start2);
                setClips(trimClip(origin, clip2.id, "out", clip2.sourceIn + nextPlay * clip2.speed, sourceDuration));
                return;
              }
              const over = hitTest(origin, at2)?.index ?? index2;
              setClips(moveTo(origin, clip2.id, over));
            },
            onPointerUp: () => {
              if (drag !== void 0 && drag.kind !== "head") {
                const origin = drag.origin;
                setClips((current) => {
                  if (current === origin) return current;
                  historyRef.current.push(origin);
                  if (historyRef.current.length > 40) historyRef.current.shift();
                  redoRef.current = [];
                  syncStacks(historyRef.current, redoRef.current);
                  return current;
                });
              }
              setDrag(void 0);
            },
            style: { overflowX: "auto", padding: "0 12px 12px", touchAction: "none" },
            children: /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { position: "relative", width: timelineWidth, minHeight: 148 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { height: 22, marginLeft: 56, position: "relative", borderBottom: `1px solid ${dx.hairline}` }, children: Array.from({ length: Math.ceil(Math.max(seqLen, 1) / step) + 1 }, (_3, index2) => {
                const mark = index2 * step;
                return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { style: { position: "absolute", left: mark * pxPerSec, top: 4, fontSize: 10, color: dx.dim, fontVariantNumeric: "tabular-nums" }, children: fmtClock(mark) }, mark);
              }) }),
              ["V1", "A1"].map((lane, laneIndex) => /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { display: "flex", alignItems: "stretch", height: laneIndex === 0 ? 64 : 36, marginTop: 8 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { width: 56, flexShrink: 0, fontSize: 10, color: dx.mute, display: "grid", placeItems: "center", borderRight: `1px solid ${dx.hairline}` }, children: lane }),
                /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: { position: "relative", flex: 1, borderRadius: 8, background: "rgba(255,255,255,.03)", border: `1px solid ${dx.hairline}` }, children: clips.map((clip2, index2) => {
                  const start2 = starts[index2] ?? 0;
                  const width = Math.max(8, clipPlayDuration(clip2) * pxPerSec);
                  const left = start2 * pxPerSec;
                  const active = selected2 === clip2.id;
                  if (laneIndex === 1) {
                    return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
                      "div",
                      {
                        style: {
                          position: "absolute",
                          top: 6,
                          bottom: 6,
                          left,
                          width,
                          borderRadius: 4,
                          background: clip2.muted ? "repeating-linear-gradient(135deg, rgba(255,255,255,.05) 0 6px, rgba(255,255,255,.02) 6px 12px)" : "rgba(120,180,140,.28)"
                        }
                      },
                      `a-${clip2.id}`
                    );
                  }
                  return /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(
                    "div",
                    {
                      "data-clip": "",
                      onPointerDown: (event) => {
                        event.stopPropagation();
                        setSelected(clip2.id);
                        seek(start2);
                        setDrag({ kind: "move", id: clip2.id, origin: clips });
                        trackRef.current?.setPointerCapture(event.pointerId);
                      },
                      style: {
                        position: "absolute",
                        top: 6,
                        bottom: 6,
                        left,
                        width,
                        borderRadius: 8,
                        background: active ? "rgba(243,228,196,.22)" : "rgba(255,255,255,.10)",
                        border: `1px solid ${active ? "rgba(255,236,210,.7)" : "rgba(255,255,255,.12)"}`,
                        boxSizing: "border-box",
                        overflow: "hidden",
                        cursor: "grab"
                      },
                      children: [
                        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("span", { style: { position: "absolute", left: 10, top: 6, fontSize: 10, color: dx.ink, fontWeight: 600 }, children: [
                          "\u7247\u6BB5",
                          index2 + 1
                        ] }),
                        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("span", { style: { position: "absolute", left: 10, bottom: 6, fontSize: 10, color: dx.mute }, children: [
                          fmtClock(clipPlayDuration(clip2)),
                          clip2.speed !== 1 ? ` \xB7 ${clip2.speed}\xD7` : ""
                        ] }),
                        active ? /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
                          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
                            "span",
                            {
                              "data-handle": "in",
                              onPointerDown: (event) => {
                                event.stopPropagation();
                                setDrag({ kind: "in", id: clip2.id, origin: clips });
                                trackRef.current?.setPointerCapture(event.pointerId);
                              },
                              style: { position: "absolute", left: 0, top: 4, bottom: 4, width: 8, borderRadius: 99, background: "#f3f3f3", cursor: "ew-resize" }
                            }
                          ),
                          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
                            "span",
                            {
                              "data-handle": "out",
                              onPointerDown: (event) => {
                                event.stopPropagation();
                                setDrag({ kind: "out", id: clip2.id, origin: clips });
                                trackRef.current?.setPointerCapture(event.pointerId);
                              },
                              style: { position: "absolute", right: 0, top: 4, bottom: 4, width: 8, borderRadius: 99, background: "#f3f3f3", cursor: "ew-resize" }
                            }
                          )
                        ] }) : null
                      ]
                    },
                    clip2.id
                  );
                }) })
              ] }, lane)),
              seqLen > 0 ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { style: {
                position: "absolute",
                top: 0,
                bottom: 0,
                left: 56 + time * pxPerSec,
                width: 2,
                marginLeft: -1,
                background: "#f3efe4",
                pointerEvents: "none",
                boxShadow: "0 0 0 1px rgba(0,0,0,.45)"
              }, children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { style: {
                position: "absolute",
                top: 0,
                left: 4,
                padding: "1px 5px",
                borderRadius: 4,
                background: "rgba(12,12,12,.82)",
                color: "#f3f3f3",
                fontSize: 10,
                whiteSpace: "nowrap"
              }, children: fmtClock(time) }) }) : null
            ] })
          }
        )
      ] }),
      children: /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { style: { flex: 1, minHeight: 0, display: "grid", placeItems: "center", padding: 16, background: "#050505", position: "relative" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
          "button",
          {
            className: "dx-hit",
            onClick: togglePlay,
            title: playing ? "\u6682\u505C" : "\u64AD\u653E",
            style: {
              position: "absolute",
              left: 20,
              bottom: 20,
              zIndex: 2,
              ...studioBtn,
              width: 36,
              height: 36,
              padding: 0
            },
            children: playing ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconPause, { size: 15 }) : /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(IconPlay, { size: 15 })
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
          "video",
          {
            ref: videoRef,
            src: mediaSrc2,
            preload: "auto",
            onPlay: () => setPlaying(true),
            onPause: () => {
              if (playRef.current !== void 0) return;
              setPlaying(false);
            },
            onLoadedMetadata: (event) => {
              const media = event.currentTarget;
              const next = media.duration;
              const width = media.videoWidth;
              const height = media.videoHeight;
              if (Number.isFinite(next) && next > 0) {
                setSourceDuration((current) => current > 0 ? current : next);
                setSize((current) => current.width === 1280 ? { width: width || current.width, height: height || current.height } : current);
                setClips((current) => current.length === 0 ? fromSource(next) : current);
              }
            },
            style: { width: "100%", height: "100%", maxHeight: "100%", objectFit: "contain", background: "#000", borderRadius: 12, filter: LOOKS2.find((item3) => item3.id === lookId)?.css ?? "none" }
          }
        )
      ] })
    }
  );
}

// src/canvas-generate.ts
function inferContinueKind(sourceKind) {
  return sourceKind === "image" || sourceKind === "video" ? "video" : "image";
}
function flowAbsolutePosition(node, byId, seen = /* @__PURE__ */ new Set()) {
  if (node.parentId === void 0 || node.parentId === "" || seen.has(node.id)) {
    return { x: node.position.x, y: node.position.y };
  }
  seen.add(node.id);
  const parent = byId.get(node.parentId);
  if (parent === void 0) return { x: node.position.x, y: node.position.y };
  const origin = flowAbsolutePosition(parent, byId, seen);
  return { x: origin.x + node.position.x, y: origin.y + node.position.y };
}
function portPoint(box2, side) {
  if (side === "left") return { x: box2.x, y: box2.y + box2.height / 2 };
  if (side === "right") return { x: box2.x + box2.width, y: box2.y + box2.height / 2 };
  if (side === "top") return { x: box2.x + box2.width / 2, y: box2.y };
  return { x: box2.x + box2.width / 2, y: box2.y + box2.height };
}
function handleToSide(handle2) {
  if (handle2 === "in") return "left";
  if (handle2 === "out") return "right";
  if (handle2 === "top") return "top";
  if (handle2 === "bottom") return "bottom";
  return void 0;
}
function sideToHandle(side) {
  if (side === "left") return "in";
  if (side === "right") return "out";
  return side;
}
function portsForHandles(source, target, sourceHandle, targetHandle) {
  const fallback = closestPorts(source, target);
  const sourceSide = handleToSide(sourceHandle) ?? fallback.sourceSide;
  const targetSide = handleToSide(targetHandle) ?? fallback.targetSide;
  const from = portPoint(source, sourceSide);
  const to = portPoint(target, targetSide);
  return { sourceSide, targetSide, sourceX: from.x, sourceY: from.y, targetX: to.x, targetY: to.y };
}
function gapAfter(start2, end, otherStart, otherEnd) {
  if (otherStart >= end) return otherStart - end;
  if (start2 >= otherEnd) return start2 - otherEnd;
  return void 0;
}
function closestPorts(source, target) {
  const dx2 = target.x + target.width / 2 - (source.x + source.width / 2);
  const dy = target.y + target.height / 2 - (source.y + source.height / 2);
  const gapX = gapAfter(source.x, source.x + source.width, target.x, target.x + target.width);
  const gapY = gapAfter(source.y, source.y + source.height, target.y, target.y + target.height);
  const horizontal = gapX !== void 0 && gapY !== void 0 ? gapX <= gapY : gapX !== void 0 ? true : gapY !== void 0 ? false : Math.abs(dx2) >= Math.abs(dy);
  const sourceSide = horizontal ? dx2 >= 0 ? "right" : "left" : dy >= 0 ? "bottom" : "top";
  const targetSide = horizontal ? dx2 >= 0 ? "left" : "right" : dy >= 0 ? "top" : "bottom";
  const from = portPoint(source, sourceSide);
  const to = portPoint(target, targetSide);
  return {
    sourceSide,
    targetSide,
    sourceX: from.x,
    sourceY: from.y,
    targetX: to.x,
    targetY: to.y
  };
}
function routeDisplayPorts(source, target, sourceHandle, targetHandle) {
  const sourceSide = handleToSide(sourceHandle);
  const targetSide = handleToSide(targetHandle);
  const explicit = sourceSide === "top" || sourceSide === "bottom" || targetSide === "top" || targetSide === "bottom";
  if (explicit) return portsForHandles(source, target, sourceHandle, targetHandle);
  return closestPorts(source, target);
}
var TITLE_CLEAR = 34;
var STACK_GAP = 18;
var GROUP_INSET_X = 28;
var GROUP_INSET_TOP = 52;
var GROUP_INSET_BOT = 36;
function fallbackCardSize(kind) {
  if (kind === "group") return { width: 640, height: 460 };
  if (kind === "text") return { width: 250, height: 180 };
  return { width: 400, height: 220 };
}
function stackBox(node) {
  const size = fallbackCardSize(node.kind);
  return {
    x: node.x,
    y: node.y,
    width: node.width ?? size.width,
    height: node.height ?? size.height
  };
}
function boxesOverlap(left, right) {
  return left.x < right.x + right.width && left.x + left.width > right.x && left.y < right.y + right.height && left.y + left.height > right.y;
}
function tidyOverlappingGroups(nodes) {
  const next = nodes.map((node) => ({ ...node }));
  for (const group of next.filter((node) => node.kind === "group")) {
    const members = next.filter((node) => node.parent === group.id);
    if (members.length === 0) continue;
    const boxes = members.map(stackBox);
    const frame2 = stackBox(group);
    const overflow = boxes.some(
      (box2) => box2.x < frame2.x - 1 || box2.y < frame2.y - 1 || box2.x + box2.width > frame2.x + frame2.width + 1 || box2.y + box2.height > frame2.y + frame2.height + 1
    );
    const collide = boxes.some((box2, index2) => boxes.some((other, otherIndex) => {
      if (otherIndex <= index2) return false;
      return boxesOverlap(
        { ...box2, height: box2.height + TITLE_CLEAR },
        { ...other, height: other.height + TITLE_CLEAR }
      );
    }));
    if (!overflow && !collide) continue;
    members.sort((left2, right) => (left2.shotIndex ?? 1e9) - (right.shotIndex ?? 1e9) || left2.y - right.y || left2.x - right.x);
    const cardWidth = Math.max(...members.map((member) => stackBox(member).width));
    let cursor = group.y + GROUP_INSET_TOP;
    const left = group.x + GROUP_INSET_X;
    for (const member of members) {
      const size = stackBox(member);
      member.x = left;
      member.y = cursor;
      cursor += size.height + TITLE_CLEAR + STACK_GAP;
    }
    group.width = Math.max(frame2.width, GROUP_INSET_X * 2 + cardWidth);
    group.height = Math.max(frame2.height, cursor - group.y - STACK_GAP + GROUP_INSET_BOT);
  }
  return next;
}

// src/client/stage/chrome.tsx
var import_react16 = require("react");

// src/card-label.ts
var PLACEHOLDERS = /* @__PURE__ */ new Set(["", "\u56FE\u7247", "\u89C6\u9891", "\u672A\u547D\u540D", "\u6587\u672C", "Image", "Video", "Untitled"]);
var FILE_EXT = /\.(png|jpe?g|webp|gif|mp4|webm|mov|mkv)$/i;
var DATE_STAMP = /-\d{4}-\d{2}(-\d{2})?(t\d{2}-\d{2}-\d{2}z?)?$/i;
var KEBAB = /^[a-z0-9]+(?:-[a-z0-9]+){3,}$/i;
function stemOf(text) {
  return text.replace(FILE_EXT, "");
}
function humanizeSlug(label2) {
  const stem = stemOf((label2 ?? "").trim());
  const cut = stem.replace(DATE_STAMP, "").replace(/[_]+/g, "-");
  const words = cut.split("-").filter((part) => part !== "" && !/^\d+$/.test(part));
  if (words.length < 2) return "";
  return words.join(" ").slice(0, 42);
}
function isAssetSlug(label2) {
  const text = (label2 ?? "").trim();
  if (PLACEHOLDERS.has(text)) return true;
  const stem = stemOf(text);
  if (DATE_STAMP.test(stem)) return true;
  if (KEBAB.test(stem) && !/[\u4e00-\u9fff]/.test(stem)) return true;
  if (stem !== text && !/[\u4e00-\u9fff]/.test(stem)) return true;
  return false;
}
function displayCardTitle(label2, prompt, shotIndex) {
  const raw = (label2 ?? "").trim();
  const stem = stemOf(raw);
  if (!isAssetSlug(raw) && stem !== "") return stem;
  const fromPrompt = (prompt ?? "").trim().split(/[\n。！？.!?]/)[0]?.trim() ?? "";
  if (fromPrompt !== "") return fromPrompt.slice(0, 36);
  if (shotIndex !== void 0) return `\u955C\u5934 ${String(shotIndex).padStart(2, "0")}`;
  return humanizeSlug(raw);
}
function nextCardLabel(current, incoming) {
  if (incoming === void 0 || incoming.trim() === "") return current;
  if (!isAssetSlug(current) && isAssetSlug(incoming)) return current;
  return incoming;
}

// src/canvas-kind.ts
var KINDS = /* @__PURE__ */ new Set(["image", "video", "audio", "text", "group", "director-stage", "edit"]);
function asKind(value) {
  return typeof value === "string" && KINDS.has(value) ? value : void 0;
}
function canvasNodeKind(input2) {
  const kind = asKind(input2.kind);
  if (kind !== void 0 && kind !== "text") return kind;
  const dataKind = asKind(input2.dataKind);
  if (dataKind !== void 0 && dataKind !== "text") return dataKind;
  const id2 = typeof input2.id === "string" ? input2.id : "";
  if (id2.startsWith("director-stage-")) return "director-stage";
  if (id2.startsWith("edit-")) return "edit";
  if (id2.startsWith("group-")) return "group";
  return kind ?? "text";
}

// src/client/stage/workstation.ts
var FRAME_ASPECTS = [
  { id: "16:9", w: 16, h: 9 },
  { id: "9:16", w: 9, h: 16 },
  { id: "1:1", w: 1, h: 1 },
  { id: "4:3", w: 4, h: 3 },
  { id: "3:4", w: 3, h: 4 },
  { id: "3:2", w: 3, h: 2 },
  { id: "2:3", w: 2, h: 3 },
  { id: "21:9", w: 21, h: 9 }
];
var IMAGE_MODELS = [
  { id: "doubao-seedream-4.5", label: "Seedream 4.5" },
  { id: "gpt-image-2", label: "GPT Image 2" },
  { id: "wan2.7-image", label: "Wan 2.7" },
  { id: "Qwen/Qwen-Image", label: "Qwen Image" }
];
var VIDEO_MODELS = [
  { id: "MiniMax-H3", label: "MiniMax H3" },
  { id: "kling-v3", label: "Kling 3" },
  { id: "wan2.7-i2v", label: "Wan I2V" },
  { id: "viduq3-pro", label: "Vidu Q3" }
];
var AUDIO_MODELS = [
  { id: "qwen3-tts-flash", label: "Qwen TTS" }
];
var VIDEO_DURATIONS = [4, 5, 6, 8, 10];
function aspectRatio(aspect) {
  const found = FRAME_ASPECTS.find((item3) => item3.id === aspect);
  return found === void 0 ? 16 / 9 : found.w / found.h;
}
function sizeFromAspect(aspect, width) {
  const ratio = aspectRatio(aspect);
  if (ratio < 1) {
    const height2 = Math.max(240, Math.min(380, Math.round(width * 0.95)));
    return { width: Math.max(160, Math.round(height2 * ratio)), height: height2 };
  }
  const height = Math.max(140, Math.min(380, Math.round(width / ratio)));
  return { width, height };
}
function specPrompt(spec) {
  const extras = [];
  if (spec.model !== void 0 && spec.model !== "") extras.push(`\u6A21\u578B: ${spec.model}`);
  if (spec.aspect !== void 0 && spec.aspect !== "") extras.push(`\u753B\u5E45: ${spec.aspect}`);
  if (spec.count !== void 0 && spec.count > 1) extras.push(`\u6B21\u6570: ${spec.count}`);
  if (spec.kind === "video" && spec.durationSec !== void 0) extras.push(`\u65F6\u957F: ${spec.durationSec}s`);
  if (spec.refIds !== void 0 && spec.refIds.length > 0) extras.push(`\u53C2\u8003\u8282\u70B9: ${spec.refIds.join(", ")}`);
  if (spec.characters !== void 0 && spec.characters.length > 0) extras.push(`\u89D2\u8272: ${spec.characters.join(", ")}`);
  return extras.length === 0 ? spec.prompt.trim() : `${spec.prompt.trim()}
${extras.join("\n")}`;
}
function modelsFor(kind) {
  if (kind === "video") return VIDEO_MODELS;
  if (kind === "audio") return AUDIO_MODELS;
  return IMAGE_MODELS;
}
function nearestAspect(width, height) {
  if (width <= 0 || height <= 0) return "16:9";
  const ratio = width / height;
  let best = "16:9";
  let delta = Number.POSITIVE_INFINITY;
  for (const item3 of FRAME_ASPECTS) {
    const gap = Math.abs(ratio - item3.w / item3.h);
    if (gap < delta) {
      delta = gap;
      best = item3.id;
    }
  }
  return best;
}
function incomingRefIds(nodeId, edges) {
  return [...new Set(edges.filter((edge) => edge.target === nodeId).map((edge) => edge.source))].slice(0, 8);
}

// src/client/stage/document.ts
var MEDIA_W = 400;
var MEDIA_H = 220;
var AUDIO_W = 400;
var AUDIO_H = 140;
var TEXT_W = 250;
var TEXT_H = 250;
var GROUP_W = 640;
var GROUP_H = 460;
var SHOT_STATUSES = ["idea", "approved", "generating", "review", "locked", "failed"];
var SHOT_STATUS_LABEL = {
  idea: "\u6784\u601D",
  approved: "\u5DF2\u6279",
  generating: "\u751F\u6210\u4E2D",
  review: "\u5BA1\u9605",
  locked: "\u9501\u5B9A",
  failed: "\u5931\u8D25"
};
var SHOT_STATUS_COLOR = {
  idea: "#8a8a8a",
  approved: "#8fdc9f",
  generating: "#f0c36a",
  review: "#9bbcff",
  locked: "#f5f5f5",
  failed: "#ff9b8f"
};
function cycleShotStatus(current) {
  const index2 = SHOT_STATUSES.indexOf(current);
  return SHOT_STATUSES[index2 < 0 ? 1 : (index2 + 1) % SHOT_STATUSES.length];
}
function asShotStatus(value) {
  return SHOT_STATUSES.includes(value) ? value : void 0;
}
function newId2(prefix) {
  return `${prefix}-${Math.random().toString(36).slice(2, 10)}`;
}
function defaultSize2(kind, aspect) {
  if (kind === "group") return { width: GROUP_W, height: GROUP_H };
  if (kind === "director-stage" || kind === "edit") return { width: 560, height: 360 };
  if (kind === "audio") return { width: AUDIO_W, height: AUDIO_H };
  if (kind === "image" || kind === "video" || kind === "media") {
    return aspect !== void 0 && aspect !== "" ? sizeFromAspect(aspect, MEDIA_W) : { width: MEDIA_W, height: MEDIA_H };
  }
  return { width: TEXT_W, height: TEXT_H };
}
function flowNodeKind(node) {
  return canvasNodeKind({
    kind: node.type === "media" ? node.data?.kind : node.type,
    id: node.id,
    dataKind: node.data?.kind
  });
}
function toFlowNodes(doc) {
  const nodes = tidyOverlappingGroups(doc.nodes);
  const byId = new Map(nodes.map((node) => [node.id, node]));
  return nodes.map((node) => {
    const isMedia = node.kind === "image" || node.kind === "video" || node.kind === "audio";
    const isGroup = node.kind === "group";
    const isDirectorStage = node.kind === "director-stage";
    const isEdit = node.kind === "edit";
    const parent = node.parent !== void 0 ? byId.get(node.parent) : void 0;
    const size = {
      width: node.width ?? defaultSize2(node.kind, node.aspect).width,
      height: node.height ?? defaultSize2(node.kind, node.aspect).height
    };
    return {
      id: node.id,
      type: isGroup ? "group" : isDirectorStage ? "director-stage" : isEdit ? "edit" : isMedia ? "media" : "text",
      draggable: node.locked !== true,
      position: parent !== void 0 ? { x: node.x - parent.x, y: node.y - parent.y } : { x: node.x, y: node.y },
      width: size.width,
      height: size.height,
      style: { ...size, overflow: "visible" },
      ...parent !== void 0 ? { parentId: parent.id, extent: "parent" } : {},
      data: {
        ...isMedia ? { kind: node.kind, path: node.path ?? "" } : {},
        ...isDirectorStage ? { kind: "director-stage" } : {},
        ...isEdit ? { kind: "edit" } : {},
        label: node.label,
        ...node.prompt !== void 0 ? { prompt: node.prompt } : {},
        ...node.shotIndex !== void 0 ? { shotIndex: node.shotIndex } : {},
        ...node.locked === true ? { locked: true } : {},
        ...node.shotStatus !== void 0 ? { shotStatus: node.shotStatus } : {},
        ...node.selectedTakeId !== void 0 ? { selectedTakeId: node.selectedTakeId } : {},
        ...node.aspect !== void 0 ? { aspect: node.aspect } : {},
        ...node.model !== void 0 ? { model: node.model } : {},
        ...node.durationSec !== void 0 ? { durationSec: node.durationSec } : {},
        ...node.lastError !== void 0 ? { lastError: node.lastError } : {},
        ...node.count !== void 0 ? { count: node.count } : {},
        ...node.characters !== void 0 && node.characters.length > 0 ? { characters: node.characters } : {},
        ...node.continuityRules !== void 0 && node.continuityRules.length > 0 ? { continuityRules: node.continuityRules } : {}
      }
    };
  });
}
function toFlowEdges(doc) {
  const ids = new Set(doc.nodes.map((node) => node.id));
  return doc.edges.flatMap((edge) => {
    if (!ids.has(edge.from) || !ids.has(edge.to)) return [];
    return [{
      id: edge.id,
      source: edge.from,
      target: edge.to,
      type: "wire",
      ...typeof edge.sourceHandle === "string" && edge.sourceHandle !== "" ? { sourceHandle: edge.sourceHandle } : {},
      ...typeof edge.targetHandle === "string" && edge.targetHandle !== "" ? { targetHandle: edge.targetHandle } : {},
      ...typeof edge.label === "string" && edge.label !== "" ? { label: edge.label } : {},
      ...edge.sourceVariantIdx !== void 0 ? { data: { sourceVariantIdx: edge.sourceVariantIdx } } : {}
    }];
  });
}
function fromFlow(nodes, edges, title, updatedAt) {
  const parentPos = new Map(nodes.map((node) => [node.id, node.position]));
  return {
    version: 1,
    updatedAt,
    ...title !== "" && title !== "\u672A\u547D\u540D\u753B\u5E03" ? { title } : {},
    nodes: nodes.map((node) => {
      const absolute = node.parentId !== void 0 && node.parentId !== "" ? { x: node.position.x + (parentPos.get(node.parentId)?.x ?? 0), y: node.position.y + (parentPos.get(node.parentId)?.y ?? 0) } : { x: node.position.x, y: node.position.y };
      const kind = flowNodeKind(node);
      const width = typeof node.style?.width === "number" ? node.style.width : void 0;
      const height = typeof node.style?.height === "number" ? node.style.height : void 0;
      return {
        id: node.id,
        kind,
        label: displayCardTitle(node.data.label, node.data.prompt, node.data.shotIndex) || node.data.label,
        ...kind === "image" || kind === "video" || kind === "audio" ? { path: node.data.path ?? "" } : {},
        ...node.data.prompt !== void 0 ? { prompt: node.data.prompt } : {},
        ...node.data.shotIndex !== void 0 ? { shotIndex: node.data.shotIndex } : {},
        ...node.data.locked === true ? { locked: true } : {},
        ...node.data.shotStatus !== void 0 ? { shotStatus: node.data.shotStatus } : {},
        ...node.data.selectedTakeId !== void 0 ? { selectedTakeId: node.data.selectedTakeId } : {},
        ...node.data.aspect !== void 0 ? { aspect: node.data.aspect } : {},
        ...node.data.model !== void 0 ? { model: node.data.model } : {},
        ...node.data.durationSec !== void 0 ? { durationSec: node.data.durationSec } : {},
        ...node.data.lastError !== void 0 ? { lastError: node.data.lastError } : {},
        ...node.data.count !== void 0 ? { count: node.data.count } : {},
        ...node.data.characters !== void 0 && node.data.characters.length > 0 ? { characters: node.data.characters } : {},
        ...node.data.continuityRules !== void 0 && node.data.continuityRules.length > 0 ? { continuityRules: node.data.continuityRules } : {},
        ...node.parentId !== void 0 && node.parentId !== "" ? { parent: node.parentId } : {},
        x: absolute.x,
        y: absolute.y,
        ...width !== void 0 ? { width } : {},
        ...height !== void 0 ? { height } : {}
      };
    }),
    edges: edges.map((edge) => {
      const variantIdx = edge.data?.sourceVariantIdx;
      return {
        id: edge.id,
        from: edge.source,
        to: edge.target,
        ...typeof edge.sourceHandle === "string" && edge.sourceHandle !== "" ? { sourceHandle: edge.sourceHandle } : {},
        ...typeof edge.targetHandle === "string" && edge.targetHandle !== "" ? { targetHandle: edge.targetHandle } : {},
        ...typeof edge.label === "string" && edge.label !== "" ? { label: edge.label } : {},
        ...variantIdx !== void 0 ? { sourceVariantIdx: variantIdx } : {}
      };
    })
  };
}

// src/client/stage/layout.ts
var SNAP_GRID = 16;
var GROUP_PAD = { x: 28, top: 48, bottom: 24 };
var CLIP_MARK = "directorx-canvas-clip";
function focusViewOptions(kind) {
  if (kind === "group") {
    return { padding: 0.16, duration: 280, maxZoom: 0.95, minZoom: 0.18 };
  }
  return { padding: 0.38, duration: 280, maxZoom: 1.2, minZoom: 0.22 };
}
function alignBoxes(boxes, kind) {
  if (boxes.length === 0) return [];
  const minX = Math.min(...boxes.map((box2) => box2.x));
  const maxX = Math.max(...boxes.map((box2) => box2.x + box2.w));
  const minY = Math.min(...boxes.map((box2) => box2.y));
  const maxY = Math.max(...boxes.map((box2) => box2.y + box2.h));
  const midX = (minX + maxX) / 2;
  const midY = (minY + maxY) / 2;
  return boxes.map((box2) => {
    if (kind === "left") return { id: box2.id, x: minX, y: box2.y };
    if (kind === "right") return { id: box2.id, x: maxX - box2.w, y: box2.y };
    if (kind === "center") return { id: box2.id, x: midX - box2.w / 2, y: box2.y };
    if (kind === "top") return { id: box2.id, x: box2.x, y: minY };
    if (kind === "bottom") return { id: box2.id, x: box2.x, y: maxY - box2.h };
    return { id: box2.id, x: box2.x, y: midY - box2.h / 2 };
  });
}
function distributeBoxes(boxes, axis) {
  if (boxes.length < 3) return boxes.map((box2) => ({ id: box2.id, x: box2.x, y: box2.y }));
  const sorted = [...boxes].sort((left, right) => axis === "x" ? left.x - right.x : left.y - right.y);
  const first = sorted[0];
  const last = sorted[sorted.length - 1];
  const span = axis === "x" ? last.x + last.w - first.x : last.y + last.h - first.y;
  const total = sorted.reduce((sum, box2) => sum + (axis === "x" ? box2.w : box2.h), 0);
  const gap = (span - total) / (sorted.length - 1);
  let cursor = axis === "x" ? first.x : first.y;
  return sorted.map((box2) => {
    const next = axis === "x" ? { id: box2.id, x: cursor, y: box2.y } : { id: box2.id, x: box2.x, y: cursor };
    cursor += (axis === "x" ? box2.w : box2.h) + gap;
    return next;
  });
}
function nudgeBoxes(boxes, dx2, dy) {
  return boxes.map((box2) => ({ id: box2.id, x: box2.x + dx2, y: box2.y + dy }));
}
function nudgeStep(snap, coarse) {
  if (snap) return coarse ? SNAP_GRID * 2 : SNAP_GRID;
  return coarse ? SNAP_GRID : SNAP_GRID / 2;
}
function groupFrame(members) {
  if (members.length === 0) return { x: 0, y: 0, w: 280, h: 200 };
  const minX = Math.min(...members.map((box2) => box2.x));
  const minY = Math.min(...members.map((box2) => box2.y));
  const maxX = Math.max(...members.map((box2) => box2.x + box2.w));
  const maxY = Math.max(...members.map((box2) => box2.y + box2.h));
  return {
    x: minX - GROUP_PAD.x,
    y: minY - GROUP_PAD.top,
    w: Math.max(280, maxX - minX + GROUP_PAD.x * 2),
    h: Math.max(200, maxY - minY + GROUP_PAD.top + GROUP_PAD.bottom)
  };
}
function readingOrder(nodes) {
  return [...nodes].sort((left, right) => {
    const leftShot = left.shotIndex;
    const rightShot = right.shotIndex;
    if (leftShot !== void 0 && rightShot !== void 0 && leftShot !== rightShot) return leftShot - rightShot;
    if (Math.abs(left.y - right.y) > 48) return left.y - right.y;
    if (left.x !== right.x) return left.x - right.x;
    return left.id.localeCompare(right.id);
  });
}
function packClip(nodes, edges) {
  if (nodes.length === 0) return void 0;
  const ids = new Set(nodes.map((node) => node.id));
  const indexOf = new Map(nodes.map((node, index2) => [node.id, index2]));
  const minX = Math.min(...nodes.map((node) => node.position.x));
  const minY = Math.min(...nodes.map((node) => node.position.y));
  return {
    mark: CLIP_MARK,
    nodes: nodes.map((node) => {
      const width = typeof node.style?.width === "number" ? node.style.width : void 0;
      const height = typeof node.style?.height === "number" ? node.style.height : void 0;
      const parent = node.parentId !== void 0 && node.parentId !== "" ? indexOf.get(node.parentId) : void 0;
      return {
        ...node.type !== void 0 ? { type: node.type } : {},
        ...node.data.kind !== void 0 ? { kind: node.data.kind } : {},
        label: node.data.label,
        ...node.data.path !== void 0 && node.data.path !== "" ? { path: node.data.path } : {},
        ...node.data.prompt !== void 0 ? { prompt: node.data.prompt } : {},
        ...node.data.shotStatus !== void 0 ? { shotStatus: node.data.shotStatus } : {},
        ...width !== void 0 ? { width } : {},
        ...height !== void 0 ? { height } : {},
        x: node.position.x - minX,
        y: node.position.y - minY,
        ...parent !== void 0 ? { parent } : {}
      };
    }),
    edges: edges.filter((edge) => ids.has(edge.source) && ids.has(edge.target)).map((edge) => ({
      from: indexOf.get(edge.source) ?? 0,
      to: indexOf.get(edge.target) ?? 0,
      ...typeof edge.label === "string" && edge.label !== "" ? { label: edge.label } : {}
    }))
  };
}
function asClipPayload(value) {
  if (value === null || typeof value !== "object") return void 0;
  const rec = value;
  if (rec.mark !== CLIP_MARK || !Array.isArray(rec.nodes)) return void 0;
  const nodes = [];
  for (const item3 of rec.nodes) {
    if (item3 === null || typeof item3 !== "object") continue;
    const node = item3;
    if (typeof node.label !== "string" || typeof node.x !== "number" || typeof node.y !== "number") continue;
    nodes.push({
      ...typeof node.type === "string" ? { type: node.type } : {},
      ...typeof node.kind === "string" ? { kind: node.kind } : {},
      label: node.label,
      ...typeof node.path === "string" ? { path: node.path } : {},
      ...typeof node.prompt === "string" ? { prompt: node.prompt } : {},
      ...typeof node.shotStatus === "string" ? { shotStatus: node.shotStatus } : {},
      ...typeof node.width === "number" ? { width: node.width } : {},
      ...typeof node.height === "number" ? { height: node.height } : {},
      x: node.x,
      y: node.y,
      ...typeof node.parent === "number" ? { parent: node.parent } : {}
    });
  }
  if (nodes.length === 0) return void 0;
  const edges = Array.isArray(rec.edges) ? rec.edges.flatMap((item3) => {
    if (item3 === null || typeof item3 !== "object") return [];
    const edge = item3;
    if (typeof edge.from !== "number" || typeof edge.to !== "number") return [];
    return [{
      from: edge.from,
      to: edge.to,
      ...typeof edge.label === "string" ? { label: edge.label } : {}
    }];
  }) : [];
  return { mark: CLIP_MARK, nodes, edges };
}
function clampMenu(x3, y2, width, height, view = { w: 1200, h: 800 }) {
  return {
    left: Math.max(12, Math.min(x3, view.w - width - 12)),
    top: Math.max(12, Math.min(y2, view.h - height - 12))
  };
}

// src/client/stage/menus.ts
var SECTION_LABEL = {
  create: "\u6DFB\u52A0\u8282\u70B9",
  import: "\u5BFC\u5165",
  primary: "",
  craft: "\u5DE5\u5177",
  edit: "\u7F16\u8F91",
  arrange: "\u6392\u5217",
  danger: ""
};
function addMenuRows(mode) {
  const create2 = [
    { id: "text", label: "\u6587\u672C", section: "create" },
    { id: "image", label: "\u56FE\u7247", section: "create" },
    { id: "video", label: "\u89C6\u9891", section: "create" },
    { id: "audio", label: "\u97F3\u9891", section: "create" },
    { id: "director-stage", label: "3D \u5BFC\u6F14\u53F0", section: "create" },
    { id: "edit", label: "\u526A\u8F91\u53F0", section: "create" }
  ];
  if (mode === "quick") return create2;
  return [
    ...create2,
    { id: "upload", label: "\u4E0A\u4F20", section: "import" },
    { id: "paste", label: "\u7C98\u8D34", section: "import", kbd: "\u2318V" }
  ];
}
function nodeMenuRows(surface) {
  if (surface.selectedCount > 1) return multiMenuRows(surface);
  if (surface.type === "group") {
    return [
      { id: "ungroup", label: "\u53D6\u6D88\u7F16\u7EC4", section: "edit" },
      { id: "delete", label: "\u5220\u9664", section: "danger", kbd: "\u232B", danger: true }
    ];
  }
  const rows = [];
  if (surface.type === "media" && surface.hasPath && surface.kind === "image") {
    rows.push(
      { id: "crop", label: "\u88C1\u526A", section: "primary" },
      { id: "redraw", label: "\u5C40\u90E8\u91CD\u7ED8", section: "primary" },
      { id: "split", label: "\u5FEB\u901F\u5207\u5206", section: "craft" }
    );
  }
  if (surface.type === "media" && surface.hasPath && surface.kind === "video") {
    rows.push(
      { id: "edit", label: "\u526A\u8F91", section: "primary" },
      { id: "frames", label: "\u622A\u5E27", section: "primary" },
      { id: "extend", label: "\u5EF6\u957F\u955C\u5934", section: "craft" },
      { id: "reshoot", label: "\u89C6\u9891\u91CD\u62CD", section: "craft" }
    );
  }
  if (surface.type === "director-stage" || surface.type === "edit") {
    rows.push({ id: "edit", label: surface.type === "edit" ? "\u6253\u5F00\u526A\u8F91" : "\u8FDB\u5165\u7247\u573A", section: "primary" });
  }
  if (surface.hasPath) rows.push({ id: "download", label: "\u4E0B\u8F7D", section: "edit" });
  if (surface.hasPath) rows.push({ id: "preview", label: "\u5168\u5C4F", section: "edit" });
  rows.push(
    { id: "duplicate", label: "\u590D\u5236", section: "edit", kbd: "\u2318D" },
    { id: "delete", label: "\u5220\u9664", section: "danger", kbd: "\u232B", danger: true }
  );
  return rows;
}
function multiMenuRows(surface = {}) {
  const rows = [
    { id: "group", label: "\u7F16\u7EC4", section: "arrange", kbd: "\u2318G" }
  ];
  if (surface.canUngroup === true) rows.push({ id: "ungroup", label: "\u53D6\u6D88\u7F16\u7EC4", section: "arrange" });
  rows.push({ id: "delete", label: "\u5220\u9664", section: "danger", kbd: "\u232B", danger: true });
  return rows;
}
function groupMenuRows(rows) {
  const order = ["create", "import", "primary", "craft", "edit", "arrange", "danger"];
  return order.flatMap((id2) => {
    const slice = rows.filter((row3) => row3.section === id2);
    if (slice.length === 0) return [];
    return [{ id: id2, label: SECTION_LABEL[id2], rows: slice }];
  });
}
function shouldNestCraft(rows) {
  return rows.filter((row3) => row3.section === "craft").length >= 3;
}

// src/client/stage/NodeWorkstation.tsx
var import_react9 = require("react");

// src/client/stage/ip-prompt.tsx
var import_react8 = require("react");

// src/ip-lexicon.ts
var RISK = {
  character: "\u89D2\u8272\u540D/\u5546\u6807\u53EF\u80FD\u53D7\u7248\u6743\u4FDD\u62A4\u3002\u6309\u89D2\u8272\u8F74\u5199\u5916\u5F62\u4E0E\u60C5\u5883\uFF0C\u4E0D\u8981\u70B9\u540D IP\u3002",
  brand: "\u54C1\u724C\u6216\u5DE5\u4F5C\u5BA4\u540D\u53EF\u80FD\u53D7\u5546\u6807\u4FDD\u62A4\u3002\u5199\u7A7A\u95F4\u4E0E\u6750\u8D28\uFF0C\u4E0D\u8981\u53EF\u8BC6\u522B\u5546\u6807\u3002",
  artist: "\u5728\u4E16\u4F5C\u8005/\u5DE5\u4F5C\u5BA4\u98CE\u683C\u70B9\u540D\u6709\u6A21\u4EFF\u98CE\u9669\u3002\u5199\u5149\u7EBF\u3001\u7B14\u89E6\u3001\u8272\u5F69\uFF0C\u4E0D\u8981\u4F5C\u8005\u540D\u3002",
  likeness: "\u771F\u4EBA\u59D3\u540D\u53EF\u80FD\u6D89\u53CA\u8096\u50CF\u6743\u3002\u5199\u4F53\u8C8C\u4E0E\u6C14\u8D28\uFF0C\u4E0D\u8981\u5199\u771F\u4EBA\u59D3\u540D\u3002",
  music: "\u66F2\u540D/\u6B4C\u624B\u70B9\u540D\u53EF\u80FD\u6D89\u53CA\u8BCD\u66F2\u4E0E\u5F55\u97F3\u6743\u3002\u5199\u914D\u5668\u4E0E\u60C5\u7EEA\uFF0C\u4E0D\u8981\u70B9\u66F2\u540D\u3002",
  combo: "\u672A\u70B9\u540D\u4E5F\u53EF\u80FD\u89E6\u53D1\u53D7\u4FDD\u62A4\u5F62\u8C61\uFF08\u5916\u5F62\u5173\u952E\u8BCD\u7EC4\u5408\uFF09\u3002\u6539\u6210\u66F4\u6CDB\u7684\u539F\u521B\u63CF\u8FF0\uFF0C\u5E76\u52A0\u8D1F\u5411\u6392\u9664\u3002"
};
var IP_AXES = {
  character: [
    "\u539F\u578B\uFF1A\u804C\u4E1A/\u7269\u79CD/\u5E74\u9F84\u6BB5\uFF0C\u4E0D\u7528\u539F\u540D",
    "\u4F53\u578B\u6BD4\u4F8B\u4E0E\u8FD0\u52A8\u65B9\u5F0F",
    "\u670D\u88C5\uFF1A\u526A\u88C1\u3001\u6750\u8D28\u3001\u8272\u5757\u5173\u7CFB\uFF1B\u4E0D\u8981\u590D\u523B\u53EF\u8BC6\u522B\u7ECF\u5178\u5957\u88C5\u526A\u5F71",
    "\u6807\u5FD7\u7269\u6539\u6210\u51E0\u4F55\u6216\u62BD\u8C61\u5F62\u72B6\uFF0C\u4E0D\u8981\u5546\u6807\u7EB9\u6837",
    "\u628A\u672C\u53E5\u5DF2\u6709\u7684\u52A8\u4F5C\u3001\u573A\u666F\u3001\u5149\u7EBF\u5199\u8FDB\u53BB"
  ],
  brand: [
    "\u7A7A\u95F4\u7C7B\u578B\u3001\u6750\u8D28\u3001\u8272\u6E29\u3001\u9648\u8BBE",
    "\u7981\u6B62\u53EF\u8BC6\u522B\u5546\u6807\u3001\u5B57\u6807\u3001\u5409\u7965\u7269\u526A\u5F71"
  ],
  artist: [
    "\u5149\u7EBF\u4E0E\u7A7A\u6C14\u611F",
    "\u7B14\u89E6\u6216\u5A92\u4ECB",
    "\u6784\u56FE\u4E60\u60EF",
    "\u8272\u5F69\u5173\u7CFB",
    "\u5386\u53F2\u65F6\u671F\u6216\u827A\u672F\u8FD0\u52A8\uFF0C\u4E0D\u8981\u4F5C\u8005\u540D/\u5DE5\u4F5C\u5BA4\u540D"
  ],
  likeness: [
    "\u5E74\u9F84\u6BB5\u3001\u4F53\u578B\u3001\u53D1\u578B\u7ED3\u6784\u3001\u795E\u6001\u3001\u670D\u88C5",
    "\u7981\u6B62\u771F\u4EBA\u59D3\u540D"
  ],
  music: [
    "\u914D\u5668\u3001\u8282\u594F\u3001\u8C03\u6027\u3001\u60C5\u7EEA",
    "\u7981\u6B62\u66F2\u540D\u4E0E\u6B4C\u624B"
  ],
  combo: [
    "\u62C6\u5F00\u5916\u5F62\u5173\u952E\u8BCD\uFF0C\u6539\u6210\u66F4\u6CDB\u7684\u539F\u521B\u7EC4\u5408",
    "\u8D1F\u5411\u6392\u9664\u5BF9\u5E94\u4E13\u540D",
    "\u4E0D\u8981\u7528\u66F4\u63A5\u8FD1\u539F\u4F5C\u7684\u7ECF\u5178\u526A\u5F71\u8865\u4E0A"
  ]
};
var IP_METHOD = {
  name: "genericization+negative",
  sources: [
    "Nature PREGen genericization (s41598-025-90827-1)\uFF1A\u63CF\u8FF0\u5C5E\u6027\uFF0C\u4E0D\u70B9\u540D\u8EAB\u4EFD",
    "arXiv Safer Prompts (2505.03338)\uFF1A\u53BB\u4E13\u540D\uFF0C\u951A\u5B9A\u827A\u672F\u8FD0\u52A8/\u65F6\u671F/\u901A\u7528\u6280\u6CD5",
    "arXiv Fantastic Copyrighted Beasts (2406.14526)\uFF1A\u53EA\u6539\u5199\u7EA6 50%\uFF0C\u6539\u5199+\u8D1F\u5411\u6392\u9664\u7EA6 80%",
    "Houston Law Review\uFF1A\u7528\u4E00\u822C\u63CF\u8FF0\u66FF\u6362\u4E13\u540D\uFF0C\u4E0D\u590D\u5236\u53EF\u8BC6\u522B\u5F62\u8C61"
  ],
  steps: [
    "\u6CDB\u5316\uFF1A\u6309\u68C0\u51FA\u7C7B\u578B\u7684\u5C5E\u6027\u8F74\u5199\u5916\u5F62/\u5149\u7EBF/\u6750\u8D28/\u5A92\u4ECB\uFF0C\u4E0D\u5199\u8EAB\u4EFD\u540D",
    "\u53BB\u4E13\u540D\uFF1A\u6210\u7A3F\u6B63\u6587\u4E0D\u518D\u51FA\u73B0\u68C0\u51FA\u8BCD\u53CA\u5176\u522B\u540D",
    "\u975E IP \u951A\u5B9A\uFF1A\u827A\u672F\u8FD0\u52A8\u3001\u5386\u53F2\u65F6\u671F\u3001\u901A\u7528\u6280\u6CD5\uFF0C\u4E0D\u8981\u5DE5\u4F5C\u5BA4\u540D",
    "\u4FDD\u7559\u60C5\u5883\uFF1A\u672C\u53E5\u91CC\u4E0E\u8EAB\u4EFD\u65E0\u5173\u7684\u52A8\u4F5C\u3001\u573A\u666F\u3001\u5149\u7EBF\u5FC5\u987B\u7559\u4E0B\u6765\u5E76\u5199\u7EC6",
    "\u8D1F\u5411\u6392\u9664\uFF1A\u628A exclude \u5199\u5165 negative_prompt\uFF08\u6539\u5199\u4E0D\u591F\uFF0C\u5FC5\u987B\u52A0\u8D1F\u5411\uFF09",
    "\u4E0D\u8981\u7528\u66F4\u63A5\u8FD1\u539F\u4F5C\u7684\u7ECF\u5178\u526A\u5F71\u6216\u914D\u8272\u5957\u4EF6\u9876\u66FF\u4E13\u540D",
    "\u7ED3\u5408\u9879\u76EE\u8BB0\u5FC6\u6539\u5199\u5F53\u524D\u955C\u5934\uFF0C\u4E0D\u8981\u7167\u6284\u4E0A\u6B21\u6210\u7A3F"
  ]
};
function item(kind, terms, exclude = []) {
  return { kind, terms, exclude: [.../* @__PURE__ */ new Set([...exclude, ...terms])] };
}
var ENTRIES = [
  item("character", ["\u8718\u86DB\u4FA0", "spiderman", "spider-man", "spider man"], ["marvel", "\u6F2B\u5A01"]),
  item("character", ["\u8759\u8760\u4FA0", "batman"], ["dc", "\u54E5\u8C2D", "gotham"]),
  item("character", ["\u8D85\u4EBA", "superman"]),
  item("character", ["\u94A2\u94C1\u4FA0", "iron man", "ironman"]),
  item("character", ["\u7F8E\u56FD\u961F\u957F", "captain america"]),
  item("character", ["\u9ED1\u5BE1\u5987", "black widow"]),
  item("character", ["\u706D\u9738", "thanos"]),
  item("character", ["\u5C0F\u4E11", "joker"]),
  item("character", ["\u795E\u5947\u5973\u4FA0", "wonder woman"]),
  item("character", ["\u54C8\u5229\u6CE2\u7279", "harry potter"]),
  item("character", ["\u4F0F\u5730\u9B54", "voldemort"]),
  item("character", ["\u76AE\u5361\u4E18", "pikachu"], ["pokemon", "\u5B9D\u53EF\u68A6", "\u53E3\u888B\u5996\u602A"]),
  item("character", ["\u5B9D\u53EF\u68A6", "pokemon", "pocket monsters"]),
  item("character", ["\u5965\u7279\u66FC"]),
  item("character", ["\u54C6\u5566a\u68A6", "\u54C6\u5566A\u68A6", "\u673A\u5668\u732B", "doraemon"]),
  item("character", ["\u521D\u97F3\u672A\u6765", "hatsune miku"]),
  item("character", ["\u9A6C\u91CC\u5965", "\u8D85\u7EA7\u9A6C\u91CC\u5965", "super mario"], ["nintendo", "\u4EFB\u5929\u5802"]),
  item("character", ["\u8DEF\u6613\u5409", "luigi"]),
  item("character", ["\u8DEF\u98DE", "\u6D77\u8D3C\u738B", "one piece"]),
  item("character", ["\u9E23\u4EBA", "\u706B\u5F71\u5FCD\u8005", "naruto"]),
  item("character", ["\u8D85\u7EA7\u8D5B\u4E9A\u4EBA", "\u9F99\u73E0", "dragon ball", "\u4E03\u9F99\u73E0"]),
  item("character", ["\u5B59\u609F\u7A7A"]),
  item("character", ["\u7C73\u8001\u9F20", "\u7C73\u5947\u8001\u9F20", "mickey mouse"]),
  item("character", ["\u5510\u8001\u9E2D", "donald duck"]),
  item("character", ["hello kitty", "hellokitty"]),
  item("character", ["\u51B0\u96EA\u5947\u7F18", "\u827E\u838E", "elisa", "elsa", "disney frozen"]),
  item("character", ["\u5C0F\u9EC4\u4EBA", "minions"]),
  item("character", ["\u718A\u672C\u718A", "kumamon"]),
  item("character", ["\u5154\u516B\u54E5", "bugs bunny"]),
  item("character", ["\u9AD8\u8FBE", "gundam"]),
  item("character", ["\u521D\u53F7\u673A", "eva\u521D\u53F7\u673A", "evangelion"]),
  item("character", ["\u8721\u7B14\u5C0F\u65B0", "\u91CE\u539F\u65B0\u4E4B\u52A9"]),
  item("character", ["\u540D\u4FA6\u63A2\u67EF\u5357"]),
  item("character", ["\u6A31\u6843\u5C0F\u4E38\u5B50"]),
  item("character", ["\u559C\u7F8A\u7F8A", "\u7070\u592A\u72FC"]),
  item("character", ["\u54EA\u5412\u4E4B\u9B54\u7AE5"]),
  item("character", ["\u661F\u6218", "\u661F\u7403\u5927\u6218", "star wars", "\u8FBE\u65AF\u7EF4\u8FBE", "darth vader", "\u7EDD\u5730"]),
  item("character", ["\u970D\u683C\u6C83\u8328", "hogwarts"]),
  item("brand", ["\u8FEA\u58EB\u5C3C", "disney", "\u534E\u7279\u8FEA\u58EB\u5C3C"]),
  item("brand", ["\u6F2B\u5A01", "marvel"]),
  item("brand", ["\u534E\u7EB3", "warner bros", "dc\u6F2B\u753B", "dc comics"]),
  item("brand", ["\u4E50\u9AD8", "lego"]),
  item("brand", ["\u8010\u514B", "nike", "swoosh"]),
  item("brand", ["\u82F9\u679Clogo", "apple logo"]),
  item("brand", ["\u9EA6\u5F53\u52B3", "mcdonald"]),
  item("brand", ["\u661F\u5DF4\u514B", "starbucks"]),
  item("brand", ["\u4EFB\u5929\u5802", "nintendo"]),
  item("artist", ["\u5BAB\u5D0E\u9A8F\u98CE\u683C", "\u5BAB\u5D0E\u9A8F", "\u5409\u535C\u529B\u98CE\u683C", "\u5409\u535C\u529B", "ghibli", "studio ghibli"]),
  item("artist", ["\u65B0\u6D77\u8BDA\u98CE\u683C", "\u65B0\u6D77\u8BDA"]),
  item("artist", ["\u4ECA\u654F\u98CE\u683C", "\u4ECA\u654F"]),
  item("artist", ["\u68B5\u9AD8\u98CE\u683C", "\u68B5\u9AD8", "van gogh"]),
  item("artist", ["\u83AB\u5948\u98CE\u683C", "\u83AB\u5948", "monet"]),
  item("artist", ["\u97E6\u65AF\u5B89\u5FB7\u68EE\u98CE\u683C", "\u97E6\u65AF\xB7\u5B89\u5FB7\u68EE", "wes anderson"]),
  item("artist", ["\u738B\u5BB6\u536B\u98CE\u683C", "\u738B\u5BB6\u536B"]),
  item("likeness", ["\u5468\u6770\u4F26", "\u5218\u4EA6\u83F2", "\u6768\u5E42", "\u6210\u9F99", "\u9A6C\u65AF\u514B", "elon musk", "\u9A6C\u4E91", "\u7279\u6717\u666E", "trump"]),
  item("music", ["\u5468\u6770\u4F26\u7684\u6B4C", "\u539F\u58F0\u5E26", "\u597D\u83B1\u575E\u914D\u4E50"])
];
var COMBOS = [
  { keys: ["\u6C34\u7BA1\u5DE5", "\u6E38\u620F"], kind: "combo", label: "\u6C34\u7BA1\u5DE5+\u6E38\u620F", exclude: ["mario", "\u9A6C\u91CC\u5965", "nintendo"] },
  { keys: ["plumber", "videogame"], kind: "combo", label: "plumber+videogame", exclude: ["mario", "nintendo"] },
  { keys: ["\u9EC4\u76AE", "\u7535\u6C14", "\u8001\u9F20"], kind: "combo", label: "\u9EC4\u76AE\u7535\u6C14\u9F20", exclude: ["pikachu", "\u76AE\u5361\u4E18", "pokemon"] },
  { keys: ["\u7EA2\u84DD\u7D27\u8EAB\u8863", "\u80F8\u53E3"], kind: "combo", label: "\u7EA2\u84DD\u7D27\u8EAB\u8863+\u5FBD\u8BB0", exclude: ["superman", "\u8D85\u4EBA"] },
  { keys: ["\u5C16\u8033\u5934\u7F69", "\u62AB\u98CE", "Gotham"], kind: "combo", label: "\u5C16\u8033\u5934\u7F69+\u62AB\u98CE", exclude: ["batman", "\u8759\u8760\u4FA0"] }
];
var KIND_LABEL = {
  character: "\u89D2\u8272 IP",
  brand: "\u54C1\u724C/\u5546\u6807",
  artist: "\u4F5C\u8005\u98CE\u683C",
  likeness: "\u771F\u4EBA\u8096\u50CF",
  music: "\u97F3\u4E50\u7248\u6743",
  combo: "\u5916\u5F62\u7EC4\u5408"
};
function escapeRegExp(value) {
  return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function termPattern(term) {
  const body = escapeRegExp(term);
  if (/^[a-z0-9]/i.test(term)) return new RegExp(`(?<![a-z0-9])${body}(?![a-z0-9])`, "gi");
  return new RegExp(body, "gi");
}
function latinWord(term, text, index2) {
  if (!/^[a-z0-9]/i.test(term)) return true;
  const before = index2 === 0 ? "" : text[index2 - 1] ?? "";
  const after = text[index2 + term.length] ?? "";
  return !/[a-z0-9]/i.test(before) && !/[a-z0-9]/i.test(after);
}
function hitFromEntry(start2, end, term, kind, exclude) {
  return {
    start: start2,
    end,
    term,
    kind,
    risk: RISK[kind],
    exclude,
    axes: IP_AXES[kind]
  };
}
var scanCacheText = "";
var scanCacheHits = [];
function scanIpRisk(text) {
  if (text === scanCacheText) return scanCacheHits;
  const hits = scanIpRiskFresh(text);
  scanCacheText = text;
  scanCacheHits = hits;
  return hits;
}
function scanIpRiskFresh(text) {
  if (text.trim() === "") return [];
  const hits = [];
  const taken = [];
  const catalog = ENTRIES.flatMap((entry) => entry.terms.map((term) => ({ term, entry }))).sort((left, right) => right.term.length - left.term.length);
  for (const { term, entry } of catalog) {
    const pattern = termPattern(term);
    let match = pattern.exec(text);
    while (match !== null) {
      const start2 = match.index;
      const end = start2 + match[0].length;
      const overlap = taken.some((span) => start2 < span.end && end > span.start);
      if (!overlap && latinWord(match[0], text, start2)) {
        taken.push({ start: start2, end });
        hits.push(hitFromEntry(start2, end, match[0], entry.kind, entry.exclude));
      }
      match = pattern.exec(text);
    }
  }
  const lower2 = text.toLowerCase();
  for (const combo of COMBOS) {
    const found = combo.keys.every((key) => lower2.includes(key.toLowerCase()));
    if (!found) continue;
    const first = combo.keys.map((key) => ({ key, at: lower2.indexOf(key.toLowerCase()) })).filter((item3) => item3.at >= 0).sort((left, right) => left.at - right.at)[0];
    if (first === void 0) continue;
    const start2 = first.at;
    const end = start2 + first.key.length;
    const overlap = taken.some((span) => start2 < span.end && end > span.start);
    if (overlap) continue;
    taken.push({ start: start2, end });
    hits.push(hitFromEntry(start2, end, combo.label, "combo", combo.exclude));
  }
  return hits.sort((left, right) => left.start - right.start);
}
function keepSpans(text, hits) {
  const parts = [];
  let cursor = 0;
  for (const hit of hits) {
    if (hit.start > cursor) {
      const frag = text.slice(cursor, hit.start).replace(/\s+/g, " ").trim();
      if (frag !== "") parts.push(frag);
    }
    cursor = Math.max(cursor, hit.end);
  }
  if (cursor < text.length) {
    const frag = text.slice(cursor).replace(/\s+/g, " ").trim();
    if (frag !== "") parts.push(frag);
  }
  return parts;
}
function collectNegatives(hits) {
  return [...new Set(hits.flatMap((hit) => hit.exclude.map((item3) => item3.trim()).filter((item3) => item3 !== "")))];
}
function mergeNegativeLine(existing, extras) {
  const parts = [
    ...(existing ?? "").split(/[,，;；]/).map((part) => part.trim()).filter((part) => part !== ""),
    "copyrighted character",
    "trademarked logo",
    ...extras
  ];
  return [...new Set(parts)].join(", ");
}
function memoryBlock(memory) {
  if (memory.length === 0) return "\u672C\u9879\u76EE\u8FD8\u6CA1\u6709\u8FD9\u7C7B\u6539\u5199\u8BB0\u5FC6\u3002\u6309\u65B9\u6CD5\u8F74\u548C\u672C\u53E5\u60C5\u5883\u5199\u7EC6\u3002";
  return [
    "\u672C\u9879\u76EE\u8BB0\u5FC6\uFF08\u7528\u6237\u7528\u8FC7\u5E76\u9A8C\u6536\u7684\u6539\u5199\uFF1B\u6309\u5F53\u524D\u955C\u5934\u6539\uFF0C\u4E0D\u8981\u7167\u6284\uFF09\uFF1A",
    ...memory.map((entry) => `- \u300C${entry.terms.slice(0, 3).join("/")}\u300D\xD7${entry.uses}\uFF1A${entry.rewrite.slice(0, 240)}`)
  ].join("\n");
}
function buildIpBrief(text, extras = {}) {
  const hits = scanIpRisk(text);
  const keep = keepSpans(text, hits);
  const exclude = collectNegatives(hits);
  const negativeLine = hits.length === 0 ? extras.existingNegative?.trim() ?? "" : mergeNegativeLine(extras.existingNegative, exclude);
  const dirty = hits.length > 0;
  const memory = extras.memory ?? [];
  const next = dirty ? [
    "directorx_knowledge_read 213",
    "\u6309 method/axes \u7ED3\u5408 keep \u4E0E\u9879\u76EE\u8BB0\u5FC6\u5199\u7EC6\u6539\u5199",
    "directorx_ip_rewrite \u9A8C\u6536\u5E76\u8BB0\u5165\u8BB0\u5FC6",
    "\u518D directorx_prompt_craft\uFF08intent=\u539F\u53E5\uFF0Cprompt=\u6539\u5199\u7A3F\uFF09"
  ] : [];
  const agentPrompt = dirty ? [
    "\u7248\u6743\u6539\u5199\u4EFB\u52A1\uFF08\u5DE5\u7A0B\u53EA\u68C0\u51FA\u548C\u65B9\u6CD5\u7F16\u6392\uFF0C\u6210\u7A3F\u5FC5\u987B\u4F60\u6309\u5F53\u524D\u955C\u5934\u5199\u7EC6\uFF0C\u7981\u6B62\u5957\u56FA\u5B9A\u66FF\u6362\u53E5\uFF09\uFF1A",
    `\u539F\u53E5\uFF1A${text.trim()}`,
    `\u68C0\u51FA\uFF1A${hits.map((hit) => `\u300C${hit.term}\u300D(${KIND_LABEL[hit.kind]})`).join("\uFF1B")}`,
    keep.length > 0 ? `\u5FC5\u987B\u4FDD\u7559\u7684\u60C5\u5883\uFF1A${keep.join(" / ")}` : "\u539F\u53E5\u51E0\u4E4E\u53EA\u6709\u4E13\u540D\uFF1A\u8865\u4E0A\u52A8\u4F5C\u3001\u573A\u666F\u3001\u5149\u7EBF\u540E\u518D\u5199\u5916\u5F62\u3002",
    "\u5C5E\u6027\u8F74\uFF1A",
    ...hits.flatMap((hit) => [`- ${hit.term}\uFF1A`, ...hit.axes.map((axis) => `  \xB7 ${axis}`)]),
    "\u65B9\u6CD5\uFF1A",
    ...IP_METHOD.steps.map((step, index2) => `${index2 + 1}. ${step}`),
    `\u4F9D\u636E\uFF1A${IP_METHOD.sources.join("\uFF1B")}`,
    memoryBlock(memory),
    `\u8D1F\u5411\u6392\u9664\u5199\u5165 generate \u7684 negative_prompt\uFF1A${negativeLine}`,
    "\u9A8C\u6536\uFF1A\u5BF9\u6210\u7A3F\u518D\u626B\uFF0Chits \u5FC5\u987B\u4E3A\u7A7A\uFF1B\u901A\u8FC7 directorx_ip_rewrite \u8BB0\u5165\u672C\u9879\u76EE\u8BB0\u5FC6\u3002"
  ].join("\n") : "";
  return {
    dirty,
    hits,
    keep,
    exclude,
    negativeLine,
    method: IP_METHOD,
    knowledge: dirty ? ["213"] : [],
    next,
    agentPrompt
  };
}
function askDshRewriteText(text, memory = []) {
  const brief = buildIpBrief(text, { memory });
  if (!brief.dirty) return "";
  return [
    "\u8BF7\u5B9E\u65BD\u7248\u6743\u5B89\u5168\u6539\u5199\uFF0C\u4E0D\u8981\u5957\u56FA\u5B9A\u6210\u7A3F\u3002",
    brief.agentPrompt,
    "\u5148 directorx_ip_scan \u53D6\u9879\u76EE\u8BB0\u5FC6\uFF0C\u518D directorx_knowledge_read 213\uFF0C\u5199\u7EC6\u540E directorx_ip_rewrite\uFF08remember:true\uFF09\u3002\u4E0D\u8981 generate\u3002"
  ].join("\n");
}

// src/client/stage/ip-prompt.tsx
var import_jsx_runtime8 = require("react/jsx-runtime");
var ASK_DSH_REWRITE = "directorx-ask-dsh";
function askDsh(text) {
  if (typeof window === "undefined" || text.trim() === "") return;
  window.dispatchEvent(new CustomEvent(ASK_DSH_REWRITE, { detail: { text } }));
}
function PromptIpField(props) {
  const hits = (0, import_react8.useMemo)(() => scanIpRisk(props.value), [props.value]);
  const overlayRef = (0, import_react8.useRef)(null);
  const handoff = () => {
    const text = askDshRewriteText(props.value);
    if (text !== "") askDsh(text);
  };
  const onScroll = (event) => {
    const node = overlayRef.current;
    if (node === null) return;
    node.scrollTop = event.currentTarget.scrollTop;
    node.scrollLeft = event.currentTarget.scrollLeft;
  };
  return /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { style: { display: "flex", flexDirection: "column", gap: 6, minWidth: 0, flex: props.style?.flex === 1 ? 1 : void 0 }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { style: { position: "relative", minWidth: 0 }, children: [
      hits.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        "div",
        {
          ref: overlayRef,
          "aria-hidden": true,
          style: {
            ...props.style,
            position: "absolute",
            inset: 0,
            pointerEvents: "none",
            whiteSpace: "pre-wrap",
            overflow: "hidden",
            wordBreak: "break-word",
            color: dx.ink,
            padding: props.style?.padding ?? 0
          },
          children: paintPrompt(props.value, hits)
        }
      ) : null,
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        "textarea",
        {
          ref: props.inputRef,
          className: props.className,
          value: props.value,
          placeholder: props.placeholder,
          rows: props.rows,
          onChange: (event) => props.onChange(event.target.value),
          onKeyDown: props.onKeyDown,
          onScroll,
          style: {
            ...props.style,
            position: "relative",
            width: "100%",
            boxSizing: "border-box",
            padding: props.style?.padding ?? 0,
            background: hits.length > 0 ? "transparent" : props.style?.background,
            color: hits.length > 0 ? "transparent" : props.style?.color ?? dx.ink,
            caretColor: dx.ink
          }
        }
      )
    ] }),
    hits.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { style: { display: "flex", flexDirection: "column", gap: 4 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6, alignItems: "center" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("span", { style: { fontSize: 10.5, color: "#ff8a80" }, children: "\u53EF\u80FD\u5B58\u5728\u7248\u6743\u98CE\u9669 \xB7 \u70B9\u540D IP \u4EA4\u7ED9 DSH \u6309\u60C5\u5883\u6539\u5199" }),
        /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
          "button",
          {
            type: "button",
            className: "dx-hit",
            title: "\u4EA4\u7ED9 DSH\uFF1A\u6309\u6CDB\u5316\u65B9\u6CD5\u7ED3\u5408\u5F53\u524D\u955C\u5934\u5199\u7EC6\uFF0C\u5E76\u8BB0\u5165\u9879\u76EE\u8BB0\u5FC6",
            onClick: handoff,
            style: {
              border: "1px solid rgba(255,138,128,.45)",
              background: "rgba(255,80,70,.12)",
              color: "#ffc7c2",
              borderRadius: 999,
              padding: "2px 8px",
              fontSize: 10.5,
              cursor: "pointer"
            },
            children: "\u4EA4\u7ED9 DSH \u6539\u5199"
          }
        ),
        hits.map((hit) => /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)(
          "span",
          {
            title: hit.risk,
            style: { fontSize: 10.5, color: "#ffc7c2" },
            children: [
              "\u300C",
              hit.term,
              "\u300D"
            ]
          },
          `${hit.start}-${hit.term}`
        ))
      ] }),
      props.compact === true ? null : /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { style: { fontSize: 10.5, color: dx.mute, lineHeight: 1.45 }, children: [
        "\u4E0D\u8981\u5957\u56FA\u5B9A\u66FF\u6362\u53E5\u3002\u6309\u5C5E\u6027\u8F74\u5199\u7EC6\uFF0C\u5E76\u52A0\u8D1F\u5411\u6392\u9664",
        hits[0] !== void 0 && hits[0].exclude.length > 0 ? `\uFF08${hits[0].exclude.slice(0, 4).join(" / ")}\uFF09` : "",
        "\u3002\u7528\u8FC7\u7684\u6539\u5199\u4F1A\u8BB0\u5165\u672C\u9879\u76EE\u8BB0\u5FC6\u3002"
      ] })
    ] }) : null
  ] });
}
function paintPrompt(text, hits) {
  if (hits.length === 0) return text;
  const parts = [];
  let cursor = 0;
  hits.forEach((hit, index2) => {
    if (hit.start > cursor) parts.push(text.slice(cursor, hit.start));
    parts.push(
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        "span",
        {
          title: hit.risk,
          style: {
            textDecorationLine: "underline",
            textDecorationStyle: "wavy",
            textDecorationColor: "#ff5a4e",
            textDecorationThickness: 2,
            textUnderlineOffset: 3
          },
          children: text.slice(hit.start, hit.end)
        },
        `${hit.start}-${index2}`
      )
    );
    cursor = hit.end;
  });
  if (cursor < text.length) parts.push(text.slice(cursor));
  return parts;
}

// src/client/stage/NodeWorkstation.tsx
var import_jsx_runtime9 = require("react/jsx-runtime");
var pop = {
  ...dxChrome,
  position: "absolute",
  bottom: 44,
  left: 0,
  zIndex: 4,
  padding: 8,
  borderRadius: 14,
  minWidth: 200
};
function NodeWorkstation(props) {
  const [open, setOpen] = (0, import_react9.useState)(void 0);
  const compact = props.compact === true;
  const models = modelsFor(props.spec.kind);
  const modelLabel = models.find((item3) => item3.id === props.spec.model)?.label ?? "\u6A21\u578B";
  const count = props.spec.count ?? 1;
  const chips = /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, { children: [
    props.sourceLabel !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("button", { className: "dx-hit", onClick: props.onClearSource, style: { ...dxGhostBtn, width: "auto", height: 26, padding: "0 8px", gap: 4, fontSize: 11, background: "rgba(255,255,255,.06)", maxWidth: 140 }, title: "\u53D6\u6D88\u4E0A\u6E38\u5F15\u7528", children: [
      /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("span", { style: { overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: [
        "\u53C2\u8003 ",
        props.sourceLabel
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconClose, { size: 10 })
    ] }) : null,
    (props.spec.refIds ?? []).map((id2) => /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("span", { title: id2, style: { ...dxGhostBtn, width: "auto", height: 22, padding: "0 7px", fontSize: 10, background: "rgba(255,255,255,.06)" }, children: [
      "\u53C2\u8003 ",
      id2.replace(/^.*-/, "").slice(0, 6)
    ] }, id2)),
    (props.spec.characters ?? []).map((name2) => /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
      "button",
      {
        className: "dx-hit",
        title: "\u79FB\u51FA\u89D2\u8272\u951A\u70B9",
        onClick: () => props.onChange({ ...props.spec, characters: (props.spec.characters ?? []).filter((item3) => item3 !== name2) }),
        style: { ...dxGhostBtn, width: "auto", height: 22, padding: "0 7px", gap: 4, fontSize: 10, background: "rgba(255,255,255,.06)" },
        children: [
          name2,
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconClose, { size: 9 })
        ]
      },
      name2
    ))
  ] });
  const send = /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
    "button",
    {
      type: "button",
      disabled: props.busy === true || props.spec.prompt.trim() === "",
      onClick: props.onSubmit,
      className: "dx-cta",
      title: "\u4EA4\u7ED9 DSH",
      style: { ...dxPill, width: 32, height: 32, flexShrink: 0, opacity: props.busy === true || props.spec.prompt.trim() === "" ? 0.4 : 1 },
      children: props.busy === true ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("span", { className: "dx-spin" }) : /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconSend, { size: 14 })
    }
  );
  const controls = /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 4, minWidth: 0, flexWrap: "wrap" }, children: [
    compact ? /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
        "button",
        {
          className: "dx-hit",
          title: props.spec.kind === "video" ? "\u89C6\u9891" : props.spec.kind === "audio" ? "\u97F3\u9891" : "\u56FE\u7247",
          style: {
            ...dxGhostBtn,
            width: 28,
            height: 26,
            background: "rgba(255,255,255,.06)"
          },
          onClick: () => props.onChange({
            ...props.spec,
            kind: props.spec.kind === "image" ? "video" : props.spec.kind === "video" ? "audio" : "image",
            model: void 0
          }),
          children: props.spec.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconVideo, { size: 12 }) : props.spec.kind === "audio" ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconAudio, { size: 12 }) : /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconImage, { size: 12 })
        }
      ),
      props.onPickRef !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
        "button",
        {
          className: "dx-hit",
          title: "\u6DFB\u52A0\u53C2\u8003",
          style: { ...dxGhostBtn, width: 28, height: 26 },
          onClick: props.onPickRef,
          children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconPlus, { size: 12 })
        }
      ) : null,
      /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
        "button",
        {
          className: "dx-hit",
          title: "\u4EA4\u7ED9 DSH",
          style: { ...dxGhostBtn, width: 28, height: 26 },
          onClick: props.onSubmit,
          children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconSpark, { size: 12 })
        }
      )
    ] }) : ["image", "video", "audio"].map((kind) => /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
      "button",
      {
        className: "dx-hit",
        style: {
          ...dxGhostBtn,
          width: "auto",
          height: 26,
          padding: "0 8px",
          gap: 4,
          fontSize: 11,
          background: props.spec.kind === kind ? "rgba(255,255,255,.12)" : "transparent"
        },
        onClick: () => props.onChange({ ...props.spec, kind, model: void 0 }),
        children: [
          kind === "image" ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconImage, { size: 12 }) : kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconVideo, { size: 12 }) : /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconAudio, { size: 12 }),
          kind === "image" ? "\u56FE\u7247" : kind === "video" ? "\u89C6\u9891" : "\u97F3\u9891"
        ]
      },
      kind
    )),
    /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("span", { style: { flex: 1 } }),
    /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(PopTrigger, { label: modelLabel, open: open === "model", onToggle: () => setOpen(open === "model" ? void 0 : "model"), children: models.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("button", { className: "dx-menu-item", style: row2, onClick: () => {
      props.onChange({ ...props.spec, model: item3.id });
      setOpen(void 0);
    }, children: item3.label }, item3.id)) }),
    props.spec.kind !== "audio" ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(PopTrigger, { label: props.spec.aspect ?? "\u753B\u5E45", open: open === "aspect", onToggle: () => setOpen(open === "aspect" ? void 0 : "aspect"), children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(4, 56px)", gap: 4, padding: 4 }, children: FRAME_ASPECTS.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
      "button",
      {
        className: "dx-hit",
        title: item3.id,
        style: {
          ...dxGhostBtn,
          width: 56,
          height: 44,
          flexDirection: "column",
          gap: 4,
          fontSize: 10,
          background: props.spec.aspect === item3.id ? "rgba(255,255,255,.12)" : "transparent"
        },
        onClick: () => {
          props.onChange({ ...props.spec, aspect: item3.id });
          setOpen(void 0);
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(AspectMark, { w: item3.w, h: item3.h }),
          item3.id
        ]
      },
      item3.id
    )) }) }) : null,
    props.spec.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(PopTrigger, { label: `${props.spec.durationSec ?? 5}s`, open: open === "duration", onToggle: () => setOpen(open === "duration" ? void 0 : "duration"), children: VIDEO_DURATIONS.map((sec) => /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("button", { className: "dx-menu-item", style: row2, onClick: () => {
      props.onChange({ ...props.spec, durationSec: sec });
      setOpen(void 0);
    }, children: [
      sec,
      "s"
    ] }, sec)) }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(PopTrigger, { label: `${count}\xD7`, open: open === "count", onToggle: () => setOpen(open === "count" ? void 0 : "count"), children: [1, 2, 3, 4].map((n2) => /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("button", { className: "dx-menu-item", style: row2, onClick: () => {
      props.onChange({ ...props.spec, count: n2 });
      setOpen(void 0);
    }, children: [
      n2,
      "\xD7"
    ] }, n2)) }),
    compact ? send : null
  ] });
  return /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
    "div",
    {
      className: "nowheel nopan nodrag",
      onPointerDown: (event) => event.stopPropagation(),
      style: {
        width: compact ? 640 : "100%",
        minWidth: compact ? 640 : 0,
        maxWidth: compact ? 650 : 520,
        padding: compact ? 12 : 10,
        borderRadius: 16,
        background: "#1f1f1f",
        border: "1px solid rgba(255,255,255,.10)",
        color: dx.ink,
        fontFamily: dx.font,
        display: "flex",
        flexDirection: "column",
        gap: 8,
        boxSizing: "border-box"
      },
      children: [
        props.spec.revise === true || props.sourceLabel !== void 0 || (props.spec.refIds ?? []).length > 0 || (props.spec.characters ?? []).length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 4, flexWrap: "wrap" }, children: [
          props.spec.revise === true ? /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
            "button",
            {
              className: "dx-hit",
              title: "\u53D6\u6D88\u91CD\u65B0\u751F\u6210\uFF0C\u6539\u4E3A\u65B0\u5EFA",
              onClick: () => props.onChange({ ...props.spec, revise: void 0, targetId: void 0 }),
              style: { ...dxGhostBtn, width: "auto", height: 22, padding: "0 7px", gap: 4, fontSize: 10, background: "rgba(255,255,255,.12)" },
              children: [
                "\u91CD\u65B0\u751F\u6210",
                /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconClose, { size: 9 })
              ]
            }
          ) : null,
          chips
        ] }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { style: { display: "flex", alignItems: "flex-start", gap: 8 }, children: [
          compact && props.onPickRef !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              className: "dx-hit",
              title: "\u6DFB\u52A0\u53C2\u8003",
              style: { ...dxGhostBtn, width: 38, height: 38, borderRadius: 10, background: "rgba(255,255,255,.08)", flexShrink: 0 },
              onClick: props.onPickRef,
              children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(IconPlus, { size: 16 })
            }
          ) : null,
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            PromptIpField,
            {
              compact,
              inputRef: props.inputRef,
              value: props.spec.prompt,
              placeholder: props.spec.revise === true ? "\u6539\u54EA\u91CC\uFF0C\u6216\u76F4\u63A5\u6CBF\u7528\u5F53\u524D\u63D0\u793A\u8BCD\u518D\u751F\u6210" : "\u63CF\u8FF0\u4EFB\u4F55\u4F60\u60F3\u8981\u751F\u6210\u7684\u5185\u5BB9",
              rows: 3,
              onChange: (value) => props.onChange({ ...props.spec, prompt: value }),
              onKeyDown: (event) => {
                if (event.key === "Enter" && !event.shiftKey) {
                  event.preventDefault();
                  props.onSubmit();
                }
              },
              style: {
                flex: 1,
                minHeight: compact ? 80 : 40,
                maxHeight: 120,
                resize: "none",
                border: "none",
                outline: "none",
                background: "transparent",
                color: dx.ink,
                fontSize: 14,
                lineHeight: 1.5,
                fontFamily: dx.font
              }
            }
          ),
          compact ? null : send
        ] }),
        controls
      ]
    }
  );
}
function AspectMark(props) {
  const max = 18;
  const ratio = props.w / props.h;
  const width = ratio >= 1 ? max : Math.max(6, Math.round(max * ratio));
  const height = ratio >= 1 ? Math.max(6, Math.round(max / ratio)) : max;
  return /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("span", { style: { width, height, borderRadius: 2, border: "1px solid rgba(255,255,255,.55)", display: "block" } });
}
function PopTrigger(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { style: { position: "relative" }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("button", { type: "button", className: "dx-hit", onClick: props.onToggle, style: { ...dxGhostBtn, width: "auto", height: 26, padding: "0 8px", fontSize: 11 }, children: props.label }),
    props.open ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("div", { style: pop, children: props.children }) : null
  ] });
}
var row2 = {
  display: "flex",
  alignItems: "center",
  width: "100%",
  textAlign: "left",
  padding: "6px 8px",
  border: "none",
  borderRadius: 8,
  background: "transparent",
  color: dx.ink,
  fontSize: 12,
  fontFamily: dx.font,
  cursor: "pointer"
};

// src/client/stage/nodes.tsx
var import_react14 = require("react");

// src/client/stage/markdown.ts
function safeHref(href) {
  const value = href.trim();
  if (value === "") return void 0;
  if (/^https?:\/\//i.test(value) || value.startsWith("mailto:")) return value;
  if (value.startsWith("/") && !value.startsWith("//")) return value;
  return void 0;
}
function isFence(line) {
  return line.trimStart().startsWith("```");
}
function isHr(line) {
  return /^[-*_]{3,}\s*$/.test(line.trim());
}
function isTableRow(line) {
  const trimmed = line.trim();
  return trimmed.startsWith("|") && trimmed.includes("|", 1);
}
function isTableSep(line) {
  return /^\s*\|?(?:\s*:?-{3,}:?\s*\|)+\s*:?-{3,}:?\s*\|?\s*$/.test(line);
}
function tableCells(line) {
  const trimmed = line.trim().replace(/^\|/, "").replace(/\|$/, "");
  return trimmed.split("|").map((cell) => cell.trim());
}
function isHeading(line) {
  return /^(#{1,3})\s+(.+)$/.exec(line);
}
function isQuote(line) {
  return line.startsWith(">");
}
function isUl(line) {
  return /^\s*[-*+]\s+/.test(line);
}
function isOl(line) {
  return /^\s*\d+\.\s+/.test(line);
}
function isBlockStart(line) {
  return isFence(line) || isHr(line) || isHeading(line) !== null || isQuote(line) || isUl(line) || isOl(line) || isTableRow(line);
}
function parseInline(src) {
  const out = [];
  let buf = "";
  const flush = () => {
    if (buf !== "") {
      out.push({ type: "text", value: buf });
      buf = "";
    }
  };
  let index2 = 0;
  while (index2 < src.length) {
    if (src[index2] === "`") {
      const end = src.indexOf("`", index2 + 1);
      if (end > index2) {
        flush();
        out.push({ type: "code", value: src.slice(index2 + 1, end) });
        index2 = end + 1;
        continue;
      }
    }
    if (src.startsWith("**", index2)) {
      const end = src.indexOf("**", index2 + 2);
      if (end > index2) {
        flush();
        out.push({ type: "strong", children: parseInline(src.slice(index2 + 2, end)) });
        index2 = end + 2;
        continue;
      }
    }
    if (src.startsWith("~~", index2)) {
      const end = src.indexOf("~~", index2 + 2);
      if (end > index2) {
        flush();
        out.push({ type: "del", children: parseInline(src.slice(index2 + 2, end)) });
        index2 = end + 2;
        continue;
      }
    }
    const mark = src[index2];
    if ((mark === "*" || mark === "_") && src[index2 + 1] !== mark) {
      const end = src.indexOf(mark, index2 + 1);
      if (end > index2) {
        flush();
        out.push({ type: "em", children: parseInline(src.slice(index2 + 1, end)) });
        index2 = end + 1;
        continue;
      }
    }
    if (src[index2] === "[") {
      const close = src.indexOf("](", index2);
      const end = close === -1 ? -1 : src.indexOf(")", close + 2);
      if (close > index2 && end > close) {
        const href = safeHref(src.slice(close + 2, end));
        if (href !== void 0) {
          flush();
          out.push({ type: "link", href, children: parseInline(src.slice(index2 + 1, close)) });
          index2 = end + 1;
          continue;
        }
      }
    }
    buf += src[index2];
    index2 += 1;
  }
  flush();
  return out;
}
function parseMarkdown(text) {
  const lines = text.replaceAll("\r\n", "\n").split("\n");
  const blocks = [];
  let index2 = 0;
  while (index2 < lines.length) {
    const line = lines[index2] ?? "";
    if (line.trim() === "") {
      index2 += 1;
      continue;
    }
    if (isFence(line)) {
      const langRaw = line.trimStart().slice(3).trim();
      const lang = langRaw === "" ? void 0 : langRaw;
      index2 += 1;
      const body = [];
      while (index2 < lines.length && !isFence(lines[index2] ?? "")) {
        body.push(lines[index2] ?? "");
        index2 += 1;
      }
      if (index2 < lines.length) index2 += 1;
      blocks.push({ type: "code", ...lang === void 0 ? {} : { lang }, value: body.join("\n") });
      continue;
    }
    if (isTableRow(line) && isTableSep(lines[index2 + 1] ?? "")) {
      const header = tableCells(line).map((cell) => parseInline(cell));
      index2 += 2;
      const rows = [];
      while (index2 < lines.length && isTableRow(lines[index2] ?? "") && !isTableSep(lines[index2] ?? "")) {
        rows.push(tableCells(lines[index2] ?? "").map((cell) => parseInline(cell)));
        index2 += 1;
      }
      blocks.push({ type: "table", header, rows });
      continue;
    }
    if (isHr(line)) {
      blocks.push({ type: "hr" });
      index2 += 1;
      continue;
    }
    const heading = isHeading(line);
    if (heading !== null) {
      const level = heading[1].length;
      blocks.push({ type: "heading", level, children: parseInline(heading[2] ?? "") });
      index2 += 1;
      continue;
    }
    if (isQuote(line)) {
      const quoted = [];
      while (index2 < lines.length && isQuote(lines[index2] ?? "")) {
        quoted.push((lines[index2] ?? "").replace(/^>\s?/, ""));
        index2 += 1;
      }
      blocks.push({ type: "quote", children: parseInline(quoted.join(" ")) });
      continue;
    }
    if (isUl(line) || isOl(line)) {
      const ordered = isOl(line);
      const items = [];
      while (index2 < lines.length && (ordered ? isOl(lines[index2] ?? "") : isUl(lines[index2] ?? ""))) {
        items.push(parseInline((lines[index2] ?? "").replace(/^\s*(?:[-*+]|\d+\.)\s+/, "")));
        index2 += 1;
      }
      blocks.push({ type: "list", ordered, items });
      continue;
    }
    const para = [];
    while (index2 < lines.length) {
      const next = lines[index2] ?? "";
      if (next.trim() === "" || isBlockStart(next)) break;
      para.push(next);
      index2 += 1;
    }
    if (para.length > 0) blocks.push({ type: "paragraph", children: parseInline(para.join(" ")) });
  }
  return blocks;
}

// src/client/stage/MarkdownView.tsx
var import_jsx_runtime10 = require("react/jsx-runtime");
function InlineView(props) {
  return props.nodes.map((node, index2) => {
    if (node.type === "text") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { children: node.value }, index2);
    if (node.type === "code") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("code", { children: node.value }, index2);
    if (node.type === "strong") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("strong", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: node.children }) }, index2);
    if (node.type === "em") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("em", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: node.children }) }, index2);
    if (node.type === "del") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("del", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: node.children }) }, index2);
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("a", { href: node.href, target: "_blank", rel: "noreferrer noopener", children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: node.children }) }, index2);
  });
}
function BlockView(props) {
  const { block, index: index2 } = props;
  if (block.type === "heading") {
    if (block.level === 1) return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("h1", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: block.children }) }, index2);
    if (block.level === 2) return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("h2", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: block.children }) }, index2);
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("h3", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: block.children }) }, index2);
  }
  if (block.type === "paragraph") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("p", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: block.children }) }, index2);
  if (block.type === "quote") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("blockquote", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: block.children }) }, index2);
  if (block.type === "hr") return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("hr", {}, index2);
  if (block.type === "table") {
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: "dx-md-table-wrap", children: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("table", { className: "dx-md-table", children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("tr", { children: block.header.map((cell, cellIndex) => /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("th", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: cell }) }, cellIndex)) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("tbody", { children: block.rows.map((row3, rowIndex) => /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("tr", { children: row3.map((cell, cellIndex) => /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("td", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: cell }) }, cellIndex)) }, rowIndex)) })
    ] }) }, index2);
  }
  if (block.type === "code") {
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("pre", { "data-lang": block.lang, children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("code", { children: block.value }) }, index2);
  }
  const Tag = block.ordered ? "ol" : "ul";
  return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(Tag, { children: block.items.map((item3, itemIndex) => /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(InlineView, { nodes: item3 }) }, itemIndex)) }, index2);
}
function MarkdownView(props) {
  const blocks = parseMarkdown(props.text);
  if (blocks.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: props.className ?? "dx-md", "data-dx-md": "", children: blocks.map((block, index2) => /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(BlockView, { block, index: index2 }, index2)) });
}

// src/client/stage/CropOverlay.tsx
var import_react10 = require("react");
var import_jsx_runtime11 = require("react/jsx-runtime");
var CROP_ASPECTS = [
  { id: "free", label: "\u81EA\u7531", value: void 0 },
  { id: "1:1", label: "1:1", value: 1 },
  { id: "16:9", label: "16:9", value: 16 / 9 },
  { id: "9:16", label: "9:16", value: 9 / 16 },
  { id: "4:3", label: "4:3", value: 4 / 3 },
  { id: "3:4", label: "3:4", value: 3 / 4 }
];
function contentBox(wellW, wellH, imgW, imgH) {
  if (imgW <= 0 || imgH <= 0 || wellW <= 0 || wellH <= 0) return { x: 0, y: 0, w: wellW, h: wellH };
  const wellR = wellW / wellH;
  const imgR = imgW / imgH;
  if (imgR > wellR) {
    const w2 = wellW;
    const h2 = wellW / imgR;
    return { x: 0, y: (wellH - h2) / 2, w: w2, h: h2 };
  }
  const h = wellH;
  const w = wellH * imgR;
  return { x: (wellW - w) / 2, y: 0, w, h };
}
function inset(box2, pad) {
  const p2 = Math.min(pad, box2.w / 4, box2.h / 4);
  return { x: box2.x + p2, y: box2.y + p2, w: box2.w - p2 * 2, h: box2.h - p2 * 2 };
}
function clampRect(rect, box2) {
  const w = Math.max(24, Math.min(rect.w, box2.w));
  const h = Math.max(24, Math.min(rect.h, box2.h));
  const x3 = Math.min(Math.max(rect.x, box2.x), box2.x + box2.w - w);
  const y2 = Math.min(Math.max(rect.y, box2.y), box2.y + box2.h - h);
  return { x: x3, y: y2, w, h };
}
function fitAspect(rect, box2, ratio) {
  if (ratio === void 0 || ratio <= 0) return clampRect(rect, box2);
  const cx = rect.x + rect.w / 2;
  const cy = rect.y + rect.h / 2;
  let w = rect.w;
  let h = w / ratio;
  if (h > box2.h) {
    h = box2.h;
    w = h * ratio;
  }
  if (w > box2.w) {
    w = box2.w;
    h = w / ratio;
  }
  return clampRect({ x: cx - w / 2, y: cy - h / 2, w, h }, box2);
}
function applyHandle(start2, dx2, dy, handle2, box2, ratio) {
  let next = { ...start2 };
  if (handle2 === "move") {
    next = { ...start2, x: start2.x + dx2, y: start2.y + dy };
    return clampRect(next, box2);
  }
  if (handle2.includes("w")) {
    next.x = start2.x + dx2;
    next.w = start2.w - dx2;
  }
  if (handle2.includes("e")) next.w = start2.w + dx2;
  if (handle2.includes("n")) {
    next.y = start2.y + dy;
    next.h = start2.h - dy;
  }
  if (handle2.includes("s")) next.h = start2.h + dy;
  if (next.w < 24) {
    if (handle2.includes("w")) next.x = start2.x + start2.w - 24;
    next.w = 24;
  }
  if (next.h < 24) {
    if (handle2.includes("n")) next.y = start2.y + start2.h - 24;
    next.h = 24;
  }
  return fitAspect(next, box2, ratio);
}
var handleCursor = {
  n: "ns-resize",
  s: "ns-resize",
  e: "ew-resize",
  w: "ew-resize",
  ne: "nesw-resize",
  sw: "nesw-resize",
  nw: "nwse-resize",
  se: "nwse-resize",
  move: "move"
};
function handleStyle(handle2, rect) {
  const size = 10;
  const half = size / 2;
  const midX = rect.x + rect.w / 2 - half;
  const midY = rect.y + rect.h / 2 - half;
  const left = handle2.includes("w") ? rect.x - half : handle2.includes("e") ? rect.x + rect.w - half : midX;
  const top = handle2.includes("n") ? rect.y - half : handle2.includes("s") ? rect.y + rect.h - half : midY;
  return {
    position: "absolute",
    left,
    top,
    width: size,
    height: size,
    borderRadius: 2,
    background: "#fff",
    boxShadow: "0 0 0 1px rgba(0,0,0,.45)",
    cursor: handleCursor[handle2],
    zIndex: 3
  };
}
function CropBox(props) {
  const wellRef = (0, import_react10.useRef)(null);
  const imgRef = (0, import_react10.useRef)(null);
  const drag = (0, import_react10.useRef)(void 0);
  const [box2, setBox] = (0, import_react10.useState)({ x: 0, y: 0, w: 0, h: 0 });
  const [rect, setRect] = (0, import_react10.useState)({ x: 8, y: 8, w: 80, h: 80 });
  const [busy, setBusy] = (0, import_react10.useState)(false);
  const measure = (0, import_react10.useCallback)(() => {
    const well = wellRef.current;
    const img = imgRef.current;
    if (well === null || img === null || img.naturalWidth === 0) return;
    const next = contentBox(well.clientWidth, well.clientHeight, img.naturalWidth, img.naturalHeight);
    setBox(next);
    setRect((current) => fitAspect(current.w < 8 ? inset(next, 16) : current, next, props.aspect));
  }, [props.aspect]);
  (0, import_react10.useEffect)(() => {
    measure();
    const well = wellRef.current;
    if (well === null) return;
    const observer = new ResizeObserver(() => measure());
    observer.observe(well);
    return () => observer.disconnect();
  }, [measure]);
  (0, import_react10.useEffect)(() => {
    setRect((current) => fitAspect(current, box2, props.aspect));
  }, [box2, props.aspect]);
  const onDown = (handle2) => (event) => {
    event.stopPropagation();
    event.preventDefault();
    event.currentTarget.setPointerCapture(event.pointerId);
    drag.current = { handle: handle2, x: event.clientX, y: event.clientY, rect };
  };
  const onMove = (event) => {
    const active = drag.current;
    if (active === void 0) return;
    event.stopPropagation();
    setRect(applyHandle(active.rect, event.clientX - active.x, event.clientY - active.y, active.handle, box2, props.aspect));
  };
  const onUp = () => {
    drag.current = void 0;
  };
  const confirm = async () => {
    const img = imgRef.current;
    if (img === null || img.naturalWidth === 0 || rect.w < 8 || busy) return;
    setBusy(true);
    try {
      const sx = (rect.x - box2.x) / box2.w * img.naturalWidth;
      const sy = (rect.y - box2.y) / box2.h * img.naturalHeight;
      const sw = rect.w / box2.w * img.naturalWidth;
      const sh = rect.h / box2.h * img.naturalHeight;
      const canvas = document.createElement("canvas");
      canvas.width = Math.max(1, Math.round(sw));
      canvas.height = Math.max(1, Math.round(sh));
      canvas.getContext("2d")?.drawImage(img, sx, sy, sw, sh, 0, 0, canvas.width, canvas.height);
      const blob = await new Promise((resolve) => canvas.toBlob(resolve, "image/jpeg", 0.92));
      if (blob === null) return;
      props.onConfirm(blob);
    } finally {
      setBusy(false);
    }
  };
  const handles = ["n", "s", "e", "w", "ne", "nw", "se", "sw"];
  return /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)(
    "div",
    {
      ref: wellRef,
      className: "nodrag nopan",
      onPointerMove: onMove,
      onPointerUp: onUp,
      onPointerCancel: onUp,
      style: { position: "absolute", inset: 0, zIndex: 8, cursor: "default" },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("img", { ref: imgRef, src: props.src, alt: "", onLoad: measure, style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain", pointerEvents: "none" } }),
        /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("div", { style: { position: "absolute", inset: 0, background: "rgba(0,0,0,.45)", clipPath: `polygon(evenodd, 0 0, 100% 0, 100% 100%, 0 100%, 0 0, ${rect.x}px ${rect.y}px, ${rect.x}px ${rect.y + rect.h}px, ${rect.x + rect.w}px ${rect.y + rect.h}px, ${rect.x + rect.w}px ${rect.y}px)` } }),
        /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)(
          "div",
          {
            onPointerDown: onDown("move"),
            style: {
              position: "absolute",
              left: rect.x,
              top: rect.y,
              width: rect.w,
              height: rect.h,
              boxSizing: "border-box",
              border: "1px solid #fff",
              boxShadow: "0 0 0 1px rgba(0,0,0,.35)",
              cursor: "move",
              zIndex: 2
            },
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("span", { style: { position: "absolute", left: "33%", top: 0, bottom: 0, width: 1, background: "rgba(255,255,255,.28)" } }),
              /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("span", { style: { position: "absolute", left: "66%", top: 0, bottom: 0, width: 1, background: "rgba(255,255,255,.28)" } }),
              /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("span", { style: { position: "absolute", top: "33%", left: 0, right: 0, height: 1, background: "rgba(255,255,255,.28)" } }),
              /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("span", { style: { position: "absolute", top: "66%", left: 0, right: 0, height: 1, background: "rgba(255,255,255,.28)" } })
            ]
          }
        ),
        handles.map((handle2) => /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("div", { className: "nodrag nopan", onPointerDown: onDown(handle2), style: handleStyle(handle2, rect) }, handle2)),
        /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("button", { type: "button", className: "nodrag nopan dx-hit dx-crop-apply", disabled: busy, onClick: () => {
          void confirm();
        }, hidden: true })
      ]
    }
  );
}
function CropBar(props) {
  const [open, setOpen] = (0, import_react10.useState)(false);
  return /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)(
    "div",
    {
      className: "nowheel nopan nodrag",
      onPointerDown: (event) => event.stopPropagation(),
      style: {
        position: "absolute",
        left: "50%",
        bottom: 18,
        transform: "translateX(-50%)",
        zIndex: 32,
        display: "flex",
        alignItems: "center",
        gap: 8,
        height: 48,
        padding: "4px 8px",
        borderRadius: 999,
        background: "rgba(31,31,31,.88)",
        border: "1px solid rgba(255,255,255,.10)",
        backdropFilter: "blur(16px)",
        color: dx.ink,
        fontFamily: dx.font
      },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("button", { type: "button", className: "dx-hit", title: "\u53D6\u6D88\u88C1\u526A", style: { ...dxGhostBtn, width: 36, height: 36, borderRadius: 999 }, onClick: props.onCancel, children: /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(IconClose, { size: 16 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { style: { position: "relative" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
            "button",
            {
              type: "button",
              className: "dx-hit",
              style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 12px", borderRadius: 999, fontSize: 12, background: "rgba(255,255,255,.08)" },
              onClick: () => setOpen((current) => !current),
              children: "\u5BBD\u9AD8\u6BD4"
            }
          ),
          open ? /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("div", { style: {
            position: "absolute",
            bottom: 40,
            left: "50%",
            transform: "translateX(-50%)",
            minWidth: 112,
            padding: 6,
            borderRadius: 12,
            background: "#1f1f1f",
            border: "1px solid rgba(255,255,255,.1)"
          }, children: CROP_ASPECTS.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
            "button",
            {
              type: "button",
              className: "dx-menu-item",
              style: {
                display: "block",
                width: "100%",
                textAlign: "left",
                padding: "7px 10px",
                border: "none",
                borderRadius: 8,
                background: props.aspect === item3.id ? "rgba(255,255,255,.1)" : "transparent",
                color: dx.ink,
                fontSize: 12,
                fontFamily: dx.font,
                cursor: "pointer"
              },
              onClick: () => {
                props.onAspect(item3.id);
                setOpen(false);
              },
              children: item3.label
            },
            item3.id
          )) }) : null
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)(
          "button",
          {
            type: "button",
            className: "dx-hit",
            disabled: props.busy === true,
            style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 14px", borderRadius: 999, fontSize: 12, background: "#f3f3f3", color: "#141414", gap: 6 },
            onClick: props.onConfirm,
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(IconCheck, { size: 14 }),
              "\u786E\u8BA4\u88C1\u526A"
            ]
          }
        )
      ]
    }
  );
}

// src/client/stage/ImageMore.tsx
var import_react11 = require("react");
var import_jsx_runtime12 = require("react/jsx-runtime");
function ImageMore(props) {
  const [open, setOpen] = (0, import_react11.useState)(false);
  const [grid, setGrid] = (0, import_react11.useState)(void 0);
  const rows = [
    { label: "\u6269\u56FE", run: props.onExpand },
    { label: "\u64E6\u9664", run: props.onErase },
    { label: "\u6807\u6CE8", run: props.onAnnotate },
    { label: "\u589E\u5F3A", run: props.onEnhance },
    { label: "\u8C03\u6574\u50CF\u7D20", run: props.onPixels },
    { label: "\u62A0\u56FE", run: props.onCutout }
  ];
  return /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("span", { style: { position: "relative", display: "grid" }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
      "button",
      {
        type: "button",
        className: "nodrag nopan dx-hit",
        title: "\u66F4\u591A",
        "data-tip": "\u66F4\u591A",
        onClick: (event) => {
          event.stopPropagation();
          setOpen((value) => !value);
          setGrid(void 0);
        },
        style: {
          width: 40,
          height: 40,
          border: "none",
          borderRadius: 9999,
          background: "transparent",
          color: "rgba(245,245,245,.9)",
          cursor: "pointer",
          display: "grid",
          placeItems: "center",
          padding: 0
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(IconMore, { size: 14 })
      }
    ),
    open ? /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)(
      "div",
      {
        className: "nodrag nopan",
        style: {
          position: "absolute",
          top: "100%",
          left: "50%",
          transform: "translateX(-50%)",
          marginTop: 8,
          width: 280,
          padding: 6,
          borderRadius: 14,
          background: "rgba(18,18,18,.96)",
          border: "1px solid rgba(255,255,255,.12)",
          boxShadow: "0 16px 40px rgba(0,0,0,.45)",
          zIndex: 9
        },
        children: [
          rows.map((row3) => /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
            "button",
            {
              type: "button",
              className: "dx-menu-item",
              style: {
                display: "flex",
                width: "100%",
                height: 34,
                padding: "0 10px",
                border: "none",
                borderRadius: 8,
                background: "transparent",
                color: "rgba(245,245,245,.92)",
                fontSize: 12,
                cursor: "pointer",
                textAlign: "left"
              },
              onClick: (event) => {
                event.stopPropagation();
                setOpen(false);
                row3.run();
              },
              children: row3.label
            },
            row3.label
          )),
          /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 6, height: 34, padding: "0 10px", position: "relative" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("span", { style: { flex: 1, fontSize: 12, color: "rgba(245,245,245,.92)" }, children: "\u5FEB\u901F\u5207\u5206" }),
            [{ c: 2, r: 2 }, { c: 3, r: 3 }, { c: 4, r: 4 }].map((item3) => /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)(
              "button",
              {
                type: "button",
                className: "dx-hit",
                style: { ...dxGhostBtn, width: "auto", height: 22, padding: "0 6px", fontSize: 10 },
                onClick: (event) => {
                  event.stopPropagation();
                  setOpen(false);
                  props.onSplit(item3.c, item3.r);
                },
                children: [
                  item3.c,
                  "\xD7",
                  item3.r
                ]
              },
              `${item3.c}x${item3.r}`
            )),
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
              "button",
              {
                type: "button",
                className: "dx-hit",
                title: "\u81EA\u9009\u884C\u5217",
                style: { ...dxGhostBtn, width: 22, height: 22, fontSize: 11 },
                onMouseEnter: () => setGrid({ cols: 4, rows: 3 }),
                onClick: (event) => event.stopPropagation(),
                children: "\u203A"
              }
            ),
            grid !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)(
              "div",
              {
                style: {
                  position: "absolute",
                  left: "100%",
                  top: 0,
                  marginLeft: 8,
                  padding: 8,
                  borderRadius: 12,
                  background: "rgba(18,18,18,.96)",
                  border: "1px solid rgba(255,255,255,.12)"
                },
                onMouseLeave: () => setGrid(void 0),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(8, 16px)", gap: 3 }, children: Array.from({ length: 64 }, (_3, index2) => {
                    const col = index2 % 8 + 1;
                    const row3 = Math.floor(index2 / 8) + 1;
                    const on = col <= grid.cols && row3 <= grid.rows;
                    return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
                      "button",
                      {
                        type: "button",
                        "aria-label": `${col}\xD7${row3}`,
                        style: {
                          width: 16,
                          height: 16,
                          padding: 0,
                          borderRadius: 3,
                          border: "none",
                          cursor: "pointer",
                          background: on ? "rgba(255,255,255,.88)" : "rgba(255,255,255,.12)"
                        },
                        onMouseEnter: () => setGrid({ cols: col, rows: row3 }),
                        onClick: (event) => {
                          event.stopPropagation();
                          setOpen(false);
                          setGrid(void 0);
                          props.onSplit(col, row3);
                        }
                      },
                      index2
                    );
                  }) }),
                  /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { style: { marginTop: 6, textAlign: "center", fontSize: 11, color: "rgba(245,245,245,.7)" }, children: [
                    grid.cols,
                    " \xD7 ",
                    grid.rows
                  ] })
                ]
              }
            ) : null
          ] })
        ]
      }
    ) : null
  ] });
}

// src/client/stage/MaskOverlay.tsx
var import_react12 = require("react");
var import_react_dom2 = require("react-dom");
var import_jsx_runtime13 = require("react/jsx-runtime");
function checker() {
  return "repeating-conic-gradient(#d0d0d0 0% 25%, #8d8d8d 0% 50%) 0 0 / 16px 16px";
}
function contentBox2(wellW, wellH, imgW, imgH) {
  if (imgW <= 0 || imgH <= 0 || wellW <= 0 || wellH <= 0) return { x: 0, y: 0, w: wellW, h: wellH };
  const wellR = wellW / wellH;
  const imgR = imgW / imgH;
  if (imgR > wellR) {
    const w2 = wellW;
    const h2 = wellW / imgR;
    return { x: 0, y: (wellH - h2) / 2, w: w2, h: h2 };
  }
  const h = wellH;
  const w = wellH * imgR;
  return { x: (wellW - w) / 2, y: 0, w, h };
}
function hostOf() {
  return document.querySelector(".dx-stage") ?? document.body;
}
function MaskOverlay(props) {
  const wellRef = (0, import_react12.useRef)(null);
  const imgRef = (0, import_react12.useRef)(null);
  const canvasRef = (0, import_react12.useRef)(null);
  const stroke = (0, import_react12.useRef)(void 0);
  const rectStart = (0, import_react12.useRef)(void 0);
  const lasso = (0, import_react12.useRef)([]);
  const undoStack = (0, import_react12.useRef)([]);
  const redoStack = (0, import_react12.useRef)([]);
  const [tool, setTool] = (0, import_react12.useState)("brush");
  const [size, setSize] = (0, import_react12.useState)(28);
  const [prompt, setPrompt] = (0, import_react12.useState)("");
  const [maskUrl, setMaskUrl] = (0, import_react12.useState)("");
  const [busy, setBusy] = (0, import_react12.useState)(false);
  const [box2, setBox] = (0, import_react12.useState)({ x: 0, y: 0, w: 0, h: 0 });
  const [rubber, setRubber] = (0, import_react12.useState)(void 0);
  const ctxOf = () => canvasRef.current?.getContext("2d") ?? null;
  const publish = () => {
    const canvas = canvasRef.current;
    if (canvas === null) return;
    setMaskUrl(canvas.toDataURL("image/png"));
  };
  const snapshot2 = () => {
    const canvas = canvasRef.current;
    const ctx = ctxOf();
    if (canvas === null || ctx === null) return;
    undoStack.current.push(ctx.getImageData(0, 0, canvas.width, canvas.height));
    if (undoStack.current.length > 24) undoStack.current.shift();
    redoStack.current = [];
  };
  const measure = (0, import_react12.useCallback)(() => {
    const well = wellRef.current;
    const img = imgRef.current;
    const canvas = canvasRef.current;
    if (well === null || img === null || canvas === null || img.naturalWidth === 0) return;
    setBox(contentBox2(well.clientWidth, well.clientHeight, img.naturalWidth, img.naturalHeight));
    if (canvas.width !== img.naturalWidth || canvas.height !== img.naturalHeight) {
      canvas.width = img.naturalWidth;
      canvas.height = img.naturalHeight;
      ctxOf()?.clearRect(0, 0, canvas.width, canvas.height);
      undoStack.current = [];
      redoStack.current = [];
      publish();
    }
  }, []);
  (0, import_react12.useEffect)(() => {
    measure();
    const well = wellRef.current;
    if (well === null) return;
    const observer = new ResizeObserver(() => measure());
    observer.observe(well);
    return () => observer.disconnect();
  }, [measure, props.src]);
  const toCanvas = (event) => {
    const canvas = canvasRef.current;
    if (canvas === null || box2.w < 1 || box2.h < 1) return { x: 0, y: 0 };
    return {
      x: (event.clientX - canvas.getBoundingClientRect().left) / box2.w * canvas.width,
      y: (event.clientY - canvas.getBoundingClientRect().top) / box2.h * canvas.height
    };
  };
  const brushWidth = () => {
    const canvas = canvasRef.current;
    if (canvas === null || box2.w < 1) return size;
    return size * (canvas.width / box2.w);
  };
  const paintBrush = (from, to) => {
    const ctx = ctxOf();
    if (ctx === null) return;
    ctx.strokeStyle = "#fff";
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.lineWidth = brushWidth();
    ctx.beginPath();
    ctx.moveTo(from.x, from.y);
    ctx.lineTo(to.x, to.y);
    ctx.stroke();
  };
  const fillRect = (a, b) => {
    const ctx = ctxOf();
    if (ctx === null) return;
    ctx.fillStyle = "#fff";
    ctx.fillRect(Math.min(a.x, b.x), Math.min(a.y, b.y), Math.abs(b.x - a.x), Math.abs(b.y - a.y));
  };
  const fillLasso = (points) => {
    const ctx = ctxOf();
    if (ctx === null || points.length < 3) return;
    ctx.fillStyle = "#fff";
    ctx.beginPath();
    ctx.moveTo(points[0].x, points[0].y);
    for (const point of points.slice(1)) ctx.lineTo(point.x, point.y);
    ctx.closePath();
    ctx.fill();
  };
  const onDown = (event) => {
    event.stopPropagation();
    event.preventDefault();
    event.currentTarget.setPointerCapture(event.pointerId);
    snapshot2();
    const point = toCanvas(event);
    if (tool === "brush") {
      stroke.current = point;
      paintBrush(point, point);
      publish();
    } else if (tool === "rect") {
      rectStart.current = point;
      setRubber({ x: point.x, y: point.y, w: 0, h: 0 });
    } else {
      lasso.current = [point];
    }
  };
  const onMove = (event) => {
    if (stroke.current === void 0 && rectStart.current === void 0 && lasso.current.length === 0) return;
    event.stopPropagation();
    const point = toCanvas(event);
    if (tool === "brush" && stroke.current !== void 0) {
      paintBrush(stroke.current, point);
      stroke.current = point;
      publish();
      return;
    }
    if (tool === "rect" && rectStart.current !== void 0) {
      const origin = rectStart.current;
      setRubber({
        x: Math.min(origin.x, point.x),
        y: Math.min(origin.y, point.y),
        w: Math.abs(point.x - origin.x),
        h: Math.abs(point.y - origin.y)
      });
      return;
    }
    if (tool === "lasso") lasso.current.push(point);
  };
  const onUp = (event) => {
    event.stopPropagation();
    if (tool === "rect" && rectStart.current !== void 0) fillRect(rectStart.current, toCanvas(event));
    if (tool === "lasso") fillLasso(lasso.current);
    stroke.current = void 0;
    rectStart.current = void 0;
    lasso.current = [];
    setRubber(void 0);
    publish();
  };
  const undo = () => {
    const canvas = canvasRef.current;
    const ctx = ctxOf();
    const prev = undoStack.current.pop();
    if (canvas === null || ctx === null || prev === void 0) return;
    redoStack.current.push(ctx.getImageData(0, 0, canvas.width, canvas.height));
    ctx.putImageData(prev, 0, 0);
    publish();
  };
  const redo = () => {
    const canvas = canvasRef.current;
    const ctx = ctxOf();
    const next = redoStack.current.pop();
    if (canvas === null || ctx === null || next === void 0) return;
    undoStack.current.push(ctx.getImageData(0, 0, canvas.width, canvas.height));
    ctx.putImageData(next, 0, 0);
    publish();
  };
  const submit = async () => {
    const canvas = canvasRef.current;
    if (canvas === null || busy) return;
    if (props.mode === "redraw" && prompt.trim() === "") return;
    setBusy(true);
    try {
      const blob = await new Promise((resolve) => canvas.toBlob(resolve, "image/png"));
      if (blob === null) return;
      props.onSubmit(blob, props.mode === "erase" ? "\u64E6\u9664\u8499\u7248\u533A\u57DF\uFF0C\u7528\u5468\u56F4\u5185\u5BB9\u81EA\u7136\u586B\u8865\uFF0C\u5176\u4F59\u753B\u9762\u4FDD\u6301\u4E0D\u53D8" : prompt.trim());
    } finally {
      setBusy(false);
    }
  };
  const hint2 = props.mode === "erase" ? "\u7ED8\u5236\u8499\u7248\u4EE5\u64E6\u9664" : "\u7ED8\u5236\u8499\u7248\u4EE5\u91CD\u7ED8";
  const canSend = props.mode === "erase" || prompt.trim() !== "";
  const scaleX = box2.w / Math.max(1, canvasRef.current?.width ?? 1);
  const scaleY = box2.h / Math.max(1, canvasRef.current?.height ?? 1);
  const bar = /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { className: "nodrag nopan dx-mask-bar", style: {
    position: "absolute",
    left: "50%",
    top: -56,
    transform: "translateX(-50%)",
    display: "flex",
    alignItems: "center",
    gap: 6,
    height: 44,
    padding: "0 8px",
    borderRadius: 999,
    background: "rgba(18,18,18,.92)",
    border: "1px solid rgba(255,255,255,.12)",
    backdropFilter: "blur(16px)",
    zIndex: 9,
    whiteSpace: "nowrap"
  }, children: [
    props.label !== void 0 && props.label !== "" ? /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("span", { style: { maxWidth: 180, overflow: "hidden", textOverflow: "ellipsis", fontSize: 12, color: "rgba(245,245,245,.8)", padding: "0 6px" }, children: props.label }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("button", { type: "button", className: "dx-hit", title: "\u53D6\u6D88", onClick: props.onCancel, style: { ...dxGhostBtn, width: 32, height: 32 }, children: /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(IconClose, { size: 14 }) }),
    ["brush", "rect", "lasso"].map((id2) => /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "button",
      {
        type: "button",
        className: "dx-hit",
        title: id2 === "brush" ? "\u7B14\u5237" : id2 === "rect" ? "\u6846\u9009" : "\u5957\u7D22",
        onClick: () => setTool(id2),
        style: { ...dxGhostBtn, width: 32, height: 32, background: tool === id2 ? "rgba(255,255,255,.16)" : "transparent" },
        children: id2 === "brush" ? "\u25CF" : id2 === "rect" ? "\u25A1" : "\u223F"
      },
      id2
    )),
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("input", { type: "range", min: 8, max: 72, value: size, onChange: (event) => setSize(Number(event.target.value)), style: { width: 84 } }),
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("button", { type: "button", className: "dx-hit", title: "\u64A4\u9500", onClick: undo, style: { ...dxGhostBtn, width: 32, height: 32 }, children: /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(IconUndo, { size: 14 }) }),
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("button", { type: "button", className: "dx-hit", title: "\u91CD\u505A", onClick: redo, style: { ...dxGhostBtn, width: 32, height: 32, opacity: redoStack.current.length === 0 ? 0.35 : 1 }, children: /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(IconRedo, { size: 14 }) })
  ] });
  const dock = /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { className: "nodrag nopan dx-mask-dock", style: {
    position: "absolute",
    left: "50%",
    bottom: 28,
    transform: "translateX(-50%)",
    display: "flex",
    alignItems: "center",
    gap: 10,
    width: 640,
    maxWidth: "calc(100% - 48px)",
    minHeight: 56,
    padding: "10px 14px",
    borderRadius: 18,
    background: "rgba(18,18,18,.94)",
    border: "1px solid rgba(255,255,255,.12)",
    boxShadow: "0 18px 40px rgba(0,0,0,.4)",
    zIndex: 28,
    fontFamily: dx.font
  }, children: [
    props.mode === "redraw" ? /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "input",
      {
        className: "nodrag nopan",
        value: prompt,
        placeholder: "\u63CF\u8FF0\u4F60\u60F3\u6539\u53D8\u4EC0\u4E48...",
        onChange: (event) => setPrompt(event.target.value),
        style: { flex: 1, minWidth: 0, height: 34, border: "none", background: "transparent", color: dx.ink, fontSize: 14, outline: "none", fontFamily: dx.font }
      }
    ) : /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("span", { style: { flex: 1, color: dx.mute, fontSize: 13 } }),
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("span", { style: { color: dx.mute, fontSize: 12, whiteSpace: "nowrap" }, children: hint2 }),
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "button",
      {
        type: "button",
        className: "dx-cta",
        disabled: busy || !canSend,
        onClick: () => {
          void submit();
        },
        style: { ...dxPill, width: 36, height: 36, opacity: busy || !canSend ? 0.4 : 1 },
        children: /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(IconCheck, { size: 15 })
      }
    )
  ] });
  return /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { ref: wellRef, className: "nodrag nopan dx-mask-overlay", style: { position: "absolute", inset: 0, zIndex: 8, overflow: "visible" }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("img", { ref: imgRef, src: props.src, alt: "", onLoad: measure, style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain", pointerEvents: "none" } }),
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "canvas",
      {
        ref: canvasRef,
        style: {
          position: "absolute",
          left: box2.x,
          top: box2.y,
          width: box2.w,
          height: box2.h,
          opacity: 0,
          pointerEvents: "none"
        }
      }
    ),
    maskUrl !== "" ? /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("div", { style: {
      position: "absolute",
      left: box2.x,
      top: box2.y,
      width: box2.w,
      height: box2.h,
      background: checker(),
      WebkitMaskImage: `url(${maskUrl})`,
      maskImage: `url(${maskUrl})`,
      WebkitMaskSize: "100% 100%",
      maskSize: "100% 100%",
      pointerEvents: "none",
      opacity: 0.72
    } }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "div",
      {
        onPointerDown: onDown,
        onPointerMove: onMove,
        onPointerUp: onUp,
        onPointerCancel: onUp,
        style: { position: "absolute", left: box2.x, top: box2.y, width: box2.w, height: box2.h, cursor: "crosshair" }
      }
    ),
    rubber !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime13.jsx)("div", { style: {
      position: "absolute",
      left: box2.x + rubber.x * scaleX,
      top: box2.y + rubber.y * scaleY,
      width: rubber.w * scaleX,
      height: rubber.h * scaleY,
      border: "1px solid #fff",
      pointerEvents: "none"
    } }) : null,
    bar,
    (0, import_react_dom2.createPortal)(dock, hostOf())
  ] });
}

// src/client/stage/ExpandOverlay.tsx
var import_react13 = require("react");
var import_react_dom3 = require("react-dom");
var import_jsx_runtime14 = require("react/jsx-runtime");
function contentBox3(wellW, wellH, imgW, imgH) {
  if (imgW <= 0 || imgH <= 0 || wellW <= 0 || wellH <= 0) return { x: 0, y: 0, w: wellW, h: wellH };
  const wellR = wellW / wellH;
  const imgR = imgW / imgH;
  if (imgR > wellR) {
    const w2 = wellW;
    const h2 = wellW / imgR;
    return { x: 0, y: (wellH - h2) / 2, w: w2, h: h2 };
  }
  const h = wellH;
  const w = wellH * imgR;
  return { x: (wellW - w) / 2, y: 0, w, h };
}
function grow(start2, dx2, dy, handle2, well, inner) {
  let x3 = start2.x;
  let y2 = start2.y;
  let w = start2.w;
  let h = start2.h;
  if (handle2.includes("w")) {
    x3 += dx2;
    w -= dx2;
  }
  if (handle2.includes("e")) w += dx2;
  if (handle2.includes("n")) {
    y2 += dy;
    h -= dy;
  }
  if (handle2.includes("s")) h += dy;
  x3 = Math.max(well.x, Math.min(x3, inner.x));
  y2 = Math.max(well.y, Math.min(y2, inner.y));
  w = Math.max(inner.x + inner.w - x3, Math.min(w, well.x + well.w - x3));
  h = Math.max(inner.y + inner.h - y2, Math.min(h, well.y + well.h - y2));
  return { x: x3, y: y2, w, h };
}
var handleCursor2 = {
  n: "ns-resize",
  s: "ns-resize",
  e: "ew-resize",
  w: "ew-resize",
  ne: "nesw-resize",
  sw: "nesw-resize",
  nw: "nwse-resize",
  se: "nwse-resize"
};
function handleStyle2(handle2, rect) {
  const size = 10;
  const half = size / 2;
  const midX = rect.x + rect.w / 2 - half;
  const midY = rect.y + rect.h / 2 - half;
  const left = handle2.includes("w") ? rect.x - half : handle2.includes("e") ? rect.x + rect.w - half : midX;
  const top = handle2.includes("n") ? rect.y - half : handle2.includes("s") ? rect.y + rect.h - half : midY;
  return {
    position: "absolute",
    left,
    top,
    width: size,
    height: size,
    borderRadius: 2,
    background: "#fff",
    boxShadow: "0 0 0 1px rgba(0,0,0,.45)",
    cursor: handleCursor2[handle2],
    zIndex: 3
  };
}
function hostOf2() {
  return document.querySelector(".dx-stage") ?? document.body;
}
function ExpandOverlay(props) {
  const wellRef = (0, import_react13.useRef)(null);
  const imgRef = (0, import_react13.useRef)(null);
  const drag = (0, import_react13.useRef)(void 0);
  const [well, setWell] = (0, import_react13.useState)({ x: 0, y: 0, w: 0, h: 0 });
  const [inner, setInner] = (0, import_react13.useState)({ x: 0, y: 0, w: 0, h: 0 });
  const [outer, setOuter] = (0, import_react13.useState)({ x: 0, y: 0, w: 0, h: 0 });
  const [busy, setBusy] = (0, import_react13.useState)(false);
  const measure = (0, import_react13.useCallback)(() => {
    const root2 = wellRef.current;
    const img = imgRef.current;
    if (root2 === null || img === null || img.naturalWidth === 0) return;
    const nextWell = { x: 0, y: 0, w: root2.clientWidth, h: root2.clientHeight };
    const nextInner = contentBox3(nextWell.w, nextWell.h, img.naturalWidth, img.naturalHeight);
    setWell(nextWell);
    setInner(nextInner);
    setOuter((current) => current.w < 8 ? nextInner : grow(current, 0, 0, "se", nextWell, nextInner));
  }, []);
  (0, import_react13.useEffect)(() => {
    measure();
    const root2 = wellRef.current;
    if (root2 === null) return;
    const observer = new ResizeObserver(() => measure());
    observer.observe(root2);
    return () => observer.disconnect();
  }, [measure]);
  const onDown = (handle2) => (event) => {
    event.stopPropagation();
    event.preventDefault();
    event.currentTarget.setPointerCapture(event.pointerId);
    drag.current = { handle: handle2, x: event.clientX, y: event.clientY, rect: outer };
  };
  const onMove = (event) => {
    const active = drag.current;
    if (active === void 0) return;
    event.stopPropagation();
    setOuter(grow(active.rect, event.clientX - active.x, event.clientY - active.y, active.handle, well, inner));
  };
  const onUp = () => {
    drag.current = void 0;
  };
  const confirm = async () => {
    const img = imgRef.current;
    if (img === null || img.naturalWidth === 0 || outer.w < 8 || busy) return;
    setBusy(true);
    try {
      const scale = img.naturalWidth / inner.w;
      const width = Math.max(1, Math.round(outer.w * scale));
      const height = Math.max(1, Math.round(outer.h * scale));
      const dx2 = (inner.x - outer.x) * scale;
      const dy = (inner.y - outer.y) * scale;
      const padded = document.createElement("canvas");
      padded.width = width;
      padded.height = height;
      const ctx = padded.getContext("2d");
      if (ctx === null) return;
      ctx.fillStyle = "#000";
      ctx.fillRect(0, 0, width, height);
      ctx.drawImage(img, dx2, dy, img.naturalWidth, img.naturalHeight);
      const mask = document.createElement("canvas");
      mask.width = width;
      mask.height = height;
      const maskCtx = mask.getContext("2d");
      if (maskCtx === null) return;
      maskCtx.fillStyle = "#fff";
      maskCtx.fillRect(0, 0, width, height);
      maskCtx.fillStyle = "#000";
      maskCtx.fillRect(dx2, dy, img.naturalWidth, img.naturalHeight);
      const paddedBlob = await new Promise((resolve) => padded.toBlob(resolve, "image/png"));
      const maskBlob = await new Promise((resolve) => mask.toBlob(resolve, "image/png"));
      if (paddedBlob === null || maskBlob === null) return;
      props.onConfirm(paddedBlob, maskBlob);
    } finally {
      setBusy(false);
    }
  };
  const handles = ["n", "s", "e", "w", "ne", "nw", "se", "sw"];
  const dock = /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: "nodrag nopan", style: {
    position: "absolute",
    left: "50%",
    bottom: 28,
    transform: "translateX(-50%)",
    display: "flex",
    alignItems: "center",
    gap: 10,
    minHeight: 52,
    padding: "8px 14px",
    borderRadius: 16,
    background: "rgba(18,18,18,.94)",
    border: "1px solid rgba(255,255,255,.12)",
    zIndex: 28,
    fontFamily: dx.font,
    color: dx.mute,
    fontSize: 12
  }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("button", { type: "button", className: "dx-hit", title: "\u53D6\u6D88", onClick: props.onCancel, style: { ...dxGhostBtn, width: 32, height: 32 }, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(IconClose, { size: 14 }) }),
    "\u62D6\u62FD\u5916\u6846\u8FDB\u884C\u6269\u56FE",
    /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("button", { type: "button", className: "dx-cta", disabled: busy, onClick: () => {
      void confirm();
    }, style: { ...dxPill, width: 36, height: 36 }, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(IconCheck, { size: 15 }) })
  ] });
  return /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)(
    "div",
    {
      ref: wellRef,
      className: "nodrag nopan dx-expand-overlay",
      onPointerMove: onMove,
      onPointerUp: onUp,
      onPointerCancel: onUp,
      style: { position: "absolute", inset: 0, zIndex: 8, overflow: "visible" },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { style: { position: "absolute", left: outer.x, top: outer.y, width: outer.w, height: outer.h, background: "#000" } }),
        /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
          "img",
          {
            ref: imgRef,
            src: props.src,
            alt: "",
            onLoad: measure,
            style: { position: "absolute", left: inner.x, top: inner.y, width: inner.w, height: inner.h, objectFit: "fill", pointerEvents: "none" }
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { style: {
          position: "absolute",
          left: outer.x,
          top: outer.y,
          width: outer.w,
          height: outer.h,
          boxSizing: "border-box",
          border: "1px solid #fff",
          boxShadow: "0 0 0 1px rgba(0,0,0,.35)",
          pointerEvents: "none"
        } }),
        handles.map((handle2) => /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: "nodrag nopan", onPointerDown: onDown(handle2), style: handleStyle2(handle2, outer) }, handle2)),
        (0, import_react_dom3.createPortal)(dock, hostOf2())
      ]
    }
  );
}

// src/client/stage/nodes.tsx
var import_jsx_runtime15 = require("react/jsx-runtime");
function mediaUrl(path) {
  return /^https?:\/\//i.test(path) ? path : withProject(`/directorx/media?path=${encodeURIComponent(path)}`);
}
var card3 = {
  width: "100%",
  height: "100%",
  borderRadius: dx.radiusCard,
  overflow: "hidden",
  background: "#1f1f1f",
  border: "none",
  color: dx.ink,
  fontFamily: dx.font,
  boxShadow: "none"
};
var handleBox = {
  position: "absolute",
  width: 28,
  height: 28,
  background: "transparent",
  border: "none",
  zIndex: 6
};
var handleLeft = { ...handleBox, left: -22, right: "auto", top: "50%", transform: "translateY(-50%)" };
var handleRight = { ...handleBox, right: -22, left: "auto", top: "50%", transform: "translateY(-50%)" };
var handleTop = { ...handleBox, top: -20, bottom: "auto", left: "50%", transform: "translateX(-50%)" };
var handleBottom = { ...handleBox, bottom: -20, top: "auto", left: "50%", transform: "translateX(-50%)" };
function Ports() {
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(Handle, { id: "in", type: "target", position: Position.Left, className: "dx-port", isConnectable: true, style: handleLeft, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-port-plus", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconPlus, { size: 13 }) }) }),
    /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(Handle, { id: "top", type: "source", position: Position.Top, className: "dx-port", isConnectable: true, style: handleTop, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-port-dot" }) }),
    /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(Handle, { id: "bottom", type: "source", position: Position.Bottom, className: "dx-port", isConnectable: true, style: handleBottom, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-port-dot" }) }),
    /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(Handle, { id: "out", type: "source", position: Position.Right, className: "dx-port dx-port-source", isConnectable: true, style: handleRight, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-port-plus", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconPlus, { size: 13 }) }) })
  ] });
}
function ToolBtn(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
    "button",
    {
      className: "nodrag nopan dx-hit",
      title: props.title,
      "data-tip": props.title,
      onClick: (event) => {
        event.stopPropagation();
        props.onClick();
      },
      style: {
        width: 40,
        height: 40,
        border: "none",
        borderRadius: 9999,
        background: "transparent",
        color: "rgba(245,245,245,.9)",
        cursor: "pointer",
        display: "grid",
        placeItems: "center",
        padding: 0,
        flexShrink: 0
      },
      children: props.children
    }
  );
}
function NodeFrame(props) {
  const [hot, setHot] = (0, import_react14.useState)(false);
  const active = props.cropping !== true && (props.selected === true || hot);
  const faceClass = [
    "dx-card-face",
    props.kind !== void 0 ? `dx-kind-${props.kind}` : "",
    props.generating === true ? "dx-face-live" : "",
    props.filled === true ? "dx-face-fill" : ""
  ].filter((part) => part !== "").join(" ");
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
    "div",
    {
      className: props.generating === true ? "dx-generating" : void 0,
      onMouseEnter: () => setHot(true),
      onMouseLeave: () => setHot(false),
      style: { width: "100%", height: "100%", overflow: "visible" },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(Ports, {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "div",
          {
            className: "nodrag nopan dx-node-toolbar",
            style: {
              position: "absolute",
              left: "50%",
              top: -80,
              transform: "translateX(-50%)",
              zIndex: 7,
              opacity: active ? 1 : 0,
              pointerEvents: active ? "auto" : "none"
            },
            children: props.toolbar
          }
        ),
        props.title !== void 0 && props.cropping !== true ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "nodrag nopan dx-card-caption", children: props.title }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "div",
          {
            className: faceClass,
            style: {
              ...card3,
              outline: props.selected === true ? `2px solid ${dx.selected}` : "none",
              outlineOffset: 0,
              borderColor: props.failed === true ? "rgba(255,155,143,.7)" : props.generating === true ? "rgba(240,195,106,.62)" : "transparent"
            },
            children: props.children
          }
        ),
        props.dock !== void 0 && props.selected === true && props.cropping !== true ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "nodrag nopan dx-card-dock", children: props.dock }) : null
      ]
    }
  );
}
function useNearViewport() {
  const ref = (0, import_react14.useRef)(null);
  const [near, setNear] = (0, import_react14.useState)(false);
  (0, import_react14.useEffect)(() => {
    const el = ref.current;
    if (el === null) return;
    const observer = new IntersectionObserver(([entry]) => {
      setNear(entry?.isIntersecting === true);
    }, { rootMargin: "120px" });
    observer.observe(el);
    return () => observer.disconnect();
  }, []);
  return [ref, near];
}
function VideoPreview(props) {
  const videoRef = (0, import_react14.useRef)(null);
  const [playing, setPlaying] = (0, import_react14.useState)(false);
  (0, import_react14.useEffect)(() => {
    if (props.active === true) return;
    videoRef.current?.pause();
    setPlaying(false);
  }, [props.active]);
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
    "div",
    {
      className: "nodrag nopan",
      onClick: (event) => {
        event.stopPropagation();
        const video = videoRef.current;
        if (video === null) return;
        if (video.paused) {
          void video.play();
          setPlaying(true);
        } else {
          video.pause();
          setPlaying(false);
        }
      },
      style: { width: "100%", height: "100%", position: "absolute", inset: 0, cursor: "pointer" },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "video",
          {
            ref: videoRef,
            src: props.src,
            muted: true,
            loop: true,
            playsInline: true,
            preload: "metadata",
            onPause: () => setPlaying(false),
            onPlay: () => setPlaying(true),
            style: { width: "100%", height: "100%", objectFit: "cover", display: "block" }
          }
        ),
        playing ? null : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: {
          position: "absolute",
          inset: 0,
          display: "grid",
          placeItems: "center",
          background: "linear-gradient(180deg, transparent 40%, rgba(0,0,0,.35))",
          pointerEvents: "none"
        }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: {
          width: 36,
          height: 36,
          borderRadius: 99,
          display: "grid",
          placeItems: "center",
          background: "rgba(243,243,243,.92)",
          color: "#141414",
          boxShadow: "0 8px 20px rgba(0,0,0,.35)"
        }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconPlay, { size: 15 }) }) })
      ]
    }
  );
}
function AudioPreview(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "nodrag nopan dx-audio-well", children: [
    /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("span", { className: "dx-audio-bars", "aria-hidden": true, children: [
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {})
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("audio", { src: props.src, controls: true, preload: "metadata" })
  ] });
}
function GeneratingHud(props) {
  const line = (props.prompt ?? "").trim();
  const copy = props.kind === "video" ? "\u6B63\u5728\u51FA\u7247" : props.kind === "audio" ? "\u6B63\u5728\u51FA\u58F0" : "\u6B63\u5728\u51FA\u56FE";
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "dx-gen-overlay", "aria-live": "polite", children: [
    /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-gen-scan" }),
    /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "dx-gen-core", children: [
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-gen-ring" }),
      props.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("span", { className: "dx-gen-bars", children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {})
      ] }) : null,
      props.kind === "audio" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("span", { className: "dx-audio-bars", "aria-hidden": true, children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {}),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("i", {})
      ] }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-gen-copy", children: copy }),
      line !== "" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-gen-prompt", children: line.length > 52 ? `${line.slice(0, 52)}\u2026` : line }) : null
    ] })
  ] });
}
function EmptyPlate(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-empty-plate", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-empty-glyph", children: props.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconVideo, { size: 22 }) : props.kind === "audio" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconAudio, { size: 22 }) : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconImage, { size: 22 }) }) });
}
var MediaCard = (0, import_react14.memo)(function MediaCard2(props) {
  const data = props.data;
  const empty2 = data.path === void 0 || data.path === "";
  const [shellRef, near] = useNearViewport();
  const src = empty2 ? "" : mediaUrl(data.path ?? "");
  const incoming = useStore(
    (state) => state.edges.filter((edge) => edge.target === props.id).map((edge) => edge.source).sort().join("|")
  );
  const takeDigest = useStore((state) => {
    const prompt = (data.prompt ?? "").trim();
    if (prompt === "") return "";
    const self2 = state.nodes.find((node) => node.id === props.id);
    const parent = self2?.parentId ?? "";
    return state.nodes.filter(
      (node) => (node.type === "media" || node.type === void 0) && (node.parentId ?? "") === parent && typeof node.data.prompt === "string" && node.data.prompt.trim() === prompt && typeof node.data.path === "string" && node.data.path !== ""
    ).map((node) => `${node.id}	${String(node.data.path)}	${String(node.data.label ?? node.id)}`).join("\n");
  });
  const refs = incoming === "" ? [] : incoming.split("|");
  const takes = takeDigest === "" ? [] : takeDigest.split("\n").map((row3) => {
    const [id2, path, label2] = row3.split("	");
    return { id: id2 ?? "", path: path ?? "", label: label2 ?? "" };
  }).filter((item3) => item3.id !== "");
  const kind = data.kind === "video" ? "video" : data.kind === "audio" ? "audio" : "image";
  const generating = data.shotStatus === "generating";
  const filled = !empty2 && !generating;
  const title = displayCardTitle(data.label, data.prompt, data.shotIndex);
  const spec = {
    kind,
    prompt: data.prompt ?? "",
    ...data.model !== void 0 ? { model: data.model } : {},
    ...data.aspect !== void 0 ? { aspect: data.aspect } : {},
    ...data.count !== void 0 ? { count: data.count } : {},
    ...data.durationSec !== void 0 ? { durationSec: data.durationSec } : {},
    ...data.characters !== void 0 ? { characters: data.characters } : {},
    ...refs.length > 0 ? { refIds: refs } : {},
    sourceId: props.id,
    ...data.revise === true ? { revise: true, targetId: props.id } : {}
  };
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
    NodeFrame,
    {
      selected: props.selected,
      generating,
      failed: data.shotStatus === "failed",
      kind,
      filled,
      cropping: data.cropping === true || data.maskMode !== void 0 || data.expanding === true,
      title: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "dx-card-meta", children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(KindGlyph, { kind, size: 13 }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "input",
          {
            className: "dx-node-title nodrag nopan",
            value: title,
            placeholder: "\u8BF7\u8F93\u5165\u6807\u9898",
            onChange: (event) => data.onRename?.(props.id, event.target.value),
            style: {
              flex: 1,
              minWidth: 0,
              height: 22,
              padding: "0 2px",
              borderRadius: 6,
              border: "1px solid transparent",
              background: "transparent",
              color: "rgba(245,245,245,.92)",
              fontSize: 12,
              fontWeight: 500,
              letterSpacing: -0.2,
              fontFamily: dx.font,
              outline: "none",
              overflow: "hidden",
              textOverflow: "ellipsis",
              whiteSpace: "nowrap"
            }
          }
        ),
        data.locked === true ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-card-lock", title: "\u5DF2\u9501\u5B9A", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconLock, { size: 12 }) }) : null
      ] }),
      dock: props.selected === true && !filled ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        NodeWorkstation,
        {
          spec,
          compact: true,
          onChange: (next) => {
            data.onPatch?.(props.id, {
              prompt: next.prompt,
              model: next.model,
              aspect: next.aspect,
              count: next.count,
              durationSec: next.durationSec,
              kind: next.kind,
              characters: next.characters
            });
            if (next.revise !== true) data.onClearRevise?.();
          },
          onSubmit: () => data.onGenerate?.(props.id),
          onPickRef: data.onPickRef === void 0 ? void 0 : () => data.onPickRef?.(props.id)
        }
      ) : void 0,
      toolbar: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [
        kind === "image" && filled ? /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u88C1\u526A", onClick: () => data.onCrop?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconCrop, { size: 16 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5C40\u90E8\u91CD\u7ED8", onClick: () => data.onRedraw?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconBrush, { size: 14 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
            ImageMore,
            {
              onExpand: () => data.onExpand?.(props.id),
              onErase: () => data.onErase?.(props.id),
              onAnnotate: () => data.onAnnotate?.(props.id),
              onEnhance: () => data.onEnhance?.(props.id),
              onPixels: () => data.onPixels?.(props.id),
              onCutout: () => data.onCutout?.(props.id),
              onSplit: (cols, rows) => data.onSplit?.(props.id, cols, rows)
            }
          )
        ] }) : null,
        kind === "video" && filled ? /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u526A\u8F91", onClick: () => data.onEdit?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconScissors, { size: 14 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u622A\u5E27", onClick: () => data.onCapture?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconImage, { size: 14 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5EF6\u957F\u955C\u5934", onClick: () => data.onExtend?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconPlus, { size: 14 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u89C6\u9891\u91CD\u62CD", onClick: () => data.onReshoot?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconEdit, { size: 14 }) })
        ] }) : null,
        empty2 && data.onUpload !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u4E0A\u4F20", onClick: () => data.onUpload?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconUpload, { size: 14 }) }) : null,
        filled ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u4E0B\u8F7D", onClick: () => data.onDownload?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconDownload, { size: 14 }) }) : null,
        filled && kind !== "audio" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5168\u5C4F", onClick: () => data.onPreview?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconMax, { size: 14 }) }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u590D\u5236", onClick: () => data.onDuplicate?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconCopy, { size: 14 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5220\u9664", onClick: () => data.onDelete?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconTrash, { size: 14 }) })
      ] }),
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          NodeResizer,
          {
            isVisible: props.selected === true && data.cropping !== true && data.maskMode === void 0 && data.expanding !== true,
            minWidth: 240,
            minHeight: kind === "audio" ? 120 : 160,
            keepAspectRatio: data.aspect !== void 0 && data.aspect !== "",
            color: "rgba(255,255,255,.45)",
            lineStyle: { border: "none" },
            handleStyle: { width: 6, height: 6, borderRadius: 1, background: "#f3f3f3", border: "none" }
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
          "div",
          {
            ref: shellRef,
            className: `dx-media-well${kind === "video" && !filled ? " dx-film" : ""}${filled ? " dx-media-fill" : ""}`,
            style: { overflow: data.cropping === true || data.maskMode !== void 0 || data.expanding === true ? "visible" : "hidden" },
            children: [
              data.expanding === true && kind === "image" && !empty2 ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ExpandOverlay, { src, onCancel: () => data.onExpand?.(""), onConfirm: (padded, mask) => data.onExpanded?.(props.id, padded, mask) }) : data.maskMode !== void 0 && kind === "image" && !empty2 ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(MaskOverlay, { src, mode: data.maskMode, onCancel: () => data.onRedraw?.(""), onSubmit: (blob, prompt) => data.onMasked?.(props.id, blob, prompt, data.maskMode ?? "redraw") }) : data.cropping === true && kind === "image" && !empty2 ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(CropBox, { src, aspect: data.cropAspect, onConfirm: (blob) => data.onCropped?.(props.id, blob) }) : empty2 && !generating ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(EmptyPlate, { kind, prompt: data.prompt }) : empty2 ? null : kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-media-bleed", style: { opacity: generating ? 0.38 : 1 }, children: near ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(VideoPreview, { src, active: props.selected === true }) : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { style: { height: "100%", display: "grid", placeItems: "center", color: dx.mute }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconVideo, { size: 22 }) }) }) : kind === "audio" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-media-bleed", style: { opacity: generating ? 0.38 : 1 }, children: near ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(AudioPreview, { src }) : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { style: { height: "100%", display: "grid", placeItems: "center", color: dx.mute }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconAudio, { size: 22 }) }) }) : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-media-bleed", style: { opacity: generating ? 0.38 : 1 }, children: near ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("img", { src, alt: "", className: "dx-media-img" }) : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { style: { height: "100%", display: "grid", placeItems: "center", color: dx.mute }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconImage, { size: 22 }) }) }),
              generating ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(GeneratingHud, { kind, prompt: data.prompt }) : null,
              data.onUpload !== void 0 && !generating && data.cropping !== true ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
                "button",
                {
                  type: "button",
                  className: "nodrag nopan dx-hit dx-replace-chip",
                  title: "\u66FF\u6362",
                  onClick: (event) => {
                    event.stopPropagation();
                    data.onUpload?.(props.id);
                  },
                  children: "\u66FF\u6362"
                }
              ) : null,
              kind === "video" && data.durationSec !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("span", { className: "dx-kind-badge dx-kind-time", children: [
                data.durationSec,
                "s"
              ] }) : null,
              data.shotStatus === "failed" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "nodrag nopan", style: {
                position: "absolute",
                inset: 0,
                background: "rgba(20,8,8,.62)",
                display: "grid",
                placeItems: "center",
                padding: 12,
                textAlign: "center"
              }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { style: { color: "#ff9b8f", fontSize: 12, marginBottom: 8 }, children: data.lastError || "\u751F\u6210\u5931\u8D25" }),
                /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("button", { className: "dx-cta", style: { ...dxPill, width: "auto", height: 28, padding: "0 10px", fontSize: 11 }, onClick: () => data.onGenerate?.(props.id), children: "\u91CD\u8BD5" })
              ] }) }) : null,
              takes.length >= 2 ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "nodrag nopan dx-take-row", style: { position: "absolute", left: 8, right: 8, bottom: 38, display: "flex", gap: 4 }, children: takes.slice(0, 6).map((peer) => {
                const path = peer.path;
                return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
                  "button",
                  {
                    title: peer.label,
                    onClick: (event) => {
                      event.stopPropagation();
                      data.onFocusTake?.(peer.id);
                    },
                    style: {
                      width: 36,
                      height: 24,
                      padding: 0,
                      borderRadius: 4,
                      overflow: "hidden",
                      border: peer.id === props.id ? "1px solid #fff" : `1px solid ${dx.hairline}`,
                      background: "#111"
                    },
                    children: path !== "" ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("img", { src: mediaUrl(path), alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : null
                  },
                  peer.id
                );
              }) }) : null
            ]
          }
        )
      ]
    }
  );
});
var TextCard = (0, import_react14.memo)(function TextCard2(props) {
  const data = props.data;
  const [editing, setEditing] = (0, import_react14.useState)(false);
  const textRef = (0, import_react14.useRef)(null);
  const wrap = (mark) => {
    const area = textRef.current;
    if (editing && area !== null) {
      const start2 = area.selectionStart;
      const end = area.selectionEnd;
      const value = area.value;
      const next = `${value.slice(0, start2)}${mark}${value.slice(start2, end) || "\u6587\u672C"}${mark}${value.slice(end)}`;
      area.value = next;
      data.onRename?.(props.id, next);
      return;
    }
    data.onRename?.(props.id, `${mark}${data.label || "\u6587\u672C"}${mark}`);
  };
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
    NodeFrame,
    {
      selected: props.selected,
      kind: "text",
      toolbar: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "H1", onClick: () => data.onRename?.(props.id, `# ${data.label || "\u6807\u9898"}`), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: { fontSize: 11 }, children: "H1" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "H2", onClick: () => data.onRename?.(props.id, `## ${data.label || "\u6807\u9898"}`), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: { fontSize: 11 }, children: "H2" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "H3", onClick: () => data.onRename?.(props.id, `### ${data.label || "\u6807\u9898"}`), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: { fontSize: 11 }, children: "H3" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u7C97\u4F53", onClick: () => wrap("**"), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: { fontWeight: 700, fontSize: 12 }, children: "B" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u659C\u4F53", onClick: () => wrap("*"), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: { fontStyle: "italic", fontSize: 12 }, children: "I" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5217\u8868", onClick: () => data.onRename?.(props.id, `- ${data.label || "\u6761\u76EE"}`), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: { fontSize: 12 }, children: "\u2261" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u590D\u5236", onClick: () => data.onDuplicate?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconCopy, { size: 14 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5220\u9664", onClick: () => data.onDelete?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconTrash, { size: 14 }) })
      ] }),
      dock: props.selected === true ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        NodeWorkstation,
        {
          spec: { kind: "image", prompt: data.label ?? "" },
          compact: true,
          onChange: (next) => data.onRename?.(props.id, next.prompt),
          onSubmit: () => data.onGenerate?.(props.id)
        }
      ) : void 0,
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          NodeResizer,
          {
            isVisible: props.selected === true,
            minWidth: 180,
            minHeight: 120,
            color: "rgba(255,255,255,.45)",
            lineStyle: { border: "none" },
            handleStyle: { width: 7, height: 7, borderRadius: 2, background: "#f3f3f3", border: "none" }
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-text-sheet", style: { padding: 16, height: "100%", boxSizing: "border-box" }, children: editing ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "textarea",
          {
            ref: textRef,
            className: "nodrag nopan",
            autoFocus: true,
            defaultValue: data.label,
            onBlur: (event) => {
              data.onRename?.(props.id, event.target.value);
              setEditing(false);
            },
            placeholder: "\u53CC\u51FB\u5F00\u59CB\u7F16\u8F91...",
            style: {
              width: "100%",
              height: "100%",
              resize: "none",
              border: "none",
              background: "transparent",
              color: dx.ink,
              fontSize: 13,
              lineHeight: 1.55,
              fontFamily: dx.font,
              outline: "none"
            }
          }
        ) : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "div",
          {
            style: { fontSize: 13, lineHeight: 1.55, minHeight: 40, color: data.label ? dx.ink : dx.dim },
            onDoubleClick: () => setEditing(true),
            children: data.label ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(MarkdownView, { text: data.label }) : "\u53CC\u51FB\u5F00\u59CB\u7F16\u8F91..."
          }
        ) })
      ]
    }
  );
});
var GroupCard = (0, import_react14.memo)(function GroupCard2(props) {
  const data = props.data;
  const [editing, setEditing] = (0, import_react14.useState)(false);
  const childCount = useStore((state) => state.nodes.filter((node) => node.parentId === props.id).length);
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { style: { width: "100%", height: "100%", overflow: "visible" }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: "dx-act-frame", style: {
    width: "100%",
    height: "100%",
    borderRadius: 22,
    border: `1px solid ${props.selected === true ? "rgba(255,244,228,.28)" : "rgba(255,255,255,.08)"}`,
    background: "linear-gradient(180deg, rgba(255,244,228,.045), rgba(255,255,255,.018))",
    color: dx.ink,
    padding: 12,
    fontSize: 13,
    fontFamily: dx.font,
    boxShadow: props.selected === true ? "inset 0 0 0 1px rgba(255,255,255,.1), 0 18px 40px rgba(0,0,0,.28)" : "inset 0 1px 0 rgba(255,255,255,.04)"
  }, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
    "div",
    {
      className: "nodrag nopan",
      onDoubleClick: () => setEditing(true),
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 8,
        color: dx.ink,
        fontWeight: 500,
        fontSize: 12,
        padding: "7px 12px",
        borderRadius: 999,
        background: "rgba(12,12,12,.62)",
        border: `1px solid ${dx.hairline}`,
        backdropFilter: "blur(12px)"
      },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(KindGlyph, { kind: "group", size: 13 }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { style: { color: dx.dim, fontSize: 10, letterSpacing: 0.6, textTransform: "uppercase" }, children: "\u5E55" }),
        editing ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "input",
          {
            autoFocus: true,
            defaultValue: data.label,
            onBlur: (event) => {
              data.onRename?.(props.id, event.target.value);
              setEditing(false);
            },
            onKeyDown: (event) => {
              if (event.key === "Enter") event.target.blur();
              if (event.key === "Escape") setEditing(false);
            },
            style: {
              width: 120,
              border: "none",
              outline: "none",
              background: "transparent",
              color: dx.ink,
              fontSize: 12,
              fontFamily: dx.font
            }
          }
        ) : data.label || "\u5206\u7EC4",
        childCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("span", { style: { color: dx.mute, fontSize: 10 }, children: [
          childCount,
          " \u955C"
        ] }) : null
      ]
    }
  ) }) });
});
var DirectorStageCard = (0, import_react14.memo)(function DirectorStageCard2(props) {
  const data = props.data;
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
    NodeFrame,
    {
      selected: props.selected,
      kind: "director-stage",
      title: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "dx-card-meta", children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(KindGlyph, { kind: "director-stage", size: 13 }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-shot-no", style: { flex: 1, minWidth: 0, overflow: "hidden", textOverflow: "ellipsis" }, children: data.label ?? "3D \u5BFC\u6F14\u53F0" }),
        data.locked === true ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-card-lock", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconLock, { size: 12 }) }) : null
      ] }),
      toolbar: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u8FDB\u5165\u7247\u573A", onClick: () => data.onEdit?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconBox, { size: 16 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u590D\u5236", onClick: () => data.onDuplicate?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconCopy, { size: 16 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5220\u9664", onClick: () => data.onDelete?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconTrash, { size: 16 }) })
      ] }),
      children: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "dx-empty-plate", children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-empty-glyph", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconBox, { size: 26 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("button", { type: "button", className: "nodrag nopan dx-hit dx-enter-chip", onClick: (event) => {
          event.stopPropagation();
          data.onEdit?.(props.id);
        }, children: "\u8FDB\u5165\u7247\u573A" })
      ] })
    }
  );
});
var EditStageCard = (0, import_react14.memo)(function EditStageCard2(props) {
  const data = props.data;
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
    NodeFrame,
    {
      selected: props.selected,
      kind: "edit",
      title: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "dx-card-meta", children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(KindGlyph, { kind: "edit", size: 13 }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-shot-no", style: { flex: 1, minWidth: 0, overflow: "hidden", textOverflow: "ellipsis" }, children: data.label ?? "\u526A\u8F91\u53F0" }),
        data.locked === true ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-card-lock", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconLock, { size: 12 }) }) : null
      ] }),
      toolbar: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u6253\u5F00\u526A\u8F91", onClick: () => data.onEdit?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconScissors, { size: 16 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u590D\u5236", onClick: () => data.onDuplicate?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconCopy, { size: 16 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ToolBtn, { title: "\u5220\u9664", onClick: () => data.onDelete?.(props.id), children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconTrash, { size: 16 }) })
      ] }),
      children: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "dx-empty-plate", children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "dx-empty-glyph", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(IconScissors, { size: 26 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("button", { type: "button", className: "nodrag nopan dx-hit dx-enter-chip", onClick: (event) => {
          event.stopPropagation();
          data.onEdit?.(props.id);
        }, children: "\u6253\u5F00\u526A\u8F91" })
      ] })
    }
  );
});
var stageNodeTypes = { media: MediaCard, text: TextCard, group: GroupCard, "director-stage": DirectorStageCard, edit: EditStageCard };

// src/client/stage/chrome.tsx
var import_jsx_runtime16 = require("react/jsx-runtime");
var menu = {
  ...dxChrome,
  position: "absolute",
  zIndex: 30,
  minWidth: 240,
  maxWidth: "min(280px, calc(100vw - 24px))",
  padding: 8,
  borderRadius: 16,
  overflow: "hidden",
  background: "#1f1f1f"
};
var item2 = {
  display: "flex",
  alignItems: "center",
  gap: 10,
  width: "100%",
  minWidth: 0,
  boxSizing: "border-box",
  textAlign: "left",
  padding: "8px 10px",
  border: "none",
  borderRadius: 9,
  background: "transparent",
  color: dx.ink,
  fontSize: 13,
  fontFamily: dx.font,
  cursor: "pointer",
  overflowWrap: "anywhere"
};
function viewSize() {
  return {
    w: typeof window === "undefined" ? 1200 : window.innerWidth,
    h: typeof window === "undefined" ? 800 : window.innerHeight
  };
}
var kbd = {
  display: "inline-flex",
  alignItems: "center",
  justifyContent: "center",
  minWidth: 20,
  height: 20,
  padding: "0 6px",
  borderRadius: 6,
  border: `1px solid ${dx.hairline}`,
  background: "rgba(255,255,255,.05)",
  color: dx.mute,
  fontSize: 10,
  letterSpacing: 0.2,
  fontFamily: dx.font
};
function MenuLabel(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-menu-label", style: {
    padding: "7px 10px 3px",
    fontSize: 10,
    letterSpacing: 0.7,
    color: dx.dim,
    fontWeight: 600
  }, children: props.children });
}
function TitlePill(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
    "input",
    {
      value: props.value,
      placeholder: "\u8BF7\u8F93\u5165\u6807\u9898",
      onChange: (event) => props.onChange(event.target.value),
      className: "dx-title",
      style: {
        width: "min(220px, 42vw)",
        maxWidth: "100%",
        height: 32,
        boxSizing: "border-box",
        padding: "0 12px",
        borderRadius: 999,
        textAlign: "center",
        fontSize: 13,
        fontWeight: 500,
        letterSpacing: 0.1,
        outline: "none",
        background: "transparent",
        border: `1px solid ${dx.hairline}`,
        color: dx.ink,
        fontFamily: dx.font
      }
    }
  );
}
function TopBar(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-topbar", style: {
    position: "absolute",
    top: 16,
    left: 80,
    right: 14,
    zIndex: 24,
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start",
    pointerEvents: "none"
  }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "dx-topbar-cluster", style: {
    ...dxChrome,
    pointerEvents: "auto",
    display: "flex",
    alignItems: "center",
    gap: 4,
    padding: 4,
    borderRadius: 999,
    maxWidth: "min(720px, calc(100% - 8px))"
  }, children: [
    props.projects.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(ProjectChip, { projects: props.projects, value: props.project, onChange: props.onProject, embedded: true }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(TitlePill, { value: props.title, onChange: props.onTitle }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "dx-topbar-meta", style: {
      height: 32,
      padding: "0 10px",
      borderRadius: 999,
      display: "flex",
      alignItems: "center",
      gap: 7,
      fontSize: 11,
      color: dx.mute,
      letterSpacing: 0.2,
      flexShrink: 0
    }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { className: props.saveState === "\u672A\u4FDD\u5B58" ? "dx-dot-warn" : "dx-dot" }),
      props.saveState,
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { opacity: 0.4 }, children: "\xB7" }),
      props.count,
      " \u955C"
    ] }),
    props.onClose !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 32, height: 32, borderRadius: 999 }, onClick: props.onClose, title: "\u5173\u95ED\u753B\u5E03", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconClose, { size: 14 }) }) : null
  ] }) });
}
function ProjectChip(props) {
  const [open, setOpen] = (0, import_react16.useState)(false);
  const root2 = (0, import_react16.useRef)(null);
  const current = props.projects.find((item3) => item3.path === props.value) ?? props.projects[0];
  (0, import_react16.useEffect)(() => {
    if (!open) return;
    const onDoc = (event) => {
      if (root2.current !== null && !root2.current.contains(event.target)) setOpen(false);
    };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { ref: root2, style: { position: "relative" }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
      "button",
      {
        className: "dx-hit",
        title: "\u5F53\u524D\u9879\u76EE\uFF08\u6BCF\u4E2A\u5DE5\u4F5C\u533A\u4E00\u4EFD\u753B\u5E03\uFF09",
        onClick: () => setOpen((value) => !value),
        style: {
          ...props.embedded === true ? {} : dxChrome,
          height: 32,
          padding: "0 10px 0 8px",
          borderRadius: 999,
          display: "flex",
          alignItems: "center",
          gap: 6,
          maxWidth: 168,
          fontSize: 12,
          fontWeight: 500,
          background: props.embedded === true ? "transparent" : dxChrome.background,
          border: props.embedded === true ? "none" : dxChrome.border
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconFolder, { size: 14 }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: current?.title || current?.path || "\u9879\u76EE" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconChevron, { size: 12 })
        ]
      }
    ),
    open ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { ...menu, left: 0, top: 44, minWidth: 220 }, children: props.projects.map((project) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
      "button",
      {
        className: "dx-menu-item",
        style: item2,
        onClick: () => {
          props.onChange(project.path);
          setOpen(false);
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { flex: 1, overflow: "hidden", textOverflow: "ellipsis" }, children: project.title || project.path }),
          project.path === props.value ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconCheck, { size: 14 }) : null
        ]
      },
      project.path
    )) }) : null
  ] });
}
function StageRail(props) {
  const rows = [
    { label: "\u6DFB\u52A0\u8282\u70B9", onClick: props.onAdd, icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconPlus, {}), primary: true },
    { label: "\u641C\u7D22\u8282\u70B9 \u2318K", onClick: props.onSearch, icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSearch, {}) },
    { label: "\u8D44\u6E90\u5E93", onClick: props.onAssets, icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGrid, {}) },
    { label: "\u4E0A\u4F20", onClick: props.onUpload, icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconUpload, {}) },
    { label: "\u5FEB\u6377\u952E ?", onClick: props.onHelp, icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconHelp, {}) }
  ];
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-rail", style: {
    position: "absolute",
    top: "50%",
    left: 16,
    transform: "translateY(-50%)",
    zIndex: 28,
    display: "flex",
    flexDirection: "column",
    gap: 8,
    padding: 8,
    width: 54,
    boxSizing: "border-box",
    borderRadius: 9999,
    background: "rgba(22,22,22,.82)",
    border: "1px solid rgba(255,255,255,.10)",
    backdropFilter: "blur(40px)",
    WebkitBackdropFilter: "blur(40px)"
  }, children: rows.map((row3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
    "button",
    {
      type: "button",
      className: "dx-rail-btn dx-hit",
      title: row3.label,
      "aria-label": row3.label,
      "data-tip": row3.label,
      "data-tip-side": "right",
      onClick: row3.onClick,
      "data-dx-menu-anchor": row3.primary === true ? "" : void 0,
      style: row3.primary === true ? { ...dxPill, width: 36, height: 36 } : { ...dxGhostBtn, width: 36, height: 36 },
      children: row3.icon
    },
    row3.label
  )) });
}
function MenuShell(props) {
  const pos = clampMenu(props.x, props.y, props.width ?? 220, props.height ?? 280, viewSize());
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
    "div",
    {
      "data-dx-menu": "",
      style: {
        ...menu,
        position: "fixed",
        zIndex: 40,
        left: pos.left,
        top: pos.top,
        width: "min(220px, calc(100vw - 24px))",
        minWidth: props.width ?? 196,
        maxWidth: "calc(100vw - 24px)",
        maxHeight: "min(72vh, 560px)",
        overflowY: "auto"
      },
      onMouseDown: (event) => event.stopPropagation(),
      onWheel: (event) => event.stopPropagation(),
      onContextMenu: (event) => event.preventDefault(),
      children: props.children
    }
  );
}
function MenuRowButton(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
    "button",
    {
      type: "button",
      className: "dx-menu-item",
      style: { ...item2, ...props.row.danger === true ? { color: "var(--dsw-alias-state-error-primary)" } : {} },
      onClick: props.onClick,
      children: [
        props.icon !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { opacity: 0.7, display: "flex" }, children: props.icon }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { flex: 1 }, children: props.row.label }),
        props.row.kbd !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { ...kbd, marginLeft: 8 }, children: props.row.kbd }) : null
      ]
    }
  );
}
function ConnectMenu(props) {
  const rows = [
    { kind: "image", label: "\u56FE\u7247", icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconImage, { size: 15 }) },
    { kind: "video", label: "\u89C6\u9891", icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconVideo, { size: 15 }) },
    { kind: "audio", label: "\u97F3\u9891", icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconAudio, { size: 15 }) },
    { kind: "text", label: "\u6587\u672C", icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconText, { size: 15 }) },
    { kind: "director-stage", label: "3D \u5BFC\u6F14\u53F0", icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSpark, { size: 15 }) },
    { kind: "edit", label: "\u526A\u8F91\u53F0", icon: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconScissors, { size: 15 }) }
  ];
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(MenuShell, { x: props.x, y: props.y, height: 236, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { padding: "6px 10px 8px", fontSize: 11, color: dx.mute }, children: "\u6DFB\u52A0\u8282\u70B9" }),
    rows.map((row3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { type: "button", className: "dx-menu-item", style: item2, onClick: () => props.onPick(row3.kind), children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { opacity: 0.7, display: "flex" }, children: row3.icon }),
      row3.label
    ] }, row3.kind))
  ] });
}
var ADD_ICON = {
  image: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconImage, { size: 15 }),
  video: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconVideo, { size: 15 }),
  audio: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconAudio, { size: 15 }),
  text: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconText, { size: 15 }),
  script: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconText, { size: 15 }),
  group: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGroup, { size: 15 }),
  "director-stage": /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSpark, { size: 15 }),
  edit: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconScissors, { size: 15 }),
  "edit-image": /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconEdit, { size: 15 }),
  "edit-video": /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconVideo, { size: 15 }),
  upload: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconUpload, { size: 15 }),
  assets: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGrid, { size: 15 }),
  paste: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconCopy, { size: 15 })
};
function AddMenu(props) {
  const mode = props.mode ?? "full";
  const groups = groupMenuRows(addMenuRows(mode));
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(MenuShell, { x: props.x, y: props.y, height: mode === "quick" ? 240 : 460, children: groups.map((group, index2) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { children: [
    index2 > 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { height: 1, margin: "6px 8px 2px", background: "var(--dsw-alias-border-l2)" } }) : null,
    group.label !== "" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(MenuLabel, { children: group.label }) : null,
    group.rows.map((row3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
      MenuRowButton,
      {
        row: row3,
        icon: ADD_ICON[row3.id],
        onClick: () => {
          if (row3.id === "paste") {
            props.onPaste?.();
            return;
          }
          props.onPick(row3.id);
        }
      },
      row3.id
    ))
  ] }, group.id)) });
}
function NodeMenu(props) {
  const rows = nodeMenuRows(props.surface);
  const nestCraft = shouldNestCraft(rows);
  const [craftOpen, setCraftOpen] = (0, import_react16.useState)(false);
  const groups = groupMenuRows(rows);
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(MenuShell, { x: props.x, y: props.y, height: Math.min(520, 56 + rows.length * 40), children: groups.map((group, index2) => {
    if (group.id === "craft" && nestCraft) {
      return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { children: [
        index2 > 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { height: 1, margin: "6px 8px 2px", background: "var(--dsw-alias-border-l2)" } }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { type: "button", className: "dx-menu-item", style: item2, onClick: () => setCraftOpen((open) => !open), children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { flex: 1 }, children: "\u5DE5\u5177" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconChevron, { size: 12 })
        ] }),
        craftOpen ? group.rows.map((row3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(MenuRowButton, { row: row3, onClick: () => props.onAction(row3.id) }, row3.id)) : null
      ] }, group.id);
    }
    return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { children: [
      index2 > 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { height: 1, margin: "6px 8px 2px", background: "var(--dsw-alias-border-l2)" } }) : null,
      group.label !== "" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(MenuLabel, { children: group.label }) : null,
      group.rows.map((row3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(MenuRowButton, { row: row3, onClick: () => props.onAction(row3.id) }, row3.id))
    ] }, group.id);
  }) });
}
function EdgeMenu(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(MenuShell, { x: props.x, y: props.y, width: 160, height: 56, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { type: "button", className: "dx-menu-item", style: { ...item2, color: "var(--dsw-alias-state-error-primary)" }, onClick: props.onDelete, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconTrash, { size: 15 }),
    "\u5220\u9664\u8FDE\u7EBF"
  ] }) });
}
function ReshootDialog(props) {
  const fallback = Math.max(2, Math.min(8, props.durationSec ?? 5));
  const [start2, setStart] = (0, import_react16.useState)("0");
  const [end, setEnd] = (0, import_react16.useState)(String(fallback));
  const [prompt, setPrompt] = (0, import_react16.useState)(props.prompt);
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
    "div",
    {
      style: {
        ...dxChrome,
        position: "absolute",
        left: "50%",
        top: "50%",
        transform: "translate(-50%, -50%)",
        zIndex: 40,
        width: "min(360px, calc(100vw - 24px))",
        maxHeight: "calc(100% - 24px)",
        boxSizing: "border-box",
        overflowY: "auto",
        padding: 16,
        borderRadius: 14
      },
      onMouseDown: (event) => event.stopPropagation(),
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 14, fontWeight: 600, marginBottom: 6 }, children: "\u5C40\u90E8\u91CD\u7ED8" }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 12, color: dx.mute, marginBottom: 12 }, children: "\u5207\u6389\u5934\u5C3E\uFF0C\u4E2D\u6BB5\u91CD\u65B0\u751F\u6210\u540E\u518D\u62FC\u63A5\u3002\u7A97\u957F 1\u201315 \u79D2\u3002" }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", gap: 8, marginBottom: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("label", { style: { flex: 1, minWidth: 0, fontSize: 11, color: dx.mute }, children: [
            "\u8D77\u70B9\u79D2",
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("input", { className: "nodrag nopan", value: start2, onChange: (event) => setStart(event.target.value), style: { display: "block", width: "100%", minWidth: 0, boxSizing: "border-box", marginTop: 4, height: 32, borderRadius: 8, border: `1px solid ${dx.hairline}`, background: "rgba(255,255,255,.04)", color: dx.ink, padding: "0 10px" } })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("label", { style: { flex: 1, minWidth: 0, fontSize: 11, color: dx.mute }, children: [
            "\u7EC8\u70B9\u79D2",
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("input", { className: "nodrag nopan", value: end, onChange: (event) => setEnd(event.target.value), style: { display: "block", width: "100%", minWidth: 0, boxSizing: "border-box", marginTop: 4, height: 32, borderRadius: 8, border: `1px solid ${dx.hairline}`, background: "rgba(255,255,255,.04)", color: dx.ink, padding: "0 10px" } })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
          "textarea",
          {
            className: "nodrag nopan",
            value: prompt,
            onChange: (event) => setPrompt(event.target.value),
            placeholder: "\u8FD9\u4E00\u6BB5\u8981\u6539\u6210\u4EC0\u4E48",
            style: { width: "100%", minWidth: 0, boxSizing: "border-box", height: 72, resize: "vertical", borderRadius: 10, border: `1px solid ${dx.hairline}`, background: "rgba(255,255,255,.04)", color: dx.ink, padding: 10, fontSize: 13, fontFamily: dx.font, marginBottom: 12 }
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", justifyContent: "flex-end", gap: 8 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 12px" }, onClick: props.onCancel, children: "\u53D6\u6D88" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
            "button",
            {
              className: "dx-cta",
              style: { ...dxPill, width: "auto", height: 32, padding: "0 14px", fontSize: 12 },
              onClick: () => props.onSubmit({ start: Number(start2), end: Number(end), prompt }),
              children: "\u5F00\u59CB\u91CD\u7ED8"
            }
          )
        ] })
      ]
    }
  );
}
function ParseSheet(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
    "div",
    {
      className: "dx-parse-sheet",
      style: {
        position: "absolute",
        left: 72,
        right: 24,
        bottom: 16,
        zIndex: 29,
        display: "flex",
        justifyContent: "center",
        pointerEvents: "none"
      },
      children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
        "div",
        {
          style: {
            ...dxChrome,
            width: "min(720px, 100%)",
            maxHeight: "min(520px, calc(100vh - 32px))",
            boxSizing: "border-box",
            pointerEvents: "auto",
            padding: 12,
            borderRadius: 14
          },
          onMouseDown: (event) => event.stopPropagation(),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 10 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 13, fontWeight: 600 }, children: "\u667A\u80FD\u89E3\u6790" }),
              /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { fontSize: 12, color: dx.mute }, children: [
                props.sourceLabel,
                " \xB7 ",
                props.shots.length,
                " \u955C"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { flex: 1 } }),
              /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 30, padding: "0 10px" }, onClick: props.onCancel, children: "\u53D6\u6D88" }),
              /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-cta", style: { ...dxPill, width: "auto", height: 30, padding: "0 12px", fontSize: 12 }, disabled: props.busy === true, onClick: props.onApply, children: "\u5E94\u7528\u5230\u753B\u5E03" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "flex", gap: 8, overflowX: "auto", paddingBottom: 2 }, children: props.shots.map((shot) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
              "div",
              {
                style: {
                  flex: "0 0 148px",
                  borderRadius: 10,
                  border: `1px solid ${dx.hairline}`,
                  background: "rgba(255,255,255,.04)",
                  overflow: "hidden"
                },
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { height: 84, background: "var(--dsw-alias-bg-base)", display: "grid", placeItems: "center", color: dx.dim, fontSize: 11 }, children: shot.framePath !== void 0 && shot.framePath !== "" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("img", { src: `/directorx/media?path=${encodeURIComponent(shot.framePath)}`, alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : `${shot.start.toFixed(1)}s` }),
                  /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { padding: "7px 8px 8px" }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { fontSize: 11, fontWeight: 600 }, children: [
                      "\u955C",
                      shot.index
                    ] }),
                    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { fontSize: 10, color: dx.mute, marginTop: 2 }, children: [
                      shot.start.toFixed(1),
                      "\u2013",
                      shot.end.toFixed(1),
                      "s"
                    ] }),
                    shot.description !== null && shot.description !== "" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 10, color: dx.dim, marginTop: 4, lineHeight: 1.4, maxHeight: 32, overflow: "hidden" }, children: shot.description }) : null
                  ] })
                ]
              },
              shot.index
            )) })
          ]
        }
      )
    }
  );
}
function ZoomHud(props) {
  const percent = Math.round(props.zoom * 100);
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "dx-zoomhud", style: {
    position: "absolute",
    left: 16,
    bottom: 16,
    zIndex: 22,
    display: "flex",
    alignItems: "center",
    gap: 4,
    padding: "6px 10px 6px 6px",
    borderRadius: 999,
    fontSize: 12,
    background: "rgba(22,22,22,.82)",
    border: "1px solid rgba(255,255,255,.10)",
    backdropFilter: "blur(16px)",
    color: dx.ink,
    fontFamily: dx.font
  }, children: [
    props.onHideWires !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28, background: props.hideWires === true ? "rgba(255,255,255,.12)" : "transparent" }, onClick: props.onHideWires, title: "\u9690\u85CF\u8FDE\u7EBF", "data-tip": "\u9690\u85CF\u8FDE\u7EBF", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconUnlink, { size: 13 }) }) : null,
    props.onSnap !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28, background: props.snap === true ? "rgba(255,255,255,.12)" : "transparent" }, onClick: props.onSnap, title: "\u7F51\u683C\u5438\u9644", "data-tip": "\u7F51\u683C\u5438\u9644", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGrid, { size: 13 }) }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28 }, onClick: props.onFit, title: "\u9002\u914D\u753B\u5E03", "data-tip": "\u9002\u914D\u753B\u5E03", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconFit, { size: 14 }) }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28 }, onClick: () => props.onZoom(Math.max(dx.minZoom, props.zoom / 1.15)), title: "\u7F29\u5C0F", "data-tip": "\u7F29\u5C0F", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconMinus, { size: 14 }) }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
      "input",
      {
        type: "range",
        min: 15,
        max: 200,
        value: percent,
        onChange: (event) => props.onZoom(Number(event.target.value) / 100),
        className: "dx-range",
        style: { width: 84 }
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28 }, onClick: () => props.onZoom(Math.min(dx.maxZoom, props.zoom * 1.15)), title: "\u653E\u5927", "data-tip": "\u653E\u5927", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconPlus, { size: 14 }) }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", disabled: !props.canUndo, style: { ...dxGhostBtn, width: 28, height: 28, opacity: props.canUndo ? 1 : 0.35 }, onClick: props.onUndo, title: "\u64A4\u9500 \u2318Z", "data-tip": "\u64A4\u9500 \u2318Z", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconUndo, { size: 14 }) }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", disabled: !props.canRedo, style: { ...dxGhostBtn, width: 28, height: 28, opacity: props.canRedo ? 1 : 0.35 }, onClick: props.onRedo, title: "\u91CD\u505A \u21E7\u2318Z", "data-tip": "\u91CD\u505A \u21E7\u2318Z", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconRedo, { size: 14 }) })
  ] });
}
function EmptyHero(props) {
  const ghost = {
    ...dxGhostBtn,
    width: "auto",
    height: 34,
    padding: "0 12px",
    gap: 6,
    fontSize: 12,
    border: `1px solid ${dx.hairline}`,
    background: "rgba(255,255,255,.04)"
  };
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-hero", style: { position: "absolute", inset: 0, display: "grid", placeItems: "center", pointerEvents: "none", zIndex: 5, padding: "72px 16px 128px" }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { textAlign: "center", fontFamily: dx.font, maxWidth: 440, padding: "0 20px" }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-hero-mark", style: {
      width: 76,
      height: 44,
      borderRadius: 10,
      margin: "0 auto 18px",
      display: "grid",
      placeItems: "center",
      background: "rgba(255,244,228,.04)",
      border: "1px dashed rgba(255,236,210,.24)",
      color: "#fff"
    }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSpark, { size: 18 }) }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 30, fontWeight: 600, letterSpacing: -0.7, color: dx.ink }, children: "\u4ECA\u5929\u62CD\u4EC0\u4E48\uFF1F" }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { marginTop: 10, color: dx.mute, fontSize: 13, lineHeight: 1.7 }, children: "\u53CC\u51FB\u955C\u5934\u5C45\u4E2D \xB7 \u53CC\u51FB\u7A7A\u767D\u6DFB\u52A0\u8282\u70B9 \xB7 \u53F3\u952E\u6253\u5F00\u83DC\u5355 \xB7 Tab \u6362\u955C" }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "dx-hero-keys", style: { marginTop: 16, display: "flex", justifyContent: "center", flexWrap: "wrap", gap: 8, color: dx.dim, fontSize: 11 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: kbd, children: "G" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { children: "\u751F\u6210" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: kbd, children: "\u2318K" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { children: "\u641C\u7D22\u8282\u70B9" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: kbd, children: "\u2318J" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { children: "DSH" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: kbd, children: "Space" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { children: "\u5E73\u79FB" })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { marginTop: 22, display: "flex", justifyContent: "center", flexWrap: "wrap", gap: 8, pointerEvents: "auto" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
        "button",
        {
          className: "dx-cta",
          onClick: props.onGenerate,
          style: {
            ...dxPill,
            width: "auto",
            height: 42,
            padding: "0 18px",
            fontSize: 13,
            fontWeight: 600,
            gap: 8
          },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSpark, { size: 15 }),
            "\u5F00\u59CB\u751F\u6210"
          ]
        }
      ),
      props.onAdd !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", onClick: props.onAdd, style: ghost, children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconPlus, { size: 14 }),
        "\u6DFB\u52A0\u8282\u70B9"
      ] }) : null,
      props.onScript !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", onClick: props.onScript, style: ghost, children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconText, { size: 14 }),
        "\u6DFB\u52A0\u6587\u672C"
      ] }) : null
    ] })
  ] }) });
}
function SearchPalette(props) {
  const filters = [
    { id: "", label: "\u5168\u90E8" },
    { id: "image", label: "\u56FE\u7247" },
    { id: "video", label: "\u89C6\u9891" },
    { id: "text", label: "\u6587\u672C" },
    { id: "group", label: "\u7F16\u7EC4" }
  ];
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-scrim", style: { position: "absolute", inset: 0, zIndex: 35, display: "grid", justifyItems: "center", alignItems: "start", paddingTop: 88 }, onMouseDown: props.onClose, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
    "div",
    {
      style: { ...dxChrome, width: "min(440px, calc(100% - 32px))", padding: 8, borderRadius: 18 },
      onMouseDown: (event) => event.stopPropagation(),
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "flex", gap: 4, padding: "4px 8px 8px" }, children: filters.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
          "button",
          {
            className: "dx-hit",
            onClick: () => props.onFilter(item3.id),
            style: {
              ...dxGhostBtn,
              width: "auto",
              height: 24,
              padding: "0 8px",
              fontSize: 11,
              background: props.filter === item3.id ? "rgba(255,255,255,.12)" : "transparent"
            },
            children: item3.label
          },
          item3.id
        )) }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "6px 8px 10px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { color: dx.mute, display: "flex" }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSearch, { size: 16 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
            "input",
            {
              autoFocus: true,
              value: props.query,
              placeholder: "\u641C\u7D22\u8282\u70B9\uFF08\u2318K\uFF09",
              onChange: (event) => props.onQuery(event.target.value),
              onKeyDown: (event) => {
                if (event.key === "Escape") props.onClose();
                if (event.key === "Enter" && props.hits[0] !== void 0) props.onPick(props.hits[0].id);
              },
              style: {
                flex: 1,
                border: "none",
                outline: "none",
                background: "transparent",
                color: dx.ink,
                fontSize: 14,
                fontFamily: dx.font
              }
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: kbd, children: "esc" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { maxHeight: 320, overflow: "auto", paddingBottom: 4 }, children: [
          props.hits.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { padding: "14px 10px", fontSize: 12, color: dx.mute }, children: "\u6CA1\u6709\u5339\u914D\u7684\u8282\u70B9" }) : null,
          props.hits.map((hit, index2) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-menu-item", style: item2, onClick: () => props.onPick(hit.id), children: [
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { opacity: 0.65, display: "flex" }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(KindGlyph, { kind: hit.kind, size: 14 }) }),
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { flex: 1, textAlign: "left", overflow: "hidden", textOverflow: "ellipsis" }, children: hit.label || hit.id }),
            index2 === 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { ...kbd, opacity: 0.7 }, children: "\u21B5" }) : null
          ] }, hit.id))
        ] })
      ]
    }
  ) });
}
function CompareOverlay(props) {
  const pane = (side) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { flex: 1, minWidth: 0, display: "flex", flexDirection: "column", gap: 10 }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 12, color: dx.mute, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", fontWeight: 500 }, children: side.label }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { flex: 1, minHeight: 220, background: "var(--dsw-alias-bg-base)", borderRadius: 12, overflow: "hidden", border: `1px solid ${dx.hairline}` }, children: side.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("video", { src: side.path, controls: true, style: { width: "100%", height: "100%", objectFit: "contain" } }) : /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("img", { src: side.path, alt: "", style: { width: "100%", height: "100%", objectFit: "contain" } }) }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-cta", style: { ...dxPill, width: "100%", height: 36, fontSize: 12, fontWeight: 600 }, onClick: () => props.onAdopt(side.id), children: "\u91C7\u7528\u6B64\u6761" })
  ] });
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-scrim", style: { position: "absolute", inset: 0, zIndex: 36, display: "grid", placeItems: "center", padding: "clamp(12px, 3vw, 24px)", overflow: "auto" }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { ...dxChrome, width: "min(980px, 100%)", maxHeight: "min(660px, 100%)", boxSizing: "border-box", padding: 18, borderRadius: 16, display: "flex", flexDirection: "column" }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", gap: 10, marginBottom: 14 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("strong", { style: { fontSize: 14, letterSpacing: -0.2 }, children: "\u53D8\u4F53\u5BF9\u6BD4" }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { type: "button", className: "dx-hit", style: { ...dxGhostBtn, width: 32, height: 32 }, onClick: props.onClose, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconClose, { size: 14 }) })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { flex: 1, minHeight: 0, display: "flex", flexWrap: "wrap", gap: 14, overflow: "auto" }, children: [
      pane(props.left),
      pane(props.right)
    ] })
  ] }) });
}
function GenerateDock(props) {
  const right = props.reserveRight ?? (props.shift === true ? 428 : 24);
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-gendock", style: {
    position: "absolute",
    left: 72,
    right,
    bottom: 16,
    zIndex: 28,
    display: "flex",
    justifyContent: "center",
    pointerEvents: "none"
  }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { width: "min(520px, 100%)", minWidth: 0, pointerEvents: "auto" }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
    NodeWorkstation,
    {
      spec: props.spec,
      busy: props.busy,
      sourceLabel: props.sourceLabel,
      onChange: props.onChange,
      onSubmit: props.onSubmit,
      onClearSource: props.onClearSource,
      inputRef: props.inputRef
    }
  ) }) });
}
function MultiSelectBar(props) {
  const [align, setAlign] = (0, import_react16.useState)(false);
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "dx-multibar", style: {
    ...dxChrome,
    position: "absolute",
    left: "50%",
    bottom: 16,
    transform: "translateX(-50%)",
    zIndex: 27,
    display: "flex",
    alignItems: "center",
    gap: 4,
    padding: 6,
    borderRadius: 999,
    maxWidth: "min(920px, calc(100% - 32px))",
    overflowX: "auto"
  }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("span", { style: { padding: "0 10px", fontSize: 12, color: dx.mute, fontWeight: 500 }, children: [
      "\u5DF2\u9009 ",
      props.count
    ] }),
    props.canCompare ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onCompare, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconCompare, { size: 14 }),
      "\u5BF9\u6BD4"
    ] }) : null,
    props.canPack === true && props.onPack !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onPack, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconVideo, { size: 14 }),
      "\u5408\u6210\u89C6\u9891"
    ] }) : null,
    props.canSheet === true && props.onSheet !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onSheet, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGrid, { size: 14 }),
      "\u4E5D\u5BAB\u683C"
    ] }) : null,
    props.canJoin === true && props.onJoin !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onJoin, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGrid, { size: 14 }),
      "\u5408\u5E76\u5BAB\u683C"
    ] }) : null,
    props.canStack === true && props.onStack !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onStack, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconCompare, { size: 14 }),
      "\u5206\u5C4F"
    ] }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onGroup, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGroup, { size: 14 }),
      "\u7F16\u7EC4"
    ] }),
    props.canUngroup ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onUngroup, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconGroup, { size: 14 }),
      "\u53D6\u6D88\u7F16\u7EC4"
    ] }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { position: "relative" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: () => setAlign((open) => !open), children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconAlign, { size: 14 }),
        "\u5BF9\u9F50"
      ] }),
      align ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { ...menu, left: 0, bottom: 40, top: "auto", minWidth: 168 }, onMouseDown: (event) => event.stopPropagation(), children: [
        [
          ["left", "\u5DE6\u5BF9\u9F50"],
          ["center", "\u6C34\u5E73\u5C45\u4E2D"],
          ["right", "\u53F3\u5BF9\u9F50"],
          ["top", "\u9876\u5BF9\u9F50"],
          ["middle", "\u5782\u76F4\u5C45\u4E2D"],
          ["bottom", "\u5E95\u5BF9\u9F50"]
        ].map(([kind, label2]) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-menu-item", style: item2, onClick: () => {
          props.onAlign(kind);
          setAlign(false);
        }, children: label2 }, kind)),
        props.count >= 3 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(import_jsx_runtime16.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-menu-item", style: item2, onClick: () => {
            props.onDistribute("x");
            setAlign(false);
          }, children: "\u6C34\u5E73\u5206\u5E03" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-menu-item", style: item2, onClick: () => {
            props.onDistribute("y");
            setAlign(false);
          }, children: "\u5782\u76F4\u5206\u5E03" })
        ] }) : null
      ] }) : null
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onLock, children: [
      props.locked ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconUnlock, { size: 14 }) : /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconLock, { size: 14 }),
      props.locked ? "\u89E3\u9501" : "\u9501\u5B9A"
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12 }, onClick: props.onGenerate, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSpark, { size: 14 }),
      "\u751F\u6210"
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 32, padding: "0 10px", gap: 6, fontSize: 12, color: "var(--dsw-alias-state-error-primary)" }, onClick: props.onDelete, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconTrash, { size: 14 }),
      "\u5220\u9664"
    ] })
  ] });
}
function InspectorSheet(props) {
  const status = asShotStatus(props.status) ?? "idea";
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "dx-inspector", style: {
    ...dxChrome,
    position: "absolute",
    top: 72,
    right: 14,
    width: 276,
    maxWidth: "calc(100% - 24px)",
    maxHeight: "calc(100% - 168px)",
    overflow: "auto",
    zIndex: 22,
    padding: 14,
    borderRadius: 20
  }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, color: dx.mute, fontSize: 11, letterSpacing: 0.3, textTransform: "uppercase", marginBottom: 10 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(KindGlyph, { kind: props.kind, size: 13 }),
      props.kindLabel
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
      "input",
      {
        value: props.label,
        onChange: (event) => props.onLabel(event.target.value),
        style: {
          width: "100%",
          boxSizing: "border-box",
          border: "none",
          outline: "none",
          background: "transparent",
          color: dx.ink,
          fontSize: 16,
          fontWeight: 600,
          letterSpacing: -0.3,
          fontFamily: dx.font,
          padding: 0,
          marginBottom: 12
        }
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 11, color: dx.mute, marginBottom: 6 }, children: "\u63D0\u793A\u8BCD" }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
      PromptIpField,
      {
        value: props.prompt,
        placeholder: "\u955C\u5934\u610F\u56FE\uFF0C\u4E0D\u662F\u6210\u7A3F\u3002DSH \u4F1A\u5148\u68C0\u7D22\u518D\u5199\u63D0\u793A\u8BCD",
        onChange: props.onPrompt,
        style: {
          width: "100%",
          minHeight: 88,
          boxSizing: "border-box",
          resize: "vertical",
          borderRadius: 12,
          border: `1px solid ${dx.hairline}`,
          background: "rgba(255,255,255,.04)",
          color: dx.ink,
          padding: 10,
          fontSize: 12,
          lineHeight: 1.55,
          fontFamily: dx.font,
          outline: "none"
        }
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 11, color: dx.mute, margin: "12px 0 6px" }, children: "\u955C\u5934\u72B6\u6001" }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: SHOT_STATUSES.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
      "button",
      {
        className: "dx-hit",
        onClick: () => props.onStatus(item3),
        style: {
          padding: "3px 8px",
          borderRadius: 999,
          border: `1px solid ${item3 === status ? SHOT_STATUS_COLOR[item3] : dx.hairline}`,
          background: item3 === status ? "rgba(255,255,255,.06)" : "transparent",
          color: item3 === status ? SHOT_STATUS_COLOR[item3] : dx.mute,
          fontSize: 11,
          cursor: "pointer",
          fontFamily: dx.font
        },
        children: SHOT_STATUS_LABEL[item3]
      },
      item3
    )) }),
    props.path !== void 0 && props.path !== "" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { marginTop: 12, fontSize: 10, color: dx.dim, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, title: props.path, children: props.path }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", gap: 6, marginTop: 14, flexWrap: "wrap" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("button", { className: "dx-cta", style: { ...dxPill, flex: 1, minWidth: 88, height: 34, fontSize: 12, fontWeight: 600, gap: 6 }, onClick: props.onGenerate, children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconSpark, { size: 13 }),
        "\u751F\u6210"
      ] }),
      props.canEdit ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 34, height: 34, border: `1px solid ${dx.hairline}` }, onClick: props.onEdit, title: "\u6253\u5F00\u7F16\u8F91\u5668", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconEdit, { size: 14 }) }) : null,
      props.canDownload ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 34, height: 34, border: `1px solid ${dx.hairline}` }, onClick: props.onDownload, title: "\u4E0B\u8F7D\u5A92\u4F53", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconDownload, { size: 14 }) }) : null,
      props.canCompare ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 34, height: 34, border: `1px solid ${dx.hairline}` }, onClick: props.onCompare, title: "\u5BF9\u6BD4\u6240\u9009\u53D8\u4F53", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconCompare, { size: 14 }) }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 34, height: 34, border: `1px solid ${dx.hairline}` }, onClick: props.onLock, title: props.locked ? "\u89E3\u9501" : "\u9501\u5B9A", children: props.locked ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconUnlock, { size: 14 }) : /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconLock, { size: 14 }) }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 34, height: 34, border: `1px solid ${dx.hairline}`, color: "var(--dsw-alias-state-error-primary)" }, onClick: props.onDelete, title: "\u5220\u9664\u8282\u70B9", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconTrash, { size: 14 }) })
    ] })
  ] });
}
function AssetDrawer(props) {
  const tabs = [
    { id: "media", label: "\u5A92\u4F53" },
    { id: "character", label: "\u89D2\u8272" },
    { id: "scene", label: "\u573A\u666F" },
    { id: "prop", label: "\u9053\u5177" },
    { id: "style", label: "\u98CE\u683C" },
    { id: "history", label: "\u5386\u53F2" }
  ];
  const q3 = props.query.trim().toLowerCase();
  const match = (blob) => q3 === "" || blob.toLowerCase().includes(q3);
  const media = (props.tab === "history" ? [...props.files].sort((a, b) => (b.at ?? 0) - (a.at ?? 0)) : props.tab === "scene" ? props.files.filter((file) => /场景|scene|set|bg|background|环境|空镜/i.test(`${file.name} ${file.path}`)) : props.tab === "style" ? props.files.filter((file) => /风格|style|look|palette|lut|调色|光影/i.test(`${file.name} ${file.path}`)) : props.files).filter((file) => match(`${file.name} ${file.path}`));
  const characters = props.characters.filter((card4) => {
    if (!match(`${card4.name} ${card4.description ?? ""} ${card4.props ?? ""}`)) return false;
    if (props.tab === "prop") return card4.props !== void 0 && card4.props !== "";
    if (props.tab === "scene") return /场景|scene|set|环境|空镜/i.test(`${card4.description ?? ""} ${card4.outfit ?? ""}`);
    if (props.tab === "style") return /风格|style|look|palette|光影|调色/i.test(`${card4.description ?? ""} ${card4.outfit ?? ""}`);
    return true;
  });
  const showChars = props.tab === "character" || props.tab === "prop" || props.tab === "scene" || props.tab === "style";
  const showMedia = props.tab === "media" || props.tab === "history" || props.tab === "scene" || props.tab === "style";
  const empty2 = (showChars ? characters.length : 0) + (showMedia ? media.length : 0) === 0 ? props.tab === "scene" ? "\u6CA1\u6709\u5339\u914D\u7684\u573A\u666F\u8D44\u4EA7" : props.tab === "prop" ? "\u8FD8\u6CA1\u6709\u9053\u5177\u951A\u70B9" : props.tab === "character" ? "\u8FD8\u6CA1\u6709\u89D2\u8272\u951A\u70B9" : props.tab === "style" ? "\u8FD8\u6CA1\u6709\u98CE\u683C\u8D44\u4EA7" : "\u8F93\u51FA\u76EE\u5F55\u8FD8\u6CA1\u6709\u5A92\u4F53" : void 0;
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "dx-asset-drawer", style: {
    ...dxChrome,
    position: "absolute",
    top: 72,
    left: 68,
    width: 360,
    maxHeight: "calc(100% - 120px)",
    overflow: "auto",
    zIndex: 26,
    padding: 12,
    borderRadius: 18
  }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 10 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("strong", { style: { fontSize: 13, display: "flex", alignItems: "center", gap: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconFolder, { size: 14 }),
        "\u8D44\u6E90\u5E93"
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28 }, onClick: props.onClose, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconClose, { size: 13 }) })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 4, marginBottom: 8 }, children: tabs.map((tab) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
      "button",
      {
        className: "dx-hit",
        onClick: () => props.onTab(tab.id),
        style: {
          ...dxGhostBtn,
          width: "auto",
          height: 24,
          padding: "0 8px",
          fontSize: 11,
          background: props.tab === tab.id ? "rgba(255,255,255,.12)" : "transparent"
        },
        children: tab.label
      },
      tab.id
    )) }),
    /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
      "input",
      {
        value: props.query,
        placeholder: "\u641C\u7D22\u8D44\u6E90",
        onChange: (event) => props.onQuery(event.target.value),
        style: {
          width: "100%",
          boxSizing: "border-box",
          marginBottom: 10,
          height: 30,
          padding: "0 10px",
          borderRadius: 10,
          border: `1px solid ${dx.hairline}`,
          background: "rgba(255,255,255,.04)",
          color: dx.ink,
          fontSize: 12,
          fontFamily: dx.font,
          outline: "none"
        }
      }
    ),
    props.canPin === true && props.onPin !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "flex", gap: 4, marginBottom: 10 }, children: [
      ["character", "\u767B\u8BB0\u4E3A\u89D2\u8272"],
      ["scene", "\u767B\u8BB0\u4E3A\u573A\u666F"],
      ["prop", "\u767B\u8BB0\u4E3A\u9053\u5177"]
    ].map(([role, label2]) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 24, padding: "0 8px", fontSize: 10 }, onClick: () => props.onPin?.(role), children: label2 }, role)) }) : null,
    empty2 !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 12, color: dx.mute, padding: "12px 4px" }, children: empty2 }) : null,
    showChars && characters.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, marginBottom: showMedia ? 10 : 0 }, children: characters.map((card4) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
      "button",
      {
        className: "dx-hit",
        onClick: () => props.onPickCharacter(card4.name, card4.refPath),
        style: {
          padding: 0,
          border: `1px solid ${dx.hairline}`,
          borderRadius: 12,
          overflow: "hidden",
          background: "rgba(255,255,255,.03)",
          color: dx.ink,
          textAlign: "left",
          cursor: "pointer"
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { height: 88, background: "#111", overflow: "hidden" }, children: card4.refPath !== "" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("img", { src: mediaUrl(card4.refPath), alt: "", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }) : /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { height: "100%", display: "grid", placeItems: "center", color: dx.mute }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(KindGlyph, { kind: "image", size: 16 }) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { padding: "6px 8px 8px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 11, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: card4.name }),
            card4.description !== "" ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { fontSize: 10, color: dx.mute, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: card4.description }) : null
          ] })
        ]
      },
      card4.name
    )) }) : null,
    showMedia && media.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }, children: media.map((file) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
      "button",
      {
        className: "dx-hit",
        onClick: () => props.onPick(file),
        title: file.name,
        style: {
          padding: 0,
          border: `1px solid ${dx.hairline}`,
          borderRadius: 12,
          overflow: "hidden",
          background: "rgba(255,255,255,.03)",
          color: dx.ink,
          textAlign: "left",
          cursor: "pointer"
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { height: 88, background: "#111", overflow: "hidden", position: "relative" }, children: [
            file.mediaType.startsWith("video/") ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("video", { src: mediaUrl(file.path), muted: true, preload: "metadata", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }) : /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("img", { src: mediaUrl(file.path), alt: "", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }),
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { position: "absolute", top: 6, left: 6, width: 18, height: 18, borderRadius: 6, display: "grid", placeItems: "center", background: "rgba(10,10,10,.55)" }, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(KindGlyph, { kind: file.mediaType.startsWith("video/") ? "video" : "image", size: 10 }) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { padding: "6px 8px 8px", display: "flex", alignItems: "center", gap: 4 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { fontSize: 11, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", flex: 1 }, children: file.name }),
            props.onUseAsRef !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
              "span",
              {
                role: "button",
                title: "\u5F53\u4F5C\u53C2\u8003",
                onClick: (event) => {
                  event.stopPropagation();
                  props.onUseAsRef?.(file);
                },
                style: { flexShrink: 0, fontSize: 10, color: dx.mute, padding: "1px 5px", borderRadius: 6, border: `1px solid ${dx.hairline}` },
                children: "\u53C2\u8003"
              }
            ) : null
          ] })
        ]
      },
      file.path
    )) }) : null
  ] });
}
function Toast(props) {
  if (props.text === void 0 || props.text === "") return null;
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: {
    ...dxChrome,
    position: "absolute",
    top: 72,
    left: "50%",
    transform: "translateX(-50%)",
    zIndex: 38,
    padding: "8px 14px",
    borderRadius: 999,
    fontSize: 12,
    fontWeight: 500,
    pointerEvents: "none"
  }, children: props.text });
}
var SHORTCUTS = [
  { keys: "\u2318Z / \u21E7\u2318Z", label: "\u64A4\u9500 / \u91CD\u505A" },
  { keys: "\u2318C / \u2318V", label: "\u590D\u5236 / \u7C98\u8D34\u8282\u70B9\u6216\u5A92\u4F53" },
  { keys: "\u2318D", label: "\u590D\u5236\u6240\u9009" },
  { keys: "\u2318A", label: "\u5168\u9009" },
  { keys: "\u232B", label: "\u5220\u9664\u6240\u9009" },
  { keys: "\u2191\u2193\u2190\u2192", label: "\u5FAE\u79FB\u4E00\u683C \xB7 \u21E7 \u52A0\u5927" },
  { keys: "Tab", label: "\u6362\u955C" },
  { keys: "G", label: "\u6253\u5F00\u751F\u6210\u680F" },
  { keys: "\u2318G", label: "\u7F16\u7EC4\u6240\u9009" },
  { keys: "S", label: "\u5FAA\u73AF\u955C\u5934\u72B6\u6001" },
  { keys: "F", label: "\u9002\u914D\u6240\u9009" },
  { keys: "E", label: "\u7F16\u8F91\u6240\u9009\u5A92\u4F53" },
  { keys: "Esc", label: "\u5148\u5173\u83DC\u5355\uFF0C\u518D\u5173\u6D6E\u5C42" },
  { keys: "\u53CC\u51FB\u8282\u70B9", label: "\u5C45\u4E2D\u5E76\u7F29\u653E\u5230\u5408\u9002\u4F4D\u7F6E" },
  { keys: "\u53CC\u51FB\u7A7A\u767D", label: "\u5FEB\u901F\u6DFB\u52A0\u8282\u70B9" },
  { keys: "\u53F3\u952E\u7A7A\u767D", label: "\u5B8C\u6574\u6DFB\u52A0 / \u5BFC\u5165" },
  { keys: "\u53F3\u952E\u8282\u70B9", label: "\u5F53\u524D\u8282\u70B9\u53EF\u7528\u64CD\u4F5C" },
  { keys: "L", label: "\u9501\u5B9A / \u89E3\u9501" },
  { keys: "\u2318K / \u2318F", label: "\u641C\u7D22\u8282\u70B9" },
  { keys: "\u2318J", label: "DSH \u4F1A\u8BDD" },
  { keys: "Space", label: "\u5E73\u79FB\u753B\u5E03" },
  { keys: "?", label: "\u5FEB\u6377\u952E" }
];
function ShortcutsSheet(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "dx-scrim", style: { position: "absolute", inset: 0, zIndex: 37, display: "grid", placeItems: "center", padding: 24 }, onMouseDown: props.onClose, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
    "div",
    {
      style: { ...dxChrome, width: "min(420px, 100%)", padding: 16, borderRadius: 20 },
      onMouseDown: (event) => event.stopPropagation(),
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("strong", { style: { fontSize: 14 }, children: "\u5FEB\u6377\u952E" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("button", { className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28 }, onClick: props.onClose, children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(IconClose, { size: 13 }) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { style: { display: "grid", gap: 6 }, children: SHORTCUTS.map((row3) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 12, fontSize: 12 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { ...kbd, minWidth: 92, justifyContent: "flex-start" }, children: row3.keys }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { style: { color: dx.mute }, children: row3.label })
        ] }, row3.keys)) })
      ]
    }
  ) });
}

// src/client/stage/DirectorStage.tsx
var import_jsx_runtime17 = require("react/jsx-runtime");
var button2 = { ...dxGhostBtn, width: "auto", height: 29, padding: "0 9px", borderRadius: 8, fontSize: 11, fontFamily: dx.font };
function DirectorStage(props) {
  const src = withProject(`/directorx/stage/?node=${encodeURIComponent(props.nodeId)}`);
  return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { "data-director-stage": "true", style: { position: "absolute", inset: 0, display: "flex", flexDirection: "column", background: "#111", color: "#eee", fontFamily: dx.font }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("header", { style: { height: 42, flexShrink: 0, display: "flex", alignItems: "center", gap: 12, padding: "0 14px", borderBottom: "1px solid #2a2a2a", zIndex: 2 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("strong", { style: { fontSize: 14, whiteSpace: "nowrap" }, children: "3D \u5BFC\u6F14\u53F0" }),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "button", className: "dx-hit", style: button2, onClick: props.onClose, children: "\u8FD4\u56DE\u753B\u5E03" }),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { style: { flex: 1 } })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
      "iframe",
      {
        title: "3D \u5BFC\u6F14\u53F0",
        src,
        style: { flex: 1, border: 0, width: "100%", minHeight: 0, background: "#111" },
        allow: "autoplay; fullscreen; microphone; camera"
      }
    )
  ] });
}

// src/client/stage/EditStage.tsx
var import_jsx_runtime18 = require("react/jsx-runtime");
var button3 = { ...dxGhostBtn, width: "auto", height: 29, padding: "0 9px", borderRadius: 8, fontSize: 11, fontFamily: dx.font };
function EditStage(props) {
  const src = withProject(`/directorx/edit/?node=${encodeURIComponent(props.nodeId)}`);
  return /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("div", { "data-edit-stage": "true", style: { position: "absolute", inset: 0, display: "flex", flexDirection: "column", background: "#111", color: "#eee", fontFamily: dx.font }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("header", { style: { height: 42, flexShrink: 0, display: "flex", alignItems: "center", gap: 12, padding: "0 14px", borderBottom: "1px solid #2a2a2a", zIndex: 2 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("strong", { style: { fontSize: 14, whiteSpace: "nowrap" }, children: "\u526A\u8F91\u53F0" }),
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("button", { type: "button", className: "dx-hit", style: button3, onClick: props.onClose, children: "\u8FD4\u56DE\u753B\u5E03" }),
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("span", { style: { flex: 1 } })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      "iframe",
      {
        title: "\u526A\u8F91\u53F0",
        src,
        style: { flex: 1, border: 0, width: "100%", minHeight: 0, background: "#111" },
        allow: "autoplay; fullscreen; clipboard-read; clipboard-write"
      }
    )
  ] });
}

// src/client/stage/SessionDock.tsx
var import_react17 = require("react");

// src/client/directorx-command.ts
var DIRECTORX_SLASH_OPTIONS = [
  { id: "board", label: "\u5236\u7247\u677F", detail: "\u5206\u955C\u8BA1\u6570 + \u63D0\u6848\u961F\u5217" },
  { id: "shotlist", label: "\u5206\u955C\u8868", detail: "\u7F16\u53F7\u955C + \u65F6\u957F\u9884\u7B97" },
  { id: "proposals", label: "\u63D0\u6848\u961F\u5217", detail: "\u5360\u4F4D\u63D0\u793A\u8BCD / \u6A21\u578B / \u89C4\u683C" },
  { id: "next", label: "\u4E0B\u4E00\u6761\u5F85\u6279", detail: "\u6700\u65E7\u5F85\u6279\u6216\u5DF2\u6279\u672A\u6267\u884C" }
];
function directorxCommandLine(optionId) {
  return optionId === "board" ? "/directorx board" : `/directorx ${optionId}`;
}
function optionalService(ctx, name2) {
  try {
    const rec = ctx;
    return rec[name2] ?? ctx.get(name2);
  } catch {
    return void 0;
  }
}
async function executeDirectorxLine(ctx, sessionId, line) {
  const remote = optionalService(ctx, "remote");
  if (remote?.commands !== void 0 && typeof remote.commands.execute === "function") {
    await remote.commands.execute(sessionId, line, []);
    return;
  }
  const connection = optionalService(ctx, "connection");
  const execute = connection?.api?.commands?.execute;
  if (execute === void 0) return;
  await execute({ sessionId, agentId: sessionId, line, images: [] });
}
function decorateDirectorx(ctx, command) {
  command.decorate({
    name: "directorx",
    available: () => true,
    ui: {
      kind: "popupSelect",
      options: async () => DIRECTORX_SLASH_OPTIONS.map((option) => ({ ...option })),
      onSelect: async (option, session) => {
        await executeDirectorxLine(ctx, session.sessionId, directorxCommandLine(option.id));
      }
    }
  });
}
function registerDirectorxSlash(ctx) {
  const attach = (host) => {
    const command = optionalService(host, "command") ?? optionalService(host, "commandUi");
    if (command === void 0 || typeof command.decorate !== "function") return false;
    decorateDirectorx(host, command);
    return true;
  };
  if (attach(ctx)) return;
  if (typeof ctx.inject !== "function") return;
  ctx.inject(["commandUi"], attach);
}

// src/client/stage/session-live.ts
function asRecord2(value) {
  if (value === null || typeof value !== "object") return void 0;
  return value;
}
function asString(value) {
  return typeof value === "string" && value !== "" ? value : void 0;
}
function inspectBlocks(value) {
  if (!Array.isArray(value)) {
    if (value !== null && typeof value === "object") {
      const rec = asRecord2(value);
      if (rec?.kind === "reasoning" || rec?.type === "reasoning") return { body: "", thinking: true };
    }
    return { body: textFromBlocks(value), thinking: false };
  }
  let thinking = false;
  const texts = [];
  for (const block of value) {
    const rec = asRecord2(block);
    if (rec === void 0) continue;
    const kind = rec.kind ?? rec.type;
    if (kind === "reasoning") {
      thinking = true;
      continue;
    }
    if ((kind === "text" || kind === void 0) && typeof rec.text === "string" && rec.text.trim() !== "") {
      texts.push(rec.text);
    }
  }
  return { body: texts.join("\n").trim(), thinking };
}
function pretty(value) {
  if (typeof value === "string") {
    const trimmed = value.trim();
    if (trimmed === "") return void 0;
    try {
      return JSON.stringify(JSON.parse(trimmed), null, 2);
    } catch {
      return trimmed.length > 4e3 ? `${trimmed.slice(0, 3999)}\u2026` : trimmed;
    }
  }
  if (value === void 0 || value === null) return void 0;
  try {
    return JSON.stringify(value, null, 2);
  } catch {
    return String(value);
  }
}
function resolveLiveSession(service, sessionId) {
  if (sessionId === void 0 || sessionId === "") return void 0;
  const rec = asRecord2(service);
  const binding = rec?.binding;
  if (typeof binding !== "function") return void 0;
  try {
    const bound = asRecord2(binding.call(service, sessionId));
    const session = bound?.session;
    if (session === null || typeof session !== "object") return void 0;
    const face = session;
    if (typeof face.subscribe !== "function" || typeof face.getSnapshot !== "function") return void 0;
    return face;
  } catch {
    return void 0;
  }
}
function textOfBlocks(value) {
  if (Array.isArray(value)) {
    const texts = [];
    for (const block of value) {
      const rec = asRecord2(block);
      if (rec === void 0) continue;
      if (rec.kind === "reasoning" || rec.type === "reasoning") continue;
      if (rec.kind === "text" || rec.type === "text") {
        if (typeof rec.text === "string" && rec.text !== "") texts.push(rec.text);
      }
    }
    if (texts.length > 0) return texts.join("\n").trim();
  }
  return textFromBlocks(value);
}
function parseAskItems(value) {
  if (!Array.isArray(value)) return [];
  const items = [];
  for (const raw of value) {
    const rec = asRecord2(raw);
    if (rec === void 0) continue;
    const id2 = asString(rec.id);
    const question = asString(rec.question);
    if (id2 === void 0 || question === void 0) continue;
    const options = Array.isArray(rec.options) ? rec.options.flatMap((option) => {
      const item3 = asRecord2(option);
      if (item3 === void 0) return [];
      const label2 = asString(item3.label);
      if (label2 === void 0) return [];
      const description = asString(item3.description);
      return [{ label: label2, ...description !== void 0 ? { description } : {} }];
    }) : void 0;
    const intentRec = asRecord2(rec.intent);
    items.push({
      id: id2,
      question,
      ...asString(rec.detail) !== void 0 ? { detail: asString(rec.detail) } : {},
      ...asString(rec.header) !== void 0 ? { header: asString(rec.header) } : {},
      ...options !== void 0 ? { options } : {},
      ...rec.multiSelect === true ? { multiSelect: true } : {},
      ...intentRec !== void 0 ? {
        intent: {
          ...asString(intentRec.kind) !== void 0 ? { kind: asString(intentRec.kind) } : {},
          ...asString(intentRec.approve) !== void 0 ? { approve: asString(intentRec.approve) } : {}
        }
      } : {}
    });
  }
  return items;
}
function parseWait(value) {
  const rec = asRecord2(value);
  if (rec === void 0 || typeof rec.respond !== "function") return void 0;
  const key = asString(rec.key) ?? "wait";
  const sessionId = asString(rec.sessionId) ?? "";
  const host = rec;
  const respond = (result) => host.respond(result);
  if (rec.kind === "question") {
    const payload = asRecord2(rec.payload);
    const questions = parseAskItems(payload?.questions ?? rec.questions);
    if (questions.length === 0) return void 0;
    return { kind: "question", key, sessionId, questions, respond };
  }
  if (rec.kind === "approval") {
    const payload = asRecord2(rec.payload);
    const approvalId = asString(payload?.approvalId) ?? asString(rec.approvalId);
    if (approvalId === void 0) return void 0;
    return {
      kind: "approval",
      key,
      sessionId,
      approvalId,
      toolName: asString(payload?.toolName) ?? asString(rec.toolName) ?? "tool",
      ...asString(payload?.reason) !== void 0 ? { reason: asString(payload?.reason) } : {},
      respond
    };
  }
  return void 0;
}
function dockItemsFromSnapshot(raw) {
  const snap = asRecord2(raw);
  if (snap === void 0) return { lines: [], waits: [], running: false, ready: false };
  const openState = asString(snap.openState);
  const ready = openState === "open" || openState === "loading" || openState === "error";
  const lines = [];
  const nodes = Array.isArray(snap.nodes) ? snap.nodes : [];
  const seenCalls = /* @__PURE__ */ new Set();
  for (const node of nodes) {
    const rec = asRecord2(node);
    if (rec === void 0) continue;
    const kind = asString(rec.kind);
    const seq = typeof rec.seq === "number" ? String(rec.seq) : String(lines.length);
    if (kind === "user" || kind === "steering") {
      const text = textOfBlocks(rec.content);
      if (text !== "") lines.push({ id: `user-${seq}`, kind: "user", text });
      continue;
    }
    if (kind === "assistant") {
      const inspected = inspectBlocks(rec.blocks ?? rec.content);
      if (inspected.body !== "") lines.push({ id: `asst-${seq}`, kind: "assistant", text: inspected.body });
      continue;
    }
    if (kind === "tool-result") {
      const call = asRecord2(rec.call);
      const name2 = asString(call?.name) ?? asString(rec.callId) ?? "tool";
      const callId = asString(rec.callId) ?? seq;
      seenCalls.add(callId);
      const err = rec.isError === true;
      const args = pretty(call?.argsRaw);
      const result = textFromBlocks(rec.content) || textFromBlocks(rec.output) || pretty(rec.result);
      lines.push({
        id: `tool-${seq}`,
        kind: "tool",
        name: name2,
        text: toolCaption(name2, typeof call?.argsRaw === "string" ? call.argsRaw : args),
        ...args !== void 0 ? { args } : {},
        ...result !== void 0 && result !== "" ? { result } : {},
        status: err ? "error" : "ok"
      });
      continue;
    }
    if (kind === "command") {
      const name2 = asString(rec.name) ?? "command";
      const outcome = asRecord2(rec.outcome);
      lines.push({
        id: `cmd-${seq}`,
        kind: "tool",
        name: name2,
        text: `${name2}\uFF1A`,
        status: outcome === void 0 ? "running" : outcome.kind === "error" ? "error" : "ok"
      });
      continue;
    }
    if (kind === "turn-error" || kind === "model-retry") {
      const text = asString(rec.message) ?? (kind === "model-retry" ? "\u6A21\u578B\u5C06\u91CD\u8BD5" : "\u672C\u8F6E\u5931\u8D25");
      lines.push({ id: `err-${seq}`, kind: "notice", text });
    }
  }
  const runningCalls = Array.isArray(snap.runningCalls) ? snap.runningCalls : [];
  for (const call of runningCalls) {
    const rec = asRecord2(call);
    const callId = asString(rec?.callId);
    const name2 = asString(rec?.name) ?? "tool";
    if (callId !== void 0 && seenCalls.has(callId)) continue;
    const argsRaw = typeof rec?.argsRaw === "string" ? rec.argsRaw : void 0;
    lines.push({
      id: `run-${callId ?? name2}`,
      kind: "tool",
      name: name2,
      text: toolCaption(name2, argsRaw),
      status: "running"
    });
  }
  const partial = asRecord2(snap.partial);
  const inspectedPartial = partial === void 0 ? { body: "", thinking: false } : inspectBlocks(partial.blocks);
  if (inspectedPartial.body !== "") {
    lines.push({ id: "partial", kind: "assistant", text: inspectedPartial.body, streaming: true });
  }
  if (snap.running === true) {
    const last = lines.at(-1);
    const streamingBody = last?.kind === "assistant" && last.streaming === true;
    const runningTool = last?.kind === "tool" && last.status === "running";
    if (!streamingBody && !runningTool) {
      lines.push({ id: "thinking", kind: "thinking", text: "\u601D\u8003\u4E2D", streaming: true });
    }
  }
  const waits = (Array.isArray(snap.pending) ? snap.pending : []).map(parseWait).filter((item3) => item3 !== void 0);
  return {
    lines,
    waits,
    running: snap.running === true,
    ready
  };
}
function linesFromFold(lines) {
  return lines.map((line) => ({
    id: line.id,
    kind: line.kind,
    text: line.text,
    ...line.name !== void 0 ? { name: line.name } : {},
    ...line.args !== void 0 ? { args: line.args } : {},
    ...line.result !== void 0 ? { result: line.result } : {},
    ...line.status !== void 0 ? { status: line.status } : {}
  }));
}
async function answerQuestion(wait, answers) {
  const receipt = await wait.respond({
    ok: true,
    value: { sessionId: wait.sessionId, answer: { answers } }
  });
  const rec = asRecord2(receipt);
  if (rec?.accepted === false) throw new Error(asString(rec.reason) ?? "\u63D0\u95EE\u56DE\u7B54\u88AB\u62D2\u7EDD");
}
async function cancelQuestion(wait) {
  const receipt = await wait.respond({
    ok: false,
    error: { code: "cancelled", message: "the user closed this question request", details: {} }
  });
  const rec = asRecord2(receipt);
  if (rec?.accepted === false) throw new Error(asString(rec.reason) ?? "\u53D6\u6D88\u63D0\u95EE\u88AB\u62D2\u7EDD");
}
function openLiveSession(service, sessionId) {
  const rec = asRecord2(service);
  const open = rec?.open;
  if (typeof open === "function") open.call(service, sessionId);
}
async function createLiveSession(service, input2) {
  const rec = asRecord2(service);
  const create2 = rec?.create;
  if (typeof create2 !== "function") return void 0;
  try {
    const result = await create2.call(service, input2.cwd !== void 0 ? { cwd: input2.cwd } : {});
    if (typeof result === "string" && result !== "") {
      openLiveSession(service, result);
      return result;
    }
    const wrapped = asRecord2(result);
    const id2 = asString(wrapped?.sessionId) ?? asString(asRecord2(wrapped?.value)?.sessionId);
    if (id2 !== void 0) {
      openLiveSession(service, id2);
      return id2;
    }
  } catch {
    return void 0;
  }
  return void 0;
}
async function answerApproval(wait, outcome) {
  const receipt = await wait.respond({
    ok: true,
    value: { sessionId: wait.sessionId, approvalId: wait.approvalId, outcome }
  });
  const rec = asRecord2(receipt);
  if (rec?.accepted === false) throw new Error(asString(rec.reason) ?? "\u5BA1\u6279\u63D0\u4EA4\u88AB\u62D2\u7EDD");
}
function resolveLiveBinding(service, sessionId) {
  const session = resolveLiveSession(service, sessionId);
  if (session === void 0) return void 0;
  const rec = asRecord2(service);
  const binding = typeof rec?.binding === "function" ? asRecord2(rec.binding.call(service, sessionId)) : void 0;
  const eventSource = asRecord2(binding?.eventSource);
  const source = eventSource !== void 0 && typeof eventSource.subscribe === "function" && typeof eventSource.getSnapshot === "function" ? {
    subscribe: (listener) => eventSource.subscribe(listener),
    getSnapshot: () => eventSource.getSnapshot()
  } : void 0;
  return {
    session,
    ...source !== void 0 ? { eventSource: source } : {}
  };
}
function chunkTextOf(value) {
  const chunk = asRecord2(value);
  if (chunk === void 0) return { text: "", reasoning: false };
  if (chunk.type === "text-delta") return { text: asString(chunk.text) ?? "", reasoning: false };
  if (chunk.type === "reasoning-delta") return { text: "", reasoning: true };
  if (chunk.type === "block-end") {
    const block = asRecord2(chunk.block);
    return block?.type === "text" ? { text: asString(block.text) ?? "", reasoning: false } : { text: "", reasoning: false };
  }
  return { text: "", reasoning: false };
}
function dockItemsFromWindow(raw, input2) {
  const window2 = asRecord2(raw);
  if (window2 === void 0) return { lines: [], running: false, ready: false };
  const openState = input2?.openState;
  const ready = openState === "open" || openState === "loading" || openState === "error";
  const running = input2?.running === true;
  const entries = Array.isArray(window2.entries) ? window2.entries : [];
  const lines = [];
  const calls = /* @__PURE__ */ new Map();
  const chunkLines = /* @__PURE__ */ new Map();
  const chunkCallLines = /* @__PURE__ */ new Map();
  const pushChunkText = (key, seq, text) => {
    const found = chunkLines.get(key);
    if (found !== void 0) {
      found.line.text += text;
      return;
    }
    const line = { id: `chunk-${key}`, kind: "assistant", text, streaming: true };
    chunkLines.set(key, { key, line });
    lines.push(line);
  };
  const dropChunks = (prefix) => {
    for (const [key, chunk] of chunkLines) {
      if (!key.startsWith(prefix)) continue;
      const at2 = lines.indexOf(chunk.line);
      if (at2 >= 0) lines.splice(at2, 1);
      chunkLines.delete(key);
    }
  };
  const pushChunkCall = (id2, name2, argsDelta) => {
    const found = chunkCallLines.get(id2);
    if (found !== void 0) {
      found.line.text = toolCaption(found.line.name ?? "tool", (found.line.args ?? "") + argsDelta);
      found.line.args = (found.line.args ?? "") + argsDelta;
      return;
    }
    const line = {
      id: `chunkcall-${id2}`,
      kind: "tool",
      name: name2 ?? "tool",
      text: toolCaption(name2 ?? "tool", argsDelta),
      args: argsDelta,
      status: "running"
    };
    chunkCallLines.set(id2, { key: id2, line });
    lines.push(line);
  };
  const dropChunkCall = (id2) => {
    const chunk = chunkCallLines.get(id2);
    if (chunk === void 0) return;
    const at2 = lines.indexOf(chunk.line);
    if (at2 >= 0) lines.splice(at2, 1);
    chunkCallLines.delete(id2);
  };
  for (const entry of entries) {
    const rec = asRecord2(entry);
    if (rec === void 0) continue;
    if (rec.type === "chunks") {
      const event2 = asRecord2(rec.event);
      if (event2 === void 0 || typeof event2.type !== "string") continue;
      const data2 = asRecord2(event2.data);
      if (data2 === void 0) continue;
      const turn = data2.turn;
      const step = data2.step;
      const key = `${String(turn)}:${String(step)}:${String(data2.index)}`;
      const firstSeq = typeof event2.seq === "number" ? event2.seq : lines.length;
      if (event2.type === "chunkrow/text-chunks") {
        const texts = Array.isArray(data2.texts) ? data2.texts.filter((part) => typeof part === "string") : [];
        if (texts.length > 0) pushChunkText(key, firstSeq, texts.join(""));
      } else if (event2.type === "chunkrow/tool-call-chunks") {
        const id2 = asString(data2.id);
        const args = Array.isArray(data2.args) ? data2.args.filter((part) => typeof part === "string") : [];
        const name2 = asString(data2.name);
        if (id2 !== void 0 && id2 !== "" && args.length > 0) {
          const call = calls.get(id2);
          if (call !== void 0 && call.argsRaw === void 0) call.argsRaw = args.join("");
          else if (call === void 0) pushChunkCall(id2, name2, args.join(""));
        }
      }
      continue;
    }
    const event = asRecord2(rec.event);
    if (event === void 0) continue;
    const type = asString(event.type);
    const data = asRecord2(event.data);
    const seq = typeof event.seq === "number" ? event.seq : lines.length;
    if (type === "user/message") {
      const parts = [];
      const content = Array.isArray(data?.content) ? data.content : [];
      for (const block of content) {
        const blockRec = asRecord2(block);
        if (blockRec === void 0) continue;
        if (blockRec.type === "image") parts.push("\u3014\u56FE\u7247\u3015");
        else if (blockRec.type === "text" && typeof blockRec.text === "string" && blockRec.text.trim() !== "") parts.push(blockRec.text.trim());
      }
      const text = parts.join("\n");
      if (text !== "") lines.push({ id: `user-${seq}`, kind: "user", text });
      continue;
    }
    if (type === "assistant/chunk") {
      if (data === void 0) continue;
      const turn = data.turn;
      const step = data.step;
      const chunk = data.chunk;
      const chunkRec = asRecord2(chunk);
      if (chunkRec?.type === "tool-call-delta") {
        const id2 = asString(chunkRec.id);
        if (id2 !== void 0 && id2 !== "") {
          const name2 = asString(chunkRec.name);
          pushChunkCall(id2, name2, asString(chunkRec.argumentsDelta) ?? "");
        }
        continue;
      }
      const delta = chunkTextOf(chunk);
      if (delta.text !== "") pushChunkText(`${String(turn)}:${String(step)}:${String(chunkRec?.index ?? 0)}`, seq, delta.text);
      continue;
    }
    if (type === "assistant/message") {
      if (data === void 0) continue;
      dropChunks(`${String(data.turn)}:${String(data.step)}:`);
      const message = asRecord2(data.message);
      const text = textOfBlocks(message?.content ?? message?.blocks);
      if (text !== "") {
        lines.push({
          id: `asst-${seq}`,
          kind: "assistant",
          text,
          ...data.interrupted === true ? { streaming: false } : {}
        });
      }
      continue;
    }
    if (type === "tool/call") {
      if (data === void 0) continue;
      const callId = asString(data.callId) ?? `call-${seq}`;
      const name2 = asString(data.name) ?? "tool";
      const argsRaw = asString(data.arguments);
      calls.set(callId, { name: name2, ...argsRaw !== void 0 ? { argsRaw } : {} });
      dropChunkCall(callId);
      lines.push({
        id: `call-${callId}`,
        kind: "tool",
        name: name2,
        text: toolCaption(name2, argsRaw),
        ...argsRaw !== void 0 ? { args: pretty(argsRaw) } : {},
        status: "running"
      });
      continue;
    }
    if (type === "tool/result") {
      if (data === void 0) continue;
      const message = asRecord2(data.message);
      const first = Array.isArray(message?.content) ? asRecord2(message.content[0]) : void 0;
      const callId = asString(first?.toolCallId) ?? `result-${seq}`;
      const call = calls.get(callId);
      const name2 = call?.name ?? "tool";
      dropChunkCall(callId);
      const err = first?.isError === true || data.error !== void 0;
      const result = textOfBlocks(first?.content);
      const running2 = lines.find((line) => line.id === `call-${callId}`);
      if (running2 !== void 0) {
        running2.status = err ? "error" : "ok";
        if (result !== "") running2.result = result;
      } else {
        lines.push({
          id: `tool-${seq}`,
          kind: "tool",
          name: name2,
          text: toolCaption(name2, call?.argsRaw),
          ...call?.argsRaw !== void 0 ? { args: pretty(call.argsRaw) } : {},
          ...result !== "" ? { result } : {},
          status: err ? "error" : "ok"
        });
      }
      continue;
    }
    if (type === "command/run") {
      const commandId = asString(data?.commandId) ?? `command-${seq}`;
      const name2 = asString(data?.name) ?? "command";
      lines.push({ id: `cmd-${commandId}`, kind: "tool", name: name2, text: `${name2}\uFF1A`, status: "running" });
      continue;
    }
    if (type === "command/done") {
      const commandId = asString(data?.commandId);
      const kind = asString(data?.kind);
      const text = asString(data?.text);
      const running2 = commandId !== void 0 ? lines.find((line) => line.id === `cmd-${commandId}`) : void 0;
      if (running2 !== void 0) {
        running2.status = kind === "error" ? "error" : "ok";
        if (text !== void 0 && text !== "") running2.result = text;
      } else {
        lines.push({
          id: `cmd-${seq}`,
          kind: "tool",
          name: "command",
          text: "command\uFF1A",
          ...text !== void 0 && text !== "" ? { result: text } : {},
          status: kind === "error" ? "error" : "ok"
        });
      }
      continue;
    }
    if (type === "turn/end") {
      const reason = asRecord2(data?.reason);
      if (reason?.kind === "error") {
        const error = asRecord2(reason.error);
        const message = asString(error?.message) ?? "\u672C\u8F6E\u5931\u8D25";
        lines.push({ id: `err-${seq}`, kind: "notice", text: message });
      }
      continue;
    }
    if (type === "llm/retry" || type === "llm/retry-started") {
      const message = asString(data?.message);
      lines.push({ id: `retry-${seq}`, kind: "notice", text: message ?? "\u6A21\u578B\u5C06\u91CD\u8BD5" });
      continue;
    }
  }
  if (running) {
    const last = lines.at(-1);
    const streamingBody = last?.kind === "assistant" && last.streaming === true;
    const runningTool = last?.kind === "tool" && last.status === "running";
    if (!streamingBody && !runningTool) {
      lines.push({ id: "thinking", kind: "thinking", text: "\u601D\u8003\u4E2D", streaming: true });
    }
  }
  return { lines, running, ready };
}
function dockItemsFromLive(binding) {
  if (binding?.session === void 0 || binding.eventSource === void 0) return void 0;
  const snapshot2 = asRecord2(binding.session.getSnapshot());
  return {
    ...dockItemsFromWindow(binding.eventSource.getSnapshot(), {
      running: snapshot2?.running === true,
      ...typeof snapshot2?.openState === "string" ? { openState: snapshot2.openState } : {}
    }),
    waits: []
  };
}
function subscribeWaitSource(pending, listener) {
  const face = asRecord2(pending);
  const subscribe = face?.subscribe;
  if (typeof subscribe !== "function") return void 0;
  try {
    const off = subscribe.call(pending, listener);
    return typeof off === "function" ? off : void 0;
  } catch {
    return void 0;
  }
}
function waitsFromPending(pending, sessionId) {
  const face = asRecord2(pending);
  const getSnapshot = face?.getSnapshot;
  if (typeof getSnapshot !== "function" || sessionId === void 0 || sessionId === "") return [];
  let snapshot2;
  try {
    snapshot2 = getSnapshot.call(pending);
  } catch {
    return [];
  }
  if (!(snapshot2 instanceof Map)) return [];
  const raw = snapshot2.get(sessionId) ?? [...snapshot2.values()].find((value) => asRecord2(value)?.sessionId === sessionId);
  const rec = asRecord2(raw);
  if (rec === void 0) return [];
  const kind = asString(rec.kind);
  const key = asString(rec.key) ?? kind ?? "wait";
  const carrier = rec;
  if (kind === "approval") {
    return [{
      kind: "approval",
      key,
      sessionId,
      approvalId: asString(rec.approvalId) ?? asString(rec.callId) ?? key,
      toolName: asString(rec.toolName) ?? "tool",
      ...asString(rec.reason) !== void 0 ? { reason: asString(rec.reason) } : {},
      respond: async (result) => {
        const body = asRecord2(result);
        const value = asRecord2(body?.value);
        if (body?.ok === false) {
          if (typeof carrier.cancel === "function") await carrier.cancel();
          return { accepted: true };
        }
        if (typeof carrier.answer !== "function") return { accepted: false, reason: "no-answerer" };
        await carrier.answer(asString(value?.outcome) ?? "allowed-once");
        return { accepted: true };
      }
    }];
  }
  if (kind === "question" || kind === "plan-review") {
    const questions = parseAskItems(rec.questions);
    if (questions.length === 0) return [];
    return [{
      kind: "question",
      key,
      sessionId,
      questions,
      respond: async (result) => {
        const body = asRecord2(result);
        const value = asRecord2(body?.value);
        if (body?.ok === false) {
          if (typeof carrier.cancel === "function") await carrier.cancel();
          return { accepted: true };
        }
        if (typeof carrier.answer !== "function") return { accepted: false, reason: "no-answerer" };
        const answer = asRecord2(value?.answer);
        await carrier.answer({ answers: Array.isArray(answer?.answers) ? answer.answers : [] });
        return { accepted: true };
      }
    }];
  }
  return [];
}

// src/client/stage/session-media.ts
var VIDEO_EXT = /\.(mp4|webm|mov|mkv)$/i;
var IMAGE_EXT = /\.(png|jpe?g|webp|gif|svg)$/i;
function asRecord3(value) {
  if (value === null || typeof value !== "object" || Array.isArray(value)) return void 0;
  return value;
}
function mediaKindOf(path, mime, toolName) {
  if (mime !== void 0 && mime.startsWith("video/")) return "video";
  if (mime !== void 0 && mime.startsWith("image/")) return "image";
  if (VIDEO_EXT.test(path)) return "video";
  if (IMAGE_EXT.test(path)) return "image";
  if (toolName === "directorx_generate_video" || toolName === "directorx_edit" || toolName === "directorx_video_process" || toolName === "directorx_timeline" || toolName === "directorx_smart_cut" || toolName === "directorx_video_concat") {
    return "video";
  }
  if (toolName === "directorx_generate_image" || toolName === "directorx_extract_frames" || toolName === "directorx_studio" || toolName === "directorx_image_edit") {
    return "image";
  }
  return void 0;
}
function collectFiles(root2) {
  const items = [];
  const push = (path, mime) => {
    if (path.trim() === "" || items.some((item3) => item3.path === path)) return;
    items.push({ path, ...mime !== void 0 && mime !== "" ? { mime } : {} });
  };
  if (Array.isArray(root2.files)) {
    for (const file of root2.files) {
      const rec = asRecord3(file);
      if (rec === void 0) continue;
      const path = typeof rec.path === "string" && rec.path !== "" ? rec.path : typeof rec.url === "string" && rec.url !== "" ? rec.url : "";
      if (path === "") continue;
      push(path, typeof rec.mimeType === "string" ? rec.mimeType : void 0);
    }
  }
  if (typeof root2.path === "string") push(root2.path);
  return items;
}
function mediaFromToolResult(result, toolName) {
  if (result === void 0 || result.trim() === "") return [];
  let parsed;
  try {
    parsed = JSON.parse(result);
  } catch {
    return [];
  }
  const rec = asRecord3(parsed);
  if (rec === void 0) return [];
  const nested = asRecord3(rec.value) ?? asRecord3(rec.result);
  const body = rec.files === void 0 && rec.path === void 0 && nested !== void 0 ? nested : rec;
  const prompt = typeof body.prompt === "string" ? body.prompt : typeof rec.prompt === "string" ? rec.prompt : void 0;
  const media = [];
  for (const file of collectFiles(body)) {
    const kind = mediaKindOf(file.path, file.mime, toolName);
    if (kind === void 0) continue;
    const name2 = file.path.split("/").pop() ?? file.path;
    const label2 = displayCardTitle(name2, prompt) || name2;
    media.push({ path: file.path, kind, label: label2 });
  }
  return media.slice(0, 8);
}

// src/client/stage/SessionDock.tsx
var import_jsx_runtime19 = require("react/jsx-runtime");
var POLL_OPEN_MS = 1400;
var POLL_RUN_MS = 500;
var POLL_FAB_MS = 3200;
function dockingModel(binding, live) {
  const fromEvents = dockItemsFromLive(binding);
  if (fromEvents !== void 0) return fromEvents;
  return live === void 0 ? void 0 : dockItemsFromSnapshot(live.getSnapshot());
}
function SessionDock(props) {
  const [fold, setFold] = (0, import_react17.useState)({ lines: [], running: false, blocked: false });
  const [listRunning, setListRunning] = (0, import_react17.useState)(false);
  const [liveTick, setLiveTick] = (0, import_react17.useState)(0);
  const [draft, setDraft] = (0, import_react17.useState)("");
  const [busy, setBusy] = (0, import_react17.useState)(false);
  const [loadError, setLoadError] = (0, import_react17.useState)();
  const [expandedLines, setExpandedLines] = (0, import_react17.useState)(() => /* @__PURE__ */ new Set());
  const [confirmFirst, setConfirmFirst] = (0, import_react17.useState)(false);
  const [starting, setStarting] = (0, import_react17.useState)(false);
  const [images, setImages] = (0, import_react17.useState)([]);
  const [models, setModels] = (0, import_react17.useState)(void 0);
  const [modelOpen, setModelOpen] = (0, import_react17.useState)(false);
  const [modelBusy, setModelBusy] = (0, import_react17.useState)(false);
  const [modelError, setModelError] = (0, import_react17.useState)();
  const logRef = (0, import_react17.useRef)(null);
  const inputRef = (0, import_react17.useRef)(null);
  const fileRef = (0, import_react17.useRef)(null);
  const foldRef = (0, import_react17.useRef)(fold);
  foldRef.current = fold;
  const binding = resolveLiveBinding(props.liveSessions, props.sessionId);
  const live = binding?.session ?? resolveLiveSession(props.liveSessions, props.sessionId);
  const liveModel = dockingModel(binding, live);
  const pendingWaits = props.pending !== void 0 ? waitsFromPending(props.pending, props.sessionId) : void 0;
  void liveTick;
  (0, import_react17.useEffect)(() => {
    if (live === void 0 && binding?.eventSource === void 0 && props.pending === void 0) return;
    void live?.open?.();
    const disposers = [
      live?.subscribe(() => setLiveTick((tick) => tick + 1)),
      binding?.eventSource?.subscribe(() => setLiveTick((tick) => tick + 1)),
      subscribeWaitSource(props.pending, () => setLiveTick((tick) => tick + 1))
    ].filter((dispose) => typeof dispose === "function");
    return () => {
      for (const dispose of disposers) dispose();
    };
  }, [live, binding, props.pending]);
  const liveReady = liveModel?.ready === true;
  const running = (liveReady ? liveModel.running : fold.running) || listRunning;
  const lines = liveReady ? liveModel.lines : linesFromFold(fold.lines);
  const waits = pendingWaits ?? (liveReady ? liveModel.waits : []);
  const blocked = !liveReady && fold.blocked && waits.length === 0;
  const refreshHistory = (0, import_react17.useCallback)(async (sessionId = props.sessionId) => {
    if (resolveLiveSession(props.liveSessions, sessionId) !== void 0) return;
    const history = props.sessions?.history;
    if (history === void 0 || sessionId === void 0 || sessionId === "") {
      setFold({ lines: [], running: false, blocked: false });
      return;
    }
    try {
      const response = await history({ sessionId, maxMessages: 80 });
      const parsed = rpcOk(response);
      if (!parsed.ok) {
        setLoadError(parsed.message);
        return;
      }
      setLoadError(void 0);
      setFold(foldSessionHistory(parsed.value));
    } catch (cause) {
      setLoadError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [props.liveSessions, props.sessionId, props.sessions]);
  const refreshRunning = (0, import_react17.useCallback)(async () => {
    const list = props.sessions?.list;
    const sessionId = props.sessionId;
    if (list === void 0 || sessionId === void 0 || sessionId === "") return;
    try {
      const response = await list();
      const flag = sessionRunningFromList(response, sessionId);
      if (flag !== void 0) setListRunning(flag);
    } catch {
    }
  }, [props.sessionId, props.sessions]);
  (0, import_react17.useEffect)(() => {
    if (props.hidden === true) return;
    if (props.open) void refreshHistory();
    void refreshRunning();
  }, [props.hidden, props.open, props.pulse, refreshHistory, refreshRunning]);
  (0, import_react17.useEffect)(() => {
    if (props.hidden === true || liveReady) return;
    const ms = props.open ? running ? POLL_RUN_MS : POLL_OPEN_MS : POLL_FAB_MS;
    const timer2 = window.setInterval(() => {
      if (props.open) void refreshHistory();
      else void refreshRunning();
    }, ms);
    return () => window.clearInterval(timer2);
  }, [liveReady, props.hidden, props.open, refreshHistory, refreshRunning, running]);
  (0, import_react17.useEffect)(() => {
    const node = logRef.current;
    if (node === void 0 || node === null) return;
    node.scrollTop = node.scrollHeight;
  }, [lines.length, lines.at(-1)?.text, props.open, waits.length]);
  (0, import_react17.useEffect)(() => {
    if (props.open && props.hidden !== true) inputRef.current?.focus();
  }, [props.open, props.hidden]);
  (0, import_react17.useEffect)(() => {
    if (props.hidden === true) return;
    const onKey = (event) => {
      if (!(event.metaKey || event.ctrlKey) || event.key.toLowerCase() !== "j") return;
      event.preventDefault();
      props.onOpenChange(!props.open);
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [props.hidden, props.open, props.onOpenChange]);
  const addImages = (0, import_react17.useCallback)(async (files) => {
    const next = files.filter((file) => file.type.startsWith("image/")).slice(0, 8);
    if (next.length === 0) return;
    const loaded = await Promise.all(next.map(readImageFile));
    setImages((current) => [...current, ...loaded].slice(0, 8));
  }, []);
  const sendText = (0, import_react17.useCallback)(async (raw) => {
    const text = raw.trim();
    if (text === "" && images.length === 0 || busy) return;
    setBusy(true);
    try {
      const sessionId = props.sessionId !== void 0 && props.sessionId !== "" ? props.sessionId : await props.onEnsureSession?.();
      if (sessionId === void 0 || sessionId === "") throw new Error("\u8FD9\u4E2A\u5DE5\u4F5C\u533A\u8FD8\u6CA1\u6709 DSH \u4F1A\u8BDD");
      const face = resolveLiveSession(props.liveSessions, sessionId);
      if (text.startsWith("/") && face?.command !== void 0 && images.length === 0) {
        const result = await face.command(text);
        throwIfRejected(result, "\u547D\u4EE4\u672A\u6267\u884C");
        setListRunning(true);
        return;
      }
      const chip = props.selectedNode;
      const gated = confirmFirst && text !== "" ? `\u672C\u6761\u6309\u300C\u624B\u52A8\u786E\u8BA4\u300D\uFF1A\u5148 directorx_confirm / directorx_propose\uFF0C\u7B49\u6211\u786E\u8BA4\u540E\u518D\u751F\u6210\u3002
${text}` : text;
      const payload = chip === void 0 || gated === "" ? gated : `[\u753B\u5E03\u8282\u70B9 ${chip.id}${chip.label !== "" ? ` ${chip.label}` : ""}]
${gated}`;
      const content = [
        ...payload !== "" ? [{ type: "text", text: payload }] : [],
        ...images.map((image) => ({
          type: "image",
          mediaType: image.mediaType,
          data: image.data,
          name: image.name
        }))
      ];
      if (content.length === 0) return;
      const mode = (face !== void 0 ? liveModel?.running : foldRef.current.running) === true ? "steer" : "queue";
      if (face?.prompt !== void 0) {
        const result = await face.prompt(content, mode);
        throwIfRejected(result, "DSH \u672A\u63A5\u53D7");
      } else {
        const prompt = props.sessions?.prompt;
        if (prompt === void 0) throw new Error("\u5F53\u524D\u65E0\u6CD5\u8FDE\u63A5 DSH \u4F1A\u8BDD\u63A5\u53E3");
        const response = await prompt({ sessionId, mode, content, requestId: mintSessionRequestId() });
        const parsed = rpcOk(response);
        if (!parsed.ok) throw new Error(parsed.message);
      }
      setListRunning(true);
      setImages((current) => {
        for (const image of current) URL.revokeObjectURL(image.preview);
        return [];
      });
      await refreshHistory(sessionId);
    } catch (cause) {
      setLoadError(errorMessage(cause instanceof Error ? cause.message : cause));
      throw cause;
    } finally {
      setBusy(false);
    }
  }, [busy, confirmFirst, images, liveModel?.running, props.liveSessions, props.onEnsureSession, props.selectedNode, props.sessionId, props.sessions, refreshHistory]);
  const send = (0, import_react17.useCallback)(async () => {
    const text = draft.trim();
    if (text === "" && images.length === 0) return;
    setDraft("");
    try {
      await sendText(text);
    } catch {
      setDraft(text);
    }
  }, [draft, images.length, sendText]);
  const startNew = (0, import_react17.useCallback)(async () => {
    if (starting || props.onNewSession === void 0) return;
    setStarting(true);
    setLoadError(void 0);
    try {
      const id2 = await props.onNewSession();
      setExpandedLines(/* @__PURE__ */ new Set());
      setDraft("");
      setListRunning(false);
      if (id2 === "") throw new Error("\u65E0\u6CD5\u521B\u5EFA\u65B0\u4F1A\u8BDD");
    } catch (cause) {
      setLoadError(cause instanceof Error ? cause.message : String(cause));
    } finally {
      setStarting(false);
    }
  }, [props, starting]);
  const stop = (0, import_react17.useCallback)(async () => {
    const sessionId = props.sessionId;
    try {
      if (live?.cancel !== void 0) {
        const result = await live.cancel();
        throwIfRejected(result, "\u505C\u6B62\u5931\u8D25");
      } else {
        const cancel = props.sessions?.cancel;
        if (cancel === void 0 || sessionId === void 0 || sessionId === "") return;
        await cancel({ sessionId });
      }
      setListRunning(false);
      await refreshHistory();
    } catch (cause) {
      setLoadError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [live, props.sessionId, props.sessions, refreshHistory]);
  const refreshModels = (0, import_react17.useCallback)(async () => {
    const sessionId = props.sessionId;
    const catalogApi = props.sessions?.modelCatalog;
    const modelsApi = props.sessions?.models;
    if (catalogApi === void 0 && (modelsApi === void 0 || sessionId === void 0 || sessionId === "")) {
      setModels(void 0);
      return;
    }
    try {
      const raw = catalogApi !== void 0 ? await catalogApi() : await modelsApi({ sessionId });
      setModels(parseSessionModels(raw));
      setModelError(void 0);
    } catch (cause) {
      setModelError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [props.sessionId, props.sessions]);
  (0, import_react17.useEffect)(() => {
    if (props.hidden === true || !props.open) return;
    void refreshModels();
  }, [props.hidden, props.open, props.sessionId, refreshModels]);
  const selectModel = (0, import_react17.useCallback)(async (selection2) => {
    const sessionId = props.sessionId;
    const api = props.sessions?.selectModel;
    if (api === void 0 || sessionId === void 0 || sessionId === "") return;
    setModelBusy(true);
    try {
      const parsed = rpcOk(await api({
        sessionId,
        provider: selection2.provider,
        model: selection2.model,
        ...selection2.reasoningEffort !== void 0 ? { reasoningEffort: selection2.reasoningEffort } : {}
      }));
      if (!parsed.ok) throw new Error(parsed.message);
      setModels((current) => current === void 0 ? current : { ...current, current: parsed.value.selected ?? selection2 });
      setModelOpen(false);
      setModelError(void 0);
    } catch (cause) {
      setModelError(cause instanceof Error ? cause.message : String(cause));
    } finally {
      setModelBusy(false);
    }
  }, [props.sessionId, props.sessions]);
  if (props.hidden === true) return null;
  const missing = props.sessions?.prompt === void 0 && live?.prompt === void 0;
  const noSession = props.sessionId === void 0 || props.sessionId === "";
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(import_jsx_runtime19.Fragment, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("style", { children: SESSION_CSS }),
    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
      "input",
      {
        ref: fileRef,
        type: "file",
        accept: "image/*",
        multiple: true,
        hidden: true,
        onChange: (event) => {
          const files = [...event.target.files ?? []];
          event.target.value = "";
          void addImages(files);
        }
      }
    ),
    props.open ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
      SessionPanel,
      {
        inspectorOpen: props.inspectorOpen === true,
        lines,
        waits,
        running,
        blocked,
        error: loadError ?? modelError,
        expandedLines,
        onToggleLine: (id2) => setExpandedLines((current) => {
          const next = new Set(current);
          if (next.has(id2)) next.delete(id2);
          else next.add(id2);
          return next;
        }),
        noSession,
        missing,
        draft,
        busy,
        images,
        logRef,
        inputRef,
        onDraft: setDraft,
        onSend: () => {
          void send();
        },
        onStop: () => {
          void stop();
        },
        onClose: () => props.onOpenChange(false),
        onLeave: props.onLeave,
        onNewSession: props.onNewSession === void 0 ? void 0 : () => {
          void startNew();
        },
        starting,
        selectedNode: props.selectedNode,
        onClearSelected: props.onClearSelected,
        onAddMedia: props.onAddMedia,
        onPickImage: () => fileRef.current?.click(),
        onRemoveImage: (id2) => setImages((current) => {
          const hit = current.find((image) => image.id === id2);
          if (hit !== void 0) URL.revokeObjectURL(hit.preview);
          return current.filter((image) => image.id !== id2);
        }),
        onPasteFiles: (files) => {
          void addImages(files);
        },
        confirmFirst,
        onConfirmFirst: setConfirmFirst,
        onChip: (text) => {
          void sendText(text).catch(() => {
          });
        },
        models,
        modelOpen,
        modelBusy,
        modelAvailable: props.sessions?.modelCatalog !== void 0 || props.sessions?.models !== void 0,
        onToggleModels: () => setModelOpen((open) => !open),
        onSelectModel: (selection2) => {
          void selectModel(selection2);
        }
      }
    ) : /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
      "button",
      {
        type: "button",
        "data-dx-session-fab": "",
        "data-running": running || void 0,
        className: "dx-hit dx-session-fab",
        title: "\u6253\u5F00 DSH \u4F1A\u8BDD \u2318J",
        "data-tip": "DSH \u4F1A\u8BDD \u2318J",
        onClick: () => props.onOpenChange(true),
        style: fabStyle,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("img", { src: "/favicon.svg", width: 24, height: 24, alt: "", draggable: false }),
          running ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "dx-dot", style: { position: "absolute", top: 7, right: 7 } }) : null
        ]
      }
    )
  ] });
}
function throwIfRejected(result, fallback) {
  if (result === null || typeof result !== "object") return;
  const rec = result;
  if (rec.ok === false) {
    const error = rec.error !== null && typeof rec.error === "object" ? rec.error : void 0;
    throw new Error(typeof error?.message === "string" && error.message !== "" ? error.message : fallback);
  }
  const nested = rec.result !== null && typeof rec.result === "object" ? rec.result : void 0;
  if (nested?.ok === false) {
    const error = nested.error !== null && typeof nested.error === "object" ? nested.error : void 0;
    throw new Error(typeof error?.message === "string" && error.message !== "" ? error.message : fallback);
  }
}
function readImageFile(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onerror = () => reject(new Error("\u8BFB\u56FE\u5931\u8D25"));
    reader.onload = () => {
      const result = reader.result;
      if (typeof result !== "string") {
        reject(new Error("\u8BFB\u56FE\u5931\u8D25"));
        return;
      }
      const comma = result.indexOf(",");
      resolve({
        id: `img-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
        name: file.name,
        mediaType: file.type || "image/png",
        data: comma >= 0 ? result.slice(comma + 1) : result,
        preview: URL.createObjectURL(file)
      });
    };
    reader.readAsDataURL(file);
  });
}
var SESSION_CHIPS = [
  {
    label: "\u68C0\u7D22\u5BFC\u6F14\u77E5\u8BC6",
    text: "\u8BF7\u7528 directorx_skill_route \u6216 directorx_knowledge_search \u68C0\u7D22\u4E0E\u5F53\u524D\u753B\u5E03\u76F8\u5173\u7684\u5BFC\u6F14\u77E5\u8BC6\u3002\u5BF9 route.articles \u548C\u547D\u4E2D\u91CC\u7684 id \u505A directorx_knowledge_read\uFF0C\u5BF9\u547D\u4E2D\u91CC\u7684 skills \u505A directorx_skill_read\u3002\u4E0D\u8981\u53E6\u8D77\u4E00\u5957\u68C0\u7D22\u8BCD\uFF0C\u4E0D\u8981\u751F\u6210\u5A92\u4F53\u3002"
  },
  {
    label: "\u8FDB\u5165\u89C6\u9891\u5DE5\u4F5C\u574A",
    text: "\u8BF7\u8C03\u7528 directorx_creative_suite action:workshop-step workshopStep:script\uFF0C\u4EE5\u5F53\u524D\u9879\u76EE\u8FDB\u5165\u516D\u6B65\u89C6\u9891\u5DE5\u4F5C\u574A\uFF1A\u5267\u672C\u2192\u62C6\u89E3\u2192\u8D44\u4EA7\u2192\u955C\u5934\u63D0\u793A\u8BCD\u2192\u751F\u6210\u2192\u4EA4\u4ED8\u3002\u5148\u53EA\u8FD4\u56DE\u5F53\u524D\u6B65\u9AA4\u3001\u68C0\u67E5\u9879\u548C\u5EFA\u8BAE\u4E0B\u4E00\u6B65\uFF0C\u4E0D\u751F\u6210\u5A92\u4F53\u3002"
  },
  {
    label: "\u5206\u6790\u53C2\u8003\u89C6\u9891",
    text: "\u8BF7\u5148 directorx_media_scene_split \u4ECE\u6211\u63D0\u4F9B\u6216\u5F53\u524D\u9009\u4E2D\u7684\u89C6\u9891\u63D0\u53D6\u6700\u591A 12 \u4E2A\u68C0\u67E5\u5E27\uFF0C\u518D directorx_video_understand \u505A\u65F6\u95F4\u7EBF\u3001\u955C\u5934\u3001\u58F0\u97F3\u548C\u8282\u594F\u5206\u6790\u3002\u628A\u5206\u6790\u8981\u70B9\u9489\u5230\u753B\u5E03\uFF0C\u4E0D\u751F\u6210\u5A92\u4F53\u3002"
  },
  {
    label: "\u4E00\u952E\u7C97\u526A",
    text: "\u8BF7\u5BF9\u5F53\u524D\u9009\u4E2D\u7684\u89C6\u9891\u8C03\u7528 directorx_media_auto_cut\u3002\u82E5\u5DF2\u6709 SRT \u548C\u53E3\u64AD\u7A3F\u5C31\u6309\u811A\u672C\u7CBE\u526A\uFF1B\u5426\u5219\u5148\u8BFB\u53D6\u5A92\u4F53\u65F6\u957F\uFF0C\u518D\u95EE\u6211\u4FDD\u7559\u7684\u8D77\u6B62\u533A\u95F4\u3002\u5B8C\u6210\u540E\u628A\u8F93\u51FA\u52A0\u5165\u753B\u5E03\u3002"
  },
  {
    label: "\u4E00\u952E\u4EA4\u4ED8",
    text: "\u8BF7\u5BF9\u5F53\u524D\u6210\u7247\u8C03\u7528 directorx_media_package\uFF0C\u751F\u6210 15 \u79D2\u9884\u544A\u548C\u5C01\u9762\uFF1B\u518D\u7528 directorx_creative_suite action:copy-harness kind:commercial \u53D6\u5F97\u5199\u4F5C\u7EA6\u675F\uFF0C\u5E76\u4E3A\u76EE\u6807\u5E73\u53F0\u63D0\u51FA 3 \u4E2A\u6807\u9898\u3002\u628A\u5168\u90E8\u4EA4\u4ED8\u7269\u52A0\u5165\u753B\u5E03\u5E76\u7528 directorx_stage \u8BB0\u5F55 deliver\u3002"
  },
  {
    label: "\u68C0\u7D22\u5BFC\u6F14\u6280\u80FD",
    text: "\u8BF7\u7528 directorx_skill_route \u5BF9\u5F53\u524D\u753B\u5E03\u6216\u6211\u7684\u4E0A\u4E00\u53E5\u505A\u6280\u80FD\u4E0E\u5DE5\u5177\u8DEF\u7531\uFF0C\u518D directorx_skill_read \u5217\u51FA\u7684\u6BCF\u4E2A\u6280\u80FD\u5168\u6587\uFF0C\u6309\u8FD4\u56DE\u7684 next \u8C03\u7528\u5DE5\u5177\u3002\u4E0D\u8981\u53EA\u770B\u76EE\u5F55\u6458\u8981\uFF0C\u4E0D\u8981\u751F\u6210\u3002"
  },
  {
    label: "\u7EE7\u7EED\u5F53\u524D\u753B\u5E03",
    text: "\u8BF7\u7528 directorx_canvas_intents { claim: true } \u9886\u53D6\u5F85\u529E\u3002\u751F\u6210\u6761\u91CC\u7684\u5B57\u53EA\u662F\u610F\u56FE\uFF1A\u5148 directorx_skill_route\uFF0C\u518D skill_read \u4E0E knowledge_read\uFF0C\u5FC5\u8981\u65F6\u5916\u90E8\u8C03\u7814\uFF0C\u518D directorx_prompt_craft\uFF0C\u4E25\u683C/\u534F\u540C\u518D propose/confirm\uFF0C\u6700\u540E\u5E26 craftId \u751F\u6210\u3002\u4E0D\u8981\u628A\u539F\u53E5\u4E22\u7ED9 generate\u3002"
  },
  {
    label: "\u6574\u7406\u5F53\u524D\u5206\u955C",
    text: "\u8BF7\u7528 directorx_canvas_shotlist \u548C directorx_canvas_summary \u6574\u7406\u5F53\u524D\u753B\u5E03\u5206\u955C\uFF0C\u5FC5\u8981\u65F6 directorx_brief\u3002\u4E0D\u8981\u751F\u6210\u5A92\u4F53\u3002"
  },
  {
    label: "\u753B\u5E03\u5DE5\u5177",
    text: "\u8BF7\u6309\u5F53\u524D\u9009\u4E2D\u8282\u70B9\u505A\u753B\u5E03\u5DE5\u5177\uFF1A\u6587\u672C\u7528 directorx_canvas_script \u751F\u6210\u5206\u955C\uFF08\u672C\u2192\u9996\u5E27\u2192\u89C6\u9891\uFF09\uFF1B\u89C6\u9891\u7528 directorx_canvas_frames \u63D0\u53D6\u5E27\uFF0C\u6216 directorx_canvas_parse \u667A\u80FD\u89E3\u6790\u6210\u5207\u70B9\u5206\u955C\u7A3F\uFF1B\u5C40\u90E8\u91CD\u7ED8\u7528 directorx_canvas_reshoot cut\uFF0C\u4E2D\u6BB5\u751F\u6210\u540E\u518D assemble\u3002\u591A\u9009\u89C6\u9891\u7528 directorx_canvas_pack \u5408\u6210\u89C6\u9891\uFF1B\u591A\u9009\u56FE/\u89C6\u9891\u7528 directorx_canvas_sheet \u51FA\u4E5D\u5BAB\u683C\uFF1B\u5355\u5F20\u56FE\u7528 directorx_canvas_split \u62C6\u5206\u5BAB\u683C\uFF1B\u591A\u5F20\u56FE\u7528 directorx_canvas_join \u5408\u5E76\u5BAB\u683C\uFF1B2\u20134 \u8DEF\u7528 directorx_canvas_stack \u5206\u5C4F\uFF1B\u786C\u5B57\u5E55\u7528 directorx_canvas_desub \u53BB\u5B57\u5E55\uFF1B\u5EF6\u957F\u7528 directorx_canvas_extend \u5207\u51FA\u5C3E\u5E27\u7A7A\u5361\uFF1B\u8BC4\u5BA1\u52A8\u56FE\u7528 directorx_canvas_gif\u3002\u7136\u540E\u518D directorx_canvas_autolink \u81EA\u52A8\u8FDE\u7EBF\u3002\u5207\u7A97/\u89E3\u6790/\u94FA\u884C/\u62FC\u63A5/\u5207\u5F00/\u62FC\u56DE/\u5206\u5C4F/\u53BB\u5B57\u5E55/\u5EF6\u957F/\u52A8\u56FE\u4E0D\u8981\u751F\u6210\u3002"
  },
  {
    label: "\u6253\u5F00\u7F16\u8F91\u53F0",
    text: "\u8BF7\u5BF9\u5F53\u524D\u9009\u4E2D\u7684\u753B\u5E03\u8282\u70B9\u8C03\u7528 directorx_studio\u3002\u82E5\u6211\u63D0\u5230\u4E86\u8272\u8C03\u6216\u98CE\u683C\uFF0C\u6309\u8BE5\u63CF\u8FF0\u8C03\u8272\u540E\u6253\u5F00\u5BF9\u5E94\u7F16\u8F91\u5DE5\u4F5C\u53F0\uFF1B\u5426\u5219\u53EA\u6253\u5F00\u7F16\u8F91\u53F0\u3002\u8C03\u8272\u540E\u628A\u65B0\u8DEF\u5F84\u5199\u56DE\u8BE5\u8282\u70B9\u3002\u4E0D\u8981\u7528\u751F\u6210\u6A21\u578B\u91CD\u7ED8\u6765\u5B8C\u6210\u8C03\u8272\u3002"
  },
  {
    label: "\u7CBE\u526A\u5F53\u524D\u955C\u5934",
    text: "\u8BF7\u5BF9\u5F53\u524D\u9009\u4E2D\u7684\u753B\u5E03\u8282\u70B9\u5148 directorx_edit_plan\uFF0C\u518D\u6309\u8DEF\u7531\u8C03\u7528\u5BF9\u5E94\u786E\u5B9A\u6027\u7F16\u8F91\u5DE5\u5177\uFF08directorx_image_edit / directorx_video_process / directorx_edit / directorx_studio\uFF09\u3002\u5FC5\u987B\u5E26 nodeId \u56DE\u5199\u8DEF\u5F84\u3002\u4E0D\u8981\u7528\u751F\u6210\u6A21\u578B\u91CD\u7ED8\u6765\u88C1\u5207\u3001\u65CB\u8F6C\u3001\u8C03\u8272\u6216\u53D8\u901F\u3002\u505A\u5B8C\u540E extract_frames\uFF0C\u518D\u7528\u5BBF\u4E3B read_image \u628A\u5E27\u9001\u8FDB\u4F1A\u8BDD\u8D28\u68C0\u3002"
  },
  {
    label: "\u8D28\u68C0\u5F53\u524D\u6210\u7247",
    text: "\u8BF7\u5BF9\u5F53\u524D\u9009\u4E2D\u955C\u5934 directorx_extract_frames \u62BD\u5173\u952E\u5E27\uFF0C\u7528\u5BBF\u4E3B read_image \u5BF9\u7167\u63D0\u793A\u8BCD\u4E0E\u8FDE\u7EED\u6027\uFF0C\u5FC5\u8981\u65F6 directorx_qa\u3002\u4E0D\u8981\u751F\u6210\u3002"
  },
  {
    label: "\u7248\u6743\u6539\u5199\u5F53\u524D\u610F\u56FE",
    text: "\u8BF7\u5BF9\u5F53\u524D\u753B\u5E03\u610F\u56FE\u6216\u6211\u521A\u5199\u7684\u53E5\u5B50 directorx_ip_scan\u3002\u82E5 hits \u975E\u7A7A\uFF1Adirectorx_knowledge_read 213\uFF0C\u6309\u8FD4\u56DE\u7684 method/axes/keep \u548C\u9879\u76EE\u8BB0\u5FC6\u5199\u7EC6\u7684\u5C5E\u6027\u63CF\u8FF0\uFF08\u4E0D\u8981\u5957\u56FA\u5B9A\u6210\u7A3F\uFF09\uFF0C\u518D directorx_ip_rewrite remember:true \u9A8C\u6536\u5E76\u8BB0\u5165\u8BB0\u5FC6\u3002\u4E0D\u8981\u628A IP \u4E13\u540D\u9001\u8FDB generate\u3002"
  },
  {
    label: "\u7F16\u6392\u5F53\u524D\u6210\u7247",
    text: "\u8BF7\u5BF9\u5F53\u524D\u753B\u5E03\u6216\u6211\u7684\u4E0A\u4E00\u53E5\u5148 directorx_brief \u548C directorx_chengpian\u3002\u6309 compose \u7684\u8DEF/\u7A3F/\u4F4D\uFF1Adirectorx_skill_route \u2192 directorx_prompt_plan \u2192 skill_read/knowledge_read \u2192 prompt_craft \u2192 generate_ready\u3002\u6210\u7247\u89D2\u5EA6\u53EA\u662F\u5199\u6CD5\uFF0C\u539F\u53E5\u4E0D\u662F\u63D0\u793A\u8BCD\u3002\u7B7E\u5B57\u524D\u4E0D\u8981 generate\u3002\u8FC7\u4E00\u9636\u6BB5\u5C31 directorx_stage record\u3002"
  },
  {
    label: "\u4FDD\u5B58\u672C\u6B21\u4E3A\u6280\u80FD",
    text: '\u8BF7\u5BF9\u521A\u5B8C\u6210\u7684\u6210\u7247 directorx_skill_capture { action: "offer", present: true }\u3002\u7528 DSH \u6807\u51C6\u63D0\u95EE\u95EE\u6211\u662F\u5426\u4FDD\u5B58\u4E3A\u300Cxx\u300D\u6280\u80FD\uFF0C\u7981\u6B62\u6B63\u6587\u83DC\u5355\u3002\u6211\u540C\u610F\u540E\u628A\u9636\u6BB5\u6D41\u7A0B\u3001directorx_note \u91CC\u7684\u4FEE\u6539\u610F\u89C1\u3001\u62D2\u56E0\u548C\u98CE\u683C\u9501\u6574\u7406\u6210 SKILL.md\uFF0C\u518D action:save\u3002\u4E0D\u8981\u5199\u5165\u63D2\u4EF6\u81EA\u5E26 skills/\u3002'
  },
  {
    label: "\u4FDD\u5B58\u672C\u7CFB\u5217\u8BBE\u5B9A",
    text: "\u8BF7\u7528 directorx_series harvest \u6536\u6210\u5F53\u524D\u89D2\u8272\u951A\u3001\u98CE\u683C\u9501\u548C\u955C\u5934\u89C4\u5219\uFF0C\u518D action:save\u3002\u8FD9\u662F\u7CFB\u5217\u5305\u4E0D\u662F\u65B9\u6CD5\u6280\u80FD\u3002\u4E0D\u8981\u751F\u6210\u5A92\u4F53\u3002"
  },
  {
    label: "\u6CBF\u7528\u7CFB\u5217\u5305",
    text: "\u8BF7 directorx_series list\uFF0C\u518D apply \u6211\u8981\u7EE7\u7EED\u7684\u90A3\u4E00\u5957\u3002\u5957\u7528\u540E\u4E0D\u8981\u91CD\u8BBE\u8BA1\u89D2\u8272\u548C\u753B\u98CE\u3002\u5148 character_list / style_get \u786E\u8BA4\uFF0C\u518D\u5199\u4E0B\u4E00\u955C\u3002\u4E0D\u8981\u751F\u6210\uFF0C\u9664\u975E\u6211\u63A5\u7740\u8BF4\u5F00\u62CD\u3002"
  },
  {
    label: "\u6539\u5F53\u524D\u955C\u5934",
    text: "\u8BF7\u5BF9\u5F53\u524D\u9009\u4E2D\u8282\u70B9 directorx_revise\uFF0Cchange \u7528\u6211\u521A\u8BF4\u7684\u90A3\u53E5\u6216\u300C\u6309\u9009\u4E2D\u955C\u5934\u7684\u63D0\u793A\u8BCD\u6536\u7D27\u8868\u60C5\u548C\u52A8\u4F5C\u300D\u3002\u8BB0\u4E0B directorx_note\u3002\u7136\u540E\u624D prompt_plan / craft / generate_ready\u3002\u56DE\u5199\u53EA\u6539\u8FD9\u4E2A\u8282\u70B9\u7684 path\u3002\u4E0D\u8981\u91CD\u505A\u6574\u677F\u3002"
  },
  {
    label: "\u5199\u573A\u9762\u63A7\u5236\u8868",
    text: "\u8BF7 directorx_blocking harvest\uFF0C\u518D schema\u3002\u7528\u5F53\u524D\u89D2\u8272\u56FE\u3001\u6211\u7ED9\u7684\u5F00\u573A\u548C\u4E8B\u4EF6\u987A\u5E8F\u5199\u6210\u573A\u9762\u63A7\u5236\u8868\uFF1A\u53F0\u8D26\u3001\u7269\u4EF6\u72B6\u6001\u673A\u3001\u72EC\u7ACB\u76F8\u673A\u3001\u53C2\u8003\u89D2\u8272\u3002\u7F3A\u5F00\u573A\u6216\u987A\u5E8F\u5C31 directorx_ask\u3002\u5199\u5B8C pin \u9489\u5230\u753B\u5E03\u3002\u4E0D\u8981\u751F\u6210\u3002"
  },
  {
    label: "\u8BC4\u5BA1\u6539\u7F16\u6587\u6863",
    text: "\u8BF7\u7528 directorx_bible detect \u627E\u5F53\u524D\u9879\u76EE\u7684\u5927\u7EB2/\u89D2\u8272/\u7F8E\u672F/\u5267\u672C/\u5206\u955C JSON\uFF0Ccheckup \u8DD1\u8D28\u91CF\u95E8\uFF0C\u518D pin \u628A Markdown \u8BC4\u5BA1\u9489\u5230\u753B\u5E03\u3002\u4E0D\u8981\u53E6\u51FA HTML\u3002\u5206\u955C\u82E5\u5728\u5207\uFF0C\u5148 directorx_shot_vocab \u518D\u5199\u8FD9\u4E00\u683C\u3002"
  }
];
var SLASH_COMMANDS = [
  { line: "/compact", label: "compact", detail: "\u538B\u7F29\u5F53\u524D\u4F1A\u8BDD\u4E0A\u4E0B\u6587" },
  { line: "/permission", label: "permission", detail: "\u5207\u6362\u6743\u9650\u9884\u8BBE", arg: true },
  { line: "/goal", label: "goal", detail: "\u8BBE\u5B9A\u6216\u67E5\u770B\u76EE\u6807", arg: true },
  { line: "/model", label: "model", detail: "\u5207\u6362\u672C\u4F1A\u8BDD\u6A21\u578B" },
  { line: "/directorx", label: "directorx", detail: "DirectorX \u547D\u4EE4", arg: true },
  ...DIRECTORX_SLASH_OPTIONS.map((option) => ({
    line: directorxCommandLine(option.id),
    label: `directorx ${option.id}`,
    detail: option.detail
  }))
];
function slashQuery(draft) {
  if (!draft.startsWith("/") || draft.includes("\n")) return void 0;
  return draft.slice(1);
}
function slashHits(draft) {
  const query = slashQuery(draft);
  if (query === void 0) return [];
  const needle = query.trim().toLowerCase();
  return SLASH_COMMANDS.filter(
    (command) => needle === "" || command.label.toLowerCase().startsWith(needle) || command.line.slice(1).toLowerCase().startsWith(needle) || command.detail.toLowerCase().includes(needle)
  ).slice(0, 8);
}
function modelTriggerLabel(view) {
  if (view === void 0) return "\u6A21\u578B";
  for (const group of view.groups) {
    if (group.id !== view.current.provider) continue;
    const hit = group.models.find((item3) => item3.id === view.current.model);
    if (hit === void 0) continue;
    const effort = view.current.reasoningEffort;
    const effortName = effort === void 0 || hit.reasoning === void 0 ? void 0 : hit.reasoning.efforts.find((item3) => item3.id === effort)?.name;
    return effortName !== void 0 ? `${hit.name} \xB7 ${effortName}` : hit.name;
  }
  return view.current.model;
}
function SessionPanel(props) {
  void props.inspectorOpen;
  const ask = props.waits.find((item3) => item3.kind === "question");
  const approval = props.waits.find((item3) => item3.kind === "approval");
  const canSend = !props.missing && !props.busy && (props.draft.trim() !== "" || props.images.length > 0);
  const hits = slashHits(props.draft);
  const [slashIndex, setSlashIndex] = (0, import_react17.useState)(0);
  (0, import_react17.useEffect)(() => {
    setSlashIndex(0);
  }, [props.draft]);
  const activeSlash = hits[Math.min(slashIndex, Math.max(0, hits.length - 1))];
  const applySlash = (command) => {
    if (command.line === "/model") {
      props.onDraft("");
      if (props.modelAvailable && !props.modelOpen) props.onToggleModels();
      return;
    }
    if (command.arg === true) {
      props.onDraft(`${command.line} `);
      return;
    }
    props.onDraft("");
    props.onChip(command.line);
  };
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
    "div",
    {
      "data-dx-session-panel": "",
      className: "nowheel nopan",
      onWheel: (event) => event.stopPropagation(),
      onPointerDown: (event) => event.stopPropagation(),
      style: {
        position: "absolute",
        top: 0,
        right: 0,
        bottom: 0,
        zIndex: 50,
        width: 420,
        maxWidth: "min(420px, 100%)",
        height: "100%",
        maxHeight: "100%",
        borderRadius: 0,
        border: "none",
        borderLeft: "1px solid var(--dsw-alias-border-l2, rgba(255,255,255,.10))",
        background: "var(--dsw-alias-bg-base, #111)",
        color: "var(--dsw-alias-label-primary, #f3f3f3)",
        fontFamily: "var(--dsw-alias-font, inherit)",
        boxShadow: "var(--dsw-shadow-lv2, -12px 0 40px rgba(0,0,0,.28))",
        display: "flex",
        flexDirection: "column",
        overflow: "hidden",
        pointerEvents: "auto"
      },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "10px 12px", borderBottom: "1px solid var(--dsw-alias-border-l1, rgba(255,255,255,.08))", flexShrink: 0 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("img", { src: "/favicon.svg", width: 16, height: 16, alt: "", draggable: false }),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("strong", { style: { fontSize: 13, fontWeight: 600 }, children: "\u5BF9\u8BDD" }),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { style: { fontSize: 11, color: props.running ? "var(--dsw-alias-state-success-primary, #8ee0a0)" : "var(--dsw-alias-label-tertiary, rgba(255,255,255,.45))" }, children: props.running ? "\u751F\u6210\u4E2D" : "\u5DF2\u5C31\u7EEA" }),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { style: { flex: 1 } }),
          props.onNewSession !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit dx-session-new", disabled: props.starting === true, style: { ...dxGhostBtn, width: "auto", height: 28, padding: "0 8px", fontSize: 11 }, onClick: props.onNewSession, title: "\u5728\u8FD9\u4E2A\u5DE5\u4F5C\u533A\u5F00\u4E00\u4E2A\u65B0\u7684 DSH \u4F1A\u8BDD", children: props.starting === true ? "\u521B\u5EFA\u4E2D\u2026" : "\u65B0\u4F1A\u8BDD" }) : null,
          props.onLeave !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("button", { type: "button", className: "dx-hit dx-session-leave", style: { ...dxGhostBtn, width: "auto", height: 28, padding: "0 8px", gap: 5, fontSize: 11 }, onClick: props.onLeave, title: "\u5173\u95ED\u753B\u5E03\uFF0C\u56DE\u5230 DSH", children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(IconLeave, { size: 12 }),
            "\u56DE DSH"
          ] }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", style: { ...dxGhostBtn, width: 28, height: 28 }, onClick: props.onClose, title: "\u6536\u8D77", children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(IconClose, { size: 13 }) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { ref: props.logRef, className: "dx-session-log", style: { flex: 1, minHeight: 0, overflow: "auto", padding: "16px 18px 20px", display: "flex", flexDirection: "column", gap: 14 }, children: [
          props.missing ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(EmptyLine, { text: "\u5F53\u524D\u65E0\u6CD5\u8FDE\u63A5 DSH \u4F1A\u8BDD\u63A5\u53E3\u3002" }) : props.noSession ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(EmptyLine, { text: "\u8FD9\u4E2A\u5DE5\u4F5C\u533A\u8FD8\u6CA1\u6709 DSH \u4F1A\u8BDD\u3002\u53D1\u4E00\u6761\u6D88\u606F\u4F1A\u5728\u6B64\u5DE5\u4F5C\u533A\u5F00\u8C08\u3002" }) : props.lines.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(EmptyLine, { text: "\u548C DSH \u8BF4\u8BDD\uFF0C\u7F16\u6392\u8FD9\u4E00\u753B\u5E03\u3002" }) : props.lines.map((line) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(LineView, { line, expanded: props.expandedLines.has(line.id), onToggle: () => props.onToggleLine(line.id), onAddMedia: props.onAddMedia }, line.id)),
          props.running && props.lines.at(-1)?.kind !== "thinking" && props.lines.at(-1)?.status !== "running" && props.lines.at(-1)?.streaming !== true ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(EmptyLine, { text: "DSH \u6B63\u5728\u5904\u7406\u2026", pulse: true }) : null,
          props.blocked ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(EmptyLine, { text: "DSH \u5728\u7B49\u6279\u51C6\u6216\u56DE\u7B54\u3002" }) : null,
          props.error !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { color: "var(--dsw-alias-state-error-primary, #ff9b8f)", fontSize: 13, lineHeight: 1.45 }, children: props.error }) : null
        ] }),
        approval !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(ApprovalCard, { wait: approval }) : null,
        ask !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(QuestionCard, { wait: ask }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { style: { padding: "8px 12px 12px", flexShrink: 0 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-session-chips", children: [
            SESSION_CHIPS.map((chip) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", disabled: props.missing || props.busy, onClick: () => props.onChip(chip.text), style: { ...dxGhostBtn, width: "auto", height: 24, padding: "0 8px", fontSize: 11, flexShrink: 0, color: "var(--dsw-alias-label-secondary, #c8c8c8)" }, children: chip.label }, chip.label)),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", title: "\u751F\u6210\u524D\u5148\u786E\u8BA4", onClick: () => props.onConfirmFirst(!props.confirmFirst), className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 24, padding: "0 8px", fontSize: 11, flexShrink: 0, background: props.confirmFirst ? "var(--dsw-alias-interactive-bg-hover, rgba(255,255,255,.12))" : "transparent" }, children: "\u624B\u52A8\u786E\u8BA4" })
          ] }),
          hits.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-slash-menu", "data-dx-slash-menu": "", children: hits.map((command, index2) => /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
            "button",
            {
              type: "button",
              className: "dx-hit",
              "data-active": index2 === slashIndex || void 0,
              onMouseDown: (event) => {
                event.preventDefault();
                applySlash(command);
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("code", { children: command.line }),
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { children: command.detail })
              ]
            },
            command.line
          )) }) : null,
          props.modelOpen && props.modelAvailable ? /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-model-menu", "data-dx-model-menu": "", children: [
            props.models === void 0 || props.models.groups.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-model-empty", children: props.modelBusy ? "\u52A0\u8F7D\u6A21\u578B\u2026" : "\u5F53\u524D\u4F1A\u8BDD\u6CA1\u6709\u53EF\u5207\u6362\u7684\u6A21\u578B\u3002" }) : props.models.groups.map((group) => /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-model-group", children: [
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-model-group-name", children: group.name }),
              group.models.map((model) => {
                const on = group.id === props.models.current.provider && model.id === props.models.current.model;
                return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
                  "button",
                  {
                    type: "button",
                    className: "dx-hit",
                    "data-active": on || void 0,
                    disabled: props.modelBusy,
                    onMouseDown: (event) => {
                      event.preventDefault();
                      props.onSelectModel({
                        provider: group.id,
                        model: model.id,
                        ...model.reasoning?.defaultEffort !== void 0 ? { reasoningEffort: model.reasoning.defaultEffort } : {}
                      });
                    },
                    children: [
                      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("strong", { children: model.name }),
                      model.description !== void 0 && model.description !== "" ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { children: model.description }) : null
                    ]
                  },
                  model.id
                );
              })
            ] }, group.id)),
            props.models !== void 0 ? (() => {
              const current = props.models.groups.find((group) => group.id === props.models.current.provider)?.models.find((model) => model.id === props.models.current.model);
              const efforts = current?.reasoning?.efforts ?? [];
              if (efforts.length === 0) return null;
              return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-model-group", children: [
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-model-group-name", children: "\u63A8\u7406\u5F3A\u5EA6" }),
                efforts.map((effort) => {
                  const on = effort.id === props.models.current.reasoningEffort;
                  return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
                    "button",
                    {
                      type: "button",
                      className: "dx-hit",
                      "data-active": on || void 0,
                      disabled: props.modelBusy,
                      onMouseDown: (event) => {
                        event.preventDefault();
                        props.onSelectModel({
                          provider: props.models.current.provider,
                          model: props.models.current.model,
                          reasoningEffort: effort.id
                        });
                      },
                      children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("strong", { children: effort.name })
                    },
                    effort.id
                  );
                })
              ] });
            })() : null
          ] }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-dsh-input", children: [
            props.selectedNode !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 6, padding: "0 12px", fontSize: 12, color: "var(--dsw-alias-label-tertiary)" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { style: { padding: "3px 8px", borderRadius: 8, background: "var(--dsw-alias-interactive-bg-hover)", maxWidth: "80%", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: props.selectedNode.label || props.selectedNode.id }),
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", style: { ...dxGhostBtn, width: 22, height: 22 }, onClick: props.onClearSelected, title: "\u53D6\u6D88\u5F15\u7528", children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(IconClose, { size: 11 }) })
            ] }) : null,
            props.images.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-dsh-rail", children: props.images.map((image) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-dsh-thumb", title: "\u79FB\u9664", onClick: () => props.onRemoveImage(image.id), children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("img", { src: image.preview, alt: image.name }) }, image.id)) }) : null,
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
              "textarea",
              {
                ref: props.inputRef,
                value: props.draft,
                disabled: props.missing,
                placeholder: props.running ? "\u63D2\u8BDD\u5F15\u5BFC\u2026" : "\u6709\u95EE\u9898\uFF0C\u5C3D\u7BA1\u95EE\uFF0C\u6216\u8F93\u5165 /",
                rows: 2,
                onChange: (event) => {
                  props.onDraft(event.target.value);
                  const node = event.target;
                  node.style.height = "auto";
                  node.style.height = `${Math.min(336, Math.max(48, node.scrollHeight))}px`;
                },
                onPaste: (event) => {
                  const files = [...event.clipboardData.items].map((item3) => item3.getAsFile()).filter((file) => file !== null && file.type.startsWith("image/"));
                  if (files.length === 0) return;
                  event.preventDefault();
                  props.onPasteFiles(files);
                },
                onKeyDown: (event) => {
                  if (hits.length > 0) {
                    if (event.key === "ArrowDown") {
                      event.preventDefault();
                      setSlashIndex((index2) => (index2 + 1) % hits.length);
                      return;
                    }
                    if (event.key === "ArrowUp") {
                      event.preventDefault();
                      setSlashIndex((index2) => (index2 - 1 + hits.length) % hits.length);
                      return;
                    }
                    if (event.key === "Tab" || event.key === "Enter" && !event.shiftKey) {
                      event.preventDefault();
                      if (activeSlash !== void 0) applySlash(activeSlash);
                      return;
                    }
                    if (event.key === "Escape") {
                      event.preventDefault();
                      props.onDraft("");
                      return;
                    }
                  }
                  if (event.key === "Escape") {
                    event.preventDefault();
                    if (props.modelOpen) props.onToggleModels();
                    else props.onClose();
                    return;
                  }
                  if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "j") {
                    event.preventDefault();
                    props.onClose();
                    return;
                  }
                  if (event.key === "Enter" && !event.shiftKey) {
                    event.preventDefault();
                    props.onSend();
                  }
                },
                style: composerStyle
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-dsh-bar", children: [
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", disabled: props.missing || props.busy, style: { ...dxGhostBtn, width: 32, height: 32 }, onClick: props.onPickImage, title: "\u6DFB\u52A0\u56FE\u7247", children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(IconPlus, { size: 15 }) }),
              props.modelAvailable ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
                "button",
                {
                  type: "button",
                  className: "dx-hit dx-model-trigger",
                  disabled: props.missing || props.modelBusy,
                  "data-open": props.modelOpen || void 0,
                  style: { ...dxGhostBtn, width: "auto", maxWidth: 148, height: 32, padding: "0 8px", fontSize: 11 },
                  onClick: props.onToggleModels,
                  title: "\u5207\u6362\u672C\u4F1A\u8BDD\u6A21\u578B",
                  children: modelTriggerLabel(props.models)
                }
              ) : null,
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
                "button",
                {
                  type: "button",
                  className: "dx-hit",
                  disabled: props.missing || props.busy,
                  style: { ...dxGhostBtn, width: 32, height: 32, fontSize: 16, fontWeight: 600 },
                  onClick: () => {
                    if (!props.draft.startsWith("/")) props.onDraft("/");
                  },
                  title: "\u659C\u6760\u547D\u4EE4",
                  children: "/"
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { style: { flex: 1 } }),
              props.running ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", style: { ...dxGhostBtn, width: 32, height: 32 }, onClick: props.onStop, title: "\u505C\u6B62", children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(IconStop, { size: 14 }) }) : null,
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", disabled: !canSend, onClick: props.onSend, className: "dx-cta", title: props.running ? "\u63D2\u8BDD" : "\u53D1\u9001", style: { ...dxPill, width: 32, height: 32, opacity: canSend ? 1 : 0.35 }, children: props.busy ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "dx-spin" }) : /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(IconSend, { size: 14 }) })
            ] })
          ] })
        ] })
      ]
    }
  );
}
function copyLine(text) {
  if (text.trim() === "") return;
  void navigator.clipboard.writeText(text).catch(() => {
  });
}
function LineView(props) {
  if (props.line.kind === "tool") return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(ToolLine, { line: props.line, onAddMedia: props.onAddMedia });
  if (props.line.kind === "notice") return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { fontSize: 13, color: "var(--dsw-alias-label-tertiary)", lineHeight: 1.5 }, children: props.line.text });
  if (props.line.kind === "thinking") {
    return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { "data-dx-thinking": "", style: { display: "flex", alignItems: "center", gap: 8, fontSize: 13, lineHeight: 1.5, color: "var(--dsw-alias-label-tertiary)", fontStyle: "italic" }, children: [
      props.line.streaming === true ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "dx-spin", style: { width: 12, height: 12, borderWidth: 1.5, borderColor: "rgba(255,255,255,.18)", borderTopColor: "rgba(255,255,255,.7)" } }) : null,
      "\u601D\u8003\u4E2D"
    ] });
  }
  const mine = props.line.kind === "user";
  const fold = props.line.kind === "assistant" && sessionTextNeedsFold(props.line);
  const text = fold && !props.expanded ? `${props.line.text.slice(0, 180).trimEnd()}\u2026` : props.line.text;
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { "data-dx-md-line": props.line.kind, className: mine ? "dx-dsh-user" : "dx-dsh-assistant", children: [
    mine ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-dsh-bubble", children: text }) : /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(MarkdownView, { text }),
    fold ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit dx-note-fold", onClick: props.onToggle, style: { ...dxGhostBtn, width: "auto", height: 24, padding: "0 7px", marginTop: 6, fontSize: 11 }, children: props.expanded ? "\u6536\u8D77" : "\u5C55\u5F00" }) : null,
    props.line.streaming === true ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "dx-stream-caret", "aria-hidden": "true" }) : null,
    mine || props.line.kind === "assistant" ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit dx-dsh-copy", title: "\u590D\u5236", onClick: () => copyLine(props.line.text), children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(IconCopy, { size: 13 }) }) : null
  ] });
}
function ToolLine(props) {
  const status = props.line.status ?? "ok";
  const media = status === "ok" ? mediaFromToolResult(props.line.result, props.line.name) : [];
  const label2 = status === "running" ? "\u8FDB\u884C\u4E2D\u2026" : status === "error" ? "\u5931\u8D25" : "\u5B8C\u6210";
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-tool-line", "data-status": status, children: [
    /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "dx-tool-line-head", children: [
      /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { style: { minWidth: 0 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-tool-name", children: (props.line.name ?? "tool").replace(/^directorx_/, "").replaceAll("_", " ") }),
        /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("strong", { children: props.line.text })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "dx-tool-status", "data-status": status, children: label2 })
    ] }),
    media.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: "dx-tool-thumbs", children: media.map((item3) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SessionMediaThumb, { media: item3, onAdd: props.onAddMedia }, item3.path)) }) : null
  ] });
}
function SessionMediaThumb(props) {
  const src = mediaUrl(props.media.path);
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
    "button",
    {
      type: "button",
      className: "nodrag nopan dx-tool-thumb",
      title: "\u53CC\u51FB\u52A0\u5165\u753B\u5E03",
      "data-tip": "\u53CC\u51FB\u52A0\u5165\u753B\u5E03",
      onDoubleClick: (event) => {
        event.preventDefault();
        event.stopPropagation();
        props.onAdd?.(props.media);
      },
      children: [
        props.media.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("video", { src, muted: true, loop: true, playsInline: true, preload: "metadata" }) : /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("img", { src, alt: props.media.label }),
        props.media.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "dx-tool-thumb-kind", children: "\u89C6\u9891" }) : null
      ]
    }
  );
}
function QuestionCard(props) {
  const questions = props.wait.questions;
  const [index2, setIndex] = (0, import_react17.useState)(0);
  const [drafts, setDrafts] = (0, import_react17.useState)(() => questions.map(() => ({ selected: [], custom: "" })));
  const [busy, setBusy] = (0, import_react17.useState)(false);
  const [error, setError] = (0, import_react17.useState)();
  const question = questions[Math.min(index2, questions.length - 1)];
  const draft = drafts[Math.min(index2, drafts.length - 1)] ?? { selected: [], custom: "" };
  const plan = question.intent?.kind === "plan-review";
  const choose = (label2) => {
    setDrafts((current) => current.map((item3, itemIndex) => {
      if (itemIndex !== index2) return item3;
      if (question.multiSelect === true) {
        const selected2 = item3.selected.includes(label2) ? item3.selected.filter((value) => value !== label2) : [...item3.selected, label2];
        return { ...item3, selected: selected2 };
      }
      return { selected: [label2], custom: "" };
    }));
    setError(void 0);
  };
  const submit = async () => {
    setBusy(true);
    setError(void 0);
    try {
      await answerQuestion(props.wait, questions.map((item3, itemIndex) => {
        const value = drafts[itemIndex] ?? { selected: [], custom: "" };
        const custom = value.custom.trim();
        return {
          id: item3.id,
          selected: custom === "" || item3.multiSelect === true ? value.selected : [],
          ...custom === "" ? {} : { custom }
        };
      }));
    } catch (cause) {
      setBusy(false);
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { "data-dx-ask-card": "", style: cardStyle, children: [
    question.header !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { fontSize: 10, color: dx.mute, marginBottom: 4 }, children: question.header }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { fontSize: 13, fontWeight: 600, lineHeight: 1.4, marginBottom: 6 }, children: question.question }),
    question.detail !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { fontSize: 12, color: dx.mute, marginBottom: 8, maxHeight: 160, overflow: "auto" }, children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(MarkdownView, { text: question.detail }) }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: (question.options ?? []).map((option) => {
      const on = draft.selected.includes(option.label);
      return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
        "button",
        {
          type: "button",
          className: "dx-hit",
          disabled: busy,
          onClick: () => choose(option.label),
          style: {
            ...dxGhostBtn,
            width: "100%",
            height: "auto",
            minHeight: 32,
            padding: "6px 10px",
            justifyContent: "flex-start",
            textAlign: "left",
            background: on ? "rgba(255,255,255,.14)" : "rgba(255,255,255,.04)",
            whiteSpace: "normal"
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("span", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("strong", { style: { fontWeight: 600 }, children: option.label }),
            option.description !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { style: { display: "block", fontSize: 11, color: dx.mute, fontWeight: 400 }, children: option.description }) : null
          ] })
        },
        option.label
      );
    }) }),
    !plan ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
      "textarea",
      {
        value: draft.custom,
        disabled: busy,
        placeholder: "\u5176\u5B83\u56DE\u7B54\u2026",
        rows: 2,
        onChange: (event) => {
          const custom = event.target.value;
          setDrafts((current) => current.map((item3, itemIndex) => itemIndex === index2 ? { selected: question.multiSelect === true ? item3.selected : [], custom } : item3));
        },
        style: { ...composerStyle, minHeight: 40, marginTop: 8 }
      }
    ) : null,
    error !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { color: "#ff9b8f", fontSize: 11, marginTop: 6 }, children: error }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { style: { display: "flex", gap: 6, marginTop: 8, flexWrap: "wrap" }, children: [
      questions.length > 1 && index2 > 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 28, padding: "0 10px", fontSize: 11 }, onClick: () => setIndex(index2 - 1), children: "\u4E0A\u4E00\u9898" }) : null,
      questions.length > 1 && index2 < questions.length - 1 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", style: { ...dxGhostBtn, width: "auto", height: 28, padding: "0 10px", fontSize: 11 }, onClick: () => setIndex(index2 + 1), children: "\u4E0B\u4E00\u9898" }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { style: { flex: 1 } }),
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
        "button",
        {
          type: "button",
          className: "dx-hit",
          disabled: busy,
          style: { ...dxGhostBtn, width: "auto", height: 28, padding: "0 10px", fontSize: 11 },
          onClick: () => {
            void cancelQuestion(props.wait).catch((cause) => setError(cause instanceof Error ? cause.message : String(cause)));
          },
          children: "\u53D6\u6D88"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
        "button",
        {
          type: "button",
          className: "dx-cta",
          disabled: busy,
          style: { ...dxPill, width: "auto", height: 28, padding: "0 12px", fontSize: 11 },
          onClick: () => {
            void submit();
          },
          children: busy ? "\u63D0\u4EA4\u4E2D\u2026" : "\u56DE\u7B54"
        }
      )
    ] })
  ] });
}
function ApprovalCard(props) {
  const [busy, setBusy] = (0, import_react17.useState)(false);
  const [error, setError] = (0, import_react17.useState)();
  const decide = async (outcome) => {
    setBusy(true);
    setError(void 0);
    try {
      await answerApproval(props.wait, outcome);
    } catch (cause) {
      setBusy(false);
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { "data-dx-ask-card": "", "data-dx-approval-card": "", style: cardStyle, children: [
    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { fontSize: 10, color: "#e6c07b", marginBottom: 4 }, children: "\u7B49\u5F85\u6279\u51C6" }),
    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { fontSize: 13, fontWeight: 600, lineHeight: 1.4 }, children: props.wait.reason ?? `\u5141\u8BB8\u8C03\u7528 ${props.wait.toolName}\uFF1F` }),
    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { fontSize: 11, color: dx.mute, marginTop: 4 }, children: props.wait.toolName }),
    error !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { style: { color: "#ff9b8f", fontSize: 11, marginTop: 6 }, children: error }) : null,
    /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { style: { display: "flex", gap: 6, marginTop: 8, justifyContent: "flex-end" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-hit", disabled: busy, style: { ...dxGhostBtn, width: "auto", height: 28, padding: "0 10px", fontSize: 11 }, onClick: () => {
        void decide("rejected");
      }, children: "\u62D2\u7EDD" }),
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("button", { type: "button", className: "dx-cta", disabled: busy, style: { ...dxPill, width: "auto", height: 28, padding: "0 12px", fontSize: 11 }, onClick: () => {
        void decide("allowed-once");
      }, children: "\u5141\u8BB8\u4E00\u6B21" })
    ] })
  ] });
}
function EmptyLine(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { style: { fontSize: 12, color: dx.mute, lineHeight: 1.5, display: "flex", alignItems: "center", gap: 8 }, children: [
    props.pulse === true ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "dx-spin", style: { borderColor: "rgba(255,255,255,.2)", borderTopColor: "#f3f3f3" } }) : null,
    props.text
  ] });
}
var cardStyle = {
  margin: "0 10px 10px",
  padding: 12,
  borderRadius: 14,
  background: "var(--dsw-alias-bg-layer-3, rgba(255,255,255,.05))",
  border: "1px solid var(--dsw-alias-border-l1, rgba(255,255,255,.08))"
};
var fabStyle = {
  position: "absolute",
  right: 16,
  bottom: 16,
  zIndex: 50,
  width: 44,
  height: 44,
  borderRadius: 999,
  display: "grid",
  placeItems: "center",
  padding: 0,
  cursor: "pointer",
  pointerEvents: "auto",
  border: "1px solid var(--dsw-alias-border-l1, rgba(255,255,255,.10))",
  background: "var(--dsw-alias-bg-layer-2, #141414)",
  color: "var(--dsw-alias-label-primary, #f3f3f3)"
};
var composerStyle = {
  display: "block",
  width: "100%",
  minWidth: 0,
  minHeight: 48,
  maxHeight: 336,
  resize: "none",
  border: "none",
  outline: "none",
  background: "transparent",
  color: "var(--dsw-alias-label-primary, #f3f3f3)",
  padding: "0 16px",
  fontSize: 16,
  lineHeight: "24px",
  fontFamily: "inherit"
};
var SESSION_CSS = `
.dx-session-chips {
  display: flex;
  gap: 6px;
  margin-bottom: 8px;
  flex-wrap: nowrap;
  overflow-x: auto;
  padding-bottom: 2px;
  scrollbar-width: none;
  mask-image: linear-gradient(90deg, #000 0, #000 calc(100% - 28px), transparent);
  -webkit-mask-image: linear-gradient(90deg, #000 0, #000 calc(100% - 28px), transparent);
}
.dx-session-chips::-webkit-scrollbar { display: none; }
.dx-slash-menu {
  display: flex;
  flex-direction: column;
  gap: 2px;
  margin-bottom: 8px;
  padding: 6px;
  border-radius: 14px;
  border: 1px solid var(--dsw-alias-border-l2, rgba(255,255,255,.10));
  background: var(--dsw-alias-bg-layer-2, #1a1a1a);
  max-height: 240px;
  overflow: auto;
}
.dx-slash-menu button {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 2px;
  width: 100%;
  padding: 8px 10px;
  border: none;
  border-radius: 10px;
  background: transparent;
  color: var(--dsw-alias-label-primary);
  text-align: left;
  cursor: pointer;
}
.dx-slash-menu button[data-active], .dx-slash-menu button:hover {
  background: var(--dsw-alias-interactive-bg-hover, rgba(255,255,255,.08));
}
.dx-slash-menu code {
  font-size: 13px;
  font-weight: 600;
}
.dx-slash-menu span {
  font-size: 11px;
  color: var(--dsw-alias-label-tertiary);
}

.dx-model-menu {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 8px;
  padding: 8px;
  border-radius: 14px;
  border: 1px solid var(--dsw-alias-border-l2, rgba(255,255,255,.10));
  background: var(--dsw-alias-bg-layer-2, #1a1a1a);
  max-height: 280px;
  overflow: auto;
}
.dx-model-group { display: flex; flex-direction: column; gap: 2px; }
.dx-model-group-name, .dx-model-empty {
  padding: 4px 10px;
  font-size: 11px;
  color: var(--dsw-alias-label-tertiary);
}
.dx-model-menu button {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 2px;
  width: 100%;
  padding: 8px 10px;
  border: none;
  border-radius: 10px;
  background: transparent;
  color: var(--dsw-alias-label-primary);
  text-align: left;
  cursor: pointer;
}
.dx-model-menu button[data-active], .dx-model-menu button:hover {
  background: var(--dsw-alias-interactive-bg-hover, rgba(255,255,255,.08));
}
.dx-model-menu strong { font-size: 13px; font-weight: 600; }
.dx-model-menu span { font-size: 11px; color: var(--dsw-alias-label-tertiary); }
.dx-model-trigger {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.dx-model-trigger[data-open] {
  background: var(--dsw-alias-interactive-bg-hover, rgba(255,255,255,.12));
}
.dx-dsh-input {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding: 10px 0 8px;
  border: 1px solid var(--dsw-alias-border-l2-darkmode-thin, var(--dsw-alias-border-l2, rgba(255,255,255,.10)));
  border-radius: 22px;
  background: var(--dsw-specific-input-major, var(--dsw-alias-bg-layer-2, #1a1a1a));
  box-shadow: var(--dsw-shadow-lv2, none);
}
.dx-dsh-rail { display: flex; gap: 8px; padding: 0 12px; overflow-x: auto; }
.dx-dsh-thumb {
  width: 56px; height: 56px; padding: 0; border: none; border-radius: 10px; overflow: hidden; background: #111; cursor: pointer;
}
.dx-dsh-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }
.dx-dsh-bar { display: flex; align-items: center; gap: 4px; padding: 0 8px; }
.dx-dsh-user { display: flex; flex-direction: column; align-items: flex-end; gap: 6px; }
.dx-dsh-bubble {
  max-width: 82%;
  background: var(--dsw-specific-bubble, var(--dsw-alias-interactive-bg-hover, rgba(255,255,255,.10)));
  border-radius: 22px;
  padding: 10px 16px;
  font-size: 16px;
  line-height: 24px;
  color: var(--dsw-alias-label-primary);
  white-space: pre-wrap;
  word-break: break-word;
}
.dx-dsh-assistant { position: relative; font-size: 16px; line-height: 24px; color: var(--dsw-alias-label-primary); }
.dx-dsh-copy {
  width: 28px; height: 28px; opacity: 0; border: none; background: transparent; color: var(--dsw-alias-label-tertiary); cursor: pointer;
}
.dx-dsh-user:hover .dx-dsh-copy, .dx-dsh-assistant:hover .dx-dsh-copy { opacity: 1; }
.dx-tool-line {
  border: 1px solid var(--dsw-alias-border-l2);
  border-radius: 12px;
  padding: 12px 14px;
  background: var(--dsw-alias-bg-layer-2);
  color: var(--dsw-alias-label-primary);
}
.dx-tool-line-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; }
.dx-tool-name { font-size: 10px; letter-spacing: .65px; text-transform: uppercase; color: var(--dsw-alias-label-caption, var(--dsw-alias-label-tertiary)); }
.dx-tool-line-head strong { display: block; margin-top: 3px; font-size: 13px; line-height: 1.35; font-weight: 600; }
.dx-tool-status {
  flex-shrink: 0; font-size: 11px; padding: 4px 8px; border-radius: 999px;
  border: 1px solid var(--dsw-alias-border-l2); color: var(--dsw-alias-label-secondary);
}
.dx-tool-status[data-status="running"] { color: var(--dsw-alias-state-warn-primary, #e6c07b); border-color: currentColor; }
.dx-tool-status[data-status="error"] { color: var(--dsw-alias-state-error-primary, #ff9b8f); border-color: currentColor; }
.dx-tool-status[data-status="ok"] { color: var(--dsw-alias-state-success-primary, #8ee0a0); border-color: currentColor; }
.dx-tool-thumbs { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 12px; }
.dx-tool-thumb {
  position: relative; width: 100%; max-width: 280px; height: 160px; padding: 0;
  border: 1px solid var(--dsw-alias-border-l2); border-radius: 8px; overflow: hidden; background: var(--dsw-alias-bg-base); cursor: pointer;
}
.dx-tool-thumb img, .dx-tool-thumb video { width: 100%; height: 100%; object-fit: cover; display: block; }
.dx-tool-thumb-kind {
  position: absolute; left: 6px; bottom: 6px; padding: 1px 6px; border-radius: 4px;
  background: rgba(8,8,8,.7); color: #f2f2f2; font-size: 10px;
}
.dx-md { font-size: 16px; line-height: 24px; color: var(--dsw-alias-label-primary); }
.dx-md p { margin: 0 0 .7em; }
.dx-md p:last-child { margin-bottom: 0; }
.dx-md hr { display: none; }
.dx-md pre { padding: 12px; border-radius: 10px; background: var(--dsw-alias-bg-layer-2); overflow: auto; font-size: 13px; line-height: 1.5; }
.dx-md-table-wrap { overflow-x: auto; margin: 8px 0 10px; max-width: 100%; }
.dx-md-table { width: 100%; border-collapse: collapse; font-size: 13px; line-height: 1.45; }
.dx-md-table th, .dx-md-table td { border: 1px solid var(--dsw-alias-border-l2); padding: 6px 8px; text-align: left; vertical-align: top; }
.dx-md-table th { color: var(--dsw-alias-label-primary); background: var(--dsw-alias-bg-layer-2); font-weight: 600; }
.dx-stream-caret {
  display: inline-block; width: 6px; height: 16px; margin-left: 3px; vertical-align: text-bottom;
  background: var(--dsw-alias-label-primary); animation: dx-blink 1s step-end infinite;
}
@keyframes dx-blink { 50% { opacity: 0; } }
`;

// src/client/stage/WireEdge.tsx
var import_react_dom4 = require("react-dom");
var import_jsx_runtime20 = require("react/jsx-runtime");
function sideToPosition(side) {
  if (side === "left") return Position.Left;
  if (side === "right") return Position.Right;
  if (side === "top") return Position.Top;
  return Position.Bottom;
}
function asSize(value, fallback) {
  if (typeof value === "number" && Number.isFinite(value) && value > 0) return value;
  if (typeof value === "string") {
    const parsed = parseFloat(value);
    if (Number.isFinite(parsed) && parsed > 0) return parsed;
  }
  return fallback;
}
function nodeBox(node) {
  return {
    x: node.internals.positionAbsolute.x,
    y: node.internals.positionAbsolute.y,
    width: asSize(node.width, asSize(node.style?.width, 400)),
    height: asSize(node.height, asSize(node.style?.height, 220))
  };
}
function wirePath(sourceX, sourceY, sourceSide, targetX, targetY, targetSide) {
  return getBezierPath({
    sourceX,
    sourceY,
    sourcePosition: sideToPosition(sourceSide),
    targetX,
    targetY,
    targetPosition: sideToPosition(targetSide)
  });
}
function wireArrow(x3, y2, side, fill) {
  const length = 8;
  const half = 5;
  const points = side === "left" ? `${x3},${y2} ${x3 - length},${y2 - half} ${x3 - length},${y2 + half}` : side === "right" ? `${x3},${y2} ${x3 + length},${y2 - half} ${x3 + length},${y2 + half}` : side === "top" ? `${x3},${y2} ${x3 - half},${y2 - length} ${x3 + half},${y2 - length}` : `${x3},${y2} ${x3 - half},${y2 + length} ${x3 + half},${y2 + length}`;
  return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("polygon", { points, fill });
}
function WireEdge(props) {
  const source = useInternalNode(props.source);
  const target = useInternalNode(props.target);
  const from = source !== void 0 ? nodeBox(source) : void 0;
  const to = target !== void 0 ? nodeBox(target) : void 0;
  const routed = from !== void 0 && to !== void 0 ? routeDisplayPorts(from, to, props.sourceHandleId, props.targetHandleId) : {
    sourceX: props.sourceX,
    sourceY: props.sourceY,
    targetX: props.targetX,
    targetY: props.targetY,
    sourceSide: handleToSide(props.sourceHandleId) ?? "right",
    targetSide: handleToSide(props.targetHandleId) ?? "left"
  };
  const [path, labelX, labelY] = wirePath(
    routed.sourceX,
    routed.sourceY,
    routed.sourceSide,
    routed.targetX,
    routed.targetY,
    routed.targetSide
  );
  const selected2 = props.selected === true;
  const stroke = selected2 ? "rgba(255,255,255,.96)" : "rgba(236,236,236,.92)";
  const label2 = typeof props.label === "string" ? props.label.trim() : "";
  const showLabel = selected2 && label2 !== "" && label2 !== "\u627F\u63A5";
  return /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)(import_jsx_runtime20.Fragment, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
      BaseEdge,
      {
        id: props.id,
        path,
        interactionWidth: 36,
        style: { ...props.style, stroke: "transparent", strokeWidth: 2 }
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)(ViewportPortal, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("svg", { className: "dx-wire-edge", width: 1, height: 1, style: { position: "absolute", overflow: "visible", pointerEvents: "none", left: 0, top: 0, zIndex: 3 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("path", { d: path, fill: "none", stroke: "rgba(0,0,0,.5)", strokeWidth: 5 }),
        /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("path", { d: path, fill: "none", stroke, strokeWidth: selected2 ? 2.6 : 2.2 }),
        wireArrow(routed.targetX, routed.targetY, routed.targetSide, stroke)
      ] }),
      showLabel ? /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
        "div",
        {
          className: "dx-wire-label nodrag nopan",
          style: {
            position: "absolute",
            left: labelX,
            top: labelY,
            transform: "translate(-50%, -50%)",
            pointerEvents: "none",
            padding: "2px 7px",
            borderRadius: 999,
            background: "rgba(16,16,16,.86)",
            border: "1px solid rgba(255,255,255,.12)",
            color: dx.mute,
            fontSize: 10,
            fontFamily: dx.font,
            whiteSpace: "nowrap"
          },
          children: label2
        }
      ) : null
    ] })
  ] });
}
function HiddenConnectionLine(_props) {
  return null;
}
function handleScreenPoint(nodeId, handleId) {
  if (typeof document === "undefined") return void 0;
  const node = document.querySelector(`.react-flow__node[data-id="${CSS.escape(nodeId)}"]`);
  if (!(node instanceof HTMLElement)) return void 0;
  const wanted = handleId !== null && handleId !== void 0 && handleId !== "" ? handleId : "out";
  const handle2 = node.querySelector(`[data-handleid="${wanted}"]`) ?? node.querySelector(wanted === "in" ? ".react-flow__handle-left" : wanted === "top" ? ".react-flow__handle-top" : wanted === "bottom" ? ".react-flow__handle-bottom" : ".react-flow__handle-right");
  if (!(handle2 instanceof HTMLElement)) return void 0;
  const rect = handle2.getBoundingClientRect();
  if (rect.width < 1 && rect.height < 1) return void 0;
  return { x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 };
}
function WireDragLayer(props) {
  const side = handleToSide(props.fromSide) ?? "right";
  const [path] = getBezierPath({
    sourceX: props.from.x,
    sourceY: props.from.y,
    sourcePosition: sideToPosition(side),
    targetX: props.to.x,
    targetY: props.to.y,
    targetPosition: Position.Left
  });
  if (typeof document === "undefined") return null;
  return (0, import_react_dom4.createPortal)(
    /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)(
      "svg",
      {
        className: "dx-wire-drag",
        width: "100%",
        height: "100%",
        style: { position: "fixed", inset: 0, pointerEvents: "none", zIndex: 2147483e3, overflow: "visible" },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("path", { d: path, fill: "none", stroke: "rgba(0,0,0,.5)", strokeWidth: 5 }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("path", { d: path, fill: "none", stroke: props.valid === true ? "rgba(255,255,255,1)" : "rgba(243,243,243,.92)", strokeWidth: 2.25 }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("circle", { cx: props.from.x, cy: props.from.y, r: 4.5, fill: "#f3f3f3", stroke: "#111", strokeWidth: 1.5 }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("circle", { cx: props.to.x, cy: props.to.y, r: 5, fill: props.valid === true ? "#fff" : "#f3f3f3" })
        ]
      }
    ),
    document.body
  );
}
function boxPort(box2, handle2) {
  const side = handleToSide(handle2) ?? "right";
  return { ...portPoint(box2, side), side };
}
function closestHandleId(box2, point) {
  const ports = [
    { id: "in", ...portPoint(box2, "left") },
    { id: "out", ...portPoint(box2, "right") },
    { id: "top", ...portPoint(box2, "top") },
    { id: "bottom", ...portPoint(box2, "bottom") }
  ];
  return ports.reduce((best, port) => {
    const distance2 = (port.x - point.x) ** 2 + (port.y - point.y) ** 2;
    return distance2 < best.distance ? { id: port.id, distance: distance2 } : best;
  }, { id: "in", distance: Number.POSITIVE_INFINITY }).id;
}

// src/client/stage/Stage.tsx
var import_jsx_runtime21 = require("react/jsx-runtime");
var EDGE_STYLE = { stroke: "rgba(255,255,255,.62)", strokeWidth: 2 };
var shell = {
  position: "relative",
  width: "100%",
  height: "100%",
  minWidth: 0,
  minHeight: 0,
  display: "flex",
  flexDirection: "column",
  background: dx.field,
  color: dx.ink,
  fontFamily: dx.font,
  overflow: "hidden"
};
var STAGE_CSS = `
.dx-stage, .dx-stage * { box-sizing: border-box; }
.dx-stage .react-flow__background { pointer-events: none !important; }
.dx-stage .react-flow__pane { cursor: grab; }
.dx-stage .react-flow__pane:active { cursor: grabbing; }
.dx-stage .react-flow__node,
.dx-stage .react-flow__node-media,
.dx-stage .react-flow__node-text,
.dx-stage .react-flow__node-group {
  position: absolute !important;
  overflow: visible !important;
  background: transparent !important;
  padding: 0 !important;
  border: none !important;
  box-shadow: none !important;
}
    .dx-stage .react-flow__handle {
      position: absolute !important;
      width: 24px !important; height: 24px !important; min-width: 24px; min-height: 24px;
      background: transparent !important; border: none !important; opacity: 1;
      pointer-events: all !important; z-index: 6;
    }
.dx-stage .react-flow__handle.react-flow__handle-left {
  left: -20px !important; right: auto !important; top: 50% !important;
  transform: translate(0, -50%) !important;
}
.dx-stage .react-flow__handle.react-flow__handle-right {
  right: -20px !important; left: auto !important; top: 50% !important;
  transform: translate(0, -50%) !important;
}
.dx-stage .react-flow__handle.react-flow__handle-top {
  top: -18px !important; bottom: auto !important; left: 50% !important;
  transform: translate(-50%, 0) !important;
}
.dx-stage .react-flow__handle.react-flow__handle-bottom {
  bottom: -18px !important; top: auto !important; left: 50% !important;
  transform: translate(-50%, 0) !important;
}
.dx-stage .react-flow__handle-top .dx-port-plus,
.dx-stage .react-flow__handle-bottom .dx-port-plus,
.dx-stage .react-flow__handle-top .dx-port-dot,
.dx-stage .react-flow__handle-bottom .dx-port-dot { opacity: 0; }
.dx-stage.dx-wiring .react-flow__handle-top .dx-port-dot,
.dx-stage.dx-wiring .react-flow__handle-bottom .dx-port-dot,
.dx-stage .react-flow__node:hover .react-flow__handle-top .dx-port-dot,
.dx-stage .react-flow__node:hover .react-flow__handle-bottom .dx-port-dot { opacity: .7; }
.dx-port-dot {
  display: block; width: 10px; height: 10px; border-radius: 99px;
  background: #f3f3f3; border: 2px solid #111;
  opacity: 0; transition: opacity .15s ease, transform .15s ease;
}
    .dx-port-plus {
      display: grid; place-items: center; width: 24px; height: 24px; border-radius: 99px;
      background: #f3f3f3; color: #141414; box-shadow: 0 4px 14px rgba(0,0,0,.35);
      opacity: 0; transition: opacity .15s ease, transform .15s ease;
    }
.dx-stage .react-flow__node:hover .dx-port-plus,
.dx-stage .react-flow__node.selected .dx-port-plus,
.dx-stage .react-flow__handle-connecting .dx-port-plus { opacity: 1; }
.dx-stage .react-flow__node.selected .react-flow__handle-left .dx-port-dot,
.dx-stage .react-flow__node.selected .react-flow__handle-right .dx-port-dot,
.dx-stage .react-flow__node:hover .react-flow__handle-left .dx-port-dot,
.dx-stage .react-flow__node:hover .react-flow__handle-right .dx-port-dot { opacity: 1; }
    .dx-stage.dx-wiring .dx-port-dot,
    .dx-stage.dx-wiring .dx-port-plus { opacity: 1; }
    .dx-stage.dx-wiring,
    .dx-stage.dx-wiring * {
      user-select: none !important;
      -webkit-user-select: none !important;
    }
    .dx-stage.dx-wiring .react-flow__node { cursor: crosshair; }
.dx-stage.dx-wiring .react-flow__node.dx-can-connect .dx-card-face,
.dx-stage.dx-wiring .react-flow__node:hover .dx-card-face {
  border-color: rgba(255,255,255,.88) !important;
  box-shadow: 0 0 0 2px rgba(255,255,255,.78), 0 0 28px rgba(255,255,255,.22) !important;
  animation: dx-connect-glow 1.05s ease-in-out infinite;
}
@keyframes dx-connect-glow {
  0%, 100% { box-shadow: 0 0 0 2px rgba(255,255,255,.72), 0 0 18px rgba(255,255,255,.16); }
  50% { box-shadow: 0 0 0 3px rgba(255,255,255,.95), 0 0 34px rgba(255,255,255,.32); }
}
.dx-stage .react-flow__connectionline,
.dx-stage svg.react-flow__connectionline {
  pointer-events: none !important;
  opacity: 0 !important;
  visibility: hidden !important;
}
.dx-stage .react-flow__renderer,
.dx-stage .react-flow__viewport,
.dx-stage .react-flow__edges,
.dx-stage .react-flow__edges svg {
  overflow: visible !important;
}
.dx-stage .react-flow__edges { z-index: 3 !important; }
.dx-stage .react-flow__nodes { z-index: 4; }
.dx-stage .react-flow__edge-path {
  stroke-linecap: round;
  stroke: transparent !important;
  fill: none !important;
}
.dx-stage .react-flow__edgeupdater {
  display: none !important;
}
.dx-stage .react-flow__arrowhead,
.dx-stage marker {
  display: none !important;
}
.dx-stage .react-flow__edge.selected .react-flow__edge-path,
.dx-stage .react-flow__edge:focus .react-flow__edge-path {
  stroke: transparent !important;
}
.dx-stage .react-flow__connection-line { stroke: rgba(255,255,255,.8); stroke-width: 2px; }
.dx-stage .react-flow__selection { background: rgba(255,255,255,.05); border: 1px solid rgba(255,255,255,.28); }
.dx-stage .react-flow__minimap,
.dx-stage .react-flow__panel.react-flow__minimap {
  position: absolute !important;
  top: auto !important;
  left: 16px !important;
  right: auto !important;
  bottom: 64px !important;
  margin: 0 !important;
  z-index: 18 !important;
  overflow: hidden;
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,.1);
  box-shadow: 0 10px 28px rgba(0,0,0,.4);
}
    .dx-node-toolbar {
      display: flex; align-items: center; gap: 4px; padding: 4px; height: 48px; box-sizing: border-box;
      background: rgba(31,31,31,.80); border: 1px solid rgba(255,255,255,.10);
      border-radius: 9999px; box-shadow: none;
      backdrop-filter: blur(16px);
    }
.dx-hit[data-tip]:not(.dx-session-fab) { position: relative; }
[data-tip]::after {
  content: attr(data-tip);
  position: absolute;
  z-index: 50;
  left: 50%;
  bottom: calc(100% + 8px);
  transform: translateX(-50%) translateY(4px);
  padding: 5px 8px;
  border-radius: 8px;
  font-size: 11px;
  font-weight: 500;
  line-height: 1.2;
  white-space: nowrap;
  color: #f2f2f2;
  background: rgba(16,16,16,.94);
  border: 1px solid rgba(255,255,255,.12);
  opacity: 0;
  pointer-events: none;
  transition: opacity .14s ease, transform .14s ease;
}
[data-tip]:hover::after { opacity: 1; transform: translateX(-50%) translateY(0); }
[data-tip-side='right']::after {
  left: calc(100% + 10px);
  right: auto;
  bottom: auto;
  top: 50%;
  transform: translateY(-50%) translateX(-4px);
}
[data-tip-side='right']:hover::after { transform: translateY(-50%) translateX(0); }
.dx-hit { transition: background .14s ease, transform .14s ease; }
.dx-hit:hover { background: rgba(255,255,255,.08) !important; }
.dx-hit:active { transform: scale(.97); }
  .dx-stage.dx-editing-image .react-flow__handle,
  .dx-stage.dx-editing-image .react-flow__edge,
  .dx-stage.dx-editing-image .dx-node-toolbar,
  .dx-stage.dx-editing-image .dx-card-caption,
  .dx-stage.dx-editing-image .dx-card-dock {
    opacity: 0 !important;
    pointer-events: none !important;
  }
  .dx-stage.dx-session-open .react-flow {
    width: calc(100% - 420px) !important;
  }
.dx-menu-item:hover { background: rgba(255,255,255,.08) !important; }
.dx-cta { transition: transform .14s ease, opacity .14s ease; }
.dx-cta:hover { transform: translateY(-1px); }
.dx-cta:active { transform: scale(.98); }
.dx-rail-btn { position: relative; z-index: 2; }
.dx-title:focus { border-color: rgba(255,255,255,.28) !important; }
.dx-node-title:hover, .dx-node-title:focus {
  border-color: rgba(255,255,255,.16) !important;
  background: rgba(12,12,12,.72) !important;
}
    .dx-card-caption {
      pointer-events: auto;
      position: absolute; left: 0; right: 0; top: -28px; bottom: auto; z-index: 6;
    }
    .dx-card-dock {
      position: absolute; left: 50%; top: 100%; transform: translateX(-50%); margin-top: 12px; z-index: 8;
      width: 640px;
    }
    .dx-card-meta {
      display: flex; align-items: center; gap: 6px; width: 100%; min-height: 22px;
      padding: 0 2px;
      background: none;
    }
    .dx-shot-no {
      flex-shrink: 0; min-width: 28px; color: #fff; font-size: 11px; font-weight: 600;
      font-variant-numeric: tabular-nums; letter-spacing: 0.04em;
    }
    .dx-card-lock { color: rgba(255,255,255,.72); display: grid; place-items: center; flex-shrink: 0; }
    .dx-face-fill { background: #1f1f1f !important; }
    .dx-kind-badge {
      position: absolute; top: 8px; left: 8px; height: 22px; min-width: 22px; padding: 0 7px;
      border-radius: 7px; display: inline-flex; align-items: center; gap: 5px;
      color: #fff; font-size: 10px; font-weight: 500; letter-spacing: 0.2px;
      background: rgba(10,10,10,.55); border: 1px solid rgba(255,255,255,.1); backdrop-filter: blur(8px);
    }
    .dx-kind-time { left: auto; right: 8px; }
    .dx-kind-image { background: #1f1f1f; }
    .dx-kind-video { background: #1f1f1f; }
    .dx-kind-text { background: #1f1f1f; }
.dx-stage .react-flow__node .dx-card-face {
  position: relative;
  transition: border-color .16s ease, box-shadow .16s ease;
}
.dx-stage .react-flow__node:hover .dx-card-face {
  border-color: rgba(255,255,255,.22);
}
.dx-media-well {
  position: absolute; inset: 0; height: auto; width: auto; background: #111;
}
    .dx-kind-image .dx-media-well, .dx-kind-video .dx-media-well, .dx-kind-audio .dx-media-well { background: #111; }
    .dx-media-fill { background: #1f1f1f; }
.dx-media-bleed, .dx-media-img {
  position: absolute; inset: 0; width: 100%; height: 100%;
}
    .dx-media-img, .dx-media-bleed video {
      object-fit: contain; display: block; width: 100%; height: 100%; background: #111;
    }
    .dx-audio-well {
      position: absolute; inset: 0; display: flex; flex-direction: column;
      align-items: center; justify-content: center; gap: 14px; padding: 16px;
    }
    .dx-audio-well audio { width: 100%; max-width: 320px; }
    .dx-audio-bars { display: flex; align-items: flex-end; gap: 3px; height: 28px; }
    .dx-audio-bars i {
      display: block; width: 3px; border-radius: 2px; background: rgba(255,255,255,.55);
      animation: dx-audio-bar .9s ease-in-out infinite;
    }
    .dx-audio-bars i:nth-child(1) { height: 8px; animation-delay: 0s; }
    .dx-audio-bars i:nth-child(2) { height: 16px; animation-delay: .1s; }
    .dx-audio-bars i:nth-child(3) { height: 24px; animation-delay: .18s; }
    .dx-audio-bars i:nth-child(4) { height: 14px; animation-delay: .08s; }
    .dx-audio-bars i:nth-child(5) { height: 10px; animation-delay: .22s; }
    @keyframes dx-audio-bar { 50% { transform: scaleY(1.7); } }
    .dx-preview {
      position: fixed; inset: 0; z-index: 40; background: rgba(0,0,0,.84);
      display: grid; place-items: center;
    }
    .dx-preview img, .dx-preview video { max-width: 92vw; max-height: 92vh; }
.dx-film::before, .dx-film::after {
  content: ''; position: absolute; top: 0; bottom: 0; width: 8px; z-index: 2; pointer-events: none;
  background: repeating-linear-gradient(180deg, rgba(255,255,255,.1) 0 3px, transparent 3px 11px);
  opacity: .28;
}
.dx-film::before { left: 0; }
    .dx-empty-plate {
      position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 10px;
      border: none; border-radius: 16px;
      background: #1f1f1f;
    }
    .dx-empty-glyph { display: grid; place-items: center; color: rgba(255,255,255,.28); }
    .dx-empty-glyph svg { width: 26px; height: 26px; }
    .dx-replace-chip {
      position: absolute; top: 8px; right: 8px; z-index: 5;
      height: 26px; padding: 0 10px; border-radius: 10px; border: none;
      background: rgba(20,20,20,.82); color: #f5f5f5; font-size: 12px; font-family: inherit;
      opacity: 0; pointer-events: none;
    }
    .dx-stage .react-flow__node:hover .dx-replace-chip,
    .dx-stage .react-flow__node.selected .dx-replace-chip { opacity: 1; pointer-events: auto; }
    .dx-enter-chip {
      height: 28px; padding: 0 12px; border-radius: 10px; border: 1px solid rgba(255,255,255,.12);
      background: rgba(255,255,255,.08); color: #f5f5f5; font-size: 12px; font-family: inherit; cursor: pointer;
    }
.dx-status-dot { width: 6px; height: 6px; border-radius: 99px; display: inline-block; flex-shrink: 0; }
.dx-multibar { scrollbar-width: none; }
.dx-multibar::-webkit-scrollbar { display: none; }
    .dx-text-sheet {
      background: #1f1f1f;
    }
.dx-act-frame { position: relative; }
.dx-act-frame::before {
  content: ''; position: absolute; left: 0; top: 18px; bottom: 18px; width: 3px; border-radius: 99px;
  background: rgba(255,255,255,.14);
}
.dx-generating { position: relative; }
.dx-face-live {
  box-shadow: 0 0 0 1px rgba(240,195,106,.28), 0 18px 40px rgba(0,0,0,.42) !important;
}
.dx-gen-overlay {
  position: absolute; inset: 0; z-index: 4; pointer-events: none;
  display: grid; place-items: center;
  background: radial-gradient(80% 70% at 50% 40%, rgba(20,16,8,.28), rgba(6,6,6,.72));
  overflow: hidden;
}
.dx-gen-scan {
  position: absolute; left: 0; right: 0; height: 34%;
  background: linear-gradient(180deg, transparent, rgba(240,195,106,.2), transparent);
  animation: dx-gen-scan 1.7s ease-in-out infinite;
}
@keyframes dx-gen-scan { 0% { top: -36%; } 100% { top: 102%; } }
.dx-gen-core {
  position: relative; z-index: 1; display: grid; place-items: center; gap: 8px; text-align: center;
  padding: 0 18px;
}
.dx-gen-ring {
  width: 36px; height: 36px; border-radius: 99px;
  border: 2px solid rgba(240,195,106,.22); border-top-color: #f0c36a;
  animation: dx-rotate .8s linear infinite;
}
.dx-gen-bars { display: flex; align-items: flex-end; gap: 3px; height: 14px; }
.dx-gen-bars i {
  display: block; width: 3px; height: 5px; border-radius: 2px; background: #f0c36a;
  animation: dx-bar .62s ease-in-out infinite alternate;
}
.dx-gen-bars i:nth-child(2) { animation-delay: .1s; }
.dx-gen-bars i:nth-child(3) { animation-delay: .2s; }
.dx-gen-bars i:nth-child(4) { animation-delay: .3s; }
@keyframes dx-bar { from { height: 4px; } to { height: 14px; } }
.dx-gen-copy { color: #f0c36a; font-size: 12px; font-weight: 600; letter-spacing: 0.3px; }
.dx-gen-prompt { color: #b9b0a0; font-size: 10.5px; line-height: 1.4; max-width: 92%; }
.dx-spin {
  width: 12px; height: 12px; border-radius: 99px;
  border: 1.5px solid rgba(20,20,20,.25); border-top-color: #141414;
  animation: dx-rotate .7s linear infinite; display: block;
}
@keyframes dx-rotate { to { transform: rotate(360deg); } }
.dx-dot, .dx-dot-warn {
  width: 6px; height: 6px; border-radius: 99px; display: inline-block; background: #6fdf86;
}
.dx-dot-warn { background: #e8c36a; }
.dx-scrim { background: rgba(0,0,0,.55); backdrop-filter: blur(8px); }
.dx-range { appearance: none; height: 2px; background: rgba(255,255,255,.18); border-radius: 99px; outline: none; }
.dx-range::-webkit-slider-thumb { appearance: none; width: 12px; height: 12px; border-radius: 99px; background: #f3f3f3; cursor: pointer; }
.dx-session-log { scrollbar-width: thin; scrollbar-color: rgba(255,255,255,.22) transparent; }
.dx-session-fab[data-running='true'] { box-shadow: 0 0 0 1px rgba(111,223,134,.45), 0 12px 28px rgba(0,0,0,.4); }
.dx-md { font-size: 13px; line-height: 1.62; color: #f1f1f1; word-break: break-word; }
.dx-md > *:first-child { margin-top: 0; }
.dx-md > *:last-child { margin-bottom: 0; }
.dx-md h1, .dx-md h2, .dx-md h3 { font-weight: 600; letter-spacing: -0.28px; margin: 12px 0 6px; line-height: 1.3; }
.dx-md h1 { font-size: 17px; }
.dx-md h2 { font-size: 15px; }
.dx-md h3 { font-size: 13.5px; }
.dx-md p { margin: 0 0 8px; }
.dx-md strong { font-weight: 600; }
.dx-md em { font-style: italic; color: #e8e8e8; }
.dx-md del { opacity: .62; }
.dx-md code {
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  font-size: 11.5px; background: rgba(255,255,255,.08);
  padding: 1px 5px; border-radius: 5px;
}
.dx-md pre {
  background: rgba(0,0,0,.38); border: 1px solid rgba(255,255,255,.08);
  border-radius: 10px; padding: 10px 12px; overflow: auto; margin: 8px 0;
}
.dx-md pre code { background: none; padding: 0; font-size: 11.5px; white-space: pre; }
.dx-md ul, .dx-md ol { margin: 4px 0 8px; padding-left: 1.2em; }
.dx-md li { margin: 3px 0; }
.dx-md blockquote {
  margin: 8px 0; padding: 2px 0 2px 10px;
  border-left: 2px solid rgba(255,255,255,.22); color: #c4c4c4;
}
.dx-md a { color: #c9d4ff; text-decoration: underline; text-underline-offset: 2px; }
.dx-md hr { border: none; border-top: 1px solid rgba(255,255,255,.1); margin: 10px 0; }
.dx-stage .react-flow__node:hover .dx-port-dot { transform: scale(1.12); }
.dx-stage.dx-hide-wires .react-flow__edge,
.dx-stage.dx-hide-wires .dx-wire-edge,
.dx-stage.dx-hide-wires .dx-wire-label,
.dx-stage.dx-hide-wires .dx-wire-halo { opacity: 0 !important; pointer-events: none !important; }
.dx-stage .react-flow__node { outline: none !important; }
.dx-stage .react-flow__node:not(.dx-can-connect) { box-shadow: none !important; }
.dx-stage .react-flow__resize-control { position: absolute !important; }
.dx-stage .react-flow__resize-control.line { display: none !important; }
.dx-stage .react-flow__resize-control.handle {
  width: 6px !important; height: 6px !important; border-radius: 1px;
  background: #f3f3f3 !important; border: none !important;
}
.dx-stage .react-flow__resize-control.handle-top,
.dx-stage .react-flow__resize-control.handle-right,
.dx-stage .react-flow__resize-control.handle-bottom,
.dx-stage .react-flow__resize-control.handle-left,
.dx-stage .react-flow__resize-control.handle.top,
.dx-stage .react-flow__resize-control.handle.right,
.dx-stage .react-flow__resize-control.handle.bottom,
.dx-stage .react-flow__resize-control.handle.left,
.dx-stage .react-flow__resize-control.line.top,
.dx-stage .react-flow__resize-control.line.right,
.dx-stage .react-flow__resize-control.line.bottom,
.dx-stage .react-flow__resize-control.line.left {
  display: none !important;
}
.dx-stage .react-flow__resize-control.top.left { top: 0 !important; left: 0 !important; }
.dx-stage .react-flow__resize-control.top.right { top: 0 !important; right: 0 !important; }
.dx-stage .react-flow__resize-control.bottom.left { bottom: 0 !important; left: 0 !important; }
.dx-stage .react-flow__resize-control.bottom.right { bottom: 0 !important; right: 0 !important; }
@media (max-width: 900px) {
  .dx-topbar { left: 16px !important; }
  .dx-zoomhud .dx-range { display: none !important; }
  .dx-title { width: 148px !important; max-width: 32vw !important; }
  .dx-gendock { left: 16px !important; }
  .dx-asset-drawer { left: 16px !important; width: min(360px, calc(100% - 32px)) !important; }
}
@media (max-width: 760px) {
  .dx-topbar-meta { display: none !important; }
  .dx-hero-keys { display: none !important; }
  .dx-title { width: 112px !important; max-width: 28vw !important; }
  .dx-inspector { top: 64px !important; width: min(276px, calc(100% - 28px)) !important; }
}
`;
async function saveBlob(blob, name2, mediaType) {
  const response = await fetch(withProject("/directorx/media"), {
    method: "POST",
    headers: { "content-type": mediaType, "x-directorx-name": encodeURIComponent(name2), ...projectHeaders() },
    body: blob
  });
  if (!response.ok) throw new Error(`\u4FDD\u5B58\u5931\u8D25 (HTTP ${response.status})`);
  return response.json();
}
function StageInner(props) {
  const snapshot2 = (0, import_react19.useSyncExternalStore)(subscribeEditor, editorSnapshot);
  const { fitView, zoomIn, zoomOut, screenToFlowPosition, flowToScreenPosition, setViewport, getViewport } = useReactFlow();
  const { zoom } = useViewport();
  const [nodes, setNodes] = (0, import_react19.useState)([]);
  const [edges, setEdges] = (0, import_react19.useState)([]);
  const nodesRef = (0, import_react19.useRef)(nodes);
  const edgesRef = (0, import_react19.useRef)(edges);
  const saveRef = (0, import_react19.useRef)(() => {
  });
  (0, import_react19.useEffect)(() => {
    nodesRef.current = nodes;
    edgesRef.current = edges;
  }, [nodes, edges]);
  const updatedAtRef = (0, import_react19.useRef)(0);
  const dirtyRef = (0, import_react19.useRef)(false);
  const titleRef = (0, import_react19.useRef)("\u672A\u547D\u540D\u753B\u5E03");
  const [title, setTitle] = (0, import_react19.useState)("\u672A\u547D\u540D\u753B\u5E03");
  const [saveState, setSaveState] = (0, import_react19.useState)("\u5DF2\u4FDD\u5B58");
  const [error, setError] = (0, import_react19.useState)(void 0);
  const [box2, setBox] = (0, import_react19.useState)({ width: 0, height: 0 });
  const stageRef = (0, import_react19.useRef)(null);
  const [picker, setPicker] = (0, import_react19.useState)(false);
  const [files, setFiles] = (0, import_react19.useState)([]);
  const [askBusy, setAskBusy] = (0, import_react19.useState)(false);
  const [studioUrl, setStudioUrl] = (0, import_react19.useState)(void 0);
  const [studioError, setStudioError] = (0, import_react19.useState)(void 0);
  const undoRef = (0, import_react19.useRef)([]);
  const redoRef = (0, import_react19.useRef)([]);
  const [historyTick, setHistoryTick] = (0, import_react19.useState)(0);
  const [helpOpen, setHelpOpen] = (0, import_react19.useState)(false);
  const [toast, setToast] = (0, import_react19.useState)();
  const uploadRef = (0, import_react19.useRef)(null);
  const uploadTargetRef = (0, import_react19.useRef)(void 0);
  const composeRef = (0, import_react19.useRef)(null);
  const fittedRef = (0, import_react19.useRef)(false);
  const nodeCountRef = (0, import_react19.useRef)(0);
  const [projects, setProjects] = (0, import_react19.useState)([]);
  const [project, setProject] = (0, import_react19.useState)(getClientProject());
  const [addMenu, setAddMenu] = (0, import_react19.useState)(void 0);
  const [nodeMenu, setNodeMenu] = (0, import_react19.useState)(void 0);
  const [reshootId, setReshootId] = (0, import_react19.useState)(void 0);
  const [reviseId, setReviseId] = (0, import_react19.useState)(void 0);
  const [parsePreview, setParsePreview] = (0, import_react19.useState)(void 0);
  const [cropId, setCropId] = (0, import_react19.useState)(void 0);
  const [cropAspect, setCropAspect] = (0, import_react19.useState)("free");
  const [maskId, setMaskId] = (0, import_react19.useState)(void 0);
  const [maskMode, setMaskMode] = (0, import_react19.useState)(void 0);
  const [expandId, setExpandId] = (0, import_react19.useState)(void 0);
  const editRestoreRef = (0, import_react19.useRef)(void 0);
  const [edgeMenu, setEdgeMenu] = (0, import_react19.useState)(void 0);
  const [compose, setCompose] = (0, import_react19.useState)({
    kind: "image",
    prompt: "",
    count: 1,
    aspect: "16:9"
  });
  const [generateOpen, setGenerateOpen] = (0, import_react19.useState)(false);
  const [searchOpen, setSearchOpen] = (0, import_react19.useState)(false);
  const [searchQuery, setSearchQuery] = (0, import_react19.useState)("");
  const [searchFilter, setSearchFilter] = (0, import_react19.useState)("");
  const [snap, setSnap] = (0, import_react19.useState)(false);
  const [hideWires, setHideWires] = (0, import_react19.useState)(false);
  const [libraryTab, setLibraryTab] = (0, import_react19.useState)("media");
  const [libraryQuery, setLibraryQuery] = (0, import_react19.useState)("");
  const [characters, setCharacters] = (0, import_react19.useState)([]);
  const [compare, setCompare] = (0, import_react19.useState)(void 0);
  const [sessionOpen, setSessionOpen] = (0, import_react19.useState)(false);
  const [sessionPulse, setSessionPulse] = (0, import_react19.useState)(0);
  const [boundSessionId, setBoundSessionId] = (0, import_react19.useState)();
  const onSessionOpenChange = (0, import_react19.useCallback)((open) => {
    setSessionOpen(open);
  }, []);
  const connectRef = (0, import_react19.useRef)(void 0);
  const connectHandledRef = (0, import_react19.useRef)(false);
  const studioSeenRef = (0, import_react19.useRef)("");
  const nodeAbsBox = (0, import_react19.useCallback)((id2) => {
    const current = nodesRef.current;
    const node = current.find((item3) => item3.id === id2);
    if (node === void 0) return void 0;
    const located = current.map((item3) => ({ id: item3.id, position: item3.position, parentId: item3.parentId }));
    const byId = new Map(located.map((item3) => [item3.id, item3]));
    const origin = flowAbsolutePosition({ id: node.id, position: node.position, parentId: node.parentId }, byId);
    const width = typeof node.width === "number" ? node.width : typeof node.style?.width === "number" ? node.style.width : defaultSize2(node.data.kind ?? node.type ?? "image").width;
    const height = typeof node.height === "number" ? node.height : typeof node.style?.height === "number" ? node.style.height : defaultSize2(node.data.kind ?? node.type ?? "image").height;
    return { x: origin.x, y: origin.y, width, height };
  }, []);
  const [wiring, setWiring] = (0, import_react19.useState)(false);
  const [wireMenu, setWireMenu] = (0, import_react19.useState)(void 0);
  const [wireDrag, setWireDrag] = (0, import_react19.useState)(void 0);
  const edgeTypes = (0, import_react19.useMemo)(() => ({ wire: WireEdge }), []);
  const selected2 = (0, import_react19.useMemo)(() => nodes.find((node) => node.selected === true), [nodes]);
  const selectedNodes = (0, import_react19.useMemo)(() => nodes.filter((node) => node.selected === true), [nodes]);
  const paintedNodes = (0, import_react19.useMemo)(() => nodes.map((node) => {
    if (cropId !== node.id && maskId !== node.id && expandId !== node.id) return node;
    return {
      ...node,
      draggable: false,
      data: {
        ...node.data,
        ...cropId === node.id ? { cropping: true, cropAspect: CROP_ASPECTS.find((item3) => item3.id === cropAspect)?.value } : {},
        ...maskId === node.id && maskMode !== void 0 ? { maskMode } : {},
        ...expandId === node.id ? { expanding: true } : {}
      }
    };
  }), [cropAspect, cropId, expandId, maskId, maskMode, nodes]);
  const [previewId, setPreviewId] = (0, import_react19.useState)(void 0);
  const actionRef = (0, import_react19.useRef)({
    openGenerate: (_id) => {
    },
    openStudioFor: (_id) => {
    },
    rename: (_id, _label) => {
    },
    cycleStatus: (_id) => {
    },
    duplicate: (_id) => {
    },
    lock: (_id) => {
    },
    download: (_id) => {
    },
    patch: (_id, _patch) => {
    },
    adopt: (_id) => {
    },
    focusTake: (_id) => {
    },
    remove: (_id) => {
    },
    runGenerate: (_id) => {
    },
    upload: (_id) => {
    },
    pickRef: (_id) => {
    },
    crop: (_id) => {
    },
    confirmCrop: (_id, _blob) => {
    },
    cancelCrop: () => {
    },
    preview: (_id) => {
    },
    redraw: (_id) => {
    },
    erase: (_id) => {
    },
    expand: (_id) => {
    },
    expanded: (_id, _padded, _mask) => {
    },
    annotate: (_id) => {
    },
    enhance: (_id) => {
    },
    pixels: (_id) => {
    },
    cutout: (_id) => {
    },
    split: (_id, _cols, _rows) => {
    },
    capture: (_id) => {
    },
    extend: (_id) => {
    },
    reshoot: (_id) => {
    },
    masked: (_id, _blob, _prompt, _mode) => {
    },
    cancelMask: () => {
    }
  });
  const attachActions = (0, import_react19.useCallback)((node) => ({
    ...node,
    draggable: node.data.locked !== true,
    data: {
      ...node.data,
      onGenerate: (id2) => actionRef.current.runGenerate(id2),
      onEdit: (id2) => actionRef.current.openStudioFor(id2),
      onRename: (id2, label2) => actionRef.current.rename(id2, label2),
      onCycleStatus: (id2) => actionRef.current.cycleStatus(id2),
      onDuplicate: (id2) => actionRef.current.duplicate(id2),
      onLock: (id2) => actionRef.current.lock(id2),
      onDownload: (id2) => actionRef.current.download(id2),
      onPatch: (id2, patch) => actionRef.current.patch(id2, patch),
      onAdoptTake: (id2) => actionRef.current.adopt(id2),
      onFocusTake: (id2) => actionRef.current.focusTake(id2),
      onDelete: (id2) => actionRef.current.remove(id2),
      onUpload: (id2) => actionRef.current.upload(id2),
      onPickRef: (id2) => actionRef.current.pickRef(id2),
      onCrop: (id2) => actionRef.current.crop(id2),
      onCropped: (id2, blob) => actionRef.current.confirmCrop(id2, blob),
      onPreview: (id2) => actionRef.current.preview(id2),
      onRedraw: (id2) => actionRef.current.redraw(id2),
      onErase: (id2) => actionRef.current.erase(id2),
      onExpand: (id2) => actionRef.current.expand(id2),
      onExpanded: (id2, padded, mask) => actionRef.current.expanded(id2, padded, mask),
      onAnnotate: (id2) => actionRef.current.annotate(id2),
      onEnhance: (id2) => actionRef.current.enhance(id2),
      onPixels: (id2) => actionRef.current.pixels(id2),
      onCutout: (id2) => actionRef.current.cutout(id2),
      onSplit: (id2, cols, rows) => actionRef.current.split(id2, cols, rows),
      onCapture: (id2) => actionRef.current.capture(id2),
      onExtend: (id2) => actionRef.current.extend(id2),
      onReshoot: (id2) => actionRef.current.reshoot(id2),
      onMasked: (id2, blob, prompt, mode) => actionRef.current.masked(id2, blob, prompt, mode),
      onClearRevise: () => setReviseId(void 0),
      ...reviseId === node.id ? { revise: true } : {}
    }
  }), [reviseId]);
  (0, import_react19.useEffect)(() => {
    if (document.getElementById("dx-fonts") !== null) return;
    const link2 = document.createElement("link");
    link2.id = "dx-fonts";
    link2.rel = "stylesheet";
    link2.href = INTER_HREF;
    document.head.appendChild(link2);
  }, []);
  (0, import_react19.useEffect)(() => {
    const el = stageRef.current;
    if (el === null) return;
    const apply2 = () => {
      const rect = el.getBoundingClientRect();
      setBox({ width: Math.round(rect.width), height: Math.round(rect.height) });
    };
    apply2();
    const observer = new ResizeObserver(apply2);
    observer.observe(el);
    return () => observer.disconnect();
  }, []);
  const applyDoc = (0, import_react19.useCallback)((doc) => {
    updatedAtRef.current = doc.updatedAt;
    const nextTitle = typeof doc.title === "string" && doc.title !== "" ? doc.title : "\u672A\u547D\u540D\u753B\u5E03";
    titleRef.current = nextTitle;
    setTitle(nextTitle);
    setNodes(toFlowNodes(doc).map((node) => attachActions(node)));
    setEdges(toFlowEdges(doc));
    const prevCount = nodeCountRef.current;
    nodeCountRef.current = doc.nodes.length;
    const shouldFit = doc.nodes.length > 0 && (!fittedRef.current || prevCount === 0 || doc.nodes.length - prevCount >= 4);
    if (shouldFit) {
      fittedRef.current = true;
      const mapped = toFlowNodes(doc);
      window.requestAnimationFrame(() => {
        void fitView({ nodes: mapped, padding: 0.2, duration: 280, maxZoom: 0.9, minZoom: 0.18 });
      });
    }
    const workspace2 = editorSnapshot().workspace;
    if (workspace2 !== null) {
      const hit = doc.nodes.find((node) => node.id === workspace2.nodeId);
      if (hit === void 0 || hit.kind !== workspace2.kind) closeWorkspace();
    }
  }, [attachActions, fitView]);
  const load = (0, import_react19.useCallback)(async () => {
    try {
      const response = await fetch(withProject("/directorx/canvas"), { headers: projectHeaders() });
      if (!response.ok) throw new Error(`\u753B\u5E03\u52A0\u8F7D\u5931\u8D25 (${response.status})`);
      applyDoc(await response.json());
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [applyDoc]);
  (0, import_react19.useEffect)(() => {
    let live = true;
    const tick = async () => {
      try {
        const response = await fetch(withProject("/directorx/studio"), { headers: projectHeaders() });
        if (!response.ok) return;
        const body = await response.json();
        const ticket = body.ticket;
        if (!live || ticket === void 0 || typeof ticket.id !== "string" || typeof ticket.path !== "string") return;
        if (studioSeenRef.current === "") {
          if (typeof ticket.at === "number" && Date.now() - ticket.at > 3e4) {
            studioSeenRef.current = ticket.id;
            return;
          }
        }
        if (ticket.id === studioSeenRef.current) return;
        studioSeenRef.current = ticket.id;
        if (ticket.kind !== "image" && ticket.kind !== "video") return;
        openEditor(ticket.kind, ticket.path, ticket.look !== void 0 ? { look: ticket.look } : void 0);
        void load();
      } catch {
      }
    };
    void tick();
    const timer2 = window.setInterval(() => {
      void tick();
    }, 1400);
    return () => {
      live = false;
      window.clearInterval(timer2);
    };
  }, [load, project]);
  (0, import_react19.useEffect)(() => {
    void fetch("/directorx/projects").then((item3) => item3.json()).then((data) => {
      const list = data.projects ?? [];
      setProjects(list);
      const next = pickDefaultProject(list, props.preferredProject);
      if (next !== void 0) {
        setClientProject(next);
        setProject(next);
      }
    }).catch(() => {
    });
  }, [props.preferredProject]);
  (0, import_react19.useEffect)(() => {
    if (project === void 0) return;
    setClientProject(project);
    dirtyRef.current = false;
    fittedRef.current = false;
    void load();
  }, [project, load]);
  (0, import_react19.useEffect)(() => {
    let cancelled = false;
    const bind = async () => {
      if (props.sessionId !== void 0 && props.sessionId !== "") {
        setBoundSessionId(props.sessionId);
        return;
      }
      if (project === void 0 || props.sessions?.list === void 0) {
        setBoundSessionId(void 0);
        return;
      }
      try {
        const listed = parseSessionList(await props.sessions.list({}));
        const workspaceRaw = props.workspace?.list === void 0 ? void 0 : await props.workspace.list({});
        const picked = pickWorkspaceSession({
          project,
          sessions: listed,
          workspaces: workspaceRaw === void 0 ? [] : parseWorkspaceList(workspaceRaw),
          archivedIds: workspaceRaw === void 0 ? [] : parseArchivedIds(workspaceRaw)
        });
        if (!cancelled) setBoundSessionId(picked?.id);
      } catch {
        if (!cancelled) setBoundSessionId(void 0);
      }
    };
    void bind();
    return () => {
      cancelled = true;
    };
  }, [project, props.sessionId, props.sessions, props.workspace]);
  const startWorkspaceSession = (0, import_react19.useCallback)(async () => {
    if (project === void 0) throw new Error("\u8FD9\u4E2A\u5DE5\u4F5C\u533A\u8FD8\u6CA1\u6709 DSH \u4F1A\u8BDD");
    const liveId = await createLiveSession(props.liveSessions, { cwd: project });
    if (liveId !== void 0 && liveId !== "") {
      setBoundSessionId(liveId);
      return liveId;
    }
    const create2 = props.sessions?.create;
    if (create2 === void 0) throw new Error("\u8FD9\u4E2A\u5DE5\u4F5C\u533A\u8FD8\u6CA1\u6709 DSH \u4F1A\u8BDD");
    const created = createdSessionId(await create2({ cwd: project, agentPreset: "directorx" }));
    if (created === void 0 || created === "") throw new Error("\u65E0\u6CD5\u5728\u8FD9\u4E2A\u5DE5\u4F5C\u533A\u521B\u5EFA DSH \u4F1A\u8BDD");
    setBoundSessionId(created);
    return created;
  }, [project, props.liveSessions, props.sessions]);
  const ensureWorkspaceSession = (0, import_react19.useCallback)(async () => {
    if (boundSessionId !== void 0 && boundSessionId !== "") return boundSessionId;
    return startWorkspaceSession();
  }, [boundSessionId, startWorkspaceSession]);
  const askDshText = (0, import_react19.useCallback)(async (text) => {
    if (text.trim() === "" || props.onAskDsh === void 0) return;
    const sessionId = await ensureWorkspaceSession();
    await props.onAskDsh(text, sessionId);
    setSessionOpen(true);
    setSessionPulse((value) => value + 1);
  }, [ensureWorkspaceSession, props]);
  (0, import_react19.useEffect)(() => {
    const onAsk = (event) => {
      const text = event.detail?.text;
      if (typeof text !== "string" || text.trim() === "") return;
      void askDshText(text).catch((cause) => {
        setError(cause instanceof Error ? cause.message : String(cause));
      });
    };
    window.addEventListener(ASK_DSH_REWRITE, onAsk);
    return () => window.removeEventListener(ASK_DSH_REWRITE, onAsk);
  }, [askDshText]);
  const saveNow = (0, import_react19.useCallback)(async () => {
    if (!dirtyRef.current) return;
    setSaveState("\u4FDD\u5B58\u4E2D\u2026");
    try {
      const doc = fromFlow(nodesRef.current, edgesRef.current, titleRef.current, updatedAtRef.current);
      const response = await fetch(withProject(`/directorx/canvas?expectedUpdatedAt=${updatedAtRef.current}`), {
        method: "PUT",
        headers: { "content-type": "application/json", ...projectHeaders() },
        body: JSON.stringify(doc)
      });
      if (response.status === 409) {
        const fresh = await fetch(withProject("/directorx/canvas"), { headers: projectHeaders() }).then((item3) => item3.json());
        updatedAtRef.current = fresh.updatedAt;
        const retry = await fetch(withProject(`/directorx/canvas?expectedUpdatedAt=${fresh.updatedAt}`), {
          method: "PUT",
          headers: { "content-type": "application/json", ...projectHeaders() },
          body: JSON.stringify({ ...doc, updatedAt: fresh.updatedAt })
        });
        if (!retry.ok) throw new Error("\u753B\u5E03\u4FDD\u5B58\u51B2\u7A81");
        const saved2 = await retry.json();
        updatedAtRef.current = saved2.updatedAt;
        dirtyRef.current = false;
        setSaveState("\u5DF2\u4FDD\u5B58");
        return;
      }
      if (!response.ok) throw new Error(`\u753B\u5E03\u4FDD\u5B58\u5931\u8D25 (${response.status})`);
      const saved = await response.json();
      updatedAtRef.current = saved.updatedAt;
      dirtyRef.current = false;
      setSaveState("\u5DF2\u4FDD\u5B58");
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
      setSaveState("\u4FDD\u5B58\u5931\u8D25");
    }
  }, []);
  const saveTimerRef = (0, import_react19.useRef)(0);
  const scheduleSave = (0, import_react19.useCallback)(() => {
    dirtyRef.current = true;
    setSaveState("\u672A\u4FDD\u5B58");
    saveRef.current = saveNow;
    window.clearTimeout(saveTimerRef.current);
    saveTimerRef.current = window.setTimeout(() => {
      void saveNow();
    }, 400);
  }, [saveNow]);
  (0, import_react19.useEffect)(() => {
    saveRef.current = scheduleSave;
  }, [scheduleSave]);
  (0, import_react19.useEffect)(() => () => {
    if (dirtyRef.current) void saveNow();
  }, [saveNow]);
  (0, import_react19.useEffect)(() => {
    const refresh = () => {
      if (dirtyRef.current) return;
      void fetch(withProject("/directorx/canvas"), { headers: projectHeaders() }).then((item3) => item3.json()).then((doc) => {
        if (doc.updatedAt !== updatedAtRef.current) applyDoc(doc);
      }).catch(() => {
      });
    };
    const timer2 = window.setInterval(refresh, 3e3);
    window.addEventListener("focus", refresh);
    return () => {
      window.clearInterval(timer2);
      window.removeEventListener("focus", refresh);
    };
  }, [applyDoc]);
  const showToast = (0, import_react19.useCallback)((text) => {
    setToast(text);
    window.setTimeout(() => setToast((current) => current === text ? void 0 : current), 1600);
  }, []);
  const runCraft = (0, import_react19.useCallback)(async (action, extra = {}) => {
    try {
      if (dirtyRef.current) await saveNow();
      const response = await fetch(withProject("/directorx/canvas/craft"), {
        method: "POST",
        headers: { "content-type": "application/json", ...projectHeaders() },
        body: JSON.stringify({ action, ...extra })
      });
      const body = await response.json();
      if (!response.ok || body.ok === false) {
        showToast(typeof body.message === "string" && body.message !== "" ? body.message : typeof body.error === "string" ? body.error : "\u64CD\u4F5C\u5931\u8D25");
        return void 0;
      }
      if (body.doc !== void 0) applyDoc(body.doc);
      if (action === "script") showToast(body.reused === true ? "\u8FD9\u9875\u5267\u672C\u5DF2\u7ECF\u62C6\u8FC7" : "\u5DF2\u751F\u6210\u5206\u955C");
      else if (action === "frames") showToast(body.reused === true ? "\u8FD9\u6BB5\u5DF2\u7ECF\u63D0\u53D6\u8FC7\u5E27" : "\u5DF2\u63D0\u53D6\u5E27");
      else if (action === "parse" && extra.preview !== true) showToast(body.reused === true ? "\u8FD9\u6BB5\u5DF2\u7ECF\u89E3\u6790\u8FC7" : `\u5DF2\u89E3\u6790 ${Array.isArray(body.shots) ? body.shots.length : ""} \u955C`);
      else if (action === "parse" && extra.preview === true && body.reused === true) showToast("\u8FD9\u6BB5\u5DF2\u7ECF\u89E3\u6790\u8FC7");
      else if (action === "reshoot") showToast(body.phase === "assemble" ? "\u5DF2\u62FC\u63A5" : "\u5DF2\u5207\u51FA\u91CD\u7ED8\u533A\u95F4");
      else if (action === "pack") showToast("\u5DF2\u5408\u6210\u89C6\u9891");
      else if (action === "sheet") showToast("\u5DF2\u751F\u6210\u4E5D\u5BAB\u683C");
      else if (action === "split") showToast("\u5DF2\u5207\u5F00");
      else if (action === "join") showToast("\u5DF2\u5408\u5E76\u5BAB\u683C");
      else if (action === "stack") showToast("\u5DF2\u5206\u5C4F");
      else if (action === "desub") showToast("\u5DF2\u53BB\u5B57\u5E55");
      else if (action === "extend") showToast("\u5DF2\u5207\u51FA\u5EF6\u957F\u4F4D");
      else if (action === "gif") showToast("\u5DF2\u5BFC\u51FA GIF");
      else showToast(`\u5DF2\u81EA\u52A8\u8FDE\u7EBF ${Array.isArray(body.added) ? body.added.length : 0} \u6761`);
      return body;
    } catch (cause) {
      showToast(cause instanceof Error ? cause.message : "\u64CD\u4F5C\u5931\u8D25");
      return void 0;
    }
  }, [applyDoc, saveNow, showToast]);
  const snapshotGraph = (0, import_react19.useCallback)(() => ({
    nodes: nodesRef.current.map((node) => ({
      ...node,
      data: {
        label: node.data.label,
        ...node.data.kind !== void 0 ? { kind: node.data.kind } : {},
        ...node.data.path !== void 0 ? { path: node.data.path } : {},
        ...node.data.prompt !== void 0 ? { prompt: node.data.prompt } : {},
        ...node.data.shotStatus !== void 0 ? { shotStatus: node.data.shotStatus } : {},
        ...node.data.shotIndex !== void 0 ? { shotIndex: node.data.shotIndex } : {},
        ...node.data.locked === true ? { locked: true } : {},
        ...node.data.selectedTakeId !== void 0 ? { selectedTakeId: node.data.selectedTakeId } : {},
        ...node.data.aspect !== void 0 ? { aspect: node.data.aspect } : {},
        ...node.data.model !== void 0 ? { model: node.data.model } : {},
        ...node.data.durationSec !== void 0 ? { durationSec: node.data.durationSec } : {},
        ...node.data.lastError !== void 0 ? { lastError: node.data.lastError } : {},
        ...node.data.count !== void 0 ? { count: node.data.count } : {},
        ...node.data.characters !== void 0 ? { characters: node.data.characters } : {}
      }
    })),
    edges: edgesRef.current.map((edge) => ({ ...edge }))
  }), []);
  const pushHistory = (0, import_react19.useCallback)(() => {
    undoRef.current.push(snapshotGraph());
    if (undoRef.current.length > 40) undoRef.current.shift();
    redoRef.current = [];
    setHistoryTick((value) => value + 1);
  }, [snapshotGraph]);
  const restoreGraph = (0, import_react19.useCallback)((shot) => {
    setNodes(shot.nodes.map((node) => attachActions(node)));
    setEdges(shot.edges);
    scheduleSave();
    setHistoryTick((value) => value + 1);
  }, [attachActions, scheduleSave]);
  const undo = (0, import_react19.useCallback)(() => {
    const prev = undoRef.current.pop();
    if (prev === void 0) return;
    redoRef.current.push(snapshotGraph());
    restoreGraph(prev);
  }, [restoreGraph, snapshotGraph]);
  const redo = (0, import_react19.useCallback)(() => {
    const next = redoRef.current.pop();
    if (next === void 0) return;
    undoRef.current.push(snapshotGraph());
    restoreGraph(next);
  }, [restoreGraph, snapshotGraph]);
  const addNode = (0, import_react19.useCallback)((kind, extra = {}, position) => {
    pushHistory();
    const aspect = kind === "image" || kind === "video" ? typeof extra.aspect === "string" && extra.aspect !== "" ? extra.aspect : "16:9" : extra.aspect;
    const size = defaultSize2(kind, aspect);
    const id2 = newId2(kind);
    const pane = stageRef.current?.querySelector(".react-flow");
    const rect = pane?.getBoundingClientRect();
    const origin = rect !== void 0 ? screenToFlowPosition({ x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 }) : { x: 200, y: 160 };
    const stack = nodesRef.current.length % 5;
    const at2 = position ?? { x: origin.x - size.width / 2 + stack * 32, y: origin.y - size.height / 2 + stack * 32 };
    const nodeType = kind === "group" ? "group" : kind === "director-stage" ? "director-stage" : kind === "edit" ? "edit" : kind === "text" ? "text" : "media";
    const node = {
      id: id2,
      type: nodeType,
      position: at2,
      width: size.width,
      height: size.height,
      style: { ...size, overflow: "visible" },
      selected: true,
      data: {
        ...kind === "image" || kind === "video" || kind === "audio" ? { kind, path: extra.path ?? "" } : {},
        ...kind === "director-stage" ? { kind: "director-stage" } : {},
        ...kind === "edit" ? { kind: "edit" } : {},
        label: extra.label ?? (kind === "group" ? "\u5206\u7EC4" : kind === "text" ? "\u6587\u672C" : kind === "director-stage" ? "3D \u5BFC\u6F14\u53F0" : kind === "edit" ? "\u526A\u8F91\u53F0" : kind === "video" ? "\u89C6\u9891" : kind === "audio" ? "\u97F3\u9891" : "\u56FE\u7247"),
        ...aspect !== void 0 ? { aspect } : {},
        ...extra
      }
    };
    setNodes((current) => [...current.map((item3) => ({ ...item3, selected: false })), attachActions(node)]);
    scheduleSave();
    return id2;
  }, [attachActions, pushHistory, scheduleSave, screenToFlowPosition]);
  const onNodesChange = (0, import_react19.useCallback)((changes) => {
    const resized = changes.some((change) => change.type === "dimensions");
    if (changes.some((change) => change.type === "position" && change.dragging === false) || changes.some((change) => change.type === "remove" || change.type === "dimensions")) {
      scheduleSave();
    }
    setNodes((current) => {
      const next = applyNodeChanges(changes, current);
      if (!resized) return next;
      return next.map((node) => {
        const width = typeof node.style?.width === "number" ? node.style.width : void 0;
        const height = typeof node.style?.height === "number" ? node.style.height : void 0;
        if (width === void 0 || height === void 0 || node.type !== "media") return node;
        if (node.data.aspect !== void 0 && node.data.aspect !== "") {
          const size = sizeFromAspect(node.data.aspect, width);
          if (size.height === height) return node;
          return { ...node, width: size.width, height: size.height, style: { ...node.style, ...size, overflow: "visible" } };
        }
        const inferred = nearestAspect(width, height);
        return { ...node, data: { ...node.data, aspect: inferred } };
      });
    });
  }, [scheduleSave]);
  const onEdgesChange = (0, import_react19.useCallback)((changes) => {
    if (changes.some((change) => change.type === "remove")) scheduleSave();
    setEdges((current) => applyEdgeChanges(changes, current));
  }, [scheduleSave]);
  const linkNodes = (0, import_react19.useCallback)((source, target, handles) => {
    if (source === target) return false;
    const locked = nodesRef.current.find((node) => node.id === target)?.data.locked === true;
    if (locked) return false;
    if (edgesRef.current.some((edge) => edge.source === source && edge.target === target)) return true;
    pushHistory();
    const sourceBox = nodeAbsBox(source);
    const targetBox = nodeAbsBox(target);
    const routed = sourceBox !== void 0 && targetBox !== void 0 ? closestPorts(sourceBox, targetBox) : void 0;
    const next = addEdge2({
      id: newId2("edge"),
      source,
      target,
      sourceHandle: handles?.sourceHandle || (routed !== void 0 ? sideToHandle(routed.sourceSide) : "out"),
      targetHandle: handles?.targetHandle || (routed !== void 0 ? sideToHandle(routed.targetSide) : "in"),
      type: "wire",
      style: EDGE_STYLE
    }, edgesRef.current);
    edgesRef.current = next;
    setEdges(next);
    scheduleSave();
    return true;
  }, [pushHistory, scheduleSave]);
  const onConnect = (0, import_react19.useCallback)((connection) => {
    if (connection.source === null || connection.target === null) return;
    connectHandledRef.current = true;
    linkNodes(connection.source, connection.target, {
      sourceHandle: connection.sourceHandle,
      targetHandle: connection.targetHandle
    });
  }, [linkNodes]);
  const onReconnect = (0, import_react19.useCallback)((oldEdge, connection) => {
    if (connection.source === null || connection.target === null || connection.source === connection.target) return;
    if (nodesRef.current.find((node) => node.id === connection.target)?.data.locked === true) return;
    pushHistory();
    setEdges((current) => reconnectEdge2(oldEdge, {
      ...connection,
      sourceHandle: connection.sourceHandle ?? oldEdge.sourceHandle ?? "out",
      targetHandle: connection.targetHandle ?? oldEdge.targetHandle ?? "in"
    }, current).map((edge) => edge.id === oldEdge.id ? { ...edge, type: "wire", style: EDGE_STYLE } : edge));
    scheduleSave();
  }, [pushHistory, scheduleSave]);
  const pointerOf = (event) => {
    if ("clientX" in event) return { x: event.clientX, y: event.clientY };
    const touch = event.changedTouches[0];
    return touch !== void 0 ? { x: touch.clientX, y: touch.clientY } : void 0;
  };
  const onConnectStart = (0, import_react19.useCallback)((event, params) => {
    const point = pointerOf(event);
    if (params.nodeId === null || params.nodeId === "" || point === void 0) return;
    document.getSelection()?.removeAllRanges();
    connectHandledRef.current = false;
    connectRef.current = { nodeId: params.nodeId, x: point.x, y: point.y, handleId: params.handleId };
    const from = handleScreenPoint(params.nodeId, params.handleId) ?? (() => {
      const box3 = nodeAbsBox(params.nodeId);
      const fromFlow2 = box3 !== void 0 ? boxPort(box3, params.handleId) : screenToFlowPosition(point);
      return flowToScreenPosition(fromFlow2);
    })();
    setWireDrag({ from, to: point, handleId: params.handleId });
    setWiring(true);
    setWireMenu(void 0);
  }, [flowToScreenPosition, nodeAbsBox, screenToFlowPosition]);
  const focusCompose = (0, import_react19.useCallback)(() => {
    window.requestAnimationFrame(() => composeRef.current?.focus());
  }, []);
  const openWireMenu = (0, import_react19.useCallback)((sourceId, point, handleId) => {
    setWireMenu({
      sourceId,
      handleId,
      x: point.x,
      y: point.y,
      flow: screenToFlowPosition(point)
    });
  }, [screenToFlowPosition]);
  const onConnectEnd = (0, import_react19.useCallback)((event) => {
    const start2 = connectRef.current;
    const already = connectHandledRef.current;
    connectRef.current = void 0;
    connectHandledRef.current = false;
    setWiring(false);
    setWireDrag(void 0);
    if (start2 === void 0) return;
    const point = pointerOf(event);
    if (point === void 0) return;
    const dropped = event.target?.closest(".react-flow__node");
    const targetId = dropped?.getAttribute("data-id") ?? void 0;
    if (targetId !== void 0 && targetId !== start2.nodeId) {
      if (already) return;
      const flow = screenToFlowPosition(point);
      const targetBox = nodeAbsBox(targetId);
      const targetHandle = targetBox === void 0 ? "in" : closestHandleId(targetBox, flow);
      linkNodes(start2.nodeId, targetId, { sourceHandle: start2.handleId ?? "out", targetHandle });
      return;
    }
    const dragged = Math.hypot(point.x - start2.x, point.y - start2.y);
    if (targetId === start2.nodeId && dragged > 10) return;
    openWireMenu(start2.nodeId, point, start2.handleId);
  }, [linkNodes, nodeAbsBox, openWireMenu, screenToFlowPosition]);
  (0, import_react19.useEffect)(() => {
    if (!wiring) return;
    const onMove = (event) => {
      event.preventDefault();
      const start2 = connectRef.current;
      if (start2 === void 0) return;
      const to = { x: event.clientX, y: event.clientY };
      const from = handleScreenPoint(start2.nodeId, start2.handleId) ?? (() => {
        const box3 = nodeAbsBox(start2.nodeId);
        const fromFlow2 = box3 !== void 0 ? boxPort(box3, start2.handleId) : screenToFlowPosition(to);
        return flowToScreenPosition(fromFlow2);
      })();
      const hit = document.elementFromPoint(event.clientX, event.clientY);
      const targetId = hit?.closest(".react-flow__node")?.getAttribute("data-id") ?? void 0;
      const locked = targetId !== void 0 && nodesRef.current.find((node) => node.id === targetId)?.data.locked === true;
      const valid = targetId !== void 0 && targetId !== start2.nodeId && !locked;
      setWireDrag({ from, to, handleId: start2.handleId, ...valid ? { targetId } : {} });
    };
    const blockSelect = (event) => event.preventDefault();
    window.addEventListener("pointermove", onMove, { passive: false });
    document.addEventListener("selectstart", blockSelect, true);
    document.addEventListener("dragstart", blockSelect, true);
    return () => {
      window.removeEventListener("pointermove", onMove);
      document.removeEventListener("selectstart", blockSelect, true);
      document.removeEventListener("dragstart", blockSelect, true);
    };
  }, [flowToScreenPosition, nodeAbsBox, screenToFlowPosition, wiring]);
  const patchSelected = (0, import_react19.useCallback)((patch) => {
    if (selected2 === void 0) return;
    setNodes((current) => current.map((node) => node.id === selected2.id ? { ...node, data: { ...node.data, ...patch } } : node));
    scheduleSave();
  }, [scheduleSave, selected2]);
  const openStudio = (0, import_react19.useCallback)((kind, path) => {
    openEditor(kind, path);
  }, []);
  (0, import_react19.useEffect)(() => {
    if (snapshot2.path === null || snapshot2.tab !== "image" && snapshot2.tab !== "video") {
      if (studioUrl !== void 0) URL.revokeObjectURL(studioUrl);
      setStudioUrl(void 0);
      setStudioError(void 0);
      return;
    }
    let live = true;
    setStudioUrl(void 0);
    setStudioError(void 0);
    void fetch(mediaUrl(snapshot2.path)).then((response) => {
      if (!response.ok) throw new Error(`\u5A92\u4F53\u52A0\u8F7D\u5931\u8D25 (HTTP ${response.status})`);
      return response.blob();
    }).then((blob) => {
      if (!live) return;
      setStudioUrl(URL.createObjectURL(blob));
    }).catch((cause) => {
      if (live) setStudioError(cause instanceof Error ? cause.message : String(cause));
    });
    return () => {
      live = false;
    };
  }, [snapshot2.path, snapshot2.tab]);
  const onStudioExport = (0, import_react19.useCallback)(async (blob, mediaType) => {
    const record = await saveBlob(blob, snapshot2.path?.split("/").pop() ?? "edit", mediaType);
    const kind = mediaType.startsWith("video/") ? "video" : "image";
    if (selected2 !== void 0 && selected2.type === "media") {
      patchSelected({ path: record.path, label: nextCardLabel(selected2.data.label, record.name) ?? record.name, kind });
    } else {
      addNode(kind, { path: record.path, label: record.name });
    }
    setEditorTab("canvas");
  }, [addNode, patchSelected, selected2, snapshot2.path]);
  const openGenerate = (0, import_react19.useCallback)((sourceId) => {
    const source = sourceId !== void 0 ? nodesRef.current.find((node) => node.id === sourceId) : nodesRef.current.find((node) => node.selected === true);
    const kind = inferContinueKind(source?.data.kind);
    const prompt = (source?.data.prompt && source.data.prompt !== "" ? source.data.prompt : source?.data.label) ?? "";
    setCompose({
      kind,
      prompt,
      count: source?.data.count ?? 1,
      aspect: source?.data.aspect ?? "16:9",
      ...source?.data.model !== void 0 ? { model: source.data.model } : {},
      ...source !== void 0 ? { sourceId: source.id, targetId: source.id } : {}
    });
    setGenerateOpen(source === void 0);
    if (source === void 0) focusCompose();
  }, [focusCompose]);
  const openStudioFor = (0, import_react19.useCallback)((id2) => {
    const node = nodesRef.current.find((item3) => item3.id === id2);
    const kind = node === void 0 ? void 0 : flowNodeKind(node);
    if (kind === "director-stage") {
      openDirectorStage(id2);
      return;
    }
    if (kind === "edit") {
      openEdit(id2);
      return;
    }
    const path = node?.data.path;
    if (node === void 0 || path === void 0 || path === "") return;
    openStudio(node.data.kind === "video" ? "video" : "image", path);
  }, [openStudio]);
  const renameNode = (0, import_react19.useCallback)((id2, label2) => {
    setNodes((current) => current.map((node) => node.id === id2 ? { ...node, data: { ...node.data, label: label2 } } : node));
    scheduleSave();
  }, [scheduleSave]);
  const patchNode = (0, import_react19.useCallback)((id2, patch) => {
    setNodes((current) => current.map((node) => {
      if (node.id !== id2) return node;
      const next = { ...node, data: { ...node.data, ...patch, lastError: patch.lastError ?? void 0 } };
      if (typeof patch.aspect === "string" && patch.aspect !== "") {
        const width = typeof node.style?.width === "number" ? node.style.width : defaultSize2(node.data.kind ?? "image").width;
        const size = sizeFromAspect(patch.aspect, width);
        next.style = { ...node.style, ...size, overflow: "visible" };
        next.width = size.width;
        next.height = size.height;
      }
      return attachActions(next);
    }));
    if (selected2?.id === id2) {
      setCompose((current) => ({
        ...current,
        ...patch.prompt !== void 0 ? { prompt: patch.prompt } : {},
        ...patch.model !== void 0 ? { model: patch.model } : {},
        ...patch.aspect !== void 0 ? { aspect: patch.aspect } : {},
        ...patch.count !== void 0 ? { count: patch.count } : {},
        ...patch.durationSec !== void 0 ? { durationSec: patch.durationSec } : {},
        ...patch.kind === "image" || patch.kind === "video" || patch.kind === "audio" ? { kind: patch.kind } : {},
        ...patch.characters !== void 0 ? { characters: patch.characters } : {}
      }));
    }
    scheduleSave();
  }, [attachActions, scheduleSave, selected2]);
  const cycleStatus = (0, import_react19.useCallback)((id2) => {
    setNodes((current) => current.map((node) => node.id === id2 ? { ...node, data: { ...node.data, shotStatus: cycleShotStatus(node.data.shotStatus) } } : node));
    scheduleSave();
  }, [scheduleSave]);
  const setShotStatus = (0, import_react19.useCallback)((id2, status) => {
    setNodes((current) => current.map((node) => node.id === id2 ? { ...node, data: { ...node.data, shotStatus: status } } : node));
    scheduleSave();
  }, [scheduleSave]);
  const focusNode = (0, import_react19.useCallback)((id2) => {
    const node = nodesRef.current.find((item3) => item3.id === id2);
    if (node === void 0) return;
    setNodes((current) => current.map((item3) => ({ ...item3, selected: item3.id === id2 })));
    void fitView({ nodes: [node], ...focusViewOptions(node.type === "group" ? "group" : "card") });
  }, [fitView]);
  const focusTake = (0, import_react19.useCallback)((id2) => {
    focusNode(id2);
  }, [focusNode]);
  const adoptTake = (0, import_react19.useCallback)((id2) => {
    const chosen = nodesRef.current.find((node) => node.id === id2);
    const otherId = compare?.find((item3) => item3 !== id2);
    pushHistory();
    setNodes((current) => current.map((node) => {
      if (node.id === id2) return { ...node, data: { ...node.data, shotStatus: "locked" } };
      if (otherId !== void 0 && node.id === otherId) return { ...node, data: { ...node.data, shotStatus: "review" } };
      if (chosen?.parentId !== void 0 && node.id === chosen.parentId) {
        return { ...node, data: { ...node.data, selectedTakeId: id2 } };
      }
      return node;
    }));
    setCompare(void 0);
    scheduleSave();
  }, [compare, pushHistory, scheduleSave]);
  const submitSpec = (0, import_react19.useCallback)(async (spec) => {
    const node = nodesRef.current.find((item3) => item3.id === (spec.targetId ?? spec.sourceId));
    const base2 = spec.prompt.trim() || (node?.data.prompt ?? "").trim() || (node?.data.label ?? "").trim();
    const prompt = spec.revise === true ? `\u955C\u6539 ${node?.data.label ?? "\u8FD9\u4E00\u955C"}\uFF1A${base2}` : spec.prompt.trim();
    if (prompt === "" || askBusy) return;
    setAskBusy(true);
    try {
      const sourceId = spec.sourceId ?? selected2?.id;
      const response = await fetch(withProject("/directorx/canvas/intent"), {
        method: "POST",
        headers: { "content-type": "application/json", ...projectHeaders() },
        body: JSON.stringify({
          kind: spec.kind,
          prompt: specPrompt({ ...spec, prompt }),
          ...sourceId !== void 0 ? { sourceId } : {},
          selectedIds: [.../* @__PURE__ */ new Set([
            ...spec.targetId !== void 0 ? [spec.targetId] : [],
            ...nodesRef.current.filter((node2) => node2.selected === true).map((node2) => node2.id)
          ])],
          ...spec.model !== void 0 ? { model: spec.model } : {},
          ...spec.aspect !== void 0 ? { aspect: spec.aspect } : {},
          ...spec.count !== void 0 ? { count: spec.count } : {},
          ...spec.durationSec !== void 0 ? { durationSec: spec.durationSec } : {},
          ...spec.refIds !== void 0 ? { refIds: spec.refIds } : {},
          ...spec.characters !== void 0 ? { characters: spec.characters } : {}
        })
      });
      if (!response.ok) throw new Error(`\u6307\u4EE4\u5165\u961F\u5931\u8D25 (${response.status})`);
      const data = await response.json();
      const message = typeof data.prompt === "string" && data.prompt !== "" ? data.prompt : specPrompt({ ...spec, prompt });
      if (props.onAskDsh !== void 0) {
        const sessionId = await ensureWorkspaceSession();
        await props.onAskDsh(message, sessionId);
      }
      setCompose((current) => ({ ...current, prompt: "", revise: void 0 }));
      setReviseId(void 0);
      setSessionOpen(true);
      setSessionPulse((value) => value + 1);
    } catch (cause) {
      const message = cause instanceof Error ? cause.message : String(cause);
      setError(message);
      const target = spec.targetId ?? spec.sourceId;
      if (target !== void 0) {
        setNodes((current) => current.map((node2) => node2.id === target ? { ...node2, data: { ...node2.data, shotStatus: "failed", lastError: message } } : node2));
      }
    } finally {
      setAskBusy(false);
    }
  }, [askBusy, ensureWorkspaceSession, props, selected2]);
  const submitAsk = (0, import_react19.useCallback)(async () => {
    await submitSpec(compose);
  }, [compose, submitSpec]);
  const runDirectGenerate = (0, import_react19.useCallback)(async (input2) => {
    patchNode(input2.nodeId, { shotStatus: "generating", lastError: void 0, prompt: input2.prompt });
    try {
      const response = await fetch(withProject("/directorx/canvas/generate"), {
        method: "POST",
        headers: { "content-type": "application/json", ...projectHeaders() },
        body: JSON.stringify(input2)
      });
      const data = await response.json();
      if (!response.ok || data.ok === false) throw new Error(typeof data.message === "string" && data.message !== "" ? data.message : "\u751F\u6210\u5931\u8D25");
      if (data.doc !== void 0) applyDoc(data.doc);
      else if (typeof data.path === "string") patchNode(input2.nodeId, { path: data.path, shotStatus: "review", lastError: void 0 });
      showToast("\u5DF2\u751F\u6210");
    } catch (cause) {
      const message = cause instanceof Error ? cause.message : String(cause);
      patchNode(input2.nodeId, { shotStatus: "failed", lastError: message });
      setError(message);
    }
  }, [applyDoc, patchNode, showToast]);
  const submitReshoot = (0, import_react19.useCallback)(async (input2) => {
    if (reshootId === void 0) return;
    const nodeId = reshootId;
    setReshootId(void 0);
    const body = await runCraft("reshoot", { nodeId, phase: "cut", start: input2.start, end: input2.end, prompt: input2.prompt });
    if (body?.midId === void 0) return;
    const firstPath = typeof body.firstId === "string" ? nodesRef.current.find((item3) => item3.id === body.firstId)?.data.path : void 0;
    const lastPath = typeof body.lastId === "string" ? nodesRef.current.find((item3) => item3.id === body.lastId)?.data.path : void 0;
    await runDirectGenerate({
      kind: "video",
      prompt: typeof body.prompt === "string" && body.prompt !== "" ? body.prompt : input2.prompt,
      nodeId: body.midId,
      task: "reshoot",
      references: [firstPath, lastPath].filter((path) => typeof path === "string" && path !== ""),
      ...typeof firstPath === "string" && firstPath !== "" ? { firstFramePath: firstPath } : {},
      ...typeof body.durationSec === "number" ? { durationSec: body.durationSec } : {}
    });
  }, [reshootId, runCraft, runDirectGenerate]);
  const openRevise = (0, import_react19.useCallback)((id2) => {
    const node = nodesRef.current.find((item3) => item3.id === id2);
    if (node === void 0) return;
    setNodes((current) => current.map((item3) => ({ ...item3, selected: item3.id === id2 })));
    setReviseId(id2);
    setCompose({
      kind: node.data.kind === "video" ? "video" : "image",
      prompt: (node.data.prompt && node.data.prompt !== "" ? node.data.prompt : node.data.label) ?? "",
      count: node.data.count ?? 1,
      aspect: node.data.aspect ?? "16:9",
      ...node.data.model !== void 0 ? { model: node.data.model } : {},
      ...node.data.durationSec !== void 0 ? { durationSec: node.data.durationSec } : {},
      ...node.data.characters !== void 0 ? { characters: node.data.characters } : {},
      ...typeof node.data.path === "string" && node.data.path !== "" ? { refIds: [id2] } : {},
      sourceId: id2,
      targetId: id2,
      revise: true
    });
    window.setTimeout(() => {
      const field = document.querySelector(".react-flow__node.selected .dx-card-dock textarea");
      if (field instanceof HTMLTextAreaElement) field.focus();
    }, 30);
  }, []);
  const openParse = (0, import_react19.useCallback)(async (id2) => {
    const node = nodesRef.current.find((item3) => item3.id === id2);
    const body = await runCraft("parse", { nodeId: id2, preview: true });
    if (body === void 0) return;
    if (body.reused === true) return;
    const shots = Array.isArray(body.shots) ? body.shots : [];
    if (shots.length === 0) {
      showToast("\u6CA1\u6709\u62C6\u51FA\u955C\u5934");
      return;
    }
    setParsePreview({
      nodeId: id2,
      sourceLabel: node?.data.label ?? "\u89C6\u9891",
      script: typeof body.script === "string" ? body.script : "",
      shots: shots.map((shot, index2) => ({
        index: typeof shot === "object" && shot !== null && typeof shot.index === "number" ? shot.index : index2 + 1,
        start: typeof shot === "object" && shot !== null && typeof shot.start === "number" ? shot.start : 0,
        end: typeof shot === "object" && shot !== null && typeof shot.end === "number" ? shot.end : 0,
        durationSec: typeof shot === "object" && shot !== null && typeof shot.durationSec === "number" ? shot.durationSec : 0,
        ...shot.framePath !== void 0 ? { framePath: shot.framePath } : {},
        description: typeof shot.description === "string" ? shot.description : null
      }))
    });
  }, [runCraft, showToast]);
  const runNodeGenerate = (0, import_react19.useCallback)((id2) => {
    const node = nodesRef.current.find((item3) => item3.id === id2);
    const spec = {
      kind: node?.data.kind === "video" ? "video" : node?.data.kind === "audio" ? "audio" : node?.data.kind === "image" ? "image" : compose.kind,
      prompt: (node?.data.prompt && node.data.prompt !== "" ? node.data.prompt : compose.prompt) ?? "",
      count: node?.data.count ?? compose.count ?? 1,
      aspect: node?.data.aspect ?? compose.aspect,
      ...node?.data.model !== void 0 ? { model: node.data.model } : compose.model !== void 0 ? { model: compose.model } : {},
      ...node?.data.durationSec !== void 0 ? { durationSec: node.data.durationSec } : {},
      ...node?.data.characters !== void 0 ? { characters: node.data.characters } : compose.characters !== void 0 ? { characters: compose.characters } : {},
      refIds: incomingRefIds(id2, edgesRef.current),
      sourceId: id2,
      targetId: id2,
      ...reviseId === id2 ? { revise: true } : {}
    };
    setCompose(spec);
    if (spec.prompt.trim() === "") {
      focusCompose();
      return;
    }
    if (spec.kind === "audio") {
      void submitSpec(spec);
      return;
    }
    const references = (spec.refIds ?? []).map((refId) => nodesRef.current.find((item3) => item3.id === refId)?.data.path).filter((path) => typeof path === "string" && path !== "");
    void runDirectGenerate({
      kind: spec.kind === "video" ? "video" : "image",
      prompt: spec.prompt,
      nodeId: id2,
      task: "generate",
      references,
      ...spec.model !== void 0 ? { model: spec.model } : {},
      ...spec.aspect !== void 0 ? { aspect: spec.aspect } : {},
      ...spec.durationSec !== void 0 ? { durationSec: spec.durationSec } : {}
    });
  }, [compose, focusCompose, reviseId, runDirectGenerate, submitSpec]);
  const openPicker = (0, import_react19.useCallback)(async () => {
    setPicker(true);
    try {
      const [media, people] = await Promise.all([
        fetch(withProject("/directorx/media/list"), { headers: projectHeaders() }).then((item3) => item3.json()),
        fetch(withProject("/directorx/characters"), { headers: projectHeaders() }).then((item3) => item3.json()).catch(() => ({ characters: [] }))
      ]);
      setFiles(media.files ?? []);
      setCharacters(people.characters ?? []);
    } catch {
      setFiles([]);
    }
  }, []);
  const pinSubject = (0, import_react19.useCallback)(async (role) => {
    const node = selected2;
    const path = node?.data.path;
    if (node === void 0 || path === void 0 || path === "") {
      showToast("\u5148\u9009\u4E2D\u4E00\u5F20\u6709\u5A92\u4F53\u7684\u8282\u70B9");
      return;
    }
    const name2 = (node.data.label || "\u672A\u547D\u540D").trim().slice(0, 80);
    try {
      const response = await fetch(withProject("/directorx/characters"), {
        method: "POST",
        headers: { "content-type": "application/json", ...projectHeaders() },
        body: JSON.stringify({
          name: name2,
          refPath: path,
          description: role === "scene" ? `\u573A\u666F \xB7 ${name2}` : node.data.prompt ?? "",
          ...role === "prop" ? { props: name2 } : {}
        })
      });
      if (!response.ok) throw new Error(`\u767B\u8BB0\u5931\u8D25 (${response.status})`);
      const listed = await fetch(withProject("/directorx/characters"), { headers: projectHeaders() }).then((item3) => item3.json());
      setCharacters(listed.characters ?? []);
      if (role === "character") {
        patchNode(node.id, { characters: [.../* @__PURE__ */ new Set([...node.data.characters ?? [], name2])] });
        setCompose((current) => ({ ...current, characters: [.../* @__PURE__ */ new Set([...current.characters ?? [], name2])] }));
      }
      setLibraryTab(role === "prop" ? "prop" : role === "scene" ? "scene" : "character");
      showToast(role === "scene" ? "\u5DF2\u767B\u8BB0\u573A\u666F" : role === "prop" ? "\u5DF2\u767B\u8BB0\u9053\u5177" : "\u5DF2\u767B\u8BB0\u89D2\u8272");
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [patchNode, selected2, showToast]);
  const uploadFiles = (0, import_react19.useCallback)(async (list, position) => {
    const targetId = uploadTargetRef.current;
    uploadTargetRef.current = void 0;
    let usedTarget = false;
    for (const file of list) {
      const record = await saveBlob(file, file.name, file.type || "application/octet-stream");
      const kind = file.type.startsWith("video/") ? "video" : "image";
      if (!usedTarget && targetId !== void 0) {
        const node = nodesRef.current.find((item3) => item3.id === targetId);
        if (node?.type === "media") {
          patchNode(targetId, { path: record.path, label: nextCardLabel(node.data.label, record.name) ?? record.name, kind });
          usedTarget = true;
          continue;
        }
      }
      addNode(kind, { path: record.path, label: record.name }, position);
    }
  }, [addNode, patchNode]);
  const deleteIds = (0, import_react19.useCallback)((ids) => {
    const drop = new Set(ids.filter((id2) => nodesRef.current.find((node) => node.id === id2)?.data.locked !== true));
    if (drop.size === 0) return;
    pushHistory();
    setNodes((current) => current.filter((node) => !drop.has(node.id)));
    setEdges((current) => current.filter((edge) => !drop.has(edge.source) && !drop.has(edge.target)));
    scheduleSave();
  }, [pushHistory, scheduleSave]);
  const duplicateNode = (0, import_react19.useCallback)((id2) => {
    const source = nodesRef.current.find((node) => node.id === id2);
    if (source === void 0) return;
    pushHistory();
    const copy = attachActions({
      ...source,
      id: newId2(source.type ?? "text"),
      position: { x: source.position.x + 40, y: source.position.y + 40 },
      selected: true
    });
    setNodes((current) => [...current.map((node) => ({ ...node, selected: false })), copy]);
    scheduleSave();
  }, [attachActions, pushHistory, scheduleSave]);
  const disconnectNode = (0, import_react19.useCallback)((id2) => {
    pushHistory();
    setEdges((current) => current.filter((edge) => edge.source !== id2 && edge.target !== id2));
    scheduleSave();
  }, [pushHistory, scheduleSave]);
  const toggleLock = (0, import_react19.useCallback)((ids) => {
    if (ids.length === 0) return;
    pushHistory();
    const shouldLock = ids.some((id2) => nodesRef.current.find((node) => node.id === id2)?.data.locked !== true);
    setNodes((current) => current.map((node) => ids.includes(node.id) ? attachActions({ ...node, draggable: !shouldLock, data: { ...node.data, locked: shouldLock } }) : node));
    scheduleSave();
    showToast(shouldLock ? "\u5DF2\u9501\u5B9A" : "\u5DF2\u89E3\u9501");
  }, [attachActions, pushHistory, scheduleSave, showToast]);
  const downloadNode = (0, import_react19.useCallback)(async (id2) => {
    const node = nodesRef.current.find((item3) => item3.id === id2);
    const path = node?.data.path;
    if (node === void 0 || path === void 0 || path === "") return;
    try {
      const response = await fetch(mediaUrl(path));
      if (!response.ok) throw new Error(`\u4E0B\u8F7D\u5931\u8D25 (${response.status})`);
      const blob = await response.blob();
      const href = URL.createObjectURL(blob);
      const link2 = document.createElement("a");
      link2.href = href;
      link2.download = node.data.label || path.split("/").pop() || "media";
      document.body.appendChild(link2);
      link2.click();
      link2.remove();
      URL.revokeObjectURL(href);
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  }, []);
  const confirmCrop = (0, import_react19.useCallback)(async (id2, blob) => {
    try {
      const saved = await saveBlob(blob, `crop-${Date.now()}.jpg`, "image/jpeg");
      const source = nodesRef.current.find((node) => node.id === id2);
      const width = typeof source?.style?.width === "number" ? source.style.width : 400;
      const at2 = source !== void 0 ? { x: source.position.x + width + 48, y: source.position.y } : void 0;
      const aspect = cropAspect === "free" ? void 0 : cropAspect;
      const nextId = addNode("image", { path: saved.path, label: saved.name || "\u88C1\u526A", ...aspect !== void 0 ? { aspect } : {} }, at2);
      linkNodes(id2, nextId);
      setCropId(void 0);
      showToast("\u5DF2\u63A5\u5230\u4E0B\u6E38");
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [addNode, cropAspect, linkNodes, showToast]);
  const spawnLinkedImage = (0, import_react19.useCallback)((sourceId, extra) => {
    const source = nodesRef.current.find((node) => node.id === sourceId);
    const width = typeof source?.style?.width === "number" ? source.style.width : 400;
    const at2 = source !== void 0 ? { x: source.position.x + width + 48, y: source.position.y } : void 0;
    const id2 = addNode("image", extra, at2);
    linkNodes(sourceId, id2);
    return id2;
  }, [addNode, linkNodes]);
  const restoreEditSize = (0, import_react19.useCallback)(() => {
    const prev = editRestoreRef.current;
    editRestoreRef.current = void 0;
    if (prev === void 0) return;
    setNodes((current) => current.map((node) => node.id !== prev.id ? node : {
      ...node,
      width: prev.width,
      height: prev.height,
      style: { ...node.style, width: prev.width, height: prev.height, overflow: "visible" }
    }));
  }, []);
  const confirmMasked = (0, import_react19.useCallback)(async (id2, blob, prompt, mode) => {
    try {
      const saved = await saveBlob(blob, `mask-${Date.now()}.png`, "image/png");
      const sourcePath = nodesRef.current.find((item3) => item3.id === id2)?.data.path;
      const targetId = spawnLinkedImage(id2, {
        label: mode === "erase" ? "\u64E6\u9664" : "\u5C40\u90E8\u91CD\u7ED8",
        prompt,
        shotStatus: "generating"
      });
      restoreEditSize();
      setMaskId(void 0);
      setMaskMode(void 0);
      await runDirectGenerate({
        kind: "image",
        prompt,
        nodeId: targetId,
        task: mode,
        references: [sourcePath, saved.path].filter((path) => typeof path === "string" && path !== ""),
        maskPath: saved.path
      });
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [restoreEditSize, runDirectGenerate, spawnLinkedImage]);
  const focusEditNode = (0, import_react19.useCallback)((id2) => {
    const node = nodesRef.current.find((item3) => item3.id === id2);
    if (node === void 0) return;
    const width = typeof node.width === "number" ? node.width : typeof node.style?.width === "number" ? node.style.width : 400;
    const height = typeof node.height === "number" ? node.height : typeof node.style?.height === "number" ? node.style.height : 220;
    if (editRestoreRef.current === void 0) editRestoreRef.current = { id: id2, width, height };
    const pane = stageRef.current?.querySelector(".react-flow")?.getBoundingClientRect();
    const aspect = height / Math.max(1, width);
    const flowW = Math.max(width, Math.min(1100, Math.round((pane?.width ?? 1100) * 0.74)));
    const flowH = Math.max(height, Math.round(flowW * aspect));
    setNodes((current) => current.map((item3) => item3.id === id2 ? { ...item3, selected: true, width: flowW, height: flowH, style: { ...item3.style, width: flowW, height: flowH, overflow: "visible" } } : { ...item3, selected: item3.id === id2 }));
    window.requestAnimationFrame(() => {
      void fitView({
        nodes: [{ id: id2, position: node.position, width: flowW, height: flowH }],
        padding: 0.12,
        duration: 240,
        maxZoom: 1.45,
        minZoom: 0.2
      });
    });
  }, [fitView]);
  const startMask = (0, import_react19.useCallback)((id2, mode) => {
    if (id2 === "") {
      restoreEditSize();
      setMaskId(void 0);
      setMaskMode(void 0);
      return;
    }
    setCropId(void 0);
    setExpandId(void 0);
    focusEditNode(id2);
    setMaskMode(mode);
    setMaskId(id2);
  }, [focusEditNode, restoreEditSize]);
  const startExpand = (0, import_react19.useCallback)((id2) => {
    if (id2 === "") {
      restoreEditSize();
      setExpandId(void 0);
      return;
    }
    setCropId(void 0);
    setMaskId(void 0);
    setMaskMode(void 0);
    focusEditNode(id2);
    setExpandId(id2);
  }, [focusEditNode, restoreEditSize]);
  const confirmExpand = (0, import_react19.useCallback)(async (id2, padded, mask) => {
    try {
      const saved = await saveBlob(padded, `expand-${Date.now()}.png`, "image/png");
      const maskFile = await saveBlob(mask, `expand-mask-${Date.now()}.png`, "image/png");
      restoreEditSize();
      setExpandId(void 0);
      const targetId = spawnLinkedImage(id2, {
        path: saved.path,
        label: "\u6269\u56FE",
        prompt: "\u6269\u5C55\u753B\u9762\uFF0C\u586B\u6EE1\u65B0\u753B\u5E45\uFF0C\u4E3B\u4F53\u4F4D\u7F6E\u4E0E\u5916\u89C2\u4FDD\u6301\u4E0D\u53D8",
        shotStatus: "generating"
      });
      await runDirectGenerate({
        kind: "image",
        prompt: "\u6269\u5C55\u753B\u9762\uFF0C\u586B\u6EE1\u65B0\u753B\u5E45\uFF0C\u4E3B\u4F53\u4F4D\u7F6E\u4E0E\u5916\u89C2\u4FDD\u6301\u4E0D\u53D8",
        nodeId: targetId,
        task: "expand",
        references: [saved.path],
        maskPath: maskFile.path
      });
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : String(cause));
    }
  }, [restoreEditSize, runDirectGenerate, spawnLinkedImage]);
  const ungroupIds = (0, import_react19.useCallback)((ids) => {
    const groups = nodesRef.current.filter((node) => ids.includes(node.id) && node.type === "group");
    if (groups.length === 0) return;
    pushHistory();
    const groupIds = new Set(groups.map((node) => node.id));
    const origin = new Map(groups.map((node) => [node.id, node.position]));
    setNodes((current) => current.filter((node) => !groupIds.has(node.id)).map((node) => {
      if (node.parentId === void 0 || !groupIds.has(node.parentId)) return node;
      const parent = origin.get(node.parentId);
      return {
        ...node,
        parentId: void 0,
        extent: void 0,
        position: { x: node.position.x + (parent?.x ?? 0), y: node.position.y + (parent?.y ?? 0) }
      };
    }));
    scheduleSave();
    showToast("\u5DF2\u89E3\u6563\u5206\u7EC4");
  }, [pushHistory, scheduleSave, showToast]);
  const applyPositions = (0, import_react19.useCallback)((next) => {
    if (next.length === 0) return;
    pushHistory();
    const map = new Map(next.map((item3) => [item3.id, item3]));
    setNodes((current) => current.map((node) => {
      const at2 = map.get(node.id);
      return at2 === void 0 ? node : { ...node, position: { x: at2.x, y: at2.y } };
    }));
    scheduleSave();
  }, [pushHistory, scheduleSave]);
  const boxesOf = (list) => list.map((node) => ({
    id: node.id,
    x: node.position.x,
    y: node.position.y,
    w: typeof node.style?.width === "number" ? node.style.width : 250,
    h: typeof node.style?.height === "number" ? node.style.height : 180
  }));
  const alignSelected = (0, import_react19.useCallback)((kind) => {
    applyPositions(alignBoxes(boxesOf(nodesRef.current.filter((node) => node.selected === true)), kind));
  }, [applyPositions]);
  const distributeSelected = (0, import_react19.useCallback)((axis) => {
    applyPositions(distributeBoxes(boxesOf(nodesRef.current.filter((node) => node.selected === true)), axis));
  }, [applyPositions]);
  const copySelected = (0, import_react19.useCallback)(async () => {
    const chosen = nodesRef.current.filter((node) => node.selected === true);
    const packed = packClip(chosen, edgesRef.current);
    if (packed === void 0) return;
    try {
      await navigator.clipboard.writeText(JSON.stringify(packed));
      showToast(`\u5DF2\u590D\u5236 ${packed.nodes.length} \u4E2A\u8282\u70B9`);
    } catch {
      showToast("\u65E0\u6CD5\u5199\u5165\u526A\u8D34\u677F");
    }
  }, [showToast]);
  const pasteClip = (0, import_react19.useCallback)(async () => {
    try {
      const text = await navigator.clipboard.readText();
      const packed = asClipPayload(JSON.parse(text));
      if (packed === void 0) return false;
      pushHistory();
      const pane = stageRef.current?.querySelector(".react-flow");
      const rect = pane?.getBoundingClientRect();
      const origin = rect !== void 0 ? screenToFlowPosition({ x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 }) : { x: 120, y: 120 };
      const created = packed.nodes.map((node) => {
        const kind = flowNodeKind({ id: "", type: node.type, data: { kind: node.kind } });
        const id2 = newId2(kind);
        return { clip: node, id: id2, kind };
      });
      const nodesToAdd = created.map((entry) => attachActions({
        id: entry.id,
        type: entry.kind === "group" ? "group" : entry.kind === "director-stage" ? "director-stage" : entry.kind === "edit" ? "edit" : entry.kind === "text" ? "text" : "media",
        position: { x: origin.x + entry.clip.x - 40, y: origin.y + entry.clip.y - 40 },
        style: {
          width: entry.clip.width ?? defaultSize2(entry.kind).width,
          height: entry.clip.height ?? defaultSize2(entry.kind).height,
          overflow: "visible"
        },
        selected: true,
        data: {
          ...entry.kind === "image" || entry.kind === "video" || entry.kind === "audio" ? { kind: entry.kind, path: entry.clip.path ?? "" } : {},
          ...entry.kind === "director-stage" ? { kind: "director-stage" } : {},
          ...entry.kind === "edit" ? { kind: "edit" } : {},
          label: entry.clip.label,
          ...entry.clip.prompt !== void 0 ? { prompt: entry.clip.prompt } : {},
          ...entry.clip.shotStatus !== void 0 ? { shotStatus: entry.clip.shotStatus } : {}
        }
      }));
      const withParents = nodesToAdd;
      const edgesToAdd = packed.edges.map((edge) => ({
        id: newId2("edge"),
        source: created[edge.from]?.id ?? "",
        target: created[edge.to]?.id ?? "",
        sourceHandle: "out",
        targetHandle: "in",
        type: "wire",
        style: EDGE_STYLE,
        ...edge.label !== void 0 ? { label: edge.label } : {}
      })).filter((edge) => edge.source !== "" && edge.target !== "");
      setNodes((current) => [...current.map((node) => ({ ...node, selected: false })), ...withParents]);
      setEdges((current) => [...current, ...edgesToAdd]);
      scheduleSave();
      showToast(`\u5DF2\u7C98\u8D34 ${withParents.length} \u4E2A\u8282\u70B9`);
      return true;
    } catch {
      return false;
    }
  }, [attachActions, pushHistory, scheduleSave, screenToFlowPosition, showToast]);
  (0, import_react19.useEffect)(() => {
    actionRef.current = {
      openGenerate,
      openStudioFor,
      rename: renameNode,
      cycleStatus,
      duplicate: duplicateNode,
      lock: (id2) => toggleLock([id2]),
      download: (id2) => {
        void downloadNode(id2);
      },
      patch: (id2, patch) => patchNode(id2, patch),
      adopt: (id2) => adoptTake(id2),
      focusTake: (id2) => focusTake(id2),
      remove: (id2) => deleteIds([id2]),
      runGenerate: (id2) => runNodeGenerate(id2),
      upload: (id2) => {
        uploadTargetRef.current = id2;
        uploadRef.current?.click();
      },
      pickRef: (id2) => {
        setNodes((current) => current.map((node) => ({ ...node, selected: node.id === id2 })));
        setLibraryTab("history");
        void openPicker();
      },
      crop: (id2) => {
        setCropAspect("free");
        setCropId(id2);
      },
      confirmCrop: (id2, blob) => {
        void confirmCrop(id2, blob);
      },
      cancelCrop: () => setCropId(void 0),
      preview: (id2) => setPreviewId(id2),
      redraw: (id2) => startMask(id2, "redraw"),
      erase: (id2) => startMask(id2, "erase"),
      expand: (id2) => startExpand(id2),
      expanded: (id2, padded, mask) => {
        void confirmExpand(id2, padded, mask);
      },
      annotate: (id2) => {
        const sourcePath = nodesRef.current.find((item3) => item3.id === id2)?.data.path;
        const prompt = "\u5728\u753B\u9762\u4E0A\u6DFB\u52A0\u6E05\u6670\u53EF\u8BFB\u7684\u6807\u6CE8\uFF0C\u4E0D\u6539\u52A8\u539F\u4E3B\u4F53";
        const targetId = spawnLinkedImage(id2, { label: "\u6807\u6CE8", prompt, shotStatus: "generating" });
        void runDirectGenerate({ kind: "image", prompt, nodeId: targetId, task: "annotate", references: typeof sourcePath === "string" && sourcePath !== "" ? [sourcePath] : [] });
      },
      enhance: (id2) => {
        const sourcePath = nodesRef.current.find((item3) => item3.id === id2)?.data.path;
        const prompt = "\u9AD8\u6E05\u653E\u5927\uFF0C\u4FDD\u6301\u6784\u56FE\u4E0E\u7EC6\u8282";
        const targetId = spawnLinkedImage(id2, { label: "\u589E\u5F3A", prompt, shotStatus: "generating" });
        void runDirectGenerate({ kind: "image", prompt, nodeId: targetId, task: "enhance", references: typeof sourcePath === "string" && sourcePath !== "" ? [sourcePath] : [] });
      },
      pixels: (id2) => {
        void (async () => {
          const node = nodesRef.current.find((item3) => item3.id === id2);
          const path = node?.data.path;
          if (path === void 0 || path === "") return;
          const image = new Image();
          image.src = mediaUrl(path);
          await image.decode();
          const canvas = document.createElement("canvas");
          canvas.width = image.naturalWidth * 2;
          canvas.height = image.naturalHeight * 2;
          canvas.getContext("2d")?.drawImage(image, 0, 0, canvas.width, canvas.height);
          const blob = await new Promise((resolve) => canvas.toBlob(resolve, "image/png"));
          if (blob === null) return;
          const saved = await saveBlob(blob, `px-${Date.now()}.png`, "image/png");
          spawnLinkedImage(id2, { path: saved.path, label: `${canvas.width}\xD7${canvas.height}` });
        })();
      },
      cutout: (id2) => {
        const sourcePath = nodesRef.current.find((item3) => item3.id === id2)?.data.path;
        const prompt = "\u62A0\u51FA\u4E3B\u4F53\uFF0C\u900F\u660E\u80CC\u666F\uFF0C\u8FB9\u7F18\u5E72\u51C0";
        const targetId = spawnLinkedImage(id2, { label: "\u62A0\u56FE", prompt, shotStatus: "generating" });
        void runDirectGenerate({ kind: "image", prompt, nodeId: targetId, task: "cutout", references: typeof sourcePath === "string" && sourcePath !== "" ? [sourcePath] : [] });
      },
      split: (id2, cols, rows) => {
        void runCraft("split", { nodeId: id2, cols: cols ?? 2, rows: rows ?? 2 });
      },
      capture: (id2) => {
        void runCraft("frames", { nodeId: id2 });
      },
      extend: (id2) => {
        void runCraft("extend", { nodeId: id2 });
      },
      reshoot: (id2) => setReshootId(id2),
      masked: (id2, blob, prompt, mode) => {
        void confirmMasked(id2, blob, prompt, mode);
      },
      cancelMask: () => {
        restoreEditSize();
        setMaskId(void 0);
        setMaskMode(void 0);
        setExpandId(void 0);
      }
    };
  }, [adoptTake, confirmCrop, confirmExpand, confirmMasked, cycleStatus, deleteIds, downloadNode, duplicateNode, focusTake, openGenerate, openPicker, openStudioFor, patchNode, renameNode, restoreEditSize, runCraft, runDirectGenerate, runNodeGenerate, spawnLinkedImage, startExpand, startMask, toggleLock]);
  const groupSelected = (0, import_react19.useCallback)(() => {
    const members = selectedNodes.filter((node) => node.type !== "group" && (node.parentId === void 0 || node.parentId === ""));
    if (members.length < 2) return;
    pushHistory();
    const frame2 = groupFrame(boxesOf(members));
    const id2 = newId2("group");
    const group = {
      id: id2,
      type: "group",
      position: { x: frame2.x, y: frame2.y },
      style: { width: frame2.w, height: frame2.h },
      selected: true,
      data: { label: "\u5206\u7EC4" }
    };
    const memberIds = new Set(members.map((node) => node.id));
    setNodes((current) => [
      attachActions(group),
      ...current.map((node) => memberIds.has(node.id) ? { ...node, parentId: id2, extent: "parent", position: { x: node.position.x - frame2.x, y: node.position.y - frame2.y }, selected: false } : { ...node, selected: false })
    ]);
    scheduleSave();
  }, [attachActions, pushHistory, scheduleSave, selectedNodes]);
  const selectAdjacent = (0, import_react19.useCallback)((direction) => {
    const list = readingOrder(nodesRef.current.map((node) => ({
      id: node.id,
      x: node.position.x,
      y: node.position.y,
      ...node.data.shotIndex !== void 0 ? { shotIndex: node.data.shotIndex } : {}
    })));
    if (list.length === 0) return;
    const current = nodesRef.current.find((node) => node.selected === true)?.id;
    const index2 = list.findIndex((item3) => item3.id === current);
    const next = list[(index2 < 0 ? 0 : index2 + direction + list.length) % list.length];
    if (next === void 0) return;
    setNodes((nodes2) => nodes2.map((node) => ({ ...node, selected: node.id === next.id })));
  }, []);
  (0, import_react19.useEffect)(() => {
    const onKey = (event) => {
      const target = event.target;
      const typing = typeof target?.matches === "function" && target.matches("input, textarea, [contenteditable=true]");
      if (typing) return;
      const studio = snapshot2.tab === "image" || snapshot2.tab === "video";
      if (studio) return;
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "z") {
        event.preventDefault();
        if (event.shiftKey) redo();
        else undo();
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "y") {
        event.preventDefault();
        redo();
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key === "0") {
        event.preventDefault();
        void fitView({ padding: 0.2, duration: 240, maxZoom: 1 });
      }
      if ((event.metaKey || event.ctrlKey) && (event.key === "=" || event.key === "+")) {
        event.preventDefault();
        void zoomIn();
      }
      if ((event.metaKey || event.ctrlKey) && event.key === "-") {
        event.preventDefault();
        void zoomOut();
      }
      if ((event.metaKey || event.ctrlKey) && (event.key.toLowerCase() === "k" || event.key.toLowerCase() === "f")) {
        event.preventDefault();
        setSearchOpen((open) => !open);
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "a") {
        event.preventDefault();
        setNodes((current) => current.map((node) => ({ ...node, selected: true })));
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "c") {
        event.preventDefault();
        void copySelected();
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "v") {
        event.preventDefault();
        void pasteClip();
        return;
      }
      if (event.key === "?" || event.shiftKey && event.key === "/") {
        event.preventDefault();
        setHelpOpen((open) => !open);
        return;
      }
      if (event.key === "Escape") {
        if (addMenu !== void 0 || nodeMenu !== void 0 || edgeMenu !== void 0 || wireMenu !== void 0) {
          setAddMenu(void 0);
          setNodeMenu(void 0);
          setEdgeMenu(void 0);
          setWireMenu(void 0);
          return;
        }
        if (helpOpen) {
          setHelpOpen(false);
          return;
        }
        if (cropId !== void 0) {
          setCropId(void 0);
          return;
        }
        if (maskId !== void 0) {
          setMaskId(void 0);
          setMaskMode(void 0);
          return;
        }
        if (previewId !== void 0) {
          setPreviewId(void 0);
          return;
        }
        if (parsePreview !== void 0) {
          setParsePreview(void 0);
          return;
        }
        if (reshootId !== void 0) {
          setReshootId(void 0);
          return;
        }
        if (reviseId !== void 0) {
          setReviseId(void 0);
          return;
        }
        if (sessionOpen) {
          setSessionOpen(false);
          return;
        }
        setPicker(false);
        setWiring(false);
        setSearchOpen(false);
        setCompare(void 0);
        setCompose((current) => ({ kind: current.kind, prompt: current.prompt }));
        setGenerateOpen(false);
        if (snapshot2.tab !== "canvas") setEditorTab("canvas");
      }
      if (event.key.toLowerCase() === "e" && !event.metaKey && !event.ctrlKey) {
        const focus = nodesRef.current.find((node) => node.selected === true && node.type === "media" && typeof node.data.path === "string" && node.data.path !== "");
        if (focus !== void 0) {
          event.preventDefault();
          openStudioFor(focus.id);
        }
        return;
      }
      if (event.key.toLowerCase() === "s" && !event.metaKey && !event.ctrlKey) {
        const focus = nodesRef.current.find((node) => node.selected === true);
        if (focus !== void 0) {
          event.preventDefault();
          cycleStatus(focus.id);
        }
      }
      if (event.key.toLowerCase() === "c" && !event.metaKey && !event.ctrlKey) {
        const media = nodesRef.current.filter((node) => node.selected === true && node.type === "media" && node.data.path);
        if (media.length >= 2) {
          event.preventDefault();
          setCompare([media[0].id, media[1].id]);
        }
      }
      if (event.key.toLowerCase() === "g" && !event.metaKey && !event.ctrlKey) {
        event.preventDefault();
        openGenerate();
      }
      if (event.key.toLowerCase() === "l" && !event.metaKey && !event.ctrlKey) {
        event.preventDefault();
        toggleLock(nodesRef.current.filter((node) => node.selected === true).map((node) => node.id));
        return;
      }
      if (event.key.toLowerCase() === "f" && !event.metaKey && !event.ctrlKey) {
        event.preventDefault();
        const focus = nodesRef.current.filter((node) => node.selected === true);
        const kind = focus.length === 1 && focus[0]?.type === "group" ? "group" : "card";
        void fitView({ nodes: focus.length > 0 ? focus : void 0, ...focusViewOptions(kind) });
        return;
      }
      if (event.key === "Tab") {
        event.preventDefault();
        selectAdjacent(event.shiftKey ? -1 : 1);
        return;
      }
      if (["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"].includes(event.key) && !event.metaKey && !event.ctrlKey) {
        event.preventDefault();
        const step = nudgeStep(snap, event.shiftKey);
        const dx2 = event.key === "ArrowLeft" ? -step : event.key === "ArrowRight" ? step : 0;
        const dy = event.key === "ArrowUp" ? -step : event.key === "ArrowDown" ? step : 0;
        applyPositions(nudgeBoxes(boxesOf(nodesRef.current.filter((node) => node.selected === true && node.data.locked !== true)), dx2, dy));
        return;
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "d") {
        event.preventDefault();
        const source = nodesRef.current.find((node) => node.selected === true);
        if (source !== void 0) duplicateNode(source.id);
      }
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "g") {
        event.preventDefault();
        groupSelected();
      }
      if ((event.key === "Backspace" || event.key === "Delete") && !event.metaKey && !event.ctrlKey) {
        const ids = nodesRef.current.filter((node) => node.selected === true).map((node) => node.id);
        const edgeIds = edgesRef.current.filter((edge) => edge.selected === true).map((edge) => edge.id);
        if (ids.length > 0 || edgeIds.length > 0) {
          event.preventDefault();
          if (ids.length > 0) deleteIds(ids);
          if (edgeIds.length > 0) {
            pushHistory();
            const drop = new Set(edgeIds);
            setEdges((current) => current.filter((edge) => !drop.has(edge.id)));
            scheduleSave();
          }
        }
      }
    };
    window.addEventListener("keydown", onKey, true);
    return () => window.removeEventListener("keydown", onKey, true);
  }, [addMenu, applyPositions, copySelected, cropId, cycleStatus, deleteIds, duplicateNode, edgeMenu, fitView, groupSelected, helpOpen, maskId, nodeMenu, openGenerate, openStudioFor, parsePreview, pasteClip, previewId, pushHistory, redo, reshootId, reviseId, scheduleSave, selectAdjacent, sessionOpen, snap, snapshot2.tab, toggleLock, undo, wireMenu, zoomIn, zoomOut]);
  (0, import_react19.useEffect)(() => {
    const onPaste = (event) => {
      const target = event.target;
      const typing = typeof target?.matches === "function" && target.matches("input, textarea, [contenteditable=true]");
      if (typing) return;
      const files2 = [...event.clipboardData?.files ?? []].filter((file) => file.type.startsWith("image/") || file.type.startsWith("video/"));
      if (files2.length === 0) return;
      event.preventDefault();
      void uploadFiles(files2);
    };
    window.addEventListener("paste", onPaste);
    return () => window.removeEventListener("paste", onPaste);
  }, [uploadFiles]);
  const dismissMenus = (0, import_react19.useCallback)(() => {
    setAddMenu(void 0);
    setNodeMenu(void 0);
    setEdgeMenu(void 0);
    setWireMenu(void 0);
    setGenerateOpen(false);
  }, []);
  (0, import_react19.useEffect)(() => {
    const open = addMenu !== void 0 || nodeMenu !== void 0 || edgeMenu !== void 0 || wireMenu !== void 0;
    if (!open) return;
    const onPointer = (event) => {
      const target = event.target;
      if (target?.closest("[data-dx-menu],[data-dx-menu-anchor]") !== null) return;
      dismissMenus();
    };
    const onWheel = (event) => {
      const target = event.target;
      if (target?.closest("[data-dx-menu]") !== null) return;
      dismissMenus();
    };
    document.addEventListener("pointerdown", onPointer, true);
    document.addEventListener("wheel", onWheel, { capture: true, passive: true });
    return () => {
      document.removeEventListener("pointerdown", onPointer, true);
      document.removeEventListener("wheel", onWheel, true);
    };
  }, [addMenu, dismissMenus, edgeMenu, nodeMenu, wireMenu]);
  const pickAdd = (0, import_react19.useCallback)((kind, flow) => {
    setAddMenu(void 0);
    if (kind === "upload" || kind === "paste") {
      if (kind === "upload") uploadRef.current?.click();
      return;
    }
    addNode(kind, {}, flow);
  }, [addNode]);
  const setZoom = (0, import_react19.useCallback)((next) => {
    const view = getViewport();
    void setViewport({ ...view, zoom: Math.max(0.1, Math.min(3, next)) });
  }, [getViewport, setViewport]);
  const studioOpen = snapshot2.tab === "image" || snapshot2.tab === "video";
  const studioTitle = displayCardTitle(selected2?.data.label, selected2?.data.prompt, selected2?.data.shotIndex) || selected2?.data.label || snapshot2.path?.split("/").pop() || (snapshot2.tab === "video" ? "\u89C6\u9891" : "\u56FE\u7247");
  const sourceNode = compose.sourceId !== void 0 ? nodes.find((node) => node.id === compose.sourceId) : void 0;
  const mediaSelected = selectedNodes.filter((node) => node.type === "media" && node.data.path);
  const multi = selectedNodes.length >= 2 && !studioOpen;
  const workspace = snapshot2.workspace;
  (0, import_react19.useEffect)(() => {
    const onMessage = (event) => {
      const data = event.data;
      if (data?.type !== "directorx:insert-media" || typeof data.path !== "string" || data.path === "") return;
      const sourceId = data.nodeId || workspace?.nodeId;
      const kind = data.kind === "video" ? "video" : data.kind === "audio" ? "audio" : "image";
      const source = sourceId !== void 0 && sourceId !== "" ? nodesRef.current.find((node) => node.id === sourceId) : void 0;
      const at2 = source !== void 0 ? { x: source.position.x + (typeof source.style?.width === "number" ? source.style.width : 560) + 48, y: source.position.y } : void 0;
      const id2 = addNode(kind, { path: data.path, label: data.name || (kind === "video" ? "\u5BFC\u51FA\u89C6\u9891" : kind === "audio" ? "\u5BFC\u51FA\u97F3\u9891" : "\u5BFC\u51FA\u56FE\u7247") }, at2);
      if (sourceId !== void 0 && sourceId !== "") linkNodes(sourceId, id2);
      if (event.source instanceof Window) {
        event.source.postMessage({ type: "directorx:insert-media:ack", channel: data.channel }, event.origin || window.location.origin);
      }
      closeWorkspace();
      showToast("\u5DF2\u63A5\u5230\u753B\u5E03\u4E0B\u6E38");
    };
    window.addEventListener("message", onMessage);
    return () => window.removeEventListener("message", onMessage);
  }, [addNode, linkNodes, showToast, workspace?.nodeId]);
  return /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { className: `dx-stage${wiring ? " dx-wiring" : ""}${hideWires ? " dx-hide-wires" : ""}${sessionOpen ? " dx-session-open" : ""}${cropId !== void 0 || maskId !== void 0 || expandId !== void 0 ? " dx-editing-image" : ""}`, style: shell, children: [
    /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("style", { children: STAGE_CSS }),
    /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
      TopBar,
      {
        title,
        onTitle: (value) => {
          setTitle(value);
          titleRef.current = value;
          scheduleSave();
        },
        projects,
        project,
        onProject: (path) => {
          dirtyRef.current = false;
          setClientProject(path);
          setProject(path);
        },
        saveState,
        count: nodes.length,
        onClose: props.onClose
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
      "input",
      {
        ref: uploadRef,
        type: "file",
        accept: "image/*,video/*",
        multiple: true,
        hidden: true,
        onChange: (event) => {
          const list = event.target.files;
          if (list !== null) void uploadFiles(Array.from(list));
          event.target.value = "";
        }
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { ref: stageRef, style: { flex: 1, minHeight: 0, minWidth: 0, position: "relative", touchAction: "none" }, children: [
      error !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("div", { style: { position: "absolute", top: 64, left: "50%", transform: "translateX(-50%)", zIndex: 25, color: "#ff9b8f", fontSize: 12, ...{ background: "rgba(40,16,14,.9)", padding: "6px 12px", borderRadius: 999 } }, children: error }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)(
        index,
        {
          nodes: wireDrag?.targetId === void 0 ? paintedNodes : paintedNodes.map((node) => node.id === wireDrag.targetId ? { ...node, className: "dx-can-connect" } : node.className !== void 0 ? { ...node, className: void 0 } : node),
          edges,
          nodeTypes: stageNodeTypes,
          onNodesChange,
          onEdgesChange,
          onConnect,
          onConnectStart,
          onConnectEnd,
          onReconnect,
          isValidConnection: (connection) => {
            if (connection.source === connection.target) return false;
            return nodesRef.current.find((node) => node.id === connection.target)?.data.locked !== true;
          },
          edgeTypes,
          connectionLineComponent: HiddenConnectionLine,
          onNodeDoubleClick: (event, node) => {
            const target = event.target;
            if (target.closest('input, textarea, [contenteditable="true"]') !== null) return;
            event.preventDefault();
            const kind = flowNodeKind(node);
            if (kind === "director-stage") {
              openDirectorStage(node.id);
              return;
            }
            if (kind === "edit") {
              openEdit(node.id);
              return;
            }
            focusNode(node.id);
          },
          onPaneContextMenu: (event) => {
            event.preventDefault();
            setNodeMenu(void 0);
            setEdgeMenu(void 0);
            setAddMenu({
              x: event.clientX,
              y: event.clientY,
              flow: screenToFlowPosition({ x: event.clientX, y: event.clientY }),
              mode: "full"
            });
          },
          onDoubleClick: (event) => {
            const target = event.target;
            if (target.closest(".react-flow__node") !== null) return;
            setNodeMenu(void 0);
            setEdgeMenu(void 0);
            setAddMenu({
              x: event.clientX,
              y: event.clientY,
              flow: screenToFlowPosition({ x: event.clientX, y: event.clientY }),
              mode: "quick"
            });
          },
          onNodeContextMenu: (event, node) => {
            event.preventDefault();
            setAddMenu(void 0);
            setEdgeMenu(void 0);
            const selected3 = nodesRef.current.filter((item3) => item3.selected === true).map((item3) => item3.id);
            const keepMulti = selected3.includes(node.id) && selected3.length > 1;
            if (!keepMulti) {
              setNodes((current) => current.map((item3) => ({ ...item3, selected: item3.id === node.id })));
            }
            setNodeMenu({
              x: event.clientX,
              y: event.clientY,
              nodeIds: keepMulti ? selected3 : [node.id]
            });
          },
          onEdgeContextMenu: (event, edge) => {
            event.preventDefault();
            setAddMenu(void 0);
            setNodeMenu(void 0);
            setEdgeMenu({ x: event.clientX, y: event.clientY, edgeId: edge.id });
          },
          onDrop: (event) => {
            event.preventDefault();
            const at2 = screenToFlowPosition({ x: event.clientX, y: event.clientY });
            if (event.dataTransfer.files.length > 0) void uploadFiles(Array.from(event.dataTransfer.files), at2);
          },
          onDragOver: (event) => {
            event.preventDefault();
            event.dataTransfer.dropEffect = "copy";
          },
          onNodeDragStart: () => {
            dismissMenus();
            pushHistory();
          },
          nodesDraggable: cropId === void 0,
          nodesConnectable: true,
          elementsSelectable: true,
          edgesReconnectable: false,
          selectNodesOnDrag: true,
          nodeDragThreshold: 1,
          connectionMode: ConnectionMode.Loose,
          connectionRadius: 32,
          connectOnClick: true,
          deleteKeyCode: null,
          onlyRenderVisibleElements: false,
          selectionOnDrag: true,
          selectionKeyCode: "Shift",
          multiSelectionKeyCode: ["Shift", "Meta"],
          panOnDrag: [1, 2],
          panActivationKeyCode: "Space",
          snapToGrid: snap,
          snapGrid: [15, 15],
          selectionMode: SelectionMode.Partial,
          elevateNodesOnSelect: true,
          zoomOnScroll: false,
          zoomActivationKeyCode: "Meta",
          panOnScroll: true,
          zoomOnPinch: true,
          zoomOnDoubleClick: false,
          preventScrolling: true,
          minZoom: dx.minZoom,
          maxZoom: dx.maxZoom,
          connectionLineType: ConnectionLineType.Bezier,
          connectionLineStyle: { stroke: "rgba(255,255,255,.5)", strokeWidth: 1.8 },
          style: {
            width: box2.width > 0 ? box2.width : "100%",
            height: box2.height > 0 ? box2.height : "100%"
          },
          defaultEdgeOptions: {
            type: "wire",
            style: { stroke: "rgba(236,236,236,.9)", strokeWidth: 2.15 },
            interactionWidth: 28
          },
          proOptions: { hideAttribution: true },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(Background, { variant: BackgroundVariant.Dots, gap: 20, size: 0.5, color: "rgba(255,255,255,.30)", bgColor: "#000000" }),
            box2.width > 1080 && !sessionOpen ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(MiniMap, { pannable: true, zoomable: true, position: "bottom-left", style: { background: "#121212", width: 132, height: 84 } }) : null,
            wireDrag !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(WireDragLayer, { from: wireDrag.from, to: wireDrag.to, fromSide: wireDrag.handleId, valid: wireDrag.targetId !== void 0 }) : null
          ]
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        StageRail,
        {
          onAdd: () => setAddMenu((current) => current !== void 0 ? void 0 : { x: 64, y: Math.max(72, Math.round((typeof window === "undefined" ? 480 : window.innerHeight) / 2 - 160)), mode: "full" }),
          onSearch: () => setSearchOpen(true),
          onAssets: () => {
            void openPicker();
          },
          onUpload: () => uploadRef.current?.click(),
          onHelp: () => setHelpOpen(true)
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        ZoomHud,
        {
          zoom,
          canUndo: historyTick >= 0 && undoRef.current.length > 0,
          canRedo: historyTick >= 0 && redoRef.current.length > 0,
          onUndo: undo,
          onRedo: redo,
          onZoom: setZoom,
          snap,
          hideWires,
          onSnap: () => setSnap((value) => !value),
          onHideWires: () => setHideWires((value) => !value),
          onFit: () => {
            const focus = nodes.filter((node) => node.selected === true);
            const kind = focus.length === 1 && focus[0]?.type === "group" ? "group" : "card";
            void fitView({
              nodes: focus.length > 0 ? focus : void 0,
              ...focus.length > 0 ? focusViewOptions(kind) : { padding: 0.2, duration: 240, maxZoom: 1 }
            });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(Toast, { text: toast }),
      nodes.length === 0 && !studioOpen ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        EmptyHero,
        {
          onGenerate: () => openGenerate(),
          onAdd: () => setAddMenu({ x: 64, y: Math.max(72, Math.round((typeof window === "undefined" ? 480 : window.innerHeight) / 2 - 160)), mode: "full" }),
          onScript: () => pickAdd("text")
        }
      ) : null,
      addMenu !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        AddMenu,
        {
          x: addMenu.x,
          y: addMenu.y,
          mode: addMenu.mode ?? "full",
          onPick: (kind) => pickAdd(kind, addMenu.flow),
          onPaste: () => {
            setAddMenu(void 0);
            void pasteClip();
          }
        }
      ) : null,
      wireMenu !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        ConnectMenu,
        {
          x: wireMenu.x,
          y: wireMenu.y,
          onPick: (kind) => {
            const source = nodesRef.current.find((node) => node.id === wireMenu.sourceId);
            const width = typeof source?.style?.width === "number" ? source.style.width : defaultSize2(source?.data.kind ?? "image").width;
            const dragged = Math.hypot(
              wireMenu.flow.x - (source?.position.x ?? wireMenu.flow.x),
              wireMenu.flow.y - (source?.position.y ?? wireMenu.flow.y)
            );
            const at2 = source !== void 0 && dragged < 80 ? { x: source.position.x + width + 48, y: source.position.y } : wireMenu.flow;
            const id2 = addNode(kind, {
              label: kind === "director-stage" ? "3D \u5BFC\u6F14\u53F0" : kind === "edit" ? "\u526A\u8F91\u53F0" : kind === "video" ? "\u4E0B\u6E38\u89C6\u9891" : kind === "audio" ? "\u4E0B\u6E38\u97F3\u9891" : kind === "image" ? "\u4E0B\u6E38\u56FE\u7247" : "\u4E0B\u6E38\u6587\u672C",
              prompt: source?.data.prompt ?? "",
              ...source?.data.aspect !== void 0 ? { aspect: source.data.aspect } : {},
              ...source?.data.model !== void 0 ? { model: source.data.model } : {},
              ...source?.data.durationSec !== void 0 ? { durationSec: source.data.durationSec } : {},
              ...source?.data.characters !== void 0 ? { characters: source.data.characters } : {},
              ...kind !== "text" && kind !== "director-stage" && kind !== "edit" ? { shotStatus: "idea" } : {}
            }, at2);
            linkNodes(wireMenu.sourceId, id2, { sourceHandle: wireMenu.handleId ?? "out", targetHandle: "in" });
            setWireMenu(void 0);
            if (kind === "director-stage" || kind === "edit") return;
            if (kind !== "text") {
              setCompose({
                kind,
                prompt: source?.data.prompt ?? source?.data.label ?? "",
                sourceId: wireMenu.sourceId,
                targetId: id2,
                count: source?.data.count ?? 1,
                ...source?.data.aspect !== void 0 ? { aspect: source.data.aspect } : { aspect: "16:9" },
                ...source?.data.model !== void 0 ? { model: source.data.model } : {},
                ...source?.data.durationSec !== void 0 ? { durationSec: source.data.durationSec } : {},
                ...source?.data.characters !== void 0 ? { characters: source.data.characters } : {},
                refIds: [wireMenu.sourceId]
              });
              focusCompose();
            }
          }
        }
      ) : null,
      nodeMenu !== void 0 ? (() => {
        const picked = nodeMenu.nodeIds.map((id2) => nodes.find((item3) => item3.id === id2)).filter((item3) => item3 !== void 0);
        const focus = picked[0];
        if (focus === void 0) return null;
        const focusId = focus.id;
        const surface = {
          type: (() => {
            const kind = flowNodeKind(focus);
            return kind === "group" || kind === "text" || kind === "director-stage" || kind === "edit" ? kind : "media";
          })(),
          kind: focus.data.kind === "video" || focus.data.kind === "image" || focus.data.kind === "audio" ? focus.data.kind : void 0,
          hasPath: typeof focus.data.path === "string" && focus.data.path !== "",
          locked: picked.every((item3) => item3.data.locked === true),
          selectedCount: picked.length,
          canUngroup: picked.some((item3) => item3.type === "group")
        };
        return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
          NodeMenu,
          {
            x: nodeMenu.x,
            y: nodeMenu.y,
            surface,
            onAction: (id2) => {
              setNodeMenu(void 0);
              if (id2 === "edit") {
                openStudioFor(focusId);
                return;
              }
              if (id2 === "crop") {
                setCropAspect("free");
                setCropId(focusId);
                return;
              }
              if (id2 === "redraw") {
                openRevise(focusId);
                return;
              }
              if (id2 === "frames") {
                void runCraft("frames", { nodeId: focusId });
                return;
              }
              if (id2 === "reshoot") {
                setReshootId(focusId);
                return;
              }
              if (id2 === "split") {
                void runCraft("split", { nodeId: focusId });
                return;
              }
              if (id2 === "extend") {
                void runCraft("extend", { nodeId: focusId });
                return;
              }
              if (id2 === "download") {
                void downloadNode(focusId);
                return;
              }
              if (id2 === "preview") {
                setPreviewId(focusId);
                return;
              }
              if (id2 === "duplicate") {
                duplicateNode(focusId);
                return;
              }
              if (id2 === "ungroup") {
                ungroupIds(picked.map((item3) => item3.id));
                return;
              }
              if (id2 === "group") {
                groupSelected();
                return;
              }
              if (id2 === "delete") {
                deleteIds(picked.map((item3) => item3.id));
              }
            }
          }
        );
      })() : null,
      reshootId !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        ReshootDialog,
        {
          durationSec: nodes.find((item3) => item3.id === reshootId)?.data.durationSec,
          prompt: nodes.find((item3) => item3.id === reshootId)?.data.prompt ?? "",
          onCancel: () => setReshootId(void 0),
          onSubmit: (input2) => {
            void submitReshoot(input2);
          }
        }
      ) : null,
      parsePreview !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        ParseSheet,
        {
          sourceLabel: parsePreview.sourceLabel,
          shots: parsePreview.shots,
          onCancel: () => setParsePreview(void 0),
          onApply: () => {
            const next = parsePreview;
            setParsePreview(void 0);
            void runCraft("parse", { nodeId: next.nodeId, shots: next.shots });
          }
        }
      ) : null,
      edgeMenu !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        EdgeMenu,
        {
          x: edgeMenu.x,
          y: edgeMenu.y,
          onDelete: () => {
            pushHistory();
            setEdges((current) => current.filter((edge) => edge.id !== edgeMenu.edgeId));
            setEdgeMenu(void 0);
            scheduleSave();
          }
        }
      ) : null,
      searchOpen && !studioOpen ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        SearchPalette,
        {
          query: searchQuery,
          filter: searchFilter,
          hits: nodes.filter((node) => {
            const kind = node.data.kind ?? node.type ?? "text";
            if (searchFilter !== "" && kind !== searchFilter) return false;
            const blob = `${node.id} ${node.type} ${kind} ${node.data.label} ${node.data.prompt ?? ""}`.toLowerCase();
            return searchQuery.trim() === "" || blob.includes(searchQuery.trim().toLowerCase());
          }).slice(0, 20).map((node) => ({
            id: node.id,
            kind: node.data.kind ?? node.type ?? "text",
            label: displayCardTitle(node.data.label, node.data.prompt, node.data.shotIndex) || node.data.label
          })),
          onQuery: setSearchQuery,
          onFilter: setSearchFilter,
          onPick: (id2) => {
            setSearchOpen(false);
            setSearchQuery("");
            focusNode(id2);
          },
          onClose: () => setSearchOpen(false)
        }
      ) : null,
      compare !== void 0 && !studioOpen ? (() => {
        const left = nodes.find((node) => node.id === compare[0]);
        const right = nodes.find((node) => node.id === compare[1]);
        if (left === void 0 || right === void 0 || !left.data.path || !right.data.path) return null;
        return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
          CompareOverlay,
          {
            left: { id: left.id, label: left.data.label, path: mediaUrl(left.data.path), kind: left.data.kind ?? "image" },
            right: { id: right.id, label: right.data.label, path: mediaUrl(right.data.path), kind: right.data.kind ?? "image" },
            onAdopt: adoptTake,
            onClose: () => setCompare(void 0)
          }
        );
      })() : null,
      generateOpen && !studioOpen && compare === void 0 && !searchOpen && parsePreview === void 0 && !multi && selected2 === void 0 && !(sessionOpen && box2.width > 0 && box2.width < 900) ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        GenerateDock,
        {
          spec: compose,
          busy: askBusy,
          reserveRight: sessionOpen ? 428 : 24,
          sourceLabel: sourceNode?.data.label,
          inputRef: composeRef,
          onChange: setCompose,
          onSubmit: () => {
            void submitAsk();
          },
          onClose: () => {
            setGenerateOpen(false);
            composeRef.current?.blur();
          },
          onClearSource: () => setCompose((current) => ({ kind: current.kind, prompt: current.prompt, count: current.count, aspect: current.aspect }))
        }
      ) : null,
      cropId !== void 0 && !studioOpen ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        CropBar,
        {
          aspect: cropAspect,
          onAspect: setCropAspect,
          onCancel: () => setCropId(void 0),
          onConfirm: () => {
            const apply2 = document.querySelector(".dx-crop-apply");
            if (apply2 instanceof HTMLButtonElement) apply2.click();
          }
        }
      ) : null,
      multi ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        MultiSelectBar,
        {
          count: selectedNodes.length,
          canCompare: mediaSelected.length >= 2,
          canUngroup: selectedNodes.some((node) => node.type === "group"),
          canPack: selectedNodes.filter((node) => node.type === "media" && node.data.kind === "video" && typeof node.data.path === "string" && node.data.path !== "").length >= 2,
          canSheet: selectedNodes.filter((node) => node.type === "media" && typeof node.data.path === "string" && node.data.path !== "").length >= 1,
          canJoin: selectedNodes.filter((node) => node.type === "media" && node.data.kind === "image" && typeof node.data.path === "string" && node.data.path !== "").length >= 2,
          canStack: selectedNodes.filter((node) => node.type === "media" && typeof node.data.path === "string" && node.data.path !== "").length >= 2 && selectedNodes.filter((node) => node.type === "media" && typeof node.data.path === "string" && node.data.path !== "").length <= 4,
          locked: selectedNodes.every((node) => node.data.locked === true),
          onCompare: () => setCompare([mediaSelected[0].id, mediaSelected[1].id]),
          onGroup: groupSelected,
          onUngroup: () => ungroupIds(selectedNodes.map((node) => node.id)),
          onPack: () => {
            const ids = selectedNodes.filter((node) => node.type === "media" && node.data.kind === "video" && typeof node.data.path === "string" && node.data.path !== "").map((node) => node.id);
            void runCraft("pack", { nodeIds: ids, transition: "cut" });
          },
          onSheet: () => {
            const ids = selectedNodes.filter((node) => node.type === "media" && typeof node.data.path === "string" && node.data.path !== "").map((node) => node.id);
            void runCraft("sheet", { nodeIds: ids });
          },
          onJoin: () => {
            const ids = selectedNodes.filter((node) => node.type === "media" && node.data.kind === "image" && typeof node.data.path === "string" && node.data.path !== "").map((node) => node.id);
            void runCraft("join", { nodeIds: ids });
          },
          onStack: () => {
            const ids = selectedNodes.filter((node) => node.type === "media" && typeof node.data.path === "string" && node.data.path !== "").map((node) => node.id);
            void runCraft("stack", { nodeIds: ids });
          },
          onAlign: alignSelected,
          onDistribute: distributeSelected,
          onLock: () => toggleLock(selectedNodes.map((node) => node.id)),
          onGenerate: () => openGenerate(selectedNodes[0]?.id),
          onDelete: () => deleteIds(selectedNodes.map((node) => node.id))
        }
      ) : null,
      selected2 !== void 0 && !studioOpen && !multi && selected2.type === "group" ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        InspectorSheet,
        {
          kind: "text",
          kindLabel: "\u5206\u7EC4",
          label: selected2.data.label,
          prompt: selected2.data.prompt ?? "",
          status: selected2.data.shotStatus,
          canEdit: false,
          canCompare: false,
          canDownload: false,
          locked: selected2.data.locked === true,
          onLabel: (value) => patchSelected({ label: value }),
          onPrompt: (value) => patchSelected({ prompt: value }),
          onStatus: (status) => setShotStatus(selected2.id, status),
          onEdit: () => {
          },
          onGenerate: () => openGenerate(selected2.id),
          onCompare: () => {
          },
          onLock: () => toggleLock([selected2.id]),
          onDownload: () => {
            void downloadNode(selected2.id);
          },
          onDelete: () => deleteIds([selected2.id])
        }
      ) : null,
      workspace !== null && workspace !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("div", { style: { position: "absolute", inset: 0, zIndex: 40, background: "#111" }, children: workspace.kind === "edit" ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(EditStage, { nodeId: workspace.nodeId, onClose: closeWorkspace }) : /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(DirectorStage, { nodeId: workspace.nodeId, onClose: closeWorkspace }) }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        SessionDock,
        {
          sessionId: boundSessionId,
          sessions: props.sessions,
          liveSessions: props.liveSessions,
          pending: props.pending,
          open: sessionOpen,
          onOpenChange: onSessionOpenChange,
          hidden: studioOpen || searchOpen || compare !== void 0 || helpOpen,
          inspectorOpen: selected2 !== void 0 && !studioOpen && !multi && selected2.type !== "media",
          pulse: sessionPulse,
          onEnsureSession: ensureWorkspaceSession,
          onNewSession: startWorkspaceSession,
          onLeave: props.onClose,
          selectedNode: selected2 !== void 0 ? {
            id: selected2.id,
            label: selected2.data.label,
            ...selected2.data.kind !== void 0 ? { kind: selected2.data.kind } : {},
            ...selected2.data.path !== void 0 ? { path: selected2.data.path } : {}
          } : void 0,
          onClearSelected: () => setNodes((current) => current.map((node) => ({ ...node, selected: false }))),
          onAddMedia: (file) => {
            const existing = nodesRef.current.find((node) => node.data.path === file.path);
            if (existing !== void 0) {
              setNodes((current) => current.map((node) => ({ ...node, selected: node.id === existing.id })));
              focusNode(existing.id);
              showToast("\u5DF2\u5728\u753B\u5E03\u4E0A\u6253\u5F00");
              return;
            }
            addNode(file.kind, { path: file.path, label: file.label, kind: file.kind });
            showToast("\u5DF2\u52A0\u5165\u753B\u5E03");
          }
        }
      ),
      helpOpen && !studioOpen ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(ShortcutsSheet, { onClose: () => setHelpOpen(false) }) : null,
      picker ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        AssetDrawer,
        {
          tab: libraryTab,
          query: libraryQuery,
          files,
          characters,
          canPin: selected2?.type === "media" && typeof selected2.data.path === "string" && selected2.data.path !== "",
          onTab: setLibraryTab,
          onQuery: setLibraryQuery,
          onClose: () => setPicker(false),
          onPick: (file) => {
            const kind = file.mediaType.startsWith("video/") ? "video" : "image";
            const empty2 = selected2?.type === "media" && (selected2.data.path === void 0 || selected2.data.path === "");
            if (empty2 && selected2 !== void 0) patchNode(selected2.id, { path: file.path, label: nextCardLabel(selected2.data.label, file.name) ?? file.name, kind });
            else addNode(kind, { path: file.path, label: file.name });
            setPicker(false);
          },
          onUseAsRef: (file) => {
            const target = selected2?.type === "media" ? selected2 : void 0;
            if (target === void 0) {
              showToast("\u5148\u9009\u4E2D\u8981\u63A5\u53C2\u8003\u7684\u8282\u70B9");
              return;
            }
            const kind = file.mediaType.startsWith("video/") ? "video" : "image";
            const id2 = addNode(kind, { path: file.path, label: file.name }, {
              x: target.position.x - 280,
              y: target.position.y
            });
            linkNodes(id2, target.id);
            setPicker(false);
          },
          onPickCharacter: (name2, refPath) => {
            addNode("image", { path: refPath, label: name2 });
            setCompose((current) => ({ ...current, characters: [.../* @__PURE__ */ new Set([...current.characters ?? [], name2])] }));
            if (selected2?.type === "media") {
              patchNode(selected2.id, { characters: [.../* @__PURE__ */ new Set([...selected2.data.characters ?? [], name2])] });
            }
            setPicker(false);
          },
          onPin: (role) => {
            void pinSubject(role);
          }
        }
      ) : null,
      studioOpen ? studioError !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("div", { style: { position: "absolute", inset: 0, zIndex: 40, background: dx.black, display: "grid", placeItems: "center", fontFamily: dx.font }, children: /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { style: { textAlign: "center", maxWidth: 420 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { style: { color: "#ff9b8f", marginBottom: 12, fontSize: 13 }, children: [
          "\u52A0\u8F7D\u5931\u8D25\uFF1A",
          studioError
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("button", { className: "dx-hit", style: { padding: "8px 14px", borderRadius: 10, border: "1px solid rgba(255,255,255,.16)", background: "rgba(255,255,255,.06)", color: "#eee" }, onClick: () => setEditorTab("canvas"), children: "\u8FD4\u56DE\u753B\u5E03" })
      ] }) }) : studioUrl === void 0 ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("div", { style: { position: "absolute", inset: 0, zIndex: 40, background: dx.black, display: "grid", placeItems: "center", color: dx.mute, fontFamily: dx.font, fontSize: 13 }, children: "\u6B63\u5728\u52A0\u8F7D\u5A92\u4F53\u2026" }) : /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(StudioErrorBoundary, { children: snapshot2.tab === "video" ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(VideoStudio, { source: studioUrl, name: studioTitle, look: snapshot2.look, nodeId: selected2?.id, onExport: (blob, type) => {
        void onStudioExport(blob, type);
      }, onClose: () => setEditorTab("canvas") }) : /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(ImageStudio, { source: studioUrl, name: studioTitle, look: snapshot2.look, onExport: (blob, type) => {
        void onStudioExport(blob, type);
      }, onClose: () => setEditorTab("canvas") }) }) : null
    ] }),
    previewId !== void 0 ? (() => {
      const node = nodes.find((item3) => item3.id === previewId);
      const path = node?.data.path;
      if (node === void 0 || path === void 0 || path === "") return null;
      return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("div", { className: "dx-preview", onClick: () => setPreviewId(void 0), children: node.data.kind === "video" ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("video", { src: mediaUrl(path), controls: true, autoPlay: true, onClick: (event) => event.stopPropagation() }) : /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("img", { src: mediaUrl(path), alt: "", onClick: (event) => event.stopPropagation() }) });
    })() : null
  ] });
}
function Stage(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(ReactFlowProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(StudioErrorBoundary, { children: /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(StageInner, { ...props }) }) });
}

// src/client/EditorDock.tsx
var import_jsx_runtime22 = require("react/jsx-runtime");
var overlay = {
  position: "fixed",
  inset: 0,
  zIndex: 10,
  minWidth: 0,
  minHeight: 0,
  overflow: "hidden",
  background: "#000",
  color: "#f5f5f5"
};
var handle = {
  width: "100%",
  height: "100%",
  minWidth: 0,
  minHeight: 0,
  display: "grid",
  placeItems: "center",
  border: "1px solid var(--dsw-alias-border-l2)",
  borderRadius: 10,
  background: "var(--dsw-alias-bg-layer-2)",
  color: "var(--dsw-alias-label-primary)",
  font: "inherit",
  fontSize: 14,
  cursor: "pointer"
};
function adaptWorkspace(props) {
  const legacy = props.remote?.workspace ?? props.connection?.api?.workspace;
  if (legacy?.list !== void 0) return legacy;
  const source = props.workspaces?.list;
  if (source === void 0) return legacy;
  return {
    ...legacy,
    list: async () => {
      const snapshot2 = source.getSnapshot();
      return {
        items: (snapshot2.items ?? []).map((view) => ({
          ...typeof view.workspaceId === "string" ? { workspaceId: view.workspaceId } : {},
          ...typeof view.path === "string" ? { path: view.path } : {},
          sessionIds: Array.isArray(view.sessionIds) ? view.sessionIds.filter((id2) => typeof id2 === "string") : []
        })),
        archivedSessionIds: Array.isArray(snapshot2.archivedSessionIds) ? snapshot2.archivedSessionIds.filter((id2) => typeof id2 === "string") : []
      };
    }
  };
}
function EditorDock(props) {
  const snapshot2 = (0, import_react21.useSyncExternalStore)(subscribeEditor, editorSnapshot);
  (0, import_react21.useEffect)(() => {
    ensureEditorOpen();
  }, []);
  const sessions = props.remote?.session ?? props.connection?.api?.sessions;
  const workspace = adaptWorkspace(props);
  if (snapshot2.open) {
    return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)("div", { style: overlay, "data-directorx-editor": "open", children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
      Stage,
      {
        sessionId: props.sessionId,
        sessions,
        liveSessions: props.liveSessions,
        pending: props.pending,
        workspace,
        onClose: closeEditor,
        onAskDsh: async (text, sessionId) => {
          const prompt = sessions?.prompt;
          if (sessionId === void 0 || sessionId === "" || prompt === void 0) {
            throw new Error("\u8FD9\u4E2A\u5DE5\u4F5C\u533A\u8FD8\u6CA1\u6709 DSH \u4F1A\u8BDD\uFF0C\u65E0\u6CD5\u628A\u753B\u5E03\u4EA4\u7ED9 DSH");
          }
          const response = await prompt({ sessionId, mode: "queue", content: [{ type: "text", text }], requestId: mintSessionRequestId() });
          const parsed = rpcOk(response);
          if (!parsed.ok) throw new Error(parsed.message);
        }
      }
    ) });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)("button", { type: "button", className: "dx-hit", style: handle, onClick: () => {
    ensureEditorOpen();
  }, title: "\u6253\u5F00 DirectorX \u753B\u5E03", "aria-label": "\u6253\u5F00 DirectorX \u753B\u5E03", children: "\u6253\u5F00 DirectorX \u753B\u5E03" });
}

// src/client/index.ts
var name = "directorx-client";
var inject = [
  "slots",
  "connection",
  "layout",
  "sessions",
  "remote",
  "remote.settings",
  "remote.session",
  "remote.workspace",
  "remote.commands",
  "settingsScope",
  "uiSession",
  "workspaces"
];
function optionalService2(ctx, name2) {
  try {
    const rec = ctx;
    return rec[name2] ?? ctx.get(name2);
  } catch {
    return void 0;
  }
}
function adaptSettings(face, flavor) {
  const rec = face;
  if (rec === void 0 || typeof rec.describe !== "function") return void 0;
  if (flavor === "typert") {
    return {
      describe: async () => rec.describe(),
      mutate: async (request) => rec.mutate(request.ns, request.ops, request.expectedRevision)
    };
  }
  return rec;
}
function apply(ctx) {
  const layout = () => ctx.get("layout");
  registerDirectorxSlash(ctx);
  const uiSession = optionalService2(ctx, "uiSession");
  const pending = uiSession?.pendingInteractions;
  const settingsCard = (name2) => {
    const remote = optionalService2(ctx, "remote");
    const connection = ctx.get("connection");
    const binder = optionalService2(ctx, "settingsScope");
    const api = remote?.settings !== void 0 ? adaptSettings(remote.settings, "typert") : adaptSettings(connection?.api?.settings, "legacy");
    return ctx.slots.register({
      name: name2,
      id: "directorx",
      key: "directorx",
      order: 30,
      label: "DirectorX",
      inject: () => ({
        scope: binder?.bind?.({ namespace: "directorx" }),
        api
      })
    }, DirectorxSettingsSection);
  };
  ctx.slots.inject("settings.section", () => settingsCard("settings.section"));
  ctx.slots.inject("settings.plugin.item", () => settingsCard("settings.plugin.item"));
  ctx.slots.inject("tool.call.toolview", () => {
    const disposers = DIRECTORX_TOOLVIEW_KEYS.map(
      (key) => ctx.slots.register({ name: "tool.call.toolview", key }, DirectorxToolRow)
    );
    return () => {
      for (const dispose of disposers) dispose();
    };
  });
  ctx.slots.inject(
    "conversation.view",
    () => ctx.slots.register({
      name: "conversation.view",
      id: "directorx-canvas",
      order: 40,
      label: "DirectorX \u753B\u5E03",
      inject: () => ({
        connection: optionalService2(ctx, "connection"),
        liveSessions: optionalService2(ctx, "sessions"),
        remote: optionalService2(ctx, "remote"),
        workspaces: optionalService2(ctx, "workspaces"),
        pending
      })
    }, EditorDock)
  );
  if (typeof window !== "undefined" && window.__directorxEditor === void 0) {
    window.__directorxEditor = {
      open: (kind, path) => {
        if (kind === "image" || kind === "video") openEditor(kind, path ?? "");
        else openCanvas();
      },
      openDirectorStage,
      close: () => {
        closeEditor();
      },
      setTab: (tab) => {
        setEditorTab(tab);
      },
      snapshot: () => {
        const current = editorSnapshot();
        return { open: current.open, tab: current.tab, kind: current.kind, path: current.path };
      },
      layoutKind: () => String(typeof layout()),
      openDetailsNow: () => layout()?.openDetails(),
      toggleSidebarNow: () => layout()?.toggleSidebar()
    };
  }
}
/*! Bundled license information:

use-sync-external-store/cjs/use-sync-external-store-shim.development.js:
  (**
   * @license React
   * use-sync-external-store-shim.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js:
  (**
   * @license React
   * use-sync-external-store-shim/with-selector.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
return module.exports; } });
//# sourceMappingURL=client.js.map

;(function(){try{var el=document.createElement('style');el.setAttribute('data-directorx','1');el.textContent="/* node_modules/@xyflow/react/dist/style.css */\n.react-flow {\n  direction: ltr;\n  --xy-edge-stroke-default: #b1b1b7;\n  --xy-edge-stroke-width-default: 1;\n  --xy-edge-stroke-selected-default: #555;\n  --xy-connectionline-stroke-default: #b1b1b7;\n  --xy-connectionline-stroke-width-default: 1;\n  --xy-attribution-background-color-default: rgba(255, 255, 255, 0.5);\n  --xy-minimap-background-color-default: #fff;\n  --xy-minimap-mask-background-color-default: rgba(240, 240, 240, 0.6);\n  --xy-minimap-mask-stroke-color-default: transparent;\n  --xy-minimap-mask-stroke-width-default: 1;\n  --xy-minimap-node-background-color-default: #e2e2e2;\n  --xy-minimap-node-stroke-color-default: transparent;\n  --xy-minimap-node-stroke-width-default: 2;\n  --xy-background-color-default: transparent;\n  --xy-background-pattern-dots-color-default: #91919a;\n  --xy-background-pattern-lines-color-default: #eee;\n  --xy-background-pattern-cross-color-default: #e2e2e2;\n  background-color: var(--xy-background-color, var(--xy-background-color-default));\n  --xy-node-color-default: inherit;\n  --xy-node-border-default: 1px solid #1a192b;\n  --xy-node-background-color-default: #fff;\n  --xy-node-group-background-color-default: rgba(240, 240, 240, 0.25);\n  --xy-node-boxshadow-hover-default: 0 1px 4px 1px rgba(0, 0, 0, 0.08);\n  --xy-node-boxshadow-selected-default: 0 0 0 0.5px #1a192b;\n  --xy-node-border-radius-default: 3px;\n  --xy-handle-background-color-default: #1a192b;\n  --xy-handle-border-color-default: #fff;\n  --xy-selection-background-color-default: rgba(0, 89, 220, 0.08);\n  --xy-selection-border-default: 1px dotted rgba(0, 89, 220, 0.8);\n  --xy-controls-button-background-color-default: #fefefe;\n  --xy-controls-button-background-color-hover-default: #f4f4f4;\n  --xy-controls-button-color-default: inherit;\n  --xy-controls-button-color-hover-default: inherit;\n  --xy-controls-button-border-color-default: #eee;\n  --xy-controls-box-shadow-default: 0 0 2px 1px rgba(0, 0, 0, 0.08);\n  --xy-edge-label-background-color-default: #ffffff;\n  --xy-edge-label-color-default: inherit;\n  --xy-resize-background-color-default: #3367d9;\n}\n.react-flow.dark {\n  --xy-edge-stroke-default: #3e3e3e;\n  --xy-edge-stroke-width-default: 1;\n  --xy-edge-stroke-selected-default: #727272;\n  --xy-connectionline-stroke-default: #b1b1b7;\n  --xy-connectionline-stroke-width-default: 1;\n  --xy-attribution-background-color-default: rgba(150, 150, 150, 0.25);\n  --xy-minimap-background-color-default: #141414;\n  --xy-minimap-mask-background-color-default: rgba(60, 60, 60, 0.6);\n  --xy-minimap-mask-stroke-color-default: transparent;\n  --xy-minimap-mask-stroke-width-default: 1;\n  --xy-minimap-node-background-color-default: #2b2b2b;\n  --xy-minimap-node-stroke-color-default: transparent;\n  --xy-minimap-node-stroke-width-default: 2;\n  --xy-background-color-default: #141414;\n  --xy-background-pattern-dots-color-default: #555;\n  --xy-background-pattern-lines-color-default: #333;\n  --xy-background-pattern-cross-color-default: #333;\n  --xy-node-color-default: #f8f8f8;\n  --xy-node-border-default: 1px solid #3c3c3c;\n  --xy-node-background-color-default: #1e1e1e;\n  --xy-node-group-background-color-default: rgba(240, 240, 240, 0.25);\n  --xy-node-boxshadow-hover-default: 0 1px 4px 1px rgba(255, 255, 255, 0.08);\n  --xy-node-boxshadow-selected-default: 0 0 0 0.5px #999;\n  --xy-handle-background-color-default: #bebebe;\n  --xy-handle-border-color-default: #1e1e1e;\n  --xy-selection-background-color-default: rgba(200, 200, 220, 0.08);\n  --xy-selection-border-default: 1px dotted rgba(200, 200, 220, 0.8);\n  --xy-controls-button-background-color-default: #2b2b2b;\n  --xy-controls-button-background-color-hover-default: #3e3e3e;\n  --xy-controls-button-color-default: #f8f8f8;\n  --xy-controls-button-color-hover-default: #fff;\n  --xy-controls-button-border-color-default: #5b5b5b;\n  --xy-controls-box-shadow-default: 0 0 2px 1px rgba(0, 0, 0, 0.08);\n  --xy-edge-label-background-color-default: #141414;\n  --xy-edge-label-color-default: #f8f8f8;\n}\n.react-flow__background {\n  background-color: var(--xy-background-color-props, var(--xy-background-color, var(--xy-background-color-default)));\n  pointer-events: none;\n  z-index: -1;\n}\n.react-flow__container {\n  position: absolute;\n  width: 100%;\n  height: 100%;\n  top: 0;\n  left: 0;\n}\n.react-flow__pane {\n  z-index: 1;\n  touch-action: none;\n}\n.react-flow__pane.draggable {\n  cursor: grab;\n}\n.react-flow__pane.dragging {\n  cursor: grabbing;\n}\n.react-flow__pane.selection {\n  cursor: pointer;\n}\n.react-flow__viewport {\n  transform-origin: 0 0;\n  z-index: 2;\n  pointer-events: none;\n}\n.react-flow__renderer {\n  z-index: 4;\n}\n.react-flow__selection {\n  z-index: 6;\n}\n.react-flow__nodesselection-rect:focus,\n.react-flow__nodesselection-rect:focus-visible {\n  outline: none;\n}\n.react-flow__edge-path {\n  stroke: var(--xy-edge-stroke, var(--xy-edge-stroke-default));\n  stroke-width: var(--xy-edge-stroke-width, var(--xy-edge-stroke-width-default));\n  fill: none;\n}\n.react-flow__connection-path {\n  stroke: var(--xy-connectionline-stroke, var(--xy-connectionline-stroke-default));\n  stroke-width: var(--xy-connectionline-stroke-width, var(--xy-connectionline-stroke-width-default));\n  fill: none;\n}\n.react-flow .react-flow__edges {\n  position: absolute;\n}\n.react-flow .react-flow__edges svg {\n  overflow: visible;\n  position: absolute;\n  pointer-events: none;\n}\n.react-flow__edge {\n  pointer-events: visibleStroke;\n}\n.react-flow__edge.selectable {\n  cursor: pointer;\n}\n.react-flow__edge.animated path {\n  stroke-dasharray: 5;\n  animation: dashdraw 0.5s linear infinite;\n}\n.react-flow__edge.animated path.react-flow__edge-interaction {\n  stroke-dasharray: none;\n  animation: none;\n}\n.react-flow__edge.inactive {\n  pointer-events: none;\n}\n.react-flow__edge.selected,\n.react-flow__edge:focus,\n.react-flow__edge:focus-visible {\n  outline: none;\n}\n.react-flow__edge.selected .react-flow__edge-path,\n.react-flow__edge.selectable:focus .react-flow__edge-path,\n.react-flow__edge.selectable:focus-visible .react-flow__edge-path {\n  stroke: var(--xy-edge-stroke-selected, var(--xy-edge-stroke-selected-default));\n}\n.react-flow__edge-textwrapper {\n  pointer-events: all;\n}\n.react-flow__edge .react-flow__edge-text {\n  pointer-events: none;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  user-select: none;\n}\n.react-flow__arrowhead polyline {\n  stroke: var(--xy-edge-stroke, var(--xy-edge-stroke-default));\n}\n.react-flow__arrowhead polyline.arrowclosed {\n  fill: var(--xy-edge-stroke, var(--xy-edge-stroke-default));\n}\n.react-flow__connection {\n  pointer-events: none;\n}\n.react-flow__connection .animated {\n  stroke-dasharray: 5;\n  animation: dashdraw 0.5s linear infinite;\n}\nsvg.react-flow__connectionline {\n  z-index: 1001;\n  overflow: visible;\n  position: absolute;\n}\n.react-flow__nodes {\n  pointer-events: none;\n  transform-origin: 0 0;\n}\n.react-flow__node {\n  position: absolute;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  user-select: none;\n  pointer-events: all;\n  transform-origin: 0 0;\n  box-sizing: border-box;\n  cursor: default;\n}\n.react-flow__node.selectable {\n  cursor: pointer;\n}\n.react-flow__node.draggable {\n  cursor: grab;\n  pointer-events: all;\n}\n.react-flow__node.draggable.dragging {\n  cursor: grabbing;\n}\n.react-flow__nodesselection {\n  z-index: 3;\n  transform-origin: left top;\n  pointer-events: none;\n}\n.react-flow__nodesselection-rect {\n  position: absolute;\n  pointer-events: all;\n  cursor: grab;\n}\n.react-flow__handle {\n  position: absolute;\n  pointer-events: none;\n  min-width: 5px;\n  min-height: 5px;\n  width: 6px;\n  height: 6px;\n  background-color: var(--xy-handle-background-color, var(--xy-handle-background-color-default));\n  border: 1px solid var(--xy-handle-border-color, var(--xy-handle-border-color-default));\n  border-radius: 100%;\n}\n.react-flow__handle.connectingfrom {\n  pointer-events: all;\n}\n.react-flow__handle.connectionindicator {\n  pointer-events: all;\n  cursor: crosshair;\n}\n.react-flow__handle-bottom {\n  top: auto;\n  left: 50%;\n  bottom: 0;\n  transform: translate(-50%, 50%);\n}\n.react-flow__handle-top {\n  top: 0;\n  left: 50%;\n  transform: translate(-50%, -50%);\n}\n.react-flow__handle-left {\n  top: 50%;\n  left: 0;\n  transform: translate(-50%, -50%);\n}\n.react-flow__handle-right {\n  top: 50%;\n  right: 0;\n  transform: translate(50%, -50%);\n}\n.react-flow__edgeupdater {\n  cursor: move;\n  pointer-events: all;\n}\n.react-flow__pane.selection .react-flow__panel {\n  pointer-events: none;\n}\n.react-flow__panel {\n  position: absolute;\n  z-index: 5;\n  margin: 15px;\n}\n.react-flow__panel.top {\n  top: 0;\n}\n.react-flow__panel.bottom {\n  bottom: 0;\n}\n.react-flow__panel.top.center,\n.react-flow__panel.bottom.center {\n  left: 50%;\n  transform: translateX(-15px) translateX(-50%);\n}\n.react-flow__panel.left {\n  left: 0;\n}\n.react-flow__panel.right {\n  right: 0;\n}\n.react-flow__panel.left.center,\n.react-flow__panel.right.center {\n  top: 50%;\n  transform: translateY(-15px) translateY(-50%);\n}\n.react-flow__attribution {\n  font-size: 10px;\n  background: var(--xy-attribution-background-color, var(--xy-attribution-background-color-default));\n  padding: 2px 3px;\n  margin: 0;\n}\n.react-flow__attribution a {\n  text-decoration: none;\n  color: #999;\n}\n@keyframes dashdraw {\n  from {\n    stroke-dashoffset: 10;\n  }\n}\n.react-flow__edgelabel-renderer {\n  position: absolute;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  user-select: none;\n  left: 0;\n  top: 0;\n}\n.react-flow__viewport-portal {\n  position: absolute;\n  width: 100%;\n  height: 100%;\n  left: 0;\n  top: 0;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  user-select: none;\n}\n.react-flow__minimap {\n  background: var( --xy-minimap-background-color-props, var(--xy-minimap-background-color, var(--xy-minimap-background-color-default)) );\n}\n.react-flow__minimap-svg {\n  display: block;\n}\n.react-flow__minimap-mask {\n  fill: var( --xy-minimap-mask-background-color-props, var(--xy-minimap-mask-background-color, var(--xy-minimap-mask-background-color-default)) );\n  stroke: var( --xy-minimap-mask-stroke-color-props, var(--xy-minimap-mask-stroke-color, var(--xy-minimap-mask-stroke-color-default)) );\n  stroke-width: var( --xy-minimap-mask-stroke-width-props, var(--xy-minimap-mask-stroke-width, var(--xy-minimap-mask-stroke-width-default)) );\n}\n.react-flow__minimap-node {\n  fill: var( --xy-minimap-node-background-color-props, var(--xy-minimap-node-background-color, var(--xy-minimap-node-background-color-default)) );\n  stroke: var( --xy-minimap-node-stroke-color-props, var(--xy-minimap-node-stroke-color, var(--xy-minimap-node-stroke-color-default)) );\n  stroke-width: var( --xy-minimap-node-stroke-width-props, var(--xy-minimap-node-stroke-width, var(--xy-minimap-node-stroke-width-default)) );\n}\n.react-flow__background-pattern.dots {\n  fill: var( --xy-background-pattern-color-props, var(--xy-background-pattern-color, var(--xy-background-pattern-dots-color-default)) );\n}\n.react-flow__background-pattern.lines {\n  stroke: var( --xy-background-pattern-color-props, var(--xy-background-pattern-color, var(--xy-background-pattern-lines-color-default)) );\n}\n.react-flow__background-pattern.cross {\n  stroke: var( --xy-background-pattern-color-props, var(--xy-background-pattern-color, var(--xy-background-pattern-cross-color-default)) );\n}\n.react-flow__controls {\n  display: flex;\n  flex-direction: column;\n  box-shadow: var(--xy-controls-box-shadow, var(--xy-controls-box-shadow-default));\n}\n.react-flow__controls.horizontal {\n  flex-direction: row;\n}\n.react-flow__controls-button {\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  height: 26px;\n  width: 26px;\n  padding: 4px;\n  border: none;\n  background: var(--xy-controls-button-background-color, var(--xy-controls-button-background-color-default));\n  border-bottom: 1px solid var( --xy-controls-button-border-color-props, var(--xy-controls-button-border-color, var(--xy-controls-button-border-color-default)) );\n  color: var( --xy-controls-button-color-props, var(--xy-controls-button-color, var(--xy-controls-button-color-default)) );\n  cursor: pointer;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  user-select: none;\n}\n.react-flow__controls-button svg {\n  width: 100%;\n  max-width: 12px;\n  max-height: 12px;\n  fill: currentColor;\n}\n.react-flow__edge.updating .react-flow__edge-path {\n  stroke: #777;\n}\n.react-flow__edge-text {\n  font-size: 10px;\n}\n.react-flow__node.selectable:focus,\n.react-flow__node.selectable:focus-visible {\n  outline: none;\n}\n.react-flow__node-input,\n.react-flow__node-default,\n.react-flow__node-output,\n.react-flow__node-group {\n  padding: 10px;\n  border-radius: var(--xy-node-border-radius, var(--xy-node-border-radius-default));\n  width: 150px;\n  font-size: 12px;\n  color: var(--xy-node-color, var(--xy-node-color-default));\n  text-align: center;\n  border: var(--xy-node-border, var(--xy-node-border-default));\n  background-color: var(--xy-node-background-color, var(--xy-node-background-color-default));\n}\n.react-flow__node-input.selectable:hover,\n.react-flow__node-default.selectable:hover,\n.react-flow__node-output.selectable:hover,\n.react-flow__node-group.selectable:hover {\n  box-shadow: var(--xy-node-boxshadow-hover, var(--xy-node-boxshadow-hover-default));\n}\n.react-flow__node-input.selectable.selected,\n.react-flow__node-input.selectable:focus,\n.react-flow__node-input.selectable:focus-visible,\n.react-flow__node-default.selectable.selected,\n.react-flow__node-default.selectable:focus,\n.react-flow__node-default.selectable:focus-visible,\n.react-flow__node-output.selectable.selected,\n.react-flow__node-output.selectable:focus,\n.react-flow__node-output.selectable:focus-visible,\n.react-flow__node-group.selectable.selected,\n.react-flow__node-group.selectable:focus,\n.react-flow__node-group.selectable:focus-visible {\n  box-shadow: var(--xy-node-boxshadow-selected, var(--xy-node-boxshadow-selected-default));\n}\n.react-flow__node-group {\n  background-color: var(--xy-node-group-background-color, var(--xy-node-group-background-color-default));\n}\n.react-flow__nodesselection-rect,\n.react-flow__selection {\n  background: var(--xy-selection-background-color, var(--xy-selection-background-color-default));\n  border: var(--xy-selection-border, var(--xy-selection-border-default));\n}\n.react-flow__nodesselection-rect:focus,\n.react-flow__nodesselection-rect:focus-visible,\n.react-flow__selection:focus,\n.react-flow__selection:focus-visible {\n  outline: none;\n}\n.react-flow__controls-button:hover {\n  background: var( --xy-controls-button-background-color-hover-props, var(--xy-controls-button-background-color-hover, var(--xy-controls-button-background-color-hover-default)) );\n  color: var( --xy-controls-button-color-hover-props, var(--xy-controls-button-color-hover, var(--xy-controls-button-color-hover-default)) );\n}\n.react-flow__controls-button:disabled {\n  pointer-events: none;\n}\n.react-flow__controls-button:disabled svg {\n  fill-opacity: 0.4;\n}\n.react-flow__controls-button:last-child {\n  border-bottom: none;\n}\n.react-flow__controls.horizontal .react-flow__controls-button {\n  border-bottom: none;\n  border-right: 1px solid var( --xy-controls-button-border-color-props, var(--xy-controls-button-border-color, var(--xy-controls-button-border-color-default)) );\n}\n.react-flow__controls.horizontal .react-flow__controls-button:last-child {\n  border-right: none;\n}\n.react-flow__resize-control {\n  position: absolute;\n}\n.react-flow__resize-control.left,\n.react-flow__resize-control.right {\n  cursor: ew-resize;\n}\n.react-flow__resize-control.top,\n.react-flow__resize-control.bottom {\n  cursor: ns-resize;\n}\n.react-flow__resize-control.top.left,\n.react-flow__resize-control.bottom.right {\n  cursor: nwse-resize;\n}\n.react-flow__resize-control.bottom.left,\n.react-flow__resize-control.top.right {\n  cursor: nesw-resize;\n}\n.react-flow__resize-control.handle {\n  width: 5px;\n  height: 5px;\n  border: 1px solid #fff;\n  border-radius: 1px;\n  background-color: var(--xy-resize-background-color, var(--xy-resize-background-color-default));\n  translate: -50% -50%;\n}\n.react-flow__resize-control.handle.left {\n  left: 0;\n  top: 50%;\n}\n.react-flow__resize-control.handle.right {\n  left: 100%;\n  top: 50%;\n}\n.react-flow__resize-control.handle.top {\n  left: 50%;\n  top: 0;\n}\n.react-flow__resize-control.handle.bottom {\n  left: 50%;\n  top: 100%;\n}\n.react-flow__resize-control.handle.top.left {\n  left: 0;\n}\n.react-flow__resize-control.handle.bottom.left {\n  left: 0;\n}\n.react-flow__resize-control.handle.top.right {\n  left: 100%;\n}\n.react-flow__resize-control.handle.bottom.right {\n  left: 100%;\n}\n.react-flow__resize-control.line {\n  border-color: var(--xy-resize-background-color, var(--xy-resize-background-color-default));\n  border-width: 0;\n  border-style: solid;\n}\n.react-flow__resize-control.line.left,\n.react-flow__resize-control.line.right {\n  width: 1px;\n  transform: translate(-50%, 0);\n  top: 0;\n  height: 100%;\n}\n.react-flow__resize-control.line.left {\n  left: 0;\n  border-left-width: 1px;\n}\n.react-flow__resize-control.line.right {\n  left: 100%;\n  border-right-width: 1px;\n}\n.react-flow__resize-control.line.top,\n.react-flow__resize-control.line.bottom {\n  height: 1px;\n  transform: translate(0, -50%);\n  left: 0;\n  width: 100%;\n}\n.react-flow__resize-control.line.top {\n  top: 0;\n  border-top-width: 1px;\n}\n.react-flow__resize-control.line.bottom {\n  border-bottom-width: 1px;\n  top: 100%;\n}\n.react-flow__edge-textbg {\n  fill: var(--xy-edge-label-background-color, var(--xy-edge-label-background-color-default));\n}\n.react-flow__edge-text {\n  fill: var(--xy-edge-label-color, var(--xy-edge-label-color-default));\n}\n/*# sourceMappingURL=client.css.map */\n";document.head.appendChild(el);}catch(e){}})();